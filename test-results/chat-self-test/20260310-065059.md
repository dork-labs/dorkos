# Chat Self-Test — 2026-03-10 06:51

## Test Config
- **URL:** http://localhost:4241/?dir=/Users/doriancollier/Keep/temp/empty
- **Focus areas:** CWD behavior — verifying new sessions use correct working directory
- **API Port:** 4242
- **Relay:** Enabled
- **Pulse:** Enabled
- **Models:** default, opus[1m], sonnet, sonnet[1m], haiku
- **Runtime:** claude-code (all capabilities enabled)
- **Server workingDirectory:** /Users/doriancollier/Keep/dork-os/core/apps/server
- **Started:** 2026-03-10 06:50:59
- **Status:** COMPLETE

---

## Phase 2 — Session Creation

- New session created via "+ New session" button
- URL session ID: `5dd3e7ee-3513-4224-b508-cdd37a49c57f`
- JSONL path: `~/.claude/projects/-Users-doriancollier-Keep-dork-os-core-apps/40d87370-800b-4b9b-aac3-6521f0ccce96.jsonl`
- **BUG: JSONL project slug is `-Users-doriancollier-Keep-dork-os-core-apps` instead of `-Users-doriancollier-Keep-temp-empty`**

## Phase 3 — Configuration

- Model selector and permission mode selector were opened and selections made (Haiku, Accept Edits)
- **Observation:** Settings did not appear to take effect — status bar showed "Opus" and "Default" after message was sent
- This is a secondary issue; the primary concern is CWD

## Phase 4 — Messages

### Message 1: `What is your current working directory? Please run pwd using the Bash tool and tell me the result.`
- **Streaming duration:** ~10s (no freeze)
- **Console errors:** None
- **Agent response:** "My current working directory is `/Users/doriancollier/Keep/dork-os/core/apps`"
- **Expected CWD:** `/Users/doriancollier/Keep/temp/empty` (from `?dir=` URL param)
- **Actual CWD:** `/Users/doriancollier/Keep/dork-os/core/apps`
- **JSONL confirms:** Tool result shows `/Users/doriancollier/Keep/dork-os/core/apps`
- **Verdict:** **BUG — CWD from URL `?dir=` parameter is lost in Relay path**

---

## Summary

**1 critical bug found.** New sessions created through the web UI with `?dir=` parameter do not use the specified working directory when Relay is enabled. The agent runs in the server's default CWD instead.

## Issues Found

### CWD Lost in Relay Message Path — Bug (Critical)

**Observed:** New session created with URL `?dir=/Users/doriancollier/Keep/temp/empty` runs the agent in `/Users/doriancollier/Keep/dork-os/core/apps` instead. JSONL file is created under the wrong project slug.

**Expected:** The agent should run `pwd` and report `/Users/doriancollier/Keep/temp/empty`.

**Root cause:** `packages/relay/src/adapters/claude-code-adapter.ts:372-441`

The server route (`apps/server/src/routes/sessions.ts:175`) correctly includes `cwd` in the relay message payload:
```typescript
{ content, cwd, correlationId }
```

But `ClaudeCodeAdapter.handleAgentMessage()` never extracts `cwd` from the payload. On line 372, it only reads from Mesh agent context:
```typescript
const agentCwd = context?.agent?.directory;
```

And on lines 380-384 and 439-441, it only passes `agentCwd` (which is `undefined` for web client sessions without Mesh context):
```typescript
this.deps.agentManager.ensureSession(ccaSessionKey, {
  ...(agentCwd ? { cwd: agentCwd } : {}),
});
const eventStream = this.deps.agentManager.sendMessage(ccaSessionKey, prompt, {
  ...(agentCwd ? { cwd: agentCwd } : {}),
});
```

Without a CWD, the session falls back to the server's default working directory.

**Contrast with Pulse path:** `handlePulseMessage()` (line 595-596) correctly extracts CWD from the payload:
```typescript
const { scheduleId, runId, prompt, cwd, permissionMode } = payload;
const effectiveCwd = cwd ?? context?.agent?.directory ?? this.config.defaultCwd;
```

**Recommendation:** Extract `cwd` from `payloadObj` in `handleAgentMessage()` (it's already parsed at line 402-405), following the same precedence pattern as `handlePulseMessage`:
```typescript
const payloadCwd = payloadObj?.cwd as string | undefined;
const agentCwd = context?.agent?.directory;
const effectiveCwd = payloadCwd ?? agentCwd;
```
Then pass `effectiveCwd` to both `ensureSession()` and `sendMessage()`.

**ADR context:** None directly applicable.

### Model/Permission Mode Not Persisting on New Session — Observation

**Observed:** Model was set to Haiku and permission to Accept Edits before sending the first message, but the status bar showed Opus/Default after the response.

**Possible cause:** The first message triggers session creation via Relay. The adapter hardcodes `permissionMode: 'default'` (line 381) and the model selection may be lost similarly. This is a secondary issue but worth investigating — the `permissionMode` should also come from the payload or session config.

## Observations (No Issues)
- Session sidebar correctly lists the new session as "Just now"
- SSE streaming completed without freezes
- Tool call card (Bash pwd) rendered correctly with expand/collapse
- JSONL transcript is consistent with what was displayed in the UI

---

**Completed:** 2026-03-10 06:55
**Duration:** ~5 minutes
**Focus areas:** CWD behavior
