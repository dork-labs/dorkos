# Chat Self-Test — 2026-03-10 07:47

## Test Config
- **URL:** http://localhost:4241/?dir=/Users/doriancollier/Keep/temp/empty
- **Expected dir:** /Users/doriancollier/Keep/temp/empty
- **Focus areas:** CWD passthrough fix (fix-relay-cwd-passthrough)
- **Started:** 2026-03-10 07:47:30
- **Status:** COMPLETE

---

## Preflight
- **API port:** 4242
- **Relay enabled:** true
- **Pulse enabled:** true
- **Models available:** default, opus[1m], sonnet, sonnet[1m], haiku
- **Default runtime:** claude-code
- **Runtime capabilities:** supportsPermissionModes=true, supportsToolApproval=true, supportsQuestionPrompt=true, supportsCostTracking=true
- **Baseline console errors:** none

## Phase 2 — Session
- **URL session ID (Agent-ID):** a948c128-c838-4c7c-abda-48162a389258
- **JSONL path:** ~/.claude/projects/-Users-doriancollier-Keep-temp-empty/cc761511-b326-4873-a5bb-a1a34fe5b015.jsonl
- **JSONL project slug:** -Users-doriancollier-Keep-temp-empty ✅ (matches expected dir)

## Phase 3 — Configuration
- **Model:** Sonnet (default)
- **Permission mode:** Default

## Phase 4 — Messages

### Message 1: `What is your current working directory? Run the pwd command to confirm.`
- **Streaming duration:** ~12s
- **Console warnings:** 0 new
- **Network status:** 202
- **Response:** "The current working directory is /Users/doriancollier/Keep/temp/empty." ✅
- **JSONL project:** -Users-doriancollier-Keep-temp-empty (correct) ✅
- **Observations:** CWD fix working correctly

---

## Summary

The `?dir=` URL parameter CWD passthrough is now working end-to-end with Relay enabled. The first test (before the additional fixes) showed the wrong CWD (`/Users/doriancollier/Keep/dork-os/core/apps`). After fixing the client-side relay path to include `cwd` in the POST body, the agent correctly reports `/Users/doriancollier/Keep/temp/empty`.

## Issues Found

### CWD missing from relay POST body — Bug (FIXED)
**Observed:** New sessions always started in `/Users/doriancollier/Keep/dork-os/core/apps` regardless of `?dir=` URL param.
**Root cause:** `use-chat-session.ts:400-403` called `transport.sendMessageRelay()` without passing `cwd`. The `Transport` interface and all implementations also lacked `cwd` in the options type. The server-side fix in `claude-code-adapter.ts` was correct but had no data to work with.
**Fix applied:**
1. `packages/shared/src/transport.ts` — added `cwd?: string` to `sendMessageRelay` options
2. `apps/client/src/layers/shared/lib/transport/relay-methods.ts` — included `cwd` in POST body JSON
3. `apps/client/src/layers/shared/lib/transport/http-transport.ts` — updated declared type
4. `apps/client/src/layers/shared/lib/direct-transport.ts` — updated options type
5. `apps/client/src/layers/features/chat/model/use-chat-session.ts` — passed `selectedCwdRef.current` in relay call

## Observations (No Issues)
- SSE streaming worked correctly, no freezes
- Session sidebar shows correct dir (`~/Keep/temp/empty`)
- JSONL created in correct project directory after fix
- Relay 30/30 tests still passing after all changes

---

**Completed:** 2026-03-10 07:57
**Status:** COMPLETE
**Focus areas:** CWD passthrough (fix-relay-cwd-passthrough)
