/**
 * Re-exports the DiscoveryStrategy interface from types.ts.
 *
 * This module exists for backward compatibility. Prefer importing
 * directly from `./types.js`.
 *
 * @module mesh/discovery-strategy
 */
export type { DiscoveryStrategy } from './types.js';
