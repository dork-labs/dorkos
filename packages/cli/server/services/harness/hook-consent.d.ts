// Type-only mirror of a module the CLI imports through the DIST layout.
// See ../core/config-manager.d.ts for the full explanation and the guard test
// that keeps this tree in step with scripts/build.ts.
export * from '../../../../../apps/server/src/services/harness/hook-consent.js';
