/**
 * Shared model — Zustand stores, React context, and global UI hooks.
 *
 * @module shared/model
 */
export { useAppStore, type ViewMode, type Theme } from './app-store';
export { useTheme } from './use-theme';
