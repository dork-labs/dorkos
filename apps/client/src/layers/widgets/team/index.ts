/**
 * Team widget — the `/team` roster page.
 *
 * Not routed yet: the route swap and the table/topology views land with the
 * page that replaces `/agents` (spec §W2.1). This is the surface it will show.
 *
 * @module widgets/team
 */
export { TeamPage } from './ui/TeamPage';
export type { TeamPageProps } from './ui/TeamPage';
