/**
 * Approvals indicator widget — the header marker that tells a person an agent is
 * waiting on them, wherever in the cockpit they happen to be, and lets them
 * answer without leaving the page.
 *
 * Mounted once by the app shell, next to the right-panel toggle.
 *
 * @module widgets/approvals-indicator
 */
export { ApprovalsIndicator } from './ui/ApprovalsIndicator';
