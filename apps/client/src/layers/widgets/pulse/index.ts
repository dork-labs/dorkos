/**
 * Pulse widget — the always-present global spine tab of the right inspector
 * panel. A stack of capped teasers (attention, activity) assembled from existing
 * dashboard models, promoted into the panel's no-selection state so the shell is
 * never a dead panel.
 *
 * @module widgets/pulse
 */
export { PulsePanel } from './ui/PulsePanel';
