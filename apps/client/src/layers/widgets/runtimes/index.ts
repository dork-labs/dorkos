/**
 * Runtimes widget — the dedicated `/runtimes` discovery + connect surface
 * (effortless-runtime-switching T2 task 3.4).
 *
 * @module widgets/runtimes
 */
export { RuntimesPage } from './ui/RuntimesPage';
