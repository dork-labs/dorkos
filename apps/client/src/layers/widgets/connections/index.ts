/**
 * Connections widget — the /connections page composing the connections
 * feature's provider setup, service grid, and accounts list.
 *
 * @module widgets/connections
 */
export { ConnectionsPage } from './ui/ConnectionsPage';
