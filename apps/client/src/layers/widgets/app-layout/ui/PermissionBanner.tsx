/** Placeholder banner for permission mode warnings (currently hidden). */
export function PermissionBanner({ sessionId: _sessionId }: { sessionId: string | null }) {
  // Hidden for now — may re-enable in the future
  return null;
}
