/**
 * Slash commands — the dropdown that opens under the composer when a message
 * starts with `/`, fuzzy-matched as you type.
 *
 * Not the ⌘K palette: that is `features/command-palette`, a dialog for
 * switching agents and reaching features. This slice draws a list inside the
 * composer and nothing else.
 *
 * @module features/slash-commands
 */
export { SlashCommandList } from './ui/SlashCommandList';
