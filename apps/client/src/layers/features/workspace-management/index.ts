/**
 * Workspace management feature — pin/unpin and dirty-safe remove actions (DOR-84).
 *
 * @module features/workspace-management
 */
export { WorkspaceActions } from './ui/WorkspaceActions';
export { usePinWorkspace, useRemoveWorkspace } from './model/use-workspace-actions';
