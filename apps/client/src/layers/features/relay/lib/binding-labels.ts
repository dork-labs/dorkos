/** Human-readable labels for session strategy values, shared by BindingList and AdapterBindingRow. */
export const STRATEGY_BADGE_LABELS: Record<string, string> = {
  'per-chat': 'Per Chat',
  'per-user': 'Per User',
  stateless: 'Stateless',
};
