import { PageHeader } from './PageHeader';

/** Marketplace sources route header — page title and command palette trigger. */
export function MarketplaceSourcesHeader() {
  return <PageHeader title="Marketplace Sources" />;
}
