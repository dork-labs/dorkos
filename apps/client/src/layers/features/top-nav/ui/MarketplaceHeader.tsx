import { PageHeader } from './PageHeader';

/** Marketplace route header — page title and command palette trigger. */
export function MarketplaceHeader() {
  return <PageHeader title="Marketplace" />;
}
