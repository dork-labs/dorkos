import { PageHeader } from './PageHeader';

/** Connections route header — page title and command palette trigger. */
export function ConnectionsHeader() {
  return <PageHeader title="Connections" />;
}
