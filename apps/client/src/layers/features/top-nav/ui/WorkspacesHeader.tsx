import { PageHeader } from './PageHeader';

/** Workspaces route header — page title and command palette trigger. */
export function WorkspacesHeader() {
  return <PageHeader title="Workspaces" />;
}
