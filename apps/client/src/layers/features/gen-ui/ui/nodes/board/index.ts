/**
 * Board widget node — the interactive game-board primitive, split into an
 * orchestrator (grid + win line + thinking state), a cell, the self-drawing
 * mark, and the victory stroke.
 *
 * @module features/gen-ui/ui/nodes/board
 */
export { BoardNode } from './BoardNode';
