/**
 * Mood widget node — the animated face, split into a container (entrance +
 * idle body motion), per-emotion faces, shared parts, and signature details.
 *
 * @module features/gen-ui/ui/nodes/mood
 */
export { MoodNode } from './MoodNode';
