{
  "version": 1,
  "tests": {
    "agent-reactions": {
      "relatedCode": [
        "apps/server/src/routes/rooms.ts",
        "apps/server/src/routes/room-caller.ts",
        "apps/server/src/services/rooms/reactions/reaction-budget.ts",
        "apps/server/src/services/core/agent-identity/agent-token-env.ts",
        "apps/client/src/layers/features/entry-actions/ui/EntryReactionRow.tsx",
        "apps/client/src/layers/entities/room/lib/reactions.ts"
      ],
      "explorationNotes": [
        "A FABRICATED X-DorkOS-Agent token does not fail \u2014 resolveAgentIdentityFromHeaders never rejects, so it falls through to the operator and the reaction lands under the PERSON's author id. A test that invented one would be green while proving the opposite of its name.",
        "Real tokens are only ever issued into a spawned session's process env (agent-token-env.ts), so the test-mode leg grew POST /api/test/agent-token { agentPath, displayName } -> { token }, which mints through the production path. Pass agent.projectPath (the canonicalised path), not agent.path.",
        "Assert the SERVER's authorIds on the entry before asserting anything visual: every DOM assertion here passes just as well when the identity fell through.",
        "The cockpit draws an agent's reaction identically to a person's by design \u2014 no data-agent, no tint. What distinguishes it is the pill's aria-label, '<name> reacted <emoji>', resolved off the room roster ('Someone who left' when it cannot be placed). data-mine is stamped only on the reader's own.",
        "Send { on: true }, never a bare toggle: POST is not idempotent and a retried request would undo itself.",
        "No turn is driven here, so the agent stays SILENT \u2014 a reaction is precisely the thing that costs no turn."
      ]
    },
    "app-loads": {
      "relatedCode": ["apps/client/src/App.tsx"]
    },
    "ask-anywhere": {
      "relatedCode": [
        "apps/client/src/layers/features/ask/ui/InteractionAsk.tsx",
        "apps/client/src/layers/features/ask/ui/AskList.tsx",
        "apps/client/src/layers/entities/attention/model/use-pending-interactions.ts",
        "apps/client/src/layers/widgets/inbox-bell/ui/InboxBell.tsx",
        "apps/server/src/routes/sessions.ts",
        "apps/server/src/services/session/session-list-broadcaster.ts"
      ],
      "explorationNotes": "The room leg is deliberately absent: a prompt on a ROOM lane needs a room, an agent bound into it and a turn dispatched by the room runner, and this leg seeds none of those (its fixture is one agent directory and a chat session). The filter is unit-tested in RoomLiveLane.test.tsx, the roomId join is server-tested in session-list-broadcaster-asks, and the whole path was walked against a real room for the phase acceptance check."
    },
    "boot-stability": {
      "relatedCode": [
        "apps/client/src/layers/shared/lib/query-persister.ts",
        "apps/client/src/layers/features/dashboard-sidebar/model/boot/use-boot-state.ts",
        "apps/client/src/layers/features/dashboard-sidebar/ui/SidebarZones.tsx"
      ]
    },
    "compaction": {
      "relatedCode": [
        "apps/client/src/layers/features/chat/ui/message/CompactBoundaryRow.tsx",
        "apps/client/src/layers/features/chat/ui/message/UserMessageContent.tsx",
        "apps/client/src/layers/features/chat/model/stream/project-session-turn.ts",
        "apps/client/src/layers/entities/session/model/stream/session-stream-store.ts",
        "apps/server/src/services/session/event-log-history.ts",
        "apps/server/src/services/runtimes/test-mode/scenario-store.ts",
        "apps/server/src/services/runtimes/test-mode/test-mode-runtime.ts",
        "apps/server/src/routes/session-command-intent-handler.ts"
      ],
      "explorationNotes": [
        "Capability L-04 (meta/chat-capabilities.md). Registered from chat-mock.spec.ts, never its own spec: the test-mode server is shared mutable state and POST /api/test/reset runs before each of that file's tests.",
        "A successful compaction draws TWO rows in sequence, from different sources: the live [data-testid=compact-boundary-row] projected from the turn event, then the durable [data-testid=compaction-row] rebuilt from history, which REPLACES it at turn_end. Never assert twice on the live row - the second assertion races the handover and reports the trigger badge as 'element(s) not found', which looks like missing metadata and is not.",
        "The live row is only observable deterministically on the 'compacting-hold' scenario, which holds the turn open until POST /api/test/finish-turn. /compact (the command intent) cannot be held, so that path pins the durable row only.",
        "Driving /compact takes TWO Enters: the first selects the palette row and fills the composer, the second submits. The suite asserts the resulting POST /command-intents/compact returns 202 rather than inferring dispatch from a row appearing later.",
        "The scenario answers 'Echo: <prompt>', so a prompt lookup needs { exact: true } or it matches the echo too and trips non-retriable strict mode.",
        "Stability-check with --repeat-each=3 --workers=1. Without --workers=1 the repeats spread across workers and race each other's resets on the shared server (measured 2/6 red, 0/6 red single-worker)."
      ]
    },
    "direct-messages": {
      "relatedCode": [
        "apps/client/src/layers/features/room-management/ui/NewDirectMessageMenu.tsx",
        "apps/client/src/layers/features/dashboard-sidebar/ui/useSectionChrome.tsx",
        "apps/server/src/services/rooms/room-service.ts"
      ]
    },
    "held-process": {
      "relatedCode": [
        "apps/server/src/services/runtimes/test-mode/held-process.ts",
        "apps/server/src/services/runtimes/test-mode/held-process-scenarios.ts",
        "apps/server/src/routes/test-control.ts",
        "apps/server/src/services/session/message-dispatcher.ts",
        "apps/client/src/layers/features/composer/ui/DispositionMenu.tsx",
        "apps/client/src/layers/features/chat/ui/message/StagedContextNote.tsx"
      ],
      "explorationNotes": [
        "Put ONE session on the held path (POST /api/test/persistent {sessionId, enabled}), never a process-wide switch: this server is shared by four concurrent Playwright projects, the same reason POST /api/test/step is session-keyed.",
        "Warmth has no product API \u2014 it is invisible to the person by design \u2014 so GET /api/test/persistent?sessionId= is the only way a browser can see it. It reads getSessionWarmth/canSteerSession/canStageSession off the runtime, not the bookkeeping behind them.",
        "ChatPage.input does NOT match the composer mid-turn: the field renames itself 'Compose next \u2014 will send when ready'. Every case here types into a running turn, so the module matches both names (see GOTCHAS).",
        "The warm-echo scenario names the path in its own answer (HELD-PROCESS-TURN-N vs NO-HELD-PROCESS) and labels staged words by route (staged-native: vs staged-folded:), so a case that quietly ran on the other path fails instead of passing.",
        "warm-echo parks on a step barrier, so every turn needs one releaseStep \u2014 which polls, because a step fired before the scenario reaches the barrier is dropped and the turn then waits forever.",
        "RED-for-the-right-reason: restoring pre-DOR-1326 semantics (nothing held, steer and stage accepted for every session) failed 7 of the 8 cases, including the Steer row OFFERED on a session that cannot cut in (DOR-1268) and Add context taking the native path so the fold never happened (DOR-1307). The eighth \u2014 the resume-path L-11 case \u2014 passed, correctly.",
        "Stability: 8/8 twice at --workers=1 (44.7s, 1.2m); the whole chromium-mock project 62/62 in 5.1m."
      ]
    },
    "interactive-prompts": {
      "relatedCode": [
        "apps/server/src/services/runtimes/test-mode/interactive-scenarios.ts",
        "apps/server/src/services/runtimes/test-mode/interaction-gate.ts",
        "apps/server/src/services/session/event-log-history.ts",
        "apps/client/src/layers/features/ask/ui/ApprovalPrompt.tsx",
        "apps/client/src/layers/features/ask/ui/AskReceipt.tsx",
        "apps/client/src/layers/features/ask/ui/AskStack.tsx",
        "apps/client/src/layers/features/ask/ui/QuestionPrompt.tsx",
        "apps/client/src/layers/features/ask/ui/QuestionAnswerSummary.tsx",
        "apps/client/src/layers/features/ask/ui/ElicitationPrompt.tsx",
        "apps/client/src/layers/features/chat/model/stream/project-session-turn.ts"
      ],
      "explorationNotes": [
        "Registered INTO chat-mock.spec.ts (not a *.spec.ts): the test-mode leg is shared mutable state and POST /api/test/reset wipes sessions globally, so a parked turn on a concurrent worker gets torn down mid-click. The .ts extension also keeps it off the cockpit leg, where every send would be a billable claude-code turn.",
        "Scenario is bound PER SESSION (POST /api/test/scenario with a sessionId), never as the server default: several scenarios park on a step barrier only their own test releases, and four Playwright projects share this leg \u2014 as the default, one would hang a neighbour's turn.",
        "Bind AFTER the app settles on a session id (poll the ?session= URL param), not before. Navigating straight to a self-minted id raced the route loader: it re-minted after the send, dropping the optimistic message and leaving the transcript at 'Start a conversation' with the scenario bound to a dead id.",
        "Buttons carry their keyboard hint in the accessible name ('Approve Enter', 'Deny Esc'), and the hint only renders while the card is active. Anchor with /^Approve\\b/ \u2014 exact never matches, and a loose /approve/i also hits the batch bar's 'Approve All'. Always Allow is the exception: it carries an explicit aria-label naming the grant ('Always Allow, this session' / ', this project' / ', all your Claude sessions', or plain 'Always Allow' when the runtime named no scope, DOR-1462), with no keyboard hint and a visible middot where the comma is.",
        "Assert the DURABLE approval-receipt (data-outcome allowed/denied), not the transient tool-approval-decided or question-prompt-submitted rows: those are replaced when the turn ends and the message is rebuilt from history.",
        "Every assertion names a branch the scenario only reaches on that answer (APPROVED-BRANCH / DENIED-BRANCH / ANSWER-RECEIVED: <choice> / ELICITATION-ACCEPTED: <value>), so 'the card went away' can never pass for 'the answer was delivered'.",
        "Stability: --repeat-each=3 --workers=1, 36/36. WITHOUT --workers=1 repeats land on parallel workers and race the shared /api/test/reset \u2014 that alone fails long-standing untouched tests 10/18.",
        "The I-05 reload tests only discriminate because the question and approval scenarios close the tool's content block BEFORE the answer (a resultless tool_result marked complete), which is what claude-code does. Drop those two emissions and both reload tests go green against the DOR-1269 bug they exist to catch.",
        "I-07 reloads AFTER turn_end, and that is what separates it from I-05: past the turn the page is served from reconstructed history (event-log-history.ts) rather than a replayed live turn, so it covers the half where the green 'Question answered' actually lived. Verified red by dropping the questionOutcome fold in event-log-history \u2014 the honest row never renders.",
        "Stability after I-07 (2026-08-17): --repeat-each=3 --workers=1 on I-07 alone, 3/3; the whole -g 'I-0' suite (11 tests, I-01 through I-07) once at --workers=1, 11/11. Not re-run at --repeat-each=3 across the whole suite, so the 36/36 figure above still refers to the pre-I-07 set.",
        "'Nobody answers' is unreachable by clicking, so the question-expires scenario emits interaction_cancelled/timeout on a step barrier: the exact event the ten-minute handler fires, through the same normalizer (\u2192 interaction_resolved/expired) and the same projector kind backfill. No clock to wait out."
      ]
    },
    "lane-no-shift": {
      "relatedCode": [
        "apps/client/src/layers/features/conversation/ui/LiveLane.tsx",
        "apps/client/src/layers/widgets/room-view/ui/RoomSurface.tsx"
      ]
    },
    "live-turn-visibility": {
      "relatedCode": [
        "apps/server/src/services/runtimes/test-mode/interactive-scenarios.ts",
        "apps/server/src/services/runtimes/test-mode/interaction-gate.ts",
        "apps/client/src/layers/features/composer/ui/InputActionButton.tsx",
        "apps/client/src/layers/features/chat/ui/message/SubagentBlock.tsx",
        "apps/client/src/layers/features/chat/ui/tasks/TaskProgressHeader.tsx",
        "apps/client/src/layers/features/chat/ui/tasks/BackgroundTaskBar.tsx"
      ],
      "explorationNotes": [
        "These rows are all about a MOMENT, and every pre-existing scenario closes that window in the same pass that opens it. Scenarios here park on POST /api/test/step barriers, so the running window is held open for as long as the assertions need on any machine.",
        "releaseStep POLLS the step route: it answers released:false until the scenario has actually reached the barrier, and firing a step blind drops it, leaving the turn waiting on a barrier nobody releases again.",
        "The Stop case uses a scenario with NO self-ending path, so 'the turn stopped' is a claim about Stop rather than about elapsed time; its text past the barrier is unreachable unless the turn kept running.",
        "A turn BLOCKED on an approval offers no Stop at all \u2014 the card replaces the composer. The runtime handles the interrupt correctly (unit-pinned); the UI has no affordance, so the spec pins that instead. See GOTCHAS.",
        "Subagent blocks are DROPPED on settle, not collapsed \u2014 assert toHaveCount(0); data-status='complete' exists only briefly before the history rebuild. And a subagent's description is on screen twice while running (block + task bar), so scope getByText to the block.",
        "The todos fixture must number tasks '1','2','3': the client's reducer discards the id on a create and assigns its own counter, so updates with any other id are silently dropped and the count never moves.",
        "Negative assertions use toHaveCount(0), not not.toContainText: an interrupted turn can leave no history, the transcript unmounts for the empty state, and not.toContainText then errors on a missing element.",
        "Stability: --repeat-each=3 --workers=1, 36/36 across both modules; full chromium-mock project 45/45."
      ]
    },
    "peek": {
      "relatedCode": [
        "apps/client/src/layers/features/conversation/ui/LivePeek.tsx",
        "apps/client/src/layers/widgets/room-view/ui/RoomLiveLane.tsx"
      ]
    },
    "room-autonomy": {
      "relatedCode": [
        "apps/server/src/services/rooms/room-collect.ts",
        "apps/server/src/services/rooms/room-trigger.ts",
        "apps/server/src/services/rooms/notices/notice-copy.ts",
        "apps/client/src/layers/widgets/one-bar/ui/RoomRunState.tsx",
        "apps/client/src/layers/widgets/room-view/ui/RoomLiveLane.tsx",
        "apps/client/src/layers/features/conversation/ui/LaneContent.tsx",
        "apps/client/src/layers/entities/room/model/use-halt-room.ts",
        "apps/client/src/layers/entities/room/model/use-room-presence.ts",
        "apps/client/src/layers/features/conversation/ui/rows/NoticeRow.tsx"
      ],
      "explorationNotes": [
        "TEST-MODE LEG ONLY. Every test un-silences a room agent, which the roomsApi fixture otherwise guarantees never happens; on the cockpit leg that is a real billable claude-code turn per test. Project chromium-rooms-agents, and the cockpit project names this file in testIgnore.",
        "The collect window is seeded per test via PATCH /api/config { rooms: { collectDebounceMs } } \u2014 read live per burst by room-collect.ts, so no restart. Restored to the 500ms schema default, which is exact because this leg's DORK_HOME is deleted before every boot.",
        "room-timeline is NOT mounted until something has been said in the room. Wait on roomsPage.roomHeader after navigating to a freshly seeded room; four tests failed on the feed barrier before that.",
        "long-turn + POST /api/test/finish-turn is the only lever for a turn that is still running. finish-turn is STICKY until POST /api/test/reset, so every test here resets first \u2014 without it the halt test silently gets an instant turn and 'room-header-halt' never renders, which reads as a broken button.",
        "The file is mode:'serial'. fullyParallel schedules individual TESTS onto workers, and these share two pieces of server-global state (the collect window and the scenario). --repeat-each must be run with --workers=1, which is what CI runs.",
        "The burst's observable is the reply TEXT: the turn prompt is the newest entry verbatim, and the simple-text scenario echoes it. arrivedDuringPrevTurn is model-facing only \u2014 no entry field, no DOM node \u2014 so the mid-turn test asserts a COUNT (two messages, two answers) and never a text match; long-turn echoes nothing.",
        "Halt on this leg proves the room's half only: TestModeRuntime.interruptQuery answers false, so this is deliberately the stubborn-runtime path. The halted notice copy carries the count ('2 agents were working'), which is how a browser can tell 'stopped everything' from 'stopped one'.",
        "The cross-room wait test seeds TWO channels sharing one agent, because the ceiling it is about is the agent's working directory rather than the room. Its observable is the room-held line, which is the only half a browser can add: the FIFO ordering across three rooms and the promote route need a deterministic clock and are pinned in room-hold-elsewhere.test.ts."
      ]
    },
    "room-conversation": {
      "relatedCode": [
        "apps/client/src/layers/widgets/room-view/ui/ChannelsPage.tsx",
        "apps/client/src/layers/widgets/room-view/ui/ChannelComposer.tsx",
        "apps/client/src/layers/features/conversation/ui/ComposerHost.tsx",
        "apps/client/src/layers/features/conversation/model/use-timeline-scroll.ts",
        "apps/client/src/layers/entities/room/model/use-room-stream.ts",
        "apps/client/src/layers/shared/model/use-document-title.ts"
      ]
    },
    "room-entry-actions": {
      "relatedCode": [
        "apps/client/src/layers/widgets/room-view/ui/RoomThreadPanel.tsx",
        "apps/client/src/layers/features/conversation/ui/rows/ThreadReplyRow.tsx",
        "apps/client/src/layers/widgets/room-view/model/use-thread-url-sync.ts",
        "apps/client/src/layers/widgets/room-view/model/use-thread-arrivals.ts",
        "apps/client/src/layers/widgets/room-view/lib/room-timeline.ts",
        "apps/client/src/layers/features/entry-actions/ui/EntryActionBar.tsx"
      ],
      "explorationNotes": [
        "Capabilities M-05 and M-06 (meta/chat-capabilities.md) live here - threads have been covered since 2026-07-30/31, which the 2026-08-14 capability audit missed.",
        "Thread state lives in a zustand store (use-room-open-thread), not the URL; use-thread-url-sync mirrors it to ?thread= with replace:true, so Back leaves the room rather than walking every thread glanced at.",
        "A deep link opens the thread to READ: the panel section (tabIndex -1) takes focus and the composer does not. That is load-bearing, not manners - Escape only reaches the panel's handler from inside it, so a linked-to panel that took no focus could be opened from the address bar and never closed by keyboard.",
        "Replies are kept out of the main timeline by one choke point, groupByThread in room-timeline.ts; assert the timeline's room-entry count is unchanged plus a single room-thread-replies row, which is what the panel costs the room.",
        "Under 768px the panel is a full-screen PUSH, not a sheet - the room column unmounts, so assert the room is gone rather than covered.",
        "A tap scrolls its target into view first and Chromium CENTRES a row that is not fully visible, so clicking a reply row above the window MOVES the room by about four messages - record a scroll position only after the row you are about to tap is on screen, and read it with settledTopVisibleEntryText() (DOR-1364).",
        "DOR-1412 - the '31 failures in 69 runs' this file was famous for is a LIFETIME TALLY, not a flake rate. manifest.json's failCount counts every INVOCATION in which any test in the file was red, so it holds every TDD red the file was in while somebody was writing it. Read back off the tracked manifest's own history (git log --all -- apps/e2e/manifest.json, then git show <commit>:apps/e2e/manifest.json): 17 of the 31 landed on #641, the branch that added the thread tests, and 5 more on a spec-editing branch on 2026-08-11. failCount stopped moving that day; the last 12 recorded runs, spanning the P4 timeline rewrite, the one-bar header and the right-panel move, were all green.",
        "DOR-1412 before/after, measured 2026-09-04. BEFORE, this file alone at --workers=1 on isolated ports, 20 repeats (380 attempts, machine load average 26-190): 2 red - one the suite-wide HMR error-boundary crash, one this file's own phone resume test. Driven deliberately, one test 40 times while a loop rewrote packages/shared/dist: 8 of 40 red, 7 showing that boundary. AFTER (HMR off on the e2e Vite legs, plus the wheel-search fix below): the same 40 under the same churn 0 of 40; 20 repeats of the whole file 380 of 380; 5 repeats of the whole file under continuous churn 95 of 95; 3 repeats beside the rest of tests/rooms at 7 workers 57 of 57. Zero hot updates in any of them. This file catches the environment's flakes more often than its siblings for one reason: it is the biggest, 19 tests and 20 page loads against per-page-load hazards.",
        "The phone resume test's wheel loop could not recover from an overshoot, and that was its own failure (DOR-1412). It only ever wheeled BACK, but the virtualizer is measuring rows while the reader crosses them and every row shorter than its 80px estimate pulls the content above it up - so the reply row can leap past the tap band between two notches, after which the loop rides to the oldest message and polls a row now below the window until it times out. The screenshot is the top of the history. RoomsPage.wheelReplyRowIntoTapRange reads the row's SIDE and corrects in either direction, resolving 'the virtualizer drew no row at all' by the ends of the history."
      ]
    },
    "room-identity": {
      "relatedCode": [
        "apps/client/src/layers/entities/room/ui/RoomTitle.tsx",
        "apps/client/src/layers/entities/room/ui/RoomAvatar.tsx",
        "apps/client/src/layers/entities/room/lib/room-display.ts",
        "apps/client/src/layers/features/dashboard-sidebar/ui/rooms/RoomRow.tsx"
      ]
    },
    "send-message": {
      "relatedCode": [
        "apps/client/src/layers/widgets/session/ui/ChatPanel.tsx",
        "apps/client/src/layers/features/conversation/ui/Timeline.tsx",
        "apps/client/src/layers/widgets/session/ui/SessionMessage.tsx"
      ]
    },
    "session-management": {
      "relatedCode": [
        "apps/client/src/layers/features/session-list/ui/EmbedSidebar.tsx",
        "apps/client/src/layers/entities/session/ui/SessionRow.tsx"
      ]
    },
    "session-switcher": {
      "relatedCode": [
        "apps/client/src/layers/features/dashboard-sidebar/ui/SessionSwitcher.tsx",
        "apps/client/src/layers/features/dashboard-sidebar/ui/AgentListItem.tsx",
        "apps/client/src/layers/features/dashboard-sidebar/model/use-live-sessions.ts"
      ]
    },
    "settings-dialog": {
      "relatedCode": ["apps/client/src/layers/features/settings/ui/SettingsDialog.tsx"]
    },
    "settings-tab-panels": {
      "relatedCode": ["apps/client/src/layers/shared/ui/navigation-layout.tsx"]
    },
    "sidebar-model-showcase": {
      "relatedCode": [
        "apps/client/src/dev/showcases/SidebarModelShowcases.tsx",
        "apps/client/src/layers/features/dashboard-sidebar/model/build-sidebar-model.ts"
      ]
    },
    "team-page": {
      "relatedCode": [
        "apps/client/src/router.tsx",
        "apps/client/src/layers/widgets/team/ui/TeamRoute.tsx",
        "apps/client/src/layers/widgets/team/ui/TeamPage.tsx",
        "apps/client/src/layers/features/team-roster/ui/TeamRosterToolbar.tsx",
        "apps/client/src/layers/widgets/one-bar/ui/TeamHeader.tsx"
      ]
    }
  }
}
