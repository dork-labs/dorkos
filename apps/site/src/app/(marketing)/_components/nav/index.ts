/**
 * The home page's own floating pill.
 *
 * A fork of the shared `MarketingNav`, kept route-private because it does a
 * different job — see {@link HomeNav} for the divergence and what is copied
 * verbatim. The shared component is untouched, and so is every other page
 * that renders it.
 *
 * @module app/(marketing)/_components/nav
 */

export { HomeNav } from './HomeNav';
