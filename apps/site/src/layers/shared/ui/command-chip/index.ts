/**
 * @module shared/ui/command-chip
 *
 * Copyable terminal-command chip. A reusable leaf component in the shared
 * layer so every install surface — the homepage install moment (feature), the
 * `/install` page and blog release footers (app) — renders commands the same
 * way instead of hand-rolling `<pre>` blocks.
 */
export { CommandChip } from './CommandChip';
