---
title: Read a ui:// MCP App resource
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Reads a `ui://` MCP App resource (SEP-1865) for client rendering. The
          server opens its own short-lived MCP client using connection config it
          captured internally — the stdio/http config never travels to the
          client. Enforces the `ui://` scheme, `text/html` mime, and that the
          server belongs to the session's MCP set.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Reads a `ui://` MCP App resource (SEP-1865) for client rendering. The server opens its own short-lived MCP client using connection config it captured internally — the stdio/http config never travels to the client. Enforces the `ui://` scheme, `text/html` mime, and that the server belongs to the session's MCP set.

<APIPage document={"../../docs/api/openapi.json"} operations={[{"path":"/api/sessions/{id}/mcp-app/resource","method":"post"}]} />