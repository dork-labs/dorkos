# Harness Smoke — Codex — 2026-09-09 07:36

## Run

- **Harness:** Codex (`codex`, engine id `codex`)
- **Status:** FREE — no model was reached, so this is a partial answer by design
- **Started:** 2026-09-09T07:36:03.110Z
- **Instrument:** none — `--free` reaches no model, so nothing was armed and nothing was billed
- **Isolation:** `HOME` and the harness's own config home both point at an empty sandbox, so no stored sign-in and no user-scope skill can reach this answer
- **Model pinned:** `gpt-5.6-luna` — The cheapest LISTED model in `codex debug models` on codex-cli 0.145.0 — its own catalog describes it as "Fast and affordable agentic coding model", against "Latest frontier" (sol) and "Balanced … everyday work" (terra). The catalog carries no prices, so this is a reading of the vendor's own descriptions rather than a measurement; `gpt-5.4-mini` calls itself "cost-efficient" and is cheaper still, but its visibility is `hide`, which is the catalog saying not to pick it. Override with `--model <slug>`.
- **Ceiling:** 0.5 USD, inert — the flag is on the command line below because the argv is the same one a paid run uses, and no API request is made for it to bound
- **Cost:** nothing. No API request was made.
- **Listing oracle:** `codex debug prompt-input` — non-model, so it costs nothing
- **Skill-injection proof:** file reads NOT denied — the skill verdict corroborates, it does not prove
- **Verdicts:** 4 pass, 0 fail, 0 finding, 5 unknown

## What this run did NOT answer

- the two hook-activation oracles, the skill-activation oracle and the sentinel — every one of them needs a session, and this harness has no free turn (its listing probe is already non-model, and nothing else about Codex is free)

Reported rather than omitted: a shorter list of verdicts reads as a clean run.

## Verdicts

- **PASS** `listing-1` — SK-01
  - Does Codex list `x` from `.agents/skills/x/SKILL.md`?
  - Listed 1× — the authored skill is native to Codex where it sits.
- **PASS** `listing-2` — SK-06, SK-09
  - Does Codex list `x` from `.agents/skills/pkg__x/SKILL.md`?
  - Listed 1× — the `pkg__x` directory loads on Codex under its frontmatter name — and together with the entry above it, two skills whose frontmatter agrees both appear rather than one collapsing into the other (SK-06).
- **PASS** `listing-3` — SK-01
  - Does Codex list `probe` from `.agents/skills/probe/SKILL.md`?
  - Listed 1× — the probe skill is loadable at all.
- **UNKNOWN** `activation-hook-1` — needs a session; this run started none
  - Does the hook Codex gets — PROJECTED — translated through the vendored event map into the generated `.codex/hooks.json`, in the `{ description, hooks }` shape Codex documents — actually fire?
  - NOT RUN. A hook fires when a session starts, and this run started none — Codex has no free turn (its listing probe is already non-model, and nothing else about Codex is free). Run it with an instrument to answer it.
- **UNKNOWN** `activation-hook-2` — needs a session; this run started none
  - Does the hook Codex gets — PROJECTED — the installed package’s hook, folded into the same generated file — actually fire?
  - NOT RUN. A hook fires when a session starts, and this run started none — Codex has no free turn (its listing probe is already non-model, and nothing else about Codex is free). Run it with an instrument to answer it.
- **UNKNOWN** `activation-skill` — needs a model turn; `--free` reaches no model
  - Did the probe skill's instruction reach the model under Codex?
  - NOT RUN. This is the one oracle that needs a model to answer, and a `--free` run reaches none. Run it with an instrument to answer it: DORKOS_HARNESS_SMOKE=1 OPENAI_API_KEY=<key>.
- **UNKNOWN** `sentinel` — needs a model to answer; none did
  - Did Codex's answer reproduce the instructions sentinel?
  - NOT RUN. There was no answer to look in: this run never reached a model. A free Codex run gets as far as the harness's own bookkeeping — a listing, and on Claude Code a started session and its hooks — and stops there, so the corroborating half has nothing to corroborate with.
- **UNKNOWN** `credential` — AGENTS.md’s four-money-paths table; `plans/harness-sync-test-plan.md` §8
  - Did Codex read OPENAI_API_KEY rather than falling back to a stored sign-in?
  - Codex does not report which credential served the turn, so this run's only defense is the gate: OPENAI_API_KEY was read from the environment and its config home was pointed at an empty sandbox. Nothing read a stored sign-in.
- **PASS** `calibration` — SK-13, SK-14
  - Does `harnessCoverage('codex')` agree with what Codex listed?
  - 3 skills discovered by the walk, all of them listed, and every fixture entry the listing carried was discovered.

## Calibration against `harnessCoverage()`

No disagreement between the compiled vendor facts and the binary on this tree.

## The tree that was asked about

- `claude-code symlink skill .claude/skills/probe`
- `claude-code symlink skill .claude/skills/x`
- `claude-code scaffold instruction .claude/CLAUDE.md`
- `claude-code symlink skill .claude/skills/pkg__x`
- `claude-code generate command .claude/commands/pkg/x.md`
- `claude-code generate command .claude/commands/pkg/.gitignore`
- `codex generate hook .codex/hooks.json`
- `codex symlink skill .agents/skills/pkg__x`
- `claude-code merge hook .claude/settings.local.json`
