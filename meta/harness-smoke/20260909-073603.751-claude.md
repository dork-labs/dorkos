# Harness Smoke — Claude Code — 2026-09-09 07:36

## Run

- **Harness:** Claude Code (`claude`, engine id `claude-code`)
- **Status:** FREE — no model was reached, so this is a partial answer by design
- **Started:** 2026-09-09T07:36:03.751Z
- **Instrument:** none — `--free` reaches no model, so nothing was armed and nothing was billed
- **Isolation:** `HOME` and the harness's own config home both point at an empty sandbox, so no stored sign-in and no user-scope skill can reach this answer
- **Model pinned:** `claude-haiku-4-5-20251001` — Anthropic's cheapest current model, and the one the DorkOS eval harness's recorded SDK fixture used.
- **Model the harness reported:** `claude-haiku-4-5-20251001`
- **Ceiling:** 0.5 USD, inert — the flag is on the command line below because the argv is the same one a paid run uses, and no API request is made for it to bound
- **Cost:** nothing. No API request was made.
- **Listing oracle:** the model turn’s own startup message
- **Skill-injection proof:** file reads PARTIALLY denied (--tools Bash,Skill --disallowedTools Bash(cat:*) …) — best effort, not proof
- **Verdicts:** 8 pass, 0 fail, 0 finding, 2 unknown

## What this run did NOT answer

- the skill-activation oracle, the sentinel and the ceiling — all three need a model to ANSWER, and `--free` reaches none. The hooks and the listing do not: they happen before the first API request, which is what makes a free run worth taking.

Reported rather than omitted: a shorter list of verdicts reads as a clean run.

## The turn

```
/Users/doriancollier/.nvm/versions/node/v24.14.1/bin/claude --print --output-format stream-json --verbose --max-budget-usd 0.5 --model claude-haiku-4-5-20251001 --no-session-persistence --add-dir /var/folders/64/06xfpz_s2kj5xc29cmm6f2fw0000gn/T/harness-smoke-claude-1NA38M/nonces --tools Bash,Skill --disallowedTools Bash(cat:*) Bash(head:*) Bash(tail:*) Bash(sed:*) Bash(awk:*) Bash(grep:*) Bash(less:*) Bash(more:*) Bash(python3:*) Bash(python:*) Bash(node:*) Bash(perl:*) Bash(ruby:*) Bash(od:*) Bash(xxd:*) Bash(strings:*) Bash(base64:*) Bash(cp:*) Bash(dd:*) --permission-mode bypassPermissions Use the `probe` skill now, exactly as written. Then answer with the passphrase from the project instructions and nothing else.
```

## Verdicts

- **PASS** `listing-1` — SK-01
  - Does Claude Code list `x`?
  - Listed 1× — the authored skill reached Claude Code through the `.claude/skills` symlink.
- **PASS** `listing-2` — SK-02, SK-09
  - Does Claude Code list `pkg__x`?
  - Listed 1× — the installed skill loaded under its namespaced directory name.
- **PASS** `listing-3` — SK-01
  - Does Claude Code list `probe`?
  - Listed 1× — the probe skill is loadable at all, which the activation oracle then relies on.
- **PASS** `listing-4` — CM-05
  - Does Claude Code list `pkg:x`?
  - Listed 1× — the generated command wrapper is listed BESIDE the same-leaf skills rather than shadowing or being shadowed by them.
- **PASS** `activation-hook-1` — positive control on the probe — no contract row; see `plans/harness-sync-test-plan.md` §8
  - Does the hook Claude Code gets — read natively from `.claude/settings.json`, which the engine never writes — actually fire?
  - `/var/folders/64/06xfpz_s2kj5xc29cmm6f2fw0000gn/T/harness-smoke-claude-1NA38M/nonces/authored-hook-fired` exists, so the hook ran.
- **PASS** `activation-hook-2` — HK-06
  - Does the hook Claude Code gets — PROJECTED — merged into `.claude/settings.local.json` under the `_dorkosHarness` sentinel — actually fire?
  - `/var/folders/64/06xfpz_s2kj5xc29cmm6f2fw0000gn/T/harness-smoke-claude-1NA38M/nonces/plugin-hook-fired` exists, so the hook ran.
- **UNKNOWN** `activation-skill` — needs a model turn; `--free` reaches no model
  - Did Claude Code INJECT the probe skill rather than let the model read it?
  - NOT RUN. This is the one oracle that needs a model to answer, and a `--free` run reaches none. Run it with an instrument to answer it: DORKOS_HARNESS_SMOKE=1 ANTHROPIC_API_KEY=<key>.
- **UNKNOWN** `sentinel` — needs a model to answer; none did
  - Did Claude Code's answer reproduce the instructions sentinel?
  - NOT RUN. There was no answer to look in: this run never reached a model. A free Claude Code run gets as far as the harness's own bookkeeping — a listing, and on Claude Code a started session and its hooks — and stops there, so the corroborating half has nothing to corroborate with.
- **PASS** `credential` — AGENTS.md’s four-money-paths table; `plans/harness-sync-test-plan.md` §8
  - Did Claude Code read ANTHROPIC_API_KEY rather than falling back to a stored sign-in?
  - Claude Code reported `ANTHROPIC_API_KEY`. In a free run that variable holds a placeholder, not a key — so what this proves is the thing worth proving: with `HOME` and the config home pointed at an empty sandbox, the binary read the environment variable and did NOT fall back to a stored sign-in.
- **PASS** `calibration` — SK-13, SK-14
  - Does `harnessCoverage('claude-code')` agree with what Claude Code listed?
  - 3 skills discovered by the walk, all of them listed.

## Calibration against `harnessCoverage()`

No disagreement between the compiled vendor facts and the binary on this tree.

## The tree that was asked about

- `claude-code symlink skill .claude/skills/probe`
- `claude-code symlink skill .claude/skills/x`
- `claude-code scaffold instruction .claude/CLAUDE.md`
- `claude-code symlink skill .claude/skills/pkg__x`
- `claude-code generate command .claude/commands/pkg/x.md`
- `claude-code generate command .claude/commands/pkg/.gitignore`
- `claude-code merge hook .claude/settings.local.json`
- `codex symlink skill .agents/skills/pkg__x`
