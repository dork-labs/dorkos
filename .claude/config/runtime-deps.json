{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Maps runtime dependencies to their codebase context for /app:runtime-upgrade. One entry per runtime SDK: where the integration lives, what confines it, where changelogs come from, which SDK exports we touch, and package-specific upgrade judgment (upgrade_notes).",
  "packages": {
    "@anthropic-ai/claude-agent-sdk": {
      "short_name": "claude-agent-sdk",
      "github_repo": "anthropics/claude-agent-sdk-typescript",
      "codebase_root": "apps/server/src/services/runtimes/claude-code/",
      "abstraction_boundary": "AgentRuntime interface",
      "related_adrs": ["0089", "0143", "0239", "0240"],
      "upgrade_notes": [
        "SDK 0.2.113+ ships a native binary and no longer bundles cli.js: CLI path resolution (sdk-utils.ts) and onboarding assumptions are version-sensitive; verify resolveClaudeCliPath behavior on any bump crossing that line.",
        "The pin lives in SEVEN places and they must all move in one commit: root package.json pnpm.overrides (the only one that actually binds while an override exists), apps/server, apps/desktop and packages/cli dependencies, the two apps/desktop optionalDependencies (-darwin-arm64, -win32-x64), and CLAUDE_SDK_VERSION in services/runtimes/claude-code/tooling/provision.ts. scripts/__tests__/dependabot-lockstep-families.test.ts guards the manifest half; provision.ts's own test guards the seventh.",
        "Never cite a line number in sdk.d.ts. It grew 7429 -> 8978 lines in the 0.3.224 -> 0.3.268 range alone, and every line citation in the codebase pointed at unrelated text afterwards. Cite the symbol (SDKUserMessage.shouldQuery), and in the minified bundle cite the string literal, never the symbol.",
        "An in-session tool's input schema must contain no z.record() and no z.json() at any depth. SDK 0.3.257+ converts tool schemas through the installed zod's own processor with a context it builds itself, and zod 4.5.3+ record handling (zod #6497) dereferences a `deferred` field that context lacks: one offending schema throws inside tools/list and the model is handed ZERO DorkOS tools, silently. UPSTREAM: anthropics/claude-agent-sdk-typescript#454, OPEN as of 2026-09-11 with two reporters. The workaround is the z.object({}).catchall(...) swap documented in mcp-tools/tool-exposure.ts, across four schemas. ON THE NEXT BUMP: check whether #454 is fixed, and if it is, revert the catchall swap back to z.record()/z.json() \u2014 it is a workaround, not a preference, and it costs the JSON Schema its propertyNames bounds plus two measured value-level differences (see connector-schemas.ts)."
      ],
      "changelog_sources": [
        {
          "type": "npm_registry",
          "priority": 1,
          "url": "https://registry.npmjs.org/@anthropic-ai/claude-agent-sdk",
          "note": "Authoritative version list and publish timestamps via .time field"
        },
        {
          "type": "github_releases",
          "priority": 2,
          "url": "https://api.github.com/repos/anthropics/claude-agent-sdk-typescript/releases",
          "note": "Primary source for per-version release notes (87+ releases, some gaps)"
        },
        {
          "type": "changelog_md",
          "priority": 3,
          "url": "https://raw.githubusercontent.com/anthropics/claude-agent-sdk-typescript/main/CHANGELOG.md",
          "note": "Most complete single doc (100+ versions); use for gap fill when GitHub releases missing"
        }
      ],
      "sdk_surface_map": {
        "query()": "claude-code-runtime.ts, messaging/message-sender.ts, messaging/runtime-cache.ts, sessions/pump-launch.ts",
        "SDKMessage": "14 files: media-capture.ts, messaging/{message-sender,phantom-cancellation,turn-liveness}.ts, sdk/sdk-event-mapper.ts, sdk/event-mappers/{message,result,stream,system}-event-mapper.ts, sessions/{persistent-dispatch,pump-turn-stream,session-pump-contract,session-pump,session-turn-windows}.ts",
        "Query": "9 files: agent-types.ts, messaging/{launch-probes,runtime-cache}.ts, sdk/{context-usage,subscription-usage}.ts, sessions/{persistent-dispatch,pump-launch,session-pump-contract,session-store}.ts",
        "Options": "messaging/launch-resolver.ts, sessions/launch-fingerprint.ts, sessions/pump-launch.ts",
        "McpServerConfig": "claude-code-runtime.ts, mcp-server-config.ts, messaging/message-sender-shared.ts, sessions/launch-fingerprint.ts",
        "McpServerStatus": "messaging/launch-probes.ts, messaging/message-sender-shared.ts",
        "PermissionUpdate": "messaging/{always-allow-scope,interaction-wait,interactive-handlers}.ts, sessions/session-store.ts",
        "PermissionUpdateDestination": "messaging/always-allow-scope.ts, sessions/session-store.ts",
        "PermissionMode": "sessions/launch-fingerprint.ts",
        "PermissionResult": "messaging/interactive-handlers.ts",
        "ElicitationRequest, ElicitationResult": "messaging/interaction-wait.ts, messaging/interactive-handlers.ts",
        "EffortLevel, ThinkingConfig": "messaging/thinking-config.ts",
        "ModelInfo": "messaging/runtime-cache.ts",
        "SdkPluginConfig": "sessions/launch-fingerprint.ts",
        "SpawnOptions, SpawnedProcess": "sessions/tracked-spawn.ts",
        "SDKControlGetContextUsageResponse": "sdk/context-usage.ts",
        "SDKControlGetUsageResponse": "sdk/subscription-usage.ts",
        "getSessionInfo()": "sessions/transcript-reader.ts",
        "renameSession()": "claude-code-runtime.ts",
        "forkSession()": "sessions/session-store.ts",
        "tool()": "mcp-tools/*.ts (13 files \u2014 the largest SDK surface in the codebase)",
        "createSdkMcpServer()": "mcp-tools/index.ts",
        "$comment": "25 distinct symbols across 55 non-test files, re-derived by parsing every `import \u2026 from '@anthropic-ai/claude-agent-sdk'` on the 0.3.268 bump (2026-09-11). Paths are relative to codebase_root."
      },
      "non_import_couplings": [
        "sessions/project-slug.ts hand-mirrors the SDK's session-slug hash algorithm (incl. the 200-char cap) with no import \u2014 grep/ESLint/surface-map all miss it; re-diff the sdk.mjs hash body on every bump, by STRING not by minified symbol (every symbol it has ever cited was renamed by the next release). Unchanged at 0.3.268.",
        "messaging/plugin-activation.ts hand-mirrors the Options plugin shape without importing it. The two DIVERGED at 0.3.268: SdkPluginConfig gained an optional skipMcpDiscovery the mirror deliberately does not carry. Still assignable, so nothing breaks \u2014 but re-read both shapes on every bump.",
        "sdk/sdk-utils.ts resolveClaudeCliPath() resolves the SDK's platform optional-dep packages by name (no SDK import); version-sensitive since the 0.2.113 native-binary split. Eight published variants at 0.3.268, unchanged, and none of them -android.",
        "services/mesh/mcp-revocation.ts (outside the adapter dir) reasons about SDK-internal MCP connect timing with a fixture-pinned test that passes even when stale \u2014 re-observe on every bump. Re-derived statically AND re-run live at 0.3.268 (second fixture committed beside the first).",
        "sdk/sdk-error-mapping.ts reasons about the CLI's own abort predicate \u2014 its cause collapse and its suppression set \u2014 read out of the shipped binary and quoted in research/20260903_claude-cli-aborted-refusal-shapes.md, which carries the recipe. No import, no grep hit, no failing test. Re-run at 0.3.268: the suppression set GREW to three members and a new `turn-abort` cause collapses into `interrupt`."
      ]
    },
    "@openai/codex-sdk": {
      "short_name": "codex-sdk",
      "github_repo": "openai/codex",
      "codebase_root": "apps/server/src/services/runtimes/codex/",
      "abstraction_boundary": "AgentRuntime interface",
      "related_adrs": ["0255", "0309", "0310"],
      "upgrade_notes": [
        "Versions track the Codex CLI release train and an active `alpha` dist-tag exists alongside `latest`; a fix may ship in alpha weeks before stable (the 0.143.0 logs_2.sqlite fix did). Check dist-tags when resolving the target, and never pin an alpha without an explicit user decision.",
        "Follow the pinned-SDK bump checklist in contributing/adding-a-runtime.md (section: Bumping a pinned SDK): dist-tag check, .d.ts diff of the ThreadEvent/ThreadItem unions and imported item types, event-mapper exhaustiveness compile, runtime conformance suite, one live smoke turn.",
        "The event mapper's `never` exhaustiveness checks fail compilation when the SDK adds union members; that is the intended upgrade tripwire, not an obstacle to suppress."
      ],
      "changelog_sources": [
        {
          "type": "npm_registry",
          "priority": 1,
          "url": "https://registry.npmjs.org/@openai/codex-sdk",
          "note": "Authoritative version list, publish timestamps (.time), and the latest/alpha dist-tags"
        },
        {
          "type": "github_releases",
          "priority": 2,
          "url": "https://api.github.com/repos/openai/codex/releases",
          "note": "Release tags are prefixed `rust-v` (e.g. rust-v0.142.5): strip the prefix when matching npm versions. Releases cover the whole Codex CLI, not just the SDK; filter notes for SDK/TypeScript relevance."
        },
        {
          "type": "changelog_md",
          "priority": 3,
          "url": "https://raw.githubusercontent.com/openai/codex/main/CHANGELOG.md",
          "note": "CLI-wide changelog; gap fill only"
        }
      ],
      "sdk_surface_map": {
        "Codex, CodexOptions": "codex-runtime.ts, mcp-server-config.ts",
        "ThreadEvent, ThreadItem (+ AgentMessageItem, CommandExecutionItem, FileChangeItem, McpToolCallItem, ReasoningItem, TodoListItem, WebSearchItem)": "event-mapper.ts",
        "ThreadOptions, SandboxMode, ModelReasoningEffort": "turn-input.ts"
      }
    },
    "@opencode-ai/sdk": {
      "short_name": "opencode-sdk",
      "github_repo": "sst/opencode",
      "codebase_root": "apps/server/src/services/runtimes/opencode/",
      "abstraction_boundary": "AgentRuntime interface",
      "related_adrs": ["0255", "0308", "0310"],
      "upgrade_notes": [
        "The SDK is only the HTTP client. The `opencode serve` sidecar binary ships as a SEPARATE package (`opencode-ai`, resolved via runtimes.opencode.binaryPath or PATH) and version-couples with the client: assess every SDK bump against the sidecar versions users actually run, and state the supported binary range in the upgrade spec.",
        "npm dist-tags include dozens of `0.0.0-snapshot-*` channels; `latest` is the only meaningful channel: never resolve a target from any other tag.",
        "Server-driven contract: most behavior lives in the sidecar, so release notes about the server can matter more than client-visible API changes even when no imported type changed."
      ],
      "changelog_sources": [
        {
          "type": "npm_registry",
          "priority": 1,
          "url": "https://registry.npmjs.org/@opencode-ai/sdk",
          "note": "Authoritative version list and publish timestamps; ignore every dist-tag except latest"
        },
        {
          "type": "github_releases",
          "priority": 2,
          "url": "https://api.github.com/repos/sst/opencode/releases",
          "note": "The only release-notes source (the repo ships no CHANGELOG.md). Monorepo covers TUI/desktop/server/SDK; filter for server + SDK relevance."
        }
      ],
      "sdk_surface_map": {
        "createOpencodeClient": "server-manager.ts",
        "OpencodeClient": "server-manager.ts, opencode-runtime.ts, mcp-manager.ts, mcp-status.ts",
        "Event, AssistantMessage, Permission, SessionStatus, Todo, ToolPart": "event-mapper.ts (ToolPart also session-mapper.ts)",
        "GlobalEvent": "global-event-hub.ts",
        "Session, Message, Part": "session-mapper.ts",
        "ProviderListResponse": "models.ts, opencode-runtime.ts",
        "McpStatus, McpLocalConfig, McpRemoteConfig": "mcp-status.ts, mcp-server-config.ts"
      }
    }
  }
}
