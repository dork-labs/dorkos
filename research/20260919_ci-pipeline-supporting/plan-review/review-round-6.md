# CI Steward plan v3: round 6, verification of the seven round-5 blockers

Reviewer: Vesper. Each item verified against `plans/ci-steward-plan.md` v3 by line.

| #   | Blocker                               | Where in v3                                                                                                                                                                                                                                                                               | Verdict                                                                                                                                                                                                  |
| --- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Labels as the freeze mechanism        | `:295` freeze records held state in `queue-state.json`, "no labels", with the skipped-run reason; `:317` priority is state, gated on `kind: incident-fix` + pipeline-only diff via `ci-steward arm`; `:399` "never labels ... nothing else can set it"                                    | met                                                                                                                                                                                                      |
| 2   | RED cannot close without a merge      | `:290` RED → RECOVERING on fix merged, or a re-dispatched targeted canary passing, or 15 min without starvation                                                                                                                                                                           | met                                                                                                                                                                                                      |
| 3   | Copilot threads block arming          | `:453` Copilot threads excluded from the unresolved-threads rule in `should-arm-automerge.sh`, the classifier and `review-gate`, with the reason                                                                                                                                          | met                                                                                                                                                                                                      |
| 4   | Cost stated as the pre-change number  | `:489` one-review figure plus the honest $1,700-3,500 range, three levers, "operator's spend decision"; "costs nothing new" gone                                                                                                                                                          | met in §5.3; **`:508` (operator action 1) and `:438` (options table) still say "about $850"**. Fix the operator line to the range before landing; it is the sentence the operator reads to approve spend |
| 5   | Phase 0 gate misses the #581 class    | `:346` phase 0 measures both refusal of a REST merge and that a merge group the new token enqueues receives check runs                                                                                                                                                                    | met                                                                                                                                                                                                      |
| 6   | §4.10 pieces in no phase              | `:518` phase 0 de-stales the skill; `:520` 1b delivers the classifier, the `watch-prs.sh` wrapper, merge-tail calling it, `ci-steward arm` + guard + WIP cap (green only) + `HANDED_OFF`, with the watcher-fixture exit gate and pinned no-empty-commit / wait-on-first-ejection remedies | met                                                                                                                                                                                                      |
| 7   | RED quarantine of a deterministic red | `:310` allowed only when the canary's own retry passed; red on retry refuses quarantine, lane is L2/L3                                                                                                                                                                                    | met                                                                                                                                                                                                      |

Folded follow-ups, spot-checked: Copilot approval route named and rejected with the security asymmetry weighed (`:444`); relative 25% trial bar with the sample read primary (`:457-458`); `review-gate` is a `merge_group` pass-through per ADR 260728-112203 (`:470`); `skip-review` retired (`:495`); `review-gh.sh` becomes an MCP tool (`:496`); §7 auto-revert wording carries the break-glass exception (`:547`).

## Decision

**SIGNED**, whole plan, v3.

One wording fix before the plan file lands, no re-review needed: `:508` and `:438` should carry the $1,700-3,500 range (or "see 5.3"), not $850. The remaining round-5 follow-ups (1-2, 4, 12-15, 17 in round 5) ride the phase PRs as the plan's "Follow-ups from round 5" list says.
