### PR #1138 [XL 1000+] docs(specs,decisions,research): notification system specified — four kinds, three tiers, one pipeline
labels=['review:light', 'skip-changelog'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-20T00:02:52Z
own commits after: ['style: prettier-format the union-merged manifests']
VERDICT: 1 important, 0 nits (review:light — quick pass only)  - **decisions/manifest.json:4710** — `260819-234830` supersedes ADR 0009, but 0009 was never flipped to `status: superseded` (manifest or file frontmatter), which the repos own `adr-drift-check.mjs` flags as a `supersedes-live-target` violation. All four new notification-system ADRs also use `status: draft`, a status the repos own ADR-writing convention says does not exist (proposed/accepted only).
  THREAD decisions/manifest.json:4710 resolved=False outdated=False
    🔴 Important: `260819-234830` sets `"supersedes": "0009"`, but ADR 0009's own entry (line 3370-3376) still has `"status": "accepted"` with no `supersededBy`, and `decisions/0009-calm-tech-notification-layers.md` frontmatter is unchanged (`status: accepted`, `superseded-by: null`). Per `.agents/skills/writing-adrs/SKILL.md:88` and the corpus checker (`.claude/scripts/adr-drift-check.mjs:180-182`, pinned by `scripts/__tests__/adr-corpus-checks.test.ts:112-137`), a `supersedes` link must point at an ADR whose status was flipped to `superseded` — otherwise it's reported as a `supersedes-live-target` drift violation. Either flip 0009 to `status: superseded` / `supersededBy: 260819-234830` in both the manifest and its file, or change this to `amends` if the intent is partial replacement.  Separately, all four new notification-system ADRs (`260819-234827`–`260819-234830`) use `status: draft`. Pe

### PR #1146 [XL 1000+] feat(server,shared,db): one notification pipeline — registry, rows with a delivery ledger, addressed SSE, and 
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-20T06:04:40Z
own commits after: ["chore(changelog): the review-fix commit joins the fragment's covers list", 'docs(api): regenerate OpenAPI spec and reference pages for the notification routes']
VERDICT: 1 important, 1 nit  This is a large, well-specified PR (unified notification pipeline: registry, service, store, entitlement, emitters, relay channel, routes, DB schema/migration, shared Zod schemas). Reviewed in four parallel passes covering the core pipeline, emitters/channels/routes, the task-completion-notifier deletion + call-site wiring, and DB/shared-schema/docs/changelog.  **🔴 Important** - `apps/server/src/services/notifications/notification-service.ts:273-311` — the dedupe check (`store.findRecent`) and the row write (`store.insert`) are both synchronous but separated by `await this.dispatchRelay(...)` on the common path, so two overlapping calls with the same dedupe key can both pass the "already said?" check before either inserts — producing duplicate rows and duplicate `notification`/SSE broadcasts for one logical event. No test exercises concurrent calls, so the gap is untested. Detail inline.  **🟡 Nit** - `apps/server/src/services/notifications/emitters/session-lifecycle.ts:16` — the `previous` lifecycle map is documented as bounded by live sessions, but normal idle eviction (`disposeProjector`) never fires a status-change event, so entries for evicted sessions are never cleared — only the rekey/retire path cleans up. Low practical impact (one string per entry). Detail inline.  **Clean:** dangling-reference sweep for the deleted `task-completion-notifier.ts` (and its two test files) found zero surviving imports/requires — only expected historical prose in specs
  THREAD apps/server/src/services/notifications/notification-service.ts:311 resolved=False outdated=False
    🔴 **Important:** The dedupe check-then-insert isn't atomic across the `await` on the common path, so "say a thing once" (the module's own documented guarantee, line 31) can be violated.  `findRecent` (line 273) and `insert` (line 311) are both synchronous, in-process SQLite calls (`notification-store.ts:136-146`, `:158-`), but they're separated by `await this.dispatchRelay(...)` at line 306 whenever `opts.delivered` isn't already supplied (i.e. every emitter path — `notify`/`resolveStanding` callers never pass `delivered`). Awaiting yields at least one microtask tick, and when `dispatchRelay` actually hands off to `deliverOverRelay` it can take real async time.  Concretely: two overlapping calls to `raise()` with the same `dedupeKey` (e.g. two emitters firing for related events, or a retry racing the original) can both pass the `alreadySaid` check at line 277 before either has called `st
  THREAD apps/server/src/services/notifications/emitters/session-lifecycle.ts:16 resolved=False outdated=False
    🟡 **Nit:** The doc comment claims `previous` is "bounded by the number of live sessions," but that's only true along the rekey/retire path (`retiredSessionId`, line 51). Normal 30-minute idle eviction goes through `disposeProjector()` (`session-state-projector.ts:2161`), which deletes the projector from the registry without emitting a status-change event — so `watchSessionLifecycle` never hears about it and the entry for that session id is never removed from `previous`. The map is technically unbounded over server uptime (though each entry is just one string key/value, so the practical impact is small).

### PR #1150 [XL 1000+] feat(server): a DM or @-mention from an agent lands in your Inbox, and reading the room clears it (DOR-1388)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-20T10:59:53Z
own commits after: []
VERDICT: 1 important, 0 nits  Solid PR overall — the notification-registry / dm.received / mention.received wiring is careful, well-documented, and the tests exercise the real RoomService + NotificationService together rather than mocking the seam. One correctness issue found and posted inline: a muted 1:1 DM that also @-mentions the operator raises no notification at all, contradicting this same file's documented invariant that muting "never suppresses mention.received."
  THREAD apps/server/src/services/rooms/room-service.ts:2761 resolved=False outdated=False
    🔴 Important: a muted DM that also @-mentions the operator raises no notification at all, contradicting this file's own stated invariant.  When `isDirectMessage` is `true`, `mentionsOperator` is forced to `false` here (`mentionsOperator && !isDirectMessage`) regardless of mute state, on the reasoning that `dm.received` will cover it. But `dm.received` itself is gated on `!roomMuted` in the emitter (`emitters/room-messages.ts:62`). So for a muted 1:1 DM room where an agent posts `"@dorian ..."`: - `dm.received`: suppressed (room muted) - `mention.received`: suppressed (forced false because `isDirectMessage` was true)  Result: total silence for a message that explicitly @-mentions the operator, even though `notification-registry.ts:403-404` says muting "never suppresses `mention.received`" and the `mention.received` entry (`notification-registry.ts:427`) says it "Pierces mute on purpose."  

### PR #1153 [XL 1000+] feat(server,client,shared,db): the escalation ladder — a blocked agent reaches your phone, and answering anywh
labels=[] imp=2 change=rebase/merge-main only rereviewed=False verdict_t=2026-08-20T14:19:54Z
own commits after: []
VERDICT: 2 important, 5 nits  Large PR (web push notifications + escalation ladder, DOR-1387). Reviewed the escalation core (server), web push infra + DB migration, client push UI/service worker, and docs/changelog/task-route consistency in parallel, then verified each finding by reading the actual code paths.  **Important:** - A projector rekey (canonical session id resolving after a session already errored) can double-arm `session.error` escalation and orphan the first timer — it fires later even after the condition cleared, since the dedupe key is keyed by session id and the two arms land under different ids (`emitters/session-lifecycle.ts`). - The web push channel's general-failure log branch leaks the subscription endpoint (a capability URL) into the persistent NDJSON server log, contradicting the module's own stated invariant — the adjacent "gone" branch gets this right (`channels/web-push.ts`).  **Nits:** a stale local push subscription not torn down when the server prunes it out from under a browser; the service worker's deep-link handler has no same-origin check (not currently reachable, but nothing at the schema layer would stop it); "Add this device" can show a false "server unavailable" during the VAPID-key loading race; boot catch-up times a parked schedule off `updatedAt`, which an unrelated edit can bump; a desktop-notifications comment overstates a compile-time guarantee that doesn't actually cover the `Set` it sits above.  Everything else — auth on the new push routes
  THREAD apps/server/src/services/notifications/emitters/session-lifecycle.ts:124 resolved=False outdated=False
    🔴 **Important**: a rekey re-announce can double-arm `session.error` and orphan the first escalation timer.  `rekeyProjector` (`session-state-projector.ts:2224-2289`) re-announces the projector's *current* status under the new canonical id via `notifyStatusChange(projector, fromId)`, passing `sessionId: newId`, `retiredSessionId: fromId`, and whatever `status.lifecycle` currently is.  If a session hits `lifecycle: 'error'` before its canonical id resolves (plausible — the SDK's init event can lag past several early events), this handler arms escalation under `oldId` with `since1` (line 122). When the id later resolves, the rekey re-announce comes through this same handler: `retiredSessionId` clears the local `previous`/`errorSince` bookkeeping for `oldId` (lines 92-95), but `before = previous.get(newId)` is `undefined` since `newId` has never been seen — so the early-return guard (line 99
  THREAD apps/server/src/services/notifications/channels/web-push.ts:219 resolved=False outdated=False
    🔴 **Important**: the general-failure branch logs the raw error, which leaks the subscription endpoint into the server's persistent log.  `web-push`'s `WebPushError` carries the subscription's `endpoint` as an own enumerable instance property. `logger.warn('[Push] Could not push to a subscribed browser', { id: subscription.id, err })` passes that error object straight through as `context`; `createFileReporter` in `lib/logger.ts:71` spreads it (`...context`) into the NDJSON line written to `{DORK_HOME}/logs/dorkos.log`, so `err.endpoint` — a capability URL, per this module's own doc comment ("the endpoint... never leaves this module except to the push service itself", lines 6-9 and 16-17) — ends up on disk on every transient push failure (timeout, DNS error, 5xx from the push service).  The "gone" branch two lines up avoids this correctly by extracting only `{ id, statusCode }`. This branc
  THREAD apps/client/src/layers/features/notifications/model/use-push-subscription.ts:155 resolved=False outdated=False
    🟡 Nit: when this browser's row disappears from the server list for a reason *other* than the explicit "Remove" click (e.g. the server pruned it after a 404/410 from the push service — `web-push.ts:206-214` — or another window removed it), this effect only clears `localStorage`. Unlike the explicit-remove path (`onSuccess` at lines 135-140), it never calls `unsubscribeThisBrowser()`. The browser keeps holding the now-dead `PushSubscription` object, so the next "Add this device" click reuses it via `existing ?? (await ...subscribe(...))` (line 236-243) — silently re-registering an endpoint the push service already rejected, and the device can never actually be pushed to again despite reappearing as "This device".
  THREAD apps/client/public/sw.js:99 resolved=False outdated=False
    🟡 Nit: `openDeepLink` has no same-origin/relative check on `deepLink` itself before navigating or opening a window — `new URL(deepLink, self.location.origin)` will happily resolve an absolute or protocol-relative string to a foreign origin. The origin check at line 100 (`client.url.startsWith(self.location.origin)`) only gates which *already-open* client gets reused, not the target `url`, so `clients.openWindow(url)` (line 107) would open any origin. Not reachable today — the only producer, `escalationDeepLink()` in `escalation-service.ts:418-425`, always returns a hardcoded relative route — but `WebPushPayloadSchema.deepLink` (`notification-schemas.ts:545`) is just `z.string().min(1)` with no format constraint, so nothing at the schema layer would catch a future caller handing this an absolute URL. Worth a defense-in-depth check (reject anything that doesn't start with `/`).
  THREAD apps/client/src/layers/features/notifications/ui/ReachMeSection.tsx:122 resolved=False outdated=False
    🟡 Nit: "Add this device" is gated on `push.availability === 'available'`, but `availability` (`use-push-subscription.ts:184-189`) resolves to `'available'` whenever the VAPID query simply hasn't resolved yet (`vapid.isSuccess` still `false`), not only once the key is known-good. Clicking during that loading window hits the `vapidKey === null` guard in `subscribe()` (`use-push-subscription.ts:161-164`) and shows "DorkOS could not set up push notifications on this computer" — a false negative for what is really just a loading race, not an actual server-unavailable state.
  THREAD apps/server/src/index.ts:1607 resolved=False outdated=False
    🟡 Nit: `since: Date.parse(task.updatedAt)` uses the generic task-row timestamp as "when this schedule started standing" for boot catch-up. `updatedAt` isn't a dedicated "parked at" mark — `PATCH /:id` (`routes/tasks.ts:386`) bumps it on any edit to a still-`pending_approval` schedule (name/prompt/etc.) without re-arming anything, since neither the arm nor resolve branch fires when `existing.status === updated.status === 'pending_approval'`. If the server restarts afterward, this understates how long the schedule has actually been waiting — the catch-up window can give an overdue escalation the "remainder of its delay" instead of firing it immediately, or (near the 4-hour boundary per the ADR) wrongly treat a week-old-but-recently-edited schedule as freshly parked. Low impact — only affects boot-time timing precision — but a dedicated `parkedAt` field would make this exact.
  THREAD apps/desktop/src/main/notifications/index.ts:296 resolved=False outdated=False
    🟡 Nit: the doc comment claims "a member added to the shared enum without a branch in `notificationDeepLink` is a compile error there, so the two cannot silently diverge" — but that compile-time guarantee is about the `switch` in `notificationDeepLink`, not this `Set`. `KNOWN_SUBJECT_TYPES` is a separate hand-maintained literal (used at line 318 to validate incoming SSE payloads) with no compiler tie to the shared enum at all. A new subject type added to the enum and wired into `notificationDeepLink`'s switch (satisfying the compile-time concern) would still be silently dropped here at parse time unless someone remembers to also update this list. Fails closed (safe), but the comment overstates the guarantee — worth either deriving this Set from the same source the switch's exhaustiveness check uses, or rewording the comment.

### PR #1164 [XL 1000+] feat(client): sections hold anything and sit at the top; the agent list stays short (DOR-1371, DOR-906, DOR-11
labels=[] imp=2 change=rebase/merge-main only rereviewed=False verdict_t=2026-08-21T23:16:15Z
own commits after: []
VERDICT: 2 important, 0 nits  - 🔴 `apps/client/src/layers/widgets/inbox-bell/ui/InboxBell.tsx` (+ its test): this file appears to revert `DOR-1395` (Inbox popover honesty + phone a11y headings + all-clear animation), which merged to `main` earlier today — its changelog fragment is still present on this branch but the behavior it describes is gone. Looks like a merge-artifact from the `origin/main` merge commit picking this branch's stale copy of an unrelated file. - 🔴 `apps/server/src/services/core/config-manager.ts:3258-3318` (`canonicalSidebar`): the one-shot conversion latch (`sidebarConverted`) is set to `true` unconditionally, before the write-through is known to succeed. If `this.store.set(\"ui\", next)` throws, every subsequent read in that process silently serves the raw un-converted legacy sidebar (`items: undefined` on a still-`agentPaths` section) instead of retrying — unlike the old per-read `normalizeSidebarPrefs` this replaces, which was resilient to exactly this. Downstream, `build-library-sections.ts`'s `groupSection` iterates `group.items` with no guard, so this can crash the sidebar model rather than just show an empty section.  Otherwise this is an exceptionally well-tested, well-documented refactor (D3 sections, agent-list floor, per-section sort, DOR-588 migration simplification) — no other correctness, FSD, SDK-confinement, or dangling-reference issues found.
  THREAD apps/client/src/layers/widgets/inbox-bell/ui/InboxBell.tsx:59 resolved=False outdated=True
    🔴 Important: this file (and its test) reverts `fix(client): the Inbox popover speaks honestly...` (DOR-1395), which merged to `main` today with its own changelog fragment (`changelog/unreleased/260821-192642-inbox-popover-honesty-and-a11y-headings.md`). That fragment is still present on this branch, but the behavior it documents is gone:  - `waitingLabel`/`waitingSummary` collapse schedules back into "requests" — the exact conflation DOR-1395 removed (`countNoun`/`listWaitingKinds` are deleted here). - The section headings (`Needs You` line 323, `Scheduled Runs` line 370, `Activity` line 416, `Standing Permissions` line 466) are back to `hidden md:block`, undoing the `sr-only md:not-sr-only` fix that kept them in the accessibility tree on phone screen readers. - The extra sentence under "Scheduled Runs" (lines 373-376) that DOR-1395 removed is back. - The "All clear" checkmark's fade/ris
  THREAD apps/server/src/services/core/config-manager.ts:3260 resolved=False outdated=False
    🔴 Important: `sidebarConverted` latches to `true` here regardless of whether the write-through below (line 3309, `this.store.set('ui', next)`) actually succeeds. The catch at line 3308-3316 only logs a warning — it never resets `this.sidebarConverted`.  So: the *first* call that finds a legacy `ui.sidebar` (bare paths / `groups[].agentPaths`) converts and returns the corrected value even if the persist throws. But every *subsequent* call in that same process (e.g. the next `GET /api/config`) takes the `if (this.sidebarConverted) return ui;` early-return at line 3259 and hands back the raw, un-converted `ui` straight from `stored` — since it was never actually persisted, it's still in the legacy shape.  Concretely: a section whose `items` field is absent (only `agentPaths` present) reaches the client as literally `items: undefined`. Nothing on the client re-validates this with Zod (`use-c

### PR #1168 [XL 1000+] feat(client): a proposed schedule makes its case on a card — who asked, why, exactly when, and try it before y
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-22T02:48:14Z
own commits after: ["chore(changelog): a curly apostrophe is not the commit's apostrophe — the covers line now matches byte-for-byte", 'chore(format): prettier pass on three files the review rounds left unformatted']
VERDICT: 1 important, 0 nits  A large, well-tested feature build (the schedule proposal becomes a full `AskCard`-family card, shared exit-timing moved to `shared/lib`, triage wording split into questions/requests/schedules). Deletion sweep for the removed `ScheduleApprovalRow` and the `features/ask/model/ask-exit-transition` move both came back clean — no dangling references.  One important finding posted inline on `apps/client/src/layers/widgets/inbox-bell/ui/InboxBell.tsx:290`: the "All clear ✓" drain beat can fire while an approved schedule's receipt is still being held on screen, because its count omits the schedule settle-hold that the ask-settling count is deliberately folded in for.
  THREAD apps/client/src/layers/widgets/inbox-bell/ui/InboxBell.tsx:293 resolved=False outdated=False
    🔴 Important: `usePinnedDrainBeat` is fed `waitingCount + settling.length`, but `settling` is only `useSettlingAsks()` — it doesn't include `shownSchedules` (the schedule hold from `useScheduleApprovalCards`, line 210). For asks, `settling.length` is deliberately added so the "All clear ✓" beat doesn't fire until a receipt has finished showing (that's the whole point of folding it in here). Schedules get an equivalent hold via `settling-approvals`, but it's never added to this count.  Concretely: open the Inbox with exactly one parked schedule and nothing else waiting, click Approve. `waitingCount` goes 1→0 as soon as the refetch drops the task from the server's list, while `shownSchedules` (and the "Needs You" section showing the "Approved" receipt via `ScheduleApprovalCard`) is still populated for `APPROVAL_SETTLE_MS`. `usePinnedDrainBeat(waitingCount + settling.length, open)` sees `bef

### PR #1173 [XL 1000+] feat(client): channels wear the one bar — the room masthead is gone (DOR-1402)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-22T04:26:43Z
own commits after: ["fix(e2e): open the room sheet by the bar's head count, not the masthead's roster (DOR-1402)"]
VERDICT: 1 important, 2 nits  Important - apps/client/src/layers/widgets/one-bar/ui/ChannelsBar.tsx:100: leavingForHome races useOneBarState().room (GET /api/rooms/:id) against useTeamRoom() (GET /api/rooms, a separate request). On a cold load of /channels?id=<teamId> the single-room fetch can resolve first, so the bar briefly names #team before flipping to "Channels" once the list catches up - the exact flash useTeamRoomRedirect/ChannelsPage were built to eliminate at the page level, just not closed at the header level. Full detail posted inline.  Nits (posted as line comments failed - both land on unchanged context lines outside the diff): - apps/e2e/manifest.json:728 - an exploration note in room-autonomys scenario metadata still names the old testid room-header-halt, which this PR renames to room-halt (gated by data-idle on room-run-state). The relatedCode entry two lines up was updated to RoomRunState.tsx; this prose note was not. - apps/e2e/pages/RoomsPage.ts:56,201,269 - three comments (roomHeader, bridgeVisibilityBadge, and the roster/membersChip doc) still say "the masthead," which this PR deletes (RoomHeader.tsx) in favor of the one bar.  Dangling-reference sweep otherwise clean: MemberList, rosterAgentFaces, ChannelsHeader, HomeMembersChip, RoomHeader, and the room-header-*/headerMark/room-member-list testids/locators have no surviving code references (only expected prose in specs/research/changelog). FSD import direction and the shared-bar/entities boundary look correct th
  THREAD apps/client/src/layers/widgets/one-bar/ui/ChannelsBar.tsx:100 resolved=False outdated=False
    🔴 **Important:** This guard races against two independently-timed queries, so the "no flash of #team in the channel bar" guarantee this PR is built around doesn't actually hold here.  `room` comes from `useOneBarState()`, ultimately `useRoom(roomId)` — `GET /api/rooms/:id` (`apps/client/src/layers/entities/room/model/use-room.ts:23-24`). `team.room` comes from `useTeamRoom()`, which is keyed off `useRooms()` — `GET /api/rooms` (`apps/client/src/layers/entities/room/model/use-rooms.ts:83-89`). These are two separate HTTP requests with no ordering guarantee, especially on a cold load of `/channels?id=<teamId>` (a bookmark or shared link — exactly the scenario `ChannelsPage.deep-link.test.tsx` was added for).  `ChannelsPage`/`useTeamRoomRedirect` handles this correctly: it withholds rendering (`'pending'`) until `team.status !== 'loading'` before deciding whether the id is #team (`apps/clie

### PR #1179 [XL 1000+] feat(client): a reload paints the sidebar from local memory, instantly (DOR-1373)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-22T09:13:41Z
own commits after: ['test(e2e): a seeded agent is on the roster before a spec looks for it (DOR-1373)', 'Revert "test(e2e): a seeded agent is on the roster before a spec looks for it (DOR-1373)"', 'test(e2e): the browser suite keeps its cold world; boot-stability opts in (DOR-1373)', 'test(e2e): the cold-world opt-out reaches every spec, not just fixture users (DOR-1373)']
VERDICT: 1 important, 0 nits  Extremely well-documented PR implementing the sidebar warm-boot persisted query cache. Reviewed the boot cache module, the auth sign-out/onboarding-restore interaction, the boot-state gate, and the new unit/e2e tests. Found one real correctness/security concern in the sign-out clearing path (inline comment on `query-persister.ts`); everything else — the allow-list design, the malformed-blob guarding, the buster/max-age handling, the FSD boundaries, and the test coverage (including a real browser reel test for the boot sequence) — held up well.
  THREAD apps/client/src/layers/shared/lib/query-persister.ts:346 resolved=False outdated=False
    🔴 **Important — sign-out's "empty write" can be overwritten by a stale throttled write already in flight, resurrecting the signed-out user's boot cache.**  `createSyncStoragePersister`'s `persistClient` is throttled internally (trailing-edge `setTimeout`, `throttleTime = SAVE_THROTTLE_MS`): each call updates the *captured args* and (re)arms a timer only if one isn't already pending; the timer fires with whichever args were most recently captured. `removeClient()` is a separate, unrelated method — it does `storage.removeItem(key)` immediately but does **not** cancel or update any pending `persistClient` timer.  `write()` here only calls the throttled `persister.persistClient(client)` for the *non-empty* branch. When the boot queries are removed (`client.clientState.queries.length === 0`), it calls `persister.removeClient()` directly, entirely bypassing the throttle — so it never tells the

### PR #1181 [XL 1000+] feat(client): the sidebar moves so you can follow it — folds spring, rows arrive and slide, drags lift and set
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-22T13:56:00Z
own commits after: []
VERDICT: 1 important, 0 nits  The continuity-motion feature (fold spring, arrival flash, reorder slide, drag lift/settle) is implemented with unusually thorough test coverage and documentation. One correctness bug found: the row-arrival flash never fires for the case that actually motivates it — a zone (Heads Up/Today) coming into existence from empty, because the section subtree freshly mounts and seeds its "seen" set with the causing row(s). Left as an inline comment on `use-arrived-rows.ts:43` with the full causal chain. No FSD/SDK/os.homedir/marketplace-rollback violations, no dangling references from the CSS/class renames (`ring-sidebar-ring ring-2` → `sidebar-drop-ring`, `dropAnimation={null}` removal), changelog fragment present and correctly scoped.
  THREAD apps/client/src/layers/features/dashboard-sidebar/ui/motion/use-arrived-rows.ts:43 resolved=False outdated=False
    🔴 **Important:** The row that causes Heads Up (or Today) to first appear never gets the arrival flash — the headline case this feature is built for.  `bodyZone()` returns `undefined` when a zone has zero rows (`build-sidebar-model.ts:500`), so `SidebarZones.tsx` only includes `'now'`/`'today'` in `drawn.zones.map(...)` when they're non-empty. When Heads Up goes from empty (unmounted) to having its first item, React mounts a brand-new `SidebarSection` subtree — a fresh `useArrivedRows` instance. Its first render seeds `seen` here with `keysOf(layoutKey)`, i.e. the very rows that just caused the zone to exist, so they're treated as "already there" rather than as arrivals. Compounding it, `SidebarSection.tsx`'s `<AnimatePresence initial={false}>` around the row list (line 145) also suppresses any entrance animation on that same first render, even if `arrived` were somehow non-empty.  The re

### PR #1214 [L 250-999] docs(specs): Desktop Resilience program — plan, specs, install-UX research
labels=['review:light'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-23T16:48:16Z
own commits after: ['docs(plans): one full release at program end, not a v0.63.1 hotfix (operator decision)']
VERDICT: 1 important, 0 nits (review:light — Important only)  This PR is docs/specs only (plan + research + two spec files + manifest additions), no code changes.  - 🔴 `specs/manifest.json` registers `desktop-renderer-supervision` under id `260823-163822`, but the spec file itself declares `260823-163732` in its header — the two ids disagree, and the manifest id is consumed by `.claude/scripts/spec-manifest-ops.ts` tooling. See inline comment.
  THREAD specs/manifest.json:20 resolved=False outdated=False
    🔴 Important: this manifest entry registers `desktop-renderer-supervision` under id `260823-163822`, but the spec file's own header declares a different id: `specs/desktop-renderer-supervision/02-specification.md:3` says "Spec id: 260823-163732". These two ids don't match. `.claude/scripts/spec-manifest-ops.ts` operates on this manifest's `id` field, so a mismatched id here is a functional inconsistency, not just a typo — pick the correct value and make both agree. (The sibling `desktop-updater-overhaul` entry at line 6 correctly matches its spec's header of `260823-163823`.)

### PR #1229 [L 250-999] fix(server,desktop): always load the current interface after an update
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-23T19:01:09Z
own commits after: []
VERDICT: 1 important, 0 nits  The cache-hygiene fix (server `Cache-Control` headers + desktop cache clear on version bump) is well-designed and well-tested. One important finding: inserting the `await clearHttpCacheOnVersionChange()` before the first `createTrackedWindow()` call in `apps/desktop/src/main/index.ts` opens a real race with the `second-instance`/`activate` handlers that can create a window early and corrupt the `mainWindow` tracking closure — see inline comment for details.
  THREAD apps/desktop/src/main/index.ts:294 resolved=False outdated=False
    🔴 Important: this `await` opens a real race that can corrupt the `mainWindow` tracking invariant.  Before this PR, `createTrackedWindow()` (line 297) ran synchronously right after `startServer()` resolved — no macrotask boundary for another event to land in between. Now `clearHttpCacheOnVersionChange()` awaits a genuine async operation (up to `CLEAR_CACHE_TIMEOUT_MS` = 5s in the worst case), so `second-instance` (`index.ts:168-175`, fired by a double-launch of the app) or `activate` (`index.ts:367-371`, dock click) can run while `mainWindow` is still `null` but `getServerPort()` is already truthy:  ``` app.on('second-instance', (_event, argv) => {   ...   showMainWindow();   // mainWindow is null, getServerPort() truthy → createTrackedWindow() fires early }); ```  (`showMainWindow`, `index.ts:120-124`)  When the `ready` handler then resumes and unconditionally calls `createTrackedWindow(

### PR #1240 [M 50-249] fix(client,server,shared,cli,docs): full autonomy copy names the approval gate, not "asking"
labels=['review:light'] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-08-23T20:50:57Z
own commits after: ['chore(docs): regenerate openapi for the reworded trust-axis description (DOR-1431)', 'test(client): OnboardingPowerStep asserts the reworded door copy (DOR-1431)']
VERDICT: 1 important, 0 nits (review:light — Important only)
  THREAD apps/client/src/layers/features/full-power-door/ui/FullPowerDoor.tsx:58 resolved=False outdated=False
    🔴 Important: `FULL_POWER_POINTS[0].lead` changed from `'Runs without asking.'` to `'No approval prompts.'`, and `FullPowerDoor.test.tsx` was updated to match — but `apps/client/src/layers/features/onboarding/__tests__/OnboardingPowerStep.test.tsx:103` still asserts `screen.getByText(/runs without asking/i)` against this same shared door copy (comment there: "The shared promise copy, not a second copy written here"). That string no longer appears anywhere in the rendered door, so this test will fail.

### PR #1241 [M 50-249] fix(client): onboarding deep-links hold their step on a cold load
labels=[] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-08-23T20:58:53Z
own commits after: []
VERDICT: 1 important, 0 nits  The DOR-1431 fix itself (gate the initial anchor on router-resolved state, one-time via `hasAnchored` ref) looks correct and well-reasoned, and the new navigation/anchor tests are otherwise solid. One test has a wrong premise — flagged inline.
  THREAD apps/client/src/layers/features/onboarding/__tests__/use-onboarding-stage.test.tsx:212 resolved=False outdated=False
    🔴 Important: this test's premise is wrong — `power` is not an unknown stage value. `ONBOARDING_STAGES` includes `'power'` (`onboarding-stage.ts:15`), and this very file already pins that `?onboarding=power` derives to `'power'` and round-trips unchanged (`useOnboardingStage — the power stage` describe block, lines 375-400). Since `isOnboardingStage('power')` is `true`, the anchor effect's `if (isOnboardingStage(raw)) return;` (`use-onboarding-stage.ts:77`) skips the rewrite here too, so `harness.readStage()` stays `'power'`, not `'welcome'` — this test should fail as written. Swap in a value that's actually outside the enum (e.g. `'xyz'`) to test the intended "unknown/invalid value" path.

### PR #1241 [M 50-249] fix(client): onboarding deep-links hold their step on a cold load
labels=[] imp=1 change=rebase/merge-main only rereviewed=False verdict_t=2026-08-23T22:32:42Z
own commits after: []
VERDICT: 1 important, 0 nits  Re-review: the fix and the new anchor/navigation tests still look correct. The one issue from the prior pass is still present — flagged inline again since the commit was updated but this test premise wasn't.
  THREAD apps/client/src/layers/features/onboarding/__tests__/use-onboarding-stage.test.tsx:212 resolved=False outdated=False
    🔴 Still unaddressed from the prior pass: this test's premise is wrong. `'power'` is a real, valid member of `ONBOARDING_STAGES` (`onboarding-stage.ts:15`), not an unknown one — the "power stage" describe block below (`use-onboarding-stage.test.tsx:385-393`) tests it as a legitimate stage that round-trips unchanged.  Because `'power'` passes `isOnboardingStage`, the anchor effect's guard (`use-onboarding-stage.ts:77`, `if (isOnboardingStage(raw)) return;`) returns early and never rewrites the param. So this test doesn't exercise the "unknown value" branch at all, and its assertion `expect(harness.readStage()).toBe('welcome')` contradicts the actual behavior (the stage should stay `'power'`).  Use a value that is genuinely outside `ONBOARDING_STAGES` (e.g. `'nonexistent'`) to test the unknown-value anchor path.

### PR #1266 [L 250-999] feat(site): /compare adjacent + alternatives pages — OpenClaw, Hermes, Buzz, Terragon, Roo Code (DOR-1468)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-24T16:59:07Z
own commits after: []
VERDICT: 1 important, 0 nits  Data-only PR (5 new /compare pages: OpenClaw, Hermes, Buzz, Terragon, Roo Code) plus a per-framing `outboundLabel`/`scopeNote` fix that already-existing pages pick up generically. Hub, sitemap, and `[slug]/page.tsx` all iterate the `comparisons` array with no hardcoded slug list, so the new pages wire up automatically — verified. All `relatedFeatures`/`featureSlugs` resolve against the feature catalog, `theirStrengths` is correctly present for the three `adjacent` entries and omitted for the two `discontinued` ones, and the new invariant tests (no stale "announcement" wording, per-framing outbound label) check out against the actual copy.  One real bug: Terragon's `pricing` cell is scored `partial` with no `source`, which the data-integrity test suite requires for every non-`no` cell — this will fail `comparisons.test.ts` as written. Left inline at the exact line.
  THREAD apps/site/src/layers/features/marketing/lib/comparisons.ts:2110 resolved=False outdated=False
    🔴 Important: Terragon's `pricing` cell scores `verdict: 'partial'` but has no `source` field. Every other `yes`/`partial` cell in this file (including all other Terragon cells) carries a `source`, and the invariant suite enforces it:  ```ts if (cell.verdict === 'no') continue; expect(cell.source, ...).toMatch(/^https:\/\//); ``` (`comparisons.test.ts:144-148`)  With no source here, `dorkosCellFor`... rather `cell.source` is `undefined`, so this assertion will throw and `comparisons.test.ts` will fail. Add a `source:` line (e.g. the `terragon-oss` repo, or drop to reference the same shutdown-notice archive already in `sources`).

### PR #1269 [L 250-999] chore(harness): format once per turn, not after every edit
labels=[] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-08-24T18:06:54Z
own commits after: ['test(harness): fixture-pin format-changed.sh, and commit the research it cites']
VERDICT: 1 important, 1 nit  - 🔴 `research/20260320_prettier_formatting_ai_agents.md:6` — `superseded_by` (and the header comments in `.claude/README.md` / `.claude/hooks/format-changed.sh`) cite `research/20260824_agent_post_edit_formatting_stale_context.md` as the evidence for moving the format hook off the per-edit path, including a specific "reproduced live in this repo twice" claim — but that file does not exist anywhere in the repo. The behavioral change itself (PostToolUse → Stop) looks sound and well-reasoned on its own merits, but the cited research backing it is unverifiable as shipped. - 🟡 `.claude/hooks/format-changed.sh:124` — the rewritten hook (13 → 126 lines, real file-collection/filter logic) has no fixture test, unlike sibling hooks of similar complexity (git-guard, process-guard).
  THREAD research/20260320_prettier_formatting_ai_agents.md:6 resolved=True outdated=False
    🔴 Important: `superseded_by` points at `research/20260824_agent_post_edit_formatting_stale_context.md`, which doesn't exist anywhere in this repo (confirmed via a full-tree search for the filename). This isn't a cosmetic broken link — that report is cited as the entire evidentiary basis for the behavioral change in this PR (moving `format-changed.sh` from `PostToolUse` to `Stop`, in `.claude/README.md`, `.claude/hooks/format-changed.sh`'s header, and again at line 28 here), including a specific claim that it "reproduced the failure live in this repo, twice, while being written." Right now there's no way to verify that claim or read the analysis behind dropping `async: true`. Either the research file needs to be added in this PR, or these citations need to point at wherever it actually landed.
    REPLY doriancollier Fixed in 66a5ca9. You were right and it was worse than it looked: the report was written into the main checkout before these worktrees existed and never picked up, so the change cited evidence a reviewer could not open. It is committed in this branch now.
  THREAD .claude/hooks/format-changed.sh:124 resolved=True outdated=False
    🟡 Nit: this hook grew from 13 lines with no real branching to 126 lines with real logic (three-source file collection, extension filtering, existence checks). The sibling hooks with comparable logic (`git-guard.mjs`, `process-guard.mjs`) each have a fixture test (`scripts/test-git-guard.sh`, `scripts/test-process-guard.sh`); this one has none, before or after. Worth a small fixture test covering the file-collection/filter logic, especially since a regression here fails silently (everything exits 0).
    REPLY doriancollier Fixed in 66a5ca9. Added scripts/test-format-changed.sh: 8 cases, 36 assertions, every one asserting exit 0, wired into the harness CI job (not fixtures, which skips pnpm install and this needs a real prettier). Verified red against four deliberately broken hooks, and I re-verified one independently.

### PR #1276 [XL 1000+] feat(server,skills,db,client): any skill can be a scheduled task (DOR-1485)
labels=['review:light'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-24T21:15:33Z
own commits after: ['docs(api): regenerate the OpenAPI spec for the schedule provenance fields', 'style(client): prettier the schedule showcase and TaskRow test']
VERDICT: 1 important, 0 nits (review:light — quick pass, Important findings only).  🔴 `apps/server/src/services/tasks/task-file-watcher.ts:181` — `handleFileChange` retires a schedule row by the sighted (unresolved) path instead of the resolved identity path when a `schedule:` block is removed from a skills-root file, so the pause silently no-ops for symlinked plugin skills or files under a symlinked ancestor (e.g. macOS `/tmp`), leaving the schedule firing after its block was removed. See inline comment for detail and the reconciler's equivalent code, which handles this correctly.
  THREAD apps/server/src/services/tasks/task-file-watcher.ts:181 resolved=False outdated=False
    🔴 Important: `retireIfPresent(filePath)` here uses the *sighted* path (what chokidar reported), not the resolved identity path. `retireIfPresent` (line 204) calls `store.markRemovedByFilePath(filePath)`, which does an exact-match `WHERE filePath = ...` lookup (`task-store.ts:1072-1082`, no path resolution). But rows discovered from a `skills` root are always keyed on the *resolved* real path (`skills-root-discovery.ts` `definitionFromBlock`/`definitionFromUnreadableBlock` use `location.resolvedPath`).  `readTaskRootFile` already computes and returns this for the `'ignored'` outcome (`resolvedPath` field, set via `fs.realpath(filePath)` in `skills-root-discovery.ts`), and the reconciler's equivalent branch correctly prefers it: `this.retireIfPresent(result.resolvedPath ?? result.filePath)` (`task-reconciler.ts`, with the comment "By the identity a row would hold, which is the resolved pat

### PR #1318 [M 50-249] fix(server,evals): an agent saves what you ask it to remember, or says it did not (DOR-1564)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-25T21:42:08Z
own commits after: []
VERDICT: 1 important, 0 nits  The DOR-1564 fix tightens `<session_model>`'s standing-preferences wording to "one-to-one chat … never in a room," explicitly to close a group-DM authority-inheritance gap (X-11b). Left one inline finding: `MEMORY_TRUST_FRAMING` (renders in `<agent_memory>` on the same turns) still carries the older "direct chat" wording this PR calls unsafe, and the new doc comment incorrectly claims the two sentences are the same. Everything else — the eval-model-recording plumbing (`run-suite.ts`, `types.ts`, tests), the README evidence tables, and the changelog fragment — checked out.
  THREAD apps/server/src/services/runtimes/shared/agent-context.ts:184 resolved=False outdated=False
    🔴 Important: The new `<session_model>` sentence deliberately says "one-to-one chat with you and never in a room" instead of "direct chat", and the doc comment above (agent-context.ts:165-169) explains why: "direct chat" is ambiguous — a DM room can seat more than two members, so a third party in a group DM could inherit operator authority (the X-11b vector). The test added in this PR even guards against regressing to "a 'direct chat' that a group DM room satisfies" (agent-context.test.ts:554-555).  But `MEMORY_TRUST_FRAMING` (packages/shared/src/convention-files.ts:110-111) still reads `'Only the operator, in a direct chat, sets your standing preferences.'` — the exact wording this PR just called unsafe — and it renders live in `<agent_memory>` (agent-context.ts:259) on every turn a memory file exists, i.e. alongside this new sentence on most turns.  The doc comment here (line 158-159) c

### PR #1331 [M 50-249] refactor(opencode): split the runtime dir under the 25-file cap (DOR-1575)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-26T11:09:53Z
own commits after: ['chore(opencode): repoint vocab-gate allowlist + ADR manifest at moved paths']
VERDICT: 1 important, 0 nits  🔴 **Important — `scripts/vocab-gate/allowlist.json:45` now points at a path this PR moved, breaking two vocab-gate tests**  This PR renames `apps/server/src/services/runtimes/opencode/check-dependencies.ts` → `.../opencode/providers/check-dependencies.ts`. `scripts/vocab-gate/allowlist.json` was not updated and still has:  ``` "path": "apps/server/src/services/runtimes/opencode/check-dependencies.ts", "terms": ["connection"], ```  `isAllowlisted` (`scripts/check-vocab-gate.ts:225`) matches by plain substring. `"...opencode/check-dependencies.ts"` is not a substring of the new `"...opencode/providers/check-dependencies.ts"` — the inserted `providers/` breaks contiguity — so this exception no longer covers the moved file. The file still carries the allowlisted copy at its new location, `check-dependencies.ts:247` (`` `Your saved ${name} connection did not work...` ``), which is a copy-bearing `description:` property the gate parses.  This will fail, in `scripts/__tests__/check-vocab-gate.test.ts` (run by `scripts-test.yml`): - `"every allowlist entry path resolves to a real file or directory"` (line 397) — `existsSync` on the stale path is now false. - `"the real repo is clean against its own shipped data (regression canary)"` (line 384) — the `connection` hit at the new path is no longer suppressed.  Fix: update the allowlist entry's `path` to `apps/server/src/services/runtimes/opencode/providers/check-dependencies.ts`.  (Could not post this inline — `scri

### PR #1344 [XL 1000+] feat(server): room-repo store, reconciler, and operator-only enable route (DOR-1592)
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-27T23:24:14Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light — quick pass; nits and the deletion sweep skipped per label)  **Important:** RoomRepoService.enable() (apps/server/src/services/rooms/repo/room-repo-service.ts) has no per-room lock guarding its create path. Two concurrent POST /:id/repo calls for the same room (double-click, or a retry after a dropped response) can both pass the readSidecar existence check before either writes. Since seedRoomMd is deterministic, the second calls commit finds nothing to commit and fails, which triggers unwindFailedEnable — and that unconditionally deletes the sidecar row and rm -rfs the repo/ directory regardless of which call created it, destroying the first calls already-committed, already-returned-201 repo. See inline comment for the full trace and a couple of fix options.  Everything else in this room-repo feature (the git hardening in room-repo-git.ts, the file-first store ordering, the reconcilers re-read-before-retire fix, the operator-only and 404-before-403 route gating, the OpenAPI drift test) is careful and well-covered by the added test suites.
  THREAD apps/server/src/services/rooms/repo/room-repo-service.ts:150 resolved=False outdated=False
    🔴 Important: `enable()` has a TOCTOU race that lets a concurrent call destroy a room repo that was just successfully created.  Two `POST /:id/repo` calls for the same room (double-click, or a client retry after a dropped response — a pattern this codebase explicitly designs around elsewhere, e.g. the reactions route's retry note) can both reach `readSidecar(roomId)` (line 150) before either has written, so both see `existing === null` and both proceed to `seedGuarded` → `seedRepo`.  `seedRoomMd` is fully deterministic (title/topic only, no timestamp), so the second call's `git commit` in `commitAll` (room-repo-git.ts) finds nothing to commit and exits non-zero — no fine-grained timing needed, just both requests passing the check before either writes. That failure lands in `seedGuarded`'s catch, which calls `unwindFailedEnable` (lines 383–394). `unwindFailedEnable` unconditionally does `s

### PR #1346 [L 250-999] docs(harness): migrate durable session lessons from agent memory into skills, rules, and guides
labels=['review:light', 'skip-changelog'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-27T23:57:31Z
own commits after: ['docs(harness): two more migrated traps — vitest --force footgun, dist-read subpaths, queue-owns-merge-strategy', 'docs(release): Phase 4 plan narration matches the corrected PR-then-tag order']
VERDICT: 1 important, 0 nits (review:light — Important findings only).  🔴 **Important** — `.claude/commands/system/release.md:275` and `:283` (Phase 4, "Present the plan") still narrate the *old* release flow — tag the local commit, then `npm publish`, then push to origin ("mechanical steps ahead ... npm publish, push to origin, and the draft GitHub Release — a patch/minor run carries all the way through these without another stop"; and "run continues through tag → npm publish → push → draft GitHub Release without stopping"). This PR rewrites Phase 6 (6.8–6.10) to require: commit on a `release/vX.Y.Z` branch → open a PR → merge queue lands it → **then** tag the merged squash SHA → publish npm. The old direct "push to origin" step (previously 6.10) was deleted, and the new 6.8 explicitly says "Do NOT tag yet." Left as-is, Phase 4 self-contradicts the corrected Phase 6 and could lead someone to tag a pre-merge commit — the exact "wrong SHA" mistake this same PR's new Check-2 text says was already hit in practice (squash rewrites the SHA). Could not post inline: these lines sit outside any diff hunk (both the MCP inline-comment tool and `review-gh.sh inline` reject the line as unresolvable in the diff), so flagging here instead. Suggest updating the Phase 4 summary/bullet to match the corrected branch → PR → merge → tag → publish order.  Rest of the PR (agent-memory migration into `.claude/rules/testing.md`, `REVIEW.md`, `contributing/browser-verification.md`, the `creating-pull-requests

### PR #1347 [XL 1000+] feat(server,shared): ROOM.md rides every room turn — pinned, capped, defused (DOR-1593)
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-28T00:29:05Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light — quick pass, Important-only, no deletion sweep).  🔴 The `RoomConventions` cache keys purely on commit sha, so a live change to `config.rooms.repo.maxRoomMdBytes` does not take effect until the room's next commit — contradicting the moduleʼs own documented "read per turn" / "next turn, not the next room" guarantee. See inline comment on `room-conventions.ts:255`.
  THREAD apps/server/src/services/rooms/repo/room-conventions.ts:255 resolved=False outdated=False
    🔴 Important: `maxRoomMdBytes()` is documented as read live "per turn" (see the dep's own doc a few lines up, and `room-repo-service.ts:103`'s "somebody who turns it down means the next turn"), but the cache-hit path here ignores it. `read()` only re-evaluates the over-cap decision when the commit sha changes (line 254-255); a cache hit returns the previously-cached `body`/`overCapBytes` verdict regardless of what `this.deps.maxRoomMdBytes()` currently returns.  Concretely: - Operator lowers the cap below an already-cached, previously-under-cap file's size: the next turn (no new commit) still serves the full stale `body`, not the notice — the cost-control promise the docstring makes is broken until somebody happens to commit again. - Operator raises the cap above an already-cached, previously-over-cap file's size: the cached `overCapBytes` entry is still returned, and `overCapNotice(overC

### PR #1363 [XL 1000+] feat(server,shared,client): a room says who picked up your message, and who is working (DOR-786)
labels=['review:light', 're-review'] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-08-28T20:44:12Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light — Important findings only)  🔴 **Important:** `workingNow()` in `use-room-details-view.ts` can display an agent as still "working" for up to 30s after it has actually finished and its reply already landed in the transcript, in a room whose live stream is open the whole time. See the inline comment on `use-room-details-view.ts:307` for the mechanism (a gap between the dispatcher's ~10s republish cadence and the presence store's `done`-requires-a-matching-key handling, combined with the new `workingAgents` snapshot never being invalidated when a reply lands).
  THREAD apps/client/src/layers/features/room-management/model/use-room-details-view.ts:317 resolved=False outdated=True
    🔴 **Important:** `workingNow` can show an agent as still working for up to `PRESENCE_TTL_MS` (30s) *after* it has actually finished and its reply is already on screen, in a room that has its live stream open the whole time.  The mechanism: - `useRoomPresenceStore.observe()` only clears a claim on a `done` signal if the store already holds a matching `(authorId, entryId)` key (`use-room-presence.ts:264-268`). If the sheet is opened *after* the agent's claim was taken but *before* the dispatcher's next periodic republish (up to 10s later, per the same file's docs), the store never records the `working` entry — so when `done` arrives moments later (the agent finishes fast), it's a silent no-op and `live` stays `[]`. - `live.length === 0` here is treated as "nothing to say, fall back to the snapshot" (line 312-317), but it's ambiguous between "confirmed nobody" and "never heard from the stre

### PR #1364 [XL 1000+] feat(desktop,client): the window that cannot stay black — renderer supervision and recovery (DOR-1453)
labels=[] imp=1 change=rebase/merge-main only rereviewed=False verdict_t=2026-08-28T20:16:07Z
own commits after: []
VERDICT: 1 important, 0 nits  Thorough desktop renderer-supervision PR (heartbeat, escalating recovery ladder, fallback page). One Important finding posted inline on `apps/desktop/src/main/renderer-health/index.ts:480`: reattaching a new primary window (via the macOS "recreate on activate" path in `main/index.ts`) does not bump `healthGeneration`, so an in-flight recovery rung from a closed/replaced window can act on the freshly attached, healthy window — including rung 3s relaunch, which can quit/restart the app for a failure that no longer applies. Not covered by the existing supervisor test suite.
  THREAD apps/desktop/src/main/renderer-health/index.ts:480 resolved=False outdated=False
    🔴 **Important:** `attachRendererSupervisor` (line 761) never bumps `healthGeneration`, and `climbLadder` (line 477) re-reads `supervised?.win` fresh after its awaits instead of using the window it was recovering when `recoverFrom` was called. `stale()` (line 480) only checks `generation !== healthGeneration || win.isDestroyed()` against whatever window is *currently* attached — not the one that actually failed.  Reachable path: `main/index.ts`'s activate handler recreates the primary window when it was closed (macOS keeps the app alive with zero windows — see the comment above `gotTheLock` handling). If a rung is in flight against window A — e.g. rung 3 awaiting `clearCacheWithinDeadline()`/`clearGpuAndCodeCaches()` (up to ~5-10s, `CLEAR_CACHE_TIMEOUT_MS` in `cache-hygiene.ts`) — and the user closes window A and reopens it (dock icon) within that window, `win.on('closed')` (line 797) nul

### PR #1375 [L 250-999] fix(client): say so when the server can't be reached — the second black-window path (DOR-1475)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-08-29T01:40:45Z
own commits after: []
VERDICT: 1 important, 0 nits
  THREAD apps/client/src/AppShell.tsx:404 resolved=False outdated=False
    🔴 Important: `configFailures` (`errorUpdateCount`) is not scoped to "this launch," and `configKeys.current()` is exactly the query key the boot cache persists across reloads.  `errorUpdateCount` is a lifetime counter on the query cache entry — it increments on every failed fetch and is never reset by a later success (this repo already documents that fact independently at `apps/client/src/layers/entities/agent/model/use-mcp-signin-flow.ts:319-323`: *"`errorUpdateCount` is a lifetime total (which would kill a healthy flow that hiccupped a few times over its life). Both were tried."*).  `configKeys.current()` (`['config','current']`) is on the boot-cache allow-list (`apps/client/src/layers/shared/lib/query-persister.ts:129-130`), persisted via a real `dehydrate()`/`hydrate()` round-trip (wired in `main.tsx` via `PersistQueryClientProvider`, exercised for real in `warm-boot.test.tsx`). `dehy

### PR #1376 [XL 1000+] feat(server,client): the five room-management verbs behind the roomsManage grant (DOR-1611)
labels=[] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-08-29T02:00:54Z
own commits after: []
VERDICT: 1 important, 0 nits.  The `roomsManage` grant (the whole point of this PR) is enforced only at the capability-registry layer (`registry.invoke` → `enforceToolGroupGrant`), which gates the MCP tools `add_room_members`/`remove_room_members`. But `RoomService.addMember`/`removeMember` also back the pre-existing HTTP routes `POST /api/rooms/:id/members` and `DELETE /api/rooms/:id/members/:authorId`, which resolve the caller from `X-DorkOS-Agent` with no grant check at all — see inline comment on `room-service.ts`. Any agent holding its own valid identity token (minted into every session env, and what the CLI/`dorkos call` already sends as this header) can add/remove room members via a direct HTTP request regardless of whether its owner ever turned "Manage rooms" on for it. This is a regression introduced by this PR: `addMember`/`removeMember` were unconditionally `requireOperator`-gated before, so an agent token could never succeed there; the new `requireRosterWriteAllowed` widens that to any agent without adding the grant check the MCP surface got.  Everything else here is unusually thorough — the spec amendments, the name-oracle fix, the per-member dedup/sanitization fixes, the fallback-seat cleanup, and the DM/system-room rename refusals all read as correct and well-tested.
  THREAD apps/server/src/services/rooms/room-service.ts:4878 resolved=True outdated=False
    🔴 Important: `requireRosterWriteAllowed` (and its callers `addMember`/`removeMember` at lines 2167-2169 and 2306-2309) let *any* identified member agent edit a room's roster — it checks only `isOwnerRecord` / `caller.kind === 'agent'`, never the `roomsManage` grant. The grant is enforced solely at the capability layer (`enforceToolGroupGrant` in `tool-group-enforcement.ts`, run from `registry.invoke`), which gates the in-session/external MCP tools `add_room_members`/`remove_room_members`. But `RoomService.addMember`/`removeMember` are *also* the direct implementation behind the plain HTTP routes `POST /api/rooms/:id/members` and `DELETE /api/rooms/:id/members/:authorId` (`apps/server/src/routes/rooms.ts:650`, `718`), which resolve the caller via `resolveCaller` → `X-DorkOS-Agent` (`apps/server/src/routes/room-caller.ts:106-131`) with **no grant check at all**.  Concretely: any agent hold
    REPLY doriancollier Fixed in the squashed head (b3fc041c5): `addMember`/`removeMember` are back to unconditional `requireOperator` (what the HTTP routes, LocalCommunityAdapter and the team-room hook call), and the capability layer now enters through new `addMemberFromTool`/`removeMemberFromTool` siblings carrying `requ

### PR #1377 [XL 1000+] feat(client): a scheduled task picks its runtime, model and effort in the app (DOR-1615, DOR-1347)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-08-29T02:30:26Z
own commits after: ['fix(client): a new task keeps your default trust stop when its agent names an unregistered runtime (DOR-1615)']
VERDICT: 1 important, 0 nits  Careful, well-documented DOR-1615/DOR-1347 change (task runtime/model/effort selection). The prototype-key-collision hardening added throughout this PR (`getRuntimeDescriptor`, `modesFor` in `TaskFormInner.tsx`, the dedicated test suites) missed one spot: `CreateTaskDialog.tsx`'s `newTaskRuntime` resolution uses a plain truthy bracket index instead of an own-property check, which can silently drop the operator's configured default trust stop back to `acceptEdits` for a new task pre-selected on an agent whose manifest runtime collides with an `Object.prototype` key. Left an inline comment with the trace and a suggested fix.  No dangling references to the removed `TASK_RUNTIME` client constant (its only remaining mentions are historical prose in server code/specs). Everything else — the Runs-on controls, the consent-door widening logic, the run-history "what it actually ran on" column, the changelog fragment, docs, and operating-skills version bump — checked out.
  THREAD apps/client/src/layers/features/tasks/ui/CreateTaskDialog.tsx:86 resolved=True outdated=True
    🔴 Important: `newTaskRuntime` uses a plain truthy index into `capabilityMap.capabilities`, which is exactly the prototype-collision hazard this PR fixes everywhere else (`getRuntimeDescriptor` now uses `Object.hasOwn`, `TaskFormInner.tsx`'s `modesFor` relies on `Object.keys(...).includes(...)` via `agentTierApplies`, and there's a dedicated test file for it).  ```js const newTaskRuntime =   initialAgentRuntime && capabilityMap?.capabilities[initialAgentRuntime]     ? initialAgentRuntime     : (capabilityMap?.defaultRuntime ?? null); ```  If `initialAgentRuntime` is `'constructor'` (or `'toString'`/`'valueOf'`/`'hasOwnProperty'`/`'__proto__'`), `capabilityMap.capabilities['constructor']` resolves to the inherited `Object` function — truthy — so `newTaskRuntime` becomes `'constructor'` instead of falling through to `capabilityMap.defaultRuntime`.  That value then feeds `useCapabilitiesForR
    REPLY doriancollier Fixed in 591b62f56. Both sites now treat registration as an own-property question: `newTaskRuntime` gates on `Object.hasOwn` (a prototype-key manifest falls through to `defaultRuntime`), and `useCapabilitiesForRuntime` returns `undefined` for non-own keys. The New Schedule test now asserts the behav

### PR #1391 [L 250-999] ci(test): shard the merge-queue sweep across four runners
labels=['skip-changelog'] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-08-29T15:15:06Z
own commits after: ['ci(test): prove the shard union, and keep one producer of the required context', 'ci(test): the union check greps a file, not a pipe its own pipefail can kill', 'chore(ci): re-trigger checks']
VERDICT: 1 important, 2 nits  Sharding the merge-queue test sweep across four runners is a well-reasoned change and the fan-in/verdict pattern mirrors `browser-test.yml`. One real gap: adding `--passWithNoTests` to the sharded queue leg removes the "fail loudly on zero matched test files" safety net for packages that do not already opt into it themselves, on the one check that actually gates merges — see inline comment. Two nits flagged for awareness (uncertain, low-cost): a same-named `test` check now has two job producers, and per-shard runs are not proven to union to full file coverage the way the Playwright shards are.
  THREAD .github/workflows/test.yml:245 resolved=True outdated=True
    🔴 Important: `--passWithNoTests` here removes the "fail loudly on zero matched test files" safety net from the merge-queue's *required* `test` check, for every package that doesn't already set `passWithNoTests: true` in its own vitest config.  Vitest's default (`passWithNoTests: false`) exits 1 when a package's test discovery matches zero files — AGENTS.md documents this exact behavior ("No test files found, exiting with code 1"). That was the thing that caught a broken `include` glob or an accidentally-emptied `__tests__` dir. The comment above justifies the flag for the case where a package legitimately contributes 0 files to *one* of four shards — but the flag can't distinguish that from a package matching 0 files in **all four** shards, i.e. discovery being broken entirely. `scripts/assert-tests-executed.sh` (unchanged by this PR) only checks that turbo ran a `test` task per package 
    REPLY doriancollier Fixed in d90cfaff7 — the net is rebuilt one level up, where it can discriminate. Each shard records what vitest actually collected (`--reporter=json` → `shard-files-N.txt` artifact), and the fan-in runs the new `scripts/assert-shard-union.sh`: every package declaring a `test` script must contribute 
  THREAD .github/workflows/test.yml:276 resolved=True outdated=True
    🟡 Nit (flagged with uncertainty, not confirmed): the required check named `test` now has two producers on the same commit for a given event — job `test` (id `test`, skipped when `merge_group`) and this job (`test-verdict`, `name: test`, real when `merge_group`) — both would post a check run literally named `test`. The header cites `no-fragment-under-skip-label` as the precedent for "a skipped job satisfies a required check," but that job is a single job definition that conditionally skips itself — not two separate job definitions simultaneously emitting check runs under the same name on one commit. I can't verify from static reading how GitHub's required-status-check evaluation resolves two same-named check runs from one workflow run; worth confirming on the first real merge-queue pass that the real (non-skipped) run is what the ruleset actually keys off, rather than assuming it from a p
    REPLY doriancollier Fixed in d90cfaff7 by removing the ambiguity instead of betting on it: the `test` job is now the ONE producer of the required context on both events — it runs the affected sweep itself on pull_request and acts as the shard fan-in on merge_group (`needs` + `always()`, with the shard-result gate as it
  THREAD .github/workflows/test.yml:221 resolved=True outdated=False
    🟡 Nit: unlike `browser-test.yml`'s `browser-test` fan-in — which downloads every shard's JSON report and asserts their *union* covers every spec file on disk — this workflow's per-shard `assert-tests-executed.sh` only proves each shard's turbo invocation touched all 18 packages; nothing here proves the four shards' vitest `--shard` selections actually union to the full test-file set for a package. Lower risk than the Playwright case this pattern was copied from (vitest's `--shard` is a first-party deterministic partition, not prone to the `testIgnore`-glob class of bug that motivated the union check there), but worth naming since a mis-partition wouldn't be caught the way the browser workflow explicitly guards against.
    REPLY doriancollier Fixed in d90cfaff7 — this PR now carries exactly the browser-test union pattern you pointed at: per-shard collected-file artifacts, fan-in downloads them, `assert-shard-union.sh` proves the union covers every package. It asserts per-package non-emptiness rather than union==disk (vitest include globs

### PR #1433 [XL 1000+] feat(server,test-utils): claude-code and codex keep the images their tools return (DOR-1664)
labels=[] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-09-02T01:22:01Z
own commits after: ['chore: re-trigger checks so the newly-required lint job reports']
VERDICT: 1 important, 0 nits  **Important:** The live media-write path (`message-sender.ts` / `pump-turn-stream.ts`) keys the `SessionAttachmentStore` by the DorkOS request-time `sessionId`, while the history-read path (`claude-code-runtime.ts:967`, via `getSessionSnapshot`'s `historyId = getInternalSessionId(sessionId)`) keys it by the canonical, post-rebind `sdkSessionId`. Per `session-store.ts`, these two ids differ on essentially every brand-new session's first turn (the SDK assigns its own session id after `system/init` and DorkOS rebinds mid-turn). The result: an image captured live is stored under the pre-rebind id, and the first history re-read after that (page reload, SSE reconnect) cannot find it under the post-rebind id, so it writes a duplicate copy at a different URL — breaking the "one file, one URL" invariant the PR's own new tests assert (which never exercise the rebind case, since they use one literal sessionId throughout). See inline comment for the full trace.  Otherwise this is a careful, well-tested change: the pure/async split for image extraction, the single-shot re-delivery guards (and the conformance-suite gate that actually forces a repeated delivery to prove it), the per-instance `mediaOutput` capability resolution, and the SSRF-safe refusal of remote image sources are all solid and consistent across the claude-code and codex adapters.
  THREAD apps/server/src/services/runtimes/claude-code/claude-code-runtime.ts:984 resolved=True outdated=False
    🔴 **Important:** This `sessionId` and the one the live turn used to store the same image are two different strings on essentially every new session's first turn, so this call orphans the live-written file and writes a duplicate.  Trace: `getMessageHistory`'s `sessionId` here comes from `getSessionSnapshot`'s `historyId = this.getInternalSessionId(sessionId) ?? sessionId` (a few lines below, unchanged by this diff) — i.e. `session.sdkSessionId`, the *canonical, post-rebind* id. But the live write path (`messaging/message-sender.ts:441-448`'s `mapSdkMessageWithMedia(opts.attachments, …, sessionId, …)`, same for the persistent-pump path through `sessions/pump-turn-stream.ts`) stores the image under `executeSdkQuery`'s own `sessionId` parameter — the id the turn was *asked* with, never reassigned even after `session.sdkSessionId` rebinds mid-turn. `persistent-dispatch.ts:313-319` says this e

### PR #1441 [L 250-999] fix(server,client,shared): let a new session take a model for the runtime you picked (DOR-1691)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-02T06:38:38Z
own commits after: ['chore(changelog,docs): fold the auto-seeded fragment and refresh the API export']
VERDICT: 1 important, 0 nits  The runtime-hint fix itself (`modelGateAuthority`, `resolveForSessionWithOwnership`/`resolveSessionRuntime`, the client hint threading) is sound — I traced the model-gate logic against all bound/unbound/hinted/unhinted combinations, checked that the hint never reaches the persisted `runtime` column (ADR-0255 first-write-wins is preserved), and confirmed `getSessionRuntimeType` stays a compatible wrapper for its many other routing-only callers (binding router, adopt-session, relay dispatch, the `/runtime-type` read endpoint). Tests in `sessions-model-gate-unbound.test.ts` exercise the real registry + DB with disjoint model catalogs, which is the right seam for this bug.  One blocking issue: a leftover seeded changelog fragment (see inline comment) will fail the changelog validate gate as committed.
  THREAD changelog/unreleased/260902-060813-let-a-new-session-take-a-model-for-the-r.md:8 resolved=True outdated=True
    🔴 Important: this fragment still carries the `dorkos-changelog:seeded` marker and the raw commit-subject bullet, never curated for a human reader. Per `changelog/README.md#seeded-fragments`, `changelog_backfill.py --validate` fails unconditionally on any fragment containing this marker — no `skip-changelog` bypass — so this will fail the `fragment-present`/validate CI gate as-is.  There's also a well-written fragment for the same change (`260902-060249-picking-a-model-on-a-new-session-works-again.md`), so this seeded stub is redundant on top of being unfinished. Either rewrite this bullet for a human and delete the marker comment, or delete this whole fragment (moving its `covers:` claim into the other fragment if needed).

### PR #1462 [XL 1000+] fix(server,marketplace): marketplace package names stay inside their own folders
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-02T19:40:25Z
own commits after: []
VERDICT: 1 important, 0 nits  This PR closes several marketplace path-traversal gaps (uninstall name traversal via `%2F`-encoded route params, cache-key escapes, local-path installs outside the directory boundary, unbounded preview, git flag injection via author-controlled URLs/refs/subpaths) with a well-structured `package-paths.ts` guard module and consistent boundary checks across the HTTP routes and MCP tools. The new tests are specific (asserting exact error types and disk-state, not loose bounds) and I traced the changed guards against their callers without finding a bypass in the traversal-defense logic itself.  One finding: the HTTP route sanitizes `PathEscapeError` before returning it (it names the cache root under `dorkHome`, which reveals the operators home directory/username), but `tool-install.ts`s two generic `errorContent(err, INSTALL_FAILED)` catches do not apply the same sanitization, so that MCP tool can leak the path straight to the calling agent. Left inline on `tool-install.ts:170`.
  THREAD apps/server/src/services/marketplace-mcp/tool-install.ts:170 resolved=False outdated=False
    🔴 Important: this PR adds boundary sanitization here, but two catches further down in this same handler (the `preview` catch at line 184, and the `install` catch at line 232) still leak `PathEscapeError`'s raw message to the calling agent.  `deps.installer.preview()`/`.install()` route through `PackageResolver.resolve()` → `PackageFetcher.fetchPackage()` → `MarketplaceCache.getPackage`/`materializePackage` (`marketplace-cache.ts:180,242`), whose `packageDir()`/`marketplaceDir()` throw `PathEscapeError` when a resolved `'marketplace'`-kind `packageName` (matched verbatim off a marketplace.json entry — `package-resolver.ts:222-231,278-288` never runs it through `assertPackageName`/`assertPathSegment`, unlike the git-derived branches) collapses outside the cache root, e.g. an entry published as `a/../../../etc`.  That's exactly the case `routes/marketplace.ts`'s `mapErrorToStatus` special-c

### PR #1463 [L 250-999] fix(server,evals): in a direct message, the answer an agent works out is the one it sends (DOR-1643)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-02T19:50:59Z
own commits after: []
VERDICT: 1 important, 0 nits  The DOR-1643 fix (spelling out the exact `post_to_room` call in the tool-only closing directive) is well-targeted and thoroughly tested for the DM/channel/addressed/unaddressed matrix, including a real render-through-three-adapters test (`room-context-runtime-neutrality.test.ts`). One gap: the spelled-out call never carries `replyTo`, so a thread reply under tool-only mode gets steered into posting at the top level instead of into the thread — flagged inline with citations.  No dangling-reference issues found (no deletions/renames in this PR), no Hard Rule violations, changelog fragment present and correctly scoped.
  THREAD apps/server/src/services/runtimes/shared/room-context-block.ts:1401 resolved=False outdated=False
    🔴 **Important — the closing directive's spelled-out call drops `replyTo`, so a copied thread reply lands in the main channel instead of the thread.**  `closingDirective` / `channelBranches` / `dmBranches` build every call through `callWith`, which only ever interpolates `data.room.id` and never `data.thread`:  ```ts const callWith = (placeholder: string): string =>   toolPrefix === undefined     ? `the posting tool, with roomId "${data.room.id}"`     : `${toolPrefix}post_to_room(roomId: "${data.room.id}", text: ${placeholder})`; ```  `post_to_room`'s `replyTo` is what makes a post land in a thread rather than the top level (`room-service.ts:2751`: *"`replyTo` is what makes this a thread reply"*; the tool handler only sets it when the model explicitly passes it, `room-capabilities.ts:696`). Threads only exist for channels (`specs/room-participation/02-specification.md:73`: *"`post_to_room

### PR #1482 [L 250-999] fix(server): the log line now says what went wrong (DOR-802)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-03T02:37:12Z
own commits after: []
VERDICT: 1 important, 0 nits  The new `serialize-error.ts`/`normalizeLogContext` machinery is well-designed and thoroughly tested (depth-bounded cause chains, AggregateError sub-errors, size clipping, and both `try/catch` layers so the reporter genuinely never throws — verified against the test suite line by line). One important finding: it widens what every `logger.error/warn(msg, err)` call site in the server writes to disk, which reopens the class of secret-leak DOR-1509 fixed for relay adapters, for the many non-relay call sites that were never audited for it. Left as an inline comment on `serialize-error.ts`.
  THREAD apps/server/src/lib/serialize-error.ts:85 resolved=False outdated=False
    🔴 **Important — this reopens the DOR-1509 secret-leak class for every caller outside `packages/relay`, not just the relay adapters that route through `describeError`.**  Before this PR, `logger.error(msg, err)` silently dropped `err.message`/`err.stack`/nested own-enumerable props (spreading an Error yields `{}`), so a caught error's content never reached the NDJSON log. That was accidental, but it was also the reason DOR-1509 could scope its fix to `describeError()` in `packages/relay` and its "never spread, serialize, or otherwise touch the error object itself" rule (`packages/relay/src/lib/describe-error.ts:43-48`) only had to cover relay adapters.  `serializeError` now unconditionally writes `err.message` (line 84), `err.stack` (line 85), and recursively expands **every own-enumerable property** of the error (line 72) and its `cause` chain — for *every* `logger.error/warn(msg, err)` 

### PR #1507 [L 250-999] fix(desktop): ship the ngrok binary Remote Access needs (#1458)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-03T12:57:08Z
own commits after: []
VERDICT: 1 important, 0 nits  Well-engineered fix for #1458 (missing ngrok native binary in the packaged desktop app). The `assertPlatformBinariesWired` gate generalizing the per-family checks in `build-server.ts` is a solid design — it discovers binary families from each dependency's own manifest rather than a hardcoded list, so a future fifth family fails loudly instead of shipping silently broken. The macOS packaged-smoke addition (`assertTunnelBinaryUnpacked`) directly closes the gap that let #1458 ship.  One Important finding posted inline: the new comment on `TUNNEL_BINARY` in `smoke-packaged.ts` claims the Windows counterpart of this check "rides `desktop-release.yml`'s `verify-windows` job instead," but that job only verifies the installer runs and `DorkOS.exe` exists — it never checks the ngrok binary (or any binary family) actually landed in `app.asar.unpacked`. The exact class of bug this PR fixes has no real coverage on Windows, contrary to what the comment tells the next reader.
  THREAD apps/desktop/scripts/smoke-packaged.ts:362 resolved=False outdated=False
    🔴 **Important:** This claims the Windows ngrok binary is verified by `desktop-release.yml`'s `verify-windows` job, but that job (lines ~371-432) only silent-installs the `.exe` and checks that `DorkOS.exe` exists at the install path — it never inspects `app.asar.unpacked` for `@ngrok/ngrok-win32-x64-msvc/ngrok.win32-x64-msvc.node` or any of the other three binary families. Nothing in CI actually asserts the Windows tunnel binary made it into the packaged app, unlike the new `assertTunnelBinaryUnpacked` check this PR adds for macOS.  That means the exact bug this PR fixes (#1458 — a family declared correctly in `package.json`/`electron-builder.yml` but still missing from the copied output) has no automated coverage on the Windows target, while this comment tells the next reader it does. Worth either wiring an equivalent existence/size check into `verify-windows` (e.g. checking the unpacke

### PR #1546 [XL 1000+] fix(client): UI audit batch 13 — the session surface: fewer things competing (DOR-1759)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-04T20:22:53Z
own commits after: ['test(client): the sticky switch answers to the name batch 08 gave it (DOR-1759)', 'test(client): permission groups are read through the card that now holds them (DOR-1759)', 'test(client): the fold-away case types its placeholder the way batch 10 does (DOR-1759)', 'style(client): prettier on the conflicted sheet test', 'style(client): prettier on the conflicted ChatPanel (DOR-1759)', 'test(e2e): the Schedule section is opened by its button, not a summary (DOR-1759)', 'fix(client): a session opened at its unread rule stops marking itself read (DOR-1759)', 'docs(changelog): fold the unread-rule fix into the batch 13 fragment (DOR-1759)']
VERDICT: 1 important, 0 nits  Reviewed the batch-13 session-surface UI work (plan auto-fold, `BottomSlot` arbitration above the composer, Pulse de-dup, and the `CollapsibleFieldCard` disclosure unification). One correctness issue found and posted inline: the new plan-fold state in `use-task-state.ts` is not reset on a session switch (ChatPanel/useTaskState are not remounted — no `key` in `SessionPage.tsx`), unlike the sibling task-list state which already handles this via `scopeChanged`. This can leave one session's hand-collapsed/auto-folded plan state bleeding into a different session that happens to share the same stream lifecycle value at switch time.  Everything else checked out: FSD imports and SDK confinement are respected, no dangling references from the `<details>` → `CollapsibleFieldCard` swaps (the file-list disclosure correctly stays native `<details>` per the new design-system doc), the `PermissionPrimer` hook/view split preserves the original eligibility logic exactly, the changelog fragment is properly formed, and the e2e/unit tests were updated consistently with the behavior changes (Pulse dedup, Advanced/Schedule sections unmounting their contents when closed, `BottomSlot` arbitration order).
  THREAD apps/client/src/layers/features/chat/model/use-task-state.ts:229 resolved=False outdated=False
    🔴 **Important:** The new fold state (`isCollapsed`, `collapsedByHandRef`, `wasRunningRef`) isn't scoped to `sessionId`, but `ChatPanel`/`useTaskState` are *not* remounted on a session switch — `SessionPage.tsx` renders `<ChatPanel sessionId={activeSessionId} .../>` with no `key` (confirmed on disk; this is intentional per the `session-state-manager` spec, so the sibling task-list `state` is deliberately reset via `scopeKeyRef`/`scopeChanged` in the effect above at lines 161-180). This fold logic has no equivalent reset.  Concretely: open session A while it's streaming (plan opens), hand-collapse it (`collapsedByHandRef.current = true`), then switch to session B, which is *also* mid-turn (`lifecycle` is `'streaming'` for B too — very plausible given DorkOS's whole premise of running multiple agents at once). Since the effect's only trigger is `lifecycle` *changing value* (`[lifecycle]` de

### PR #1560 [L 250-999] test(e2e,client): the browser suite stops hot-replacing itself mid-run (DOR-1412)
labels=['review:light'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-05T00:09:54Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light scope — Important findings only)
  THREAD apps/e2e/pages/RoomsPage.ts:962 resolved=False outdated=False
    🔴 Important: `wheelReplyRowIntoTapRange`'s "come forward again" branch doesn't sustain — it can oscillate forever at the top edge instead of searching forward, defeating the fix this PR is making.  `back` is recomputed from scratch each poll tick from `isAtTop()` (`scrollTop <= 64`), not from a remembered search direction. Trace it: scrolling back reaches the top (`isAtTop()` true, row still `absent`) → `back = false` → wheel forward by `notch` (`view.height / 3`, i.e. far more than 64px). Next tick, `scrollTop` is now `> 64` so `isAtTop()` is `false`; if the row is still `absent` (hasn't been rendered into range by one notch), `back` flips back to `true` → wheel back by `notch`, landing back near `scrollTop = 0`. Every subsequent tick repeats this: at-top → forward one notch → not-at-top-and-absent → back one notch → at-top again — an infinite two-step oscillation that never progresses 

### PR #1570 [XL 1000+] fix(desktop,client,shared,server): the desktop app restarts and resets through its own supervisor (DOR-542)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-05T05:02:42Z
own commits after: []
VERDICT: 1 important, 0 nits  This is a well-structured, thoroughly-tested refactor/fix (DOR-542): moving instance-lock reading into @dorkos/shared, and giving the desktop shell its own restart/reset supervisor path instead of relying on the server re-execing itself. Tests are unusually rigorous (edge cases for stale/recycled pids, unreadable lock files, partial deletes, multi-window origin gaps).  One real bug found: runReset() in apps/desktop/src/main/admin/index.ts can tell the user "Your data was deleted" when it was not, on the untested "already restarting" race (see inline comment). No other blocking issues found - no FSD/SDK-confinement/os.homedir/marketplace-rollback violations, changelog fragment and ADR amendment are correctly formed, barrel exports and package.json exports map are consistent, and no dangling references from the instance-lock module move.
  THREAD apps/desktop/src/main/admin/index.ts:189 resolved=False outdated=False
    🔴 **Important:** `runReset`'s catch-block message can falsely claim data was deleted.  `deletion` falls back to `'Your data was deleted.'` whenever `err` isn't a `RestartFailedError`. But `restartServer` also throws a plain `Error('DorkOS is already restarting its server. Give it a moment.')` (server-process.ts:456-458) when a restart/reset is already in flight — and that guard fires *before* `whileStopped` (i.e. `wipeDataDirectory`) ever runs. In that case nothing was deleted, yet this returns `{ ok: false, message: "Your data was deleted. DorkOS is already restarting its server. Give it a moment." }`.  This is reachable in practice: the app supports multiple cockpit windows (`window-manager.ts`'s own-origin `window.open` handling) sharing the same main-process supervisor state, so a Restart/Reset click in one window while another is already restarting hits this path. It directly contra

### PR #1581 [L 250-999] fix(cli): the tunnel URL printout listens to the live server, not a bundled copy (DOR-1745)
labels=[] imp=1 change=rebase/merge-main only rereviewed=False verdict_t=2026-09-05T10:10:13Z
own commits after: []
VERDICT: 1 important, 0 nits  The core fix (apps/server/src/index.ts exporting the live `tunnelManager` singleton, and `packages/cli/src/cli.ts` reading it off `server.tunnelManager` instead of importing `../server/services/core/tunnel-manager.js` directly) is sound and well-tested — traced the import chain, confirmed `tunnelManager` at apps/server/src/index.ts:37 is the same instance the boot path starts/stops, and confirmed the new `server-shims.test.ts` singleton guard has no false positives against the other shims today. The QR-code `this`-binding fix (calling `generator.generate(...)` as a method instead of a detached destructured function) is a real, correctly-reasoned bug fix. The deleted `packages/cli/server/services/core/tunnel-manager.d.ts` shim has no surviving references anywhere in the tree. Docs update in `tunnel-setup.mdx` matches the new behavior.  One Important finding posted inline: the second changelog fragment (`260905-100157-fold-adversarial-review-nits-on-the-tunn.md`) still carries the unedited `dorkos-changelog:seeded` marker, which fails `changelog_backfill.py --validate` unconditionally per `changelog/README.md#seeded-fragments` — and its content is redundant with the first fragment's `covers:` list anyway, so it should just be deleted.
  THREAD changelog/unreleased/260905-100157-fold-adversarial-review-nits-on-the-tunn.md:8 resolved=False outdated=True
    🔴 Important: this fragment still contains the `dorkos-changelog:seeded` marker (line 8) and the raw, unedited commit-subject bullet ("Fold adversarial review nits on the tunnel printout"). Per `changelog/README.md#seeded-fragments`, `changelog_backfill.py --validate` fails on any fragment that still contains this marker, unconditionally and with no `skip-changelog` bypass — so this will fail CI as-is.  The commit it covers is already described for humans in `changelog/unreleased/260905-093740-tunnel-url-printout.md`'s `covers:` list, so the fix here is just to delete this whole fragment rather than rewrite it.

### PR #1605 [XL 1000+] fix(connectors): verify raw MCP before connecting
labels=['review:deep'] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-09-06T01:38:40Z
own commits after: []
VERDICT: 1 important, 2 nits  Deep review of the raw-MCP connector-verification rewrite (DOR-738). The `ConnectorFlowBindings`/`RawMcpConnectorProvider` concurrency work (LRU binding store, single-flight polls, generation-guarded client store, provider-identity-pinned disconnect races) is careful and well-verified — the accompanying spec doc even documents a revert-and-confirm regression check per finding, which checks out against the test suite. No correctness bugs found in that part.  One finding could not be posted inline because it sits outside this PR's diff hunks (GitHub rejects review comments on unchanged lines):  🔴 **Important** — `apps/server/src/services/connectors/custody-disclosure.ts:70-75`: the `external` custody sentence still claims *"that server manages its own sign-in"* — the pre-PR OAuth-style raw-MCP behavior. This PR's entire point is that raw MCP no longer runs any sign-in; it verifies a preconfigured connection by probing `initialize`/`tools/list` directly, and the new `docs/connections/raw-mcp.mdx` says so explicitly ("DorkOS does not run a sign-in flow for this setup"). The stale sentence is not just docs drift — it renders live in `ConnectDialog.tsx`'s `ConnectConnectedStep` (via `account.disclosure`) and in the `connector.start_connect` chat capability's message (`connector-capabilities.ts`), where it now reads "...that server manages its own sign-in. I'll check the configured connection now." — contradicting itself in the same sentence. This file's own hea
  THREAD apps/server/src/routes/connectors.ts:105 resolved=True outdated=False
    🟡 Nit: if `flowBindings.record()` throws the capacity error here, `provider.startConnect()` has already succeeded and minted a provider-local flow (e.g. `RawMcpConnectorProvider` already added an entry to its own `_flows`), but that flow is never bound to a public id, so it can never be polled or cancelled — it just sits until the provider's own bound eviction (`MAX_CONNECT_FLOWS`) reclaims it. Self-healing given the bound, but worth a comment noting the orphan, or a rollback call, since it's easy to miss on the next read.
    REPLY doriancollier Documented the capacity edge and bounded provider-flow cleanup in 706ae0fd1c3d2c106fe966e581b92b68ad097fe0. The comment explains why the current port has no safe rollback identity; no cancellation or disconnect behavior was invented. The same independent REVIEW.md reviewer checked this final delta a

### PR #1612 [XL 1000+] feat(connectors): add stable connection identities and scoped access foundation
labels=['review:deep'] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-09-06T09:47:50Z
own commits after: []
VERDICT: 1 important, 5 nits  Reviewed the full connector-foundation PR (DB schema/migration, shared schemas, server services/routes, and the client agentPath-provenance flow) across five parallel passes plus direct verification. This is a large, well-tested, additive-only foundation change — the legacy `connected_accounts` tables are deliberately retained side by side with the new `connections` model (no dangling references from that coexistence), the `agentPath` trust boundary is verified server-side against Mesh before being persisted (`routes/sessions.ts`), and the startup reordering in `index.ts` (constructing the connector registry/attachment stores before `MeshCore`, registering the unregister-cleanup callback before `reconcileOnStartup()`) is sound end-to-end.  🔴 **Important** - `legacy-connection-migration.ts:411-420` — a failed backfill discards the real error (`void error`) with no server-side logging anywhere in the call chain; a production migration failure would be completely invisible in the logs, discoverable only via the 503s it causes.  🟡 **Nits** (5) - `session-exposure.ts:505-513` (`invalidateProvider`) and `attachment-store.ts:102` (`deleteAgent`) are both now dead in production — each was superseded by a new method in this PR but kept alive only by its own unit test. - `connection-store.ts:115` hardcodes `mode: byo` regardless of provider custody — worth confirming intentional for managed providers. - `0086_perfect_whiplash.sql:6,20,96,153` — `ON DELETE cascade` 
  THREAD apps/server/src/services/connectors/legacy-connection-migration.ts:486 resolved=True outdated=False
    🔴 **Important:** the migration-failure catch discards the real error entirely — `void error;` with no `logger.*` call anywhere in this function (the file imports no logger at all). I traced every downstream reader of this result (`ConnectionStore.health()`, `ConnectorRegistry.migrationHealth()`, `apps/server/src/index.ts`) and none of them logs the failure either — `index.ts` never even calls `migrationHealth()`.  The comment above the `catch` correctly reasons about not leaking SQL/paths *over the wire* to REST callers, but that's a different concern from server-side observability. As written, if this backfill fails in production, connector reads/writes 503 for every user and there is **zero trace in the logs of why** — an operator has no way to diagnose it short of reproducing locally. Per this repo's own guidance on recovery-path code ("can this itself fail, and what happens if it doe
    REPLY doriancollier Added an error-level migration diagnostic with migration version, failing phase, category, and a bounded SQLite or enumerated invariant cause code. The logger never receives the raw exception, SQL, parameters, paths, or private provider references; the HTTP response remains scrubbed. A real malforme
  THREAD apps/server/src/services/connectors/attachment-store.ts:102 resolved=True outdated=True
    🟡 Nit: `deleteAgent()` is documented as the agent-unregister cascade but has no production caller anymore — the actual cascade now runs through `ConnectionStore.removeAgentAccess`'s own raw-SQL delete (wired via `registry.removeAgentAccess` in `agent-access-cleanup.ts`). This method is exercised only by its own test (`attachment-store.test.ts:81,91,97`, one of which literally comments `// the unregister cascade`, which is no longer true). Two independent implementations of "delete an agent's connector attachments" is a footgun if one is updated later and the other isn't — worth removing the dead one.
    REPLY doriancollier Removed the unused AgentConnectorAttachmentStore.deleteAgent method and its dedicated tests. Agent unregister cleanup remains owned by the canonical ConnectionStore.removeAgentAccess path; its cascade coverage is retained.  Addressed in `88cb81737808af03d0a018c5a816407d034dfa8f`. Independent REVIEW.
  THREAD apps/server/src/services/connectors/connection-store.ts:115 resolved=True outdated=True
    🟡 Nit: `mode` is hardcoded to `'byo'` for every provider instance regardless of `capabilities.custody` (which can be `'managed'`). Given the spec's later workstreams (P4: "tenant-scoped managed connector service") presumably key off this `mode` column to distinguish managed/BYO instances, worth confirming this is deliberate P1 scaffolding (no consumer reads `mode` yet) rather than an oversight that will need a backfill later.
    REPLY doriancollier Confirmed and documented the distinction: mode describes who supplies the provider instance, while custody describes where credentials are held. A locally configured Composio account is BYO even when Composio holds its credentials. P1 registers these local instances as BYO; the hosted service will e
  THREAD apps/client/src/layers/features/chat/model/use-session-submit.ts:246 resolved=True outdated=False
    🟡 Nit: this `cwd` (and `isNewSession`'s session-list lookup keyed by it) comes from the store-only `selectedCwdRef`/`useAppStore`, while `ChatPanel` computes the `agentPath`/`agentLookupPending`/`agentLookupFailed` it passes in from `useDirectoryState()` (URL-first: `urlDir ?? storeDir`). `useDirectoryState` only syncs `urlDir → store` in a `useEffect`, so right after a `?dir=` navigation there's a narrow window where the two disagree — `agentPath` could be computed for the new directory while this function's own `cwd`/`isNewSession` still reflect the old one. Likely unreachable today (a human send or the network-gated auto-send won't race the effect), but worth a comment noting the two hooks must stay in sync, or unifying on one cwd source.
    REPLY doriancollier Documented the URL-to-store synchronization requirement and added a real ChatPanel regression for the hard case: the URL agent is already in the exact query cache, the stream is hydrated, the store starts at a different directory, and agent lookup is forbidden. The first and only POST contains the U
  THREAD packages/db/drizzle/0086_perfect_whiplash.sql:260 resolved=True outdated=False
    🟡 Nit: the `ON DELETE cascade` FKs to `connections(id)` (lines 6, 20, 96, 153) can never actually cascade — this trigger unconditionally aborts any `DELETE` on `connections`. Harmless today, but worth a one-line comment on those FKs noting they're intentionally inert defense-in-depth rather than load-bearing, so a future reader doesn't assume cascading delete is how connection removal actually works.
    REPLY doriancollier Added a comment beside the tombstone-only delete trigger explaining that foreign keys still protect reference integrity, while disconnect retains the connection tombstone and therefore does not use their delete cascades.  Addressed in `88cb81737808af03d0a018c5a816407d034dfa8f`. Independent REVIEW.md
  THREAD apps/server/src/index.ts:2220 resolved=True outdated=False
    🟡 Nit: now that this call site switched to `invalidateProviderInstance`, the old `invalidateProvider(providerType)` method it used to call (`session-exposure.ts:505-513`) has no production callers left — a repo-wide grep for `.invalidateProvider(` only turns up its own unit test (`session-exposure.test.ts:361`). Its docstring ("Called when a provider is unregistered…") is now stale. Per this repo's "when something is superseded, remove it" convention, that method and its dedicated test look like dead code the PR should drop rather than leave alive only because a test still calls it.
    REPLY doriancollier Removed the unused type-wide invalidateProvider method and its dedicated test. All production cleanup uses invalidateProviderInstance. Updated the two historical direct-connect specification/task references to record that P1 superseded the former type-wide behavior with exact-instance invalidation. 

### PR #1618 [M 50-249] fix(skills): skills with nested metadata and the writing-for-humans skill load again (DOR-1828)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-06T15:36:20Z
own commits after: ['chore(changelog): one fragment for DOR-1828, written for a human']
VERDICT: 1 important, 0 nits  Clean, well-scoped fix: widens `SkillFrontmatterSchema.metadata` from `Record<string,string>` to `Record<string,unknown>` so third-party skills nesting objects under `metadata` (and the `writing-for-humans` skill, whose own unquoted `description:` colon broke YAML parsing) stop being silently dropped. The new repo-wide `repo-skill-corpus.test.ts` walks all 31 tracked `SKILL.md` files through the production parser/schema, which is exactly the kind of regression guard this bug needed, and the corpus-size assertion (>10) rules out a zero-subject pass. The harness test's `version` narrowing to `typeof version === 'string'` correctly follows from the schema widening, and no other consumer of skill `metadata` assumes string values.  One blocking issue: one of the two changelog fragments still carries the uncurated `dorkos-changelog:seeded` marker and duplicates the hand-written fragment — flagged inline, will fail the required `changelog_backfill.py --validate` check as-is.
  THREAD changelog/unreleased/260906-150915-skills-with-nested-metadata-and-the-writ.md:8 resolved=False outdated=True
    🔴 Important: this fragment is a leftover auto-seeded duplicate of `changelog/unreleased/260906-150645-skills-with-nested-settings-no-longer-vanish.md`, and it still carries the `dorkos-changelog:seeded` marker uncurated. Per `changelog/README.md#seeded-fragments`, `changelog_backfill.py --validate` fails on any fragment that still contains this marker, with no `skip-changelog` bypass — this is a required, unconditional CI step, so as-is this PR's changelog check will fail. Since the change is already covered by the hand-written fragment, delete this whole file instead of curating it.

### PR #1635 [XL 1000+] feat(connectors): enforce brokered execution across DorkOS
labels=['review:deep'] imp=4 change=rebase/merge-main only rereviewed=True verdict_t=2026-09-06T22:46:54Z
own commits after: []
VERDICT: 4 important, 4 nits  This is a large, well-executed rework of connector execution into a brokered model. Six parallel focused passes covered execution/authorization, principal/authority-cleanup, management review/action services, core connector services (with a full dangling-reference sweep), client UI, and server wiring/e2e. The core authorization design is solid: `authorization-service.ts` and `execution-broker.ts` recheck canonical grants live on every call with a synchronous re-check closing the TOCTOU window right before dispatch, principal resolution fails closed everywhere checked, and the dangling-reference sweep for deleted files/hooks/components (`use-agent-connectors`, `AgentAccounts`, `nango-proxy-mcp`) came back clean — the five previously-privileged capabilities were correctly relocated to the new brokered execution surface, not dropped. No Hard Rule violations (SDK confinement, `os.homedir()`) were found anywhere in the reviewed files.  Found four real Important-severity bugs, all verified by reading the code directly: - `connector-management.ts:236` — `GET /program/reviews/:id` hangs (no response sent) for a normal owner/operator session instead of returning an error. - `management-review-service.ts:317` — `list()` throws entirely (409) if any single review is mid-resolution, hiding the whole list instead of just that item. - `agent-access-cleanup.ts:29-38` — agent-removal cleanup calls `authorityCleanup.revokeAgent` before `registry.removeAgentAccess` with no
  THREAD apps/server/src/routes/connector-management.ts:240 resolved=True outdated=False
    🔴 Important: `resolveReviewRequester` falls back to `resolveConnectorOperator` (line 170) whenever there is no `Authorization` header, which returns a truthy `{kind: "operator", ...}` for any ordinary cookie-authenticated owner session (or, with login disabled, any trusted-origin browser request). This handler only checks `requester.kind !== "program"` and returns with no response in that case — the request never gets a `res.status()/res.json()` call and hangs until proxy/socket timeout. Since this route has no `Authorization` header requirement documented anywhere client-side, a normal owner browsing to this endpoint (or a future client wiring it up) will hang indefinitely. The existing test only covers `Authorization: Bearer invalid` (401) and an agent header (403), not the header-omitted operator-session case.
    REPLY doriancollier The program review status route now sends 401 connector_program_credential_required when the requester is an owner session without a verified API key, instead of returning without a response. Route regressions cover the header-omitted case alongside invalid credentials and agent identity.  Verified 
  THREAD apps/server/src/services/connectors/management-review-service.ts:323 resolved=True outdated=False
    🔴 Important: `list()` maps every matching row through `publicItem()`, which throws `ConnectorManagementReviewError("review_resolving")` (see lines 450-457) whenever a row is `approved` with no `resolutionJson` yet and is present in `activeResolutions`. That throw is not caught here, so it propagates out of `list()` and the route (`GET /reviews`, connector-management.ts:245-254) turns it into an HTTP 409 for the *entire* list. One connect/disconnect approval that is mid-flight (awaiting `provider.startConnect()`/`provider.disconnect()`) is enough to make the whole review list disappear behind an error for the owner instead of just that one item being flagged as resolving. No test exercises `list()` while a resolution is in flight.
    REPLY doriancollier The correction adds an explicit resolving projection for active approvals across owner list/detail and requester-safe status. Duplicate decisions return that existing state without applying again. Once the process no longer owns an unfinished action, the result remains outcome_unknown. Regression te
  THREAD apps/server/src/services/connectors/agent-access-cleanup.ts:29 resolved=True outdated=False
    🔴 Important: `removeAccess` calls `deps.authorityCleanup.revokeAgent` (line 31) before `deps.registry.removeAgentAccess` (line 38), with nothing between them to guarantee the second step still runs if the first throws. Mesh's `onUnregister` cascade (packages/mesh/src/mesh-agent-management.ts:462-468) wraps each callback in a try/catch that only logs a warning — there is no retry. So if `revokeAgent` throws for any reason, `removeAgentAccess` never executes and the removed agent's durable `connectionOperationGrants`, `agentConnectionAttachments`, `sessionConnectionOverrides`, and `connectorEventSubscriptions` are left live with no path back to cleanup. Contrast with the disconnect path (`connection-store.ts` `recordDisconnect`), which revokes durable grants first and is safe even if a later step fails — this path has the ordering reversed.
    REPLY doriancollier Agent removal independently attempts the migration revocation marker, canonical grant cleanup, and pending/runtime authority cleanup, then rethrows the first failure. Regression tests force each stage to fail and prove the remaining stages still run; the durable fallback remains effective when canon
  THREAD apps/client/src/layers/entities/connectors/model/use-connector-management.ts:46 resolved=True outdated=False
    🔴 Important: `useResolveConnectorManagementReview` (here) and `useApplyConnectorReconciliation` (line 78-80) only invalidate `connectorKeys.accounts()`/review lists, never `connectorKeys.sessions()`. Compare with the sibling hook `use-connector-accounts.ts:33-34`, which invalidates both `accounts()` and `sessions()` after a mutation. Approving a `set_agent_access`/`remove_agent_access` review or applying a reconciliation changes which agents have access, but per-session connector views (`use-session-connectors.ts`, keyed by `connectorKeys.session()`) will keep showing stale access badges until something else happens to refetch them.
    REPLY doriancollier Both management-review resolution and reconciliation application now invalidate the session connector query prefix in addition to accounts/reviews. Hook regressions cover both mutations; removing the new invalidations causes the tests to fail.  Verified on e7cd602998da9f49475bb50eb12920f114b5fad3. I
  THREAD apps/client/src/layers/features/connections/ui/ManagementReviews.tsx:84 resolved=True outdated=True
    🟡 Nit: the "Recent decisions" section only renders when `resolvedItems.length > 0`, and `resolvedItems` defaults to `[]` on `resolved.isError` (line 45). A failed fetch for resolved reviews is therefore indistinguishable from "no resolved reviews yet" — no `QueryErrorState`/retry is shown, unlike the pending list right above it which handles `pending.isError` explicitly.
    REPLY doriancollier Recent-decisions fetch failures now show a retryable QueryErrorState instead of disappearing as an empty history. The owner UI regression checks the failure and retry path.  Verified on e7cd602998da9f49475bb50eb12920f114b5fad3. Independent review passed, and the [GitHub re-review](https://github.com
  THREAD apps/server/src/routes/connectors.ts:181 resolved=True outdated=True
    🟡 Nit: `deps.authorityCleanup?.revokeConnection(...)` uses optional chaining, so if `authorityCleanup` is ever left unwired for this route, pending-authority cleanup on disconnect silently no-ops instead of failing loudly. Production wiring in index.ts always supplies it today, but the optionality invites a future misconfiguration to fail silently.
    REPLY doriancollier The connector router now requires the authority-cleanup dependency and calls it directly on disconnect. Its route fixture supplies the port and asserts the exact cleanup invocation.  Verified on e7cd602998da9f49475bb50eb12920f114b5fad3. Independent review passed, and the [GitHub re-review](https://g
  THREAD apps/e2e/tests/connections/owner-management.ts:71 resolved=True outdated=False
    🟡 Nit: several locators in this file match on user-facing copy rather than `data-testid` — e.g. dialog names like 'Pause work'/'Disconnect work', and text like 'Approved and applied' or 'This will remove this account from 1 agent.' (lines 71-75, 103-105, 159-160). Per this repo's precedent (PR #575), a copy change in ConnectionAccessDialog/ManagementReviews for unrelated reasons (wording, translation) will silently break these tests. Prefer stable `data-testid`s for the dialog/outcome assertions.
    REPLY doriancollier Owner-management browser coverage now uses stable IDs for dialogs, rows, impact, custody and outcome state instead of matching those presentation strings. The page object uses the same dialog identifiers.  Verified on e7cd602998da9f49475bb50eb12920f114b5fad3. Independent review passed, and the [GitH
  THREAD apps/server/src/services/connectors/authority-cleanup-port.ts:12 resolved=True outdated=True
    Nit: the ownedSessionIds field is required by this port and computed and passed by callers (management-action-service.ts around lines 107-113), but the cleanup service implementation never reads it - it is dropped on the floor. Not currently exploitable since connection-store.ts revokes the session-scoped grants directly elsewhere, but it is a dead parameter, and the existing test for it passes a value without asserting any effect from it. Either wire it up or drop it from the port.
    REPLY doriancollier Removed ownedSessionIds from the cleanup port and its unused caller/test plumbing. Exact agent/connection scoping remains on the canonical cleanup selectors.  Verified on e7cd602998da9f49475bb50eb12920f114b5fad3. Independent review passed, and the [GitHub re-review](https://github.com/dork-labs/dork

### PR #1638 [L 250-999] fix(server): local installs and template addresses answer to the same boundary (DOR-1825)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-06T23:06:38Z
own commits after: []
VERDICT: 1 important, 0 nits  The template-address allowlist (`isSupportedTemplateSource`) and its regexes check out against every case in the new test matrix, and the 400-vs-500 status mapping in `agent-creator.ts` is correctly scoped to only the `UNSUPPORTED_SOURCE` code. One important gap left inline: the boundary fix for `file://` addresses covers package installs (`PackageFetcher.fetchFromGit`) but not marketplace source registration/refresh (`fetchMarketplaceJson`/`fetchDorkosSidecar`), which read local `file://` paths with no `validateBoundary` call at all — the same bug class this PR closes elsewhere in the same file.
  THREAD apps/server/src/services/marketplace/package-fetcher.ts:563 resolved=False outdated=False
    🔴 Important: This closes the boundary hole for package installs (`fetchFromGit`'s `file://` branch), but the same class of bug survives one method away.  `fetchMarketplaceJson` (~line 315) and `fetchDorkosSidecar` (~line 350) also branch on `isFileUrl(source.source)` and call `readLocalMarketplaceJson`/`readLocalDorkosSidecar`, which convert straight to a filesystem path via `fileUrlToPath` and `readFile` it — with no `validateBoundary` call anywhere in that path. `assertLocalPathAllowed`/`validateBoundary` are never invoked there.  This is reachable: `POST /api/marketplace/sources` lets an operator register a `MarketplaceSource` with a `file://` address, and `isSupportedMarketplaceSourceUrl` (`source-url-policy.ts:205-210`) explicitly allows any `file://` path with only a "does it parse" check — no boundary check at registration either. A marketplace source pointed at `file:///etc` (or 

### PR #1676 [S 10-49] docs(audits): six gaps the first pulse found
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-07T21:22:21Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light — quick pass only)  🔴 Important: `.claude/commands/ui-audit/pulse.md`'s new contract 1 ("a lens missing from it is skipped and named in the report; the pulse stops only when the file itself is absent") contradicts the Bootstrap rule it cites in `.agents/skills/auditing-ui/SKILL.md` §1 (lines 72-77, unchanged by this PR), which still says a lens missing from stamps causes pulse to stop, not skip. See inline comment for detail.
  THREAD .claude/commands/ui-audit/pulse.md:18 resolved=False outdated=False
    🔴 This contradicts the "skill §1" section it cites. `.agents/skills/auditing-ui/SKILL.md`'s Bootstrap rule (lines 72-77, unchanged by this PR) still reads: "`diff` scoping and `/ui-audit:pulse` require an existing `stamps.json`. With no stamps file, **or for a lens missing from it**, they **stop and say so**, naming the fix: run `full` ... to establish the baseline." That is the opposite of what this PR makes `pulse.md` say — that a lens missing from stamps is merely "skipped and named in the report" while only a wholly-absent file stops the pulse.  Since these markdown files are the executable procedure (not just prose), an agent following `pulse.md` will silently skip an un-stamped lens while the doc it cites for justification says pulse should refuse and stop. Either the Bootstrap rule in SKILL.md needs the matching update, or this contract change needs to be reverted — right now they

### PR #1690 [XL 1000+] feat(harness): every kind in .claude/ is listed, and none stays silent (DOR-1845)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-08T07:49:30Z
own commits after: []
VERDICT: 1 important, 0 nits  Extremely thorough PR: adds a full `.claude/`-source inventory to the harness engine, with property-based tests explicitly targeting the omission/divergence/zero-subject-pass failure modes this repo's reviews call out by name, and self-documented fixes for several bugs found in earlier review rounds (recursion, symlinked directories, subagent identity, the gray-matter cache trap).  One real finding survived: `planClaudeSkillsDirSkill`'s `alsoCanonical` branch (`plan/source-artifacts.ts` ~288-293) hardcodes `drop` for every non-claude-code harness when a skill exists in both `.agents/skills` and `.claude/skills`, without checking that harness's own vendor facts. OpenCode, Cursor and Copilot all document reading `.claude/skills` directly (per this very PR's `vendor-facts/index.ts`), so they would actually see that second copy rather than have it "dropped" — the stale-drop shape (SK-05) the rest of this module exists to fix. A test in `claude-only-skills.test.ts` already pins the wrong answer for OpenCode, contradicting the sibling test in the same file that gets it right for the unlisted, single-root case. Left as an inline comment with the specifics.
  THREAD packages/harness/src/plan/source-artifacts.ts:288 resolved=False outdated=False
    🔴 **Important:** The `alsoCanonical` branch always answers `drop` for every non-`claude-code` harness, without consulting that harness's vendor facts — unlike the general branch a few lines below (295-335), which correctly checks `skillsFactsFor(harness).readPaths.project.includes(CLAUDE_SKILLS_DIR)` before deciding.  But OpenCode, Cursor and Copilot's own `readPaths.project` in `vendor-facts/index.ts` *all* include `.claude/skills` (Copilot even lists it *before* `.agents/skills`). So for a skill that exists under the same name in both `.agents/skills` and `.claude/skills`, those three harnesses are told the `.claude/skills` copy was "dropped" — implying they never see it — when their own documented read paths say they would load it (possibly instead of the canonical copy). This is exactly the stale-drop shape (SK-05) the rest of this module exists to fix, reintroduced here.  It's also 

### PR #1700 [M 50-249] fix(server): scheduled tasks start at the repo root in dev too (DOR-1859)
labels=['review:light'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-08T12:10:00Z
own commits after: ['chore(changelog): fold the seeded stub into the curated fragment']
VERDICT: 1 important, 0 nits  The `resolve-root.ts` fix itself is correct and well-tested: `findWorkspaceRoot` walks up from the module's own directory for `pnpm-workspace.yaml` (the one file that exists only at the repo root), which correctly replaces the old fixed three-hop climb across the tsx/dist/bundled layouts, and the fallback chain still lets CLI/desktop-set `DORKOS_DEFAULT_CWD` short-circuit the walk entirely in packaged builds. Flagged one issue: a leftover unrewritten `dorkos-changelog:seeded` marker in a duplicate changelog fragment that will fail CI's changelog validate gate.
  THREAD changelog/unreleased/260908-115833-scheduled-tasks-start-at-the-repo-root-i.md:8 resolved=False outdated=True
    🔴 Important: this fragment still contains the unrewritten `dorkos-changelog:seeded` marker comment. Per `changelog/README.md`, `.claude/scripts/changelog_backfill.py --validate` fails on any fragment that still contains it, with no `skip-changelog` bypass, and that validate step runs unconditionally in CI — so this will fail the build as-is.  This also looks like a duplicate of `changelog/unreleased/260908-115817-scheduled-tasks-start-at-the-repo-root.md`, which already covers the same commit with a properly rewritten bullet. Recommend just deleting this fragment rather than editing it.

### PR #1716 [L 250-999] fix(chat): preserve task order across history responses
labels=[] imp=1 change=no change at all rereviewed=True verdict_t=2026-09-08T20:04:02Z
own commits after: []
VERDICT: 1 important, 0 nits  The `use-task-state.ts` rewrite (order-based fetch/live-fold reconciliation replacing the timestamp-based guard) is well-tested and internally consistent — traced the new `taskHistoryOrder` sequencing against the new test cases and it holds up, including the remount and out-of-order-completion races.  🔴 Important: `contributing/environment-variables.md` documents `VITEST_MAX_WORKERS` as limiting Vitest worker count, and `turbo.json` passes it through, but nothing reads it anywhere in the repo (confirmed against vitest/@vitest source and all 21 `vitest.config.ts` files) — unlike `VITEST_RETRY`, which every config wires explicitly. The documented command is a no-op. See inline comment.
  THREAD contributing/environment-variables.md:32 resolved=True outdated=False
    🔴 Important: `VITEST_MAX_WORKERS` is documented here (and passed through in `turbo.json:7`) as limiting Vitest to N workers, but nothing in the repo reads it. It's not in vitest or `@vitest/*` (checked `node_modules`), and none of the 21 `vitest.config.ts` files reference it — contrast with `VITEST_RETRY`, which every one of those configs wires explicitly via `retry: process.env.VITEST_RETRY ? Number(process.env.VITEST_RETRY) : 0` (e.g. `apps/server/vitest.config.ts:387`). Running `VITEST_MAX_WORKERS=2 pnpm verify` on a busy machine today does nothing — the exact scenario this doc section is written for. Either wire it into each `vitest.config.ts` (`poolOptions.threads.maxThreads`/`minThreads` or similar), or drop the doc and the `turbo.json` passthrough until it's implemented.
    REPLY doriancollier This finding is contradicted by the pinned dependency and the executed test. Vitest 4.1.11 reads this variable itself, so each project config does not need to duplicate the reader. In the installed `vitest/dist/chunks/coverage.DM_a_rWm.js:380`, config resolution contains:  ```js if (process.env.VITE
    REPLY doriancollier Resolved as a false positive after independent adjudication of both automated comments. A separate reviewer independently checked the pinned Vitest4.1.11 native environment reader and the sealed real resolved-worker probe, and confirmed the evidence in [the earlier reply](https://github.com/dork-lab

### PR #1716 [L 250-999] fix(chat): preserve task order across history responses
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-08T20:08:12Z
own commits after: []
VERDICT: 1 important, 0 nits  Re-review: head commit unchanged since my last pass (`da7633bee`). The `use-task-state.ts` order-based fetch/live-fold rewrite was already reviewed and holds up.  🔴 Important (still unaddressed): `VITEST_MAX_WORKERS` is documented (`contributing/environment-variables.md:32`) and passed through (`turbo.json:7`), but nothing reads it — no `vitest.config.ts` sets worker concurrency from it, unlike `VITEST_RETRY` which is wired into all 21 configs. The documented command is a no-op. See inline comment.
  THREAD contributing/environment-variables.md:32 resolved=True outdated=False
    🔴 Important: `VITEST_MAX_WORKERS` is documented here (and passed through in `turbo.json:7`) as limiting Vitest to N workers, but nothing in the repo reads it. It's not in vitest or `@vitest/*` (checked `node_modules`), and none of the 21 `vitest.config.ts` files reference it — contrast with `VITEST_RETRY`, which every one of those configs wires explicitly via `retry: process.env.VITEST_RETRY ? Number(process.env.VITEST_RETRY) : 0` (e.g. `apps/server/vitest.config.ts:387`). Running `VITEST_MAX_WORKERS=2 pnpm verify` on a busy machine today does nothing — the exact scenario this doc section is written for. Either wire it into each `vitest.config.ts` (`poolOptions.threads.maxThreads`/`minThreads` or similar), or drop the doc and the `turbo.json` passthrough until it's implemented.
    REPLY doriancollier This finding is contradicted by the pinned dependency and the executed test. Vitest 4.1.11 reads this variable itself, so each project config does not need to duplicate the reader. In the installed `vitest/dist/chunks/coverage.DM_a_rWm.js:380`, config resolution contains:  ```js if (process.env.VITE
    REPLY doriancollier Resolved as a false positive after independent adjudication of both automated comments. A separate reviewer independently checked the pinned Vitest4.1.11 native environment reader and the sealed real resolved-worker probe, and confirmed the evidence in [the earlier reply](https://github.com/dork-lab
  THREAD contributing/environment-variables.md:32 resolved=True outdated=False
    🔴 Important (still unaddressed from prior review): this documents `VITEST_MAX_WORKERS=2 pnpm verify` as limiting Vitest worker concurrency, and `turbo.json:7` passes it through, but nothing in the repo actually reads it — no `vitest.config.ts` sets `maxWorkers`/`minWorkers` from this env var. Compare to `VITEST_RETRY`, which every one of the 21 `vitest.config.ts` files wires explicitly (`retry: process.env.VITEST_RETRY ? Number(process.env.VITEST_RETRY) : 0`). As written, the documented command is a no-op — it changes nothing about worker count. Either wire `VITEST_MAX_WORKERS` into the configs (e.g. `pool: 'threads'` with `poolOptions.threads.maxThreads`, or vitest's own `--max-workers` CLI translation) or drop the doc/turbo passthrough.
    REPLY doriancollier Resolved as a false positive after independent adjudication of both automated comments. A separate reviewer independently checked the pinned Vitest4.1.11 native environment reader and the sealed real resolved-worker probe, and confirmed the evidence in [the earlier reply](https://github.com/dork-lab

### PR #1719 [XL 1000+] docs(specs): harness-sync-global — decomposed into four slices (DOR-1857)
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-08T20:50:16Z
own commits after: []
VERDICT: 1 important, 0 nits (review:light — quick pass, Important findings only)  Reviewed the two new task-decomposition files (`03-tasks.json` / `03-tasks.md`, pure additions, no code). One finding: several `packages/harness/src/plan/projector.ts` line citations (tasks 2.2 and 3.1) point past the end of that 602-line file — verified against the current tree. Left inline with the correct line numbers.
  THREAD specs/harness-sync-global/03-tasks.json:97 resolved=False outdated=False
    🔴 Important: The `packages/harness/src/plan/projector.ts` line citations in this task doc don't match the file on this tree. `projector.ts` is 602 lines total, but:  - Task 2.2 cites "the partition at `:682-690` and the drop at `:821-827`" — both past EOF. The actual partition (`globalInstalls`/`unsupportedType`/`projectable`) is at `projector.ts:432-438`, and the actual drop (`dropWholePlugin(plugin, 'global-scope install; …')`) is at `projector.ts:587-593`. - Task 3.1 cites "`buildPlan` runs fourteen stages unconditionally, in order (`plan/projector.ts:660-836`)" — also past EOF. `buildPlan` actually spans roughly `projector.ts:367-602`.  Since this doc's whole design relies on precise `file:line` anchors to drive later automated implementation tasks, these stale citations will send an implementer to the wrong (nonexistent) location for two of the four tasks in slice A1/A2. The same wr

### PR #1727 [XL 1000+] feat(client): the Skills page shows every skill and which of your tools can see it (DOR-1894)
labels=[] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-09T00:36:30Z
own commits after: []
VERDICT: 1 important, 0 nits  The Skills-page rework (deleting `SkillPacksList`/`ScopeBadge` in favor of `entities/harness`, trimming the harness barrel, unit-labeling the drop/notice badges, fixing the command-wrap) is clean — ran the dangling-reference sweep on every removed export (`harnessKeys`, the lib helpers/types, `ADOPTABLE_ADVICE`, `SkillPacksList`, `ScopeBadge`, the `useInstalledPackages` `options` param) and found no surviving references outside the slice; FSD import direction is respected throughout.  One 🔴 Important left inline: `useManagedAgentFacts` reads `harnessStatus.counts.skills` without gating on `state === "ready"`, so once the Skills page has been opened on a project whose manifest is unreadable or in an unavailable build, the profile row will show "Skills: 0 skills" — the exact false-zero this PR set out to eliminate, just reached through a different state.
  THREAD apps/client/src/layers/features/profile/model/use-managed-agent-facts.ts:108 resolved=False outdated=False
    🔴 **Important:** `skills: harnessStatus?.counts.skills ?? null` doesn't check `harnessStatus.state === 'ready'` before trusting `counts.skills`.  Per the schema's own contract (`packages/shared/src/harness-schemas.ts:253-254`, and the fixtures at `apps/client/src/layers/entities/harness/__fixtures__/harness-status.ts:212-229`), every non-`ready` state (`not-set-up`, `unreadable`, `unavailable`) answers with `counts.skills: 0` — not "unknown", literally zero by convention. Since `useHarnessStatusCached` just reads whatever `useHarnessStatus` last put in the cache regardless of `state`, opening the Skills page once on a project whose manifest is unreadable (bad JSON) or in an Obsidian/embedded build (`unavailable`) will populate the cache, and this line will then read `counts.skills = 0` and render `countValue(0, ...)` → **"Skills: 0 skills"** on the profile row.  That's the exact "Skills 

### PR #1748 [XL 1000+] feat(harness): the adopt planner refuses by rule and names the way out (DOR-1943)
labels=['skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-09T13:24:18Z
own commits after: []
VERDICT: 1 important, 1 nit  **🔴 Important** — `packages/harness/src/adopt/plan.ts:80-116`: neither explicit nor auto mode dedupes candidates by name across different harness-owned roots (e.g. `.claude/skills/foo` and `.opencode/skills/foo`, both non-canonical, non-declared — nothing in `read.ts` collapses `owned` by name). Explicit mode (`plan.ts:81`, `.find()`) silently resolves to whichever candidate the inventory lists first; auto mode (`decide()`, `plan.ts:102-116`) can plan two `AdoptMove`s with an identical `to` target, since `targetState` is read once before either move lands, so R4 cannot catch a same-run collision. Posted inline with detail.  **🟡 Nit** — `meta/harness-sync-capabilities.md:109` (unchanged by this diff, so not inline-commentable): SRC-10 still says "no `adopt/` module, no CLI subcommand" as part of its "not built" reasoning, but this PR adds `packages/harness/src/adopt/`. The overall verdict is still correct (unwired, no CLI, no `resolveSourceRoots` producer), but that specific clause is now stale.  Everything else here is excellent: the refusal ladder, allowlist, and refusal sentences are exhaustively tested (frozen-literal assertions, a property test that auto never moves what explicit would refuse, a no-filesystem-import guard on the pure planner, a value-never-leaks-into-a-sentence test), the reader/status-model duplication is pinned by a cross-package agreement test, and the `blockedWritePath` extraction from `findBlockedWritePaths` preserves the original
  THREAD packages/harness/src/adopt/plan.ts:88 resolved=False outdated=False
    🔴 Neither explicit nor auto mode accounts for two candidates sharing a `name` across different harness-owned roots (e.g. `.claude/skills/foo` and `.opencode/skills/foo`, neither canonical nor declared — nothing dedupes `owned` by name in `read.ts`, and `HARNESS_OWNED_SKILL_ROOTS` deliberately spans multiple tool-owned roots).  - **Explicit mode** (`plan.ts:81`): `input.candidates.find((entry) => entry.name === request.name)` silently returns whichever candidate happens to be first in inventory order. A person running `dorkos harness adopt foo` meaning the OpenCode-authored `foo` could silently get the unrelated Claude-authored `foo` moved instead, with no indication a second candidate existed. - **Auto mode** (`decide()`, `plan.ts:102-116`): iterates every candidate independently; if both same-named candidates pass R2–R7, it produces **two `AdoptMove`s with an identical `to`** (`.agents/

### PR #1753 [L 250-999] fix(site): make feedback Linear issues readable, parseable, and forgery-resistant
labels=['skip-changelog'] imp=2 change=new PR commits after finding rereviewed=False verdict_t=2026-09-09T22:21:04Z
own commits after: ['chore(changelog): drop auto-seeded fragment — internal Linear triage formatting, no user surface']
VERDICT: 2 important, 0 nits  This PR replaces a pre-rendered `diagnosticsSummary` string with raw `diagnostics`/`transcriptExcerpt` fields, moves rendering (fencing, newline-collapsing, truncation) into `linear.ts`, and adds `surface`/`submissionUrl`. Good, well-tested hardening against forged `Key:` metadata lines in the reporter-supplied identity fields.  🔴 Two Important findings posted inline: - `attachmentUrls` is interpolated into the description without the same `oneLine()` treatment as `reporterName`/`contact`/`route`, and it is the *last* section in the description — so an embedded newline (which `z.string().url()` does not reject, per WHATWG URL-parser whitespace stripping) lets a forged `Key:` line land after the genuine identity block, undermining this PRs own hardening. - The new changelog fragment still contains the unedited `dorkos-changelog:seeded` marker comment, which `changelog_backfill.py --validate` fails on unconditionally (no `skip-changelog` bypass per `changelog/README.md`) — this will red the required changelog-validity check in the merge queue as-is.  No nits beyond those.
  THREAD changelog/unreleased/260909-221414-harden-the-feedback-description-against.md:8 resolved=False outdated=True
    🔴 Important: this fragment still carries the unedited `dorkos-changelog:seeded` marker comment. Per `changelog/README.md` ("Seeded fragments"): `.claude/scripts/changelog_backfill.py --validate` fails on **any** fragment that still contains this marker, with **no `skip-changelog` bypass** — and `--validate` is the step CI runs unconditionally. The PR's `skip-changelog` label only affects the coverage gate (`--check`), not validity.  Either rewrite the bullet and delete this comment, or delete the whole fragment if the change is deemed non-user-facing (which is what the `skip-changelog` label suggests was intended here).
  THREAD apps/site/src/lib/feedback/linear.ts:220 resolved=False outdated=False
    🔴 Important: `attachmentUrls` (rendered a few lines below via `` `- ${url}` ``, unchanged by this PR) is the one field this hardening pass didn't route through `oneLine()` — and it's still interpolated raw.  `z.string().url()` (`apps/site/src/app/api/feedback/route.ts:98`) validates via the `URL` constructor, which per the WHATWG URL spec strips embedded ASCII tab/CR/LF from the input during parsing but does **not** alter the string Zod stores — so `"https://x/\nSubmission: https://evil.example/steal"` passes `.url()` and survives into this array with the raw `\n` intact. Mapped unescaped, that newline breaks the bullet into two lines, and the second becomes its own `Submission:` line appended *after* the real identity block (the Attachments section is the last one in `sections`).  That's exactly the class of attack the new `oneLine()`/fencing in this PR defends against for `reporterName

### PR #1764 [L 250-999] fix(connections): bound managed sign-in waits (DOR-1965)
labels=[] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-09-10T17:49:55Z
own commits after: ['fix(connections): bound managed sign-in waits (DOR-1965)']
VERDICT: 1 important, 0 nits  This PR bounds the previously-unbounded managed-connector sign-in path with a well-ordered, decreasing timeout chain across layers (client 75s → server CloudLink 60s → site route 45s → Composio link-create 15s), and adds solid tests confirming private account details never leak into logs. The ambiguous-outcome (`start_unknown`) handling is consistent with the existing conservative design elsewhere in the codebase.  🔴 One regression flagged inline: the new nested `try/catch` in `ComposioManagedAccountClient.getToolkitAuthentication` (`packages/connector-providers/src/composio/managed-account-client.ts:216-225`) swallows `ComposioAuthenticationSetupError` (the `unsupported_metadata` case) and remaps it to a generic `invalid_provider_response`, discarding the specific safe message and breaking the `instanceof ComposioAuthenticationSetupError` check in `authentication-service.ts:366`. Untested, so it is currently silent.
  THREAD packages/connector-providers/src/composio/managed-account-client.ts:226 resolved=False outdated=False
    🔴 This new inner `try/catch` swallows `ComposioAuthenticationSetupError` and remaps it to a generic `invalid_provider_response`.  `normalizeComposioToolkitAuthentication` deliberately throws `ComposioAuthenticationSetupError('unsupported_metadata')` for the "unrecognized keys" case (`authentication-configuration.ts:112-114`), with a specific, already-safe message ("This service declares account-field constraints DorkOS does not support yet."). Before this PR that error propagated unchanged through the outer `catch` → `rethrowSafe`, which special-cases `error instanceof ComposioAuthenticationSetupError` (line 157-161) to rethrow it as-is.  Now the bare `catch {}` here catches it first and converts it to `ComposioManagedAccountError('invalid_provider_response', …)`, so it's no longer an instance of `ComposioAuthenticationSetupError` by the time it reaches `authentication-service.ts:366` (`

### PR #1769 [M 50-249] fix(harness): recheck Windows links after removal races
labels=['review:light'] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-09-11T01:13:15Z
own commits after: []
VERDICT: 1 important, 0 nits  🔴 Important — `packages/harness/src/apply/global-apply.ts:509` (not part of this diff, so could not be anchored inline): `applyGlobalSymlink` still has the same unguarded `rmSync(absTarget, { force: true })` that `apply.ts`'s `applySymlink` just had fixed in this PR. Its own docstring says it is "the same shape as the project apply's" (`global-apply.ts:473-476`), but the Windows removal-race retry added here (`apply.ts:207-230`) was not mirrored to it. On Windows, a concurrent writer replacing this entry between the `pathExists`/`linkMatchesPlan` check and this `rmSync` can still throw `EPERM` and abort the whole global sync — the exact race DOR-1981 fixes for the project tier is left open for the global tier, where concurrent writers to the same home directory are a scenario this same file's header explicitly designs around.  Otherwise the fix in `apply.ts`/`windows-links.ts` is sound: traced the new retry loop (attempt vs. removalRetries counters, 64-retry/~1s budget, EPERM+win32-only gating, EACCES/EIO passthrough, POSIX no-op) against all seven new tests in `symlink-removal-race.test.ts` and each one's expected call counts and outcomes match the code exactly.

### PR #1779 [M 50-249] fix(tasks): a full disk no longer takes the whole server down (FB-17)
labels=[] imp=2 change=no change at all rereviewed=False verdict_t=2026-09-11T02:54:19Z
own commits after: []
VERDICT: 2 important, 0 nits  Solid, well-tested fix for the crash (ENOSPC in the heartbeat timer no longer takes the process down, and the write/heartbeat paths degrade to follower correctly). Two gaps in the `createExclusive()` corner, both filed inline:  - 🔴 `createExclusive()` writes directly to the real lock path with `{ flag: "wx" }` instead of temp-file + rename, so a write failure partway through (e.g. ENOSPC) can leave a corrupt file at `lockPath` that `read()` treats as unparseable (`null`) forever, while a future `createExclusive()` hits `EEXIST` on the same leftover file and is treated as "lost the race." Net effect: leader election can permanently deadlock for that `dorkHome`, even after disk space frees up — silent and worse than the crash being fixed. - 🔴 `reportedWriteFailure` is reset only in `write()`s success path, never in `createExclusive()`s. Since `release()` unlinks the lock on clean shutdown, ordinary restarts go through `createExclusive()`; if that path fails once and later succeeds, the flag stays stuck `true`, silencing every subsequent full-disk report the docstring promises gets logged again.  No dangling-reference or Hard Rule issues — this PR only touches `scheduler-lock.ts`, its test, and a changelog fragment.
  THREAD apps/server/src/services/tasks/scheduler-lock.ts:218 resolved=False outdated=False
    🔴 **`createExclusive()` can permanently deadlock leader election after a single ENOSPC, even once the disk recovers.**  Unlike `write()`, this writes directly to the real `lockPath` with `{ flag: 'wx' }` rather than temp-file + rename. `wx` (`O_CREAT|O_EXCL`) creates the directory entry on `open()` before any data is written, so if the subsequent `write(2)` fails with `ENOSPC` (a very plausible near-full-disk shape — space runs out mid-write, not before `open`), a truncated/empty file is left behind at `lockPath` itself.  On the next `tryAcquire()` (this branch is only reached when `existing === null`, i.e. `read()` at line 106/267 failed to parse the on-disk content — which is exactly what a truncated file produces, see `read()` at line 267-284 returning `null` for anything unparseable): `existing` is still treated as `null`, so `createExclusive()` runs again — but now `writeFileSync(..
  THREAD apps/server/src/services/tasks/scheduler-lock.ts:198 resolved=False outdated=False
    🔴 **`reportedWriteFailure` is only reset on a successful `write()` (line 250), never on a successful `createExclusive()` (line 210-213) — so it can get stuck `true` forever, silencing every future disk-full report.**  `release()` unlinks the lock file on clean shutdown (called from `task-scheduler-service.ts`), so an ordinary restart routes the next boot's `tryAcquire()` through the `existing === null` → `createExclusive()` path, not `write()`. Sequence that breaks the "once per spell" contract this docstring promises ("The counter resets on the next successful write, so a second outage is reported again"):  1. Disk is briefly full at boot → `createExclusive()` fails → `onWriteFailed` logs once, sets `reportedWriteFailure = true`. 2. Disk recovers → next `tryAcquire()` call succeeds via `createExclusive()` (line 210-213) → becomes leader, but nothing resets the flag. 3. Disk fills again 

### PR #1791 [M 50-249] docs(contributing): feedback pipeline operations runbook
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-11T16:54:02Z
own commits after: []
VERDICT: 1 important, 0 nits  🔴 Important: the new runbook documents `LINEAR_BUG_LABEL_ID`/`LINEAR_FEATURE_LABEL_ID` as required Vercel env vars and references a `Bug`/`Feature` top-level labeling mechanism that no longer exists in the code — the pipeline now resolves `reported/*` labels by name at runtime (`reported-labels.ts`), and `env.ts` explicitly documents that there is deliberately no label-id var. See inline comment for details and the three affected spots in the file.
  THREAD contributing/feedback-pipeline-ops.md:61 resolved=False outdated=False
    🔴 Important: `LINEAR_BUG_LABEL_ID` / `LINEAR_FEATURE_LABEL_ID` are documented here as Vercel env vars this pipeline needs, but they don't exist in the current code path. `apps/site/src/env.ts:150-153` explicitly says "There is deliberately no label-id var here" — the kind-to-label mapping is resolved by name at runtime via `reportedLabelIdsForKind` (`apps/site/src/lib/feedback/reported-labels.ts:24-27`), which stamps a `reported/*` child label, never the top-level `Bug`/`Feature` labels (see `linear.ts:14-17`: "never as `Bug` or `Feature`, which are triage's own verdict vocabulary"). Setting these two vars in Vercel does nothing — Zod's `webEnvSchema.parse` silently strips them.  Same stale mechanism appears in the "Kind labels (FB team)" row (line 25, `Bug` / `Feature` with those same ids) and in the verification section (line 89: "the issue is in the FB team's Triage with the `Bug` lab

### PR #1808 [XL 1000+] feat(rooms): a server-owned canvas every agent in a room can drive (DOR-1999)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T01:36:24Z
own commits after: ["chore(rooms): count read_canvas among the room domain's observe reads (DOR-1999)", 'fix(rooms): spell the document-key separator as an escape sequence (DOR-1999)', 'chore(changelog): cover the escape-sequence fix in the room canvas fragment (DOR-1999)']
VERDICT: 1 important, 0 nits  This is an unusually well-documented and thoroughly tested PR implementing the room shared canvas (spec `room-canvas`, DOR-1999): schema, migration (with a real-file upgrade test), the single-writer `RoomCanvasService`, HTTP routes, MCP `read_canvas` tool with the §8.1 cross-tree read guard, per-turn ceiling wired through config write-policy/disclosure/safe-defaults, and cross-runtime enforcement (claude-code, codex, test-mode) with a conformance-style catalog-drift test. I traced the config governance chain, the db schema against the migration SQL, the eviction/ledger/turn-budget bookkeeping, the dedupe-key sharing between client and server, and the MCP tool-security guarding of `read_canvas` — all consistent and well-covered by tests.  🔴 Important — `apps/server/src/services/rooms/canvas/document-key.ts:44`: the line `return createHash(\x27sha256\x27).update(\`${scope} ${sourceKey}\`)...` contains a literal NUL byte (`0x00`) between `${scope}` and `${sourceKey}`, not a space character as it visually renders (confirmed via `od -An -tx1` and `grep -aP "\x00"` on disk). This is why the PR diff shows this entire file as `Binary files /dev/null and b/.../document-key.ts differ` rather than a normal text diff, and why `file` reports it as `data` instead of ASCII/UTF-8 text. I could not attach this as a normal inline comment for the same reason — GitHub has no diff line to anchor it to, since the file has no text-diff hunk. Practical impact: every future chang

### PR #1809 [XL 1000+] feat(rooms): Canvas and Browser tabs in every room, shared live (DOR-2000)
labels=[] imp=2 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T03:43:41Z
own commits after: ['fix(rooms): never report a write as done before the room has taken it', 'docs(changelog): fold the write-confirmation fix into the room-canvas entry']
VERDICT: 2 important, 0 nits  This is a large, well-documented feature (room-owned shared canvas) with strong test coverage across the store slice, the resync/edit-lock race, and the reading-permission logic. FSD layering, SDK confinement, and the changelog fragment all look correct; no dangling references from the CanvasRenderer/CanvasViews refactor.  Two Important findings posted inline, both instances of a discard-on-failure bug that the PR itself fixes for a sibling control (the Browser tab address bar preserves a refused edit and shows why — these two do not):  1. `RoomCanvasMarkdown.tsx` (save on the room canvas markdown editor) silently drops the users unsaved draft when the write to the room fails. 2. `FilePreviewDialog.tsx` / `room-files-source.ts` (Put on the canvas) closes the dialog and gives no feedback at all if the room refuses the write, contradicting the "no caller may decide nothing" principle stated elsewhere in this same PR.
  THREAD apps/client/src/layers/features/canvas/ui/room/RoomCanvasMarkdown.tsx:63 resolved=True outdated=True
    🔴 **Important:** `save()` calls `setDraft(null)` unconditionally, right after firing `onSave` (which is itself fire-and-forget — `attempt()` in `RoomCanvasBody.tsx` doesn't await it either). If the room refuses the write (network blip, archived room, etc.), the toast fires but the editor has already dropped back to `editable`/read-only mode showing `content.content` — the user's draft is gone, not just unsaved.  This is exactly the class of bug this PR fixes elsewhere: `CanvasBrowserContent.tsx`'s address bar keeps the typed URL and reopens in edit mode on a refusal (see `refusedAddress` state and its test "says why the room refused it, and keeps what was typed"). The markdown editor has no equivalent — `save()` should keep `draft` (and editing) until the write actually resolves, only clearing it on success.
    REPLY doriancollier Fixed in fdeff20b1: `attempt()` now resolves to whether the write landed and `onSave` returns it; the editor awaits it, keeps the draft and the lock throughout, shows "Saving…", and on refusal keeps both with a line saying the words are still there. Only success clears the draft. RTL test: a 409 sav
  THREAD apps/client/src/layers/features/file-explorer/ui/FilePreviewDialog.tsx:346 resolved=True outdated=True
    🔴 **Important:** clicking "Put on the canvas" calls `source.showToEveryone?.(path)` and immediately `onClose()`, with no feedback either way. `showToEveryone` (room-files-source.ts:99-105) is fire-and-forget and only `console.warn`s on failure — the dialog has already closed, so a refused write (archived room, network error, etc.) looks identical to success from the user's perspective.  This directly contradicts the principle this same PR states for the other room-canvas writes in `use-room-canvas.ts`: "Each one rejects rather than swallowing... every caller now has to decide what the person sees... and none of them may decide 'nothing'." This caller decides nothing. Consider surfacing the failure (e.g. a toast) the same way `RoomCanvasBody`'s `attempt()` does for close/pin/update.
    REPLY doriancollier Fixed in fdeff20b1: `showToEveryone` rejects instead of warning, and the dialog awaits it — closes on success, and on refusal stays open with the room's own sentence under the button. RTL test for the refusal path.  <!-- agent:provenance {"v":1,"harness":"claude-code","sessionId":"session_014p7NaGsd

### PR #1814 [XL 1000+] feat(canvas): your agent can use the page in the embedded browser, not just look at it (DOR-2007)
labels=[] imp=2 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T07:08:37Z
own commits after: ['docs(api): regenerate the OpenAPI export for the browser driving route (DOR-2007)']
VERDICT: 2 important, 0 nits  Big, well-tested feature (six browser-driving verbs on top of the existing DevTools capture shim, server-side driver-seat arbitration for multi-window sessions). Server-side seat resolution, staleness and eviction, the request/response protocol, the in-page shim, and the security posture (opaque sandbox origin, source-identity plus origin checks, no api reference in shim source) all look sound and are exercised by unit, integration and real-browser Playwright tests.  Two important findings posted inline:  1. use-devtools-bridge.ts: the handshook ref is never reset on same-tab navigation, so a driver claim can report instrumented true for a page that is not actually instrumented, right after navigating away from one that was -- defeating the instant-refusal UX this PR specifically built and tested for the reverse case. 2. browser-seat/act-protocol.ts and its callers: several refusal and timeout messages tell the agent to call get_ui_state to discover a browser tabs documentId, but get_ui_state private-session response (UiStateSchema) has no field enumerating open documents or tabs -- only the room-canvas branch does. The documented discovery path is unreachable for the ordinary non-room multi-tab case.  No dangling-reference or hard-rule issues found (SDK confinement, FSD layers, os.homedir are all respected; changelog fragment present and correctly scoped).
  THREAD apps/client/src/layers/features/canvas/model/use-devtools-bridge.ts:341 resolved=True outdated=False
    🔴 **Important: `handshook` never resets on same-tab navigation, so a stale `instrumented: true` claim survives navigating away from an instrumented page.**  `handshook` (declared line 252) is only ever set to `true` here on `hello`; nothing sets it back to `false`. The claim effect (deps `[transport, sessionId, documentId, logicalUrl]`, ~line 458-476) re-runs on every `logicalUrl` change (i.e. every address-bar navigation in this same Browser tab — `CanvasBrowserContent` doesn't remount on `currentUrl` changes, only on `content.url`), and immediately re-claims with `instrumented: handshook.current`.  The `'navigated'` case (line 368-375) resets the pending capture buffers on a navigation boundary but doesn't touch `handshook`. So: open an instrumented DorkOS-served preview (handshook → true), then navigate the same tab to an external site or anything the shim can't reach — the very next 
    REPLY doriancollier Confirmed and fixed in #1816 (this PR merged before the fix could be pushed; the local test gate was refused by unrelated load-timing tests). The handshake is now keyed to the document (URL plus reload counter) instead of a sticky boolean, so a same-tab navigation to a page without the shim reports 
  THREAD apps/server/src/services/session/browser-seat/act-protocol.ts:46 resolved=True outdated=False
    🔴 **Important: this tells the agent to call `get_ui_state` to discover a browser tab's `documentId`, but for a private (non-room) session `get_ui_state` cannot return that.**  Every documentId-disambiguation path points here: this note, `UNKNOWN_DOCUMENT_NOTE`; `drivingTimeoutNote` (act-protocol.ts:80, "Call get_ui_state to see what is open"); `DOCUMENT_INPUT`'s own description in `target.ts:76` ("Ids come from get_ui_state"); and the prompt in `context-builder.ts:383` ("pass one from get_ui_state to pick").  But `get_ui_state`'s private-session branch (`createGetUiStateHandler` in `apps/server/src/services/runtimes/claude-code/mcp-tools/ui-tools.ts:262`, returning `session.uiState ?? DEFAULT_UI_STATE`) is typed as `UiStateSchema` (`packages/shared/src/schemas.ts:5586`), whose `canvas` field is only `{ open: boolean, contentType: enum | null }` — there is no list of open documents/tabs o
    REPLY doriancollier Confirmed and fixed in #1816. A private session's `get_ui_state` carries no tab list, and the client mints the id, so the server cannot list it there. The four notes now point at the path that works everywhere: every driving result names the tab it acted on and its id (on refusal too), and leaving `

### PR #1818 [XL 1000+] feat(claude-code): refuse asks at once on scheduled runs instead of waiting ten minutes
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T09:25:54Z
own commits after: ['feat(client): say what a scheduled run does with an ask nobody can answer']
VERDICT: 1 important, 1 nit  No factual issues found with the core logic: the instant-refusal path (`refuseWithNobodyToAsk`), the `trigger`-based `unattended` gating on both dispatch paths, the new `no_approval_surface` reason plumbing, and the `run-refusals` dedup/summary logic all check out against their callers and tests.  🔴 Important — `apps/client/src/layers/features/tasks/ui/TaskFormInner.tsx:434-435` (not touched by this PR, so no inline comment could attach): this note still says an unanswered ask on a scheduled run is "refused after 10 minutes," which is exactly the behavior this PR removed (asks are now refused the instant they are raised, per `interactive-handlers.ts`). The comment above it (lines 424-428) also still cites the deleted `SESSIONS.INTERACTION_TIMEOUT_MS` mechanism. The stale copy is pinned green by `CreateTaskDialog.test.tsx:650` (`expect(note).toHaveTextContent(/refused after 10 minutes/)`). This PR already rewrote the equivalent copy in `docs/guides/task-scheduler.mdx` and `docs/concepts/answering-agents.mdx` — this in-app string (and its test) looks like the one spot that was missed.  🟡 Nit (posted inline) — `apps/server/src/services/tasks/run-activity.ts:145` cites a nonexistent `RefusedAskLog.wasNewRefusal` method; the real one is `observe()`.  <!-- agent:provenance {"v":1,"harness":"claude-code","surface":"github-review"} -->
  THREAD apps/server/src/services/tasks/run-activity.ts:145 resolved=True outdated=True
    🟡 Nit: this TSDoc cites `RefusedAskLog.wasNewRefusal`, but that method doesn't exist — the actual (and correctly-used, at `task-scheduler-service.ts:1319`) method is `observe()`, which returns the refusal only on a tool's first occurrence. Looks like the doc wasn't updated after a rename during development.
    REPLY doriancollier Fixed in 5f80e64. The TSDoc now says the caller emits only when `RefusedAskLog.observe` answers with a refusal, which it does on a tool's first one and never again. `wasNewRefusal` was a two-call protocol the previous review round replaced with the return value, and this doc was the one place that s

### PR #1822 [XL 1000+] feat(canvas): the session canvas lives on the server, and the ui capability domain opens (DOR-2006)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T11:55:50Z
own commits after: ['docs(api): regenerate the API export after merging main (DOR-2006)', 'fix(canvas): import the localStorage canvas one document at a time (DOR-2006)', 'chore(changelog): fold the import fix into the canvas fragment (DOR-2006)', 'fix(canvas): a window claims the driver seat when a person acts in it (DOR-2006)', 'chore(changelog): fold the driver-seat fix into the canvas fragment (DOR-2006)']
VERDICT: 1 important, 0 nits  This is a large, thoroughly-documented feature PR moving the session canvas to the server (mirroring the existing room-canvas design), with the client, server routes, MCP tool wiring, Obsidian read-only embed, and orphan-sweep all covered by tests. Wiring (FSD layers, SDK confinement, scope routing between room/session channels) checked out clean; no dangling references found from the use-canvas-persistence / canvas-session-helpers deletions.  🔴 One finding: the one-time localStorage to server canvas import (use-session-canvas.ts) can permanently lose a document when a legacy entry holds more than one and a write fails partway through -- the retry logic's 'table already filled' check treats any existing document as proof the whole import already completed, so an interrupted multi-document import never finishes on retry. See inline comment for the mechanics and why the existing failure-path test does not catch it.
  THREAD apps/client/src/layers/features/canvas/model/use-session-canvas.ts:251 resolved=True outdated=False
    🔴 **Partial import can silently lose a document, not just delay it.**  When a legacy entry holds more than one document, a failure partway through the `for` loop (line 166-168) throws, the outer `catch` un-latches `imported` and *keeps* the local entry for retry — but on the next hydrate, `transport.listSessionCanvas` now returns the document(s) that already landed, so `existing.length > 0` (line 159) is true and the entry is dropped immediately (line 163) without ever writing the remaining document(s).  Concretely: legacy entry `[A, B]` → first hydrate writes `A` successfully, then the `B` write throws → entry is kept as designed → next hydrate sees `existing = [A]` → takes the "already filled" branch and deletes the entry → `B` is now gone from both `localStorage` and the server, permanently.  The existing test `'KEEPS the entry when a POST fails, and retries on the next hydrate'` only
    REPLY doriancollier Confirmed and fixed in ab246ce29. The import is now per document: it computes which of the entry's documents the server does not hold yet (keyed content by the same source key the server dedupes on; `json`/`widget` by content), sends only those, and deletes the local entry only once every one is pre

### PR #1823 [L 250-999] docs(specs): mark the five Claude Agent SDK 0.3.268 specs implemented
labels=['review:light', 'skip-changelog'] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T16:26:30Z
own commits after: ['docs(specs): add implementation summaries for the five SDK 0.3.268 specs']
VERDICT: 1 important, 0 nits  All five specs flipped to `implemented` in this PR lack the `04-implementation.md` artifact the canonical status table requires for that status, and four of them still say `status: specified` in their own frontmatter. See inline comment on specs/manifest.json:25 for detail.
  THREAD specs/manifest.json:25 resolved=True outdated=False
    🔴 Important: This PR flips five specs to `implemented`, but per the canonical status table in `.claude/skills/managing-specs/SKILL.md` (`implemented` → "Spec has `04-implementation.md`"), none of them qualify:  - `claude-agent-sdk-upgrade-0.3.268` has **only** `specs/claude-agent-sdk-upgrade-0.3.268/01-ideation.md` — no `02-specification.md`, no `04-implementation.md`. Per the same table it shouldn't even be `specified` yet, let alone `implemented`. - `ambient-background-tasks`, `auto-mode-classifier-context`, `unattended-session-permission-prompts`, and `plugin-reload-cache-cost` each have `01-ideation.md` + `02-specification.md` only — no `04-implementation.md` — and their own doc frontmatter/header (`specs/<slug>/02-specification.md:1-10`) still reads `status: specified`.  The skill also says the manifest should "Never edit it by hand — always use the manifest-ops script" (`spec-manif
    REPLY doriancollier Both fixed in 22224e5cc.  **Five `04-implementation.md` files added**, one per spec, written from the merged PR bodies and the decision records (40-90 lines each): `claude-agent-sdk-upgrade-0.3.268` (#1798, #1802, #1803, #1806), `plugin-reload-cache-cost` (#1819), `unattended-session-permission-prom

### PR #1825 [XL 1000+] feat(rooms): follow a teammate's browser, and discuss a document (DOR-2010)
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-12T18:27:58Z
own commits after: ["fix(rooms): strip the beat pin's comments with the repo's own lexer (DOR-2010)", "docs(changelog): cover the lexer fix from the phase's own entry", "fix(rooms): keep a stale follow position off a follower's own scroll (DOR-2010)", "docs(changelog): cover the scroll guard from the phase's own entry"]
VERDICT: 1 important, 1 nit  Solid, well-tested feature (follow a teammate's browser + per-document Discuss threads on room canvas). Server-side follow claims, membership gating, people-only enforcement, and the discuss-thread transaction all check out — no Hard Rule violations found.  🔴 Important - `apps/client/src/layers/features/canvas/ui/room/RoomCanvasBody.tsx:276-287` — the effect that follows a teammate's browser applies `panel.current.scrollTop = followedView.scrollY` unconditionally, without checking that `followedView.documentId` matches the document actually on screen (the `activate(...)` branch right above it does check this). A stale follow position naming a document that isn't currently in `documentIds` (e.g. just closed/evicted, up to `ROOM_FOLLOW_TTL_MS` = 30s of staleness) will still hijack the scroll position of whatever unrelated document the follower currently has open. `CanvasBrowserContent.tsx`'s sibling implementation guards this correctly; `RoomCanvasBody.tsx` should apply the same document-match check before touching `scrollTop`. No test in `RoomCanvasFollow.test.tsx` exercises `scrollTop`, so this wouldn't be caught by the suite as written. Full detail in the inline comment.  🟡 Nit (not inline — file isn't part of this PR's diff) - `apps/e2e/manifest-curated.json:157` still references `apps/client/src/layers/entities/room/model/use-room-presence.ts`, which this PR renames to `model/live/use-room-presence.ts`. It's hand-curated metadata (`relatedCode`) rather 
  THREAD apps/client/src/layers/features/canvas/ui/room/RoomCanvasBody.tsx:311 resolved=True outdated=False
    🔴 **Important**: the scroll application isn't gated on the followed document actually being the one on screen.  The `activate(...)` branch correctly checks `followedView.documentId !== activeDocumentId && documentIds.includes(followedView.documentId)` before switching tabs, but the `panel.current.scrollTop = followedView.scrollY` branch right below it has no equivalent check — it fires whenever `followedView.scrollY` is defined, regardless of whether `followedView.documentId` matches `activeDocumentId` or is even present in `documentIds`.  Concretely: if the leader's followed document has just been closed/evicted (or is otherwise not currently in this follower's `documentIds`), `activate` is skipped but the scroll assignment still runs — against whatever document the follower currently has open, using a scroll offset that belongs to an unrelated document. Since a follow position's TTL is
    REPLY doriancollier Confirmed and fixed in d647d62ab. The scroll now applies only when the followed document is one this viewer holds AND is the active one; a position for another open document activates it and returns, and the offset rides the next pass once the panel shows that document (the same shape `CanvasBrowser

### PR #1829 [S 10-49] docs(decisions): record the held-call ending the expiry review surfaced (DOR-1932)
labels=['review:light', 'skip-changelog'] imp=1 change=no change at all rereviewed=False verdict_t=2026-09-12T20:10:23Z
own commits after: []
VERDICT: 1 important, 0 nits  This PR (docs-only: ADR + spec) documents a decision as `accepted`/"implementation lands with this record," claiming both capability-approval-hold sites now return a distinct `approval_no_longer_valid` payload on `expired` and keep the delivery claim. Verified against the current code: `mcp-projection.ts`, `mcp-tool-gate.ts`, and `capability-approval-hold.ts` still implement the old behavior exactly (stale `approval_required` payload returned verbatim, delivery claim released on `expired`) — no such payload exists anywhere in the tree outside these two markdown files. Flagged inline on both files with citations.  <!-- agent:provenance {"v":1,"harness":"claude-code","surface":"github-actions"} -->
  THREAD decisions/260912-190915-an-approval-expires-on-a-sweep-and-the-agent-is-told.md:78 resolved=False outdated=False
    🔴 Important: this describes behavior that isn't in the codebase. The ADR's own Status line says "the implementation lands with this record," but this PR touches only these two markdown files — no code changed. Checked both hold sites:  - `apps/server/src/services/core/capabilities/mcp-projection.ts:356-357,368` (`holdAndResume`) — TSDoc and code still say `expired`/`timeout` degrade to "the EXACT `approval_required` payload," and `return textResult(payload)` does exactly that. - `apps/server/src/services/core/mcp-tool-gate.ts:300-302` (`runGatedInSession`) — same: "The original `approval_required` payload is returned verbatim" for `timeout`/`expired`. - `apps/server/src/services/core/capabilities/capability-approval-hold.ts:236-243` — the `finally` block releases the delivery claim whenever `outcome !== 'granted' && outcome !== 'denied'`, which includes `expired`; it does not "keep" the 
  THREAD specs/approval-expiry-notice/02-specification.md:241 resolved=False outdated=False
    🔴 Important: same issue as the companion ADR — this section states as fact that both hold sites now return `approval_no_longer_valid` on `expired` and keep the delivery claim, but `apps/server/src/services/core/capabilities/mcp-projection.ts`, `mcp-tool-gate.ts`, and `capability-approval-hold.ts` still implement the old behavior verbatim (stale `approval_required` payload returned, claim released on `expired`). No code in this PR changes that. See the ADR comment for the specific line citations.

### PR #1846 [XL 1000+] feat(client): say plainly what each mode lets Codex do
labels=[] imp=1 change=rebase/merge-main only rereviewed=False verdict_t=2026-09-14T00:54:40Z
own commits after: []
VERDICT: 1 important, 0 nits  The DOR-2019 read-only-mode-hint feature is well-built and well-tested overall (isSilentReadOnly, the copy updates, and the hook all have direct unit coverage, and the docs/contributing guide/changelog all agree with the shipped copy), but the once-per-session "leaving the mode spends it" logic in `use-read-only-mode-hint.ts` has a cross-session bug — see inline comment. `ChatPanel` is a persistent component reused across session switches (no `key` on it in `App.tsx` or `SessionPage.tsx`), so the hook's `wasSilent` ref carries state from the previously viewed session into the next one, and can spuriously mark a session's hint as "spent" before it was ever eligible or shown.
  THREAD apps/client/src/layers/features/status/model/use-read-only-mode-hint.ts:90 resolved=True outdated=True
    🔴 Important: `wasSilent` is a single ref shared across session switches, not scoped to the session being viewed. `ChatPanel` is mounted once and just receives a new `sessionId` prop on navigation (`App.tsx:196` and `SessionPage.tsx:72` both pass `sessionId={activeSessionId}` with no `key`), so this hook instance persists across sessions.  Concretely: view session A (Codex, silent) — `wasSilent.current` becomes `true`. Switch to session B (any non-silent runtime/mode, e.g. Claude) — the effect sees `silent=false`, `wasSilent.current` still `true` from A, and `sessionId` now truthy for B, so it calls `spendHint(B)` even though B was never shown the notice. If B's runtime is later switched into Codex's silent default within that same session, `readOnlyHintSpent[B]` is already `true`, so the notice never appears — silently defeating the feature for a session that never got to see it.  The te
    REPLY doriancollier Fixed, and the ref is gone rather than reset.  Progress now lives per session id in the chat store (`readOnlyHint: Record<string, 'shown' | 'spent'>`, swept in `forgetSession`), so nothing about one conversation can reach the next. `ChatPanel` not being keyed by session is exactly why a ref was the 

### PR #1852 [L 250-999] feat(feedback): /feedback:triage reads GitHub issues and answers reporters where they wrote (DOR-2033)
labels=['skip-changelog'] imp=1 change=rebase/merge-main only rereviewed=False verdict_t=2026-09-14T13:00:40Z
own commits after: []
VERDICT: 1 important, 0 nits  This PR is a prose/config-only change (`.claude/commands/`, `.github/`, `contributing/`, `decisions/`) rewriting `/feedback:triage` to add GitHub-issue intake alongside the existing in-app FB pipeline, and retiring the never-created `needs-triage` label in favor of a marker-based (`<!-- beat:heard -->` etc.) queue definition. The design itself (two-key mirror/beat separation, four-backtick fence to survive reporter-supplied fences, last-`Source:`-line matching to survive Linear's autolink rewrite, per-reply human approval, comment-before-close ordering) is careful and internally consistent across `triage.md`, `feedback-pipeline-ops.md`, and the ADR amendment. Ran the dangling-reference sweep for the removed `needs-triage` label — its only surviving reference is the ADR amendment explaining it's gone, which is correct.  🔴 One finding (inline on `.claude/commands/feedback/triage.md:41`): the command's core dependencies, `meta/user-care.md` and the `writing-to-users` skill, do not exist anywhere in the repo and are not added by this PR. The command's own guard means it fails safe (stops and says which one is missing) rather than misbehaving, but as merged, every invocation of the new GitHub-intake flow — Step 0, both new reply beats, the queue-health rows, both medians — hits that guard and never runs.
  THREAD .claude/commands/feedback/triage.md:41 resolved=False outdated=False
    🔴 Important: `meta/user-care.md` and the `writing-to-users` skill don't exist anywhere in the repo (verified with a full-tree search), and this PR doesn't add either. Neither the file list for this PR nor `Glob("meta/*.md")` / `Glob(".claude/skills/**")` / a repo-wide grep for `writing-to-users` turns up anything — the only skill of that shape is the pre-existing `writing-for-humans` (`.agents/skills/writing-for-humans/SKILL.md`).  This command cites `meta/user-care.md` as the whole promise Step 0 exists to serve (line 18), as the source of the five health-row definitions (line 330) and the two medians (line 345), and cites the `writing-to-users` skill as how every heard/decided/shipped reply gets drafted (lines 24, 194, 278, 385). `contributing/feedback-pipeline-ops.md` also leans on `meta/user-care.md` (e.g. "so both doors get the treatment `meta/user-care.md` promises").  The command 

### PR #1886 [L 250-999] docs(specs): warm process lifecycle, keep background work and show what the agent says between turns
labels=['review:light', 'skip-changelog'] imp=1 change=new PR commits after finding rereviewed=True verdict_t=2026-09-15T21:08:45Z
own commits after: ['docs(decisions): mark the passages ADR 260812-134510 loses to 260915-202228']
VERDICT: 1 important, 0 nits.  This is a docs/specs-only PR (new ADR `260915-202228` + `warm-process-lifecycle` spec, amending ADR `260812-134510`). Reviewed under `review:light` (Important findings only).  🔴 The child ADR names the clauses it retires from the parent, but the parent ADR file itself is never edited to reciprocate — a documented, non-negotiable step in `.agents/skills/writing-adrs/SKILL.md`'s `amends` convention (see inline comment for the exact requirement and precedent). Everything else checked out: manifest entries in `decisions/manifest.json` and `specs/manifest.json` are consistent with the ADR/spec frontmatter, the `amends`/`supersedes`/`superseded-by` fields are set correctly per convention, and no code, FSD, SDK-confinement, or `os.homedir()` concerns apply since this PR touches no source files.
  THREAD decisions/260915-202228-a-warm-process-ends-only-when-it-is-quiet.md:19 resolved=False outdated=True
    🔴 Important: this ADR names the clauses it retires here, but never edits the *parent* ADR (`decisions/260812-134510-persistent-streaming-input-sessions.md`) to do the same — that file isn't touched anywhere in this diff. `.agents/skills/writing-adrs/SKILL.md` §"Partial supersession: the `amends` relation" requires step 2: "Parent's Status section names exactly what is retired: quote the clause, list any Consequences bullets that fall with it, then state what still governs," with `260726-170125` as the worked example (its Status section explicitly quotes the retired clause and links forward to the amending ADR). Left as-is, `260812-134510` still reads as fully in force with no pointer to this amendment — the exact failure mode the skill's own "flagship example" (`260727-182651`/`260713-143958`) was written to prevent. Please add the reciprocal note to the parent's Status section before me

### PR #1893 [M 50-249] fix(client): room messages indent lists like session messages do
labels=[] imp=1 change=new PR commits after finding rereviewed=False verdict_t=2026-09-15T22:16:36Z
own commits after: ['docs(changelog): cover the rename commit under the same fragment (DOR-2074)']
VERDICT: 1 important, 0 nits  🔴 Important — `apps/client/src/layers/features/conversation/ui/message/message-variants.ts:70` still references `var(--msg-assistant-font-weight)`, but this PR renames that custom property to `--msg-prose-font-weight` in both `apps/client/src/index.css:319` and `apps/obsidian-plugin/src/styles/plugin.css`. The old variable no longer exists anywhere in the tree, so `font-[var(--msg-assistant-font-weight)]` resolves to nothing and every assistant-role message row (session, room, onboarding narration — anywhere through `Message.Content`/`MessageRoot`, confirmed via `MessageBody.tsx:60`) silently loses its font-weight-300 styling in both themes. Should be `font-[var(--msg-prose-font-weight)]`.  (Could not attach this inline — `message-variants.ts` is not part of this PR's diff, so GitHub rejected a review comment on it.)  Rest of the change is clean: the `msg-assistant` → `msg-prose` rename in `index.css`/`plugin.css` is otherwise consistent with its call sites, `render-room-body.tsx` correctly threads the new `className` prop through `MarkdownContent` (verified `cn(...)` merges it), the new `RoomMessage` test pins the fix at the right seam, and the changelog fragment is correctly formatted under `### Fixed`.

### PR #1908 [XL 1000+] feat(cloud): the plan-aware app — plan card, credits gauge, upgrade nudge, seat management (DOR-2027)
labels=[] imp=1 change=rebase/merge-main only rereviewed=True verdict_t=2026-09-16T07:49:51Z
own commits after: []
VERDICT: 1 important, 0 nits  This is a large, well-tested feature PR (the plan-aware cloud surfaces: plan card, credits gauge, upgrade nudge, seat management, and credits-as-inference-source). Catalog-blindness and graceful-degradation are consistently enforced across client, server, and shared schemas, and match against the `@dork-labs/cloud-api` contract schemas field-for-field. The money-path documentation (`AGENTS.md`) and its enforcing test (`paid-provider.test.ts`) were updated together and stay consistent. FSD layering, SDK confinement, and the `os.homedir()` rule are all respected (not touched). No deletions/renames requiring a dangling-reference sweep — this PR is additive except for the money-path table update in `AGENTS.md`, which I verified has no stale references elsewhere.  One finding, inline on `CloudPlanPanel.tsx`: the top-level panel does not check the loading state of `useCloudPlan()`, so a linked, paying user will see "Link this instance to a DorkOS account above…" flash on every Settings load until the query resolves — the exact failure mode the adjacent `isError` branch was written to avoid, but missed for the loading case. Sibling components in the same slice (`PlanCard`, `CreditsGauge`) handle this correctly, so it reads as an oversight rather than a deliberate choice.  <!-- agent:provenance {"v":1,"harness":"claude-code","account":"claude"} -->
  THREAD apps/client/src/layers/features/cloud-plan/ui/CloudPlanPanel.tsx:42 resolved=True outdated=False
    🔴 `useCloudPlan()` isn't destructured for `isLoading`/`isPending` here, so while the query is still in flight `data` is `undefined` and this falls into the same branch as "no cloud account" — a linked, paying user opening Settings will always see "Link this instance to a DorkOS account above…" for the duration of the fetch, before it flips to the real panel.  This is exactly the failure mode the `isError` branch right above was added to avoid (per the comment on line 23-25: telling a linked person to link is worse than saying nothing) — but the same problem exists for the loading state and isn't handled. Every sibling component in this slice gets this right (e.g. `PlanCard.tsx` and `CreditsGauge.tsx` both check `isLoading` before falling through to the `!data?.available` branch), so this looks like an oversight rather than a deliberate omission.  Suggest returning `null` (or a skeleton) 
    REPLY doriancollier Fixed in e97f6542. The panel now gates on `isPending` before it decides linked-vs-unlinked, so nothing about linking is said until the read settles. The children's skeletons came out with it — the panel owns that state now, and two components deciding what "still loading" looks like is how they drif

