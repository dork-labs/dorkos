{
  "$comment": "Scoped exceptions for banned-terms.json. Each entry suppresses hits on files whose repo-relative path CONTAINS `path` (a plain substring, not a glob). Omit `terms` to allowlist every banned term at that path; scope it to specific terms when the file is legitimate for one wave's word but would be a real hit for a later wave's. Keep entries file- or directory-scoped and explain the domain reason — this file is the audit trail for every 'connection' the gate lets through.",
  "entries": [
    {
      "path": "apps/client/src/layers/features/connections/",
      "terms": ["connection"],
      "reason": "The /connections page itself — Connections is the word's one legitimate domain (plan D2)."
    },
    {
      "path": "apps/client/src/layers/entities/connectors/",
      "terms": ["connection"],
      "reason": "Connectors entity layer backing the Connections page (OAuth connect flow, ConnectDialog copy)."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/RelayHealthBar.tsx",
      "terms": ["connection"],
      "reason": "computeHealthState()'s \"N connection(s) active/disconnected\" and \"No connections configured\" count configured relay adapters (Telegram, Slack, webhook instances) — each adapter IS a Connection entity on the /connections page. This is the entity noun, not a network-health reading of whether a link is up; the adapter's own reachability is reported separately (\"Reachable\"/\"Not reachable\", per TestStep)."
    },
    {
      "path": "apps/site/src/layers/features/marketing/lib/features.ts",
      "terms": ["connection"],
      "reason": "Two senses coexist here pending copy review: the Mesh Topology feature's \"every connection\" describes graph edges between agents, not link health; the Accounts feature's \"Attach a connection to one session\" is Connections-domain (an attached account). Neither is the network-health sense DOR-855 retires. Revisit if this file grows a true network-status blurb."
    },
    {
      "path": "apps/site/src/app/(marketing)/test/",
      "terms": ["connection"],
      "reason": "Marketing visual-design sandbox routes (hero/diagram prototypes) — \"connection lines\" describes SVG graph edges in a prototype, not shipped product copy."
    },
    {
      "path": "apps/server/src/services/core/mcp-tool-tiers.ts",
      "terms": ["connection"],
      "reason": "MCP tool titles \"List/Turn a chat connection on/off/Reload chat connections\" describe relay adapters (Telegram/Slack) — the same Connections-domain entity as RelayHealthBar, surfaced to the agent rather than the human."
    },
    {
      "path": "apps/server/src/services/runtimes/opencode/check-dependencies.ts",
      "terms": ["connection"],
      "reason": "\"Your saved {provider} connection didn't work\" reports a stale/invalid stored credential for a model provider — Connections-domain (an attached provider account), not a network-reachability reading."
    },
    {
      "path": "apps/server/src/services/core/openapi-registry.ts",
      "terms": ["connection"],
      "reason": "OpenAPI `description` fields for the /api/docs developer reference, describing the MCP client's connection config and an attached account's connection details (spec security language) — Connections-domain API documentation, not cockpit network-status copy."
    }
  ]
}
