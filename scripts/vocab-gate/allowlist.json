{
  "$comment": "Scoped exceptions for banned-terms.json. Each entry suppresses hits on files whose repo-relative path CONTAINS `path` (a plain substring, not a glob). Omit `terms` to allowlist every banned term at that path; scope it to specific terms when the file is legitimate for one wave's word but would be a real hit for a later wave's. Keep entries file- or directory-scoped and explain the domain reason — this file is the audit trail for every 'connection' the gate lets through.",
  "entries": [
    {
      "path": "apps/site/src/app/connectors/managed/fields/ManagedAccountFieldsForm.tsx",
      "terms": ["connection"],
      "reason": "The owner-authorized account form names the exact Connections account being created. Its closed failure messages describe that account entity, not network reachability."
    },
    {
      "path": "apps/site/src/app/connectors/managed/fields/page.tsx",
      "terms": ["connection"],
      "reason": "The hosted account authorization page confirms a new Connections account before returning to the app. This is the account entity, not network-health copy."
    },
    {
      "path": "apps/client/src/layers/features/connections/",
      "terms": ["connection"],
      "reason": "The /connections page itself — Connections is the word's one legitimate domain (plan D2)."
    },
    {
      "path": "apps/client/src/layers/entities/connectors/",
      "terms": ["connection"],
      "reason": "Connectors entity layer backing the Connections page (OAuth connect flow, ConnectDialog copy)."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/MessagingConnections.tsx",
      "terms": ["connection"],
      "reason": "The Messaging region of the /connections page (DOR-857). \"Remove this connection?\" names the entity a person is deleting — one Telegram bot or Slack app they set up — which is exactly what Connections means. Not the network-health sense DOR-855 retires: whether that connection is currently reachable is reported separately (\"Reachable\" / \"Not reachable\", per TestStep)."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/RelayHealthBar.tsx",
      "terms": ["connection"],
      "reason": "computeHealthState()'s \"N connection(s) active/disconnected\" and \"No connections configured\" count configured relay adapters (Telegram, Slack, webhook instances) — each adapter IS a Connection entity on the /connections page. This is the entity noun, not a network-health reading of whether a link is up; the adapter's own reachability is reported separately (\"Reachable\"/\"Not reachable\", per TestStep)."
    },
    {
      "path": "apps/site/src/layers/features/marketing/lib/features.ts",
      "terms": ["connection"],
      "reason": "Two senses coexist here pending copy review: the Mesh Topology feature's \"every connection\" describes graph edges between agents, not link health; the Accounts feature's \"Attach a connection to one session\" is Connections-domain (an attached account). Neither is the network-health sense DOR-855 retires. Revisit if this file grows a true network-status blurb."
    },
    {
      "path": "apps/site/src/app/(marketing)/test/",
      "terms": ["connection"],
      "reason": "Marketing visual-design sandbox routes (hero/diagram prototypes) — \"connection lines\" describes SVG graph edges in a prototype, not shipped product copy."
    },
    {
      "path": "apps/server/src/services/core/mcp-tool-tiers.ts",
      "terms": ["connection"],
      "reason": "MCP tool titles \"List/Turn a chat connection on/off/Reload chat connections\" describe relay adapters (Telegram/Slack) — the same Connections-domain entity as RelayHealthBar, surfaced to the agent rather than the human."
    },
    {
      "path": "apps/server/src/services/mesh/mcp-capabilities.ts",
      "terms": ["connection"],
      "reason": "The `mcp_add` tool description tells an agent to read `connection.authKind` off the entry the call returns. That is a literal property path on the shipped MCP-server-entry shape (`agent-mcp-server-service.ts` reads `existing.connection.authKind`), addressed to a machine that will parse it — not the network-health sense DOR-855 retires. Rewriting the word here would make the description name a field that does not exist, so this is allowlisted rather than reworded, exactly as openapi-registry.ts is for the same reason."
    },
    {
      "path": "apps/server/src/services/runtimes/opencode/providers/check-dependencies.ts",
      "terms": ["connection"],
      "reason": "\"Your saved {provider} connection didn't work\" reports a stale/invalid stored credential for a model provider — Connections-domain (an attached provider account), not a network-reachability reading."
    },
    {
      "path": "apps/server/src/services/core/openapi-registry.ts",
      "terms": ["connection"],
      "reason": "OpenAPI `description` fields for the /api/docs developer reference, describing the MCP client's connection config and an attached account's connection details (spec security language) — Connections-domain API documentation, not cockpit network-status copy."
    },
    {
      "path": "apps/client/src/layers/features/settings/config/tool-inventory.ts",
      "terms": ["connection"],
      "reason": "The Settings Tools tab group label \"Connection management\" (plan D4) names the tools that set up and route the Telegram/Slack/webhook Connections — the same Connections-domain entity as RelayHealthBar and mcp-tool-tiers, not the network-health sense DOR-855 retires."
    },
    {
      "path": "apps/client/src/layers/entities/session/config/origin-descriptors.ts",
      "terms": ["connection"],
      "reason": "The session-origin badge label \"Connection\" (plan D2, DOR-859) marks a session started by an inbound Telegram/Slack message — it names the Connections-domain entity that reached the agent, not whether a network link is up. The origin's health is never shown here."
    },
    {
      "path": "apps/client/src/layers/entities/binding/ui/BindingDialog.tsx",
      "terms": ["connection"],
      "reason": "The add/edit-binding dialog's \"Add connection\" / \"Edit connection\" / \"Remove connection\" copy (plan D2, DOR-859) names the entity a person is creating or deleting — one binding of a Telegram/Slack/webhook to an agent, which is exactly what a Connection is. Not the network-health sense DOR-855 retires; a binding's reachability is reported separately by its test action."
    },
    {
      "path": "apps/client/src/layers/features/agent-settings/ui/IntegrationsTab.tsx",
      "terms": ["connection"],
      "reason": "The per-agent Connections tab's toasts (\"Connection added/removed/paused\") and its \"Add a connection\" copy (plan D2, DOR-859) name the Connections-domain entity — a Telegram/Slack/webhook binding for this agent — not a network-health reading. Whether a binding is currently reachable is reported by its own test action, not this copy."
    },
    {
      "path": "apps/client/src/layers/features/agent-settings/ui/IntegrationPicker.tsx",
      "terms": ["connection"],
      "reason": "The per-agent Connections tab's picker \"Add connection\" button and \"No connections available\" empty state (plan D2, DOR-859) name the Connections-domain entity — a Telegram/Slack/webhook binding for this agent — not a network-health reading."
    },
    {
      "path": "apps/client/src/layers/features/agent-settings/ui/IntegrationBindingCard.tsx",
      "terms": ["connection"],
      "reason": "The per-agent Connections tab's binding card \"Remove connection\" confirmation (plan D2, DOR-859) names the Connections-domain entity a person is deleting — one Telegram/Slack/webhook binding — not a network-health reading."
    },
    {
      "path": "apps/client/src/layers/features/mesh/ui/AdapterNode.tsx",
      "terms": ["connection"],
      "reason": "The topology graph's adapter-node badge (\"1 connection\" / \"N connections\", plan D2, DOR-896) counts bindings attached to that adapter — the Connections-domain entity, same count IntegrationBindingCard and RelayHealthBar report elsewhere. Not a network-health reading."
    },
    {
      "path": "apps/client/src/layers/features/mesh/ui/BindingEdge.tsx",
      "terms": ["connection"],
      "reason": "The topology graph's binding-edge \"Remove connection\" aria-label (plan D2, DOR-896) names the Connections-domain entity a person is deleting — one adapter-to-agent binding — the same action IntegrationBindingCard's confirm dialog names elsewhere. The edge's own fallback display label ('Connection', in resolveDisplayLabel) is the same entity name but isn't a copy-bearing position the gate's static scan reaches (a bare `return` — the documented gap in check-vocab-gate.ts's module doc), so it needs no entry, only this note for the audit trail."
    },
    {
      "path": "apps/client/src/layers/features/mesh/ui/TopologyGraph.tsx",
      "terms": ["connection"],
      "reason": "The topology graph's keyboard-fallback summary, drag-to-bind hint, and remove-binding confirm dialog (plan D2, DOR-896) all name the same Connections-domain entity as BindingEdge and AdapterNode in this directory: an adapter-to-agent binding a person creates or deletes. Not a network-health reading — a binding's own reachability is reported by its test action, not this copy."
    },
    {
      "path": "apps/site/src/layers/features/marketing/lib/comparisons.ts",
      "terms": ["mission control"],
      "reason": "The GitHub Agent HQ compare page's FAQ answers the question \"What is GitHub Mission Control?\" — GitHub's own blog named its agent-hub page that, and the FAQ says so exactly to note the docs don't use the name. AGENTS.md carves this out: GitHub ships a product literally named Mission Control. This is DorkOS's own carve-out, not the copy this wave retires."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/SetupGuideSheet.tsx",
      "terms": ["connection"],
      "reason": "The setup guide for one Telegram/Slack/webhook connection (DOR-1754) — \"instructions for setting up this connection\" names the entity being set up, not whether a link is up."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/ConversationRow.tsx",
      "terms": ["connection"],
      "reason": "\"N bindings already exist for this connection\" (DOR-1754) counts routes attached to one Telegram/Slack connection — the same entity AdapterNode and RelayHealthBar count elsewhere."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/adapter/",
      "terms": ["connection"],
      "reason": "The connection card and its parts (DOR-1754): \"Connection actions\", \"Add connection\", and the \"Connection added\" toast all name one Telegram/Slack/webhook a person set up — the Connections-domain entity MessagingConnections lists. The card reports reachability separately, in its own words."
    },
    {
      "path": "apps/client/src/layers/features/relay/ui/wizard/",
      "terms": ["connection"],
      "reason": "The add-a-connection wizard (DOR-1754). \"Connection ID\" and the name field's helper text describe the entity being created; the wizard's own test step reports reachability in the plain-language family DOR-855 moved to."
    },
    {
      "path": "apps/client/src/layers/entities/relay/model/use-adapter-catalog.ts",
      "terms": ["connection"],
      "reason": "The \"Connection removed\" toast (DOR-1754) reports the deletion of one Telegram/Slack/webhook — the same entity, and the same wording, as the per-agent Connections tab's toasts in IntegrationsTab.tsx."
    },
    {
      "path": "apps/client/src/layers/entities/binding/ui/BindingAdvancedSection.tsx",
      "terms": ["connection"],
      "reason": "\"This connection is set to X\" (DOR-1754) names the binding whose permission mode is on screen — the same entity BindingDialog's own copy names one layer up. Not a network-health reading."
    },
    {
      "path": "apps/client/src/layers/features/agent-settings/ui/ToolsTab.tsx",
      "terms": ["connection"],
      "reason": "The per-agent \"Connection management\" tool group (DOR-1754) — the same group, under the same name, as tool-inventory.ts's, which is allowlisted for the same domain reason."
    },
    {
      "path": "apps/server/src/services/core/openapi-registry.ts",
      "terms": [
        "integration",
        "integrations",
        "connector",
        "connectors",
        "adapter",
        "adapters",
        "provider",
        "providers"
      ],
      "reason": "The /api/docs developer reference. ADR 260804-021140 renamed the product copy and left the WIRE untouched on purpose — `/api/connectors/providers/{provider}` takes a `provider` path parameter, a relay binding points at an `adapter` id, and `platform:<platform>:<adapter>:<user id>` is a literal stored identity string. A description that stopped naming those would name fields that do not exist, which is the reason mcp-capabilities.ts is allowlisted one entry above. Two hits are a different word entirely: git's \"integration tree\", and the path `docs/integrations/sse-protocol.mdx`. Whole-file scope is the finest this gate offers; a genuinely product-facing sentence added to this file would slip through, so read the two-layer table in the ADR before writing one."
    },
    {
      "path": "apps/server/src/services/core/operator/operator-capabilities.ts",
      "terms": ["provider", "providers"],
      "reason": "`operator.config_get` and `operator.config_update` describe the config.json fields by their literal names to the agent reading them — the `providers` map, `providersConfigured`, \"the provider ids that have a credential\", and OpenCode's own `provider` setting. Renaming the words would name keys that are not in the file. Scoped to `provider` only: an `adapter` or `integration` in this file would be prose, not a field name, and should red."
    },
    {
      "path": "apps/server/src/services/connectors/connector-capabilities.ts",
      "terms": ["connector", "connectors", "adapter", "adapters"],
      "reason": "MCP tool descriptions for the connector domain, addressed to an agent choosing a route: \"a purpose-built relay adapter outranks a gateway connector, which outranks a raw MCP server\" names the three architecture seams by their own names, which is what makes the ranking readable. Deliberately NOT scoped to `provider`: the one sentence that told an agent to point the user at \"a provider key\" named a UI that no longer exists and was rewritten to \"Composio & Nango\" (DOR-1814), so `provider` coming back here should red."
    },
    {
      "path": "apps/server/src/services/marketplace-mcp/marketplace-capabilities.ts",
      "terms": ["adapter", "adapters"],
      "reason": "The marketplace search and list tools enumerate the package TYPES an author writes in a manifest (agent/plugin/skill-pack/adapter). ADR 260804-021140's scoped-word registry keeps `adapter` in the marketplace-author domain; a filter value that stopped matching the manifest key would simply not work."
    },
    {
      "path": "apps/server/src/services/marketplace/conflict-detector.ts",
      "terms": ["adapter"],
      "reason": "\"An adapter of type X is already configured\" is the install-time collision between two marketplace packages, named with the package type the author declared — the same marketplace-author domain the ADR's registry keeps the word in."
    },
    {
      "path": "apps/server/src/services/runtimes/test-mode/runtime-constants.ts",
      "terms": ["integration"],
      "reason": "\"Deterministic allow/deny for integration tests\" is the software-testing sense of the word, not the retired product noun, on the `test-mode` runtime that exists only for e2e and reaches no user surface."
    },
    {
      "path": "apps/client/src/layers/features/marketplace/ui/MarketplaceSidebar.tsx",
      "terms": ["connector", "connectors"],
      "reason": "The marketplace's Connectors facet. Kept deliberately at the DOR-1754 close-out (\"The marketplace's Connectors facet stays as it is\") on the ADR's scoped-word registry: the catalog and the Connections page are different pages, so the author word and the user word never share a viewport."
    },
    {
      "path": "apps/client/src/layers/features/runtime-connect/",
      "terms": ["provider", "providers"],
      "reason": "The OpenCode model-provider connect path — \"Provider\", \"Connect provider\", \"Saving your provider key…\". ADR 260804-021140's registry allows `provider` in OpenCode's model-provider picker, which never co-renders with the Connections page; the word here means whoever serves the model, not a service an agent acts on."
    },
    {
      "path": "apps/client/src/layers/features/status/ui/ModelSelectionList.tsx",
      "terms": ["provider"],
      "reason": "\"Connect a provider to search everything you can run\" is the model picker's own empty state — the same model-provider sense the ADR keeps for OpenCode, offering the same action runtime-connect performs."
    },
    {
      "path": "apps/client/src/layers/shared/ui/unverified-catalog-notice.tsx",
      "terms": ["provider"],
      "reason": "The unverified model-catalog notice, shown beside the model picker and offering the same action: the model-provider sense, never a Connections-domain account."
    },
    {
      "path": "apps/client/src/layers/features/dashboard-sidebar/model/fixtures/",
      "terms": ["adapter"],
      "reason": "Sidebar journey fixtures: invented session titles a fictional developer typed (\"Port the codex adapter\"). Simulated user content, not DorkOS's authored copy — the same reason the gate skips `__tests__/` — and the module header says to read these, never edit them."
    },
    {
      "path": "apps/site/src/app/(marketing)/marketplace/privacy/page.tsx",
      "terms": ["adapter"],
      "reason": "\"The package type (agent, plugin, skill-pack, adapter)\" lists what the marketplace records about an install, naming the manifest keys an author writes — the marketplace-author domain the ADR's registry keeps."
    },
    {
      "path": "apps/site/src/layers/features/marketing/lib/comparisons.ts",
      "terms": ["adapter", "provider", "connector", "connectors"],
      "reason": "Comparison pages describe OTHER products in their own words, and a competitor's feature name is not ours to rename: DeepSeek Harness's plugin architecture (\"the model adapter, the tools, the session log\"), Grok Bot's Plugins screen, whose own documentation calls what it installs \"connectors\", and the model-provider sense in \"whatever provider or local model you chose\", which the ADR keeps anyway. None of it is DorkOS naming its own Connections surface — the rows that describe DorkOS say \"connection\"."
    }
  ]
}
