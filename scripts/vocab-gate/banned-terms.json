{
  "$comment": "Retired vocabulary the DOR-855+ Language & IA program has moved off of, banned wave by wave (plans/language-ia-simplification.md). Adding a wave here is the ONLY change extending the gate needs — check-vocab-gate.ts and its allowlist mechanism stay put. A term is matched literally and case-insensitively, fenced with a word boundary at each end that HAS a word character: 'connection' does not fire on 'Connections', 'ConnectionState' or 'reconnecting', while a punctuation term like '...' matches wherever it appears in a copy position.",
  "waves": [
    {
      "id": "wave-1",
      "issue": "DOR-855",
      "note": "\"Connection\" stops meaning network health in user copy — the word belongs to the Connections domain only (plan D2). Legitimate Connections-domain usage is allowlisted per file in allowlist.json, not exempted here.",
      "terms": ["connection"]
    },
    {
      "id": "wave-2",
      "issue": "DOR-1517",
      "note": "\"Mission control\" and \"cockpit\" are retired for good — the operator ruled both out on 2026-08-24 and the category phrase became \"one place\" (meta/website-copy/decisions.md, Decision 18; meta/brand-foundation.md §10). This wave covers render-path strings in app source. The prose surfaces an AST walk cannot see — READMEs, docs MDX, blog posts, context7.json, the PWA manifest — are guarded by scripts/check-banned-words.sh instead. GitHub ships a product literally named \"Mission Control\", so a genuine reference to it is allowlisted per file rather than exempted here.",
      "terms": ["mission control", "cockpit"]
    },
    {
      "id": "wave-3",
      "issue": "DOR-1756",
      "note": "Typography, not vocabulary — but the same failure and the same fix. The app shipped 161 strings spelling an ellipsis \"…\" and 40 spelling it \"...\", and 88 apostrophes as `&apos;` beside 72 as `&rsquo;`, which render at different widths and read as two products. The house rule is one character each, written literally: `…`, `’`, `“`, `”` (contributing/design-system.md, \"Punctuation\"). `&quot;` joined the ban after review found it live in production copy (`&quot;{name}&quot; will stop working…`, `MessagingConnections.tsx`) — it is the escape `react/no-unescaped-entities` actually pushes authors toward for a straight `\"`, not `&ldquo;`/`&rdquo;`, which nobody in this codebase writes. A term here is matched literally, not as a whole word — see termMatcher() in check-vocab-gate.ts.",
      "terms": ["...", "&apos;", "&rsquo;", "&ldquo;", "&rdquo;", "&quot;"]
    }
  ]
}
