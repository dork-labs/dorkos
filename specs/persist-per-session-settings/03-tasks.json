{
  "spec": "specs/persist-per-session-settings/02-specification.md",
  "slug": "persist-per-session-settings",
  "generatedAt": "2026-06-08T00:00:00Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[persist-per-session-settings] [P1] Add 5 nullable session-settings columns + Drizzle migration",
      "description": "Extend the existing `session_metadata` table (ADR-0255) with five nullable per-session settings columns so settings can survive idle eviction and server restart.\n\nFILE: `packages/db/src/schema/sessions.ts`. Add the `integer` import and the five columns. Replace the current table definition so it reads exactly:\n\n```ts\nimport { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core';\n\nexport const sessionMetadata = sqliteTable('session_metadata', {\n  // --- Immutable identity (first-write-wins; ADR-0255) ---\n  sessionId: text('session_id').primaryKey(),\n  runtime: text('runtime').notNull(),\n  agentPath: text('agent_path'),\n  createdAt: text('created_at').notNull(),\n  // --- Mutable per-session settings (last-write-wins; ADR-0260) ---\n  // NULL = \"no explicit preference; use the runtime's default.\"\n  permissionMode: text('permission_mode'),\n  model: text('model'),\n  effort: text('effort'),\n  fastMode: integer('fast_mode', { mode: 'boolean' }),\n  autoMode: integer('auto_mode', { mode: 'boolean' }),\n});\n```\n\nKeep the existing `SessionMetadata` / `NewSessionMetadata` inferred type exports and the existing comment about `createdAt` being ISO 8601 text. Preserve the column-group comments verbatim (immutable identity vs mutable settings; `NULL = runtime default`).\n\nGENERATE THE MIGRATION: run `pnpm --filter @dorkos/db db:generate`. This emits a new `packages/db/drizzle/0015_*.sql` containing `ALTER TABLE \"session_metadata\" ADD COLUMN ...` statements for all five columns (existing migrations run 0000 through 0014). Commit the generated `0015_*.sql` and the updated `packages/db/drizzle/meta/` snapshot files.\n\nMigrations auto-apply at boot via `migrate(db, { migrationsFolder })` in `runMigrations()` (`packages/db/src/index.ts:41-44`); no manual apply step.\n\nACCEPTANCE CRITERIA:\n- All five columns are nullable, so the migration is purely additive (`ADD COLUMN`) and backward-compatible — existing rows get `NULL` (= runtime default), no backfill.\n- `pnpm --filter @dorkos/db db:check` passes (the CI gate fails if generated SQL is uncommitted), confirming the schema and generated SQL are in sync.\n- A fresh DB boot creates the columns; an existing DB boot applies the `ALTER`s without data loss.\n- `pnpm typecheck` passes.\nVERIFY: `pnpm --filter @dorkos/db db:generate && pnpm --filter @dorkos/db db:check && pnpm typecheck`.",
      "activeForm": "Adding session_metadata settings columns and migration",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.2"]
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[persist-per-session-settings] [P1] Add SessionSettings schema, SessionSettingsPort, and capability default",
      "description": "Establish the shared contract: one `SessionSettings` type, a narrow injectable port, and a runtime-declared default permission mode. No runtime/route behavior change in this task — contract only.\n\nFILE 1: `packages/shared/src/schemas.ts`. The five settings fields are currently inlined in `UpdateSessionRequestSchema` (lines 131-142). Add a reusable `SessionSettingsSchema` and refactor `UpdateSessionRequestSchema` to extend it. Insert immediately before the existing `UpdateSessionRequestSchema` declaration and replace that declaration:\n\n```ts\nexport const SessionSettingsSchema = z.object({\n  permissionMode: PermissionModeSchema.optional(),\n  model: z.string().optional(),\n  effort: EffortLevelSchema.optional(),\n  fastMode: z.boolean().optional(),\n  autoMode: z.boolean().optional(),\n});\nexport type SessionSettings = z.infer<typeof SessionSettingsSchema>;\n\n// Refactor the existing request schema to reuse it:\nexport const UpdateSessionRequestSchema = SessionSettingsSchema.extend({\n  title: z.string().min(1).max(200).optional(),\n}).openapi('UpdateSessionRequest');\n```\n\nKeep the existing `export type UpdateSessionRequest = z.infer<typeof UpdateSessionRequestSchema>;`. `PermissionModeSchema` (line 17) and `EffortLevelSchema` (line 98) already exist in this file. Preserve the `.openapi('UpdateSessionRequest')` registration so OpenAPI docs are unaffected.\n\nFILE 2: `packages/shared/src/agent-runtime.ts`. Add the narrow port interface and the optional setter, mirroring the existing `AgentRegistryPort`/`RelayPort` + `setMeshCore`/`setRelay` pattern. Import `SessionSettings` from `./types.js` (add it to the existing type import block at the top). Add the port interface near `AgentRegistryPort`/`RelayPort` (around line 65):\n\n```ts\n/**\n * Narrow port for durable per-session settings. The API core implements this\n * (over session_metadata) and injects it into runtimes; runtimes never touch\n * the DB directly. Mirrors AgentRegistryPort/RelayPort. See ADR-0260.\n */\nexport interface SessionSettingsPort {\n  getSessionSettings(sessionId: string): Promise<SessionSettings | null>;\n  saveSessionSettings(sessionId: string, settings: SessionSettings): Promise<void>;\n}\n```\n\nAdd the optional setter on the `AgentRuntime` interface, in the `--- Dependency injection (optional) ---` block alongside `setMeshCore?`/`setRelay?` (around line 461-465):\n\n```ts\n/** Inject the core session-settings store for durable hydrate/write-through. */\nsetSessionSettings?(port: SessionSettingsPort): void;\n```\n\nAdd a runtime-declared default to `RuntimeCapabilities.permissionModes` (currently lines 131-134). Change the shape to:\n\n```ts\npermissionModes: {\n  supported: boolean;\n  /** Mode id used when a session has no stored preference (NULL in the store). */\n  default: string;\n  values: PermissionModeDescriptor[];\n};\n```\n\nNote: `SessionSettings` will need to be exported from `@dorkos/shared/types` — it is defined in `schemas.ts` and `types.ts` re-exports schema types; add the re-export there if the barrel does not pick it up automatically.\n\nACCEPTANCE CRITERIA:\n- `SessionSettingsSchema` exists; `UpdateSessionRequestSchema` is `SessionSettingsSchema.extend({ title })` and still has all original fields (permissionMode, model, effort, fastMode, autoMode, title) and the OpenAPI registration.\n- `SessionSettingsPort` and `AgentRuntime.setSessionSettings?` exist; the setter is optional.\n- `RuntimeCapabilities.permissionModes` has a required `default: string` field.\n- `pnpm typecheck` passes for `@dorkos/shared` (this task intentionally breaks the two capability constants that omit `default` — those are fixed in task 2.2).\nVERIFY: `pnpm --filter @dorkos/shared typecheck` (expect only the two missing-`default` errors in the server's runtime-constants, addressed by 2.2).",
      "activeForm": "Adding SessionSettings schema, port, and capability default",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Core",
      "subject": "[persist-per-session-settings] [P2] Implement session-settings store on RuntimeRegistry + unit tests",
      "description": "Make `RuntimeRegistry` the authoritative owner of session-settings persistence by implementing the `SessionSettingsPort` structurally (it already owns `session_metadata` and the DB handle and can satisfy the `runtime NOT NULL` constraint via `getSessionRuntimeType`).\n\nFILE: `apps/server/src/services/core/runtime-registry.ts`. Add three methods to the `RuntimeRegistry` class. Import `SessionSettings` from `@dorkos/shared/types` (or `@dorkos/shared/agent-runtime`) at the top. Implement:\n\n```ts\nasync getSessionSettings(sessionId: string): Promise<SessionSettings | null> {\n  const db = this.requireDb('getSessionSettings');\n  const row = db\n    .select({\n      permissionMode: sessionMetadata.permissionMode,\n      model: sessionMetadata.model,\n      effort: sessionMetadata.effort,\n      fastMode: sessionMetadata.fastMode,\n      autoMode: sessionMetadata.autoMode,\n    })\n    .from(sessionMetadata)\n    .where(eq(sessionMetadata.sessionId, sessionId))\n    .get();\n  if (!row) return null;\n  // NULL column -> omitted key (do not surface explicit nulls as values).\n  const settings: SessionSettings = {};\n  if (row.permissionMode != null) settings.permissionMode = row.permissionMode as SessionSettings['permissionMode'];\n  if (row.model != null) settings.model = row.model;\n  if (row.effort != null) settings.effort = row.effort as SessionSettings['effort'];\n  if (row.fastMode != null) settings.fastMode = row.fastMode;\n  if (row.autoMode != null) settings.autoMode = row.autoMode;\n  return settings;\n}\n\nasync saveSessionSettings(sessionId: string, settings: SessionSettings): Promise<void> {\n  const db = this.requireDb('saveSessionSettings');\n  // only keys explicitly provided are written\n  const patch: Partial<typeof sessionMetadata.$inferInsert> = {};\n  if (settings.permissionMode !== undefined) patch.permissionMode = settings.permissionMode;\n  if (settings.model !== undefined) patch.model = settings.model;\n  if (settings.effort !== undefined) patch.effort = settings.effort;\n  if (settings.fastMode !== undefined) patch.fastMode = settings.fastMode;\n  if (settings.autoMode !== undefined) patch.autoMode = settings.autoMode;\n  if (Object.keys(patch).length === 0) return;\n  const runtime = await this.getSessionRuntimeType(sessionId); // infer 'claude-code' if no row\n  db.insert(sessionMetadata)\n    .values({ sessionId, runtime, createdAt: new Date().toISOString(), ...patch })\n    .onConflictDoUpdate({ target: sessionMetadata.sessionId, set: patch }) // identity columns untouched on conflict\n    .run();\n}\n\n/** Batch read for the list-route overlay (not part of the narrow port). */\ngetSessionSettingsMany(ids: string[]): Map<string, SessionSettings> { /* single `WHERE sessionId IN (...)` */ }\n```\n\nImplement `getSessionSettingsMany` with a single `db.select(...).from(sessionMetadata).where(inArray(sessionMetadata.sessionId, ids)).all()` query (import `inArray` from `@dorkos/db` / `drizzle-orm`; add it to the re-export in `packages/db/src/index.ts` line 54 if not already exported), mapping each row to a `SessionSettings` with the same NULL-to-omitted logic as `getSessionSettings`. Return an empty `Map` when `ids` is empty (skip the query). Add full TSDoc to all three methods.\n\nUPSERT semantics to preserve: creates the row (with inferred runtime) if a PATCH arrives before the first message; otherwise updates only the provided setting columns, leaving identity (`runtime`, `agentPath`, `createdAt`) intact via `onConflictDoUpdate({ set: patch })`.\n\nTESTS: `apps/server/src/services/core/__tests__/runtime-registry.test.ts` (extend the existing file or create it). Use a real in-memory better-sqlite3 DB via `createDb(':memory:')` + `runMigrations(db)` from `@dorkos/db`, then `registry.setDb(db)`. Cases:\n- `saveSessionSettings` UPSERT creates a row with the inferred runtime (`'claude-code'`) when none exists.\n- `saveSessionSettings` on an existing row updates only the provided columns and leaves `runtime`/`agentPath`/`createdAt` intact on conflict (seed a row via `persistSessionRuntime` first, then patch only `permissionMode`).\n- `saveSessionSettings` with an empty/all-undefined settings object is a no-op (no row created).\n- `getSessionSettings` maps `NULL` columns to omitted keys (a row with only `permissionMode` set returns `{ permissionMode }` with no `model`/`effort` keys).\n- Booleans round-trip correctly (`fastMode: true`, `autoMode: false` persist and read back as booleans, not 1/0).\n- `getSessionSettingsMany` batch-reads multiple ids in one call and returns a `Map`; empty input returns an empty `Map`.\n\nACCEPTANCE CRITERIA: all unit tests pass; methods satisfy `SessionSettingsPort` structurally.\nVERIFY: `pnpm vitest run apps/server/src/services/core/__tests__/runtime-registry.test.ts` and `pnpm typecheck`.",
      "activeForm": "Implementing RuntimeRegistry session-settings store",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": []
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Core",
      "subject": "[persist-per-session-settings] [P2] Hydrate + write-through in claude-code runtime; wire port; seed all five settings",
      "description": "Make the claude-code runtime a pure executor that hydrates from and writes through to the core store, and wire the port at the composition root. Also fix the two capability constants to declare a `default`.\n\nFILE 1: `packages/shared/src/agent-runtime.ts` — widen `SessionOpts` and `MessageOpts` to carry all five settings. `SessionOpts` (currently lines 143-148) becomes the settings plus cwd/hasStarted:\n\n```ts\nexport interface SessionOpts extends SessionSettings {\n  permissionMode: PermissionMode; // required here (final fallback already resolved)\n  cwd?: string;\n  hasStarted?: boolean;\n}\n```\n\nNote `SessionSettings.permissionMode` is optional but `SessionOpts.permissionMode` must stay required (the caller resolves the fallback before calling). Add `model?`, `effort?`, `fastMode?`, `autoMode?` to `MessageOpts` (currently lines 150-163) by extending `SessionSettings` (keep the existing `cwd`, `systemPromptAppend`, `uiState`, `title` fields and their TSDoc).\n\nFILE 2: `apps/server/src/services/runtimes/claude-code/sessions/session-store.ts`. Add the injected port + default mode and a `configureSettings` method to the `SessionStore` class:\n\n```ts\nprivate settingsPort?: SessionSettingsPort;\nprivate defaultPermissionMode: PermissionMode = 'default';\n/** Inject the core settings store and the runtime's default fallback mode. */\nconfigureSettings(port: SessionSettingsPort, defaultMode: PermissionMode): void {\n  this.settingsPort = port;\n  this.defaultPermissionMode = defaultMode;\n}\n```\n\nImport `SessionSettingsPort` from `@dorkos/shared/agent-runtime`. In `ensureForMessage` (lines 86-119), in the cold path (`if (!existing)`), read persisted settings once and seed with precedence `per-send override (opts) -> persisted -> runtime default`. Replace the current `this.ensureSession(sessionId, { permissionMode: opts?.permissionMode ?? 'default', cwd: opts?.cwd, hasStarted: hasTranscript })` block with:\n\n```ts\nconst persisted = await this.settingsPort?.getSessionSettings(sessionId);\nthis.ensureSession(sessionId, {\n  permissionMode: opts?.permissionMode ?? persisted?.permissionMode ?? this.defaultPermissionMode,\n  model: opts?.model ?? persisted?.model,\n  effort: opts?.effort ?? persisted?.effort,\n  fastMode: opts?.fastMode ?? persisted?.fastMode,\n  autoMode: opts?.autoMode ?? persisted?.autoMode,\n  cwd: opts?.cwd,\n  hasStarted: hasTranscript,\n});\n```\n\nIn `ensureSession` (lines 53-78), seed all five settings on the in-memory `AgentSession` (currently only `permissionMode`/`cwd`/`hasStarted` are set). Add to the `this.sessions.set(...)` object: `model: opts.model, effort: opts.effort, fastMode: opts.fastMode, autoMode: opts.autoMode` (the `AgentSession` type in `agent-types.ts` already has these optional fields).\n\nWRITE-THROUGH in `updateSession` (lines 155-201): persist the user's choice FIRST so intent is durable regardless of the live query. At the top of the method (after resolving/auto-creating the session), build a `changedSettings` object from the provided opts (only defined keys) and `await this.settingsPort?.saveSessionSettings(sessionId, changedSettings)` before mutating in-memory state. (The best-effort live-apply / no-revert change to the `setPermissionMode` block is task 2.3 — do NOT change the revert behavior here; 2.3 owns that.) Only user-driven PATCHes reach `updateSession`, so transient per-send overrides are never persisted.\n\nFILE 3: `apps/server/src/services/runtimes/claude-code/claude-code-runtime.ts`. Add a `setSessionSettings` method forwarding to the store, placed in the dependency-injection block alongside `setMeshCore` (around line 119-122):\n\n```ts\n/** Inject the core session-settings store for durable hydrate/write-through. */\nsetSessionSettings(port: SessionSettingsPort): void {\n  this.sessionStore.configureSettings(port, CLAUDE_CODE_CAPABILITIES.permissionModes.default);\n}\n```\n\nImport `SessionSettingsPort` from `@dorkos/shared/agent-runtime`. `CLAUDE_CODE_CAPABILITIES` is already imported (line 33).\n\nFILE 4: `apps/server/src/services/runtimes/claude-code/runtime-constants.ts`. Add `default: 'default',` to `CLAUDE_CODE_CAPABILITIES.permissionModes` (insert between `supported: true,` on line 30 and `values: [` on line 31).\n\nFILE 5: `apps/server/src/services/runtimes/test-mode/runtime-constants.ts`. Add a `default` to `TEST_MODE_CAPABILITIES.permissionModes` (around line 36-37). Use `default: 'always-allow',` (the first declared test-mode mode id; test-mode ids deliberately differ from Claude's).\n\nFILE 6: `apps/server/src/index.ts`. Wire the port at the composition root. `runtimeRegistry.setDb(db)` is at line 131; `claudeRuntime` is registered at line 191; `claudeRuntime.setMeshCore(meshCore)` is at line 279. Add, after `setDb` has run and `claudeRuntime` is non-null (alongside the `if (claudeRuntime) { claudeRuntime.setMeshCore(meshCore); ... }` block near line 278):\n\n```ts\nclaudeRuntime.setSessionSettings(runtimeRegistry); // registry satisfies SessionSettingsPort structurally\n```\n\n`TestModeRuntime` leaves the optional `setSessionSettings` setter unimplemented (no behavior change) — do not add it there.\n\nACCEPTANCE CRITERIA:\n- Cold `ensureForMessage` with a persisted row hydrates all five settings with precedence opts > persisted > runtime default.\n- `ensureSession` seeds all five settings on the in-memory session.\n- `updateSession` calls `saveSessionSettings` with exactly the changed fields, persisting before in-memory mutation.\n- Both capability constants compile with the new required `default` field; the test-mode `default` is a valid test-mode mode id.\n- `pnpm typecheck`, `pnpm lint`, and the existing claude-code/session-store/test-mode capabilities tests pass.\nVERIFY: `pnpm typecheck && pnpm lint && pnpm vitest run apps/server/src/services/runtimes/test-mode/__tests__/capabilities.test.ts`.",
      "activeForm": "Wiring hydrate/write-through and the settings port",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "1.2", "2.1"],
      "parallelWith": []
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Core",
      "subject": "[persist-per-session-settings] [P2] Always-on bypass capability + best-effort live mode switch (ADR-0261)",
      "description": "Allow instant permission-mode switching on active sessions (including escalation to bypass) with no 422/revert, by always launching SDK queries with the bypass capability and making the live `setPermissionMode` best-effort.\n\nFILE 1: `apps/server/src/services/runtimes/claude-code/messaging/message-sender.ts`. The current block at lines 235-238 reads:\n\n```ts\nsdkOptions.permissionMode = session.permissionMode;\nif (session.permissionMode === 'bypassPermissions') {\n  sdkOptions.allowDangerouslySkipPermissions = true;\n}\n```\n\nReplace it so the flag is set UNCONDITIONALLY, decoupled from the current mode (remove the `if` guard):\n\n```ts\n// Was: only when session.permissionMode === 'bypassPermissions'\nsdkOptions.permissionMode = session.permissionMode;\nsdkOptions.allowDangerouslySkipPermissions = true; // always — grants the capability to switch live\n```\n\nWhy this is safe (empirically verified, ADR-0261): an isolated SDK probe ran `query()` in `default` mode with and without the flag; both invoked `canUseTool` for `Write` and honored a `deny` (file not written) — identical behavior. The flag is a pure capability gate the SDK consults only when `permissionMode === 'bypassPermissions'`; it does not weaken `default`/`acceptEdits`/`plan`/`dontAsk` (documented evaluation order: Hooks -> Deny -> Permission mode -> Allow -> `canUseTool`). The single tool-executing launch point is the `query(...)` call at `message-sender.ts:290`. The warm-up `query()` in `runtime-cache.ts:184` (`query({ prompt: neverYield, options: { cwd } })`) never executes tools and is unaffected — leave it alone.\n\nFILE 2: `apps/server/src/services/runtimes/claude-code/sessions/session-store.ts`. Make `updateSession`'s live mode switch best-effort. The current block (lines 176-195) reverts and re-throws on failure:\n\n```ts\nif (opts.permissionMode) {\n  const prevMode = session.permissionMode;\n  session.permissionMode = opts.permissionMode;\n  if (session.activeQuery) {\n    try {\n      await session.activeQuery.setPermissionMode(\n        opts.permissionMode as Parameters<typeof session.activeQuery.setPermissionMode>[0]\n      );\n    } catch (err) {\n      session.permissionMode = prevMode;\n      logger.error('[updateSession] setPermissionMode failed', { sessionId, err });\n      throw err;\n    }\n  }\n  ...\n}\n```\n\nChange it so the persisted/in-memory mode is kept on live-apply failure — do NOT revert (`session.permissionMode = prevMode`) and do NOT re-throw. On a failed live `setPermissionMode`, log it and keep the new mode (it applies on the next turn). With the flag now always present (FILE 1), the bypass switch succeeds instantly mid-run. The write-through `saveSessionSettings` (added in task 2.2) already persisted intent first, so the order is: persist -> set in-memory mode -> best-effort live apply.\n\nFILE 3: `apps/server/src/routes/sessions.ts`. The PATCH handler (lines 132-159) wraps `runtime.updateSession(...)` in a try/catch that returns `sendError(res, 422, ..., 'PERMISSION_MODE_FAILED')`. Since `updateSession` no longer throws on live-apply failure, this 422 path becomes dead. Remove the 422 try/catch around `runtime.updateSession` and call it directly (keep the `if (!updated) return sendError(res, 404, ...)` check). Do not introduce any new error code for live-apply failures — the contract is now no-422/no-revert.\n\nACCEPTANCE CRITERIA:\n- `message-sender.ts` sets `sdkOptions.allowDangerouslySkipPermissions = true` unconditionally; the `if (session.permissionMode === 'bypassPermissions')` guard is gone.\n- `session-store.ts` `updateSession` no longer has the `prevMode` revert and no longer re-throws on live-apply failure; the failure is swallowed (logged) and the new mode is retained.\n- `sessions.ts` PATCH no longer returns 422 for a failed live mode switch.\n- The warm-up query in `runtime-cache.ts:184` is untouched.\n- `pnpm typecheck` and `pnpm lint` pass; existing message-sender / session-store tests pass (update any test that asserted the old 422/revert behavior).\nVERIFY: `pnpm typecheck && pnpm lint && pnpm vitest run apps/server/src/services/runtimes/claude-code/__tests__/`.",
      "activeForm": "Making bypass capability always-on and live switch best-effort",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.2"],
      "parallelWith": ["2.4"]
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "Core",
      "subject": "[persist-per-session-settings] [P2] Overlay persisted settings in GET /:id and GET / route handlers",
      "description": "Unify the three display surfaces (session-list badge, in-session toolbar, enforcement) by overlaying the persisted store over transcript-derived values in the HTTP read routes. Display overlay lives in the route (HTTP-only) so the narrow port is not widened with batch-read methods; the route already imports `runtimeRegistry`.\n\nFILE: `apps/server/src/routes/sessions.ts`.\n\nGET /:id (lines 53-67): after `const session = await runtime.getSession(projectDir, internalSessionId);` and the `if (!session) return 404` check, overlay persisted settings. Read `const stored = await runtimeRegistry.getSessionSettings(internalSessionId);` and, when `stored` is non-null, assign each defined stored field onto `session` (store wins; transcript = fallback for legacy rows). For example:\n\n```ts\nconst stored = await runtimeRegistry.getSessionSettings(internalSessionId);\nif (stored) {\n  if (stored.permissionMode !== undefined) session.permissionMode = stored.permissionMode;\n  if (stored.model !== undefined) session.model = stored.model;\n  if (stored.effort !== undefined) session.effort = stored.effort;\n  if (stored.fastMode !== undefined) session.fastMode = stored.fastMode;\n  if (stored.autoMode !== undefined) session.autoMode = stored.autoMode;\n}\nres.json(session);\n```\n\nGET / (list, lines 25-40): after `const sessions = await runtime.listSessions(projectDir);`, batch-read with one query and overlay per session (no N+1). Use the session ids from the listed sessions:\n\n```ts\nconst storedMany = runtimeRegistry.getSessionSettingsMany(sessions.map((s) => s.id));\nfor (const s of sessions) {\n  const stored = storedMany.get(s.id);\n  if (!stored) continue;\n  if (stored.permissionMode !== undefined) s.permissionMode = stored.permissionMode;\n  if (stored.model !== undefined) s.model = stored.model;\n  if (stored.effort !== undefined) s.effort = stored.effort;\n  if (stored.fastMode !== undefined) s.fastMode = stored.fastMode;\n  if (stored.autoMode !== undefined) s.autoMode = stored.autoMode;\n}\nres.json(sessions.slice(0, limit));\n```\n\nNote `getSessionSettingsMany` is synchronous (returns a `Map`, no `await`); `getSessionSettings` is async. Consider extracting the per-session field overlay into a small local helper to avoid duplicating the five assignments (DRY / 3-strike rule). A legacy session with no stored row must fall back to the transcript-derived values without error (the overlay is skipped when `stored` is null/missing).\n\nACCEPTANCE CRITERIA:\n- `GET /:id` returns stored values over transcript-derived ones; legacy sessions (no row) fall back to transcript without error.\n- `GET /` overlays stored values via a single batch query (no per-session DB call).\n- After this, badge (list), toolbar (`getSession`), and enforcement read the same persisted values.\n- `pnpm typecheck` and `pnpm lint` pass.\nVERIFY: `pnpm typecheck && pnpm lint && pnpm vitest run apps/server/src/routes/__tests__/sessions.test.ts` (if present).",
      "activeForm": "Overlaying persisted settings in session read routes",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": ["2.3"]
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Testing",
      "subject": "[persist-per-session-settings] [P3] Full test suite: hydration, write-through, overlay, flag-inertness, instant switch",
      "description": "Author the full Vitest suite from the spec's Testing Strategy, each test with a purpose comment. Some store-only cases are covered by task 2.1; this task covers the remaining hydration/write-through/overlay/regression cases and confirms the whole feature green.\n\nTEST CASES (use `vi.mock` where needed; real in-memory `createDb(':memory:')` + `runMigrations` for store-backed cases; `FakeAgentRuntime`/`collectSseEvents` from `@dorkos/test-utils` and SDK scenario builders in `services/runtimes/claude-code/__tests__/sdk-scenarios.ts` where appropriate):\n\n- RuntimeRegistry settings store (`apps/server/src/services/core/__tests__/runtime-registry.test.ts`): `saveSessionSettings` UPSERT creates a row with inferred runtime when none exists; updates only provided columns and leaves `runtime`/`agentPath` intact on conflict; `getSessionSettings` maps `NULL`->omitted; round-trips booleans correctly; `getSessionSettingsMany` batch read. (Extends what task 2.1 wrote — ensure all listed cases exist.)\n- Hydration (`session-store` / `claude-code-runtime` tests): cold `ensureForMessage` with a persisted `bypassPermissions` row -> the in-memory session is `bypassPermissions` (regression test for the reported bug — set bypass, evict/recreate, assert hydrated bypass); precedence `opts override > persisted > runtime default`; no persisted row -> runtime default (`'default'`); all five settings hydrate, not just `permissionMode`.\n- Write-through: `updateSession` calls `saveSessionSettings` with exactly the changed fields; a per-send `MessageOpts.permissionMode` override does NOT trigger a write (assert `saveSessionSettings` is not called on the send path).\n- Display overlay (`sessions` route tests): `GET /:id` and `GET /` return store values over transcript-derived ones; a legacy session with no row falls back to the transcript without error.\n- Cross-runtime: a runtime that does not implement `setSessionSettings` still functions (the port is optional — assert no crash when the setter is absent); a stored mode not in the resolved runtime's capability `values` falls back to that runtime's `default`.\n- Flag-inertness regression guard (ADR-0261): with `allowDangerouslySkipPermissions: true` always set, `default` mode still routes to `canUseTool` and a `deny` still blocks the tool — assert the message-sender sets the flag unconditionally, and assert `canUseTool` gating is unchanged. This guards against a future SDK version making the flag non-inert.\n- Instant live switch: switching an active session to bypass persists the mode and does NOT 422 or revert; a failed live `setPermissionMode` is swallowed (mode still persisted for the next turn — assert no throw and the in-memory mode remains the new value).\n- Migration: `pnpm --filter @dorkos/db db:check` passes (generated SQL committed); fresh-DB boot creates the columns; existing-DB boot applies the `ALTER`s without data loss.\n\nFollow `.claude/rules/testing.md`: tests in `__tests__/`, `vi.clearAllMocks()` in `beforeEach`, no arbitrary timeouts, test behavior not implementation, restore any `console`/spy mocks in `afterEach`. SDK-level scenario builders must stay inside the `services/runtimes/claude-code/` ESLint boundary.\n\nACCEPTANCE CRITERIA: every case above exists with a purpose comment and passes. Full repo gates green.\nVERIFY: `pnpm test -- --run && pnpm typecheck && pnpm lint && pnpm --filter @dorkos/db db:check`.",
      "activeForm": "Writing the full settings-persistence test suite",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.1", "2.2", "2.3", "2.4"],
      "parallelWith": ["3.2"]
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Testing",
      "subject": "[persist-per-session-settings] [P3] Manual reproduction of both fixed bugs",
      "description": "Manually reproduce both original bugs end-to-end and confirm they are fixed, using a live dev server (`pnpm dev` or `pnpm dev:dogfood`) and the testing agent / browser.\n\nREPRO 1 — eviction/restart durability (the reported bug): In the UI, set a session's permission mode to `bypassPermissions` (badge shows \"Permissions bypassed\"). Then either wait past the 30-minute idle eviction (`SESSIONS.TIMEOUT_MS`) OR restart the dev server (faster — wipes the in-memory warm cache). Send a message that writes a file. CONFIRM: NO approval prompt appears (the runtime hydrated `bypassPermissions` from `session_metadata` on the cold path), and the badge/toolbar still show bypass and agree with what the agent enforces.\n\nREPRO 2 — instant live switch (ADR-0261): Start an actively-running session in a non-bypass mode, then switch it to `bypassPermissions` mid-run via the permission-mode dropdown. CONFIRM: no 422 error and no optimistic-UI revert; bypass takes effect for the rest of the current turn (the live `setPermissionMode` succeeds because the query was launched with `allowDangerouslySkipPermissions`).\n\nOptionally inspect the SQLite `session_metadata` row (e.g. via `apps/server/.temp/.dork/` in dev or `~/.dork/` in dogfood) to confirm `permission_mode='bypassPermissions'` is persisted.\n\nACCEPTANCE CRITERIA: both repros confirm the fixed behavior (no prompt after eviction/restart; no 422/revert on live switch). Record the observed outcome.\nVERIFY: manual — drive the live app and observe; confirm the persisted DB row when in doubt.",
      "activeForm": "Manually reproducing and confirming both bug fixes",
      "size": "small",
      "priority": "medium",
      "dependencies": ["2.1", "2.2", "2.3", "2.4"],
      "parallelWith": ["3.1"]
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "Documentation",
      "subject": "[persist-per-session-settings] [P4] TSDoc, contributing guide note, and flip ADR-0260/0261 to accepted",
      "description": "Document the new contract and flip the two ADRs to accepted once implementation is verified.\n\nTSDOC (verify present / add): `SessionSettingsPort` (in `packages/shared/src/agent-runtime.ts` — already added in 1.2); the new `RuntimeRegistry` methods `getSessionSettings`/`saveSessionSettings`/`getSessionSettingsMany` (in `apps/server/src/services/core/runtime-registry.ts` — added in 2.1); the schema column groups in `packages/db/src/schema/sessions.ts` documenting immutable identity (first-write-wins; ADR-0255) vs mutable settings (last-write-wins; ADR-0260) and that `NULL = runtime default` (the group comments from task 1.1 satisfy this — confirm they read clearly). TSDoc is lint-enforced (`eslint-plugin-jsdoc`) on exports.\n\nCONTRIBUTING GUIDE: add the \"core owns session-settings persistence via a narrow port\" pattern to `contributing/architecture.md` (or the runtime/session guide if one is a better fit — check `contributing/INDEX.md`). Describe: persisted store = source of truth, in-memory `AgentSession` = warm cache, SDK transcript = legacy fallback; runtimes hydrate in `ensureForMessage` and write through in `updateSession` via the injected `SessionSettingsPort`; display overlay lives in the route. Aim it at future runtime authors (e.g. Codex) so they inherit durable settings by accepting one injected port. Follow `writing-developer-guides` conventions.\n\nADRs: flip `decisions/0260-persist-per-session-settings-via-narrow-core-port.md` and `decisions/0261-always-launch-with-dangerously-skip-permissions-for-instant-mode-switching.md` from `proposed` to `accepted` (use `/adr:review`), and update `decisions/manifest.json` accordingly. Only flip after tests (3.1) and manual repro (3.2) confirm the behavior.\n\nACCEPTANCE CRITERIA: TSDoc present on the listed exports (lint clean); the contributing guide has the narrow-port pattern section; both ADRs are `accepted` in their frontmatter and in `decisions/manifest.json`.\nVERIFY: `pnpm lint` (TSDoc) and `grep -c accepted decisions/0260-*.md decisions/0261-*.md`.",
      "activeForm": "Documenting the port pattern and accepting the ADRs",
      "size": "small",
      "priority": "medium",
      "dependencies": ["3.1"],
      "parallelWith": []
    }
  ]
}
