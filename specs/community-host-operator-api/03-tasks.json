{
  "spec": "specs/community-host-operator-api/02-specification.md",
  "slug": "community-host-operator-api",
  "generatedAt": "2026-09-23T18:00:00Z",
  "mode": "full",
  "lastDecomposeDate": "2026-09-23",
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Phase 1 — Host API keys, limits, and usage",
      "subject": "[community-host-operator-api] [P1] Add host API keys: scoped, revocable credentials for host routes",
      "description": "Build P1 of the spec: a host-owned machine credential for `/api/v1/host/*` that can never reach community content.\n\n**Migration 0012 (whole file, including the limit tables task 1.2 uses):** `host_api_keys` (id, label, prefix, secret_hash UNIQUE, scopes text[] with a subset check, issued_via, issued_by_user_id NULL, created_at, expires_at, last_used_at, revoked_at, revoked_by_user_id NULL); `host_audit_events` gains `actor_kind text NOT NULL DEFAULT 'person' CHECK (actor_kind IN ('person','api_key','offline'))`, nullable `actor_user_id`, `actor_api_key_id uuid NULL REFERENCES host_api_keys(id)` and `CHECK ((actor_kind='person') = (actor_user_id IS NOT NULL) AND (actor_kind='api_key') = (actor_api_key_id IS NOT NULL))`; `community_creation_receipts.operator_user_id` nullable beside `operator_api_key_id` with an exactly-one check; `bootstrap_grants.revoked_by_api_key_id` with the revocation check widened to \"revoked_at set iff exactly one revoker set\"; `community_limits(community_id PK FK, max_active_members, max_storage_bytes, limits_version, updated_at)`; `member_limit_overrides(community_id, member_id, agents_per_member CHECK BETWEEN 1 AND 1000, updated_at, PRIMARY KEY (community_id, member_id))` with a composite tenant FK to `members(community_id, id)`; `managed_blobs_community_usage_idx ON managed_blobs(community_id, state, purpose) INCLUDE (byte_size)`; `entries_community_created_idx ON entries(community_id, created_at DESC)`. The deletion worker deletes `community_limits` and `member_limit_overrides` rows with the tenant.\n\n**Scopes:** `communities:read` (list, single read `GET /host/communities/:id` — new, same projection as the list — usage reads, import reads), `communities:write` (create pending_owner with short name and limits, reissue/revoke owner claims, abandon, set limits and member overrides, set/release short names), `communities:lifecycle` (suspend, resume, hold, release, request/cancel host deletion), `communities:import`. No scope manages keys or operators or reaches inside a community.\n\n**Token:** `dkh_` + 43 base64url chars (32 bytes from `randomToken()`); display `prefix` = first 10 chars, never used for lookup; only `hashSecret(secret)` stored. Keys are host-owned: revoking an operator does not revoke keys they issued; the key list shows the issuer.\n\n**Issuance:** `POST /api/v1/host/api-keys` (operator session only, never a key, plus `password` reauth via `auth.api.verifyPassword`; body label, scopes, `expiresInDays` 1–365 or null; browser default 90); response is the projection plus secret once, `Cache-Control: no-store`. `POST /host/api-keys/:id/rotate` (session + reauth; successor with same label/scopes; old key's `expires_at = now() + overlapMinutes`, 0–1,440). `POST /host/api-keys/:id/revoke` (session, no reauth, sets `revoked_at`, no un-revoke). `GET /host/api-keys` (session only, projections only). Offline CLI `node dist/host-keys.js issue --label <text> --scope <scope>… [--expires-in-days <n>]`, plus `list` and `revoke <id>`, against `COMMUNITY_DATABASE_URL`, prints the secret once, writes an audit row with `actor_kind='offline'`.\n\n**Pipeline:** `requireHostOperator` → `requireHostAuthority(c, auth, pool, scope)` returning `HostActor = {kind:'person', userId, name} | {kind:'api_key', keyId, scopes}`. With an `Authorization` header only the key is considered (a cookie on the same request is ignored); a non-`dkh_` bearer is `401 UNAUTHENTICATED`. Lookup by hash where `revoked_at IS NULL AND (expires_at IS NULL OR expires_at > now())`; a miss is `401` and counts against the socket peer via `limitAttempts` (new `COMMUNITY_HOST_KEY_ATTEMPTS_PER_MINUTE`, default 20, ceiling 100). Missing scope: `403 FORBIDDEN` \"This key does not allow that action.\" No header: today's session + `host_operators` check; a person holds every scope. `assertHostOperator` → `assertHostActor(client, actor)` re-reads the key row `FOR SHARE` inside every write transaction so a revocation that commits first wins. `last_used_at` updated at most once a minute per key, outside the request transaction, never failing the request. `bearer(c)` answers `401` for any `dkh_` token before a member-credential lookup. (Create's `limits` field lands in 1.2 and `shortName` in 3.1, each entering the idempotency payload hash.) Audit actions `api_key.issue|rotate|revoke`; changed-field names only, never values; reads not audited.\n\n**Wire:** add to `community-admin-wire.ts` `CommunityAdminHostApiKeyScopeSchema`, `CommunityAdminHostApiKeySchema`, `…ListSchema`, `…IssueRequestSchema`, `…RotateRequestSchema`, `…SecretResponseSchema`, `…RevokeRequestSchema` exactly as the spec's Wire schemas section writes them.\n\n**Host page:** an API keys section (list, issue with one-time reveal and copy, rotate, revoke with a confirmation naming the key).\n\n**Acceptance (release gate):** the full 16-row adversarial matrix in the spec's \"Adversarial matrix for P1\" passes against real Postgres, each refused row asserting no row changed in `members`, `connection_grants`, `agent_credentials`, `bootstrap_grants`, `entries`, `channels`: K_all on every content route (channels, entries, members, invites, agents, exports, attachment download, SSE) and the singleton alias is `401`; K_all cannot reissue claims for an active community (`409 STATE_CONFLICT`); a claim token without a session is `401`; K_all on key management is `403`; K_read on every mutating host route is `403` with no audit row; revoked and expired keys `401`; a revocation committing while a request waits on the community lock makes it fail `401`; cookie + invalid key is `401`; a member grant bearer on host routes is `401`; no response or log contains a `dkh_` secret, hash, or claim token beyond the one-time response; audit rows name `actor_kind='api_key'` and the key. Also: `communities:read` key cannot suspend (403) but a person can; the key row stores `hashSecret(secret)` and no column contains the secret; a rotated key works for `overlapMinutes` and fails one minute later (injected clock); the offline command's key works on the next request and writes an `offline` audit row.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding host API keys",
      "size": "xl",
      "priority": "high",
      "dependencies": [],
      "parallelWith": [],
      "files": [
        "apps/community/migrations/0012_host_keys_and_limits.sql",
        "apps/community/src/migrate.ts",
        "apps/community/src/schema.ts",
        "apps/community/src/host-authority.ts (new)",
        "apps/community/src/routes/host.ts",
        "apps/community/src/routes/host-keys.ts (new)",
        "apps/community/src/host-keys.ts (new, CLI)",
        "apps/community/src/data.ts",
        "apps/community/src/config.ts",
        "apps/community/src/browser/CommunityApp.tsx (host page: API keys section)",
        "packages/shared/src/community-admin-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Phase 1 — Host API keys, limits, and usage",
      "subject": "[community-host-operator-api] [P2] Add host-set community limits, typed cap errors, and a host usage read",
      "description": "Build P2 on the tables migration 0012 created in task 1.1.\n\n**Limits:** `community_limits.max_active_members` (default none) enforced at admission and reactivation in `routes/invites.ts` (not owner claim or first install); `community_limits.max_storage_bytes` (default none) at attachment and icon commit; `COMMUNITY_AGENTS_PER_OWNER` — **default stays 20, configured maximum stays 100** (operator, 2026-09-23; do not change `config.ts`'s default); `member_limit_overrides.agents_per_member` (1–1,000) per person. Effective agent limit = `override ?? config`. No community-wide agent cap, no history limit. Lowering a limit below use removes nothing; it only refuses the next growing action.\n\n**Errors (add to `CommunityWireErrorCodeSchema`):** `409 MEMBER_LIMIT_REACHED` \"This community is full. Ask its owner to make room.\"; `409 STORAGE_LIMIT_REACHED` \"This community is out of file space.\"; `409 AGENT_LIMIT_REACHED` \"You have reached your agent limit in this community.\" (replaces today's `429 RATE_LIMITED` on the agent cap). Upload and post rate windows stay `429`.\n\n**Enforcement:** members — inside the admission transaction after the invite row lock, lock `community_limits` `FOR UPDATE` (row created lazily; no row = no limit, no lock), count active members, refuse before any insert/reactivation; re-admitting an active person does not count twice. Storage — counted bytes are `SUM(byte_size)` of `managed_blobs` with `purpose IN ('attachment','icon') AND state IN ('stored','committed')`; exports exempt; `pending_delete` not counted. Fast pre-check on declared `byteSize` (icons ≤ 2 MiB) before upload; authoritative check in the commit transaction after `prepareManagedBlobCommit`'s lifecycle lock under `pg_advisory_xact_lock(hashtext('dorkos:storage:' || community_id))` only when a storage limit exists; refusal discards via `discardManagedBlob`. Agents — the existing count query reads the override and throws `AGENT_LIMIT_REACHED`; `/agents/recover` unchanged.\n\n**Routes:** `PUT /api/v1/host/communities/:id/limits` (`communities:write`, `limitsVersion` optimistic concurrency, first write uses 1; stale → `409 STATE_CONFLICT`; allowed in every lifecycle except `deletion_pending`; audit `community.limits`). `PUT /host/communities/:id/members/:memberId/limits` (`communities:write`; set or clear one override; `404` unless the member belongs to that community; response only `communityId, memberId, agentsPerMember, effectiveAgentsPerMember`; audit `member.limits`). `GET /host/communities/:id/usage` (`communities:read`) returns exactly `activeMembers`, `activeAgents`, storage by purpose (attachment, icon, export, import staging, pending delete, counted total), limits + `limitsVersion`, `lastPostDate` (UTC date of newest entry via `max(created_at)::date` on `entries_community_created_idx`, or null), `measuredAt`. `GET /host/usage?after=<id>&limit=1..100` pages the same in UUID order with `next`. No names, handles, emails, channel/message counts, file names, or per-person numbers. Create accepts `limits` in the same transaction and idempotency hash. Wire: `CommunityAdminLimitsSchema`, `…LimitsUpdateRequestSchema`, `…MemberLimitsRequestSchema`, `…MemberLimitsSchema`, `CommunityAdminUsageSchema`, `…UsagePageSchema` as the spec writes them.\n\n**UX:** host page Limits section per community (two number fields, empty = no limit, current use beside each); the invitation preview says \"This community is full. Ask its owner to make room.\" before sign-up; the DorkOS app enrollment flow shows the agent-limit message. No copy mentions a host, plan, or price.\n\n**Acceptance:** with `maxActiveMembers=3` and 3 active, a 4th admission is `409 MEMBER_LIMIT_REACHED` inserting no member, handle, or invite use; after one leaves the same invitation succeeds. Two admissions racing for the last seat (test-hook barrier) produce exactly one success. With `maxStorageBytes=10 MiB`, two concurrent 6 MiB uploads produce one committed attachment and one `409 STORAGE_LIMIT_REACHED` whose reservation is queued for deletion. An owner export succeeds when counted bytes equal the limit. Deleting a file frees space at once. With default config the 21st agent enrollment is `409 AGENT_LIMIT_REACHED` (not 429); with `COMMUNITY_AGENTS_PER_OWNER=100` the 101st is refused, a member override of 150 lets the 101st succeed for that member only, and another member still stops at 100; `COMMUNITY_AGENTS_PER_OWNER=101` fails config parsing. Usage for a seeded community equals independently computed sums and a JSON key scan finds no name, handle, email, file name, or channel data. Lowering any limit changes no row outside `community_limits`. `GET /host/usage` over 250 seeded communities returns three pages whose union is every community once; `lastPostDate` is a date, null with no entries, never a time. Create with `shortName`/`limits` replayed with different limits is `409 IDEMPOTENCY_CONFLICT`. Adversarial rows 6–8 and 16 (limits below use, override response carries no identity, override for another community's member is 404, audit rows) pass.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding community limits and usage",
      "size": "large",
      "priority": "high",
      "dependencies": [
        "1.1"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/src/limits.ts (new)",
        "apps/community/src/routes/host.ts",
        "apps/community/src/routes/invites.ts",
        "apps/community/src/routes/agents.ts",
        "apps/community/src/routes/attachments.ts",
        "apps/community/src/routes/administration.ts",
        "apps/community/src/storage/managed-blobs.ts",
        "apps/community/src/browser/CommunityApp.tsx (host page limits/usage; invitation preview)",
        "packages/shared/src/community-admin-wire.ts",
        "packages/shared/src/community-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Phase 2 — Host hold and host-started deletion",
      "subject": "[community-host-operator-api] [P-hold] Add a read-only host hold and notice-gated host-started deletion",
      "description": "Both halves ship: the operator approved host-started deletion on 2026-09-23 (spec Open Question 1). ADR `260923-121712` (accepted) amends `260920-201101`.\n\n**Migration 0013:** `communities_lifecycle` check gains `held`; columns `held_from_state`, `held_at`, `deletion_notice_at`, `deletion_from_state`, `delete_requested_by_host_actor`; `delete_requested_by` nullable; `community_deletion_jobs.requested_by_member_id` nullable beside `requested_by_host_actor` (key or user id) with an exactly-one check; the suspension check allows `suspended_from_state='held'`; the deletion check requires exactly one requester; `enforce_community_owner_lifecycle()` counts `held` among states needing exactly one active owner.\n\n**Hold:** `PATCH /host/communities/:id/lifecycle` (`communities:lifecycle`, current `lifecycleVersion`) with `action` in `suspend | resume | hold | release | set_notice` (`CommunityAdminHostLifecycleRequestSchema`, replacing the two-action request). `hold` from `active` or `archived`, recording `held_from_state`; `release` returns to it; suspension may start from `held` and resume returns to `held`. While held, people can do exactly what they can in `archived` (history, threads, roster, authorized files, the `{read}`-only `history_only` pairing flow); every growing action (post, upload, invitation, join, write-scope pairing, agent enrollment, settings edit) is `423 COMMUNITY_HELD` (new error code). Member erasure (`specs/community-member-erasure/`) stays allowed while held, as in `archived`. The owner keeps reauthenticated owner export and their own deletion request; cannot archive, restore, transfer, or release. Entering a hold runs `revokeTenantAccess`; release revives nothing. `deletionNoticeAt` optional on `hold`, and settable by `set_notice`: at least `COMMUNITY_HOST_DELETION_NOTICE_DAYS` ahead (default 14, minimum 7, maximum 365); may be moved later or cleared; moving earlier than 7 days from now is refused.\n\n**Host-started deletion:** `POST /host/communities/:id/deletion` (`communities:lifecycle`, body `lifecycleVersion`, `confirmIdSuffix` = last 8 chars of the UUID) only when `held`, a `deletionNoticeAt` is set, and it has passed; otherwise `409 STATE_CONFLICT`. Enters the existing `deletion_pending` with the same seven-day `delete_after`, worker, blob inventory, progress, and tombstone. `DELETE /host/communities/:id/deletion` cancels a host-started deletion back to `held`. The host cannot cancel or speed an owner-requested deletion; the owner cannot cancel a host-started one. An owner deletion started from `held` cancels back to `held` (`deletion_from_state`). A `suspended` community cannot be host-deleted directly.\n\n**Projection:** `CommunityAdminLifecycleSchema` gains `held`; the host projection gains `deletionNoticeAt` and `deletionRequestedBy: 'owner' | 'host' | null`.\n\n**UX:** banner on every channel of a held community: \"This community is on hold by its host. You can read it but not post.\" plus, with a notice date, \"The host plans to delete it after <date>. The owner can export it until then.\" Owner Settings keep Export reachable and explain Archive, Restore, and Transfer are unavailable during a hold. Host page shows the hold, notice date, and after it passes a Delete action asking for the last eight UUID characters.\n\n**Acceptance:** a held community refuses post, upload, invitation, join, write-scope pairing, agent enrollment, and settings edit with `423 COMMUNITY_HELD`, and still serves history reads, the `{read}` archived pairing flow, and the owner's reauthenticated export (fails if the hold reuses suspension or archive). Holding revokes grants and agent credentials; release returns to the recorded prior state and revives none. Host deletion is `409` for active, archived, suspended, held-without-notice, and before the notice date (injected clock); after it, `deletion_pending` with `delete_after` +7 days and a host requester. The owner cannot cancel a host-started deletion; the host can, back to `held`; an owner deletion from `held` cancels back to `held`. A notice under 7 days ahead is refused. Community B is untouched by every hold, release, and host deletion of A (two-community isolation fixture). Backout note (document in the PR): release every hold and cancel every host deletion before reverting code; 0013 stays.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding the host hold and host-started deletion",
      "size": "large",
      "priority": "high",
      "dependencies": [
        "1.2"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/migrations/0013_host_hold.sql",
        "apps/community/src/migrate.ts",
        "apps/community/src/schema.ts",
        "apps/community/src/routes/host.ts",
        "apps/community/src/data.ts",
        "apps/community/src/tenant-context.ts",
        "apps/community/src/storage/managed-blobs.ts",
        "apps/community/src/routes/administration.ts",
        "apps/community/src/deletion-worker.ts",
        "apps/community/src/config.ts",
        "apps/community/src/browser/CommunityApp.tsx (banner, owner settings, host page)",
        "packages/shared/src/community-admin-wire.ts",
        "packages/shared/src/community-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Phase 3 — Short names",
      "subject": "[community-host-operator-api] [P6] Add short names: host-set /<name> addresses beside /c/<uuid>",
      "description": "Build P6 on the Community server and its browser. ADR `260923-121152` (accepted): a short name is a mutable, host-unique alias, never identity. Credentials, invitation/pairing/claim links, stored connections, SSE, and API routes keep the UUID.\n\n**Grammar:** 3–32 chars, lowercase ASCII letters, digits, single hyphens, starts with a letter, no trailing hyphen: `^[a-z](?:[a-z0-9]|-(?=[a-z0-9])){2,31}$`; input lowercased and trimmed first. `COMMUNITY_RESERVED_SHORT_NAMES` shared constant: `api, assets, c, claim, host, join, pairing, health, auth, login, logout, signin, signup, settings, admin, static, public, www, help, docs, status, well-known, favicon, robots, sitemap, new, import, invite, deletion`; hosts add more with the `COMMUNITY_RESERVED_SHORT_NAMES` env var (comma-separated, same grammar).\n\n**Migration 0014:** `community_short_names(short_name text PRIMARY KEY, community_id uuid NOT NULL REFERENCES communities(id), state text NOT NULL CHECK (state IN ('current','retired')), created_at, retired_at)` with a partial unique index for one `current` per community; `released_short_names(name_hmac text PRIMARY KEY, available_at timestamptz)`. Deletion worker deletes a community's name rows and writes their hold rows.\n\n**Behaviour:** host sets on create (`shortName` in the create request, part of the idempotency hash) or later `PUT /host/communities/:id/short-name` (`communities:write`, body `{shortName | null}`); rename retires the current row and inserts the new one in one transaction under the community lock; null retires. Retired names stay bound for the community's lifetime. `DELETE /host/communities/:id/short-names/:name` releases a retired name (audited `community.short_name.release`). A name freed by abandoning a never-claimed `pending_owner` community (a cancelled or failed import, an unclaimed start) is freed at once with no hold. Other released or deletion-freed names are held for `COMMUNITY_SHORT_NAME_COOLOFF_DAYS` (default 90, 0–365) as an HMAC-SHA-256 under an HKDF key from `COMMUNITY_AUTH_SECRET` (info `community-short-name-hold`); no name kept in clear text after deletion. `DELETE /host/short-name-holds/:name` lifts one hold (audited). Errors `409 SHORT_NAME_TAKEN` (bound current/retired or in cool-off), `409 SHORT_NAME_RESERVED`. Host projection gains `shortName`.\n\n**Public lookup:** `GET /api/v1/community-names/:name`, unauthenticated, `200 {communityId, shortName}` (current name) only for `active`, `archived`, or `held`; every other case (unknown, reserved, malformed, pending_owner, suspended) returns the identical `404 NOT_FOUND`; rate limited per socket peer by `COMMUNITY_NAME_LOOKUPS_PER_MINUTE` (default 60, ceiling 600). No listing or prefix search. Wire `CommunityWireShortNameLookupSchema`, `CommunityShortNameSchema`, `CommunityAdminShortNameUpdateRequestSchema`.\n\n**Browser:** `main.ts` serves `index.html` for `GET /:name` and `/:name/*` when the name matches the grammar and is not reserved; other paths keep `404`. `BrowserRoot` resolves a short-name path via the lookup, renders with the UUID, `history.replaceState` to the current name if a retired one was used, and on 404 shows the chooser with \"No community at this address.\" `tenantApiPath` and `parseCommunitySettingsPath` take `{communityId, basePath}`; `/<name>/settings[/<section>]` works; `CommunityApp.tsx` keeps the short-name base when arrived by one. Server-minted links stay `/c/<uuid>`; \"Copy link\" offers the short-name address when one exists. Host page Web address section (current name, retired names with Release).\n\n**Acceptance:** lookup of `acme` returns the UUID; after renaming to `acme-labs`, `acme` returns the same UUID with `shortName: 'acme-labs'` and another community cannot take `acme`. After release (or deletion), re-creating `acme` is `409 SHORT_NAME_TAKEN` until the cool-off ends (injected clock) then succeeds, and no database row contains `acme` after deletion. Abandoning a never-claimed `pending_owner` community named `acme` frees `acme` at once (no clock movement, no hold row). Identical `404` bodies for unknown, reserved, malformed, pending_owner, suspended. A test pins that every literal top-level path in `main.ts` and `BrowserRoot.tsx` is reserved. Invitation, pairing approval, and owner-claim links for a named community still use `/c/<uuid>`. Browser tests: short-name navigation and rename redirect. Backout: revert code; tables ignored; short-name URLs 404.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding community short names",
      "size": "large",
      "priority": "high",
      "dependencies": [
        "2.1"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/migrations/0014_short_names.sql",
        "apps/community/src/migrate.ts",
        "apps/community/src/schema.ts",
        "apps/community/src/routes/short-names.ts (new)",
        "apps/community/src/routes/host.ts",
        "apps/community/src/deletion-worker.ts",
        "apps/community/src/config.ts",
        "apps/community/src/main.ts",
        "apps/community/src/browser/BrowserRoot.tsx",
        "apps/community/src/browser/api.ts",
        "apps/community/src/browser/CommunityApp.tsx",
        "packages/shared/src/community-admin-wire.ts",
        "packages/shared/src/community-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Phase 3 — Short names",
      "subject": "[community-host-operator-api] [P6] Let DorkOS connect to a community from a /<name> link",
      "description": "`parseCommunityLink` accepts a third exact shape, `/<name>`, with the same scheme, credential, query, fragment, port, and encoded-path rules as `/c/<uuid>`, returning `{ origin, communityId: null, shortName }`. The pairing service then calls `GET /api/v1/community-names/<name>` through the same pinned-origin fetch (no redirects, same DNS checks), validates the returned UUID with `communityIdPattern`, and continues exactly as for `/c/<uuid>`. The stored connection records only the UUID, so a rename or release never changes which community a connection talks to. Depends on 3.1 for `CommunityWireShortNameLookupSchema` and the grammar in `@dorkos/shared`.\n\n**Acceptance:** unit tests for `/Acme` (lowercased), `/acme/`, `/acme/x`, `/%61cme`, `/api`, and a lookup that answers a malformed UUID. A stored connection made from `/<name>` keeps working after the community is renamed and its old name released (fails if the name was stored as identity). Pasting `host/<name>` into DorkOS Connect works like a `/c/<uuid>` link. Changelog fragment. This task touches `apps/server`, not `apps/community`, so it may run beside the `apps/community` chain once 3.1 has merged.",
      "activeForm": "Teaching the DorkOS connection parser short names",
      "size": "small",
      "priority": "medium",
      "dependencies": [
        "3.1"
      ],
      "parallelWith": [
        "4.1",
        "4.2",
        "5.1"
      ],
      "files": [
        "apps/server/src/services/communities/remote/pinned-origin.ts",
        "apps/server/src/services/communities/remote/pairing-service.ts",
        "apps/server/src/services/communities/remote/__tests__/"
      ],
      "issue": null
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "Phase 4 — Import an owner export",
      "subject": "[community-host-operator-api] [P3] Import, part 1: schema, manifest contract, historical members, and the upload",
      "description": "First half of P3. ADR `260923-121153` (accepted). Nothing in this task restores content; it builds the job, the contract, and the upload.\n\n**Migration 0015:** `members.user_id` nullable; `members.origin text NOT NULL DEFAULT 'native' CHECK (origin IN ('native','imported'))`; `CHECK (user_id IS NOT NULL OR (origin='imported' AND NOT active))`; `communities.imported_at timestamptz NULL`; `managed_blobs_purpose` check gains `import_staging` (shown in usage as import staging, never counted against the storage limit); `community_imports(id uuid PK, community_id uuid UNIQUE REFERENCES communities(id), idempotency_key text UNIQUE, payload_hash text, state, upload_token_hash text UNIQUE, upload_expires_at, archive_sha256 text NULL, archive_bytes bigint NULL, staging_blob_key text NULL REFERENCES managed_blobs(blob_key), manifest_version int NULL, failure_code text NULL CHECK (failure_code ~ '^[A-Z][A-Z0-9_]{0,63}$'), attempts int, next_attempt_at, created_by_user_id text NULL, created_by_api_key_id uuid NULL, created_at, updated_at)` with an exactly-one-creator check; `community_import_files(import_id, source_attachment_id, blob_key, state CHECK IN ('stored','verified'), PRIMARY KEY (import_id, source_attachment_id))`; a zero-grace rule for host deletion of an unclaimed imported community. This is the one migration old code cannot fully tolerate once an import completes (forward-fix only).\n\n**Manifest contract:** publish `CommunityExportManifestV1Schema` in `community-wire.ts` exactly as the spec writes it (strict; `seq` as a decimal string; `email` nullable), pinned by a round-trip test: a real owner export from the test database parses. The owner export's member query becomes a `LEFT JOIN \"user\"` so a re-export keeps historical authors with `email: null`. Every query joining `\"user\"` from `members` tolerates NULL. This `LEFT JOIN`, the nullable `email`, and nullable `members.user_id` are shared with the member-erasure spec (`specs/community-member-erasure/`, DOR-2247): whichever lands first makes them and the other drops its copy; the user-presence check becomes `user_id IS NOT NULL OR erased_at IS NOT NULL OR (origin='imported' AND NOT active)` if erasure landed first (whichever migration lands second owns the combined check).\n\n**Create and upload:** `POST /api/v1/host/imports` (`communities:import`) with idempotency key, `name`, optional `description`, `admissionPolicy`, `shortName`, `limits`, `autoCommit` (`CommunityAdminImportCreateRequestSchema`); one transaction creates the `pending_owner` community, its limits and short name, the import row (`awaiting_upload`), and a one-time upload token (24 h, hashed at rest, returned once, no-store); no owner claim yet; replay returns the same import with `uploadToken: null`, a different payload is `409 IDEMPOTENCY_CONFLICT`. `PUT /api/v1/imports/:importId/archive` accepts the upload token as bearer or host authority with `communities:import`; requires `Content-Length` (≤ 1 GiB) and `X-Archive-SHA256`; streams into a reserved `import_staging` blob with the `BlobStore.put` byte limit, bypassing the ~96 KiB JSON body buffer in `app.ts` like the attachment route; mismatch discards the blob (`400 IMPORT_ARCHIVE_INVALID`); same-hash repeat after success is `200`, different hash `409 IDEMPOTENCY_CONFLICT`; success consumes the token and moves to `validating`. `GET /host/imports/:id` (`communities:read`), `POST /host/imports/:id/cancel` (any state before `ready`; blobs to pending-deletion cleanup; the `pending_owner` community removed by the abandon path, which now accepts a community whose only content came from an unfinished import). Expired upload windows end the same way. Owner-claim issue is refused (`409 STATE_CONFLICT`) while an import is not `ready`. Wire: `CommunityAdminImportCreateRequestSchema`, `…ImportStateSchema`, `…ImportReportSchema`, `…ImportSchema`, `…ImportCreateResponseSchema`; host projection gains `importState`. Audit `import.create`, `import.upload`, `import.cancel`.\n\n**Acceptance:** the manifest round-trip test passes against a real export; a re-export of a community holding a historical member (user_id NULL) parses and includes the author with `email: null` (fails if the inner join drops them); create/replay/conflict behave as above; upload with wrong length or hash leaves no blob after cleanup; upload token works once and never appears in logs or lists; cancelling at `awaiting_upload` removes the community and every blob; claim issue is refused before `ready`.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Laying the import foundation and upload",
      "size": "large",
      "priority": "high",
      "dependencies": [
        "3.1"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/migrations/0015_imports.sql",
        "apps/community/src/migrate.ts",
        "apps/community/src/schema.ts",
        "apps/community/src/routes/imports.ts (new)",
        "apps/community/src/routes/exports.ts",
        "apps/community/src/app.ts",
        "apps/community/src/storage/managed-blobs.ts",
        "apps/community/src/routes/host.ts",
        "packages/shared/src/community-wire.ts",
        "packages/shared/src/community-admin-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "4.2",
      "phase": 4,
      "phaseName": "Phase 4 — Import an owner export",
      "subject": "[community-host-operator-api] [P3] Import, part 2: validate, report, commit, restore, and owner adoption",
      "description": "Second half of P3: the resumable background worker (like the deletion worker, one job at a time per replica with `SKIP LOCKED`).\n\n**Validate:** stream the staging blob through `fflate`'s `Unzip`. First entry must be `manifest.json` (≤ 16 MiB); every other entry `attachments/<uuid>`; any other name, duplicate, directory, or encrypted entry fails. Manifest parses with `CommunityExportManifestV1Schema`, `scope` must be `owner`, every collection ≤ 10,000 rows; unknown version fails `IMPORT_VERSION_UNSUPPORTED`. Referential checks: every entry's channel, author, parent, thread root, and mention resolves in the manifest; every attachment's channel, entry, uploader resolves; mentions resolve to exactly one member or agent; thread roots are not replies; `seq` unique per channel; exactly one member with `role='owner'` and `active=true`, equal to `requesterMemberId`. Totals: attachment `byteSize` sum ≤ 1 GiB, each ≤ 25 MiB, and within the target's storage limit if set (`STORAGE_LIMIT_REACHED`). Length and SHA-256 of every attachment computed from the stream. Pass → `validated` with a counts-only report (`CommunityAdminImportReportSchema`: channels, entries, attachments, historical members, historical agents, audit events, attachment bytes, counted bytes, `fitsStorageLimit`, source lifecycle) — never text or names.\n\n**Commit:** `POST /host/imports/:id/commit` (`communities:import`) starts the restore; `autoCommit: true` skips the pause; a `validated` import not committed in 7 days is cancelled.\n\n**Restore files:** stream again; for each attachment reserve a managed blob (`purpose='attachment'`) via a new `reserveImportBlob` that accepts only a `pending_owner` community whose import is `restoring` (same lifecycle lock and version recheck); stored length and SHA-256 must equal the manifest; uncompressed bytes counted as they stream so an entry inflating past its declared size stops at size+1; record `community_import_files` after each file; a restart skips `verified` files.\n\n**Restore rows:** one transaction under the community lock inserts channels (`last_seq = max(seq)`, `epoch = 1`), historical members (`user_id NULL`, `active=false`, `origin='imported'`, keeping display name, handle, role; handles stay reserved in `community_handles`), agents (`active=false`, `revoked_at` set), entries (`idempotency_key = 'import:' || source id`, `payload_hash` = SHA-256 of text), mentions, attachments, audit events — all IDs derived `uuidv5(namespace = importId, name = sourceId)` via Node `crypto`, original timestamps kept; the community gets a new server-minted UUID. Commits every reserved blob, sets `communities.imported_at`, writes tenant audit `community.import` (`actor_kind='system'`) and host audit `import.complete`, marks `ready`, queues the staging blob for deletion. Nothing is visible and no file is `committed` until this transaction commits. Failures record a redacted `failure_code` (`IMPORT_ARCHIVE_INVALID`, `IMPORT_NOT_OWNER_EXPORT` for `scope: 'personal'`, `IMPORT_VERSION_UNSUPPORTED`, `IMPORT_TOO_LARGE`, `STORAGE_LIMIT_REACHED`, `IMPORT_CHECKSUM_MISMATCH`, `IMPORT_STORAGE_UNAVAILABLE`), audit `import.fail`; transient storage errors retry with the cleanup backoff, validation errors do not.\n\n**Claim and adoption:** once `ready`, the host issues a claim with the existing reissue route. Redemption binds the claimant's account to the adopted owner row (`user_id` set, `active=true`) instead of inserting a new owner; the adopted owner is added to every channel. Others rejoin by invitation. A `ready` community never claimed is abandoned through the ordinary deletion job using the host requester columns from 0013, with zero grace.\n\n**UX:** host page Import section (create, show the upload link once, watch progress and report, commit, cancel); each imported channel shows \"History imported from another host on <date>.\" at the start of imported history; past authors show as former members.\n\n**Acceptance:** round trip — seed A with private and public channels, threads, human and agent mentions, attachments, an archived channel, removed members, revoked agents; owner-export; import on the same host; claim: counts match; every entry's text, sequence, thread shape, mention order, author name, timestamp match through the ID map; attachment bytes hash to the original checksum; the claimant owns the adopted row and sees their past entries as theirs; no other historical member has a `user_id`; no grant, credential, pairing, or invitation exists. Importing twice yields disjoint IDs. Worker killed after half the files and restarted stores each file once with no orphan. Tampered archives each fail with the named code, leaving no committed row and, after cleanup, no blob: changed attachment byte (`IMPORT_CHECKSUM_MISMATCH`), mention outside the manifest, `../x` entry name, second `manifest.json`, `scope: 'personal'` (`IMPORT_NOT_OWNER_EXPORT`), `version: 2`, entry inflating past declared size, manifest over 16 MiB. Over-limit import fails `STORAGE_LIMIT_REACHED` before any file restores. Pause at `validated` with a report equal to the seeded source containing no text or names (key and value scan); nothing visible until commit; `autoCommit` proceeds; cancel at `validated` leaves nothing. Worker killed between the last file and the row transaction leaves no visible channel and no `committed` blob. UUIDv5 unit test against RFC 4122 vectors.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Building the import worker and restore",
      "size": "xl",
      "priority": "high",
      "dependencies": [
        "4.1"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/src/import-worker.ts (new)",
        "apps/community/src/routes/imports.ts",
        "apps/community/src/routes/host.ts",
        "apps/community/src/storage/managed-blobs.ts (reserveImportBlob)",
        "apps/community/src/routes/host.ts (owner-claim redemption adopts the owner row)",
        "apps/community/src/deletion-worker.ts",
        "apps/community/src/browser/CommunityApp.tsx (host page Import section; imported-history line)",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "5.1",
      "phase": 5,
      "phaseName": "Phase 5 — DorkOS app entry points",
      "subject": "[community-host-operator-api] [P5] Publish the communities contract in packages/cloud-api",
      "description": "Contract-first, its own PR (the workspace rule: a change needing both repos lands the public contract first). Mechanism only: no plan, price, catalog value, supplier, or host literal; the origin a community lives on is a runtime value.\n\n**Published (settled in the PR, 2026-09-23; see spec §P5 for the table).** Routes, each tied to an app action: `GET /v1/communities`, `GET /v1/communities/name-check?name=`, `POST /v1/communities`, `POST /v1/communities/{communityId}/claim-link`, `POST /v1/communities/{communityId}/keep` (confirms the `actions.keep.wouldHold` preview via `expectedHeldCommunityIds`; `conflict` if stale), `POST /v1/communities/{communityId}/restore`, `POST /v1/communities/moves`, `GET /v1/communities/moves` (unfinished plus finished within 7 days), `GET /v1/communities/moves/{moveId}`, `POST /v1/communities/moves/{moveId}/upload` (fresh upload target while `awaiting_upload`), `POST /v1/communities/moves/{moveId}/cancel`. The private plan's `POST /v1/communities/import` is the `moves` family. The claim link and upload token are returned once, only by the routes that create them, and carry `ONE_TIME_CREDENTIAL_META`; starts and moves answer `replayed`. `HostedCommunity` carries `hold`, `deletionAt`, a service `notice`, limits, usage and `actions`. Links: `HttpsUrlSchema` for links a person is sent to, `ServerUrlSchema` (https or loopback http) for server links, both also JSON Schema patterns. Tied fields are enforced with refinements. `CommunityShortNameSchema` publishes the P6 grammar. In `billing.ts`, additive and optional: `EntitlementCommunityLimitsSchema { maxCommunities, maxMembersPerCommunity, maxStorageBytesPerCommunity }`, all nullable, as `EntitlementLimitsSchema.communities?`, and `EntitlementsSchema.used.communities?`. New `Problem` codes `community_name_taken`, `community_name_reserved`, `import_too_large` (replacing the drafted `import_invalid`); new optional `Problem.actionUrl` (https) and `actionLabel`, dropped when malformed. Every new enum is listed with its reason in `catalog-blindness.test.ts`. Objects are `z.object`, like the rest of the package.\n\n**Acceptance:** conformance fixtures parse for every new route and `Problem` code; `EntitlementsSchema` parses a fixture with and without the new `communities` group and `used.communities` (fails if either is required); the catalog-blindness test lists every new enum with its reason; the published `.d.ts` and JSON Schema carry no plan id, plan name, or SKU id; the credential marker sits on exactly the claim link and upload token at the creating paths, and no credential-named field appears elsewhere in the family; unsafe link schemes are refused; each tied field combination has a negative test. Changelog fragment. Additive under the package's own rule, so backout is removing nothing.",
      "activeForm": "Publishing the communities contract",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": [
        "1.1",
        "1.2",
        "2.1",
        "3.1",
        "4.1",
        "4.2"
      ],
      "files": [
        "packages/cloud-api/src/communities.ts (new)",
        "packages/cloud-api/src/routes.ts",
        "packages/cloud-api/src/problem.ts",
        "packages/cloud-api/src/billing.ts",
        "packages/cloud-api/src/index.ts",
        "packages/cloud-api/src/primitives.ts",
        "packages/cloud-api/src/__tests__/schema-walk.ts (new, shared walker)",
        "packages/cloud-api/src/__tests__/communities.test.ts (new)",
        "packages/cloud-api/src/__tests__/catalog-blindness.test.ts",
        "packages/cloud-api/fixtures/v1/ (conformance fixtures)",
        "packages/cloud-api/README.md"
      ],
      "issue": null
    },
    {
      "id": "5.2",
      "phase": 5,
      "phaseName": "Phase 5 — DorkOS app entry points",
      "subject": "[community-host-operator-api] [P5] Add Start a community and Move a community here to the DorkOS app",
      "description": "The \"Add community\" menu in `CommunityContextSwitcher.tsx` gains **Start a community** and **Move a community here**, rendered only when the cloud-link summary (`GET /api/cloud/status`, `features/cloud-link`) says the installation is linked. Unlinked: not rendered, and no request to the hosted service. Connect, Join, Create, and Deploy are unchanged. The local server makes every hosted-service call with the installation credential it already holds, through new routes under `/api/cloud/communities/*` (list, name check, start, claim link, move start, move list, move poll, fresh upload target, move cancel, and an upload relay that streams the file to the returned upload target; keep and restore only if the switcher shows held communities). It relays the parsed value, never the raw body, and never passes a field marked `ONE_TIME_CREDENTIAL_META` to the browser. The browser never sees that credential, the claim token, or the upload token. Move state is read from the service on every poll (no local cache or config field). FSD: new `features/community-hosting` slice, imported through its barrel.\n\n**Start a community:** form (name 1–80 required; optional web address with the short-name grammar as a hint) → submitting (locked, progress) → claim (\"Finish in your browser. Sign in or create your account there, then come back.\" with **I've finished**; the app opens the claim link in the browser) → connecting (ordinary pairing flow with the returned canonical link) → done (new community selected). Errors: \"That web address is taken.\" / \"That web address can't be used.\" on the field; an entitlement refusal shows the service's own text and a link to the account page it names; unreachable: \"Couldn't reach your DorkOS account. Try again.\" keeping the form. The app never branches on a plan id.\n\n**Move a community here:** explain (\"Moving copies your community's history and files. Everyone joins again and reconnects their DorkOS. Your old community keeps running until you delete it.\" plus how to export: Settings → Export → confirm with your password → save) → choose `.zip`, name, web address → uploading (determinate progress) → importing (polled; survives app close and reload) → claim and connecting → \"Your history is here. Send invitations so people can join again.\" Errors: not an owner export, damaged file, too large, out of file space, not supported — one plain sentence each with the next step; cancel available until ready. Entitlement numbers (`limits.communities`, `used.communities`) render only when present (\"You can start 2 more communities\").\n\n**Acceptance:** with the installation unlinked the switcher renders neither item and the client makes no request to `/api/cloud/communities/*` (mock transport). `apps/server` route tests use a fixture-driven fake hosted server; no test reaches a real service; the browser-facing responses carry no token. Each dialog state renders at phone, tablet, and desktop widths in the Dev Playground; move state survives a reload. User guide in `docs/`: moving a community, what moves and what does not, joining again after a move (`writing-for-humans`). Changelog fragment. Touches `apps/server` and `apps/client`, not `apps/community`, so it runs beside the Community chain.",
      "activeForm": "Adding the app's community hosting entry points",
      "size": "large",
      "priority": "high",
      "dependencies": [
        "5.1"
      ],
      "parallelWith": [
        "2.1",
        "3.1",
        "4.1",
        "4.2"
      ],
      "files": [
        "apps/server/src/routes/cloud-communities.ts (new, or apps/server/src/routes/cloud.ts)",
        "apps/client/src/layers/features/community-hosting/ (new slice)",
        "apps/client/src/layers/features/dashboard-sidebar/ui/context/CommunityContextSwitcher.tsx",
        "apps/client/src/dev/ (playground showcases)",
        "docs/ (Communities user guide: starting and moving a hosted community)"
      ],
      "issue": null
    },
    {
      "id": "6.1",
      "phase": 6,
      "phaseName": "Phase 6 — Host links and OpenID Connect",
      "subject": "[community-host-operator-api] [P4] Add host-configured terms, privacy, and report links",
      "description": "Configuration, each optional: `COMMUNITY_TERMS_URL`, `COMMUNITY_PRIVACY_URL` (`https:` only), `COMMUNITY_REPORT_ABUSE_URL` (`https:` or `mailto:`). `GET /api/v1/host-links` (public) returns `CommunityWireHostLinksSchema { termsUrl, privacyUrl, reportAbuseUrl }`, each nullable. Sign-in and sign-up show \"Terms\" and \"Privacy\" when set; the account menu shows all three; each message's menu shows \"Report\" when `reportAbuseUrl` is set, opening it in a new tab with exactly `?community=<uuid>&entry=<uuid>` (for `mailto:` the two UUIDs go in the body). No message text, name, or other identifier. Nothing shows when unset; the server sends nothing to the link targets.\n\nOrdering note: this task has no migration and depends on 4.2 only for same-surface sequencing. A host that needs these links before import lands may move it earlier in the `apps/community` chain; record the move in both tasks' dependencies if so.\n\n**Acceptance:** unset → three nulls and no page renders Terms, Privacy, or Report. Set → each appears in its placement, and Report carries exactly the community and entry UUIDs. `COMMUNITY_TERMS_URL=http://example.com` fails configuration parsing. Fails if a link can be non-HTTPS or carry message data.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding host links",
      "size": "small",
      "priority": "high",
      "dependencies": [
        "4.2"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/src/routes/host-links.ts (new)",
        "apps/community/src/config.ts",
        "apps/community/src/app.ts",
        "apps/community/src/browser/ (sign-in, account menu, message menu)",
        "packages/shared/src/community-wire.ts",
        "apps/community/API.md"
      ],
      "issue": null
    },
    {
      "id": "6.2",
      "phase": 6,
      "phaseName": "Phase 6 — Host links and OpenID Connect",
      "subject": "[community-host-operator-api] [P4] Add optional OpenID Connect sign-in set by host configuration",
      "description": "Configuration, all or none, validated in `parseConfig` like the Google and GitHub pairs: `COMMUNITY_OIDC_ISSUER_URL` (HTTPS, or HTTP on localhost), `COMMUNITY_OIDC_CLIENT_ID`, `COMMUNITY_OIDC_CLIENT_SECRET`, optional `COMMUNITY_OIDC_LABEL` (1–40 chars, default \"Single sign-on\"), optional `COMMUNITY_OIDC_SCOPES` (default `openid email profile`, must include `openid`). `createCommunityAuth` adds Better Auth's `genericOAuth` plugin with one provider, `providerId: 'oidc'`, `discoveryUrl: <issuer>/.well-known/openid-configuration`, `pkce: true`; discovery runs at first use, never at startup. Callback `<COMMUNITY_PUBLIC_URL>/api/auth/oauth2/callback/oidc`, printed in the startup log line and the deployment guide. The `databaseHooks.user.create.before` admission check runs for OIDC sign-ups (invitation or owner claim still required). `disableImplicitLinking: true` stays: an OIDC identity whose email matches an existing account is refused; people link from their account page. `email_verified` must be true. `CommunityWireAuthOptionsSchema` gains `oidc: z.strictObject({ label }).nullable()`; the button shows only when non-null. Email and password stays enabled regardless. An OIDC-created account can set a password (Better Auth `setPassword` behind a same-origin, session-required route, audited `account.password_set`). Export, archive, transfer, deletion, and key issuance stay password-reauthenticated and say \"Set a password in your account to do this.\" (OIDC reauthentication is a later follow-up, spec Q5.)\n\n**Acceptance:** against an in-process fake issuer (minimal discovery document and token endpoint): an invited person signs up through OIDC; an uninvited person is refused; an identity matching an existing password account is refused without linking; `email_verified: false` is refused. With OIDC unset, `auth-options` returns `oidc: null` and the server makes no outbound request at startup. An OIDC-created account can set a password and then sign in with it while the issuer is unreachable. Backout: unset the variables.\n\nLanding order: this task touches `apps/community` and its migrations, so it lands in strict sequence with the other `apps/community` tasks (1.1 → 1.2 → 2.1 → 3.1 → 4.1 → 4.2 → 6.1 → 6.2). Migrations are serial: 0012 (1.1), 0013 (2.1), 0014 (3.1), 0015 (4.1); each is appended to the list in `apps/community/src/migrate.ts` and mirrored in `src/schema.ts`. One PR in the merge queue at a time on this surface; stack the next and rebase.\n\nEvery task: tests carry a purpose comment and name the failure they catch; Postgres integration tests use `vitest.pg.config.ts`, storage tests run on both filesystem and S3 BlobStores (`vitest.s3.config.ts`); the existing tenancy and administration isolation suites stay green unchanged; the deployment smoke still runs with every DorkOS host blocked. Add `API.md` lines for any route added, and one changelog fragment in `changelog/unreleased/` written with the `writing-for-humans` skill. No host route may read channels, entries, files, the member directory, invitations, or content audit.",
      "activeForm": "Adding generic OpenID Connect sign-in",
      "size": "medium",
      "priority": "medium",
      "dependencies": [
        "6.1"
      ],
      "parallelWith": [],
      "files": [
        "apps/community/src/auth.ts",
        "apps/community/src/config.ts",
        "apps/community/src/routes/ (auth options, set-password route)",
        "apps/community/src/browser/ (sign-in button, account page)",
        "packages/shared/src/community-wire.ts",
        "apps/community/DEPLOYMENT.md"
      ],
      "issue": null
    },
    {
      "id": "7.1",
      "phase": 7,
      "phaseName": "Phase 7 — Documentation for hosts",
      "subject": "[community-host-operator-api] [Docs] Write the Community host documentation for the new host tools",
      "description": "Each code task already adds its own `API.md` routes and changelog fragment; this task writes the operator-facing guides once the tools exist, so they describe what shipped rather than what was planned. Written with the `writing-for-humans` skill for the user guide; the operations guides stay precise.\n\n- `OPERATIONS.md`: issuing the first host key offline (`node dist/host-keys.js issue …`), rotating keys when staff change (keys are host-owned and outlive the operator who issued them), scopes and why to withhold `communities:lifecycle` from provisioning keys, setting limits and per-member agent overrides (1–1,000), reading usage, holds and notice dates, host-started deletion (held, notice ≥ 7 days, UUID suffix, 7-day cancellable window), releasing short names and the cool-off (and that rotating `COMMUNITY_AUTH_SECRET` ends outstanding holds early), watching, committing, and cancelling imports, and the three host links.\n- `DEPLOYMENT.md` and `README.md`: every new configuration key (`COMMUNITY_HOST_KEY_ATTEMPTS_PER_MINUTE`, `COMMUNITY_HOST_DELETION_NOTICE_DAYS`, `COMMUNITY_RESERVED_SHORT_NAMES`, `COMMUNITY_SHORT_NAME_COOLOFF_DAYS`, `COMMUNITY_NAME_LOOKUPS_PER_MINUTE`, the OIDC set, the three link URLs), the OIDC callback URL, and the agents-per-person setting: **default 20, maximum 100**, plus the per-member override up to 1,000.\n- `RECOVERY.md`: import is not a backup; host backups remain the recovery path.\n- `API.md`: a final pass that every new route and error code (`MEMBER_LIMIT_REACHED`, `STORAGE_LIMIT_REACHED`, `AGENT_LIMIT_REACHED`, `SHORT_NAME_TAKEN`, `SHORT_NAME_RESERVED`, `IMPORT_ARCHIVE_INVALID`, `COMMUNITY_HELD`) is documented.\n- `docs/` user guide: web addresses (short names, renames, old addresses still work).\n\n**Acceptance:** every configuration key parsed in `apps/community/src/config.ts` appears in `DEPLOYMENT.md`; no guide claims a hosted service, price, or plan; `docs:coverage` passes for any new MDX page; the banned-words and vocab gates pass.",
      "activeForm": "Writing the Community host documentation",
      "size": "medium",
      "priority": "medium",
      "dependencies": [
        "1.2",
        "2.1",
        "3.1",
        "4.2",
        "6.1",
        "6.2"
      ],
      "parallelWith": [
        "5.2"
      ],
      "files": [
        "apps/community/OPERATIONS.md",
        "apps/community/DEPLOYMENT.md",
        "apps/community/README.md",
        "apps/community/RECOVERY.md",
        "apps/community/API.md",
        "docs/ (Communities user guide: web addresses)"
      ],
      "issue": null
    }
  ]
}
