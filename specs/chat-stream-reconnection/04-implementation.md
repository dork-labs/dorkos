# Implementation Summary: Runtime-Agnostic Session Hydration & Resumable Streaming

**Created:** 2026-06-10
**Last Updated:** 2026-06-10
**Spec:** specs/chat-stream-reconnection/02-specification.md

## Progress

**Status:** In Progress
**Tasks Completed:** 14 / 18 (Phases 1–5 complete — server + client streaming foundation + live sidebar/list + **chat SEND restored** + DOR-81 queue scoping)
**Branch:** `feat/chat-stream-reconnection` (Phase 5 UNCOMMITTED on top of `7ebfe27d` — user commits)
**Resume at:** #19 (SessionEvent contract extension for live-turn fidelity, blocks #16), then Phase 6 (#15 stateless stub / #16 live browser acceptance / #17 cross-client / #18 docs+ADRs).

## Tasks Completed

### Session 1 - 2026-06-10

**Phase 1 — Foundation (contract + projector + Claude adapter)** — GATE 1 PASSED ✅

- Task #1: Add runtime-neutral session-stream Zod schemas to @dorkos/shared
- Task #2: Extend AgentRuntime + Transport with the three contract methods (+ compiling stubs in all implementors)
- Task #3: Build SessionStateProjector with EventLog + RingBuffer
- Task #4: Implement the contract on the Claude Code adapter (JSONL-backed)

Gate 1 verification: `pnpm typecheck` 21/21, `pnpm lint` 0 errors, server session+claude-code suites 479 passing, shared session-stream 15 passing. Holistic code review run; 2 criticals (C1, C2) found and fixed with regression tests before proceeding.

**Phase 2 — Durable per-session stream + turn decouple + global status stream** — GATE 2 PASSED ✅

- Task #5: Add durable `GET /api/sessions/:id/events` (snapshot→replay→live; `Last-Event-ID`/`?after=`; `id: <sid>-<seq>` frames; runtime-agnostic via the AgentRuntime contract). Also fixed Phase-1 I2 via an additive `signal?: AbortSignal` on `subscribeSession`.
- Task #6: Decouple turn execution from the POST — trigger-only, returns `202 { sessionId: canonicalId }`; turn runs detached feeding the projector via `feedProjector`; single delivery path. New `services/session/trigger-turn.ts`. Wired Phase-1 I1 (`disposeProjector` + `markInterrupted` on eviction). All pre-existing POST-streaming tests converted to the new contract.
- Task #7: Wire global status stream — `services/session/session-list-broadcaster.ts` iterates `runtime.subscribeSessionList()` → `eventFanOut.broadcast` on `/api/events`; started/stopped in `index.ts`.

Gate 2 verification: full server suite 2810 passing (192 files) before the gate review; holistic review found 2 more criticals + 3 important, ALL fixed; post-fix consolidated run 1200 passing (routes + session + claude-code), `pnpm typecheck` 21/21, `pnpm lint` 0 errors, `sessions.ts` back under the 500-line rule.

Phase-2 review fixes (all with regression tests):

- **C1** — canonical-id projector orphan: a new session's turn fed the projector under the client UUID, but the 202 returns the canonical id; client re-keying `/events` to canonical hit a fresh empty projector → turn invisible. Fixed with `rekeyProjector(oldId, newId)` (moves the SAME instance, preserving identity so in-flight feeds + open subscriptions survive; ADR-0267 no-alias preserved — one key per projector), invoked in `triggerTurn` when canonical≠request.
- **C2** — errored/interrupted turn settled to `idle` (failure masked on cold hydrate). Fixed: `turn_end` now settles to a terminal lifecycle (`error`/`interrupted`) via `deriveTurnEndLifecycle(terminalReason)`.
- **I1** — same-client concurrent turns could release each other's lock. Fixed with a per-acquisition `token` (symbol) — `releaseLock` no-ops unless the token matches the current lock.
- **I2** — `sessions.ts` > 500 lines: extracted `GET /:id/events` + `parseResumeCursor` into `routes/session-events-handler.ts` (524→441 lines).
- **I3** — broadcaster `start()` not exception-safe: wrapped iterator construction in try/catch (server stays up, discovery off on failure).

**Phase 3 — Client streaming foundation (StreamManager + per-session store + subscribe-first hydration + flag/stream removal)** — GATE 3 PASSED ✅

- Task #8: Connection-only `StreamManager` singleton (`apps/client/src/layers/shared/lib/transport/stream-manager.ts`) owning two `SSEConnection`s (durable `/api/sessions/:id/events` + global `/api/events`); injectable connection factory (test seam); HMR-safe via `import.meta.hot`; Zod-validates every frame. New per-session `session-stream-store.ts` (`applySnapshot`/`applyEvent`, idempotent seq apply, field-wise `status_change`/`contextUsage` merge), `session-list-store.ts`, and `session-stream-binding.ts` (entities→shared glue). **FSD note:** `shared` can't import `entities`, so StreamManager is connection-only and the binding lives in `entities`.
- Task #9: Projection `features/chat/model/stream/project-session-turn.ts` (`SessionEvent[]` + `HistoryMessage[]` → `ChatMessage[]`), `derive-rendered-state.ts` (transitional `hasStreamState()` bridge — stream store primary, legacy `session-chat-store` fallback until the send path is rewired in #13), `derive-status-bar.ts`, `use-session-stream.ts` (subscribe-first hydration). Rewired `use-chat-session.ts` (messages/status/pendingInteractions from the stream store), `ChatStatusSection.tsx` (status from snapshot → non-null on cold mount), `ChatInputContainer.tsx`. **Scope:** RECEIVE/RENDER/HYDRATE only — the submit-path 202 rewrite is #13 (so chat _send_ stays non-functional until #13; the durable RECEIVE path is provable via snapshot+/events hydration).
- Task #10: Removed `enableCrossClientSync` ("Multi-window sync") flag end-to-end + one-time localStorage purge migration (`app-store-helpers.ts`/`app-store.ts`); deleted the legacy `/api/sessions/:id/stream` client subscription (`use-session-history.ts`) and the server route (`sessions.ts`, 636→553 lines); **retired presence** (removed `presenceInfo`/`presenceTasks`, deleted `ClientsItem.tsx`/`SyncItem.tsx` + status-bar registry entry); re-sourced the connection indicator from `useSessionStreamConnection`; removed the AdvancedTab toggle.

Gate 3 verification: `pnpm typecheck` 21/21, `pnpm lint` 0 errors, full `pnpm test -- --run` client 4170 passing + server 2811 passing. Holistic code review (0 criticals, 3 important + minors); all three important fixed with regression tests:

- **Review #1 (Important, fixed)** — `applyEvent` ran `touchAndGet` (LRU reorder + possible sibling eviction + re-render churn) BEFORE the `seq <= lastAppliedSeq` idempotency guard, so gap-replayed/duplicate events had side-effects. Moved the guard ahead of any LRU mutation; regression test asserts `sessionAccessOrder` identity + sibling untouched on a duplicate-seq event.
- **Review #3 (Important, fixed)** — rendered pending-interactions derived only from `inProgressTurn`, which the projector nulls on `turn_end` even when still `blocked`; the snapshot's authoritative `pendingInteractions` was never projected → a session blocked-after-turn-end then refreshed showed no Approve/Deny card (regression of the DOR-73 recovery this spec generalizes + requirement #1). Projection now folds `streamState.pendingInteractions` (DTO→part via the inverse of `interactionEventToDTO`, deduped by interaction id) so the card renders regardless of turn state.
- **Review minor (fixed)** — `session_removed` now evicts the per-session stream store too (was list-store only).

**Phase 4 — Live sidebar/session-list + status via the global stream (drop the timer poll)** — GATE 4 PASSED ✅

- Task #11: Removed the sessions timer poll (`refetchInterval: SESSIONS_REFETCH_MS`, 60s) from `use-sessions.ts` and deleted the now-unused `SESSIONS_REFETCH_MS` constant. New `useGlobalSessionStream()` (`entities/session/model/use-global-session-stream.ts`), mounted ONCE in `AppShell.tsx`: opens the global `/api/events` stream (`initSessionStreamBinding()` + `streamManager.connectList()`, both idempotent) and reflects the live `session-list-store` `sessions` map into the **shared `['sessions', cwd]` TanStack Query cache** via the exported pure `reconcileSessionsCache(queryClient, next, prev)`. **Design:** patch the single shared cache rather than rewire each consumer — that cache is the read surface for `useSessions` (sidebar), the router `sessionRouteLoader` (`['sessions', dir ?? null]`, picks `[0]` as most-recent), dashboard sidebar, command palette, attention items, etc., so ALL go live at once with minimal blast radius. Cache patches are per-session, keyed by `session.cwd ?? null` (naturally scopes multi-project; a cwd the global stream doesn't cover keeps its cold-loaded list — no wholesale rebuild/wipe), and re-sorted most-recent-first to preserve the loader's `[0]` contract. Idempotent-skip via immer reference identity (`prev[id] === next[id]`); status-only store changes short-circuit (`state.sessions === prev`). ~~The server emits one `session_upserted` per on-disk session on `/api/events` connect (initial inventory)~~ **[corrected in #18: wrong — `/api/events` sends only `connected` + heartbeats on connect; the one-shot inventory is emitted at broadcaster START (to zero clients). Hard-refresh hydration comes from the cold `GET /api/sessions` query; the stream is the live overlay. CLI-created sessions still appear via the watcher's live `session_upserted` (requirement #3).]**
- Task #12: Re-described the "Background refresh" toggle as an opt-in external-session polling fallback (copy-only). The section header/copy were already reframed by #10 (presence retirement); updated the `SwitchSettingRow` description in `AdvancedTab.tsx` to "Poll for updates to sessions running outside DorkOS (e.g. the Claude Code CLI). Enable only if external activity isn't appearing promptly." Verified (grep) `enableMessagePolling` gates ONLY optional polling (`use-session-history.ts:72`, `use-task-state.ts:104` refetchInterval gates) + the two toggle UIs (AdvancedTab + ChatStatusSection configure popover); NO correctness path depends on it. Default stays `false` (`app-store-helpers.ts:108`).

Gate 4 verification: `pnpm typecheck` 21/21, `pnpm lint` 0 errors (47 pre-existing warnings, 0 new), full `pnpm test -- --run` client 4180 passing (368 files; +10 new tests), server cached-green (no server inputs changed). Holistic code review: PASS-WITH-FIXES — verified the immer identity assumption, cwd keying, sort/loader contract, double-connect idempotency, and the #12 correctness gate all hold. Fixes applied with a regression test:

- **Review #1 (Important, fixed)** — the hook split work across TWO effects (effect A `connectList()`, effect B `getState()`+`subscribe()`), so the global connection could open BEFORE the store subscription installed → a list frame landing in that window mutated the store but was never reflected into the cache (stale-after-refresh; not self-healing for one-shot `session_removed`). Collapsed to a SINGLE effect with guaranteed order: **subscribe → reconcile-current → connect**. Regression test seeds the store BEFORE mount and asserts the cache reflects it after mount.
- **Review #3 (Minor, fixed)** — dropped a redundant `[...next]` array copy before `.sort()` (`next` is always freshly allocated).
- **Review #5 (Minor, conscious deferral)** — `useSessionListSessions`/`useSessionListStatus` selectors are exported but have no production consumer yet. The chosen patch-the-cache design doesn't read them, and the server doesn't emit `session_status` on the global stream (Phase-1 I3). They're the store's ready public API for when status lands (active-session status already hydrates via the per-session snapshot, #9). Kept intentionally.

**Phase 5 — DOR-74 canonical-id + restore chat SEND, DOR-81 queue scoping, + two browser-found integration fixes** — GATE 5 PASSED ✅

- Task #13: Rewrote the chat submit path from the retired in-band SSE pipeline to the trigger-only `202 {sessionId}` POST + durable `/events`. New `Transport.postMessage` (returns `{ sessionId: canonicalId }`; replaces the SSE-consuming `sendMessage`) on HttpTransport + DirectTransport. Submit (`use-session-submit.ts`): optimistic user message in the stream store (the contract carries no user-message event), subscribe-first `attachSession`, `postMessage`; on a canonical-id rekey re-attach + move optimistic msg/queue/cache + `router.replace` the URL once (`onSessionIdChangeReplace`); `stop()`→`interruptSession`. `useTurnEndReconcile` (new) reloads `getMessages` on the streaming→settled edge → `setHistoryMessages` (full-fidelity persist) + clears the optimistic msg. Projection renders the optimistic user bubble. **Retired the legacy pipeline:** deleted `stream/stream-manager.ts` + `use-stream-handler.ts`, removed the `done`-event remap block, deleted the legacy tests (resolves the naming collision).
- Task #14 (DOR-81): Moved the compose-next queue from component-local `useState` into the per-session stream store keyed by sessionId (`queuedMessages` + actions `enqueue`/`remove`/`clear`/`moveQueue`/`update` + `useSessionQueue`). Flush is origin-pinned (auto-flush reads only the active session's queue + `submitContent` drops a cross-session flush as defense-in-depth); `prevStatusRef` resets on session change; `moveQueue` mirrors #13's rekey so a message queued under the client UUID survives the canonical swap. **NOT `key={sessionId}` on ChatPanel** — that would remount mid-send during #13's canonical-id URL rewrite; the store-keying achieves isolation without remounting (deliberate deviation from the task's original wording). Fixed a latent shared-array bug in the store's default state (`freshSessionState()`).

**Two integration bugs found via live browser dogfooding (Playwright on :6241), both fixed + tested:**

- **`/events` 404'd any session not tracked in the IN-MEMORY store** → the client SSEConnection retried 5× and showed "Sync offline". This hit BOTH brand-new client UUIDs AND existing on-disk sessions (sessions live on disk as JSONL; `hasSession()` is in-memory only). Fix: `session-events-handler.ts` no longer 404s a well-formed id — it serves the snapshot (disk history, empty for a truly-new id) + a live subscription (projector created on demand). Malformed id still 400. Browser-verified: both session types → `/events` 200, single connection, no retry. Test rewritten (`sessions-events.test.ts`: "serves a well-formed session not tracked in memory as empty-live").
- **The durable `/events` URL omitted `?cwd=`** → the server built the cold snapshot from the DEFAULT project dir, returning EMPTY history for any session outside it (user saw no message history on existing sessions, while `/messages`, `/:id`, `/tasks` — which pass cwd — worked). Fix: `StreamManager.attachSession(sessionId, cwd)` appends `?cwd=`, threaded through `useSessionStream(sessionId, cwd)` → `use-chat-session` + `use-session-submit`; idempotency keys on (sessionId, cwd). Browser-verified: existing-session history now renders.

Gate 5 verification: `pnpm typecheck` 21/21, `pnpm lint` 0 errors (47 pre-existing warnings), full `pnpm test -- --run` client 4114/4115 + server green (the 1 failure is a pre-existing flaky timer in `sse-connection.test.ts` — passes 40/40 in isolation; untouched by Phase 5). Holistic code review (FAIL→fixed): one **Critical** + related issues, all fixed with regression tests:

- **C1 (Critical, fixed)** — the success path emits NO event carrying `lifecycle` (only `turn_end` with `terminalReason`), and the client's `projectEvent` `turn_end` case was a status no-op → `status.lifecycle` stayed `'streaming'` forever after a live turn (couldn't send a 2nd message without refresh; the reconcile never fired). Fix: client `turn_end` now derives the settled lifecycle from `terminalReason` (mirrors the server projector's `deriveTurnEndLifecycle` — idle/interrupted/error/blocked).
- **I1 (Important, fixed)** — once C1 fired, `setHistoryMessages` replaced `messages` but kept `inProgressTurn`, rendering the assistant reply TWICE. Fix: `setHistoryMessages` also clears `inProgressTurn` (the reload now carries that turn).
- **C2 (test-masking, fixed)** — the reconcile test fabricated a `status_change{lifecycle:'idle'}` the real server never sends, so it passed despite C1. Fix: the test drives the settle with a real `turn_end` (no lifecycle status_change) and asserts the session returns to idle + the assistant reply renders exactly once.
- **I2 (fixed)** — `draftRef` in `use-chat-queue.ts` wasn't session-scoped; now cleared on sessionId change (cross-session-leak class).
- **M2 (fixed)** — the reconcile's `getMessages` catch now logs.

## Files Modified/Created

**Source files:**

- `packages/shared/src/session-stream.ts` (new) — runtime-neutral contract: `SessionStatus`, `SessionEvent` (discriminated union, `seq` per member), `SessionSnapshot`, `SessionListEvent` + sub-schemas. `status_change` carries a deep-partial `contextUsage` (C2 fix).
- `packages/shared/src/agent-runtime.ts` (mod) — `getSessionSnapshot`/`subscribeSession`/`subscribeSessionList` on `AgentRuntime` (ctx: `SessionOpts`).
- `packages/shared/src/transport.ts` (mod) — same three, client-facing signatures (`sessionId, sinceCursor?, cwd?`).
- `packages/shared/package.json` (mod) — `./session-stream` export entry.
- `apps/server/src/services/session/session-state-projector.ts` (new) — per-session seq owner; `ingest`/`subscribe`/`buildSnapshot`/`replayFrom`/`resolveInteraction`/`markInterrupted`; `getOrCreateProjector`/`disposeProjector` registry.
- `apps/server/src/services/session/event-log.ts` (new) — append-only ordered log (cap 5000).
- `apps/server/src/services/session/ring-buffer.ts` (new) — bounded current-turn ring (cap 200, lazy TTL ~10min, no leaked timers).
- `apps/server/src/services/session/index.ts` (mod) — barrel exports.
- `apps/server/src/services/runtimes/claude-code/claude-code-runtime.ts` (mod) — three contract methods delegating to the projector; JSONL loader injected via `getMessageHistory`.
- `apps/server/src/services/runtimes/claude-code/sessions/session-event-normalizer.ts` (new) — `toRawSessionEvent`, `feedProjector` (SDK-free; the #6 turn-feed seam).
- `apps/server/src/services/runtimes/claude-code/sessions/session-list-watcher.ts` (new) — `watchSessionList`, 250ms debounce, working `.return()`.
- `apps/server/src/services/runtimes/claude-code/index.ts` (mod) — exports normalizer/watcher.
- `apps/server/src/services/runtimes/test-mode/test-mode-runtime.ts` (mod) — compiling stubs (replaced in #15).
- `apps/client/src/layers/shared/lib/transport/session-methods.ts` (mod) — HttpTransport throwing stubs (wired in #5/#7/#8/#11).
- `apps/client/src/layers/shared/lib/direct-transport.ts` (mod) — DirectTransport throwing stubs (Phase 3/6).
- `packages/test-utils/src/fake-agent-runtime.ts` (mod) — `vi.fn()` spies.
- `packages/test-utils/src/mock-factories.ts` (mod) — `createMockTransport` + `emptyAsyncIterable`.

_Phase 2:_

- `apps/server/src/routes/sessions.ts` (mod) — new `GET /:id/events` (extracted to handler), `POST /:id/messages` rewritten trigger-only (202), `/stream` marked deprecated-pending.
- `apps/server/src/routes/session-events-handler.ts` (new) — `sessionEventsHandler` + `parseResumeCursor` (extracted from sessions.ts per I2).
- `apps/server/src/services/session/trigger-turn.ts` (new) — `triggerTurn`, `DetachedTurnLifecycle`, canonical-id tap, detached error guard, token-guarded lock.
- `apps/server/src/services/session/session-list-broadcaster.ts` (new) — iterates `subscribeSessionList` → `eventFanOut`.
- `apps/server/src/services/session/session-state-projector.ts` (mod) — `rekeyProjector`, `peekProjector`, `deriveTurnEndLifecycle`, I2 abort/finally in `subscribe()`.
- `apps/server/src/services/runtimes/claude-code/claude-code-runtime.ts` (mod) — eviction → `markInterrupted`+`disposeProjector`; `signal`/`token` threading.
- `apps/server/src/services/runtimes/claude-code/sessions/session-lock.ts` (mod) — per-acquisition `token` for owner-checked release.
- `apps/server/src/index.ts` (mod) — start/stop the session-list broadcaster.
- `packages/shared/src/agent-runtime.ts` + `transport.ts` (mod) — additive `signal?`/`token?` params.
- `packages/shared/src/schemas.ts` (mod) — `SendMessageResponseSchema`.
- `apps/server/src/services/core/openapi-registry.ts` (mod) — registered `GET /:id/events`; POST is now 202.

_Phase 4:_

- `apps/client/src/layers/entities/session/model/use-global-session-stream.ts` (new) — `useGlobalSessionStream()` (single-effect subscribe→reconcile→connect) + exported pure `reconcileSessionsCache`.
- `apps/client/src/layers/entities/session/model/use-sessions.ts` (mod) — removed the `refetchInterval` poll + `QUERY_TIMING` import.
- `apps/client/src/layers/shared/lib/constants.ts` (mod) — removed the now-unused `SESSIONS_REFETCH_MS`.
- `apps/client/src/layers/entities/session/index.ts` (mod) — barrel-export `useGlobalSessionStream`.
- `apps/client/src/AppShell.tsx` (mod) — mount `useGlobalSessionStream()` once (before the onboarding early-return).
- `apps/client/src/layers/features/settings/ui/AdvancedTab.tsx` (mod) — re-described the "Background refresh" `SwitchSettingRow` (external-session fallback framing).

**Test files:**

- `packages/shared/src/__tests__/session-stream.test.ts` (new, 15)
- `apps/server/src/services/session/__tests__/session-state-projector.test.ts` (new, 17 incl. C1/C2 regressions)
- `apps/server/src/services/session/__tests__/ring-buffer.test.ts` (new, 4)
- `apps/server/src/services/session/__tests__/event-log.test.ts` (new, 2)
- `apps/server/src/services/runtimes/claude-code/__tests__/session-event-normalizer.test.ts` (new, 25 incl. C2 regression)
- `apps/server/src/services/runtimes/claude-code/__tests__/claude-code-runtime-session-contract.test.ts` (new)
- `apps/server/src/services/runtimes/claude-code/__tests__/session-list-watcher.test.ts` (new)

_Phase 2 tests:_ `sessions-events.test.ts` (durable stream), `sessions-trigger.test.ts` (trigger-only, canonical-id, lock, error lifecycle, C1 integration), `events-status.test.ts` (global broadcaster + I3), `session-lock.test.ts` (token-guarded release / I1), plus migrated `sessions-streaming.test.ts` / `sessions.test.ts` / `sessions-multi-runtime.test.ts` and projector C1/C2 regressions.

_Phase 4 tests:_ `use-global-session-stream.test.tsx` (new, 8 — `reconcileSessionsCache` upsert/update/remove/identity-skip/sort + hook live upsert/remove, external-CLI-session surfacing, and the connect-before-subscribe regression), `use-sessions.test.tsx` (+1 — no-timer-poll via fake-timer advance), `AdvancedTab.test.tsx` (+1 — external-session fallback copy), `app-shell-slots.test.tsx` (mod — added `useGlobalSessionStream` to the session-barrel mock).

## Known Issues

**Resolved during GATE 1 & 2** (all with regression tests): Phase-1 C1 (replayFrom gap), C2 (outputTokens clobber); Phase-1 carry-forwards I1 (projector disposal — wired into eviction) and I2 (subscribe abort — `AbortSignal`); Phase-2 C1 (canonical-id orphan — `rekeyProjector`), C2 (errored turn → terminal lifecycle), I1 (token-guarded lock), I2 (sessions.ts split), I3 (broadcaster startup try/catch).

**Still open / carry-forward:**

- **I3 (Phase 1) — `subscribeSessionList` emits `session_upserted`/`session_removed` but not `session_status`.** External per-session liveness (streaming vs idle for a CLI session no client has opened) is NOT on the global stream. **Phase 4 outcome:** the sidebar list is now live via `session_upserted` metadata diffs (title/updatedAt/preview/contextTokens) reflected into the `['sessions', cwd]` cache (requirement #3 met for list accuracy). The `session-list-store` `statuses` slice + `useSessionListStatus` selector are wired and ready, but a live per-row lifecycle dot for purely-external sessions still awaits the server emitting `session_status` on the global stream. **Carry to #16** (and a candidate for #19-era work or a follow-up): emit `session_status` from `subscribeSessionList`.
- **External per-session JSONL delta → projector feeding** (so an externally-driven session's `subscribeSession` streams live mid-turn) deferred to #9 (needs JSONL-delta → StreamEvent re-parse). `subscribeSessionList` discovery of external sessions IS done.
- **Chat SEND — RESTORED in Phase 5 (#13).** The submit path now reads the `202 {sessionId}`, shows an optimistic user message, re-attaches `/events` to the canonical id, rewrites the URL, and reconciles canonical history on turn_end. Unit-tested + the receive/history/connection paths browser-verified. **Live multi-turn send (send → stream → settle-to-idle → send again) should be confirmed in #16** — C1 (the turn-settle fix) is what enables the second send, and a live LLM turn is the definitive proof (deferred from this session per the user's "stop after Phase 5" + cost of a real turn).
- Pre-existing: `SessionStatusSchema.partial()` injects `runningSubagentCount: 0` via `.default(0)` at the Zod-parse wire boundary; harmless (projector operates on in-memory `RawSessionEvent`s, not parse output).

**Phase-3 carry-forwards / decisions (read before #11–#18):**

- **DECISION NEEDED — live-turn rendering fidelity (review #2).** The runtime-neutral `SessionEvent` contract (ADR-0263) intentionally omits `thinking`, `hook_*`, `tool_progress`, and `memory_recall` (server normalizer returns `null` for them). Completed turns still render full fidelity from JSONL history (`snapshot.messages`), but during a **live** turn (post-#13) thinking blocks / streaming tool-progress / hook results / memory-recall chips will NOT appear until the turn completes and history reloads. This was not surfaced at ideation. Options: (a) ACCEPT the leaner live render; (b) EXTEND the contract to carry these. **RESOLVED — user chose (b) EXTEND. Tracked as task #19** (extend `SessionEventSchema` + Claude normalizer + projector + client projection + ADR-0263; blocks #16). Other adapters MAY omit these → degrade to lean render, never break (stays runtime-neutral).
- **Dead code — `watchSession` + `session-broadcaster.ts` (server).** The deleted `GET /:id/stream` route was their only production caller. Left in place (they're `AgentRuntime`/`Transport` interface members implemented by all runtimes + `FakeAgentRuntime`; retiring is interface surgery that entangles #15). **Follow-up:** retire after #15 lands (fold into #18).
- **Naming collision (Minor).** Two modules export `streamManager`/`StreamManager`: the NEW connection singleton `shared/lib/transport/stream-manager.ts` (web durable streams) and the LEGACY send-path `features/chat/model/stream/stream-manager.ts` (deleted when #13 retires the in-band pipeline). #11/#13 agents: import the new one from `@/layers/shared/lib/transport`; the legacy one is import-path-distinct and slated for deletion.
- **Presence retired (intentional).** `presenceInfo`/`presenceTasks`/the presence badge rode the now-deleted cross-client-sync stream, were gated behind the (removed) off-by-default flag, and aren't a spec requirement. The new `SessionEvent`/`SessionListEvent` contract doesn't carry presence. Possible follow-up: re-add presence to the global `/api/events` stream if desired.
- **`subscribeSessionList` still emits no `session_status` (Phase-1 I3).** External per-session liveness surfaces only via `session_upserted` metadata diffs. **Carry to #11/#16.**

**Phase-5 carry-forwards / decisions (read before #15–#18):**

- **Dead code on the legacy `session-chat-store` — `isRemapping` + `renameSession` (review M1).** After #13 removed the `done`-event remap, nothing sets `isRemapping:true` and `renameSession` has no production caller. Left inert (removing cascades into `session-chat-store.test.ts`). **Fold into the consolidated dead-code retirement** alongside `watchSession`/`session-broadcaster.ts` (post-#15, in #18).
- **`/pending-interactions` pull endpoint still 404s an in-memory-untracked session** (same class as the `/events` bug, NOT fixed). It's now redundant — the `/events` snapshot carries `pendingInteractions` (DOR-73 recovery flows through the stream store). **Candidate for removal in #18**, or relax its gate like `/events` if any client path still depends on the pull.
- **`key={sessionId}` deliberately NOT added to ChatPanel** (DOR-81 task wording). It would remount mid-send during #13's client-uuid→canonical-id URL rewrite; the per-session store-keying isolates queue/draft state without remounting. If a future change needs a hard session boundary, key on a STABLE id, not the rewritten `?session=`.
- **Pre-existing flaky test:** `apps/client/src/layers/shared/lib/transport/__tests__/sse-connection.test.ts` (`'reconnecting', 1` assertion, timer-based) fails ~1/run under full-suite parallel load, passes 40/40 in isolation. Untouched by this spec. Candidate for a `vi.useFakeTimers` hardening pass (out of scope here).
- **Live-send acceptance is #16.** C1 (turn settles to idle on `turn_end`) enables multi-turn send; confirm end-to-end in the browser there.

**Client contract (server side, for #13):** open `GET /events` (snapshot→replay→live) for the active session BEFORE/at POST so `turn_start` isn't missed (StreamManager + `useSessionStream` already do this); POST returns `202 { sessionId: canonicalId }`; on the 202, #13 must re-key the URL + call `streamManager.attachSession(canonicalId)` (the server already rekeyed the projector, so snapshot+replay covers any gap). Two SSE connections total per client: active-session `/events` + global `/api/events` (both wired by #8/#9). `Transport.subscribeSession`/`getSessionSnapshot`/`subscribeSessionList` remain client-side stubs — the WEB path uses `StreamManager`+`SSEConnection` directly; those Transport methods are the Obsidian `DirectTransport` (in-process) seam, implemented in #15/#6-era follow-up.

## Implementation Notes

### Session 1

**Fixed during GATE 1 (Phase-1 review):**

- **C1** — `SessionStateProjector.replayFrom` short-circuited on a non-empty ring and never consulted the EventLog; since the ring clears on `turn_start`, resuming with a cursor predating the current turn silently dropped the prior turn's tail + `turn_end`. Fixed by merging ring+log by seq (ordered, deduped). The gap-free resumability invariant the whole spec depends on.
- **C2** — the final `session_status` lacks `outputTokens`; the normalizer coerced it to `0` and the projector's shallow status merge clobbered the real running count. Fixed by (a) normalizer emitting only present fields, (b) `status_change` carrying a deep-partial `contextUsage`, (c) projector field-wise merging `contextUsage`. The resolved `SessionSnapshot.status.contextUsage` stays a complete object.

**Architecture established:** DorkOS owns the runtime-neutral seq + projection (ADR-0263/0264); the projector is the single server-side source of truth fed by adapter-normalized `RawSessionEvent`s; persistence is injected (`buildSnapshot(loadHistory)`), so the Claude adapter stays JSONL-backed and the stateless runtime (#15) will back the same contract with the EventLog.

**Phase 2 outcome:** the server now delivers turns exactly once over a durable, resumable per-session SSE (`/events`) decoupled from the POST, plus a global discovery/status stream (`/api/events`). Both go through the `AgentRuntime` contract (no Claude special-casing), so the stateless runtime in #15 will exercise the identical route/client code path. Every server-side spec acceptance for Phases 1–2 is met and gated; four criticals surfaced by holistic review were all fixed before proceeding. **Next: Phase 3 (#8) — the client `StreamManager` singleton + per-session Zustand store, then subscribe-first hydration (#9) and flag removal (#10).**

### Session 2 — full-branch review vs spec (2026-06-10, post-Phase-5, pre-#19)

User-requested holistic review of ALL branch work against `02-specification.md` + world-class bar. Two code-reviewer agents (server+shared / client) with repro verification, plus inline trace of Phase-5 hotspots. Full suite green at time of review (after the fixes below).

**Fixed in this session (uncommitted, with regression tests):**

1. **Reconcile-vs-queued-flush race (Critical).** The DOR-81 auto-flush fires on the SAME settle edge as `useTurnEndReconcile`'s history reload; the reload's `.then` then cleared the _new_ send's optimistic message and `setHistoryMessages` wiped the _new_ turn's `inProgressTurn`. Fixed: per-session "owned optimistic id" captured at the →streaming edge (only the settled turn's own message is cleared) + `setHistoryMessages(..., { preserveInProgressTurn })` when a new turn is already streaming. Tests: `use-session-submit.test.tsx` (flush-race), `session-stream-store.test.ts`.
2. **Mid-turn-reconnect duplicate user bubble (Important).** `applySnapshot` kept the optimistic message even when the snapshot's history already ended with it (user msg is written to JSONL at turn start). Fixed: content-compare against the snapshot's last user message (best-effort; transformContent sends won't match and self-heal at settle). Tests added.
3. **Ghost duplicate sidebar entry after DOR-74 rekey (Important).** The rekey block inserted the canonical cache row but never removed the client-UUID row (its own comment claimed it did); with the sessions poll gone nothing healed it. Fixed + test.

**MERGE BLOCKERS found by review (verified, NOT yet fixed — need new tasks):**

- **SRV-C1 — stale resume cursor → permanently deaf stream.** After server restart (projector counter resets) or beyond the EventLog trim horizon (cap 5000), a reconnect with `Last-Event-ID` gets empty replay AND the live filter `seq > cursor` drops everything forever; no snapshot, no error. Repro'd. Violates the spec's restart-degradation requirement (Non-Goals). Fix: projector `oldestSeq`/cursor validation in `session-events-handler.ts`; unserveable resume → fall back to cold snapshot path (or explicit `reset` event) + restart-degradation test.
- **CLI-C1 — live Approve/Deny card never renders.** During a live turn `approval_required` lands only in `pendingInteractions`; `partsContainInteraction` sees the bare `tool_call` part (same id) and skips folding → no `interactiveType`, no card, no shortcuts; session blocks with no affordance (refresh ironically fixes it — inverts DOR-73). Also NO interaction-resolution signal exists: server `resolveInteraction` deletes silently; client store only clears via snapshot → ghost pending cards after reconcile. Fix: upsert interaction fields when the matching part lacks `interactiveType`; add resolution signal (event or clear-on-`tool_result`). Blocks #16 (its matrix checks approvals).
- **CLI-C2 — Transport seam bypassed.** `DirectTransport.postMessage` THROWS → Obsidian send hard-broken on-branch. `StreamManager` hardcodes relative `/api/...` URLs → packaged Electron (loadFile + localhost API) can't reach the durable streams, which are now the SOLE delivery path. `Transport.getSessionSnapshot/subscribeSession/subscribeSessionList` are throwing stubs in BOTH adapters. Untracked by #15–#18 — needs its own task (DirectTransport wiring + baseUrl-aware stream URLs).
- **SRV-I1 — abort-listener leak:** projector `subscribe` adds one `{once:true}` abort listener per delivered event, never removed on the normal path (repro'd: 26 listeners/25 events; MaxListeners warning at 11; grows for hours on durable connections).
- **SRV-I2 — projector eviction broken for rekeyed sessions:** SessionStore keys by request UUID, `rekeyProjector` re-keys registry to canonical → eviction's `peekProjector(uuid)` misses → permanent projector+EventLog (≤5000 events) leak per new session; `markInterrupted` also skipped.
- **SRV-I3 — unbounded projector registry:** any well-formed id on `/events`/snapshot creates a never-disposed projector (no-404 policy side-effect). Dispose empty projectors on last detach or LRU-cap.

**IMPORTANT (quality bar / fold into #16–#19 or new tasks):**

- **SRV-I4 — multi-project sidebar liveness regressed:** `session-list-broadcaster` watches only `DEFAULT_CWD`; non-default `?dir=` lists never live-update now the poll is gone (Kai = 5 projects). Watch known project dirs or re-scope req #3.
- **CLI-B4 — live todo updates severed:** `todo_update` stored then dropped by projection; `onTaskEvent` has no caller; `['tasks']` never invalidated on settle; TaskListPanel only updates via default-off poll (violates G6 spirit). Wire into #19's projection work.
- **CLI-B5 — three SSE connections:** `event-stream-context.tsx` holds its own `/api/events` alongside `streamManager.connectList()` → doubled global traffic; spec budget is two. Unify.
- **CLI-B6 — status strip dead values:** `streamStartTime`/`estimatedTokens`/`isTextStreaming` have no writers → "0m 00s" + "~0 tokens" every live turn; completion chip never shows. Derive from `turn_start`/`text_delta` (#19-adjacent).
- **ConnectionItem copy now dishonest:** "messages you send and receive are unaffected" — false; `/events` IS the chat path. Rewrite copy.
- **CLI-B7:** idle-status RTT window allows double-submit (no busy state between Enter and first server frame). **CLI-B9:** switch-back to a background-settled session fires spurious settle → reload + notification sound. **CLI-B10:** `hasUnseenActivity` never set (writer deleted) → unseen-activity border/agent-status readers are dead UI.
- **SRV minor:** projector imports claude-code `listPendingInteractions` (domain cycle + `as unknown as` cast) — move selector into `services/session/`; `feedProjector` (SDK-free, used by routes for every runtime) misplaced in claude-code dir; stale "TODO(task #6)" comment in normalizer; backpressure ignored on `res.write` (durable stream + fan-out `drain` no-op); `04-implementation.md` Task-#11 claim about connect-time `session_upserted` inventory is wrong (emitted once at broadcaster start, to zero clients) — correct in #18.

**Cleanup debts (no-legacy standard, fold into #18):** stale transition comments (`derive-rendered-state.ts` "#10/#13", `use-chat-session.ts`); dual history pipeline (legacy chat-store seeding + `mountGeneration`/`historySeeded` machinery) still parallel to snapshot hydration; throwing Transport stubs; unused `ensureSession`/`disconnectList`/`markToolCallResponded` no-op chain.

**Verdict:** architecture matches the spec's snapshot+resumable-delta intent and the happy path is correct + well-tested (180 server / 1,236 client tests in scope green; typecheck green). NOT mergeable yet: SRV-C1, CLI-C1, CLI-C2 + the two server leaks are blockers; the live-fidelity surface (approvals, todos, elapsed/tokens, indicator copy) is below the world-class bar until #19 + the items above land. Recommended order: commit this state → file blockers as tasks → fix SRV-C1/I1/I2 + CLI-C1 → #19 → CLI-C2 → #15–#18.

### Session 2 (continued) — hardening batch DONE (post-review blockers, uncommitted on top of db81d2b0)

Phase 5 + review fixes committed as `db81d2b0`. The review's merge blockers were then fixed and gated (typecheck 21/21, lint 0 errors, full `pnpm test -- --run` green; client 4,123 / server 2,820+):

1. **SRV-C1 FIXED — stale resume cursor.** `id:` frames now carry a process **stream epoch** (`<sid>-<epoch>-<seq>`, `STREAM_EPOCH` in `session-events-handler.ts`); a `Last-Event-ID` from a previous process routes to the cold path (bare-integer cursors are not comparable across restarts). Same-epoch unservable gaps (EventLog trim) throw `StaleResumeCursorError` (new, `@dorkos/shared/session-stream`) EAGERLY from `projector.subscribe()`/adapter `subscribeSession` (validated via `assertResumable` + `EventLog.earliestSeq()`); the route catches and falls back to snapshot+live. AgentRuntime TSDoc + OpenAPI docs updated. Tests: epoch-mismatch fallback, StaleResumeCursorError fallback (route), ahead-of-counter / trim-floor / boundary cases (projector).
2. **SRV-I1 FIXED — abort-listener leak.** The park race now removes its abort listener after EVERY settle (named handler + removeEventListener), not just when abort fires. Test asserts ≤1 listener after 25 delivered events (`getEventListeners`).
3. **SRV-I2 FIXED — rekeyed-projector eviction leak.** `SessionStore.checkSessionHealth` returns each evicted session's UUID AND canonical `sdkSessionId`; the runtime disposes projectors under both. Test: rekey → evict → projector interrupted + dropped.
4. **SRV-I3 FIXED — unbounded projector registry.** `subscribeFrom` counts subscribers; on last detach an EMPTY projector (cursor 0, no interactions) self-disposes via identity-guarded `disposeProjectorIfCurrent`. Tests: empty self-disposes; non-empty survives.
5. **CLI-C1 FIXED — live Approve/Deny + resolution signal.** (a) `partsContainInteraction` now only skips when the part already CARRIES interaction state — a bare live `tool_call` part gets the approval fields upserted (the card renders live). (b) New contract event **`interaction_resolved`** (`{id, resolution?: approved|denied|answered}`): projector's `resolveInteraction` (previously DEAD CODE — nothing called it; ghost cards persisted until TTL even server-side) now ingests it as a seq'd event; `ClaudeCodeRuntime.approveTool/submitAnswers/submitElicitation` notify via `notifyInteractionResolved` (peeks uuid + canonical alias); client StreamManager forwards it (12 event types now); client store drops the DTO + records the event in the turn; projection `foldInteractionResolved` settles the part (denied→error, answered→complete, approved→running) and kills the countdown. Tests at every layer incl. the exact live-bare-tool_call + DTO combination.

**Carry-forwards:** timeout/abort-path resolutions (auto-deny, SDK abort) do NOT emit `interaction_resolved` — expiry self-heals via the remainingMs selector on both ends; acceptable. `?after=` query resumes carry no epoch (still floor-validated). ADR-0263/0264 must document the epoch + `interaction_resolved` (fold into #18).

**REMAINING (in-session tasks #4-#7):** #19 contract extension (+ fold in B4 todo wiring, B6 status-strip derivation) → CLI-C2 Transport seam (untracked before; now task #5) → quality pass (SRV-I4 multi-project watch, B5 third connection, indicator copy, B7/B9/B10) → #15-#18.

### Session 3 — task #19 contract extension + B4/B6 DONE (uncommitted on top of bdd63648)

In-session task #4 (= spec task #19, folding in review items CLI-B4 + CLI-B6). Gated: typecheck 21/21, lint 0 errors (the 2 new `set-state-in-effect` warnings resolved with justified disables, matching the `use-task-state` precedent), full `pnpm test -- --run` green (20/20 tasks; client 366 files / 4,140 tests).

1. **Contract: 4 fidelity members (12 → 16 SessionEvent types).** `thinking_delta` (reuses `ThinkingDeltaSchema`), `tool_progress` (reuses `ToolProgressEventSchema`; delta semantics appended to the tool part's progress output), `hook_update` (collapses `hook_started/progress/response` into ONE upsert-by-`hookId` member with a `status` field — the `subagent_update` precedent; only `hookId`+`status` required so each phase carries just its fields), `memory_recall` (reuses `MemoryRecallEventSchema`). Documented as an OPTIONAL fidelity tier: adapters MAY omit them; clients degrade to a lean render with no behavioral branch. `HookStatusSchema` now exported from `schemas.ts`. ADR-0263 updated (core vs fidelity tier + collapse-multi-phase-events convention).
2. **Claude normalizer** (`session-event-normalizer.ts`): maps the 6 StreamEvents onto the 4 members (`hook_response.outcome` → status via `mapHookOutcome`; null `toolCallId` preserved for session-level hooks). **Projector needs NO change** — fidelity events ride `inProgressTurn` via the generic push and replay like any event (test: normalizer→projector→`replayFrom` end-to-end).
3. **Client pipeline:** StreamManager `SESSION_EVENT_TYPES` → 16; store `TURN_EVENT_TYPES` records all four in the turn. `project-session-turn.ts` grew pure folds mirroring the legacy handlers exactly: thinking coalesces + finalizes on first `text_delta` (no `elapsedMs` — the contract carries no timestamps; canonical history supplies it post-reconcile); `tool_progress` appends to `progressOutput`, cleared by a result-carrying `tool_result`; `hook_update` merges onto its HookPart with a pass-local ORPHAN BUFFER for hooks preceding their `tool_call` (legacy `orphanHooksRef` parity; `foldToolCall` drains it); session-level hooks (no toolCallId) drop; `memory_recall` pins a streaming part at index 0 with first-writer-wins path dedup (legacy `upsertMemoryRecallPart` parity).
4. **CLI-B4 FIXED — live todo wiring.** New `use-todo-events.ts`: forwards NEWLY-streamed `todo_update`s from the projected turn to `onTaskEvent` (TaskListPanel + celebrations — `onTaskEvent` had NO caller since Phase 5). Snapshot-hydrated events (seq ≤ snapshot cursor) are suppressed via a per-session `{floor, seen}` watermark that re-anchors when a new snapshot arrives (covers server-restart seq resets) — replays can't re-pop celebrations. `useTurnEndReconcile` now takes `queryClient` and invalidates `['tasks', sessionId]` on settle (canonical re-sync).
5. **CLI-B6 FIXED — status strip live again.** New `use-stream-timing.ts` derives all three dead values from the projection: `streamStartTime` anchored per-session at the →streaming edge (Map survives session switches; attach-mid-turn anchors at attach time — honest best effort, the contract has no timestamps), `estimatedTokens` = Σ `text_delta` chars / 4 (legacy heuristic), `isTextStreaming` with the legacy 500ms decay (timer re-armed while up so an unrelated re-render can't strand it; session switches can't false-flag growth). `useChatSession` stops reading the three dead legacy-store fields.

**Notes for #18 (docs/cleanup):** legacy `stream-event-handler.ts` remains live ONLY via `use-pending-interactions.ts` (recovery Path A/B renderers) — the thinking/hook/progress/memory branches in it are now dead weight to retire with the dual-pipeline cleanup. `subagent_text_delta` (live subagent text) and `permission_denied` (denial chip) are NOT in the contract — deliberately out of #19's scope; decide in #18 whether to add or document as legacy-only.

**REMAINING (in-session tasks #5-#7):** CLI-C2 Transport seam → quality pass (SRV-I4, B5, copy, B7/B9/B10) → #15-#18 (#16 browser acceptance now unblocked).

**Session-3 self-review (pre-commit) — 2 fixes:**

1. **Duplicate tool parts on live streamed input (pre-existing, surfaced by the orphan-drain change).** The adapter emits `tool_call_start` (no input) + one `tool_call_delta` PER streamed `input_json_delta` fragment, and the normalizer maps ALL of them to `tool_call` — `foldToolCall` pushed a part per event, so a live tool call rendered N duplicate parts with only the last settling on `tool_result`. Fixed: `foldToolCall` upserts by `toolCallId`, APPENDING input fragments (legacy `handleToolCallDelta` parity; safe because the two adapter input sources — streamed fragments vs the whole-input assistant-message fallback gated on `!toolInputReceived` — are mutually exclusive). Regression test added.
2. **Stale `streamStartTime` anchor across turns.** A session that settled and started a NEW turn while backgrounded (snapshot on switch-back), or back-to-back turns coalesced into one render by the queued flush, reused the previous turn's wall-clock anchor — wrong elapsed time. Fixed: the anchor is keyed to the turn's identity (`inProgressTurn[0].seq`, the turn_start) and re-anchors when it changes. Regression test added.

Verified-safe during the trace: `deriveFromParts` ignores thinking/memory parts (no content/toolCalls leakage); StreamManager validates new frames via the rebuilt `SessionEventSchema.safeParse`; effect ordering in `use-todo-events` (ref-sync declared before the scanner); `message-event-mapper`'s whole-input `tool_call_delta` fallback can't double-append. Re-gated after fixes: typecheck 21/21, lint 0 errors, full suite green (server 193/2,829; client 366/4,142).

### Session 3 (cont.) — acceptance-run fixes F1/F2/F4/F5 — DONE

Browser acceptance run #1 (perm:default, two concurrent tool-heavy sessions; report `test-results/session-switch-test/20260610-173202.md`) re-confirmed check #6 PASS on the live path and found the **first-turn id split-brain**. Fixes, all gated green (typecheck 21/21, lint 0 errors, full `pnpm test -- --run`):

1. **F1 — snapshot history under client id** (`claude-code-runtime.ts getSessionSnapshot`): translate the history-loader id via `getInternalSessionId(id) ?? id` (the `GET /:id/messages` translation). Test: contract test "translates a client-facing id…".
2. **F2 — rekey race** (`trigger-turn.ts`): `tapFirstEvent` → `tapEachEvent`; `tryRekey` retries per yielded event until `getInternalSessionId` resolves, then disarms. The 202's canonical id stays best-effort (no latency change). Plus **alias-aware projector resolution** (`resolveLiveProjector` = `peekProjector(id) ?? peekProjector(internalId ?? id)`) used by `getSessionSnapshot`/`subscribeSession`/`notifyInteractionResolved`. Tests: route test "rekeys even when the canonical id resolves only AFTER the first event"; contract tests for snapshot+subscribe via the alias. Residual (accepted): canonical-id access in the sub-second window BEFORE the SDK init resolves still mints a fresh projector — needs an sdkId→requestUUID reverse index to close; the sidebar can't navigate there before the JSONL exists, so it is not reachable in practice.
3. **F4 — composer draft** : deleted `use-interactive-draft.ts` + its call in `ChatInputContainer` — the per-session store input already survives the interaction-card swap; the hook's non-session-scoped ref RESTORED the old session's draft into a newly-switched session. Regression test: "never writes to the composer input on interactive-mode transitions". Not fixed (out of scope, standard SPA): drafts do not survive a full page reload (in-memory store).
4. **F5 — ghost steer-cancelled interaction**: new `interaction_cancelled` StreamEvent (shared schemas) pushed by all three interactive handlers' abort+timeout paths — incl. the previously-missing abort listener on `handleAskUserQuestion` (the live repro). Normalizer maps it to `interaction_resolved{resolution:'cancelled'}` (enum widened in the contract + ADR-0263 note); projector `untrackInteraction` already drops the DTO and settles `blocked`. Client `resolvedToolStatus` maps `cancelled` → `error`. Tests: 3 handler tests (question abort, approval abort, approval timeout) + normalizer mapping case.

F3 (sidebar liveness, user-reported) folded into the task #6 quality pass (SRV-I4) — next batch after CLI-C2.

### Session 4 — sidebar liveness (F3/SRV-I4 status half) + resume-bootstrap suppression — DONE

Two user-reported issues (2026-06-11), both fixed and gated green (typecheck 21/21, lint 0 errors, full `pnpm test -- --run`: server 193 files / 2,850; client 368 files / 4,166 incl. new suites).

1. **"Continue from where you left off." fake user messages** (in-session task #11). Root cause: the CLI writes a resume-bootstrap pair on EVERY `query({resume})` turn DorkOS triggers — an `isMeta:true` user record ("Continue from where you left off.") + a `model:'<synthetic>'` assistant record ("No response requested."). The CLI hides `isMeta` records in its own UI; DorkOS's `parseTranscript` never checked the flag (156 bootstrap records + skill-expansion prompts + caveats were all rendering as operator messages). Fixed in `transcript-parser.ts`: skip `isMeta` user records (still flushing a pending slash command — the expansion record that follows `<command-name>` is itself isMeta and is the flush trigger); skip the synthetic 'No response requested.' assistant record (synthetic API-error notices stay visible; compaction summaries are NOT isMeta and are unaffected). Live path verified clean (the message mapper never emits user text). 4 regression tests.
2. **Sidebar agent/session liveness dead** (in-session task #12; the status half of SRV-I4/F3). Diagnosis: broken at BOTH ends — the projector is the only component that knows lifecycle, but `session_status` was never emitted onto `/api/events` (the list watcher only emits upserted/removed); the client's `useSessionBorderState`/`useAgentHottestStatus` read only the legacy chat store, whose sole remaining writer is the pending-interactions recovery path (hence "only shows when awaiting approval"); `useSessionListStatus` had ZERO consumers; and `DashboardSidebar` passes `sessions=[]` to every non-active `AgentListItem`, so agent rows could never light up. Fix, end to end:
   - **Shared**: `session_status` list event gains optional `cwd` (lets clients aggregate liveness per agent).
   - **Server**: `onProjectorStatusChange` registry-level listener; `ingest` notifies on lifecycle TRANSITIONS only (token deltas never fan out), `markInterrupted` notifies, `rekeyProjector` adopts the canonical id and re-announces under it; `getOrCreateProjector(id, cwd?)` stamps cwd first-writer-wins (wired at the POST trigger + snapshot + subscribe sites). `SessionListBroadcaster.start()` subscribes (installed before, and surviving, a watcher construction failure) and `stop()` detaches. This path is cwd-agnostic — liveness works for ANY working directory, unlike discovery.
   - **Client**: `session-list-store` records `statusCwds`; `useSessionBorderState` merges three sources hottest-wins (stream store w/ pendingInteractions → list-store lifecycle → legacy chat store; `borderKindFromLifecycle` maps streaming/blocked/error, interrupted→idle); `useAgentHottestStatus(sessionIds, agentPath)` adds stream-store + list-store scans AND a cwd-match scan so a COLLAPSED agent row lights up from fan-outs alone; `AgentListItem` passes `path`.
   - **Tests**: 6 projector fan-out tests (transitions/no-token-deltas/blocked-resolve/interrupt/rekey-re-announce/unsub + cwd stamping), 3 broadcaster wire tests over real SSE (incl. liveness surviving watcher failure; 2 pre-existing broadcaster-leak tests fixed to stop()), 7 border-state merged-source tests, 8 agent-hottest tests (incl. collapsed-agent cwd match).
   - **Residuals**: list-store statuses are in-memory with no restart re-baseline — a server restart mid-turn can leave a stale 'streaming' border until the next transition (the markInterrupted degradation hook covers the server side; consider a status sweep/clear on `/api/events` reconnect with the task-#6 discovery work). Discovery (`session_upserted`) is still DEFAULT_CWD-only — the remaining half of SRV-I4 in task #6.
   - **code-reviewer pass (pre-commit) — 4 findings fixed**: (C1) pre-rekey transitions broadcast under the request UUID landed in `statusCwds` with NO removal path (no session_removed ever fires for a request UUID) → permanent false 'Working' on the agent row via the cwd scan. Fixed: `session_status` gains optional `retiredSessionId`; the rekey re-announce names the old UUID and `applyListEvent` drops its entries. (I2) unbounded `statuses`/`statusCwds` growth on long-lived clients (settled sessions outside DEFAULT_CWD never removed) → settled lifecycles (idle/interrupted) now PRUNE instead of store (absent ≡ idle to `borderKindFromLifecycle`). (I3) test gaps closed: throwing-listener isolation, rekey retire signal, broadcaster stop→start re-subscribes exactly once. (M4) subscribe-path cwd fallback could mis-stamp a session to the default agent first-writer-wins → the POST trigger's operator-chosen cwd now overwrites. Two list-store tests updated for prune semantics (idle fixtures → streaming).

**REMAINING (in-session tasks #5-#7):** CLI-C2 Transport seam → quality pass (SRV-I4 discovery half, B5, copy, B7/B9/B10) → #15-#18.

### Session 5 — CLI-C2 Transport seam (in-session task #5) — DONE

Fixed the three CLI-C2 gaps end to end, restoring the Obsidian and packaged-Electron surfaces to the trigger-only + durable-stream architecture. Gated green (typecheck 21/21, lint 0 errors, full `pnpm test -- --run`: server 194 files / 2,856; client 369 files / 4,188 incl. 3 new suites; obsidian plugin builds).

1. **Transport contract** (`packages/shared/src/transport.ts`): `subscribeSession` gains optional `signal?: AbortSignal` (4th param), mirroring `AgentRuntime.subscribeSession` — a bare `iterator.return()` cannot interrupt a generator parked on an un-settleable wait, so the signal is the deterministic teardown for re-target/switch.
2. **HttpTransport stubs → real** (new `session-stream-methods.ts` factory, composed into `http-transport.ts`; stubs deleted from `session-methods.ts`): contract-level single-shot primitives over `fetch` + `parseSSEStream` — `getSessionSnapshot` captures the leading snapshot frame of a cold `/events` connect then aborts (no REST snapshot endpoint exists); `subscribeSession` iterates with `?after=` resume, skips the cold snapshot frame, drops malformed frames (warn), and throws `StaleResumeCursorError` when a snapshot arrives on a RESUME connect (the server's unservable-cursor fallback — silently skipping it would hide the gap); `subscribeSessionList` filters the unified `/events` fan-out to the 3 list discriminants. NO reconnection here by design — resilience (backoff/heartbeat/Last-Event-ID epoch) stays in StreamManager/SSEConnection, the app's HTTP path.
3. **DirectTransport stubs → real** (`direct-transport.ts`): services.runtime widened with the AgentRuntime snapshot/subscribe trio (ctx `{cwd: cwd ?? vaultRoot, permissionMode: 'default'}`); the two subscribe methods return the runtime's iterables DIRECTLY (an `async *`/`yield*` wrapper serializes `return()` behind a parked `next()` — see review C-finding below); new REQUIRED `turnTrigger` service; `postMessage` triggers a detached in-process turn and throws a typed `SESSION_LOCKED` error when not accepted (only reachable from a DIFFERENT client id — same-client acquire steals); class gains `clientId = embedded-<uuid>`.
4. **Embedded trigger** (new `apps/server/src/services/session/embedded-turn-trigger.ts`): `createEmbeddedTurnTrigger(runtime, feedProjector)` mirrors the POST route exactly — `getOrCreateProjector` + authoritative cwd overwrite + `triggerTurn` with the same deps/lock-token wiring. `feedProjector` injected to keep services/session runtime-neutral (`FeedProjector` type consolidated into `trigger-turn.ts`). New package export `@dorkos/server/services/session`.
5. **StreamManager source abstraction** (`stream-manager.ts` + new `transport-stream-pump.ts`): pluggable source — `useHttpSource(baseUrl)` (default `/api`; packaged Electron passes the same resolved origin as HttpTransport via `main.tsx`) and `useTransportSource(transport)` (embedded: pumps `getSessionSnapshot` → `snapshot` handler → `subscribeSession(snapshot.cursor, signal)` → typed handlers; list pump iterates `subscribeSessionList` with `iterator.return()` teardown). Same validated listener fan-out either way; identical-source reconfigure is a no-op; a real source switch tears down and re-attaches whatever was live. Web path byte-identical (default `/api` URLs unchanged).
6. **Wiring**: `main.tsx` `streamManager.useHttpSource(apiBaseUrl)`; Obsidian `CopilotView` wires `turnTrigger` + `useTransportSource(transport)` and now detaches both streams in `onClose`. New client package export `@dorkos/client/lib/transport`.
7. **Tests**: server `embedded-turn-trigger.test.ts` (4: accept+lock+feed, locked-409 parity, canonical id, authoritative cwd); client `session-stream-methods.test.ts` (10: snapshot capture/protocol-violation/non-OK, resume `?after=`, snapshot-skip, malformed-drop, list filtering, StaleResumeCursorError on resume-fallback, external-signal chaining); `direct-transport-streams.test.ts` (9: ctx mapping, yield-through, postMessage trigger/lock/fallback, **iterable identity** — the no-wrapper proof); stream-manager source describes (9 new: baseUrl URLs, source-switch re-attach incl. transport, identical-source no-op, pump snapshot→connected→live, detach aborts the in-process signal, **parked-list-iterable teardown via `iterator.return()`**, pumped-frame validation).
8. **code-reviewer pass (pre-commit) — all findings fixed**: (I1, verified leak) `DirectTransport`'s `yield*` delegation wrappers serialized `iterator.return()` behind a parked `next()` — the list stream has no abort signal, so `disconnectList()` would hang and leak the runtime's chokidar watcher per Obsidian view reopen → direct iterable return + identity/teardown regression tests. (I2) HTTP `subscribeSession` silently swallowed the stale-cursor cold fallback → now throws `StaleResumeCursorError`, matching DirectTransport/AgentRuntime semantics. (I3) teardown test gaps closed (parked list iterable, signal chaining, transport-source re-attach). (M4) lockedBy/lockedAt approximation documented. (M5) stale "11 event types" comment fixed. (M6) identical-source churn guard. (M7) CopilotView onClose teardown. (M8) `FeedProjector` type consolidation. Deferred: `direct-transport.ts` file-size split (batch reduced it 751→~640); the brief third `/events` connection a standalone `getSessionSnapshot` opens (note for task #6's connection-budget work).

**Verification honesty:** the Obsidian surface is verified by build + unit/contract tests, not by a live vault run; packaged-Electron stream URLs are verified by unit test, not a packaged run. Live verification rides with task #7/#16 (browser acceptance + surfaces smoke).

**REMAINING (in-session tasks #6-#7):** quality pass (SRV-I4 discovery half, B5 third connection, ConnectionItem copy, B7/B9/B10, server minors, list-store status re-baseline on reconnect) → #15-#18.

### Session 6 — quality pass, server batch (in-session task #6, batch A) — DONE

The server half of the task-#6 quality pass. Gated green (typecheck 21/21, lint 0 errors, server 195 files / 2,860 tests incl. 2 new suites, client 369 files / 4,188, obsidian plugin builds).

1. **SRV-I4 discovery — root cause was WORSE than filed: live discovery was dead for EVERY cwd, not just non-default ones.** The repo ships chokidar v5, which (since v4) does not support globs — `chokidar.watch(join(dir, '*.jsonl'))` watches a literal path named `*.jsonl` that never exists and silently never fires (verified empirically against the installed chokidar; the unit tests mocked chokidar, which is exactly how it shipped green). Rewrote `session-list-watcher.ts` fleet-wide AND v5-correct:
   - Watches the `~/.claude/projects` ROOT (`depth: 1`, no glob, `.jsonl` filtered in-handler) — new slug dirs created mid-watch are picked up (verified), covering every project (the SRV-I4 multi-project requirement; the `AgentRuntime.subscribeSessionList` contract was already ratified as "ALL sessions the adapter can observe").
   - Per-dir debounced rescans + per-dir `known` maps: a streaming burst re-scans only its own project, and one project's rescan can never "remove" another's sessions.
   - Each emitted Session carries its TRUE `cwd` from the JSONL head (the slug is lossy), which `use-global-session-stream.ts` already keys its `['sessions', cwd]` cache patches by — so multi-project sidebars route correctly with no client change.
   - `TranscriptReader` gains `getProjectsRoot()` + `listSessionsInDir(dir)` (`listSessions` now boundary-validates then delegates). `ClaudeCodeRuntime.subscribeSessionList(_ctx)` ignores ctx (fleet-wide contract); broadcaster ctx comment updated.
   - Tests: unit suite rewritten (7 tests — incl. an explicit "watch target is the root, NOT a glob" regression guard, per-dir rescan scoping, non-jsonl ignore, unchanged-metadata suppression) + a NEW real-chokidar integration suite (`session-list-watcher.integration.test.ts`) proving events actually fire end-to-end across dirs, including a dir created mid-watch — the test the mocked suite could never be.
   - **Same bug class fixed in `task-file-watcher.ts`** (`*/SKILL.md` glob → dir watch + basename filter): task SKILL.md hot-reload was equally dead.
2. **`listPendingInteractions` → `services/session/pending-interactions.ts`** (runtime-neutral): signature now takes the interactions MAP with a structural `PendingInteractionEntry` (`{type, startedAt, snapshot}`) — the projector's `toInteractiveSession()` fake-session adapter and its `as unknown as PendingInteraction` cast are DELETED (both the claude-code store's live entries and the projector's recovery records satisfy the entry shape directly). claude-code's `messaging/pending-interactions.ts` removed; store/projector/route-tests updated; selector test moved + simplified.
3. **`session-event-normalizer.ts` → `services/session/`**: it is runtime-neutral (StreamEvent → RawSessionEvent, both SDK-free shared types) and `triggerTurn` drives EVERY runtime through `feedProjector` — so the `FeedProjector` injection seam is gone entirely: `trigger-turn.ts` imports it directly, `TriggerTurnOpts.feedProjector` and the `FeedProjector` type are deleted, `createEmbeddedTurnTrigger(runtime)` drops its second param, the POST route and CopilotView drop their injections, and the claude-code barrel stops re-exporting it. Stale module-doc TODO ("task #6 will rewire the POST") and the wrong SDK-boundary rationale rewritten. `embedded-turn-trigger.test.ts` reworked to assert through the projector's replay buffer (behavior) instead of a recording-feed seam.
4. **SSE backpressure**: `event-fan-out.broadcast` had a literal no-op `once('drain', () => {})` (which also piled up a listener per congested write) → replaced with a buffered-bytes ceiling (`SSE.MAX_BUFFERED_BYTES`, 1 MiB): a client whose socket stays congested past it is destroyed (auto-reconnects + re-baselines) instead of buffering server memory without bound; under the ceiling a slow client keeps every frame. The durable `GET /:id/events` send loop now AWAITS `drain` (abort-signal-bounded `once`) when `res.write` returns false — gap-free (pauses, never skips) and a multi-thousand-frame replay can no longer balloon the response buffer.
5. **Flake note**: `sessions-interactive.test.ts` "cross-surface /deny → stale /approve" failed 2-of-3 FULL-suite runs (404 vs 200) while passing in isolation and in the final gate run; the test is fully mocked and the deny route untouched by this batch — filed as a suspected load flake alongside the known `sse-connection` one; re-check at next gate.

**REMAINING (task #6 batch B — client):** CLI-B5 third SSE connection (`event-stream-context.tsx` vs `streamManager.connectList`), ConnectionItem dishonest copy, B7 RTT double-submit, B9 switch-back spurious settle+sound, B10 dead `hasUnseenActivity` writer, list-store status re-baseline on `/api/events` reconnect, stale "discovery only removes DEFAULT_CWD" comment in `session-list-store.ts`; then #15-#18.

### Session 7 — quality pass, client batch (in-session task #6, batch B) — DONE

The client half of the task-#6 quality pass; with this, in-session task #6 is COMPLETE. Gated green (typecheck 21/21, lint 0 errors, server 195 files / 2,860 tests, client 369 files / 4,207 tests incl. extended suites, obsidian plugin builds). No flakes observed in this gate run (the two suspected load flakes from Session 6 did not reproduce).

1. **CLI-B5 — third `/api/events` connection eliminated.** `event-stream-context.tsx` owned its own module-singleton `SSEConnection` to a RELATIVE `/api/events` (also broken in packaged Electron/embedded), duplicating `streamManager.connectList()`. The StreamManager's global list stream is now the single unified connection:
   - `stream-manager.ts` gains `GENERIC_EVENTS` (connected, tunnel*status, extension_reloaded, relay*\*) registered on the list connection and dispatched to a new multi-subscriber `subscribeEvent(name, handler)` API (handlers looked up live, so subscribe-before-or-after-connect both work); list-connection state is now tracked and observable (`getListConnectionState` / `getListFailedAttempts` / `subscribeListConnectionState`); `getAttachedSessionId()` exposed; the list connection enables visibility optimization (the historical `/api/events` hidden-tab release), with `SSEConnectionLike.enableVisibilityOptimization?()` optional so pumps/fakes are exempt; `TransportListStreamPump` now reports honest connected/disconnected states.
   - `event-stream-context.tsx` REWRITTEN connection-free: same public API (`EventStreamProvider`/`useEventStream`/`useEventSubscription`/`KnownEvent` — now an alias of `GenericEventName`), but it drives `streamManager.connectList()` and mirrors the manager's list state; refetch-on-reconnect (invalidateQueries on reconnecting→connected) installed once at module level. Embedded mode: the transport source yields no generic events, so subscriptions are inert no-ops by design.
   - The Session-5 residual is documented at the source: HTTP `getSessionSnapshot`'s transient extra connection is now flagged "do not call over HTTP — the StreamManager cold connect already carries the snapshot" (`session-stream-methods.ts`).
2. **List-store status re-baseline on reconnect.** `session_status` is fan-out-only (no replay for late joiners — verified against `routes/events.ts`), so a status held across a disconnect can describe a turn that settled while the stream was down (server restart = stale 'streaming' border forever). The binding now subscribes to the list connection state and calls a new `resetStatuses()` (statuses + statusCwds only; metadata and unseen flags survive) on EVERY entry into `connected` — boot is a no-op, reconnects and hidden-tab re-opens self-heal.
3. **B7 — trigger RTT double-submit window closed.** POST is a 202 trigger, so the lifecycle reads `idle` for a full RTT + turn spin-up after Enter; a second Enter double-submitted instead of queueing. New `triggerPending` latch in the stream store: set by `executeSubmission` alongside the optimistic message (moved to the canonical id on rekey), cleared by `turn_start`/`turn_end`/POST-failure/15s watchdog (`TIMING.TRIGGER_PENDING_TIMEOUT_MS`, releases a 202-then-dropped turn); `selectRenderedStatus` reads it as `streaming`, so the composer's queue path engages from the moment of Enter.
4. **B9 — switch-back spurious settle + sound fixed at the mechanism.** The turn-end reconcile detected streaming→settled edges against a baseline that survived switches, so a cold snapshot reporting "idle" where the stale projection said "streaming" fired the settle effects (notification sound + redundant history reload) on every switch-back to a background-settled session. The stream store now increments `hydrationGeneration` per `applySnapshot`; the reconcile re-baselines (no fire) whenever the generation moves — snapshot-induced transitions are discoveries, live ones still settle (a RESUME replay carries no snapshot, so a replayed `turn_end` still reloads + sounds, which is correct because no fresh snapshot exists there).
5. **B10 — `hasUnseenActivity` wired for real (the writer was dead in production).** Decision: KEEP the feature — the list store prunes settled statuses, so without it every trace of finished background work vanishes at the moment it completes (the anti-Kai outcome). Moved to the LIST store (`unseen: Record<sessionId, cwd|null>`), fed by the binding on a background streaming/blocked→settled `session_status` edge (skipped for `streamManager.getAttachedSessionId()` — the operator is watching that one). Cleared when the session becomes active (mount + reactive subscription, so a settle frame racing the switch can't strand a flag), and on session removal. Border + agent-hottest hooks read the list store; the cwd value lets a COLLAPSED agent row light up unseen. `hasUnseenActivity` deleted from the legacy chat store; dev showcase rewired.
6. **ConnectionItem honest copy.** "messages you send and receive are unaffected" was false — the durable `/events` stream IS chat delivery. Labels and hover copy rewritten: reconnecting = "incoming messages and updates are paused — nothing is lost; anything missed replays when it reconnects" (true: snapshot+replay); disconnected = "new messages and updates will not appear until it's restored".
7. **Stale comment** in `session-list-store.ts` ("discovery only removes DEFAULT_CWD sessions") corrected — discovery is fleet-wide after batch A; the prune justification now cites `session_removed` firing only on transcript deletion.
8. **Tests**: stream-manager +6 (generic dispatch before/after connect, no list-event interference, unsubscribe, list state + failed attempts, visibility opt, attach lifecycle); event-stream-context REWRITTEN against the manager seam (12); binding +4 (background settle marks unseen with cwd, attached session exempt, no-edge no-mark, re-baseline clears statuses but not unseen); stream/list stores +7 (latch clear on turn_start/turn_end, hydrationGeneration, markUnseen/clearUnseen, removal cleanup, resetStatuses scope); submit suite +3 (status holds streaming through the trigger RTT and hands over to turn_start, latch release on POST failure, snapshot-discovered settle fires NO effects while a subsequent live settle still does); SessionRow/border-state/hottest suites migrated to the list-store unseen source.

**Deferred (explicitly):** `direct-transport.ts` split (now 809 lines; needs its own focused refactor — flagged for the task-#18 cleanup pass).

**REMAINING (in-session task #7):** spec tasks #15–#18 — stateless stub runtime; browser acceptance re-run (`/chat:session-switch-test`); cross-client tests; docs/ADRs + consolidated dead-code retirement (incl. the direct-transport split).

### Session 8 — stateless stub runtime (spec task #15 / in-session task #7.1) — DONE

The runtime-agnosticism proof (ADR-0263 Decision 1, "own the boundary, not the bytes"): `TestModeRuntime` is now a STATELESS, DorkOS-EventLog-backed contract adapter holding no native transcript. Gated green (typecheck 21/21, lint 0 errors, server 197 files / 2,876 tests incl. 2 new suites, client 369 / 4,207 unchanged); code-reviewer dispatched, 1 important + 4 minor findings all fixed pre-commit.

1. **Contract gap closed at the schema: `turn_start` carries `userMessage` (optional).** The EventLog never recorded the user's prompt — no `SessionEvent` member carried it — so a log-backed snapshot would reconstruct answers with no questions. The POST is trigger-only (ADR-0264), so the durable stream is the only delivery path and therefore the only place the trigger content can ride for log-only persistence. `feedProjector` gains `opts.userMessage` stamped onto the synthesized `turn_start`; `triggerTurn` passes `content`. Additive for Claude (clients ignore it; JSONL stays their history source; nothing validates `turn_start` strictly — verified).
2. **`services/session/event-log-history.ts` (NEW, runtime-neutral):** `reconstructHistoryFromEvents(events)` folds completed turns into `HistoryMessage[]` — user message from `turn_start.userMessage` (including the OPEN turn, matching Claude where the user message is on disk at turn start), assistant message per `turn_end`-closed turn (text_delta accumulation; tool_call/tool_result merged by toolCallId; tool_progress appended), deterministic ids `user-<seq>`/`assistant-<seq>`. Documented simplifications: no `parts`/thinking, interleaving collapsed; depth bounded by `EVENT_LOG_MAX_EVENTS` (that IS the log-backed retention policy).
3. **`test-mode/session-registry.ts` (NEW):** in-memory tracked-session metadata (id/title-from-first-message/preview/permissionMode/model/cwd) + `session_upserted` fan-out. `subscribe()` is a hand-rolled iterator (queue/waiter/closed; single-consumer documented) whose `return()` resolves a parked `next()` — the broadcaster awaits `iterator.return()` on stop and an async generator would deadlock it. NO filesystem watch anywhere.
4. **`TestModeRuntime` contract methods real:** `getSessionSnapshot` = `projector.buildSnapshot(EventLog-reconstruction loader)`; `subscribeSession` = `projector.subscribe(sinceCursor, signal)` (alias resolution deliberately omitted — `getInternalSessionId` returns undefined so a rekey can never fire; verified against `triggerTurn.tryRekey`); `subscribeSessionList` = registry stream; `getMessageHistory` reconstructs via `peekProjector` (peek, not create — an unknown id must not mint registry garbage); `listSessions`/`getSession`/`updateSession` serve the tracked set; `sendMessage` records the session (the discovery source).
5. **Review fix (important): `/api/test/reset` was a half-reset** — metadata cleared but projectors (the runtime's ONLY persistence) survived, resurrecting pre-reset history for a reused id and accumulating forever (counter>0 blocks self-dispose). `resetTrackedSessions()` now disposes every tracked id's projector; `registry.reset()` emits `session_removed` to live subscribers. The route resolves TestModeRuntime via DYNAMIC import (app.ts imports the router statically; a static class import would defeat the index.ts `DORKOS_TEST_RUNTIME` gating) inside try/catch (async Express-4 handler would otherwise hang on rejection). Minors fixed: post-`return()` `next()` reports done (queue cleared on close), codepoint-safe preview truncation, fidelity-simplification docs.
6. **Tests:** `event-log-history.test.ts` (6 — folding rules incl. open-turn, trimmed head, non-message events); `test-mode-runtime.test.ts` (10, "Decision 1 / runtime-agnosticism end-to-end" — snapshot reconstruction, history parity, gap replay + live continuation, subscribe-first hydration, eager `StaleResumeCursorError`, list inventory + live upsert + clean return-while-parked, cwd-scoped listing, reset disposes projectors + emits removals); `sessions-multi-runtime.test.ts` trigger test UPGRADED from projector-poking to true end-to-end (POST 202 → cold `GET /:id/events` snapshot with EventLog-reconstructed messages → `?after=` resume replaying the gap with no snapshot frame) through the REAL handler/registry/runtime — the "no runtime branching" acceptance criterion.
7. **Gate note:** one full-suite run showed 4 failures in 1 server file that vanished in two consecutive immediate re-runs (file name not captured — output filter ate it); affected suites for this change ran green 3×. Watch at the next gate alongside the two known load flakes (neither reproduced).

**REMAINING (in-session task #7):** spec tasks #16 (browser acceptance re-run — add a reset-reuses-session-id e2e case per review recommendation), #17 (cross-client tests), #18 (docs/ADRs + consolidated dead-code retirement incl. the direct-transport split).

## Session 9 — browser acceptance re-run + F2 regression fix + e2e reset pin (spec task #16 / in-session task #7.2) — DONE

Report: `test-results/session-switch-test/20260611-145454.md` (perm:default, Playwright MCP, two concurrent sessions, vs prior `20260610-173202.md`).

1. **Prior findings re-tested**: F1 PASS, F3 PASS (amber/blue/idle dots + "Awaiting approval" icon live), F4 PASS (draft survives card swap), F5 PASS (no ghost; auto-deny lifecycle clean; mid-turn steer left an unrelated pending approval intact). **F2 FAILED on entry with a NEW root cause** and was fixed mid-run:
2. **NF-1 / F2 fix (`trigger-turn.ts`)**: the batch-A per-event rekey retry disarmed on ANY truthy `getInternalSessionId` — but `ensureSession` SEEDS an identity mapping (`sdkSessionId === sessionId`, session-store.ts:92-104) before the SDK init assigns the real id, so the retry latched the identity on event #1 and never rekeyed. Now identity does not disarm (`if (!canonical || canonical === sessionId) return`). Regression test in `sessions-trigger.test.ts` models identity→real-id. Verified live post-fix: both id namespaces converge mid-first-turn; sidebar SDK-id navigation lands on the live blocked state.
3. **Spot-checks**: B7 PASS (second send queues, no 409), B9 PASS (window-marker + Audio spy: no reload/sound on switch-back), B10 PASS (blue unseen dot on background settle, clears on view), multi-turn send PASS, server restart clears stale liveness + dead cards on reconnect PASS. Cross-client: a foreign-client POST mid-turn returns 202 and steers (no 409).
4. **New findings**: NF-2 queued message LOST across id-view switch/refresh (matrix check #4 FAIL — queue is in-memory + bucketed by queue-time id; candidates: alias-keyed bucket or DOR-82 server-owned queue); NF-3 duplicate rail row post-rekey (client-id placeholder lingers next to the SDK-id row).
5. **e2e reset pin (review recommendation)**: `apps/e2e/tests/chat-mock.spec.ts` — session id reused across `POST /api/test/reset` gets a fresh session (also pins turn_start.userMessage reconstruction over real HTTP). Greening the suite surfaced + fixed: NF-4 `resolveRuntimeTypeForNewSession` honors manifest runtime only when registered (+unit test); NF-5 `getSessionRuntimeType` row-less inference falls back to default type when claude-code unregistered (was 503ing every pre-first-message read on the test-mode server — the silent killer of the whole mock e2e suite); NF-6 composer `aria-label` (accessible name lost under AnimatedPlaceholder); NF-7 four async handlers now forward `resolveForSession` throws to the error middleware instead of hanging (Express 4); chat-mock.spec.ts opts into sequential mode (shared-server reset race under fullyParallel). Suite 4/4 (was 2/4 broken, masked — e2e is not in the `pnpm test` gate).
6. **Gate**: typecheck 21/21, lint 0 errors, server 197 files / 2,878 + 1 skipped, client 369 / 4,207. No flakes reproduced.
7. **Surfaces**: web client fully exercised live; Obsidian/desktop remain build-verified only.

REMAINING (task #7): #17 cross-client tests, #18 docs/ADRs + consolidated dead-code retirement.

**Session 9 review (code-reviewer agent): ready to merge.** Mutation-tested the F2 regression test (fails against the old disarm logic); confirmed production behavior preserved for both fallbacks (claude-code always registered there; defaultType initializes to 'claude-code'); verified the SSE catch precedes header flush. Post-review fixes applied: log line when a manifest runtime preference is discarded; stale TSDoc on getSessionRuntimeType/resolveForSession corrected; explicit `let` annotations; e2e post-reset read asserts `res.ok()` (the lenient `[]` fallback stays only inside the polls). Deferred to task #18: a shared `asyncHandler` wrapper to close the Express-4 async-rejection class structurally (~14 remaining `resolveForSession` sites; this batch fixed the four hit live). Gate re-run green after fixes.

## Session 10 — cross-client tests + NF-2/NF-3 fixes (spec task #17 / in-session task #7.3) — DONE

Closes the acceptance run's one matrix FAIL (NF-2 queued-message loss) and its minor duplicate-row finding (NF-3), and pins the multi-client behaviors that were verified only by hand. Gated green (typecheck 21/21, lint 0 errors, server 198 files / 2,881 + 1 skipped incl. the new cross-client suite, client 370 / 4,224); code-reviewer dispatched — no blockers, 3 minors + 1 nit ALL fixed pre-commit.

1. **NF-2 root cause confirmed**: the 202's canonical id is best-effort — in the common Claude path the SDK id resolves only when the init message lands, AFTER the POST returned the request UUID, so the client's 202-path rekey (queue move, URL replace, row cleanup) never fired. The retire announce (`session_status.retiredSessionId`, emitted by `rekeyProjector` the moment the id resolves) was reaching the client and being used ONLY to prune liveness/unseen state.
2. **NF-2 fix — follow the rekey at both observation points.** `session-stream-store.ts`: `moveQueue` replaced by `migrateSessionContinuity(from, to)` — moves the compose-next queue (append), the optimistic user message (target wins), and the trigger latch (OR); idempotent (second observer finds an empty source); the source PROJECTION is left intact so a still-open retired-id view keeps rendering. Called from the 202 path (`use-session-submit`, refactored onto it) AND from the binding on the retire announce. `session-list-store.ts` records `rekeys: Record<retiredId, canonicalId>` (+ drops any metadata row under the retired id); new `useSessionRekeyTarget` selector. New `useSessionRekeyRedirect` (lives in `use-session-stream.ts` as its id-side companion — a separate file would breach the chat/model 25-file directory cap enforced by the pre-commit dir-size hook), wired into `useChatSession`: when the ACTIVE session id is retired, fires the same in-place `onSessionIdChangeReplace` the 202 path uses — the URL rewrite is now uniform regardless of WHEN the id resolves. Review fix: the 15s trigger watchdog now resolves `rekeys[latchedId]` before checking/clearing, so a latch migrated to the canonical id can't escape it (composer-wedge regression test with fake timers). HONEST LIMIT: the queue is still in-memory — a hard refresh loses it; that half is DOR-82 (server-owned queue) territory, noted, not silently claimed fixed.
3. **NF-3 fix — placeholder row resolved in place.** `use-global-session-stream.ts`: new `reconcileRetiredSessions` sweeps newly-retired ids across EVERY `['sessions', *]` cache — RE-IDing the placeholder to the canonical id when the canonical row hasn't landed (mirrors the 202 path's optimistic re-insert; the actively-driven session never vanishes from the rail — review fix, was drop-only) and dropping it as a duplicate once the watcher's upsert exists. Subscription extended to watch the `rekeys` map alongside `sessions`.
4. **New server suite `sessions-cross-client.test.ts`** (FakeAgentRuntime + REAL projector registry + real routes; `attachEventStream` handle exported from trigger-turn-helpers): (a) two consumers — live-from-start and cold mid-turn — converge: B's snapshot carries the in-progress prefix, live frames are EXACTLY consecutive from `cursor+1` (review fix: consecutive-seq assertion, not just monotonic — a silently dropped non-text event now fails the pin), both end on the same final cursor; (b) an approval resolved from a second `X-Client-Id` emits `interaction_resolved` on EVERY consumer's stream (approve seam wired to the real `projector.resolveInteraction`, the same call the Claude adapter makes); (c) second-client mid-turn POST → 409 `SESSION_LOCKED` naming the holder against a REAL `SessionLockManager`, then 202 accepted after forcing the lock past `LOCK_TTL_MS`.
5. **The acceptance run's "mid-turn 202 steer" explained**: it was lock-TTL expiry — `LOCK_TTL_MS` is 5 minutes and A2's tool-heavy turn had outlived it; the second POST acquired the expired lock and the Claude CLI delivered the resume-during-active-turn as a steer. The route-level takeover contract is pinned (4c); the steer delivery itself is Claude-CLI behavior, documented as not pinnable server-side. DOR-82 will replace this incidental semantics with explicit queue/steer/interrupt dispositions.
6. **Coverage already in place (verified, not duplicated)**: Last-Event-ID/?after resume across a drop incl. epoch mismatch + stale-cursor fallback (`sessions-events.test.ts`), fresh-lock foreign 409 (`sessions-trigger.test.ts`), cross-surface single-resolve guards (`sessions-interactive.test.ts`).
7. **Tests net**: stream/list store +8 (migration semantics ×6, retire records rekey, retired metadata row dropped), binding +1 (retire announce migrates + records), global-stream +4 (re-id sweep, duplicate drop, new-only processing, live announce re-ids placeholder), rekey-redirect +4 (live announce, retired-at-mount, unrelated/null no-op, settles after replace), submit +1 (watchdog follows rekey), server +3 (the cross-client pins).

REMAINING (task #7): #18 — docs/ADRs 0263–0267 + consolidated dead-code retirement (legacy stream-event-handler branches, watchSession/session-broadcaster removal, subagent_text_delta/permission_denied contract decision, dead isRemapping/renameSession + redundant /pending-interactions pull, direct-transport split, shared asyncHandler wrapper, docs/AGENTS.md POST-streaming references).

## Session 11 — docs/ADRs + consolidated dead-code retirement (spec task #18 / in-session task #7.4) — DONE — SPEC COMPLETE

**Gate:** typecheck 21/21 clean; lint 0 errors (3 batch-adjacent warnings fixed; pre-existing warnings untouched); full `pnpm test -- --run` green except two load flakes that pass in isolation (`extensions.test.ts` supertest socket hang-up; `session-list-watcher.integration.test.ts` real-chokidar timeout — new entries in the known-flake list); e2e chat-mock 3/4 — see point 8. Parts 4–6 executed by three parallel subagents on disjoint file sets; all output reviewed.

1. **Legacy recovery path retired (the whole chain).** Verified first: the `/events` snapshot's `pendingInteractions` is projected by `project-session-turn.ts` into the SAME pending part shapes the live events produce, and `replayInteractionEvent` (Path B's entry point) had zero consumers — so the pull was unreachable-in-effect. Deleted: `GET /:id/pending-interactions` route + OpenAPI registration, `Transport.getPendingInteractions` + Http/Direct implementations, `AgentRuntime.getPendingInteractions` + claude-code/test-mode/Fake implementations + the session-store wrapper, `use-pending-interactions.ts`, `stream-event-handler.ts` + `stream-tool-handlers.ts` + `stream-event-types.ts` (the projection only ever used `deriveFromParts`; `stream-event-helpers.ts` shrunk to it), `lib/pending-interaction-events.ts` (Path B's mapper — zero production callers since the `/stream` route died), `PendingInteractionsResponseSchema`, `QUERY_TIMING.PENDING_INTERACTIONS_STALE_TIME_MS`, and 13 test files that tested the deleted code (projection equivalents verified present: memory_recall pin in `project-session-turn.test.ts`, DOM half in `MemoryRecallBlock.test.tsx`, route describe blocks excised from `sessions-interactive.test.ts`). The `_partId` text-key in `AssistantMessageContent` lost its only producer with the handler — simplified to the index key.
2. **`watchSession` + `SessionBroadcaster` removed.** The broadcaster's two consumer paths (SSE `registerClient`, callback `registerCallback` via `runtime.watchSession`) both died with `GET /:id/stream`; chokidar JSONL watching for the PER-SESSION stream went with it (`watchSessionList`/`session-list-watcher` — the global list stream — is untouched and live). Interface member removed from `AgentRuntime` + all three implementations; test-mode's relay-console forwarding inside watchSession was already dead (nothing called it); `index.ts` shutdown + setRelay comments updated. The now-producer-less `sync_update`/`sync_connected`/`presence_update` StreamEvent members + payload schemas removed from `@dorkos/shared`.
3. **Shared `asyncHandler`** (`apps/server/src/lib/async-handler.ts`): generic over the Express handler type params (P/ResBody/ReqBody/ReqQuery defaulted to Express's own) so route-param inference survives the wrap — necessary because `@types/express@5` types `ParamsDictionary` values as `string | string[]` (runtime is Express 4). Applied to all 17 session routes + `models.ts` + `subagents.ts`; pure-forwarding try/catch removed (specific mappings kept: tasks→404, fork→500, reload→500, stop→500, interrupt→500); `commands.ts` left (fully covered); `relay.ts`'s resolveForSession already double-guarded; the SSE `/:id/events` handler keeps its own catch-before-flush. `parseSessionId` now takes `unknown` (it's a validator). Regression tests: a `RuntimeNotRegisteredError` rejection → 503 `RUNTIME_NOT_AVAILABLE`, a generic rejection on `/approve` (which never had try/catch) → 500 `INTERNAL_ERROR` — both previously HUNG.
4. **`direct-transport.ts` split** (subagent): 802 → 64-line composing class + six modules under `shared/lib/direct/` (`services` 103, `session-methods` 275, `session-stream-methods` 72, `system-methods` 275, `mesh-methods` 72, `stub-methods` 34), mirroring the http `transport/` factory convention (Object.assign + declaration merging). Public surface byte-compatible; obsidian-plugin tsc verified; direct-transport-streams 9/9.
5. **ADRs 0262–0267 finalized** (subagent; manifest statuses draft→proposed, the implemented-awaiting-`/adr:review` state): 0263 records the 16-member contract incl. the projector-assigned seq, the fidelity tier, `turn_start.userMessage`, `interaction_resolved` (incl. `cancelled`) — and the EXPLICIT exclusion decision for `subagent_text_delta` + `permission_denied` (normalizer default-drops them; lifecycle rides `subagent_update`; denials ride `interaction_resolved {denied}` live — and the agent caught that the `PermissionDeniedPart`/chip has NO producer in the new pipeline, recorded honestly as a future-revision candidate). 0264: trigger-only 202 + best-effort canonical id + `<sid>-<epoch>-<seq>` frame ids + StaleResumeCursorError cold-path. 0265: the implemented event set (the draft's `agent_status`/`presence` never shipped — client-side cwd aggregation documented as the decision) + `retiredSessionId`. 0266: flag removal + the consequence that the pull endpoint became deletable. 0267: BOTH rekey observation points + `migrateSessionContinuity` + the DOR-82 in-memory-queue limit. 0262: historical record kept, dated amendment marks both paths removed and enumerates what survives (selector, countdown fields, restart boundary).
6. **Docs swept** (subagent): AGENTS.md Sessions section, `docs/integrations/sse-protocol.mdx` (full rewrite), `docs/concepts/{sessions,transport,architecture}.mdx`, `docs/contributing/architecture.mdx`, `docs/integrations/building-integrations.mdx`, `contributing/{api-reference,architecture,data-fetching,interactive-tools,state-management,configuration,project-structure,INDEX}.md`, `.claude/commands/chat/session-switch-test.md` technical notes; OpenAPI docs REGENERATED offline (`pnpm docs:export-api` + fumadocs) — `pending-interactions/get.mdx` gone, `events/get.mdx` created. Deliberately left: `docs/changelog.mdx` (history), `chat/self-test.md` + two `.claude/rules/` filename mentions (out of scope — follow-ups), and `contributing/architecture.md`'s `ui_command` claim (the emission exists in `ui-tools.ts` but `ui_command` is NOT in the SessionEvent union — same dropped-by-normalizer class as the 0263 exclusions; unresolved in code, flagged).
7. **Task-#11 claim corrected** in this file (Session “Phase 4” notes): `/api/events` sends only `connected` + heartbeats on connect — there is NO connect-time `session_upserted` inventory (it fires once at broadcaster start, to zero clients); hard-refresh hydration comes from the cold `GET /api/sessions` query, the stream is the live overlay.
8. **e2e chat-mock 3/4 — pre-existing, NOT this batch.** `renders tool call card for tool-call scenario` fails when run after the simple-text test (passes alone). Verified server-correct end-to-end by hand (snapshot AND `GET /:id/messages` both carry the completed `toolCalls`), then verified the failure reproduces IDENTICALLY at HEAD `2f976a4c` with this batch stashed — pre-existing on the branch, likely the `/events` attach racing the test-mode instant turn so the card must render from completed-history messages. Earlier 4/4 results were taken on reused warm servers (`reuseExistingServer: !CI` — kill stale 4243/4248 listeners before trusting e2e results). FOLLOW-UP: fix the snapshot-completed-message tool-card render (or the test's attach race) in its own change.

**Spec task #18 COMPLETE → in-session task #7 COMPLETE → all 19 spec tasks done.** Honesty carry-forwards: Obsidian/desktop surfaces remain build-verified only (no live pass this spec); compose-next queue is in-memory (DOR-82); `subagent_text_delta`/`permission_denied`/`ui_command` are documented contract exclusions with no live delivery path.

**Session 11 review (code-reviewer agent): ready to merge, no criticals.** Independently verified every deletion reference-free, the asyncHandler conversion mapping-for-mapping behavior-preserving, the direct/ split byte-compatible (obsidian tsc clean), and the ADR claims accurate against code (EVENT_LOG_MAX_EVENTS/RING_BUFFER/CANONICAL_ID_TIMEOUT/16-member union all spot-checked). All three findings fixed in-batch: (1) `GET /:id/events` was the last unguarded async session handler — now wrapped in `asyncHandler` (pre-flush escapes get JSON errors; post-flush Express destroys the socket, the correct SSE failure mode; handler retyped from `RequestHandler` to an explicit async signature, pre-flush comment corrected); (2) the three `_partId` key-stability tests were vacuous post-simplification (the component ignores `_partId`; index keys can't collide in one render) — replaced with a real interleaved text/tool_call/text render assertion; (3) `DirectTransportServices.runtime` still required `ensureSession`/`sendMessage` with zero call sites (sends ride `turnTrigger` per ADR-0264) — dropped, seam comment added. Reviewer recommendation noted: file the follow-up to decide add-or-delete for the producer-less `PermissionDeniedPart`/chip + `ui_command` (recorded in ADR-0263; candidates for a contract revision). Gate re-run after fixes: route suites 46 files/693 tests green, typecheck 21/21, lint 0 errors.
