# DorkOS Connections implementation record

**Spec:** `specs/white-label-connections/02-specification.md`  
**Umbrella:** DOR-1792  
**Project:** DorkOS Connections  
**Branch:** programme record on `codex/connections-program`; P2 implementation on `codex/connections-execution`
**Status:** In progress — P1 is merged; P2 is converging through independent review
**Updated:** 2026-09-06

## Prerequisites and related work

| Item    | Role                                                                              | State                                                                                                                                 |
| ------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| DOR-738 | Independent prerequisite: raw MCP must verify authenticated initialization        | Merged in [PR #1605](https://github.com/dork-labs/dorkos/pull/1605) at `8913d60f1f0e3d317c91f11b77ba00172a344e63`; not counted in 0/7 |
| DOR-740 | Existing issue reused and respecified for workstream 6 agent request/grant/resume | Planned                                                                                                                               |

## Initial worker roster

| Role                                                 | Owner                                | Scope                                                                    |
| ---------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------ |
| Programme orchestration and architecture audit       | Root Codex agent                     | Integration boundaries, tracker projection, worker dispatch, final gates |
| Ideation, specification, decomposition, proposed ADR | `spec_author`                        | Frozen artifacts through independent review                              |
| Workstreams 1–7                                      | Unassigned; tracker issues projected | One isolated worktree/writer per implementation branch                   |
| Adversarial review                                   | Separate reviewer per branch         | Independent review before PR creation                                    |

## Workstream status

| ID  | Workstream                          | Tracker                                                                                                                        | State       | Evidence                |
| --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------- | ----------------------- |
| 1.1 | Contracts and migration             | [DOR-1793](https://linear.app/dorkspace/issue/DOR-1793/white-label-connections-p1-stable-provider-instance-connection-grant)   | Merged      | See P1 checkpoint below |
| 2.1 | Authorization, execution, and usage | [DOR-1794](https://linear.app/dorkspace/issue/DOR-1794/white-label-connections-p2-enforced-connector-execution-across-dorkos)  | In progress | See P2 session below    |
| 3.1 | Complete local experience           | [DOR-1795](https://linear.app/dorkspace/issue/DOR-1795/white-label-connections-p3-complete-connections-management-access)      | Pending     | —                       |
| 4.1 | Managed tenant service              | [DOR-1796](https://linear.app/dorkspace/issue/DOR-1796/white-label-connections-p4-tenant-scoped-managed-connector-service-in)  | Pending     | —                       |
| 5.1 | Managed and BYO events              | [DOR-1797](https://linear.app/dorkspace/issue/DOR-1797/white-label-connections-p5-signed-managed-and-byo-event-ingress-with)   | Pending     | —                       |
| 6.1 | Agent request, grant, and resume    | [DOR-740](https://linear.app/dorkspace/issue/DOR-740/white-label-connections-p6-respec-dor-740-for-private-agent-requests)     | Pending     | —                       |
| 7.1 | Rollout and production evidence     | [DOR-1798](https://linear.app/dorkspace/issue/DOR-1798/white-label-connections-p7-remove-legacy-paths-verify-all-surfaces-and) | Pending     | —                       |

Root takes ownership of this record after the specification and task decomposition freeze. Implementation PRs must add verification evidence here without marking managed production capabilities available before the separate real provisioning smoke passes.

The specification incorporated both independent review rounds before implementation dispatch; PR #1599 merged the reviewed artifacts on 2026-09-05.

## P1 implementation checkpoint

The foundation implementation started from `origin/main` commit `d432fb71b1351c763b6893bc58a08b48a0b848de` plus the intent seed `f566e2234`. It incorporated the independently reviewed specification tree `c814e43982e50d5a1c5bbebbf6ca7e6690036850`, later merged on `origin/main` as `ff0bf1c272870b23de10982df79d8cc0e3a20ac8`.

P1 adds shared stable IDs and strict review-action schemas, an instance-bound provider contract, and the provider-neutral SQLite foundation in numbered migration `0086_perfect_whiplash`. The database now separates provider instances, stable DorkOS connection IDs, private provider references, provider authentication state, local pause, and a durable disconnect tombstone. Immutable operation revisions include their reviewed classification, and grants and usage point to the exact revision. Event subscriptions, a restart-safe protected inbox, payload-free receipts, review requests, and private agent-resume rows are ready for later workstreams.

The application backfill performs no provider or network calls while SQLite holds its transaction. It seeds configured instances and unavailable legacy defaults, maps every legacy account to a new stable ID, preserves both attachment levels, gives session overrides an owner only from the exact current session-to-agent relationship, and creates no grant when operation discovery is incomplete or ownership is unresolved. Failure rolls back the new rows and ledger claim, preserves the legacy rows, returns a scrubbed `migration_failed` health response, and blocks connector reads and writes while the rest of the server remains bootable.

Live P1 consumers now use the canonical connection and attachment stores. Compatibility provider MCP methods remain because P2 owns the execution-boundary cutover. Connect-flow routing binds an opaque public flow to the exact provider instance and private flow, coalesces and pins active polls, retains a bounded schema-validated public terminal replay, and invalidates successful replay when the connection is disconnected. A disconnect during an in-flight explicit reconnect cancels the exact private provider flow through the stored tombstone binding and cannot revive the stable connection; an ambiguous disconnected target remains denied. Provider removal can target one exact instance and leaves same-type siblings routable.

New sessions send the exact selected registered-agent path on their first turn only after the client lookup completes. The server verifies that path against Mesh before its existing first-write session binding accepts it. An ordinary directory remains a valid unowned session, while connector attachment returns a stable, actionable conflict until the operator chooses a registered agent. Migrated unresolved overrides remain denied and cannot acquire ownership from that live path.

Focused Node 24.14.1 verification covers the real pre-0086 fixture, rollback and second boot, stable public DTOs, public error scrubbing, same-type provider instances, exact-provider flow routing after registry replacement, deferred inventory and reconnect polling across disconnect, agent/session cleanup, first-turn ownership, operation-revision immutability, strict review payloads, provider conformance, and durable inbox claim/retry/expiry behavior. The latest combined run of every changed test plus the hermetic connector evals passed 461 tests across 28 files. A deliberate mutation that returned the caught migration error failed the public-route regression with the sentinel private provider reference, and the restored scrubbed boundary passed. Shared, database, test-utility, server, client, and eval-suite typechecks pass; their package lints pass with pre-existing warnings only. The connector eval fixtures use stable IDs, canonical private bindings, and an explicit session owner; no live provider was called.

The first aggregate verification attempt exposed the connector eval suite as a missed stable-ID consumer; the hermetic Gmail and Slack fixtures then passed 7 tests after adopting canonical private bindings. The next attempt stopped at the generated documentation coverage map after the connector-author guide changed; regeneration restored its exact check. The third attempt reached the database package and exposed the authoritative expected-table inventory, with 160 of 161 database tests passing; after adding all 13 migration 0086 tables, the focused migration suite passed 23 of 23. These were fixture and generated-metadata corrections, and each stopped gate was kept red until its real consumer was updated.

The next aggregate run reached every affected test package: 29 of 31 Turbo tasks passed with no cached test task, while the server reported 22 failures across 12 files and stopped the client task. The preserved server log is `.dork/flow/evidence/verify-oversubscribed-server-test.normalized.log`. Two failures exposed real ownership-fixture gaps: cwd routing and multi-runtime provenance passed an unregistered `agentPath`, and connector capability conformance used neither a session owner nor a canonical connection. Those fixtures now traverse the production validation and stable foreign-key path, passing 19, 38, and 71 tests respectively. The remaining reported timeout, connection-reset, and room-route cases pass in isolation; this is isolation evidence only, and the serialized aggregate gate remains required before review.

The credential-free Connections browser path passed two tests in Chromium: a first message persisted the selected registered agent, session attachment returned 200 and rendered tools on, and detach returned 204. It ran with:

```bash
DORKOS_COCKPIT_VITE_PORT=4679 DORKOS_MOCK_PORT=4379 DORKOS_MOCK_VITE_PORT=4529 pnpm --use-node-version=24.14.1 --filter @dorkos/e2e exec playwright test tests/connections/connections.spec.ts --project=chromium-connections --workers=1
```

The AccountRow dev showcase was inspected in the real browser with its accessibility tree and a dark-mode screenshot at `.dork/flow/evidence/p1-accountrow-paused.png`; the operator-paused account visibly reads `paused` while retaining its account name, custody sentence, and disconnect action. Independent Stage 1 specification compliance and Stage 2 adversarial review remain before a PR opens.

The dependency integration checkpoint incorporates the exact independently reviewed DOR-738 source tree at `706ae0fd1c3d2c106fe966e581b92b68ad097fe0` and the serialized verification scheduling source at `d02f9c525408293f0993f9b64d28d4cdf544c0b2`. Raw MCP connections now complete an authenticated protocol probe before they are recorded, verification-only starts need no authorization URL, and late or repeated polls preserve the opaque public flow ID, exact provider instance, private provider flow, and stable reconnect target. Client generations prevent a closed dialog from being reopened by a late start result. The P1 lifecycle boundary refuses paused or disconnected connections before resolving a provider tool endpoint; a focused public-session regression proves those local states make zero endpoint calls while DELETE alone can read the private tombstone owner.

After resolving the dependency contracts against stable P1 IDs, 155 focused connector, route, session exposure, provider conformance, and client tests passed across 10 files. The server, client, and test-utility package typechecks passed, their lints reported no errors, and the intact checkpoint hook passed all 32 typecheck tasks and 21 lint tasks. Both API generators completed under Node 24.14.1; the exported OpenAPI schema now records the durable `paused` state in public account and session-exposure responses.

The first serialized aggregate verification reached 27 of 32 Turbo tasks before two server HTTP requests failed: one mock OAuth registration returned a one-off 404 and one unauthorized room read lost its socket. Neither application assertion failed. Both files then passed together and across ten fresh-process repetitions, for 320 passing focused assertions. The unchanged aggregate rerun passed all 32 Turbo tasks in 2 minutes 34 seconds, with five tasks executed and 27 exact-tree cache hits; both earlier files passed in that run. The full pipefail logs are `.dork/flow/evidence/p1-integrated-verify.log` and `.dork/flow/evidence/p1-integrated-verify-rerun.log`.

After replaying the foundation onto merged dependency commit `8913d60f1f0e3d317c91f11b77ba00172a344e63`, the final Node 24.14.1 aggregate verification passed all 32 Turbo tasks with zero cached tasks in 6 minutes 51 seconds. Its full pipefail log is `.dork/flow/evidence/p1-curated-verify.log`.

The first independent specification review reproduced three boundary gaps. Startup reconciliation could remove an expired agent before connector cleanup was registered, an idempotent session detach returned 500 for an unknown connection, and shared conformance never called `execute` when a provider declared execution unsupported. The correction registers cleanup before reconciliation, preserves ownership checks while making an unknown connection a 204 no-op, and requires each unsupported provider method to return its declared typed envelope without private references or URLs.

Agent removal now writes a durable, non-FK legacy-consent marker before checking application-migration health. The cleanup callback is registered before startup reconciliation; there is no broad orphan sweep when Mesh is disabled, unavailable, or still within its unreachable-agent grace period. The 0086 backfill copies agent consent only for a currently registered, unmarked agent and applies the same condition to migrated session ownership. A real failed-migration restart fixture re-registers the same agent ID and proves retained legacy consent does not return as canonical authority, while a fresh explicit canonical attachment still succeeds. Removing that marker predicate made the restart test fail with the old attachment restored; the unmodified focused set passed 205 tests across 11 files.

The first corrected aggregate reached 25 of 27 Turbo tasks before three server tests failed. Two public migration-failure fixtures used consent from an unregistered agent; the new backfill correctly skipped those rows, so they no longer exercised their intended malformed-account boundary. Registering the fixture agent restored the real failure and scrubbed 503 assertions. The third failure was an HTTP parse error in the unrelated room-holds suite. The three files then passed together with 28 tests, and the server typecheck and lint passed. The full aggregate failure is preserved at `.dork/flow/evidence/p1-stage1-correction-verify.log`; the focused follow-up is `.dork/flow/evidence/p1-stage1-aggregate-failures-focused.log`.

The corrected aggregate rerun passed both migration-failure fixtures and 16,467 of 16,468 server assertions, then stopped on a socket hang-up before an unrelated Rooms request reached the application logger. The exact Rooms file passed all 8 tests in the single authorized isolation run. No retry, timeout increase, or Rooms code change followed. The full rerun, failure block, and isolation logs are `.dork/flow/evidence/p1-stage1-correction-verify-rerun.log`, `.dork/flow/evidence/p1-stage1-rooms-failure-block.log`, and `.dork/flow/evidence/p1-stage1-rooms-focused-once.log`. A narrow independent re-review remains required.

The narrow correction review found that the failed-migration restart fixture proved the attachment marker but did not distinguish the matching session-owner predicate. The fixture now carries legacy session metadata and an attached override through same-ID re-registration. It proves that the canonical override remains unowned and needs reconciliation, with no session grant. Removing only the removal-marker condition from session-owner resolution makes that assertion fail; restoring the predicate passes both focused suites with 6 tests.

P1 then incorporated the exact independently reviewed DOR-1804 candidate at `c587973184c5d747a639fb899803f99aca580f24`, which merged on `origin/main` as `60e0940d1bce609d0c6272eccd74b809fe1a5efc`. The shared HTTP test facade and stable listening targets replace direct Supertest use while preserving every P1 boundary assertion and each original fresh-application construction. On the composed source tree, the Node 24.14.1 aggregate verification passed all 32 Turbo tasks in 6 minutes 5 seconds, with 16 tasks executed and 16 exact-tree cache hits. Its full pipefail log is `.dork/flow/p1-dor1804-verify.log`.

The Stage 2 adversarial review reproduced two access-boundary gaps. Removing one stable connection from an agent left that agent's exact grants, subscriptions, owned-session overrides, and hydrated tool endpoint live. The correction performs one exact agent-and-connection transaction, invalidates the affected live session exposure before returning, and rechecks durable consent after an awaited provider endpoint lookup so a concurrent removal cannot republish stale access. Focused route tests preserve another agent's access to the same connection and the selected agent's access to another connection. Deliberately removing the cache invalidation or the post-await consent fence makes those regressions fail.

The same review found that a failed or still-pending registered-agent lookup could let a first message proceed without the immutable agent provenance. The submission boundary now keeps new-session sends closed for both states while preserving successful unowned-directory sessions and existing-session replies. Programmatic launch auto-send stays armed until the lookup resolves, then submits and consumes the launch prompt exactly once. Removing either the underlying pending guard or the launch latch deferral makes the real ChatPanel regressions fail. The final focused client set passed 27 tests, and the exact correction patch received an early independent code pass with no open findings.

The final composition uses the actual merged DOR-1804 base, retains P1 migration `0086_perfect_whiplash` immediately after the base's `0085`, and differs from that base only in 89 P1 paths. Its Node 24.14.1 aggregate verification passed all 32 Turbo tasks in 4 minutes 6 seconds, with six tasks executed and 26 exact-tree cache hits. The full pipefail log is `.dork/flow/evidence/p1-final-verify.log`. The same Stage 1 reviewer and a fresh Stage 2 reviewer must still bind their final reports to the immutable pushed composition before the P1 pull request opens. Workstreams 2–7 remain pending; this record does not mark the wider Connections programme implemented.

Automated review then tightened the same P1 boundaries. Migration failures now emit a bounded server-only phase, category, and safe driver or invariant code while the public `migration_failed` response remains scrubbed; mutation checks prove neither the response nor the diagnostic carries the private legacy reference, SQL, parameters, or a filesystem path. Superseded type-wide cache and attachment methods were removed, the historical direct-connect record now names the exact-instance replacement, and comments distinguish locally owned BYO deployment mode from managed provider custody and retained foreign keys from the tombstone disconnect path. A real cached-agent, hydrated-launch regression also pins the URL-first directory and immutable agent path on the first message. The exact 12-path correction received an early independent review pass with no open findings.

The subsequent Node 24.14.1 aggregate ran all affected typecheck and lint tasks, passed all 14,408 client assertions, and passed 16,466 of 16,467 server assertions before the untouched Rooms 500-file provenance-cost test reached its global five-second test timeout. Its full log is `.dork/flow/evidence/p1-automated-review-final-verify.log`; this run is recorded as failed, not green. The one authorized focused run of the unchanged Rooms file passed all 38 assertions, including its fixed git-command count and service-time bounds, in 10.84 seconds for the file. That isolation result does not establish the aggregate timeout's cause. The independent affected pre-push gate remains required before the corrected commit is pushed.

## P2 execution session — 2026-09-06

**Work item:** DOR-1794

**Worker:** `/root/connections_broker_impl`

**Worktree:** `/Users/doriancollier/.dork/workspaces/dorkos/codex-connections-execution`

**Branch:** `codex/connections-execution`

P2 began from the root-provisioned combined P1 tree at `2875bdadf16b71385322cfd42b1a6401f0f5380f`. The first contract checkpoint is `aba5d035e774d7328885a5bd7e8eac7bc17dc48e`. It freezes strict execution/retry/public DTOs, an authenticated server principal, and runtime/cleanup ports. Focused schema, conformance, and principal tests passed; the intact commit hook passed formatting, all lint packages, and all typecheck tasks. Raw evidence identity and honest retention limits are recorded in `.dork/flow/evidence/p2-leaf-contract-checkpoint.json`.

Early adversarial review found three unresolved contract gaps: trusted toolkit-version discovery, nested principal immutability plus mint confinement, and the typed result of approving a connection review. These are being corrected before provider/runtime implementation is dispatched. The database, authorization, broker, reconciliation, review, REST/CLI, and minimum owner UI remain in progress. No P2 phase is complete and no managed provider is production-ready.

P1 subsequently merged on `origin/main` as `6f61d153492ce6941446c4fe158375910a8548f9` at 2026-09-06 11:49:08Z. Root verified all 91 P1 source blobs against the reviewed composition, all four test and three browser merge-queue checks were green, DOR-1793 is Done, and the P1 worktrees were cleaned. The source and cleanup records are `.dork/flow/p1-actual-merged-source-proof.json` and `.dork/flow/p1-cleanup-proof.json` in the programme worktree. The earlier failed aggregate attempts above remain failures; the merged queue evidence is the closing proof.

The P2 branch was rebased onto that exact merge with an unchanged four-commit range and then added the durable execution state, approval binding, broker, and exact cleanup checkpoints through `6f7771ccceb95fc6497262e4ff1c486f2dc210e9`. Independent review found that exact agent/connection removal initially revoked a whole-turn runtime bearer and thereby disabled unrelated connections. The correction leaves that bearer live, relies on fresh exact broker preflight to deny the removed connection, and still revokes every bearer on whole-agent removal. Focused differential tests and the server typecheck passed; independent re-review remains pending.

Owner management and migrated-account reconciliation are committed through `651438a28fedadd1856ac7e27aa2819dc2d25722`. The checkpoint adds complete provider-version discovery snapshots, server-owned preview membership and expiry, named-agent-only exact grant replacement, durable owner/program review requests, restart-safe connect approval outcomes, real approved lifecycle/access effects, exact pending-authority cleanup, and strict REST/Transport caller separation. API-key calls bind to their stable credential id and may create a review request; owner list, decision, direct connect/disconnect, and reconciliation paths refuse program, agent, approval-token, and untrusted-origin callers. Nine focused files passed 53 tests, four package typechecks passed, and the intact commit hook passed all 20 lint and 31 typecheck tasks. Independent review remains required.

The follow-up review boundary persists the account label and status, agent name, and exact requested immutable operation names and versions when a management request is created. Owner reads report when its target later becomes unavailable and still permit denial without approving a missing target. The same correction enforces the session precedence ladder during execution: an explicit attached override uses only session grants, while no override inherits agent grants. Safe retries now revalidate a durable runtime binding before a second provider attempt. Focused regressions and an independent correction review remain required before the minimum owner UI consumes this amended checkpoint. P2 remains in progress.

The branch then integrated shipped main commit `d931dadf08447661f85617e1792d025d47721672`. It keeps the shipped Better Auth `0087_account_issuer` migration unchanged and renumbers the unshipped P2 chain to `0088`, `0089`, and `0090`, with later journal timestamps and a regenerated snapshot chain that includes `account.issuer`. Migration fixtures cover both a database already at shipped main and the earlier P1 shape.

The provider boundary now performs complete, cancellable toolkit-version and operation discovery before dispatch. Composio execution stays inside the server adapter at SDK version `0.18.1`, selects the configured server user and exact connected account, and returns a normalized result without provider session or transport secrets. Provider construction exposes only a digest of the exact configuration it used. Composio universal-auth-key mode, Nango execution, and raw-MCP execution return typed unsupported outcomes where the current adapters cannot prove the required exact semantics; none fabricates success or exposes a vendor execution endpoint.

Connector execution is now owned by one authorization and broker path for runtime MCP, program REST, and CLI callers. The broker checks live owner, agent, session override, connection, immutable revision, argument binding, approval binding, runtime turn, and migration health before dispatch and again at every eligible retry. Usage stores an immutable payload-free intent and a separate append-only terminal receipt. Runtime principals are server minted, tied to the current boot and canonical turn context, tombstoned before durable revocation, and revalidated after awaited authority checks. Claude Code, Codex, and OpenCode receive an internal loopback MCP server; external MCP never receives connector account selectors or execution tools.

The private runtime surface deliberately contains five tools. Two read-only tools list only connections and exact schemas already executable by the authenticated turn, and three tools execute revisions classified as read, write, or destructive. This supersedes the earlier three-tool plan because an agent otherwise had no safe way to discover the stable IDs and input schema for access it already held. All five reject caller-supplied owner, agent, session, provider, and other undeclared selectors. A provider material change marks its connections for reconciliation, so the next discovery and execution both refuse until the owner explicitly reviews the new material.

The minimum owner experience lists durable management requests, shows the stored connection, agent, and immutable operation context, and resolves each request without fetching a newer catalog in its place. Reconciliation applies only to named agents and preserves omitted agents and still-valid pinned grants unless the owner explicitly removes them. Programs can create a request and poll only their own scrubbed lifecycle projection; approval and provider authentication remain distinct, and private authentication URLs stay owner only. The CLI uses those requester-safe routes and carries the exact original execution target when retrying with an approval token.

P2 also retires the old agent/session attach mutations and private provider-MCP account management capabilities. Retained compatibility mutation routes return an explicit retired response pointing to the Connections access editor. Session status is read-only and derives usable access from live canonical grants, including the explicit session-override precedence. Current guides and generated API documentation describe brokered execution and the narrower support of each provider.

Independent review is still active on the composed cutover and runtime discovery corrections. Provider SDK and runtime lifecycle leaves passed separate Stage 2 reviews; the owner UI and CLI passed their corrected Stage 1 reviews; the shared broker, authority, persistence, reconciliation, and management corrections passed their bounded Stage 1 reviews. The final pushed candidate still requires the combined verification gate and two fresh cross-seam Stage 2 reviews before a pull request opens. P2 remains in progress until those gates pass.

All bounded Stage 1 reviews subsequently closed with no open findings. Final affected verification ran on Node 24.14.1. The unchanged-source `pnpm verify` v4 run passed script tests, root lint, all 53 affected typecheck/lint tasks, and 13 package suites before the client React-version canary reached its five-second test timeout after 14,431 other client tests passed. That unchanged canary passed 5/5 in isolation. Deterministic completion covered the five packages the wrapper had not reached; the first server completion exposed three real integration canary failures in the shared rate-limit key and the retired-tool counts. After those corrections, the full server package passed 966 files with one skipped and 16,624 tests with 37 skipped; the stricter three-file cohort passed 25/25, server typecheck passed, and server lint reported zero errors. The wrapper run remains recorded as failed rather than being relabeled green. The per-package coverage and raw log map are in `.dork/flow/evidence/p2-final-affected-coverage.md`.

The final candidate still requires its intact commit and pre-push hooks, a pushed source-identical landing branch based on `d931dadf08447661f85617e1792d025d47721672`, and two fresh independent Stage 2 reviews before any pull request opens. P2 remains in progress.

The two fresh reviews of the first pushed candidate found five cross-seam corrections. Claude Code now registers the same five private connector tools as the other runtimes while retaining its dynamic per-turn structural principal; the general and external MCP catalogs remain account-free. Program review polling now assigns a nonzero exit to every state that has not produced an observable applied effect. An approved owner action whose durable receipt cannot be written remains an explicit `outcome_unknown` across same-process reads and restart, without replay or conversion to ordinary expiry. Reconciliation verifies every retained pinned revision against a complete exact-version catalog before describing it as supported, preserving older valid pins while failing safely when the provider cannot prove that version. Codex interruption revokes the turn binding before a parked SDK iterator settles and still closes the process path if durable revocation reports an error.

The provider handoff now carries a server-only final-dispatch authorization callback. Composio evaluates it after exact-account preflight and exact-schema retrieval, immediately before the operation request can begin. The callback revalidates the current runtime principal, owner, agent, session override, grant, connection, provider material generation, argument binding, and destructive approval. After every asynchronous identity and owner check settles, it performs one final synchronous canonical connector snapshot before permitting dispatch; revoking a grant or disabling a connection during the last principal-authority await is therefore refused. A refusal records a known not-sent terminal result and never becomes `outcome_unknown`. The final correction cohort passed 234 tests across 14 files, and the shared, server, client, and CLI typechecks passed on Node 24.14.1. Scoped lint reported no errors. OpenAPI and generated API-doc output remained source-identical. These corrections still require independent convergence review and the final hook-preserving publication gate.
