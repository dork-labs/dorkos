{
  "spec": "specs/core-extensions/02-specification.md",
  "slug": "core-extensions",
  "generatedAt": "2026-06-13T20:27:40Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Rename + generalized staging",
      "subject": "[core-extensions] [P1] Rename builtin-extensions source + service dirs and update build cpSync",
      "description": "Rename the in-repo source and service directories from `builtin-extensions` to `core-extensions`. The runtime `{dorkHome}/extensions/` discovery directory is UNCHANGED — only the in-repo source dirs are renamed.\n\nSteps (in order):\n1. Move source dir: `apps/server/src/builtin-extensions/` → `apps/server/src/core-extensions/`. It currently holds `marketplace/` (`extension.json` id `marketplace` name \"Dork Hub\", `index.ts`, `server.ts`). Use `git mv` so history is preserved.\n2. Move service dir: `apps/server/src/services/builtin-extensions/` → `apps/server/src/services/core-extensions/`. It currently holds `ensure-marketplace.ts` and `__tests__/`. Use `git mv`.\n3. Update the build step in `apps/server/package.json:14`. The current line is:\n   `\"build\": \"tsc && node -e \\\"require('fs').cpSync('src/builtin-extensions','dist/builtin-extensions',{recursive:true,filter:p=>!p.endsWith('.ts')})\\\"\",`\n   Change BOTH dir names to `core-extensions`:\n   `\"build\": \"tsc && node -e \\\"require('fs').cpSync('src/core-extensions','dist/core-extensions',{recursive:true,filter:p=>!p.endsWith('.ts')})\\\"\",`\n4. Grep the whole `apps/server/src/` tree for any remaining references to `builtin-extensions` (import paths, string literals, comments) and update them. Known call site: `apps/server/src/index.ts:37` imports `ensureBuiltinMarketplaceExtension` from `'./services/builtin-extensions/ensure-marketplace.js'` — this import path is fixed in task 1.2 when the file is renamed, but the directory portion must resolve.\n\nEdge cases / notes:\n- This task is purely structural (rename + path fixups). The actual `ensureCoreExtensions()` rewrite and `index.ts` wiring change happen in task 1.2.\n- `dist/builtin-extensions` artifacts may exist from prior builds; a clean build will regenerate `dist/core-extensions`.\n\nAcceptance criteria:\n- [ ] `apps/server/src/core-extensions/marketplace/` exists; `apps/server/src/builtin-extensions/` is gone.\n- [ ] `apps/server/src/services/core-extensions/` exists (with `ensure-marketplace.ts` + `__tests__/` for now); `apps/server/src/services/builtin-extensions/` is gone.\n- [ ] `apps/server/package.json` build line references `src/core-extensions` and `dist/core-extensions`.\n- [ ] `grep -rn 'builtin-extensions' apps/server/src` returns no results (the directory-name references; the `ensureBuiltinMarketplaceExtension` symbol is renamed in 1.2).\n- [ ] `pnpm --filter @dorkos/server build` succeeds and copies `dist/core-extensions/marketplace/extension.json`.\n- [ ] Run `pnpm test -- --run` for the server package (note `reference_vitest_dev_env_gotcha`: bare `pnpm vitest run` falsely fails two DEV-env tests; build packages before bare vitest in the worktree).",
      "activeForm": "Renaming builtin-extensions directories and updating the build cpSync step",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Rename + generalized staging",
      "subject": "[core-extensions] [P1] Generalize ensure-marketplace into ensureCoreExtensions() scanner and wire into index.ts",
      "description": "Replace the one-off `ensure-marketplace.ts` (`ensureBuiltinMarketplaceExtension`) with a directory scanner that version-stages every bundled core extension and returns their tier metadata. Behavior-preserving: Dork Hub (`marketplace`) still stages and stays enabled.\n\nCreate `apps/server/src/services/core-extensions/ensure-core-extensions.ts` exporting `ensureCoreExtensions(dorkHome): Promise<CoreExtensionInfo[]>`:\n\n```typescript\nconst CORE_SOURCE_DIR = path.resolve(__dirname, '../../core-extensions');\n\n/** Stage every bundled core extension into {dorkHome}/extensions/<id>/; return their tier metadata. */\nexport async function ensureCoreExtensions(dorkHome: string): Promise<CoreExtensionInfo[]> {\n  // for each subdir of CORE_SOURCE_DIR with a valid extension.json:\n  //   compare bundled vs installed version → fresh-copy / upgrade-copy / no-op (unchanged logic)\n  //   collect { id, defaultEnabled: manifest.defaultEnabled !== false, canDisable: manifest.canDisable !== false }\n}\n```\n\nImplementation details:\n- Reuse the EXISTING read-manifest-version + `fs.cp` copy logic from `ensure-marketplace.ts` verbatim per-extension (read bundled manifest version, compare to installed version at `{dorkHome}/extensions/<id>/extension.json`, copy tree on fresh-install or upgrade, no-op when versions match). Do not change the copy mechanics — only loop it over each subdir of `CORE_SOURCE_DIR`.\n- Scan `CORE_SOURCE_DIR` (`path.resolve(__dirname, '../../core-extensions')`) for subdirectories; skip any subdir lacking a valid `extension.json`.\n- For each staged extension collect `{ id, defaultEnabled: manifest.defaultEnabled !== false, canDisable: manifest.canDisable !== false }` and return the array.\n- The `CoreExtensionInfo` interface (`{ id: string; defaultEnabled: boolean; canDisable: boolean }`) is defined/consumed in the resolution helper (task 3.3). For phase 1, define it locally in this file (or import it once 3.3 lands) — it must match `{ id: string; defaultEnabled: boolean; canDisable: boolean }`.\n- `manifest.defaultEnabled` / `manifest.canDisable` are optional manifest fields added in phase 2 (task 2.3). In phase 1 they are absent, so `!== false` yields `true` for both — preserving today's \"Dork Hub stages and stays on\" behavior.\n\nWire into `apps/server/src/index.ts`:\n- Remove the import at line 37: `import { ensureBuiltinMarketplaceExtension } from './services/builtin-extensions/ensure-marketplace.js';` and replace with `import { ensureCoreExtensions } from './services/core-extensions/ensure-core-extensions.js';`.\n- Replace the call at line ~167 `await ensureBuiltinMarketplaceExtension(dorkHome);` with `const coreExtensions = await ensureCoreExtensions(dorkHome);` keeping the SAME non-fatal try/catch wrapper.\n- Capture the returned `CoreExtensionInfo[]` and pass it to the `ExtensionManager` (constructor arg or a `setCoreExtensions()` setter). NOTE: the manager wiring that consumes this list lands in phase 3 (task 3.4/3.5); in phase 1, capture the value and pass it through if the constructor already accepts it, otherwise leave a clear single-line comment marking where it will be consumed. Do not drop the value.\n- Delete `apps/server/src/services/core-extensions/ensure-marketplace.ts` once the new file is in place and the import is repointed.\n\nError handling / edge cases:\n- Staging stays NON-FATAL: a copy failure for one extension must not crash startup (same try/catch posture as today). Log and continue.\n- A subdir without a valid `extension.json` is skipped silently (not staged, not returned).\n\nTests — migrate and generalize `ensure-marketplace.test.ts` → `apps/server/src/services/core-extensions/__tests__/ensure-core-extensions.test.ts`:\n- Multi-extension scan (more than one subdir under `CORE_SOURCE_DIR`).\n- Fresh-install / upgrade / no-op per extension (carry over the existing version-diff cases).\n- Returns correct `CoreExtensionInfo[]` (`id`, `defaultEnabled`, `canDisable` derived via `!== false`).\n- Idempotent on repeat calls (second call is a no-op when versions match).\n\nAcceptance criteria:\n- [ ] `apps/server/src/services/core-extensions/ensure-core-extensions.ts` exports `ensureCoreExtensions(dorkHome): Promise<CoreExtensionInfo[]>` scanning `CORE_SOURCE_DIR`.\n- [ ] `apps/server/src/services/core-extensions/ensure-marketplace.ts` is deleted.\n- [ ] `apps/server/src/index.ts` calls `ensureCoreExtensions(dorkHome)` in a non-fatal try/catch and captures the return value; no remaining reference to `ensureBuiltinMarketplaceExtension`.\n- [ ] `apps/server/src/services/core-extensions/__tests__/ensure-core-extensions.test.ts` exists with the multi-ext scan / fresh / upgrade / no-op / returns-info / idempotent cases, all passing.\n- [ ] `pnpm test -- --run` passes for the server package (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Generalizing ensure-marketplace into the ensureCoreExtensions scanner",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Config + manifest",
      "subject": "[core-extensions] [P2] Extend extensions config schema with deviation-list disabled[] field",
      "description": "Extend the `extensions` block in the config schema to support deviation lists: the existing `enabled` (things turned ON that default OFF) plus a new `disabled` (things turned OFF that default ON). Backward-compatible and additive.\n\nEdit `packages/shared/src/config-schema.ts` (~line 135 — currently `extensions: { enabled: string[] }`). Replace with:\n\n```typescript\nextensions: z\n  .object({\n    /** Extension IDs the user turned ON that default OFF (user/marketplace + default-off core). */\n    enabled: z.array(z.string()).default(() => []),\n    /** Extension IDs the user turned OFF that default ON (default-on core). */\n    disabled: z.array(z.string()).default(() => []),\n  })\n  .default(() => ({ enabled: [], disabled: [] })),\n```\n\nMental model to preserve in comments: both lists record DEVIATIONS from each extension's default state. `enabled` records overrides for things whose default is off; `disabled` records overrides for things whose default is on. This is the JetBrains `disabled_plugins.txt` model generalized to two defaults.\n\nFollow the `.claude/skills/adding-config-fields/` lifecycle for the schema portion:\n- Verify the `USER_CONFIG_DEFAULTS` parse check still passes with the new shape (an empty/default config must parse to `{ enabled: [], disabled: [] }`).\n- The conf migration is a separate task (2.2); the docs updates are folded into phase 6 (task 6.2). Add the field and defaults here, plus the upgrade-path test placeholder via task 2.2.\n\nEdge cases:\n- Both inner arrays AND the outer object carry `.default(() => [])` / `.default(() => ({ enabled: [], disabled: [] }))` so a config missing `extensions` entirely, or missing either inner key, parses cleanly.\n\nTests:\n- Add/extend a schema test asserting: empty config → `{ enabled: [], disabled: [] }`; a config with only `enabled` populated → `disabled` defaults to `[]`; both lists round-trip.\n\nAcceptance criteria:\n- [ ] `packages/shared/src/config-schema.ts` `extensions` is the two-list object above with all three `.default()` calls.\n- [ ] `USER_CONFIG_DEFAULTS` parses to `extensions: { enabled: [], disabled: [] }`.\n- [ ] Schema test covers empty / enabled-only / both-populated cases.\n- [ ] `pnpm typecheck` passes for `@dorkos/shared` and downstream consumers compile (the config-manager and discovery consume this type).\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Extending the extensions config schema with the disabled deviation list",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.2"],
      "parallelWith": ["2.3"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Config + manifest",
      "subject": "[core-extensions] [P2] Add version-keyed config migration backfilling extensions.disabled",
      "description": "Append a version-keyed migration to `CONFIG_MIGRATIONS` that backfills `extensions.disabled: []` for pre-existing configs that predate task 2.1. The user's `~/.dork/config.json` already exists in production, so this must be additive and must never remove `enabled`.\n\nEdit `apps/server/src/services/core/config-manager.ts` (`CONFIG_MIGRATIONS` ~line 81). Append an entry:\n\n```typescript\n// version key resolved at release time — /system:release detects config drift and scaffolds the keyed migration at tag time\n'<next-release>': (store) => {\n  const ext = store.get('extensions');\n  if (ext && !Array.isArray((ext as { disabled?: unknown }).disabled)) {\n    store.set('extensions', { ...ext, disabled: [] });\n  }\n},\n```\n\nImportant notes on the version key:\n- Use the literal placeholder `'<next-release>'` and add a clear comment (above) noting it must be replaced with the actual release version at tag time, and that `/system:release` detects this drift and scaffolds/fills the keyed migration. Do NOT hardcode a real version — `projectVersion` derives from `SERVER_VERSION` (dynamic, never hardcoded) and the migration key is resolved at release.\n- The migration is append-only; do not modify existing entries.\n- The corrupt-recovery path reuses the same conf options, so this migration applies equally on recovery.\n\nFollow the `.claude/skills/adding-config-fields/` lifecycle for the migration + test portion:\n- Add an upgrade-path test to `apps/server/src/services/core/__tests__/config-manager.test.ts`: a prior config shaped `{ extensions: { enabled: ['foo'] } }` (no `disabled`) → after migration gains `disabled: []` AND preserves `enabled: ['foo']`. Also assert a config with `disabled` already present is left untouched (idempotent).\n\nEdge cases:\n- A config with no `extensions` key at all is handled by the schema defaults (task 2.1), not this migration — the guard `if (ext && ...)` skips when `extensions` is absent.\n- Only backfill when `disabled` is not already an array (guard against double-application).\n\nAcceptance criteria:\n- [ ] `CONFIG_MIGRATIONS` has a new placeholder-keyed entry (`'<next-release>'`) with the backfill body above and a TODO/comment explaining the release-time key resolution via `/system:release`.\n- [ ] `config-manager.test.ts` upgrade-path test: prior config without `extensions.disabled` gains `[]`; `enabled` preserved; pre-migrated config untouched.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Adding the version-keyed config migration to backfill extensions.disabled",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": ["2.3"]
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Config + manifest",
      "subject": "[core-extensions] [P2] Add defaultEnabled + canDisable optional fields to ExtensionManifestSchema",
      "description": "Add two optional fields to the extension manifest schema. Both are harmless for user/marketplace extensions (ignored). `extension.json` is a DorkOS-internal manifest (not the Claude-Code `marketplace.json`), so adding fields carries no marketplace-format-compat concern.\n\nEdit `packages/extension-api/src/manifest-schema.ts` (`ExtensionManifestSchema`). Add:\n\n```typescript\n/** For core extensions: whether this ships enabled. Omitted/true = on, false = off. Ignored for user extensions. */\ndefaultEnabled: z.boolean().optional(),\n/** Whether the user may disable this extension. Defaults to true. false = always on, no toggle shown. */\ncanDisable: z.boolean().optional(),\n```\n\nNotes:\n- Both are `.optional()` (no `.default()`) — consumers derive the effective value via `manifest.defaultEnabled !== false` and `manifest.canDisable !== false` (omitted → treated as `true`). This matches the `ensureCoreExtensions()` derivation in task 1.2.\n- Ensure the inferred TypeScript type (`ExtensionManifest`) picks up both optional fields and re-exports cleanly from the package barrel.\n\nTests — add a manifest schema test (in the extension-api package's test dir, alongside existing manifest-schema tests):\n- `defaultEnabled` / `canDisable` parse when present (true and false).\n- Both omitted → manifest still valid; fields are `undefined`.\n- Round-trip: a manifest with `defaultEnabled: false, canDisable: true` parses and retains those values.\n\nAcceptance criteria:\n- [ ] `ExtensionManifestSchema` includes `defaultEnabled: z.boolean().optional()` and `canDisable: z.boolean().optional()` with the documenting comments.\n- [ ] The inferred `ExtensionManifest` type exposes both optional fields; package builds/typechecks.\n- [ ] Manifest schema test covers present (true/false), omitted, and round-trip cases.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Adding defaultEnabled and canDisable fields to the manifest schema",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.2"],
      "parallelWith": ["2.1", "2.2"]
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Add origin field to ExtensionRecord, ExtensionRecordPublic, and toPublic()",
      "description": "Add `origin: 'core' | 'user'` tracking to the extension record types and surface it to the client. Origin is derived from the startup staging set (the IDs `ensureCoreExtensions()` staged), not from manifest claims — VS Code's `isBuiltin`-by-location pattern, which can't be spoofed.\n\nEdit `packages/extension-api/src/types.ts`:\n- Add `origin: 'core' | 'user'` to `ExtensionRecord`.\n- Add `origin: 'core' | 'user'` to `ExtensionRecordPublic`.\n\nEdit `apps/server/src/services/extensions/extension-manager-types.ts`:\n- Include `origin` in the `toPublic()` mapping so it is surfaced to the client (copy `record.origin` through to the public shape).\n\nNotes:\n- This task only adds the field to the types and the `toPublic()` passthrough. The value is ASSIGNED during discovery (task 3.4: `rec.origin = coreIds.has(id) ? 'core' : 'user'`). Until 3.4 lands, the field exists on the type; discovery sets it.\n- Keep the field required (not optional) on both record types — discovery always sets it.\n\nTests:\n- Extend the `toPublic()` test (or `extension-manager-types` test) to assert `origin` is carried through from the internal record to the public record for both `'core'` and `'user'` values.\n\nAcceptance criteria:\n- [ ] `ExtensionRecord` and `ExtensionRecordPublic` in `packages/extension-api/src/types.ts` both carry `origin: 'core' | 'user'`.\n- [ ] `toPublic()` in `apps/server/src/services/extensions/extension-manager-types.ts` copies `origin` to the public shape.\n- [ ] Test asserts `origin` survives `toPublic()`.\n- [ ] `pnpm typecheck` passes (discovery + manager will reference `origin`).\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Adding the origin field to extension record types and toPublic",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.3"],
      "parallelWith": ["3.2", "3.3"]
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Create extension-enable-resolution pure helper (defaultsOn, isEnabled, setEnabled)",
      "description": "Extract the on/off decision into a pure, unit-testable helper used by BOTH discovery (initial status) and the manager (which list to mutate on toggle). This keeps the logic DRY and isolated.\n\nCreate `apps/server/src/services/extensions/extension-enable-resolution.ts` with these exports:\n\n```typescript\ninterface CoreExtensionInfo {\n  id: string;\n  defaultEnabled: boolean;\n  canDisable: boolean;\n}\ninterface ExtensionsConfig {\n  enabled: string[];\n  disabled: string[];\n}\n\n/** True when an extension's baseline (pre-override) state is ON. */\nfunction defaultsOn(id: string, core: Map<string, CoreExtensionInfo>): boolean {\n  const info = core.get(id);\n  return info ? info.defaultEnabled : false; // user extensions default off\n}\n\n/** Resolve whether an extension should be enabled, given config overrides. */\nfunction isEnabled(\n  id: string,\n  config: ExtensionsConfig,\n  core: Map<string, CoreExtensionInfo>\n): boolean {\n  return defaultsOn(id, core)\n    ? !config.disabled.includes(id) // default-on: off only if explicitly disabled\n    : config.enabled.includes(id); // default-off: on only if explicitly enabled\n}\n```\n\nAlso add a `setEnabled(id, on, config, core)` helper that returns the NEXT `{ enabled, disabled }` config based on the toggle-routing rule (the lists capture deviations, so the helper dictates which list to mutate):\n\n| Extension | Default | Enable action | Disable action |\n| --- | --- | --- | --- |\n| default-on core (Dork Hub) | on | remove id from `disabled` | add id to `disabled` |\n| default-off core (Hello World, Linear Loop) | off | add id to `enabled` | remove id from `enabled` |\n| user / marketplace | off | add id to `enabled` | remove id from `enabled` |\n\nSo `setEnabled` logic: if `defaultsOn(id, core)` → on=remove id from `disabled`, off=add id to `disabled`; else → on=add id to `enabled`, off=remove id from `enabled`. Always return a NEW `{ enabled, disabled }` object (no mutation of the input); dedupe so an id is never added twice.\n\nExport `CoreExtensionInfo`, `ExtensionsConfig`, `defaultsOn`, `isEnabled`, `setEnabled`. The `CoreExtensionInfo` type defined here is the canonical one consumed by `ensureCoreExtensions()` (task 1.2 should import it from here once this lands) and the manager.\n\nTests — new `apps/server/src/services/extensions/__tests__/extension-enable-resolution.test.ts`:\n- default-on baseline: core ext with `defaultEnabled:true`, absent from both lists → `isEnabled` true.\n- default-off baseline: core ext with `defaultEnabled:false`, absent → false; user ext (not in core map), absent → false.\n- All six enable/disable→list-mutation cases from the table (default-on enable/disable; default-off-core enable/disable; user enable/disable).\n- \"new core extension on upgrade\": a freshly-shipped core id absent from both `enabled` and `disabled` → resolves to its `defaultEnabled` (on for default-on, off for default-off).\n- default-off-core opt-in path: add to `enabled` → `isEnabled` true.\n- `setEnabled` returns new objects without mutating input; no duplicate ids.\n\nAcceptance criteria:\n- [ ] `extension-enable-resolution.ts` exports `CoreExtensionInfo`, `ExtensionsConfig`, `defaultsOn`, `isEnabled`, `setEnabled` as pure functions (no I/O, no config-manager imports).\n- [ ] `extension-enable-resolution.test.ts` covers default-on/off baselines, all six toggle→list cases, new-core-on-upgrade, and default-off opt-in.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Creating the extension-enable-resolution pure helper",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": ["3.1", "3.3"]
    },
    {
      "id": "3.3",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Point CoreExtensionInfo type from ensureCoreExtensions to the shared resolution helper",
      "description": "Consolidate the `CoreExtensionInfo` type so `ensureCoreExtensions()` and the resolution helper share one canonical definition (avoids drift between the staging output and the resolution input).\n\nSteps:\n1. In `apps/server/src/services/core-extensions/ensure-core-extensions.ts` (task 1.2), remove any local `CoreExtensionInfo` definition and import it from `apps/server/src/services/extensions/extension-enable-resolution.ts` (task 3.2): `import type { CoreExtensionInfo } from '../extensions/extension-enable-resolution.js';`.\n2. Confirm `ensureCoreExtensions()` returns `Promise<CoreExtensionInfo[]>` with the exact shape `{ id, defaultEnabled, canDisable }` matching the canonical type.\n3. Verify no second definition of `CoreExtensionInfo` remains in the server tree (grep).\n\nNotes:\n- This is a small consolidation task that exists because 1.2 (phase 1) and 3.2 (phase 3) both need the type. The canonical home is the resolution helper.\n- If task 1.2 already imported from the resolution helper (because 3.2 landed first in the worktree branch), this task is a no-op verification.\n\nAcceptance criteria:\n- [ ] `CoreExtensionInfo` is defined once (in `extension-enable-resolution.ts`) and imported by `ensure-core-extensions.ts`.\n- [ ] `grep -rn 'interface CoreExtensionInfo' apps/server/src` returns exactly one result.\n- [ ] `ensure-core-extensions.test.ts` still passes with the shared type.\n- [ ] `pnpm typecheck` and `pnpm test -- --run` pass (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Consolidating the CoreExtensionInfo type into the resolution helper",
      "size": "small",
      "priority": "medium",
      "dependencies": ["3.2"],
      "parallelWith": ["3.1"]
    },
    {
      "id": "3.4",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Update discover() signature for tier-aware status + origin derivation",
      "description": "Change discovery to take the full config + core info and set both `origin` and tier-aware `status` per record.\n\nEdit `apps/server/src/services/extensions/extension-discovery.ts`. The current `discover(cwd, enabledIds: string[])` (~line 31) becomes:\n\n```typescript\nasync discover(\n  cwd: string | null,\n  config: ExtensionsConfig,\n  core: Map<string, CoreExtensionInfo>,\n): Promise<ExtensionRecord[]>\n```\n\nImport `ExtensionsConfig`, `CoreExtensionInfo`, and `isEnabled` from `./extension-enable-resolution.js` (task 3.2).\n\nIn the status-resolution loop (~lines 50–70), after the compatibility checks, for each record:\n- Set `rec.origin = core.has(rec.id) ? 'core' : 'user'`.\n- Set `rec.status = isEnabled(rec.id, config, core) ? 'enabled' : 'disabled'` (replacing the old `enabledIds.includes(rec.id)` membership check).\n- Add `origin` to the discovery log line for observability (the log that records each discovered extension's id/scope/status now also logs `origin`).\n\nEdge case (local override of a core id): a local `.dork/extensions/<id>` override of a core id is a dev-only edge case; `origin` remains `'core'` by id membership in the `core` map, using whichever code won the global/local merge. Do not special-case the merge — just key `origin` off `core.has(rec.id)` after the merge.\n\nTests — update `apps/server/src/services/extensions/__tests__/extension-discovery.test.ts`:\n- New `discover(cwd, config, core)` signature (callers pass `{ enabled, disabled }` + a `Map`).\n- Tier-aware status: a default-on core ext absent from `disabled` → `'enabled'`; same ext in `disabled` → `'disabled'`. A default-off core ext absent from `enabled` → `'disabled'`; in `enabled` → `'enabled'`. A user ext only enabled via `enabled`.\n- `origin` derivation: ids in the `core` map → `origin: 'core'`; others → `origin: 'user'`.\n\nAcceptance criteria:\n- [ ] `discover()` has the new three-arg signature and imports `isEnabled`/types from the resolution helper.\n- [ ] Each record gets `rec.origin` (from `core` membership) and `rec.status` (from `isEnabled`).\n- [ ] Discovery log line includes `origin`.\n- [ ] `extension-discovery.test.ts` updated for the new signature, tier-aware status, and origin derivation; passing.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Updating discover() for tier-aware status and origin derivation",
      "size": "medium",
      "priority": "high",
      "dependencies": ["3.1", "3.2"],
      "parallelWith": []
    },
    {
      "id": "3.5",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Route enable/disable through setEnabled, hold core map, add canDisable guard",
      "description": "Make the manager tier-aware: hold the core map, pass the full config + core map to discovery on reload, and route toggles through the `setEnabled()` helper so the correct deviation list is mutated. Public API and return shapes are unchanged.\n\nEdit `apps/server/src/services/extensions/extension-manager.ts`:\n\n1. Hold `coreExtensions: Map<string, CoreExtensionInfo>` built from the `CoreExtensionInfo[]` passed in from `index.ts` (constructor arg or `setCoreExtensions(infos: CoreExtensionInfo[])` setter that builds the `Map` keyed by `id`). Import `CoreExtensionInfo`, `ExtensionsConfig`, `setEnabled` from `./extension-enable-resolution.js`.\n\n2. `reload()` (~line 107): read the FULL `extensions` config via `configManager.get('extensions')` (now `{ enabled, disabled }`) and pass `{ enabled, disabled }` + the core map to `discover()`:\n   `const config = configManager.get('extensions'); const records = await discovery.discover(cwd, config, this.coreExtensions);`\n\n3. `enable()` (~line 198) and `disable()` (~line 227): replace the direct `enabled`-list mutation at lines 210–213 and 235–238 with the `setEnabled()` helper. Read current config, compute the next config, write it back:\n   - `enable(id)`: `const next = setEnabled(id, true, configManager.get('extensions'), this.coreExtensions); configManager.set('extensions', next);` then reload/recompile as today.\n   - `disable(id)`: `const next = setEnabled(id, false, configManager.get('extensions'), this.coreExtensions); configManager.set('extensions', next);` then reload/recompile as today.\n\n4. `canDisable:false` guard in `disable()`: if the record's `origin === 'core'` and its `canDisable === false` (look up via the core map: `this.coreExtensions.get(id)?.canDisable === false`), `disable()` returns `null` (no-op) — defense in depth behind the UI. Do this guard BEFORE mutating config.\n\nWire from `index.ts`: ensure the `CoreExtensionInfo[]` captured in task 1.2 is now actually passed to the manager (constructor or setter) before `initialize()`/`reload()` runs. Complete the passthrough that task 1.2 stubbed.\n\nNotes:\n- Public API (`enable`/`disable` signatures, return shapes) and the HTTP route contract are unchanged — the server alone decides which list to mutate.\n- `setEnabled` returns the whole `{ enabled, disabled }`, so write the entire object back, not just one list.\n\nTests — update `apps/server/src/services/extensions/__tests__/extension-manager.test.ts`:\n- enable/disable route to the correct deviation list by origin/default: disabling a default-on core ext adds its id to `disabled` (not removes from `enabled`); enabling it removes from `disabled`. Enabling a default-off core ext or a user ext adds to `enabled`; disabling removes from `enabled`.\n- `canDisable:false` disable is a no-op: returns `null`, config unchanged.\n- reload passes `{ enabled, disabled }` + core map to discover (verify via the discovery mock/spy).\n\nAcceptance criteria:\n- [ ] Manager holds `coreExtensions: Map<string, CoreExtensionInfo>` (from index.ts).\n- [ ] `reload()` passes `{ enabled, disabled }` + core map to `discover()`.\n- [ ] `enable()`/`disable()` call `setEnabled()` and write the full `{ enabled, disabled }` back; old direct `enabled`-only mutation (lines 210–213, 235–238) removed.\n- [ ] `disable()` returns `null` for `canDisable:false` core extensions before any config mutation.\n- [ ] `index.ts` passes the staged `CoreExtensionInfo[]` to the manager.\n- [ ] `extension-manager.test.ts` updated for correct-list routing + canDisable no-op; passing.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Routing enable/disable through setEnabled and adding the canDisable guard",
      "size": "medium",
      "priority": "high",
      "dependencies": ["3.4", "3.3"],
      "parallelWith": []
    },
    {
      "id": "3.6",
      "phase": 3,
      "phaseName": "Origin + tier-aware resolution",
      "subject": "[core-extensions] [P3] Verify extension HTTP routes still flip state under the new model",
      "description": "Confirm the extension HTTP API surface is unchanged and still works under the new deviation-list model. The endpoints `POST /api/extensions/:id/enable` and `POST /api/extensions/:id/disable` are untouched — the server (manager) decides which list to mutate, so the route handlers in `apps/server/src/routes/extensions.ts` need no signature change.\n\nSteps:\n1. Review `apps/server/src/routes/extensions.ts` enable/disable handlers — confirm they delegate to `extensionManager.enable(id)` / `extensionManager.disable(id)` and do not themselves touch `config.extensions.enabled`. If any route handler reads or mutates the config lists directly, refactor it to delegate to the manager (it should already).\n2. No new endpoints, no contract change.\n\nTests — update `apps/server/src/routes/__tests__/extension-routes.test.ts` (or wherever extension route tests live):\n- The unchanged endpoints still flip state correctly under the new model: `POST .../enable` on a default-on core ext that was disabled removes it from `disabled`; `POST .../disable` on it adds it to `disabled`. `POST .../enable` on a user/default-off ext adds to `enabled`; `.../disable` removes from `enabled`.\n- `POST .../disable` on a `canDisable:false` core ext is a no-op (route returns the appropriate response; manager returned `null`).\n\nNotes:\n- This task is mostly verification + test updates; expect minimal-to-no source change in the route file. If the route returns a 200/204 today on disable, keep that behavior; if `disable()` now returns `null` for locked extensions, ensure the route surfaces a sensible response (e.g. the unchanged record or a clear status) without erroring.\n\nAcceptance criteria:\n- [ ] `apps/server/src/routes/extensions.ts` enable/disable endpoints are unchanged in contract and delegate to the manager.\n- [ ] `extension-routes.test.ts` asserts endpoints still flip state correctly (default-on core via `disabled`; default-off/user via `enabled`) and that `canDisable:false` disable is a no-op.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Verifying extension HTTP routes still flip state under the new model",
      "size": "small",
      "priority": "medium",
      "dependencies": ["3.5"],
      "parallelWith": []
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "Settings UI",
      "subject": "[core-extensions] [P4] Add origin to the client extension type",
      "description": "Surface `origin` on the client-facing extension type so the settings UI can partition by tier.\n\nEdit `apps/client/src/layers/features/extensions/model/types.ts`:\n- Add `origin: 'core' | 'user'` to the client `LoadedExtension` (the client record type that mirrors `ExtensionRecordPublic`). The server already includes `origin` in `toPublic()` (task 3.1), so this is a type alignment so the field is typed/accessible in the UI.\n\nNotes:\n- `api/queries.ts` is UNCHANGED — `useExtensions` (GET, 30s poll), `useEnableExtension`/`useDisableExtension` (POST + invalidate) need no changes; the server decides which list to mutate so the HTTP surface (`POST /api/extensions/:id/{enable,disable}`) is untouched.\n- Keep the field required (matches the server's required `origin`).\n\nTests:\n- If a type/parse test exists for the client extension model, extend it to expect `origin`. Otherwise this is covered transitively by the `ExtensionsSettingsTab.test.tsx` (task 4.2) which depends on `origin`.\n\nAcceptance criteria:\n- [ ] Client `LoadedExtension` type in `model/types.ts` carries `origin: 'core' | 'user'`.\n- [ ] `api/queries.ts` is unchanged.\n- [ ] `pnpm typecheck` passes for `@dorkos/client`.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Adding origin to the client extension type",
      "size": "small",
      "priority": "high",
      "dependencies": ["3.5"],
      "parallelWith": ["4.3"]
    },
    {
      "id": "4.2",
      "phase": 4,
      "phaseName": "Settings UI",
      "subject": "[core-extensions] [P4] Partition ExtensionsSettingsTab into Core and Installed sections",
      "description": "Split the flat extensions list into two labeled sections partitioned by `origin`.\n\nEdit `apps/client/src/layers/features/extensions/ui/ExtensionsSettingsTab.tsx` (currently a flat `extensions.map`, ~lines 75–83). Partition the extensions by `origin`:\n- \"Core extensions\" section: `extensions.filter(e => e.origin === 'core')` — Dork Hub (on), Hello World (off), Linear Loop (off) once phase 5 lands. This section always has members.\n- \"Installed extensions\" section: `extensions.filter(e => e.origin === 'user')` — anything the user added. Empty by default: render a short empty-state with a pointer to Dork Hub (e.g. \"No extensions installed yet. Browse Dork Hub to add some.\").\n\nEach section renders `ExtensionCard` per extension (the card changes land in task 4.3). Use clear section headers/labels consistent with the existing settings design system (study the surrounding Settings tab markup for heading style; do not invent a new pattern — follow established conventions per the codebase-excellence standard).\n\nNotes:\n- Maintain existing sort/order within each section (manifest/discovery order unless an explicit order is already used). No `CORE_EXTENSION_ORDER` list is required for the initial set.\n- Keep loading/error states for the `useExtensions` query intact.\n- Responsive by default — the two-section layout must work on mobile, tablet, and desktop (per the quality standard).\n\nTests — new `apps/client/src/layers/features/extensions/ui/__tests__/ExtensionsSettingsTab.test.tsx` (use mock `Transport` via `TransportProvider` and the testing patterns in `.claude/rules/testing.md`):\n- Two sections render with the correct headers (\"Core extensions\", \"Installed extensions\").\n- Partition by `origin`: a core-origin extension appears under Core; a user-origin extension under Installed.\n- Empty Installed section shows the empty-state pointer when no user extensions exist.\n\nAcceptance criteria:\n- [ ] `ExtensionsSettingsTab.tsx` renders \"Core extensions\" and \"Installed extensions\" sections, partitioned by `origin`.\n- [ ] Empty Installed section shows an empty-state with a Dork Hub pointer.\n- [ ] `ExtensionsSettingsTab.test.tsx` asserts two sections, correct partition by origin, and the empty-state.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Partitioning the settings tab into Core and Installed sections",
      "size": "medium",
      "priority": "high",
      "dependencies": ["4.1"],
      "parallelWith": []
    },
    {
      "id": "4.3",
      "phase": 4,
      "phaseName": "Settings UI",
      "subject": "[core-extensions] [P4] Handle canDisable:false in ExtensionCard and keep health badge distinct from toggle",
      "description": "Make `ExtensionCard` honor the `canDisable` flag and keep the runtime-health badge visually distinct from the on/off toggle.\n\nEdit `apps/client/src/layers/features/extensions/ui/ExtensionCard.tsx`:\n- When `manifest.canDisable === false`, render the toggle as locked/absent (no interactive switch) with a small \"Required\" hint instead of the normal toggle. Otherwise render the normal on/off toggle (existing behavior).\n- Keep the health/availability badge (compile error / runtime error / unavailable) VISUALLY DISTINCT from the on/off toggle — these communicate different things (research pitfall #4: \"disabled by user\" is the toggle state; \"unavailable due to runtime error\" is the health badge). Do not conflate a runtime-error state with the off state of the toggle.\n\nNotes:\n- `canDisable` comes from the extension's manifest (surfaced through the public record). For the initial core set every extension has `canDisable: true`, so the locked path is exercised primarily by tests; build it now so the reserved flag works when used.\n- Follow the existing card design system (badges, switch component) — do not introduce a new toggle/badge primitive.\n- Disabling Dork Hub is allowed in the initial set (no locked core extension), so the normal toggle path remains the default.\n\nTests — add to `ExtensionsSettingsTab.test.tsx` (task 4.2) or a dedicated `ExtensionCard.test.tsx`:\n- `canDisable: false` → toggle is locked/absent and a \"Required\" hint shows.\n- `canDisable: true` (or omitted) → normal interactive toggle.\n- A health/error state renders the badge distinctly from the toggle (badge present AND toggle present, not merged).\n\nAcceptance criteria:\n- [ ] `ExtensionCard.tsx` renders a locked/\"Required\" state when `canDisable === false`, normal toggle otherwise.\n- [ ] Health/availability badge is rendered distinctly from the on/off toggle.\n- [ ] Tests cover canDisable:false lock, normal toggle, and badge-vs-toggle separation.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Handling canDisable in ExtensionCard and separating the health badge",
      "size": "medium",
      "priority": "high",
      "dependencies": ["4.1"],
      "parallelWith": ["4.2"]
    },
    {
      "id": "5.1",
      "phase": 5,
      "phaseName": "Initial core set + examples removal",
      "subject": "[core-extensions] [P5] Set Dork Hub manifest defaultEnabled:true, canDisable:true",
      "description": "Mark the Dork Hub (`marketplace`) core extension as default-on and disableable in its manifest.\n\nEdit `apps/server/src/core-extensions/marketplace/extension.json` (the dir renamed in task 1.1):\n- Add `\"defaultEnabled\": true` and `\"canDisable\": true`.\n\nNotes:\n- `defaultEnabled: true` could be omitted (the `!== false` derivation defaults to on), but set it explicitly for clarity and to make the curated tier self-documenting.\n- This makes Dork Hub the default-on member that exercises the `disabled`-list opt-out path. A fresh user sees Dork Hub enabled; disabling it adds `marketplace` to `extensions.disabled`.\n- Disabling Dork Hub is allowed — the `/marketplace` route already degrades gracefully when the extension is absent. No code change needed for that here.\n\nTests:\n- Covered by `ensure-core-extensions.test.ts` (task 1.2/3.3) returning `{ id: 'marketplace', defaultEnabled: true, canDisable: true }`, and by discovery/manager tests for the default-on path. Add an assertion to `ensure-core-extensions.test.ts` for the marketplace entry's derived info if not already present.\n\nAcceptance criteria:\n- [ ] `apps/server/src/core-extensions/marketplace/extension.json` has `defaultEnabled: true` and `canDisable: true`.\n- [ ] `ensureCoreExtensions()` returns `{ id: 'marketplace', defaultEnabled: true, canDisable: true }`.\n- [ ] A fresh-config user resolves Dork Hub to `enabled` (verified by discovery/manager tests).\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Setting Dork Hub manifest defaults to default-on and disableable",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.2", "3.5"],
      "parallelWith": ["5.2"]
    },
    {
      "id": "5.2",
      "phase": 5,
      "phaseName": "Initial core set + examples removal",
      "subject": "[core-extensions] [P5] Move Hello World + Linear Loop into core-extensions as default-off, delete examples/extensions",
      "description": "Relocate the two real extensions out of `examples/extensions/` into the core tier as default-off members, then delete `examples/extensions/` entirely.\n\nSteps:\n1. Move `examples/extensions/hello-world/` → `apps/server/src/core-extensions/hello-world/` (use `git mv`). In its `extension.json`, add `\"defaultEnabled\": false` and `\"canDisable\": true`. (id `hello-world`, Name \"Hello World\".) Hello World doubles as the canonical authoring skeleton (its source is the reference) and a live toggleable demo.\n2. Move `examples/extensions/linear-issues/` → `apps/server/src/core-extensions/linear-issues/` (use `git mv`). In its `extension.json`, add `\"defaultEnabled\": false` and `\"canDisable\": true`. (id `linear-issues`, Name \"Linear Loop\", v2.0.0.) Linear Loop incubates here until `@dorkos/extension-api` is published and it migrates to the marketplace; it ships OFF by default.\n3. Drop `examples/extensions/hello-world-js/` (not carried forward).\n4. Delete `examples/extensions/` entirely after the two moves. If `examples/` becomes empty, leave the parent dir decision to whatever else lives there — only remove `examples/extensions/`.\n\nEdge cases / safety:\n- The copy-on-upgrade in `ensureCoreExtensions()` overwrites CODE files only; Linear Loop's Linear API key lives in the existing encrypted secret store, not in the staged code tree — staging never touches secrets (research pitfall #2). No data co-location.\n- Both default-off members exercise the `enabled`-list opt-in path, proving that branch from day one.\n- Confirm the moved extensions have valid `extension.json` so `ensureCoreExtensions()` picks them up (a subdir without a valid manifest is skipped).\n- Linear Loop declares `externalHosts: [\"https://api.linear.app\"]` and ships off — no new network surface for default users.\n\nTests:\n- Extend `ensure-core-extensions.test.ts` (task 1.2/3.3) to assert all three core extensions are scanned and return correct info: `marketplace` (defaultEnabled true), `hello-world` (false), `linear-issues` (false). Since the test uses the real `CORE_SOURCE_DIR` or a fixture, ensure it reflects the three-extension set.\n- Discovery test (task 3.4) should resolve `hello-world` and `linear-issues` to `'disabled'` for a fresh config (absent from `enabled`), and to `'enabled'` when added to `enabled`.\n\nAcceptance criteria:\n- [ ] `apps/server/src/core-extensions/hello-world/extension.json` has `defaultEnabled: false`, `canDisable: true`.\n- [ ] `apps/server/src/core-extensions/linear-issues/extension.json` has `defaultEnabled: false`, `canDisable: true`.\n- [ ] `examples/extensions/` no longer exists; `hello-world-js` is dropped.\n- [ ] `ensureCoreExtensions()` returns the three-extension set with correct derived info.\n- [ ] `pnpm --filter @dorkos/server build` copies all three into `dist/core-extensions/`.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Moving Hello World and Linear Loop into core-extensions and deleting examples",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "3.5"],
      "parallelWith": ["5.1"]
    },
    {
      "id": "6.1",
      "phase": 6,
      "phaseName": "Docs + hand-edit warning",
      "subject": "[core-extensions] [P6] Add config-load hand-edit warning for default-on core ids in enabled",
      "description": "Add a cheap, honest-by-design guardrail: on config load/validation, if any DEFAULT-ON core extension id appears in `extensions.enabled`, log a one-line warning (that entry is a no-op; the user likely meant to remove it from `disabled`).\n\nWhere: this check needs the core map (which IDs are default-on), so the most natural place is right after `ensureCoreExtensions()` returns the `CoreExtensionInfo[]` and before/at the first manager reload — i.e. in `apps/server/src/index.ts` near the staging call site, or as a small helper invoked there. (The config-manager itself does not know about core defaults, so do not put it inside `config-manager.ts`.)\n\nLogic:\n- For each `info` in the staged `CoreExtensionInfo[]` where `info.defaultEnabled === true`: if `configManager.get('extensions').enabled.includes(info.id)`, log a single-line warning, e.g.:\n  `logger.warn('Extension \"%s\" is default-on; listing it in extensions.enabled is a no-op. To disable it, add it to extensions.disabled instead.', info.id)`\n- One line per offending id. Do not mutate config (just warn) — the resolution helper already ignores the redundant entry.\n\nNotes:\n- This is a no-op for the common case (most users never hand-edit). It only fires for hand-editors who put a default-on id in the wrong list (research pitfall #1).\n- Use the project logger (`@dorkos/shared/logger` or the server's logger convention — match the surrounding code).\n\nTests:\n- Unit test the warning helper: given a core map with a default-on id and a config whose `enabled` contains that id → warning logged once. Given the same id only in `disabled` → no warning. Given a default-off core id in `enabled` → no warning (that is the correct opt-in path). Spy on the logger and restore it in `afterEach` (per testing rules — never leave console/logger mocks without cleanup).\n\nAcceptance criteria:\n- [ ] A config-load warning fires once per default-on core id found in `extensions.enabled`, with a clear one-line message pointing to `extensions.disabled`.\n- [ ] No warning for default-off core ids or for ids only in `disabled`.\n- [ ] Config is not mutated by the warning.\n- [ ] Warning helper has a unit test with logger spy + cleanup.\n- [ ] `pnpm test -- --run` passes (heed `reference_vitest_dev_env_gotcha`; build packages before bare vitest).",
      "activeForm": "Adding the config-load hand-edit warning for misplaced default-on core ids",
      "size": "small",
      "priority": "medium",
      "dependencies": ["3.5", "5.1"],
      "parallelWith": ["6.2"]
    },
    {
      "id": "6.2",
      "phase": 6,
      "phaseName": "Docs + hand-edit warning",
      "subject": "[core-extensions] [P6] Rewrite extension-authoring + configuration docs for Core Extensions",
      "description": "Update the documentation to match the now-real Core Extensions behavior and the new config model. This corrects existing drift (the docs currently describe a `stageBuiltinExtension()` method that does not exist and claim built-ins are \"always enabled\").\n\n1. `contributing/extension-authoring.md`: rewrite the \"Built-in Extensions\" section as \"Core Extensions\".\n   - Remove the phantom `extensionManager.stageBuiltinExtension()` reference and the \"do not appear as user-togglable items / are always enabled\" claim — neither is true.\n   - Describe the real model: core extensions live in `apps/server/src/core-extensions/<id>/`, are staged at startup by `ensureCoreExtensions()` into `{dorkHome}/extensions/`, appear in Settings under \"Core extensions\" with working toggles, and declare `defaultEnabled` / `canDisable` in their `extension.json`.\n   - Update the Quick Start: it currently says to copy `examples/…` to `~/.dork/extensions/`. That folder is gone. Instead reference the shipped Hello World core extension (`apps/server/src/core-extensions/hello-world/`) as the canonical authoring skeleton, and the scaffolding flow for new user extensions.\n   - Document `defaultEnabled` (omitted/true = ships on, false = ships off) and `canDisable` (default true; false = locked on, no toggle) manifest fields.\n\n2. `contributing/configuration.md`: document the new `extensions.disabled` field and the deviation-list mental model — `enabled` records overrides for default-off extensions (opt-in); `disabled` records overrides for default-on extensions (opt-out). Note the hand-edit caveat (a default-on id in `enabled` is a no-op; use `disabled` to turn it off).\n\n3. `docs/getting-started/configuration.mdx`: same — document `extensions.disabled` and the deviation-list model for end users (Fumadocs-rendered).\n\nNotes:\n- Historical spec docs that mention `examples/extensions/` are LEFT AS-IS (point-in-time records). Do not rewrite specs/research files.\n- This completes the `adding-config-fields` lifecycle's docs step started in phase 2.\n- Match the existing doc structure/voice (brand voice: confident, minimal, technical) — do not introduce hype language.\n\nAcceptance criteria:\n- [ ] `contributing/extension-authoring.md` \"Built-in Extensions\" section is rewritten as \"Core Extensions\" with the real staging model, no `stageBuiltinExtension`/\"always enabled\" claims, an updated Quick Start pointing at the Hello World core extension, and documented `defaultEnabled`/`canDisable` fields.\n- [ ] `contributing/configuration.md` documents `extensions.disabled` + the deviation-list model + the hand-edit caveat.\n- [ ] `docs/getting-started/configuration.mdx` documents `extensions.disabled` + the deviation-list model.\n- [ ] No specs/research files were modified.\n- [ ] `grep -rn 'stageBuiltinExtension' contributing docs` returns no results.",
      "activeForm": "Rewriting extension-authoring and configuration docs for Core Extensions",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["5.2"],
      "parallelWith": ["6.1"]
    }
  ]
}
