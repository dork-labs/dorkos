{
  "spec": "specs/relay-inbox-lifecycle/02-specification.md",
  "slug": "relay-inbox-lifecycle",
  "generatedAt": "2026-03-05T00:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Endpoint Type Metadata",
      "subject": "[relay-inbox-lifecycle] [P1] Add EndpointType and inferEndpointType to packages/relay/src/types.ts",
      "description": "Add the `EndpointType` union type and `inferEndpointType()` pure function to `packages/relay/src/types.ts`, appended after the `DeliveryResult` interface.\n\nThe spec instructs:\n\n```typescript\n/** Categorization of a Relay endpoint by subject prefix. */\nexport type EndpointType = 'dispatch' | 'query' | 'persistent' | 'agent' | 'unknown';\n\n/**\n * Derive the logical type of a Relay endpoint from its subject prefix.\n *\n * Mirrors the prefix-matching convention used in ClaudeCodeAdapter and\n * throughout the subject hierarchy. Zero schema change — type is never stored.\n *\n * @param subject - The endpoint's full subject string\n */\nexport function inferEndpointType(subject: string): EndpointType {\n  if (subject.startsWith('relay.inbox.dispatch.')) return 'dispatch';\n  if (subject.startsWith('relay.inbox.query.'))    return 'query';\n  if (subject.startsWith('relay.inbox.'))           return 'persistent';\n  if (subject.startsWith('relay.agent.'))           return 'agent';\n  return 'unknown';\n}\n```\n\nAppend this block at the end of `packages/relay/src/types.ts`, after the `DeliveryResult` interface (which currently ends around line 366).\n\nAlso export `inferEndpointType` from `packages/relay/src/index.ts`. Currently the index exports types from `types.ts` but does not export functions from it. Add:\n\n```typescript\nexport { inferEndpointType } from './types.js';\nexport type { EndpointType } from './types.js';\n```\n\nAcceptance criteria:\n- `inferEndpointType('relay.inbox.dispatch.abc')` returns `'dispatch'`\n- `inferEndpointType('relay.inbox.query.abc')` returns `'query'`\n- `inferEndpointType('relay.inbox.myagent')` returns `'persistent'`\n- `inferEndpointType('relay.agent.lifeOS')` returns `'agent'`\n- `inferEndpointType('relay.human.console.x')` returns `'unknown'`\n- `inferEndpointType('relay.system.pulse.x')` returns `'unknown'`\n- `pnpm typecheck` passes with no errors",
      "activeForm": "Adding EndpointType union and inferEndpointType utility to relay types",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Endpoint Type Metadata",
      "subject": "[relay-inbox-lifecycle] [P1] Extend RelayOptions with TTL fields in packages/relay/src/types.ts",
      "description": "Extend the `RelayOptions` interface in `packages/relay/src/types.ts` with two new optional fields for dispatch inbox TTL configuration. These fields will be consumed by the TTL sweeper added to `RelayCore` in task 2.1.\n\nAdd the following fields inside the `RelayOptions` interface (after the `adapterContextBuilder` field, currently the last field ending around line 171):\n\n```typescript\nexport interface RelayOptions {\n  // ... all existing fields unchanged ...\n\n  /**\n   * TTL for dispatch inboxes in milliseconds.\n   * Dispatch inboxes older than this are swept automatically.\n   * Default: 30 * 60 * 1000 (30 minutes)\n   */\n  dispatchInboxTtlMs?: number;\n\n  /**\n   * Interval between TTL sweep runs in milliseconds.\n   * Default: 5 * 60 * 1000 (5 minutes)\n   */\n  ttlSweepIntervalMs?: number;\n}\n```\n\nThis is a non-breaking additive change — all existing callers that construct `RelayCore` with `RelayOptions` continue to work unchanged because both fields are optional.\n\nAcceptance criteria:\n- `RelayOptions` compiles with both fields present as optional\n- Passing `{ dispatchInboxTtlMs: 100, ttlSweepIntervalMs: 50 }` to `RelayCore` does not cause TypeScript errors\n- `pnpm typecheck` passes",
      "activeForm": "Extending RelayOptions interface with TTL sweep fields",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Server-Side TTL Sweeper",
      "subject": "[relay-inbox-lifecycle] [P2] Add TTL sweeper and getDispatchInboxTtlMs to RelayCore",
      "description": "Implement the dispatch inbox TTL sweeper inside `packages/relay/src/relay-core.ts`. This prevents dispatch inboxes from leaking indefinitely when callers crash or omit cleanup.\n\n**Step 1 — Add import** at the top of `relay-core.ts`:\n\n```typescript\nimport { inferEndpointType } from './types.js';\n```\n\n(Place near the other local imports such as `validateSubject`, `matchesPattern` etc.)\n\n**Step 2 — Add three private fields** to the `RelayCore` class (after the existing `configWatcher` field at line ~153):\n\n```typescript\nprivate readonly dispatchInboxTtlMs: number;\nprivate readonly ttlSweepIntervalMs: number;\nprivate ttlSweepInterval?: ReturnType<typeof setInterval>;\n```\n\n**Step 3 — Initialize in constructor** (after `this.startConfigWatcher()` call, around line 234):\n\n```typescript\nthis.dispatchInboxTtlMs = options?.dispatchInboxTtlMs ?? 30 * 60 * 1000;\nthis.ttlSweepIntervalMs = options?.ttlSweepIntervalMs ?? 5 * 60 * 1000;\nthis.startTtlSweeper();\n```\n\n**Step 4 — Add `startTtlSweeper()` private method** (add in the Private Helpers section at the bottom of the class):\n\n```typescript\n/**\n * Start the periodic TTL sweeper for dispatch inboxes.\n *\n * Runs every `ttlSweepIntervalMs`. Checks all registered endpoints;\n * unregisters dispatch inboxes older than `dispatchInboxTtlMs`.\n * Uses `.unref()` so the timer does not prevent process exit.\n *\n * Race safety: `unregisterEndpoint()` returns `false` gracefully for\n * already-removed endpoints (e.g., caller cleaned up between sweeps).\n */\nprivate startTtlSweeper(): void {\n  this.ttlSweepInterval = setInterval(async () => {\n    const now = Date.now();\n    for (const endpoint of this.endpointRegistry.listEndpoints()) {\n      if (inferEndpointType(endpoint.subject) === 'dispatch') {\n        const age = now - new Date(endpoint.registeredAt).getTime();\n        if (age > this.dispatchInboxTtlMs) {\n          await this.unregisterEndpoint(endpoint.subject).catch(() => undefined);\n        }\n      }\n    }\n  }, this.ttlSweepIntervalMs);\n  // Don't prevent process exit if close() is not called\n  this.ttlSweepInterval.unref();\n}\n```\n\n**Step 5 — Add public accessor** (add in the Query Facade section of `RelayCore`, near `listEndpoints()`):\n\n```typescript\n/** Returns the configured dispatch inbox TTL in milliseconds. */\ngetDispatchInboxTtlMs(): number {\n  return this.dispatchInboxTtlMs;\n}\n```\n\n**Step 6 — Update `close()` method** — add TTL sweeper teardown as the FIRST line of the method body (before `this.subscriptionRegistry.clear()`):\n\n```typescript\nasync close(): Promise<void> {\n  if (this.closed) return;\n  this.closed = true;\n\n  // Stop TTL sweeper first to prevent sweep races during shutdown\n  if (this.ttlSweepInterval) {\n    clearInterval(this.ttlSweepInterval);\n    this.ttlSweepInterval = undefined;\n  }\n\n  // ... rest of existing close() body unchanged ...\n}\n```\n\nAcceptance criteria:\n- `RelayCore` constructed without options still starts the sweeper with 30-min TTL and 5-min sweep interval\n- `RelayCore` constructed with `{ dispatchInboxTtlMs: 100, ttlSweepIntervalMs: 50 }` uses the provided values\n- `getDispatchInboxTtlMs()` returns the configured value\n- Calling `close()` clears the interval without error\n- The interval is created with `.unref()` so tests do not hang\n- `pnpm typecheck` passes",
      "activeForm": "Implementing TTL sweeper and accessor in RelayCore",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": []
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Server-Side TTL Sweeper",
      "subject": "[relay-inbox-lifecycle] [P2] Write TTL sweeper integration test in relay-cca-roundtrip.test.ts",
      "description": "Add the TTL sweeper integration test to `packages/relay/src/__tests__/relay-cca-roundtrip.test.ts`. The test uses `vi.useFakeTimers()` to avoid real delays.\n\nAdd this test inside the existing `describe` block, after the existing tests:\n\n```typescript\nit('TTL sweeper unregisters dispatch inboxes after configured TTL', async () => {\n  // Purpose: guard against TTL sweeper regression — dispatch inboxes must auto-expire.\n  // Uses vi.useFakeTimers() to avoid real delays.\n  vi.useFakeTimers();\n\n  const shortRelay = new RelayCore({\n    dataDir: path.join(tmpDir, 'ttl-test'),\n    dispatchInboxTtlMs: 100,       // 100ms TTL\n    ttlSweepIntervalMs: 50,        // 50ms sweep\n    adapterRegistry: new SingleAdapterRegistry(cca),\n  });\n\n  await shortRelay.registerEndpoint('relay.inbox.dispatch.ttl-test-uuid');\n  expect(shortRelay.listEndpoints()).toHaveLength(1);\n\n  // Advance time past TTL + sweep interval\n  await vi.advanceTimersByTimeAsync(200);\n\n  expect(shortRelay.listEndpoints()).toHaveLength(0);\n\n  await shortRelay.close();\n  vi.useRealTimers();\n});\n```\n\nAlso add the relay_send_and_wait end-to-end test for progress accumulation at the subscribe level:\n\n```typescript\nit('relay_send_and_wait resolves with populated progress array for CCA progress streaming', async () => {\n  // Purpose: end-to-end guard for relay_send_and_wait Phase 3 enhancement.\n  // relay_send_and_wait must accumulate progress events from query inbox and return them\n  // in the response, not prematurely resolve on the first progress event.\n\n  // This test validates the subscribe-level behavior by using RelayCore directly.\n  // Register a query inbox and simulate the message flow that relay_send_and_wait uses.\n  const inboxSubject = 'relay.inbox.query.e2e-test';\n  await relay.registerEndpoint(inboxSubject);\n\n  const progressEvents: unknown[] = [];\n  let finalPayload: unknown;\n\n  relay.subscribe(inboxSubject, (envelope) => {\n    const payload = envelope.payload as Record<string, unknown>;\n    if (payload?.type === 'progress' && payload?.done === false) {\n      progressEvents.push(payload);\n    } else {\n      finalPayload = payload;\n    }\n  });\n\n  // Simulate CCA publishing: 2 progress events + final agent_result\n  await relay.publish(inboxSubject, { type: 'progress', step: 1, step_type: 'message', text: 'step1', done: false }, { from: 'relay.agent.cca' });\n  await relay.publish(inboxSubject, { type: 'progress', step: 2, step_type: 'tool_result', text: 'tool output', done: false }, { from: 'relay.agent.cca' });\n  await relay.publish(inboxSubject, { type: 'agent_result', text: 'Final answer', done: true }, { from: 'relay.agent.cca' });\n\n  await new Promise((resolve) => setTimeout(resolve, 30));\n\n  expect(progressEvents).toHaveLength(2);\n  expect(finalPayload).toMatchObject({ type: 'agent_result', done: true });\n\n  await relay.unregisterEndpoint(inboxSubject);\n});\n```\n\nAcceptance criteria:\n- TTL sweeper test passes without real delays\n- relay_send_and_wait progress accumulation test passes\n- `pnpm vitest run packages/relay/src/__tests__/relay-cca-roundtrip.test.ts` exits green",
      "activeForm": "Writing TTL sweeper and relay_send_and_wait integration tests",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "relay_send_and_wait Progress Aggregation",
      "subject": "[relay-inbox-lifecycle] [P3] Update createRelayQueryHandler to accumulate progress events",
      "description": "Update `createRelayQueryHandler` in `apps/server/src/services/core/mcp-tools/relay-tools.ts` to accumulate `type:progress, done:false` events without resolving, and include them in the final return value.\n\n**Step 1 — Add import** at the top of `relay-tools.ts`:\n\n```typescript\nimport type { RelayProgressPayload } from '@dorkos/shared/relay-schemas';\n```\n\n**Step 2 — Replace the Promise block** (currently lines ~159–180, the block that starts `const reply = await new Promise<{ payload: unknown; from: string; id: string }>`):\n\nReplace the existing Promise block with this progress-accumulating version:\n\n```typescript\nconst progressEvents: RelayProgressPayload[] = [];\n\nconst reply = await new Promise<{\n  payload: unknown;\n  progress: RelayProgressPayload[];\n  from: string;\n  id: string;\n}>((resolve, reject) => {\n  let cleanup: () => void = () => {};\n\n  const timer = setTimeout(() => {\n    cleanup();\n    reject(new Error(`relay_send_and_wait timed out after ${timeoutMs}ms (sent ${sentMessageId})`));\n  }, timeoutMs);\n\n  const unsub = relay.subscribe(inboxSubject, (envelope) => {\n    const payload = envelope.payload as Record<string, unknown>;\n\n    // Accumulate progress events (type:progress, done:false) without resolving\n    if (payload?.type === 'progress' && payload?.done === false) {\n      progressEvents.push(payload as RelayProgressPayload);\n      return;\n    }\n\n    // Any final message (agent_result with done:true, or plain payload for non-CCA compat)\n    cleanup();\n    resolve({ payload, progress: progressEvents, from: envelope.from, id: envelope.id });\n  });\n\n  cleanup = () => {\n    clearTimeout(timer);\n    unsub();\n  };\n});\n```\n\n**Step 3 — Update the return statement** (currently `return jsonContent({ reply: reply.payload, from: reply.from, replyMessageId: reply.id, sentMessageId });`):\n\n```typescript\nreturn jsonContent({\n  reply: reply.payload,\n  progress: reply.progress,\n  from: reply.from,\n  replyMessageId: reply.id,\n  sentMessageId,\n});\n```\n\n**Step 4 — Update the tool description** for `relay_send_and_wait` in `getRelayTools()` (around line 338, the string passed as second argument to `tool('relay_send_and_wait', ...)`):\n\n```typescript\ntool(\n  'relay_send_and_wait',\n  'Send a message to an agent and WAIT for the reply in a single call. Preferred over ' +\n  'relay_send + relay_inbox polling for request/reply patterns. Internally registers an ' +\n  'ephemeral inbox, sends the message with replyTo set, and blocks until the target agent ' +\n  'replies or the timeout elapses.\\n\\n' +\n  'Response: { reply, progress, from, replyMessageId, sentMessageId }\\n' +\n  '  reply: the agent\\'s final response payload\\n' +\n  '  progress: array of intermediate steps (populated for CCA agents on multi-step tasks;\\n' +\n  '            empty for non-CCA or instant responses)\\n' +\n  '            Each step: { type: \"progress\", step, step_type: \"message\"|\"tool_result\", text, done: false }',\n  { ... }, // params unchanged\n  createRelayQueryHandler(deps)\n),\n```\n\nBackward compatibility: Existing callers that destructure only `{ reply, from, replyMessageId }` from `relay_send_and_wait` results are unaffected — `progress` is an additive new field.\n\nAcceptance criteria:\n- When the mock subscribe handler emits 2 progress events then an `agent_result`, the returned `progress` array has length 2 and `reply` contains the `agent_result` payload\n- When the mock subscribe handler emits a plain `{ text: 'hello' }` payload (no `type:progress`), `progress` is an empty array and `reply` is the plain payload\n- The `finally` block still calls `relay.unregisterEndpoint(inboxSubject)` for cleanup in all paths\n- `pnpm vitest run apps/server/src/services/core/__tests__/` passes",
      "activeForm": "Updating relay_send_and_wait handler to accumulate and return progress events",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "relay_send_and_wait Progress Aggregation",
      "subject": "[relay-inbox-lifecycle] [P3] Write relay_send_and_wait progress unit tests in relay-tools.test.ts",
      "description": "Add unit tests for the updated `createRelayQueryHandler` and `createRelayListEndpointsHandler` to `apps/server/src/services/core/__tests__/relay-tools.test.ts`. If this test file does not exist, create it following the existing test patterns for the `mcp-tools/` directory.\n\nThe tests mock `relayCore` and verify behavior without a real relay instance.\n\n```typescript\ndescribe('relay_list_endpoints with type metadata', () => {\n  it('returns correct type for dispatch, query, persistent, and agent endpoints', async () => {\n    // Purpose: verify inferEndpointType is applied to each endpoint in response.\n    const mockEndpoints = [\n      { subject: 'relay.inbox.dispatch.abc', hash: 'h1', maildirPath: '/tmp/a', registeredAt: new Date().toISOString() },\n      { subject: 'relay.inbox.query.def', hash: 'h2', maildirPath: '/tmp/b', registeredAt: new Date().toISOString() },\n      { subject: 'relay.inbox.myagent', hash: 'h3', maildirPath: '/tmp/c', registeredAt: new Date().toISOString() },\n      { subject: 'relay.agent.lifeOS', hash: 'h4', maildirPath: '/tmp/d', registeredAt: new Date().toISOString() },\n      { subject: 'relay.human.console.x', hash: 'h5', maildirPath: '/tmp/e', registeredAt: new Date().toISOString() },\n    ];\n    const mockRelay = {\n      listEndpoints: vi.fn().mockReturnValue(mockEndpoints),\n      getDispatchInboxTtlMs: vi.fn().mockReturnValue(30 * 60 * 1000),\n    };\n    const handler = createRelayListEndpointsHandler({ relayCore: mockRelay as never });\n    const result = await handler();\n    const parsed = JSON.parse(result.content[0].text);\n    const bySubject = Object.fromEntries(parsed.endpoints.map((e: { subject: string; type: string }) => [e.subject, e.type]));\n    expect(bySubject['relay.inbox.dispatch.abc']).toBe('dispatch');\n    expect(bySubject['relay.inbox.query.def']).toBe('query');\n    expect(bySubject['relay.inbox.myagent']).toBe('persistent');\n    expect(bySubject['relay.agent.lifeOS']).toBe('agent');\n    expect(bySubject['relay.human.console.x']).toBe('unknown');\n  });\n\n  it('returns expiresAt ISO string for dispatch endpoints and null for others', async () => {\n    // Purpose: verify TTL transparency field computation.\n    const registeredAt = new Date('2026-03-05T10:00:00.000Z').toISOString();\n    const ttlMs = 30 * 60 * 1000;\n    const mockEndpoints = [\n      { subject: 'relay.inbox.dispatch.abc', hash: 'h1', maildirPath: '/tmp/a', registeredAt },\n      { subject: 'relay.inbox.query.def', hash: 'h2', maildirPath: '/tmp/b', registeredAt },\n    ];\n    const mockRelay = {\n      listEndpoints: vi.fn().mockReturnValue(mockEndpoints),\n      getDispatchInboxTtlMs: vi.fn().mockReturnValue(ttlMs),\n    };\n    const handler = createRelayListEndpointsHandler({ relayCore: mockRelay as never });\n    const result = await handler();\n    const parsed = JSON.parse(result.content[0].text);\n    const dispatch = parsed.endpoints.find((e: { subject: string }) => e.subject === 'relay.inbox.dispatch.abc');\n    const query = parsed.endpoints.find((e: { subject: string }) => e.subject === 'relay.inbox.query.def');\n    // Dispatch: expiresAt = registeredAt + 30min\n    expect(dispatch.expiresAt).toBe(new Date(new Date(registeredAt).getTime() + ttlMs).toISOString());\n    expect(query.expiresAt).toBeNull();\n  });\n});\n\ndescribe('relay_send_and_wait progress accumulation', () => {\n  it('accumulates progress events and returns them in progress array', async () => {\n    // Purpose: verify progress[] is populated when progress events precede agent_result.\n    const progressPayload1 = { type: 'progress', step: 1, step_type: 'message', text: 'Analyzing...', done: false };\n    const progressPayload2 = { type: 'progress', step: 2, step_type: 'tool_result', text: 'file contents', done: false };\n    const finalPayload = { type: 'agent_result', text: 'Done.', done: true };\n\n    const mockRelay = {\n      registerEndpoint: vi.fn().mockResolvedValue({}),\n      publish: vi.fn().mockResolvedValue({ messageId: 'msg-1', deliveredTo: 1 }),\n      subscribe: vi.fn().mockImplementation((_subject: string, handler: (env: unknown) => void) => {\n        // Simulate async delivery of progress events then final result\n        setTimeout(() => handler({ payload: progressPayload1, from: 'relay.agent.b', id: 'e1' }), 5);\n        setTimeout(() => handler({ payload: progressPayload2, from: 'relay.agent.b', id: 'e2' }), 10);\n        setTimeout(() => handler({ payload: finalPayload, from: 'relay.agent.b', id: 'e3' }), 15);\n        return vi.fn(); // unsub\n      }),\n      unregisterEndpoint: vi.fn().mockResolvedValue(true),\n    };\n    const handler = createRelayQueryHandler({ relayCore: mockRelay as never });\n    const result = await handler({\n      to_subject: 'relay.agent.b',\n      payload: { task: 'do work' },\n      from: 'relay.agent.a',\n      timeout_ms: 5000,\n    });\n    const parsed = JSON.parse(result.content[0].text);\n    expect(parsed.progress).toHaveLength(2);\n    expect(parsed.progress[0]).toMatchObject({ type: 'progress', step: 1 });\n    expect(parsed.reply).toMatchObject({ type: 'agent_result', done: true });\n  });\n\n  it('returns empty progress array when first message is non-progress (non-CCA compat)', async () => {\n    // Purpose: backward compat — agents that reply with a plain payload still resolve correctly.\n    const plainPayload = { text: 'hello' };\n\n    const mockRelay = {\n      registerEndpoint: vi.fn().mockResolvedValue({}),\n      publish: vi.fn().mockResolvedValue({ messageId: 'msg-1', deliveredTo: 1 }),\n      subscribe: vi.fn().mockImplementation((_subject: string, handler: (env: unknown) => void) => {\n        setTimeout(() => handler({ payload: plainPayload, from: 'relay.agent.b', id: 'e1' }), 5);\n        return vi.fn();\n      }),\n      unregisterEndpoint: vi.fn().mockResolvedValue(true),\n    };\n    const handler = createRelayQueryHandler({ relayCore: mockRelay as never });\n    const result = await handler({\n      to_subject: 'relay.agent.b',\n      payload: { task: 'quick question' },\n      from: 'relay.agent.a',\n      timeout_ms: 5000,\n    });\n    const parsed = JSON.parse(result.content[0].text);\n    expect(parsed.progress).toEqual([]);\n    expect(parsed.reply).toEqual(plainPayload);\n  });\n});\n```\n\nAcceptance criteria:\n- All 4 new tests pass\n- `pnpm vitest run apps/server/src/services/core/__tests__/` exits green",
      "activeForm": "Writing relay_send_and_wait progress and relay_list_endpoints unit tests",
      "size": "medium",
      "priority": "high",
      "dependencies": ["3.1"],
      "parallelWith": []
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "CCA Broadens Streaming to All Inbox ReplyTos",
      "subject": "[relay-inbox-lifecycle] [P4] Update ClaudeCodeAdapter to stream progress for all relay.inbox.* replyTos",
      "description": "Update `packages/relay/src/adapters/claude-code-adapter.ts` `handleAgentMessage()` to send progress events to query inboxes, replacing the previous dispatch-only branching with a unified `isInboxReplyTo` check.\n\nLocate the section starting around line 422–429 (the comment block and variable declarations for `isDispatchInbox`/`isQueryInbox`) and apply these changes:\n\n**Step 1 — Replace the two `isDispatchInbox`/`isQueryInbox` variable declarations** with a single `isInboxReplyTo`:\n\n```typescript\n// Before (lines ~427–429):\nconst isDispatchInbox = envelope.replyTo?.startsWith('relay.inbox.dispatch.');\n// Non-dispatch inbox replyTo (relay.inbox.query.* etc.) → existing aggregated behavior\nconst isQueryInbox = envelope.replyTo?.startsWith('relay.inbox.') && !isDispatchInbox;\n\n// After:\n// All relay.inbox.* replyTos now receive full streaming (progress events + final agent_result).\n// relay_send_and_wait accumulates progress internally and returns a single MCP response.\nconst isInboxReplyTo = envelope.replyTo?.startsWith('relay.inbox.');\n```\n\n**Step 2 — Replace the streaming loop branching** (the `if (isDispatchInbox) { ... } else if (isQueryInbox) { ... } else { ... }` block inside the `for await` loop, lines ~444–475):\n\n```typescript\nif (isInboxReplyTo) {\n  // All relay.inbox.* — same progress streaming as dispatch (formerly dispatch-only)\n  if (event.type === 'text_delta') {\n    const data = event.data as { text: string };\n    messageBuffer += data.text;\n    collectedText += data.text;\n  }\n  if (event.type === 'tool_call_start' && messageBuffer) {\n    stepCounter++;\n    await this.publishDispatchProgress(envelope, stepCounter, 'message', messageBuffer, ccaSessionKey);\n    messageBuffer = '';\n  }\n  if (event.type === 'tool_result') {\n    stepCounter++;\n    const data = event.data as { content?: string; tool_use_id?: string };\n    const text = typeof data.content === 'string' ? data.content : JSON.stringify(data);\n    await this.publishDispatchProgress(envelope, stepCounter, 'tool_result', text, ccaSessionKey);\n  }\n} else {\n  // relay.agent.*, relay.human.* — existing raw event streaming (unchanged)\n  await this.publishResponse(envelope, event, ccaSessionKey);\n}\n```\n\n**Step 3 — Replace the post-loop block** (currently has two separate `if (isDispatchInbox ...)` and `if (isQueryInbox ...)` blocks, lines ~478–490):\n\n```typescript\n// After loop — flush and publish final result for all relay.inbox.* replyTos\nif (isInboxReplyTo && envelope.replyTo && this.relay) {\n  if (messageBuffer) {\n    stepCounter++;\n    await this.publishDispatchProgress(envelope, stepCounter, 'message', messageBuffer, ccaSessionKey);\n  }\n  await this.publishAgentResult(envelope, collectedText, ccaSessionKey);\n}\n```\n\nNote: `publishDispatchProgress` already uses `envelope.replyTo` as the publish target, so the method name's 'dispatch' label does not restrict it to dispatch inboxes only — it works correctly for any `relay.inbox.*` replyTo.\n\nAlso update the comment block at lines ~422–426 to reflect the new unified behavior:\n\n```typescript\n// All relay.inbox.* replyTos (dispatch, query, persistent) receive full streaming:\n// incremental progress events + final agent_result.\n// relay_send_and_wait accumulates progress internally and presents a single MCP response.\n// Other addresses (relay.human.console.*, relay.agent.*) receive raw event streaming.\n```\n\nAcceptance criteria:\n- `isDispatchInbox` and `isQueryInbox` variables no longer exist in the file\n- `isInboxReplyTo` is used in the loop and post-loop blocks\n- `pnpm typecheck` passes\n- `pnpm vitest run packages/relay/src/__tests__/relay-cca-roundtrip.test.ts` passes (after task 4.2 updates the existing backward-compat test)",
      "activeForm": "Updating ClaudeCodeAdapter to unify inbox streaming for all relay.inbox.* replyTos",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "2.1"],
      "parallelWith": []
    },
    {
      "id": "4.2",
      "phase": 4,
      "phaseName": "CCA Broadens Streaming to All Inbox ReplyTos",
      "subject": "[relay-inbox-lifecycle] [P4] Update existing backward-compat test in relay-cca-roundtrip.test.ts",
      "description": "The test at line 262 of `packages/relay/src/__tests__/relay-cca-roundtrip.test.ts` — `'still publishes single agent_result for relay.inbox.query.* replyTo (backward compat)'` — asserts that query inboxes receive exactly one message with no progress events. After Phase 4 (task 4.1), this assertion is incorrect: query inboxes now receive progress events followed by `agent_result`, matching dispatch inbox behavior.\n\nRename the test and update its assertions:\n\n```typescript\nit('publishes progress events followed by agent_result for relay.inbox.query.* replyTo', async () => {\n  // Purpose: verify Phase 4 behavior — query inboxes now receive full streaming.\n  // This replaces the old \"backward compat\" guard which was for spec #91 only.\n  await relay.registerEndpoint('relay.inbox.query.existing-test');\n\n  const receivedPayloads: unknown[] = [];\n  relay.subscribe('relay.inbox.query.existing-test', (envelope) => {\n    receivedPayloads.push(envelope.payload);\n  });\n\n  vi.mocked(agentManager.sendMessage).mockReturnValue(\n    (async function* () {\n      yield { type: 'text_delta', data: { text: 'Thinking...' } } as StreamEvent;\n      yield { type: 'tool_call_start', data: { tool_use_id: 'tu1', name: 'Read' } } as StreamEvent;\n      yield { type: 'tool_result', data: { tool_use_id: 'tu1', content: 'file' } } as StreamEvent;\n      yield { type: 'text_delta', data: { text: 'Done.' } } as StreamEvent;\n      yield { type: 'done', data: {} } as StreamEvent;\n    })(),\n  );\n\n  await relay.publish(\n    'relay.agent.lifeOS-session',\n    { text: 'question' },\n    { from: 'relay.agent.sender', replyTo: 'relay.inbox.query.existing-test' },\n  );\n\n  await new Promise((resolve) => setTimeout(resolve, 50));\n\n  const types = receivedPayloads.map((p) => (p as Record<string, unknown>).type);\n  expect(types).toContain('progress');\n  expect(types[types.length - 1]).toBe('agent_result');\n});\n```\n\nThe old test asserted:\n- `receivedPayloads` has length 1\n- No progress events (`hasProgress === false`)\n\nThe new test asserts:\n- At least one `progress` type is present\n- The last message type is `agent_result`\n\nThis matches the actual behavior after the Phase 4 change.\n\nAcceptance criteria:\n- The renamed test passes with the new assertions\n- No other tests in the file are broken\n- `pnpm vitest run packages/relay/src/__tests__/relay-cca-roundtrip.test.ts` exits green",
      "activeForm": "Updating backward-compat test to reflect new query inbox streaming behavior",
      "size": "small",
      "priority": "high",
      "dependencies": ["4.1"],
      "parallelWith": []
    },
    {
      "id": "5.1",
      "phase": 5,
      "phaseName": "Update relay_list_endpoints handler with type metadata",
      "subject": "[relay-inbox-lifecycle] [P5] Update createRelayListEndpointsHandler to include type and expiresAt fields",
      "description": "Update `createRelayListEndpointsHandler` in `apps/server/src/services/core/mcp-tools/relay-tools.ts` to call `inferEndpointType` and compute `expiresAt` for each endpoint.\n\n**Step 1 — Import** `inferEndpointType` at the top of `relay-tools.ts`:\n\n```typescript\nimport { inferEndpointType } from '@dorkos/relay';\n```\n\n(Confirm this function is exported from `packages/relay/src/index.ts` — task 1.1 adds it. If the import is unavailable before task 1.1 is merged, ensure proper sequencing.)\n\n**Step 2 — Replace the handler body**:\n\n```typescript\n/** List all registered Relay endpoints with type and TTL metadata. */\nexport function createRelayListEndpointsHandler(deps: McpToolDeps) {\n  return async () => {\n    const err = requireRelay(deps);\n    if (err) return err;\n    const relay = deps.relayCore!;\n    const endpoints = relay.listEndpoints();\n    const dispatchTtlMs = relay.getDispatchInboxTtlMs();\n    const typed = endpoints.map((ep) => {\n      const type = inferEndpointType(ep.subject);\n      const expiresAt =\n        type === 'dispatch'\n          ? new Date(new Date(ep.registeredAt).getTime() + dispatchTtlMs).toISOString()\n          : null;\n      return { ...ep, type, expiresAt };\n    });\n    return jsonContent({ endpoints: typed, count: typed.length });\n  };\n}\n```\n\n**Step 3 — Update the tool description** for `relay_list_endpoints` in `getRelayTools()` (currently the string `'List all registered Relay endpoints.'`):\n\n```typescript\ntool(\n  'relay_list_endpoints',\n  'List all registered Relay endpoints. Each endpoint includes subject, hash, maildirPath, ' +\n  'registeredAt, type (\\'dispatch\\'|\\'query\\'|\\'persistent\\'|\\'agent\\'|\\'unknown\\'), and expiresAt ' +\n  '(ISO timestamp for dispatch endpoints indicating 30-min TTL expiry; null for others).',\n  {},\n  createRelayListEndpointsHandler(deps)\n),\n```\n\nAcceptance criteria:\n- `relay.listEndpoints()` return value is mapped with `type` and `expiresAt` fields\n- Dispatch endpoints have a non-null `expiresAt` ISO string ≈ `registeredAt + 30min`\n- Non-dispatch endpoints have `expiresAt: null`\n- The `count` field still matches the array length\n- `pnpm typecheck` passes\n- Unit tests from task 3.2 that cover `createRelayListEndpointsHandler` pass",
      "activeForm": "Updating relay_list_endpoints handler to attach type and expiresAt per endpoint",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1", "2.1"],
      "parallelWith": ["3.1"]
    },
    {
      "id": "6.1",
      "phase": 6,
      "phaseName": "context-builder Documentation Update",
      "subject": "[relay-inbox-lifecycle] [P6] Update RELAY_TOOLS_CONTEXT in context-builder.ts",
      "description": "Update `RELAY_TOOLS_CONTEXT` in `apps/server/src/services/core/context-builder.ts` to reflect the three spec enhancements so that agents prompted with this context understand the new capabilities.\n\nLocate the `RELAY_TOOLS_CONTEXT` string constant (currently starting at line 13). Apply three targeted updates:\n\n**Update 1 — relay_send_and_wait workflow block** (currently lines ~26–29):\n\nChange:\n```\nWorkflow: Query another agent — SHORT tasks (≤10 min, PREFERRED)\n1. mesh_list() to find available agents and their agent IDs\n2. relay_send_and_wait(to_subject=\"relay.agent.{theirAgentId}\", payload={task}, from={myAgentId}, timeout_ms=600000)\n   → Blocks until reply (max 10 min / 600 000 ms)\n   → Returns: { reply, from, replyMessageId, sentMessageId }\n```\n\nTo:\n```\nWorkflow: Query another agent — SHORT/MEDIUM tasks (≤10 min, PREFERRED)\n1. mesh_list() to find available agents and their agent IDs\n2. relay_send_and_wait(to_subject=\"relay.agent.{theirAgentId}\", payload={task}, from={myAgentId}, timeout_ms=600000)\n   → Blocks until reply (max 10 min / 600 000 ms)\n   → Returns: { reply, progress, from, replyMessageId, sentMessageId }\n     reply: agent's final response\n     progress: intermediate steps (empty for quick replies; populated for multi-step CCA tasks)\n               Each: { type: \"progress\", step, step_type: \"message\"|\"tool_result\", text, done: false }\n```\n\n**Update 2 — Subject hierarchy** (currently line ~19):\n\nChange:\n```\n  relay.inbox.dispatch.{UUID}          — ephemeral inbox for relay_send_async (caller-managed)\n```\n\nTo:\n```\n  relay.inbox.dispatch.{UUID}          — ephemeral inbox for relay_send_async (caller-managed; server auto-expires after 30 min)\n```\n\n**Update 3 — Add relay_list_endpoints note** (add after the Error codes line, before the closing `</relay_tools>` tag):\n\n```\nrelay_list_endpoints → each endpoint includes: subject, type, expiresAt\n  type: 'dispatch'|'query'|'persistent'|'agent'|'unknown'\n  expiresAt: ISO expiry timestamp for dispatch endpoints; null for others\n```\n\nAcceptance criteria:\n- The `RELAY_TOOLS_CONTEXT` string contains `progress` in the relay_send_and_wait return shape description\n- The dispatch inbox subject line mentions `server auto-expires after 30 min`\n- The `relay_list_endpoints` note with `type` and `expiresAt` is present\n- `pnpm test -- --run` passes (no runtime errors from context-builder)",
      "activeForm": "Updating RELAY_TOOLS_CONTEXT with progress, TTL, and endpoint type documentation",
      "size": "small",
      "priority": "medium",
      "dependencies": ["3.1", "4.1", "5.1"],
      "parallelWith": []
    },
    {
      "id": "7.1",
      "phase": 7,
      "phaseName": "Full Test Suite Verification",
      "subject": "[relay-inbox-lifecycle] [P7] Run full test suite and verify all phases pass",
      "description": "Run the complete test suite to verify all five implementation phases are working correctly and no regressions were introduced.\n\nRun the following verification commands in order:\n\n```bash\n# Phase 1+5 (relay-tools unit tests — type metadata and progress accumulation):\npnpm vitest run apps/server/src/services/core/__tests__/\n\n# Phase 2+4 (relay-core TTL sweeper + CCA roundtrip with query inbox streaming):\npnpm vitest run packages/relay/src/__tests__/relay-cca-roundtrip.test.ts\n\n# Full relay package tests:\npnpm vitest run packages/relay/src/__tests__/\n\n# Full suite:\npnpm test -- --run\n```\n\nExpected outcomes:\n- All new tests pass: TTL sweeper test, relay_send_and_wait progress accumulation (e2e and unit), relay_list_endpoints type/expiresAt tests, updated query-inbox CCA roundtrip test\n- No existing tests are broken\n- TypeScript compilation (`pnpm typecheck`) passes across all packages\n\nIf any test fails, identify the root cause and fix before marking this task complete. Common failure points:\n- `inferEndpointType` not exported from `packages/relay/src/index.ts` (task 1.1)\n- `getDispatchInboxTtlMs()` not declared on `RelayCore` type (task 2.1)\n- Fake timers leaking between tests — ensure `vi.useRealTimers()` in `afterEach` or within the TTL sweeper test's finally block\n- The old backward-compat assertion `expect(receivedPayloads).toHaveLength(1)` still present in the test file (task 4.2)",
      "activeForm": "Running full test suite to verify all relay-inbox-lifecycle changes",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1", "1.2", "2.1", "2.2", "3.1", "3.2", "4.1", "4.2", "5.1", "6.1"],
      "parallelWith": []
    }
  ]
}
