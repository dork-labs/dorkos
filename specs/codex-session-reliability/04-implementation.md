# Reliable Codex sessions: implementation

Status: implementation verified. The final pushed-head review receipt, PR, merge checks, and cleanup are tracked by [DOR-1925](https://linear.app/dorkspace/issue/DOR-1925). The delivery task stays open until those steps finish.

## Scope and isolation

Integration worktree: `/Users/doriancollier/.dork/workspaces/dorkos/codex-codex-session-reliability`, branch `codex/codex-session-reliability`. Investigation base: `52e574e66f2580a65feb217f13d75a4b084840e2`. Delivery base: `341bafdb9a3a916572cebf0a5f81a1ceb11bf88f`. The final integration preserves main’s renewable Connections leases, runtime environment filtering, Harness changes, and completed review-workflow repair. Parallel specification-index additions were preserved.

Three GPT-5.6-sol workers implemented model compatibility, error recovery, and authenticated agent tools in separate worktrees. A separate GPT-5.6-sol reviewer used `REVIEW.md`, the specification, and pushed commits. The operator explicitly authorized autonomous delivery and cleanup. The configured workhorse was unavailable, so GPT-5.6-sol followed the operator’s model preference. Decomposition is recorded directly in canonical JSON because this harness has no Task API projection.

## Findings and resulting behavior

The reported session reproduced two backend rejections: the bundled Codex 0.147.0 was too old for Astra, and its offered GPT-5.4 was unavailable to the ChatGPT account. Its catalog also omitted Astra. Codex 0.153.4 returned the current account’s six visible models, with Astra as default. CLI, SDK, desktop platform packages, provisioning, and the lockfile now move together.

Model discovery uses the resolved executable and current Codex home. It bounds process lifetime, pagination, output size, and optional metadata reads; handles split UTF-8; coalesces requests; and never extends the original five-minute stale deadline or reuses another authentication identity’s catalog. Missing discovery produces an honest unknown result. Effective context limits come from same-version, fresh Codex-owned metadata: Astra is 272,000 × 95% = 258,400; Spark is 128,000 × 95% = 121,600. Optional metadata cannot hold up confirmed model choices. CLI and desktop version identities are both covered. Final browser inspection also caught that the SDK reports cumulative input usage; the runtime now uses the exact native thread’s latest `last_token_usage.total_tokens` and `model_context_window`. Directory discovery, tail size and elapsed time are bounded; active threads are not held up by large archives. This pair survives durable status replay. A missing native reading retains the prior valid reading and never substitutes cumulative SDK input.

Known model rejections now provide a direct model-menu action and explain upgrades, including custom Codex installations. Raw diagnostics remain under Details. Known nonfatal warnings no longer look like failed turns. Creating a Codex agent preserves its runtime in the new session URL, and the greeting waits until sending is actually allowed. Collapsed JSON results retain visible expansion controls and an ellipsis.

With app login enabled, Codex and OpenCode use a private authenticated DorkOS tool endpoint. Each request is bound to its exact runtime, session, agent, and working directory. The agent identity and permission ceiling are snapshotted before launch; concurrent turns keep distinct ceilings; recorded revocation remains authoritative. Mid-turn manifest edits cannot widen that turn’s permissions, while later turns honor current configuration. Public MCP and A2A authentication remain unchanged.

## Verification

- The original real-browser test failed before the upgrade with the reported newer-Codex rejection. The updated test passed against the same account.
- Packaged CLI browser pass after the runtime-security integration: agent creation/greeting and a real Codex start → reply → populated context meter → reload → unchanged meter → remembered follow-up, **2 tests passed in 19.3 seconds**, then **2 passed in 20.2 seconds** after the cache-privacy correction. The test is opt-in for real model turns; ordinary creation coverage needs no Codex installation.
- The originally failed disposable session recovered through Choose model. With local app login enabled, it successfully invoked `dorkos.list_member_rooms({})` and returned `#team`, including after the turn-identity fix, server restart, and runtime-security integration. A thread created on September 8 resumed on September 9 and reported 83,527 current-context tokens / 258,400, while cumulative SDK input was 928,342; the durable status exactly matched the native measurement. No shell fallback or room write was used.
- Desktop and mobile recovery checks passed. The mobile model action opens the current account’s picker. The live model endpoint reports the verified effective context limits.
- Full server suite: **17,355 passed, 42 existing opt-in skips** across 1,013 files. Full client suite: **14,637 passed**, with one lint-guard timeout during heavy machine load. The guard then passed unchanged in the final focused run.
- All 19 other package suites completed successfully across the initial run and the full relay/site reruns. Relay: **1,935 passed, 19 skipped**. Site: **1,071 passed**.
- Final combined runtime and security checks: **584 tests passed, 7 existing opt-in skips** across 30 files. The final native-reader hardening suite passed **12 tests**, independently repeated by the reviewer. Earlier integrated delta and guard: **45 tests passed** across five files. Native context pipeline: **165 tests passed**, including the normalizer/projector cold snapshot. The pinned-main Harness merge: **37 tests passed**. Independent security review: **78 tests passed**; independent context review: **40 tests passed**. Root scripts: **354 tests passed**. SDK lockstep checks: **7 passed**. Final affected lint/typecheck and prerequisite builds: **55 tasks passed** on the combined implementation. The final test-only hardening also passed worker typecheck, lint and formatting. The packaged CLI build passed.

Initial broad runs saturated the shared machine. Two unchanged native watcher tests also reproduced intermittently on pinned main; a fresh `fs.watch()` process reported `EMFILE`. Stopping only the disposable test app released its handles, after which both native tests passed (75 assertions), followed by a clean full relay run. A site migration timeout passed unchanged alone and in the full site rerun. No assertions or timeouts were weakened.

The reviewer deliberately broke permission snapshots, revocation checks, UTF-8 handling, cache deadlines, kickoff provenance, and effective-window calculation; the corresponding tests failed and returned green after restoration. Those findings and comment corrections were resolved. The reviewer then proved the native-reader guards catch an unbounded scan, wrong-thread attribution and missing abort-time handle closure; each deliberate mutation failed, and the restored tests passed. Implementation and test review converged before the final combined pushed-head receipt and PR.

## PR verification follow-through

PR #1741 passed every application check, including all four test shards. Its initial API-reference freshness failure was fixed by regenerating the two new error categories; independent regeneration was byte-identical.

Three automated review attempts exited while background agents were still running. The separately reviewed prerequisite [PR #1743](https://github.com/dork-labs/dorkos/pull/1743) restricted the reviewer’s available built-in tools while retaining the trusted GitHub helper, read restrictions and fail-closed verdict gate. Its 163-check regression suite and independent mutations passed, followed by all PR and merge-queue checks, including the full browser suite. It merged as `94bbbb587bd2a0037e5e24ab1eb846acec165df8`.

An automatic re-review of the older #1741 branch then correctly refused its stale workflow version. The branch was brought onto the merged prerequisite. After refreshing the compiled Harness dependency, combined runtime, security and Harness checks passed **742 tests with 7 existing opt-in skips across 32 files**. The automated deep review then completed with **0 important findings and 0 nits** ([run](https://github.com/dork-labs/dorkos/actions/runs/34354316863)). DOR-1948 is closed; PR #1743 is merged and live-verified.

PR #1744 then landed renewable Connections leases and runtime environment filtering. The combined implementation preserves the exact turn ownership and renewal permit through the identity-snapshot layer, extends only an existing live snapshot, and never refreshes its frozen permission ceiling. Revocation, expiry, refused renewals, and renewal errors cannot revive stale identity. A real private-endpoint test verifies access after renewal. Model discovery now uses the same filtered Codex authentication environment for account-file lookup and subprocess launch; its cache tracks permitted account-file and variable-name identities, without retaining credential values or their hashes. Changed parent environment values follow the documented server-restart contract.

Integration verification passed **77 focused tests** and the server type check, followed by **575 runtime/security tests with 7 existing opt-in skips across 33 files**. The packaged CLI build and API/MDX regeneration passed. Both real-browser tests passed, and a separate authenticated Codex turn called `list_member_rooms` successfully and preserved its context reading across reload. The independent reviewer caught the initial environment-value hash; it was removed to preserve the upstream privacy contract, and 21 catalog/census tests plus server type checking passed. The reviewer also identified a permit-forwarding assertion that needed reference identity rather than deep equality; it was strengthened so replacing the opaque permit fails. Final review, pushed-head CI, merge confirmation, and cleanup remain delivery evidence on DOR-1925.

## Evidence and delivery boundary

The disposable app used development ports 16341/16342 and then the packaged CLI at localhost:16342 and private data below the worktree’s ignored `.dork/flow/codex-session-reliability/`. Operator ports 4242/6242 were untouched. Screenshots, browser recordings, sanitized tool/model receipts, and review evidence are preserved separately before cleanup. Authentication files, cookies, private databases, and browser traces are excluded from exported evidence. Switching to the packaged production-mode app required a fresh disposable login at localhost; the initial development cookie fixture was rejected, and no authentication code was changed.

The code repair does not itself replace the operator’s already-running build. PR checks, merge confirmation, tracker completion, and lossless worktree cleanup are recorded on DOR-1925 rather than claimed in advance here.
