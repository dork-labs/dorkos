# Tasks — sidebar-simplification

Generated from `03-tasks.json` (2026-08-19T21:10:00Z). The JSON is canonical; this file is for browsing. Umbrella: DOR-1365.

## P0 — Honesty fixes

### Task 0.1: Remove the sidebar model's dead glyph kinds, unused row fields and duplicate constants; narrow ConversationSurface

- **Size:** medium · **Priority:** high · **Depends on:** — · **Parallel with:** 0.2
- **Issue:** DOR-1366 (parent DOR-1365)

Design: 02-specification.md §D7. In apps/client/src/layers/features/dashboard-sidebar: (1) remove the `face-stack` and `person-avatar` members of `SidebarIconId` (build-sidebar-model.ts ~272-273) and every producer (library-rows.ts ~100-102, select-today-items.ts ~164-165) — RoomRow derives faces from the item index and GenericRowFromModel never had a branch for them; (2) remove the `SidebarRowModel` fields `origin`, `projectLabel`, `actions`, `status` (build-sidebar-model.ts ~281-317) and their producers in the rules; keep `liveCount` as the ONE consumed field: `AgentListItem` reads `row.liveCount` from the model and stops calling `useLiveSessionCount` per row, `LIVE_CHIP_THRESHOLD` in AgentListItem.tsx is deleted in favour of `LIVE_CHIP_MIN` in library-rows.ts; (3) `SidebarIconId` `discovery` and `digest` collapse to `digest` (SidebarModelRow.tsx ~79-81); (4) SidebarSection.tsx ~25 keys its container by section id (`dms`) not the label string; (5) stale comment SidebarDnd.tsx ~78-80 ('mobile sidebar is a scrollable Sheet') corrected to the truth (no Sheet below 768px, see AppShell); (6) stale `filterSidebarItems` references in sidebar-item.ts ~79,87 deleted; (7) `ConversationSurface` (features/conversation/model/conversation-context.ts ~19) narrows to `'session' | 'room'` and RoomSurface.tsx ~377 writes `'room'` — the `'dm'` value has no readers. Acceptance: `pnpm --filter @dorkos/client typecheck` and `pnpm vitest run apps/client/src/layers/features/dashboard-sidebar` green; `grep -rn "face-stack\|person-avatar\|projectLabel\|LIVE_CHIP_THRESHOLD" apps/client/src` returns nothing outside tests that were updated; the dev playground (`SidebarModelShowcases.tsx` ~199) no longer references `person-avatar`; a unit test asserts `AgentListItem` renders the live chip from `row.liveCount` (≥2) without `useLiveSessionCount`. Changelog fragment with a `covers:` block (this is internal cleanup — a `chore` commit needs no fragment; if any user-visible change is made, write one).

### Task 0.2: Make the lying verbs true and the inert roll-up rows do something (DOR-1105)

- **Size:** small · **Priority:** high · **Depends on:** — · **Parallel with:** 0.1
- **Issue:** DOR-1367 (parent DOR-1365)

Design: 02-specification.md §D2 (verbs) and §D3 (roll-up targets), §D7. (1) Team page `features/agents-list/lib/agent-columns.tsx` ~231-256: the `MessageSquare` action labelled 'Chat with <Name>' opens a SESSION — relabel to 'Open session with <Name>' and use an 'open' icon (e.g. lucide `SquareArrowOutUpRight` or whichever 'open' glyph the design system already uses; grep for an existing one). (2) Profile: `features/profile/ui/ProfileHeader.tsx` ~199-202 button 'Message' → 'Open session' (it navigates to /session via ProfileView.tsx ~113-117; `profile-message.ts` `kind: 'agent-session'` unchanged). (3) Command palette `features/command-palette/ui/RoomCommandItem.tsx` ~54: a DM row reads 'Message Ana' → 'Open conversation with Ana' (it opens an existing room). (4) Roll-up rows: `SidebarChrome.tsx` ~239-244 currently no-ops on `rollup` / `suggestion` / `digest` targets. Give the three that survive this programme real destinations: `automated` → `useTodayRevealStore.getState().toggle()` (expand/collapse Today's automated fold); `working` and `now-overflow` → `navigate({ to: '/' })` (home is #team, where presence shows). `section-count` ('N inactive') is replaced in task 2.2 — leave it inert here but add a code comment pointing at 2.2. (5) `SidebarChrome.tsx` ~408-409 memo deps list `setRightPanelOpen`/`setActiveRightPanelTab` which the memo body never references — remove them. Acceptance: unit tests for each roll-up target's effect (a test that fails if the target is a no-op); `AgentListItem`/`RoomCommandItem`/`ProfileHeader`/`agent-columns` tests updated to the new copy; `apps/e2e` specs that assert 'Chat with' / 'Message' copy updated; typecheck + the affected vitest files green. Changelog fragment (user-visible copy + behaviour) with a `covers:` block listing the exact commit subjects; reference DOR-1105 in the commit subject.

## P1 — Structure and bottom slot

### Task 1.1: Two levels, one header style: geometry tokens, unified folding headers, Library heading removed, DM stack fixed, muted rows legible

- **Size:** xl · **Priority:** high · **Depends on:** 0.1, 0.2 · **Parallel with:** 1.2
- **Issue:** DOR-1368 (parent DOR-1365)

Design: 02-specification.md §D1 (read it fully; the decision record is design-decisions.md §1). Build: (a) three CSS custom properties on the sidebar root — `--sidebar-header-x: 12px`, `--sidebar-row-x: 20px`, `--sidebar-nested-x: 12px` — and derive every left inset from them (`SIDEBAR_ROW_INSET` in shared/ui/sidebar-row.tsx, the glyph-action overlay `left-2`, SidebarZone/SidebarSection paddings); row label lands at 46px, nested rows (a section's members) at header 24 / glyph 32 / label 58. (b) ONE header look for every header — Heads up, Today, Getting started, Pins, Channels, Direct messages, Agents, and groups/sections: `shared/ui/section-header.tsx` renders 11px `font-medium` `text-sidebar-foreground/70`, no icon, h-7; the zone `<h2>` in SidebarZone.tsx ~103-108 is removed and the zone `<section>` gets `aria-label={ZONE_LABEL[zone.id]}` (keep `data-sidebar-zone`; `ZONE_LABEL.library` stays as the AT name, never painted); section-header icons (the `#`, bubble, robot) are removed. (c) Every header folds: click anywhere / Enter / Space toggles; Heads up and Today become collapsible sections with persisted collapse keys — widen `SidebarSectionIdSchema` (packages/shared/src/config-schema.ts ~374-393) with `now` and `today` (the drop-unknown sanitizer at ~417-441 makes this safe; verify with a test) and route their toggles through `setSectionCollapsed`; a folded header shows its roll-up (`roll-up-collapsed-section.ts`) as trailing muted text, and Heads up's roll-up carries the needs-you count; Alt/Option-click on any header folds/unfolds ALL headers (widen `SidebarZones.tsx` ~111-137 from the Library zone to every zone). (d) Hover chrome: chevron + the section `+` appear at the right on hover / focus-within (opacity 0→1, 120 ms); the `+` becomes a roving-focus stop — `use-roving-focus.ts` ~107-113 adds `[data-sidebar-section-action]` to `stopsIn`, and the group-create / rename inputs are excluded from the tabIndex=-1 stamping while mounted (~216-219) — so New channel / New message / New agent / New section are keyboard reachable. (e) Rows: glyph slot `justify-start` (sidebar-row.tsx ~533-539); the agent row moves its face into the glyph slot (AgentListItem.tsx ~189 currently puts `AgentIdentity` in the title slot) and the `xs` disc becomes 18px (identity-avatar.tsx ~112) so `#`, single face and agent face are flush; `AgentIdentity` name variant matches the row's 13px; multi-face DM stack max 2 faces at sidebar size with `-space-x-3.5` (RoomAvatar.tsx ~51,151 — make `maxFaces` and overlap props with these xs defaults). (f) Muted rows (DOR-1098): drop `opacity-60`; a muted row keeps full label contrast and loses bold/badge/dot. (g) Mobile: `mobile-tabs.ts` ~48 tab label 'Library' → 'All' (id stays `library`). (h) Update `apps/e2e/tests/dashboard-sidebar/sidebar-row-gutter.spec.ts` to assert the token-derived computed values, `DashboardSidebar.test.tsx` / `SidebarZones.damping-seam` / `MobileTabsLayout` tests, `library-rules.test.ts`, and the dev playground (`SidebarModelShowcases.tsx`, `SidebarShowcases.tsx`) for the new headers; add a playground state 'Folded headers with roll-ups'. (i) Spec amendments in `specs/sidebar-now-today-library/02-specification.md` (BC-1/BC-2 → headers fold, zones stay landmarks via aria-label; BC-28..33 → no visible Library heading; BC-30 → fold-all widened) and a note in its `design-decisions.md` §2/§8/§9/§11 'superseded by sidebar-simplification D1'. (j) `contributing/design-system.md` documents the three tokens. Acceptance: Playwright gutter spec green with tokens; unit tests prove Heads up/Today fold + persist + roll-up text; a test proves the `+` is reachable by arrow keys; axe contrast on muted rows ≥ 4.5:1 in the playground spec; typecheck/lint/test for client + shared green; browser-verify the panel against a freshly started server (see memory: vite keeps optimized deps in memory) and attach a screenshot to the PR. Changelog fragment with `covers:`.

### Task 1.2: One bottom slot: pinned above the footer, one card at a time, × always, dismissal in config

- **Size:** large · **Priority:** high · **Depends on:** 0.1, 0.2 · **Parallel with:** 1.1
- **Issue:** DOR-1369 (parent DOR-1365)

Design: 02-specification.md §D4 (decision record design-decisions.md §4). Build: (a) `features/dashboard-sidebar/ui/SidebarBottomSlot.tsx` — a sibling of `SidebarContent` inside the sidebar `<nav>` in `DashboardSidebar.tsx` (`shrink-0 px-2 pb-2 empty:p-0`, the precedent is `features/session-list/ui/EmbedSidebar.tsx` ~100-120), taking an ordered candidate list and rendering the FIRST whose `show` is true: 1 getting-started progress (`ProgressCard`, `useOnboarding().shouldShowGettingStarted`, existing `dismissOnboarding`), 2 update pill (`UpdatePill`, existing `dismissedUpgradeVersions`), 3 profile prompt (`ProfilePromptCard`, existing `useProfile()` gate), 4 promo (`PromoCard`, `usePromoSlot('dashboard-sidebar')`, `maxUnits` 1). If the FSD lint rejects a feature→feature import for the progress/profile cards, make the slot a dumb arbiter in `shared/ui` and build the candidate list in `AppShell`. (b) `AppShell.tsx` ~556-568 stops rendering `ProgressCard` / `ProfilePromptCard` in `SidebarFooter`; `SidebarFooterStrip` keeps the nav strip + Ask DorkBot and the `UpdatePill` moves into the slot. Mount the slot in `EmbedSidebar.tsx` (`agent-sidebar`) and at the bottom of the Home panel in `MobileTabsLayout.tsx`. (c) Dismissal in config: add `ui.promos.dismissedIds: string[]` (default `[]`) to `UserConfigSchema` per the `adding-config-fields` skill (Zod field → per-field AND object-literal defaults → semver-keyed conf migration in `services/core/config-manager.ts` → docs), plus the three operator policy tables (`config-disclosure.ts`, `config-write-policy.ts`, `safe-defaults/default-verdicts.ts` — agent-writable like `ui.sidebar`), and route `dismissedPromoIds` / `dismissPromo` in `app-store-preferences.ts` ~170-189 to config (read the old localStorage key `dorkos-dismissed-promo-ids` once as a one-time import into config, then ignore it). (d) `PromoCard.tsx` gains a `×` (`aria-label="Dismiss"`) wired to `dismissPromo`; its `layout` prop is gated by `useReducedMotion`. (e) `promo-registry.ts` ~40-52: `remote-access.shouldShow` becomes `!ctx.isDesktopApp && !ctx.remoteAccessConfigured` — extend `PromoContext` with both facts (desktop: the existing electron detection helper; remote access: whatever the config exposes for tunnel/remote access — if no such fact exists in config, DELETE the `remote-access` promo instead). (f) Slot motion: entrance (height + opacity, 160 ms) plays only when a card newly qualifies AFTER boot (`mountedAtBoot` ref), never on first paint; × collapses height; reduced motion → instant. (g) Playground: `PromoShowcases.tsx` gains 'Bottom slot in a 272px panel with an overflowing list' showing the four candidates in turn; fix the stale 'Dismiss a promo card above' copy at ~161-163. (h) Tests: `DashboardSidebar.test.tsx` replaces the `PromoSlot: () => null` mock (~397) with an assertion that the slot is OUTSIDE the scroller and keeps the <200-line / no-model-read guard green; unit tests for arbiter priority, × persisting to config, one-time localStorage import, `remote-access` trigger truth table; a Playwright spec: with an overflowing list the slot is visible without scrolling, × hides it, and it stays hidden after reload; `MobileTabsLayout` test for the Home-panel slot. Acceptance: all of the above green; typecheck for client + shared + server; browser screenshot attached. Changelog fragment with `covers:`.

## P2 — One door, sections

### Task 2.1: One door to an agent: 1 agent = session, 2+ = group message, hand-made 1:1 DMs leave the Library; group-DM titles follow the roster (DOR-772)

- **Size:** xl · **Priority:** high · **Depends on:** 1.1 · **Parallel with:** 2.2
- **Issue:** DOR-1370 (parent DOR-1365)

Design: 02-specification.md §D2 (decision record design-decisions.md §2; rooms spec §8.5/§14.4 context in research/20260819_sidebar-simplification-review.md §3.3). Build: (a) `NewMenu.tsx` ~148-198: `new-message` label 'Direct message…' → 'Group message…' (id unchanged; `NEW_MENU_ITEM_IDS` in create-flow-store.ts unchanged). (b) Picker rule in `features/room-membership/ui/NewDirectMessageMenu.tsx` (+ `useStartDirectMessage`): confirm button 'Start group message' enabled at 2+ agents; with exactly ONE selected agent the button reads 'Open session with <Name>' and calls the same door as the agent row (`startNewSession(path)` / resolve-or-mint via `resolveSessionForCwd`); inline helper text under the picker: 'One agent opens a session. Two or more start a group message.' The deep-link from the Direct messages header `+` keeps id `new-message`. (c) The Library rule (model): a room is a hand-made 1:1 DM when `kind === 'dm'` AND its roster is exactly one agent plus the viewer AND `bridge == null`; `build-library-sections.ts` EXCLUDES such rooms from the Direct messages section (reason `library:dm-suppressed-1to1`); they still appear in Today under the existing rules (directed unread / recent activity) and in ⌘K; the agent row gains an unread dot when its hand-made 1:1 DM has a directed unread — build a `dmUnreadByAgentPath` map once in `buildSidebarItems` (sidebar-item.ts) and consume it in `library-rows.ts` `agentRow` → `derive-unread-signal.ts`. Agent-initiated DMs (`services/relay/notify-dm.ts`) are hand-made-shaped and follow the same rule. No server migration. (d) Group-DM title follows its roster (DOR-772): when a DM's `title` equals the roster-derived `directMessageTitle` of the PREVIOUS roster (i.e. was auto-generated), the server's add-member path (`services/rooms/room-roster.ts`) recomputes it from the new roster; a user-renamed title is left alone; client `directMessageTitle` helper shared. (e) Copy: command-palette DM rows already updated in 0.2; `docs/concepts/rooms.mdx` gets one paragraph (one agent = session; two or more = group message; lines an agent opens show in Today). (f) Spec amendments: `specs/sidebar-now-today-library/02-specification.md` BC-34 (unchanged in substance — now the only door) and BC-45; `specs/rooms/02-specification.md` §8.5 marked RESOLVED ('converged: the session is the 1:1 conversation; DMs are for 2+ participants and for lines an agent or a bridge opens') and §14.4 'A DM has no cwd' corrected to what `room-turn-runner.ts` ~268 does (`cwd = the agent's own directory`). (g) Tests: `NewMenu.test.tsx`, `one-create-surface.test.ts` (ids unchanged — these should stay green; update label assertions), picker rule unit tests (1 → session door, 2+ → room create, button copy), `library-rules.test.ts` cases for the suppression rule (hand-made 1:1 suppressed; bridged 1:1 kept; 2-agent DM kept; suppressed DM still in Today with unread; agent-row dot), server test for title-follows-roster (auto title updates; renamed title kept), e2e `direct-messages` spec updated for the picker rule. Acceptance: all green; typecheck client + server + shared; browser-verify the picker both ways and attach screenshots. Changelog fragment with `covers:` (reference DOR-772 in the relevant commit subject).

### Task 2.2: Sections for anything, top-level; Agents shows recent + pinned with 'All N agents →'; honest section controls; Channels sort pref (DOR-906); back-compat removal (DOR-588)

- **Size:** xl · **Priority:** high · **Depends on:** 1.1 · **Parallel with:** 2.1
- **Issue:** DOR-1371 (parent DOR-1365)

Design: 02-specification.md §D3 (decision record design-decisions.md §3; groups trace in research/20260819_sidebar-simplification-review.md §3.4). Build: (a) Rename every user-facing 'group' for this feature to 'section' — `NewMenu.tsx` ('Section…', 'Empty section…', 'Custom rules…'), `SectionHeaderMenuItems.tsx` (Rename/Delete/Mute section, Edit rules, Convert to manual section), `AgentRowMenuItems.tsx` and `rooms/RoomRowMenuItems.tsx` ('Move to section ▸', 'Remove from section', 'New section…'), `GroupCreateInput.tsx` placeholder, empty hints ('Drag channels, conversations or agents here' / 'No agents match these rules'), `use-sidebar-dnd.ts` announcement copy, `create-flow-store.ts` copy. Schema names (`groups`, `SidebarGroupSchema`) are NOT renamed. (b) Top-level render: `build-library-sections.ts` ~425-454 emits sections as peers — order `[...sections in prefs.groups order, pins, channels, dms, agents]` (sections first); remove `SidebarSectionModel.subsections` (build-sidebar-model.ts ~338-339) and the `SidebarSection.tsx` ~189-192 subsection render; sections keep `group:<id>` ids; Alt-click fold-all (widened in 1.1) enumerates them as before; `useSectionChrome.tsx` ~291 mounts the creation field ABOVE the first section (or where the Agents header was when no sections exist) so 'New section…' from any row lands in one place. (c) Always offered: `NewMenu` shows `new-group` for everyone; `offersGroupAffordances` (build-library-sections.ts ~57-60) gates only the smart presets and 'Custom rules…'. (d) Honest controls: `groupSection` applies `group.displayFilter` via `filteredAgentRows` and emits `options` so the header radio reads from the model (write the test FIRST — it must fail on current code); the 'Mute section' item is hidden for smart sections (`buildGroupHeaderMenuNodes`; `apply-mute-rules.ts` keeps skipping them); a mixed section sorted by 'Recent activity' uses `room.lastActivityAt` for rooms in the recency resolver (~325-327) instead of `null`; delete stale `filterSidebarItems` doc lines (if 0.1 has not already); amend `specs/sidebar-now-today-library/02-specification.md` R3 to match the code (ungrouped Agents has no manual reorder). (e) Agents = recent + pinned: in `entities/session/model/agent-attention.ts` the `inactive` boundary becomes 7 days since the LATER of the viewer's last interaction and the agent's last activity; `build-library-sections.ts` lists agents with `attention !== 'inactive'` OR pinned OR in a section, with a FLOOR of 8 (fill with the most recently active inactive agents); replace the `reveal` row 'N inactive' with a row labelled `All N agents` (trailing →, `target: { kind: 'command', id: 'open-team' }`) present whenever N > shown, whose `SidebarChrome.openTarget` navigates to `/team`; delete the dead `section-count` roll-up (closes DOR-1105 fully). (f) DOR-906: per-section `sortMode` (`name | recent`, default `name`) for `channels` and `dms` in `SidebarSectionsSchema` (additive; conf migration per `adding-config-fields`); `buildChannelsHeaderMenuNodes` and the DMs header offer Sort; `build-library-sections.ts` applies it. (g) DOR-588: remove `tolerateLegacySidebarEncoding` + `normalizeSidebarPrefs` (config-schema.ts ~558 and their call sites) — one release has passed. (h) Tests: `library-rules.test.ts` (top-level order; floor of 8; All N agents row; suppression interplay with 2.1 — coordinate by rebasing), `use-sidebar-dnd.test.ts` copy, `SidebarDnd.test.tsx`, `SectionHeaderMenuItems.test.tsx`, `AgentRowMenuItems.test.tsx`, `RoomRowMenuItems.test.tsx`, `NewMenu.test.tsx`, `smart-group-presets.test.ts`, `sidebar-model-prefs.test.ts`, config-schema tests for the new fields + migration, `agent-attention` threshold test, e2e `sidebar-groups.spec.ts` (+ drag a ROOM into a section; + 'New section…' from a room row lands above the first section), `mobile-sidebar-navigation.spec.ts` if it counts sections. (i) Spec amendments: `specs/sidebar-groups/`, `specs/smart-agent-groups/` (copy + top-level), `specs/sidebar-now-today-library` BC-28 (order) and BC-32 (an empty section still renders). Acceptance: all green; typecheck client + shared + server; browser-verify a section holding a channel + an agent + a DM rendered top-level and attach a screenshot. Note: task 2.1 edits `NewMenu.tsx` (the `new-message` label) in parallel — expect a trivial merge. Changelog fragment with `covers:` (reference DOR-906, DOR-588, DOR-1105 in the relevant commit subjects).

## P3 — Initial load

### Task 3.1: Boot state, skeleton and one reveal; one fetch per fact; scroll-to-active latches at settle; faces never flip (DOR-1143); BC-49 removed

- **Size:** xl · **Priority:** high · **Depends on:** 1.1, 1.2 · **Parallel with:** 2.1, 2.2
- **Issue:** DOR-1372 (parent DOR-1365)

Design: 02-specification.md §D6 (decision record design-decisions.md §6; the load trace is research/20260819_sidebar-simplification-review.md Appendix C). Build: (a) `model/use-boot-state.ts` (new) + `useSidebarState` exposes `boot: 'cold' | 'warm' | 'settled'` — `warm` when every boot query has `dataUpdatedAt > 0` at mount (hydrated from a persisted cache; task 3.2 provides the cache, this task provides the seam), `cold` otherwise, `settled` when the boot gate opens: `['config','current']` ∧ rooms ∧ threads ∧ mesh ∧ (manifests ∨ no paths) ∧ recent-sessions ∧ team roster, OR 1500 ms since mount (then settled with per-query degradation — pending sources read as empty, as today). Replace `rosterResolved` (use-sidebar-state.ts ~315-318) with the gate for `useJourneyFacts` and `useGettingStartedRetirement`. (b) `ui/SidebarSkeleton.tsx` (new, from the unused `SidebarMenuSkeleton` in shared/ui/sidebar.tsx ~599-611): three header bones + 8 row bones at the token geometry; `SidebarZones` renders it while `boot === 'cold'` with `aria-busy` on the nav and no live-region text; `SidebarHeaderBlock.tsx` ~59-63 renders a skeleton pill instead of 'Your team' while the roster is pending on a cold boot. (c) One reveal: when `cold → settled`, `AnimatePresence mode="wait"` cross-fades skeleton → zones once (160 ms opacity + 4 px rise; zones stagger 30 ms, rows never); `warm` → `initial={false}`; reduced motion → instant. (d) One fetch per fact: `useOnboarding` (features/onboarding/model/use-onboarding.ts ~7), `SidebarHeaderBlock.tsx` ~83-87, `useTours`, `useProfile` move from the bare `['config']` key to `configKeys.current()` (one query; `useConfig` is the reader) — add a unit test that fails on any remaining `['config']` literal in apps/client/src (grep-based); `useJumpBackIn` (~28,31,84) and `useSidebarState` (~158) share ONE `['recent-sessions', 24]` query, the sidebar taking its 10 via `select`; `useDigestFacts` ~169,209 reads its once-a-day latch only once `boot === 'settled'`. (e) `use-scroll-to-active.ts` ~44-63 latches the anchor at the first settled model (not at TodayZone mount), positions with `behavior: 'auto'` in a `useLayoutEffect`, and smooth-scrolls only on anchor changes after settle. (f) DOR-1143: agent rows and DM faces never paint before manifests on a cold boot (the gate); delete the false comment at `sidebar-item.ts` ~110-138; unit test: given manifests, the face on the first painted model equals the manifest face (and a test that a directory WITHOUT a manifest keeps its path-hash face across re-renders). (g) Remove BC-49: the `welcomeBack` prop + `useState` latch in sidebar-row.tsx ~164,481,519 and the CSS glow in index.css ~1054-1065; amend `specs/sidebar-now-today-library/02-specification.md` BC-49 ('removed — the digest row carries while-you-were-away; nothing glows on first paint'). (h) `AppShell.tsx` ~433-435 blank gate stays (it shortens automatically once config is cached in 3.2). (i) Playground: 'Sidebar boot' page with cold-skeleton and warm-paint states. (j) Tests: boot-gate truth table (each query pending/settled, timeout), `boot` transitions, skeleton geometry, reveal under reduced motion is instant, scroll-to-active latch, the `['config']` grep test, the shared recent-sessions key. Acceptance: all green; typecheck client; browser-verify a cold load (clear storage) shows the skeleton then ONE reveal, and attach a short screen recording or two screenshots. Changelog fragment with `covers:` (reference DOR-1143).

### Task 3.2: Boot from local memory: persisted TanStack Query cache for the boot queries, and a boot-stability browser test

- **Size:** large · **Priority:** high · **Depends on:** 3.1 · **Parallel with:** —
- **Issue:** DOR-1373 (parent DOR-1365)

Design: 02-specification.md §D6 'Persisted cache' + 'Evidence'. Build: (a) add `@tanstack/react-query-persist-client` and `@tanstack/query-sync-storage-persister` to apps/client at the same major as `@tanstack/react-query` (5.x); (b) `shared/lib/query-persister.ts` (new): `createSyncStoragePersister({ storage: window.localStorage, key: 'dorkos:rq:<server origin>' })`, `buster: __APP_VERSION__` (use the existing app-version define; add one if absent), `maxAge: 24h`, `dehydrateOptions.shouldDehydrateQuery` ALLOW-LIST: `configKeys.current()`, `roomKeys.list()`, `roomKeys.threads()`, `['mesh','agent-paths']`, `agentKeys.resolved(*)`, `['recent-sessions', *]`, the team roster key — nothing else (no session transcripts, no events); only when the transport is `HttpTransport` (web) — the Obsidian `DirectTransport` never persists; (c) `main.tsx` ~218 switches to `PersistQueryClientProvider` with `persistOptions`; hydration is synchronous so the first render sees cached data (verify: `queryClient.getQueryState(configKeys.current())?.dataUpdatedAt > 0` before the first fetch resolves, which is what task 3.1's `boot === 'warm'` reads); (d) a 'Clear sidebar cache' control in the dev playground (and a `dorkos:rq:*` wipe on sign-out / server change if such a hook exists); (e) measure and record in the PR the serialized cache size for the `power` fixture scale (60 rooms / 30 agents / 24 sessions) — must be well under 1 MB; (f) browser test `apps/e2e/tests/dashboard-sidebar/boot-stability.spec.ts`: seed, load once (cold) → assert the skeleton appears then exactly one reveal; reload (warm) → assert (1) no skeleton, (2) every sidebar row's `boundingBox()` at +100 ms equals at +2000 ms, (3) no `data-arrived` rows on boot, (4) each agent row's face (`data-emoji` or the rendered avatar attribute — add a stable data attribute if needed) at first paint equals after settle; (g) unit tests: `shouldDehydrateQuery` allow-list (a query outside the list is NOT persisted), origin-keyed storage key, buster invalidation, embed (DirectTransport) does not persist. Acceptance: all green; typecheck client; the e2e spec runs in the browser-test job (register it where the suite expects; check `apps/e2e/manifest.json` conventions); browser-verify warm reload paints complete in the first frame and attach evidence. Changelog fragment with `covers:`.

## P4 — Motion and performance

### Task 4.1: Continuity motion: fold with a count, arrive-and-settle, layout moves, drag lift/ring/settle

- **Size:** large · **Priority:** medium · **Depends on:** 1.1, 1.2, 2.2, 3.1 · **Parallel with:** 4.2
- **Issue:** DOR-1374 (parent DOR-1365)

Design: 02-specification.md §D5 (decision record design-decisions.md §5; demos on the visual-companion page `.dork/visual-companion/88892-1787170146/content/02-sidebar-review.html`). Build, all gated by `useReducedMotion` / `MotionConfig reducedMotion="user"` (instant when reduced): (a) Fold: `SidebarSection` body in a `motion.div` with `animate={{ height }}` spring (stiffness 400, damping 36, ≈180 ms) and the chevron `rotate` 0/−90; the folded roll-up text fades in 120 ms. (b) Arrive: rows entering Today / Heads up AFTER boot get `initial={{ opacity: 0, y: -6 }}` → animate 160 ms plus a one-shot tint (`data-arrived` attribute driving a 200 ms `motion-safe:` CSS keyframe on background-color); keyed by row id; `AnimatePresence initial={false}` on the list so boot never animates; leaving rows fade 120 ms. (c) Move: `layout` on `SidebarModelRow` inside each section's `SidebarMenu` so a reorder after the Today hold releases, a 'Move to section', the 4 am archive and the Agents recency reshuffle slide instead of pop; `layoutDependency` = the section's row-id list (scope the FLIP per section; never whole-panel). (d) Drag: `SidebarDndPrimitives` overlay `scale: 1.02` + shadow token; drop-target ring `inset 0 0 0 2px` at 45% ring colour instead of the background wash; keep dnd-kit's default settle spring. (e) Hover reveals stay opacity-only 120 ms (from 1.1). (f) Budget: every added animation ≤200 ms except the fold spring; nothing loops; the existing holds (Today order hold, Getting-started return, all-clear) are untouched — add a test that the Today order hold still suppresses reorder while the pointer is inside. (g) Tests: reduced-motion renders instantly (no transition classes / motion props resolved to `false`); boot has no `data-arrived` rows (coordinate with 3.2's e2e); fold persists and animates once; a render-count test that `layout` rows do not re-animate on unrelated model rebuilds (the 60 s clock tick). Acceptance: all green; typecheck client; `build-sidebar-model.performance.test.ts` still ≤5 ms; browser-verify fold/arrive/move and attach a short recording. Changelog fragment with `covers:`.

### Task 4.2: Performance: memoised RoomRow with a lazy menu, one fleet walk per render, one item index, slow/fast inputs split

- **Size:** large · **Priority:** medium · **Depends on:** 2.2, 3.1 · **Parallel with:** 4.1
- **Issue:** DOR-1375 (parent DOR-1365)

Design: 02-specification.md §D8 (evidence: research/20260819_sidebar-simplification-review.md §6). Build: (a) `RoomRow.tsx` becomes `React.memo`; its six mutation hooks (~128-133) and `useSidebarPrefs`/`useUpdateSidebarPrefs` (~355-356) move into `ui/rooms/use-room-row-menu.ts` (new) mounted only when the row's menu opens; pin/mute/section membership reads come from `SidebarChrome` context (one subscription); add a render-count test: a prefs write re-renders zero RoomRows whose own data did not change. (b) `useSectionChrome.tsx` ~154-197 `unreadIds` and `smartGroupCandidates` (and the two derived lists) are computed once in `SidebarZones` and passed via `SidebarChrome` context; `SmartGroupRuleDialog` mounts lazily. (c) `SidebarChrome.tsx` ~187-200 stops building a second `buildSidebarItems` index — `SidebarState` exposes the model's `itemIndex` and `roomVisualOf` reads it. (d) `use-roving-focus.ts` ~225 `useEffect(sync)` runs on `[sectionRowIds]` (plus a `MutationObserver` for the inline inputs) instead of every commit. (e) `useSidebarState` splits the 20-dependency snapshot into `slowInputs` (agents, rooms, prefs, manifests, threads) and `fastInputs` (now, attention, signals, activeTarget) memoised separately and spread into one `SidebarState`, so a clock tick does not re-derive the slow half; `buildSidebarModel`'s signature is unchanged. (f) `useReservationGuard` stays dev-only. (g) Tests: the render-count tests above; `build-sidebar-model.performance.test.ts` still ≤5 ms; `use-sidebar-state.test.tsx` referential-stability cases extended for the split; `SidebarDnd` and `useSectionChrome` tests green. Acceptance: all green; typecheck client; a React Profiler measurement in the PR body (before/after commit counts for a prefs write and a clock tick on the `power` fixture). No changelog fragment needed unless behaviour changes (internal — use a `perf`/`chore` subject; if `fragment-present` insists, a one-line 'Fixed: the sidebar does less work when you pin or mute' fragment).

## P5 — Close-out

### Task 5.1: Close-out: docs, spec amendment sweep, product media re-capture, playground audit, Linear close-out

- **Size:** medium · **Priority:** medium · **Depends on:** 4.1, 4.2, 3.2, 2.1 · **Parallel with:** —
- **Issue:** DOR-1376 (parent DOR-1365)

Design: 02-specification.md 'Documentation' + 'Implementation Phases' Wave 7. Do: (a) verify every spec amendment named in tasks 1.1, 2.1, 2.2, 3.1 actually landed (grep the amended BC ids); write any that were missed; (b) `docs/`: update or add the sidebar concept page (headers fold, sections, bottom slot, one door; plain language per `writing-for-humans`), `docs/concepts/rooms.mdx` paragraph present; `contributing/design-system.md` tokens present; (c) re-capture the sidebar product media with the `capturing-product-media` skill (shots that include the sidebar: home, session, channels; check the shot registry) — run the capture pipeline, review the output, commit the new media; (d) Dev Playground audit per `maintaining-dev-playground`: every new/changed sidebar component has a showcase (headers, bottom slot, skeleton/boot, sections top-level, All N agents row, motion states); (e) `pnpm knip` for dead code left by the programme; (f) changelog: read every fragment this programme wrote in `changelog/unreleased/` for plain language and correct `covers:` subjects; (g) ADRs: run `/adr:review`-style check — the proposed ADRs extracted from this spec move to `accepted` once the implementing PRs are merged (update status + manifest); (h) mark the spec `implemented` (`spec-manifest-ops.ts update-status sidebar-simplification implemented`) and write `specs/sidebar-simplification/04-implementation.md` (what shipped, PR list, deviations); (i) Linear: close DOR-1105, DOR-1143, DOR-772, DOR-906, DOR-588, DOR-1098 with evidence comments (the PR numbers), close DOR-329 (done-labelled epic), comment on DOR-341 (parked; group emoji not done), DOR-1094/DOR-1097 (note what changed, leave open), DOR-654 (stale e2e manifest path — fix it here if still stale). Acceptance: all green; the PR for docs/media/spec is merged; every sub-issue of the umbrella is Done; the umbrella is Done.
