{
  "spec": "specs/unified-workflow-system/02-specification.md",
  "slug": "unified-workflow-system",
  "generatedAt": "2026-06-14T00:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "0.1",
      "phase": 0,
      "phaseName": "P0 — Scaffold",
      "subject": "[unified-workflow-system] [P0] Stand up the .agents/flow/ marketplace plugin-type package skeleton",
      "description": "Create the logical/canonical root `.agents/flow/` as a DorkOS marketplace `plugin`-type package (Decision #24, §14). This is the registry that makes the whole system one identifiable installable unit.\n\nFiles to create:\n- `.agents/flow/README.md` — placeholder/initial manual (stages, modes, command↔state map, gates, adapter interface, AND the autonomous-mode server dependency note). Full content lands in later phases; scaffold the headings now.\n- `.agents/flow/SPEC.md` — placeholder/initial contract (stage model, `PMClient` interface for P5, config schema). Scaffold headings now.\n- `.agents/flow/config.json` — initial config matching the §9 resolved defaults (full schema validated in task 0.3). Copy the §9 JSONC block verbatim as the starting default (`tracker: \"linear\"`, `identity.agent: \"auto\"`, `gates.planApproval: false`, `decomposition.subIssueThreshold: \"xl\"`, `context.perIssue: \"fresh-session\"`, `autonomy.seat: \"pulse\"`, etc.).\n- `.agents/flow/manifest.json` — declares every member of the bundle and where it projects (skills synced, commands Claude-native, hook in settings.json, templates loaded by skills).\n- `.agents/flow/skills/` — empty dir (stage skills + `adapters/linear/` land in P1).\n- `.agents/flow/templates/` — empty dir (records/docs/pr.md land in later phases).\n- `.claude-plugin/plugin.json` — embedded so the package is simultaneously a Claude Code plugin and a marketplace entry (via `requiresClaudePlugin()`).\n\nThe honest layout (ground truth: the manifest syncs SKILLS ONLY; commands/hooks stay Claude-native):\n```\n.agents/flow/\n  README.md   SPEC.md   config.json + config.schema.json   manifest.json\n  skills/     ← stage skills (gerund) + adapters/linear/   → SYNCED to .claude/skills/ (symlink)\n  templates/  ← records/ (by type) · docs/ · pr.md          → loaded by skills\n.claude/commands/flow/   ← thin /flow + /flow:<stage> triggers   (Claude-native)\n.claude/settings.json    ← the unified loop hook                 (Claude-native)\n<repo>/WORKFLOW.md        ← optional per-repo override\n.dork/tasks/flow-drain/SKILL.md   ← the autonomous Pulse schedule (project-scoped)\n```\n\nv1 package contributes only `commands`/`skills`/`hooks`/`templates` — NO `extensions` layer (that's P5). Keep `.agents/` as the cross-harness glue (skills + AGENTS.md) that plugins don't cover. The three standards layer rather than compete; extracting the product extension later is additive, not a rewrite.\n\nThe Flow Engine — Harness Linear project + umbrella issue DOR-129 already exist; this task does not create Linear records.\n\nAcceptance (P0): `.agents/flow/` loads as a plugin; `manifest.json` lists every member; `pnpm lint`/`typecheck` clean.",
      "activeForm": "Standing up the .agents/flow/ marketplace plugin package skeleton",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["0.3"]
    },
    {
      "id": "0.2",
      "phase": 0,
      "phaseName": "P0 — Scaffold",
      "subject": "[unified-workflow-system] [P0] Register the flow bundle in harness-sync wiring and verify symlink",
      "description": "Wire the `flow` bundle into the harness-sync mechanism so its skills appear under `.claude/skills/` via symlink while commands stay Claude-native and the hook stays in `.claude/settings.json`.\n\nGround truth (from `project_dual_harness_skills` and §14): `.agents/harness.manifest.json` is custom to this repo and today syncs SKILLS ONLY (symlink `.agents/skills/X → .claude/skills/X`). Per the sync spec, commands stay Claude-native in `.claude/commands/` (no repo-local command format for Cursor/Codex) and hooks stay in `.claude/settings.json` (`hookPolicies: projection none`).\n\nFiles to modify/create:\n- `.agents/harness.manifest.json` — register the `flow` bundle (declare the flow skills for symlink sync). Follow the existing bundle-declaration pattern in the manifest.\n- Run the skill-sync mechanism (see `syncing-agent-skills` skill) so `.agents/flow/skills/*` (when populated in P1) symlink into `.claude/skills/`. For P0 the skills dir is empty, so this task establishes the registration + sync invocation; verify the symlink mechanism resolves the `flow` bundle path correctly.\n\nReference the sync-harnesses spec at `syncing-agent-skills/references/sync-harnesses-spec.md`. Do not duplicate skill files — canonical skills live in `.agents/` and symlink into `.claude/`.\n\nAcceptance (P0): the bundle's skills appear under `.claude/skills/` via symlink (verified once P1 populates `.agents/flow/skills/`); the manifest registration is valid JSON and `pnpm lint`/`typecheck` stay clean.",
      "activeForm": "Registering the flow bundle in harness-sync wiring",
      "size": "small",
      "priority": "high",
      "dependencies": ["0.1"],
      "parallelWith": []
    },
    {
      "id": "0.3",
      "phase": 0,
      "phaseName": "P0 — Scaffold",
      "subject": "[unified-workflow-system] [P0] Define the Zod config schema and generate config.schema.json",
      "description": "Author the Zod schema for `.agents/flow/config.json` and generate `.agents/flow/config.schema.json` via `z.toJSONSchema` (the `conf` precedent — see `reference_conf_and_config` and the `adding-config-fields` skill).\n\nThe schema must validate the full §9 config shape with these resolved defaults baked in:\n- `tracker: \"linear\"`\n- `identity: { agent: \"auto\", reviewer: null, marker: \"— 🤖 /flow\" }`\n- `ownership: { claimAssignedToAgent: true, claimUnassigned: true, claimAssignedToHuman: false, claimAssignedToOthers: false, scope: [\"issues\", \"projects\"] }`\n- `comments: { respondWhen: \"addressed\", ambiguousBias: \"quiet\" }`\n- `stages: { capture, triage, ideate, specify, decompose, execute, verify, review, done }` — each stage has `{ command, label, optional stateCategory, optional humanGate }`. `execute`/`verify` carry `stateCategory: \"started\"`; `review` carries `{ stateCategory: \"started\", humanGate: true }` and NO command; `done` carries `stateCategory: \"completed\"`.\n- `autonomy: { default: \"auto\", concurrency: \"sequential\", wipCap: { global: 2, perProject: 1 }, seat: \"pulse\" }`\n- `involvement: { comms: \"infer-from-trigger\", calibration: { proceedSilentlyWhen: [\"reversible\", \"confident\"], alwaysAsk: [\"irreversible-or-destructive\", \"outward-facing\", \"secrets-or-spend\", \"scope-change\"], stageBias: { intake: \"ask\", execution: \"proceed-and-log\" }, assumptionLog: { artifact: true, ticketComment: \"pm-driven\" } }, nudge: { relay: false, telegram: false } }`\n- `dispatch: { rank: [\"unblockers\", \"priority\", \"projectStatus\", \"type\", \"size\", \"age\"], sizeOrder: \"small-first\" }`\n- `gates: { planApproval: false, review: { mergeOnApproval: true, requireCiGreen: true, teardownWorktree: true, onConflict: \"resolve-if-mechanical\", ciRetries: 1, reapproveOnFunctionalChange: true, maxMergeAttempts: 3 }, circuitBreaker: { estimateMultiplier: 2, tokenBudget: 2000000 } }`\n- `context: { perIssue: \"fresh-session\", perStage: \"fresh-subagent\", compactionTrigger: 0.65, stageBudgets: { specify: 40000, decompose: 40000, execute: 80000, verify: 40000, review: 30000 }, externalize: [\"flow-state.json\", \"execution.log.jsonl\", \"flow-history.tsv\"] }`\n- `workspace: { isolation: \"worktree\", flow: \"gtr\", autoTeardown: true }`\n- `recovery: { maxRetries: 2, onExhausted: \"block\", staleAfter: \"5m\" }`\n- `decomposition: { mode: \"hybrid\", subIssueThreshold: \"xl\" }`\n- `evidence: { ui: \"auto\", temporal: \"video\", logic: \"test-summary\", attachTo: [\"pr\", \"tracker\"] }`\n\nDecide placement of the Zod source (e.g. a TS module under `.agents/flow/` or a packages location that can be tsc-checked) consistent with repo conventions; the generated `config.schema.json` lives at `.agents/flow/config.schema.json` and is referenced by `config.json`'s `$schema`.\n\nUnit tests (Vitest, §Testing Strategy — Config schema): Zod parse of valid/invalid `config.json`; `z.toJSONSchema` round-trip; default resolution asserting `planApproval:false`, `subIssueThreshold:\"xl\"`, `perIssue:\"fresh-session\"`, `seat:\"pulse\"`.\n\nAcceptance (P0): `config.schema.json` is generated from the Zod schema; the §9 `config.json` parses against it; `pnpm lint`/`typecheck` clean; the schema unit tests pass.",
      "activeForm": "Defining the Zod config schema and generating config.schema.json",
      "size": "medium",
      "priority": "high",
      "dependencies": ["0.1"],
      "parallelWith": []
    },
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Build the adapters/linear/ adapter skill (the v1 PMClient) owning all tracker I/O",
      "description": "Create `.agents/flow/skills/adapters/linear/` — a single adapter skill that owns EVERY `mcp__linear__*` / Composio call and fulfils the PMClient verbs as a documented prose contract (§3). This is the v1 realization of `PMClient`: no code interface exists yet; the typed TS `interface PMClient` is what the P5 server build promotes it into. The agnosticism win — 'all Linear lives in one place' — must be real in v1.\n\nLinear access: Linear MCP primary; Composio CLI v0.2.31 (`composio execute LINEAR_*`, `--account personal`/DorkOS account) fallback. Never the artblocks work account. See `reference_linear_composio_access_dorkos`.\n\nThe skill must document the WorkItem normalization shape:\n```\nWorkItem { id, identifier, title, description,\n  type,            // idea|research|hypothesis|task|monitor|signal|meta\n  stateCategory,   // matched on CATEGORY (backlog|unstarted|started|completed|canceled), never name\n  stateName,       // display only\n  priority,        // 0–4\n  size,            // points/t-shirt (drives sub-issue promotion + ranking)\n  project,         // { id, name, stateCategory, lead }\n  parent, relations { blocks[], blockedBy[], children[], relatedTo[], duplicateOf? },\n  labels[],        // includes stage/* and agent/*\n  assignee,        // → classifyOwnership(): mine|reviewer|other|unassigned\n  agentDisposition // ready|claimed|completed|needs-input\n}\n```\n\nAnd the capability verbs the generic layer knows (each mapped to the right Linear MCP/Composio call): `getCurrentUser`, `getProjects`, `getEligibleWork`, `getInbox`, `getRelations`, `claim`, `transition`, `comment`, `assignToHuman`, `attachEvidence`, `needsInput`, `link`, `createSubIssue`.\n\nRequirements:\n- State machine = `agent/*` labels, NOT the ephemeral `plan` field (Huginn durability lesson).\n- `getInbox` returns assigned-to-me + @mentions + new comments since last tick; each carries `{ item, comment: { author, mentions[], body } }` for the comment-response rules (§5).\n- `claim` is durable: write claim label + move state (survives restart).\n- `needsInput` = elicitation: label + comment + assign-to-human + stop.\n- Graceful degradation: trackers lacking `project.stateCategory`/`priority`/`size` (e.g. GitHub Issues) supply what exists; document that the dispatch policy treats missing fields as neutral.\n\nUnit/guard tests (§Testing Strategy): mock Linear MCP/Composio; assert each verb maps to the right call. Add a grep/lint guard that finds ZERO `mcp__linear__*`/Composio strings outside `adapters/linear/`.\n\nAcceptance (P1): a grep guard finds zero `mcp__linear__*`/Composio strings outside `adapters/linear/`; the adapter verb contract tests pass; tracker writes are confined to this single skill (single audit surface, §Security).",
      "activeForm": "Building the adapters/linear/ adapter skill (the v1 PMClient)",
      "size": "large",
      "priority": "high",
      "dependencies": ["0.1", "0.2"],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Create intake-stage gerund skills (capturing-work, triaging-work) and thin /flow:capture, /flow:triage commands",
      "description": "Extract the intake stages into generic, PM-agnostic gerund-named stage skills and add thin `/flow:<stage>` trigger commands.\n\nStage skills to create under `.agents/flow/skills/`:\n- `capturing-work/` — absorbs today's `/linear:idea` + `capturing-linear-ideas`. PM projection (Linear): `type/idea`, Triage.\n- `triaging-work/` — absorbs today's `/pm` triage/intake + complexity routing. PM projection (Linear): type-label set, Backlog/Todo. (Note: `/pm`'s 7 jobs split into `triaging-work` + the loop engine + an `audit` skill per §5.10.)\n\nThin commands to create under `.claude/commands/flow/` (Claude-native, NOT under `.agents/`):\n- `/flow:capture` → invokes `capturing-work`.\n- `/flow:triage` → invokes `triaging-work`.\n\nEach `/flow:<stage>` command must be ≤ ~40 LOC and ONLY invoke the stage skill (mirrors `/spec:execute` today at 39 LOC). No logic inline.\n\nGeneric stage skills must NEVER touch a tracker API directly — all tracker I/O flows through `adapters/linear/` (task 1.1). Call that adapter skill instead of touching tracker strings.\n\nThis is a pure refactor (no behavior change): existing manual flows must still work. Hard rename — remove the legacy commands (`/linear:idea`; the `/pm` triage path), no aliases.\n\nAcceptance (P1): `/flow:capture` and `/flow:triage` commands are each ≤ ~40 LOC; the grep guard finds zero tracker strings in these skills; the legacy commands are removed; existing manual capture/triage flows still work.",
      "activeForm": "Creating capturing-work and triaging-work stage skills with thin commands",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.3", "1.4", "1.5"]
    },
    {
      "id": "1.3",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Wire ideating-features + create specifying-work skill and thin /flow:ideate, /flow:specify commands",
      "description": "Point the existing IDEATE stage skill at the unified stage model and extract SPECIFY into a generic gerund skill, with thin trigger commands.\n\nStage skills:\n- `ideating-features` (EXISTS) — IDEATE stage. Absorbs today's `/ideate`. PM projection (Linear): `stage/ideate`. Update this skill to point at the unified stage model (per §Documentation: 'point at the unified stage model'). Externalize the ideation/specification/03-tasks.json/ADR doc scaffolds currently inline in the `/ideate` command into `templates/docs/` (the templates set is owned by task 4.x but the externalization of these scaffolds out of the thick command happens here so the command stays thin).\n- `specifying-work/` (NEW, under `.agents/flow/skills/`) — SPECIFY stage. Absorbs today's `/ideate-to-spec` + `/spec:create`. PM projection (Linear): `stage/specify`.\n\nThin commands under `.claude/commands/flow/`:\n- `/flow:ideate` → invokes `ideating-features`. ≤ ~40 LOC.\n- `/flow:specify` → invokes `specifying-work`. ≤ ~40 LOC.\n\nGeneric stage skills must NEVER touch a tracker API directly — route all tracker I/O through `adapters/linear/` (task 1.1).\n\nPure refactor (no behavior change): existing manual `/ideate` and `/ideate-to-spec`/`/spec:create` flows must still work via the new commands. Hard rename — remove the legacy commands, no aliases.\n\nAcceptance (P1): `/flow:ideate` and `/flow:specify` are each ≤ ~40 LOC; grep guard finds zero tracker strings in `specifying-work`; legacy commands removed; manual ideate/specify flows still work; doc scaffolds externalized so `/ideate` logic no longer carries inline templates.",
      "activeForm": "Wiring ideating-features and creating specifying-work with thin commands",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.2", "1.4", "1.5"]
    },
    {
      "id": "1.4",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Create decomposing-work, wire executing-specs + verifying-work + closing-work skills and thin /flow:decompose, /flow:execute, /flow:verify, /flow:done commands",
      "description": "Extract the execution-and-close stages into generic gerund skills (or wire existing ones) with thin trigger commands.\n\nStage skills:\n- `decomposing-work/` (NEW, under `.agents/flow/skills/`) — DECOMPOSE stage. Absorbs today's `/spec:decompose` + `/spec:tasks-sync`. PM projection (Linear): `stage/decompose`, plan checklist.\n- `executing-specs` (EXISTS) — EXECUTE stage. Absorbs today's `/spec:execute` + worktree setup/teardown. PM projection (Linear): In Progress, `agent/claimed`. Point at the unified stage model.\n- `verifying-work/` (NEW) — VERIFY stage. Absorbs today's `/review-recent-work`, browser proof, code review (`browser-testing`, `requesting-code-review`, `verification-before-completion`). PM projection: evidence on issue/PR. (Full browser proof pipeline is P4; this task wires the skill + thin command; VERIFY behavior detail lands in P4.)\n- `closing-work/` (NEW) — DONE stage. Absorbs today's `/linear:done` + `closing-linear-loop`. PM projection (Linear): Done, `agent/completed`.\n\nNote: REVIEW is a human gate with NO skill — the engine parks there (handled in P2). MONITOR/SIGNAL (`monitoring-work`, optional tail) is out of v1 scope (Decision #25) — do not build it here.\n\nThin commands under `.claude/commands/flow/` (each ≤ ~40 LOC, invoke-only):\n- `/flow:decompose` → `decomposing-work`\n- `/flow:execute` → `executing-specs`\n- `/flow:verify` → `verifying-work`\n- `/flow:done` → `closing-work`\n\nGeneric stage skills must NEVER touch a tracker API directly — route all tracker I/O through `adapters/linear/` (task 1.1).\n\nPure refactor (no behavior change): existing `/spec:decompose`, `/spec:execute`, `/review-recent-work`, `/linear:done` flows must still work via the new commands. Hard rename — remove the legacy commands, no aliases. Breadcrumb logic copy-pasted ×4 collapses into one adapter call.\n\nAcceptance (P1): `/flow:decompose`, `/flow:execute`, `/flow:verify`, `/flow:done` are each ≤ ~40 LOC; grep guard finds zero tracker strings in these skills; legacy commands removed; existing manual flows still work.",
      "activeForm": "Creating decompose/execute/verify/done stage skills with thin commands",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.2", "1.3", "1.5"]
    },
    {
      "id": "1.5",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Add /flow orchestrator command and remove legacy command surface (hard rename, no aliases)",
      "description": "Add the top-level `/flow` orchestrator command and complete the hard rename of the legacy command surface.\n\nFiles to create under `.claude/commands/flow/`:\n- `/flow` orchestrator command — the entry point that routes to stages / drives the loop (the `/flow auto` terminal-drain mode is implemented in P2; this task establishes the `/flow` command surface and routing skeleton, ≤ ~40 LOC, delegating to skills/engine).\n\nLegacy command surface to REMOVE (hard rename, no aliases — Decision #8, §5.10):\n- `/ideate`, `/ideate-to-spec` (replaced by `/flow:ideate`, `/flow:specify`)\n- `/pm`, `/linear:idea`, `/linear:done` (replaced by `/flow:triage`/`/flow:capture`/`/flow:done` + the loop engine + an `audit` skill)\n- `/spec:create`, `/spec:decompose`, `/spec:execute`, `/spec:tasks-sync`, `/review-recent-work` (replaced by `/flow:specify`, `/flow:decompose`, `/flow:execute`, `/flow:verify`)\n- `/worktree:*` usage is absorbed into EXECUTE's worktree setup/teardown.\n\nAlso fold the duplicate skills problem: single canonical skill + sync (no `.claude/` vs `.agents/` duplication for the flow bundle).\n\nNote: this task coordinates the final removal once tasks 1.2–1.4 have introduced the replacement `/flow:<stage>` commands; sequence the deletions so no flow is left without a replacement at any commit.\n\nAcceptance (P1): the legacy commands are removed (hard rename, no aliases); `/flow` orchestrator exists and is ≤ ~40 LOC; the full command list now presents one identifiable `/flow` prefixed system; existing manual flows still work end-to-end through the new commands.",
      "activeForm": "Adding the /flow orchestrator and removing the legacy command surface",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.2", "1.3", "1.4"],
      "parallelWith": []
    },
    {
      "id": "1.6",
      "phase": 1,
      "phaseName": "P1 — Extract & thin",
      "subject": "[unified-workflow-system] [P1] Extend 03-tasks.json schema with issue/parentIssue and add the PM-agnostic provenance block",
      "description": "Close the schema gap so the task→issue mapping has a canonical home, and generalize the scalar `linear-issue:` frontmatter into a PM-agnostic provenance block (§8).\n\nSchema extension (`specs/<slug>/03-tasks.json`):\n- Add optional per-task `issue` and `parentIssue` fields (currently absent). These are the ONLY place the task→issue mapping lives. A promoted sub-issue gets its canonical home here.\n- A flat top-level `issues: […]` list is REJECTED — it duplicates the task→issue map and reintroduces drift.\n- Sub-issue promotion fires only when `size ≥ decomposition.subIssueThreshold` (default `\"xl\"`); parent size does NOT additionally gate. The vast majority of tasks stay checklist lines.\n\nProvenance block (PM-agnostic, replacing scalar `linear-issue:` frontmatter):\n- Name exactly ONE tracker home: an `issue` (small spec) OR a `project` (large spec) — 1:1 at the anchor.\n- Shape: a one-line provenance block with `tracker` / `issue` / `project` fields. Back-links are bidirectional but ID-only (stable, low-churn). Extend the provenance block to ADRs and research, not just specs.\n- Link cardinality: spec ─(1:1)─▶ ONE tracker home; `03-tasks.json` per-task `issue` carries the 1:many (normalized, MOST tasks have none); typed relations (related/supersedes) go via adapter `link()`, not frontmatter.\n\nCollapse the dual task system: `03-tasks.json` is canonical; the built-in Task API becomes a PROJECTION for live display only, not a second source. The active phase is mirrored into the ticket as a checklist GENERATED from `03-tasks.json`, never hand-edited.\n\nUnit tests (§Testing Strategy — 03-tasks.json schema extension): `issue`/`parentIssue` optional fields parse; promotion fires only at `size ≥ \"xl\"`; `03-tasks.json` round-trips the new fields.\n\nAcceptance (P1): `03-tasks.json` round-trips the new `issue`/`parentIssue` fields; the provenance block parses and names one issue-or-project; the schema extension unit tests pass; the dual task system is collapsed (Task API is a projection).",
      "activeForm": "Extending 03-tasks.json schema with issue/parentIssue and adding the provenance block",
      "size": "medium",
      "priority": "high",
      "dependencies": ["0.3"],
      "parallelWith": ["1.2", "1.3", "1.4"]
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "P2 — Unify the loop",
      "subject": "[unified-workflow-system] [P2] Implement the calibration ladder for uncertainty-gated involvement",
      "description": "Implement the calibration ladder — the single most important behavior in the system (§5). Involvement is uncertainty-gated, not stage-gated. At each decision point the agent walks the ladder top-down and acts on the first matching row:\n\n| # | Condition | Behavior | Blocks loop? |\n|---|-----------|----------|--------------|\n| 0 | Floor — irreversible/destructive · outward-facing · secrets/spend/prod · material scope change | Stop & ask (`needsInput()`) — even at full confidence | yes |\n| 1 | Reversible + confident | Proceed silently | no |\n| 2 | Sticky + not-confident | Stop & ask | yes |\n| 3 | Reversible + not-confident (the ambiguous middle) | Routed by stage bias | stage-dependent |\n| 4 | Sticky + confident | Proceed, but announce | no |\n\nEvidence-based definitions (not mood-based):\n- Confident = the answer is determined by the frozen spec, an ADR/decision, a strong codebase convention, or a prior human answer — NOT a hunch. If resolving requires guessing intent or choosing between materially-different approaches with no steer, it is NOT confident.\n- Reversible = cheap to undo inside the loop (code edit, worktree file, draft comment). Sticky = costly or visible to undo.\n\nStage bias routes row 3, the frozen spec is the cut line:\n- Intent stages (CAPTURE/TRIAGE/IDEATE/SPECIFY) → ask.\n- Execution stages (DECOMPOSE/EXECUTE/VERIFY) → proceed on the best default + log the assumption.\n- Yields 'IDEATE asks freely, EXECUTE asks rarely' as an emergent property of one rule.\n\nOnly three behaviors exist: proceed-silently, proceed-with-a-trail, stop-and-ask. The only things that block the autonomous loop are the floor (row 0), sticky uncertainty (row 2), and uncertainty in intent stages (row 3-ask).\n\nEvery non-obvious call leaves a trail: rows 3-proceed and 4 write an `agent/assumption` comment + stage-artifact note, auditable at the review gate. Answers become memory by being written where row 1's evidence-test will find them next time (decisions table / ADR / config.json) — no separate store.\n\nDrive behavior from `involvement.calibration` config: `proceedSilentlyWhen: [\"reversible\",\"confident\"]`, `alwaysAsk: [\"irreversible-or-destructive\",\"outward-facing\",\"secrets-or-spend\",\"scope-change\"]`, `stageBias: { intake: \"ask\", execution: \"proceed-and-log\" }`, `assumptionLog: { artifact: true, ticketComment: \"pm-driven\" }`.\n\nUnit tests (Vitest, §Testing Strategy — Calibration ladder): table-driven cases over the 5 rows × {reversible/sticky} × {confident/not} × {intake/execution stage} → expected behavior (proceed-silent / proceed-with-trail / stop-ask). This is the single most important behavior — pin it.\n\nAcceptance (P2): the calibration-ladder unit suite passes; the three behaviors are correctly routed for every row×stage combination.",
      "activeForm": "Implementing the calibration ladder for uncertainty-gated involvement",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "0.3"],
      "parallelWith": ["2.2", "2.3"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "P2 — Unify the loop",
      "subject": "[unified-workflow-system] [P2] Implement the dispatch policy (eligibility filter + ranking ladder)",
      "description": "Implement the two-pass, config-driven dispatch policy that chooses what to work on (§4).\n\nPass 1 — Eligibility (filter OUT an item if):\n- `stateCategory` not dispatchable\n- lacks `agent/ready` (PM-driven mode)\n- `blockedBy` any open item\n- its `project.stateCategory` is completed/canceled\n- per-project or global WIP cap reached (`autonomy.wipCap: { global: 2, perProject: 1 }`)\n- its `classifyOwnership` class isn't in the `ownership` claim policy (never the reviewer's or others' items; in shared-account mode never an item merely on the shared account without `agent/claimed`).\n\nPass 2 — Ranking (order survivors; ordered tiers, later tiers break ties):\n```\n1 unblockers      — items that block others rank first (clear the chain)\n2 priority        — urgent → high → medium → low → none\n3 project status  — issues in In Progress projects before Planned\n4 type            — optional per-type weighting\n5 size            — small-first (config: dispatch.sizeOrder) or large-first\n6 age             — oldest created first\n7 identifier      — deterministic final tiebreak\n```\n\nWeights live in config (`dispatch.rank: [\"unblockers\",\"priority\",\"projectStatus\",\"type\",\"size\",\"age\"]`, `dispatch.sizeOrder: \"small-first\"`); re-prioritizing never touches code. Relations feed tier 1 + the blocked-by eligibility filter. Graceful degradation: missing priority/size are treated as NEUTRAL.\n\nThe policy consumes the `WorkItem[]` shape normalized by `adapters/linear/` (task 1.1) and the `classifyOwnership` primitive (task 3.1, but the dispatch eligibility consumer can stub/accept it as input for testing).\n\nOpen question resolved direction (P2, non-blocking): the per-tick ranking runs INLINE in the dispatched session in v1 (fresh session, no accumulation); revisit if ranking grows.\n\nUnit tests (Vitest, §Testing Strategy — Dispatch ranking + eligibility): given a `WorkItem[]`, assert the ordered survivor list and that blocked / `other`-owned / WIP-capped items are filtered. Edge cases: missing priority/size (neutral), `blockedBy` open item.\n\nAcceptance (P2): the dispatch ranking + eligibility unit suite passes; blocked/other-owned/WIP-capped items are filtered; the survivor ordering matches the 7-tier ladder; missing fields are neutral.",
      "activeForm": "Implementing the dispatch policy (eligibility + ranking)",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "0.3"],
      "parallelWith": ["2.1", "2.3"]
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "P2 — Unify the loop",
      "subject": "[unified-workflow-system] [P2] Implement config-driven gates and the auto-merge recovery ladder",
      "description": "Implement the hard gates and the auto-merge recovery ladder, all config-driven (§5, §6).\n\nThe hard gates:\n1. Question / soft-escalation (any stage, dynamic) — driven by the calibration ladder (task 2.1).\n2. Plan-approval gate (after DECOMPOSE, before EXECUTE) — OFF by default (`gates.planApproval: false`, §7.4): the engine flows DECOMPOSE→EXECUTE automatically and surfaces plan assumptions at the human-review gate. Operators who want a pre-code checkpoint flip it on.\n3. Human-review gate (after VERIFY) — ALWAYS ON. PR + evidence → In Review → assign to human → stop. On approval + CI green → auto-merge + close + teardown.\n4. Circuit breaker — stop + escalate if a unit exceeds `estimate × N` wall-clock (`gates.circuitBreaker.estimateMultiplier: 2`) or a token budget (`gates.circuitBreaker.tokenBudget: 2000000`).\n\nAuto-merge recovery ladder (§6) — approval authorizes ONE specific state: this diff, green, cleanly mergeable. If that state can't be reproduced automatically at merge time, the agent does NOT merge — it checks three preconditions, each failure routing through the calibration ladder:\n\n| Precondition | Pass | Fail → disposition |\n|---|---|---|\n| Mergeable? | clean replay → continue, announce rebase | conflict → mechanical/no-functional-risk (both add an import, append a changelog/lockfile) → resolve + announce; real tradeoff (overlapping logic edits) → bounce (`agent/needs-rebase`, In Progress, comment, re-assign) |\n| CI green? | green → continue | red → retry once (flake guard, `gates.review.ciRetries: 1`); still red → re-enter EXECUTE→VERIFY, regenerate evidence |\n| Functionally unchanged since approval? | mechanical-only → merge + close + teardown (announce) | behavior-altering → re-request approval (re-assign, fresh evidence) (`gates.review.reapproveOnFunctionalChange: true`) |\n\nRunaway bounce (fix→review→red over `gates.review.maxMergeAttempts: 3`) → circuit-breaker escalation (`agent/blocked` + nudge).\n\nConfig knobs: `gates.review.mergeOnApproval: true`, `requireCiGreen: true`, `teardownWorktree: true`, `onConflict: \"resolve-if-mechanical\"`. Tracker writes (transitions, comments, assigns) go through `adapters/linear/` (task 1.1). The calibration ladder (task 2.1) decides mechanical-vs-functional.\n\nUnit tests (Vitest, §Testing Strategy — Auto-merge recovery ladder): mechanical vs functional conflict/CI/drift → resolve+announce vs bounce vs re-approve.\n\nAcceptance (P2): gates are config-driven (incl. `planApproval:false`, auto-merge-on-approval); the auto-merge recovery ladder unit suite passes; runaway bounce escalates via the circuit breaker.",
      "activeForm": "Implementing config-driven gates and the auto-merge recovery ladder",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "2.1", "0.3"],
      "parallelWith": ["2.2"]
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "P2 — Unify the loop",
      "subject": "[unified-workflow-system] [P2] Implement mode orthogonality, comms routing, and /flow auto terminal draining",
      "description": "Generalize the `autonomous-check` Stop-hook concept into the canonical stage state and implement mode orthogonality + comms routing + terminal-drain autonomous mode (§2, §5).\n\nMode orthogonality — trigger source (manual CLI vs PM-driven) is orthogonal to execution mode (step vs autonomous):\n\n|  | Step (run one stage, stop) | Autonomous (run to a gate) |\n|---|---|---|\n| Manual (CLI/slash) | `/flow:specify`, `/flow:execute` | `/flow auto` — drain the ready queue from the terminal |\n| PM-driven | rare; explicit single-stage advance | default — Pulse tick claims an issue, carries it to the review gate (task 2.5) |\n\nImplement `/flow auto` (the manual autonomous terminal-drain): drain the ready queue sequentially from the terminal, using the dispatch policy (task 2.2) to pick the next eligible top-ranked issue, carrying each to its review gate, uncertainty-gated (task 2.1). Runs WITHOUT a DorkOS server (server-free manual mode).\n\nGeneralize the autonomous Stop-hook: the old `autonomous-check.mjs` (Ralph-Wiggum Stop-loop reading `roadmap/roadmap.json`) is replaced by reading the canonical stage state. Update `.claude/settings.json` with the unified loop hook (Claude-native, stays in settings.json per harness-sync ground truth). Remove the old `roadmap.json`-coupled hook.\n\nComms channel — inferred from trigger, config-overridable (`involvement.comms: \"infer-from-trigger\"`):\n- CLI with a live session → ask interactively (`AskUserQuestion`).\n- PM-driven/away → post a comment, apply `agent/needs-input`, assign to the human, resume on their reply (`getInbox`).\n- optional Relay/Telegram nudge (`involvement.nudge: { relay: false, telegram: false }`).\n\nReading the channel back (comment-response) — hard rules then a conservative soft zone:\n1. never answer its own comments (author == agent or carries `identity.marker`);\n2. always respond when directly addressed (@mention / explicit `/flow` token — overrides ownership);\n3. resume when an `agent/needs-input` item gets a non-agent comment;\n4. stay out of `other`-owned threads unless mentioned;\n5. soft zone leans quiet — over-responding is the worse failure.\n(The `classifyOwnership` consumer for these rules is built in P3; wire the hooks/structure here and consume `classifyOwnership` as input.)\n\nAll tracker I/O goes through `adapters/linear/` (task 1.1).\n\nAcceptance (P2): `/flow auto` drains a ready queue sequentially in the terminal; mode is orthogonal to trigger; the unified loop hook reads canonical stage state (old roadmap.json hook removed); comms routing infers channel from trigger; server-free manual mode is unaffected.",
      "activeForm": "Implementing mode orthogonality, comms routing, and /flow auto draining",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.1", "2.2", "2.3"],
      "parallelWith": []
    },
    {
      "id": "2.5",
      "phase": 2,
      "phaseName": "P2 — Unify the loop",
      "subject": "[unified-workflow-system] [P2] Seat the autonomous loop on Pulse — ship the flow-drain SKILL.md schedule + dispatch brief",
      "description": "Seat the v1 autonomous loop on DorkOS Pulse (not a bespoke watcher). Pulse already provides a contextless code-loop (croner) that dispatches a fresh, isolated, resumable, runtime-agnostic agent session per run — exactly the orchestrator shape §5.11/§5.12 require — so there is no scheduler to build (this answers DOR-89's 'does Pulse subsume the poll loop?' → yes) (§10).\n\nThe schedule is a file. Create the project-scoped task definition `<project>/.dork/tasks/flow-drain/SKILL.md`. The SQLite `pulseSchedules`/`pulseRuns` tables are a derived cache (file-first, ADR-0043 pattern). Content (copy verbatim as the template):\n```markdown\n## <!-- <project>/.dork/tasks/flow-drain/SKILL.md -->\n\nname: flow-drain\ndisplay-name: /flow — drain ready queue\ndescription: Claim the top-ranked eligible issue and carry it to its review gate.\ncron: \"*/10 * * * *\"\ntimezone: America/Los_Angeles\nenabled: true\nmax-runtime: 2h\npermissions: acceptEdits\n\n---\n\nRun one tick of the /flow autonomous loop:\n\n1. Via adapters/linear, fetch eligible work and rank it (dispatch ladder, §4).\n2. Claim the top issue (durable label + state), provision its worktree.\n3. Carry it through the stages to its gate — uncertainty-gated involvement (§5).\n4. Stop at the human-review gate or on a genuine question (needs-input).\n```\n\nReference the existing Pulse/Tasks infrastructure (`apps/server/src/services/tasks/`): `TaskSchedulerService` (croner, `protect:true`, `maxConcurrentRuns`), `TaskFileWatcher` (chokidar), file-first SKILL.md task definitions in `~/.dork/tasks/` (global) and `<project>/.dork/tasks/` (project), `pulseSchedules`/`pulseRuns` SQLite cache.\n\nKey behaviors to wire:\n- One tick = one issue. Each croner fire is a fresh run-session (`sessionId = run.id`) that claims and works exactly one issue to its gate, then ends — preserving fresh-session-per-issue (§7.7). `croner protect:true` prevents overlapping runs (sequential WIP-1); `maxConcurrentRuns`/`wipCap` govern concurrency when raised.\n- Activation requires only: the DorkOS server running (hosts the chokidar watcher + croner) and — for a project task — the project's DorkOS agent registered (a global `~/.dork/tasks/` task is watched unconditionally). No build step, no migration; dropping the file is picked up live.\n- Server-free manual mode unaffected (`/flow`/`/flow:<stage>`/`/flow auto`).\n- Honest dependency statement ships in the package README: autonomous mode depends on a running DorkOS server (Pulse); manual mode does not.\n- Portability fallback (DOCUMENTED, NOT built): a generic `claude -p`-per-issue watcher is the documented alternative seat for a non-DorkOS repo (`autonomy.seat: \"watcher\"`). v1 builds only the Pulse seat.\n\nIntegration test (§Testing Strategy — Pulse seat): drop a `flow-drain` SKILL.md into a temp `.dork/tasks/`; assert `TaskFileWatcher` syncs it to `pulseSchedules`, croner schedules it, and a fire dispatches one fresh session with the resolved worktree cwd. Use a `FakeAgentRuntime` so no real model runs. Reuses the existing tasks test patterns. Plus a stage→projection round-trip test: a stage transition writes the right `stage/*` label + state category through the (mocked) adapter.\n\nAcceptance (P2): a `flow-drain` Pulse schedule (server up) claims and advances one issue per tick in a fresh session; the Pulse-seat integration test passes; the stage→projection round-trip test passes.",
      "activeForm": "Seating the autonomous loop on Pulse with the flow-drain schedule",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.4"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "P3 — Agent-as-team-member v1",
      "subject": "[unified-workflow-system] [P3] Implement classifyOwnership and identity mode detection (two-account vs shared-account)",
      "description": "Implement the identity & ownership model — one primitive, two consumers (§7).\n\nGoverning principle: ownership/authorship live in labels + a comment marker (durable, always present); a dedicated agent account is a cleaner enhancement when it exists.\n\nIdentity modes:\n- Two-account (ideal): agent (`identity.agent`) and human (`identity.reviewer`) have distinct tracker accounts — assignee + authorship unambiguous.\n- Shared-account (fallback): agent acts AS the human's account; claims via the `agent/claimed` LABEL only, recognizes its own comments by MARKER (`identity.marker: \"— 🤖 /flow\"`), hands off by label + nudge.\n- The mode is DETECTED, not configured: `identity.reviewer` unset or == `identity.agent` ⇒ shared mode; otherwise two-account.\n\nOne primitive, two consumers:\n- `classifyOwnership(item) → mine | reviewer | other | unassigned` — compares `assignee`/`project.lead` against `identity.agent`/`identity.reviewer`. Drives BOTH dispatch eligibility (§4, task 2.2) and comment-handling (§5, task 2.4 / 3.2).\n- The `ownership` config declares which classes the agent may claim (`claimAssignedToAgent`, `claimUnassigned`, `claimAssignedToHuman: false`, `claimAssignedToOthers: false`) — applied to issues AND projects (`ownership.scope: [\"issues\",\"projects\"]`). In shared mode it never auto-claims items merely on the shared account without `agent/claimed`.\n\nIdentity resolution: no personal identity ships in the package — defaults (`identity.agent: \"auto\"`, `identity.reviewer: null`) resolve at runtime from the installer's authenticated account (the adapter's `getCurrentUser`).\n\nUnit tests (Vitest, §Testing Strategy — classifyOwnership): two-account vs shared-account (DETECTED) × {mine/reviewer/other/unassigned}. Purpose: the claim/comment gate.\n\nAcceptance (P3): `classifyOwnership` unit suite passes for both account modes × all four classes; identity mode is detected from `identity.reviewer`; `identity.agent: \"auto\"` resolves from the authenticated account via the adapter; the agent claims only permitted classes.",
      "activeForm": "Implementing classifyOwnership and identity mode detection",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "0.3"],
      "parallelWith": ["3.2"]
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "P3 — Agent-as-team-member v1",
      "subject": "[unified-workflow-system] [P3] Implement inbox polling, comment/assign/handoff, soft-escalation, and durable label claims",
      "description": "Implement the agent-as-team-member behaviors that let the agent operate in a shared tracker (§5, §7; Phase 3 acceptance).\n\nBehaviors to implement (all tracker I/O via `adapters/linear/`, task 1.1):\n- Inbox polling: `getInbox(agent)` returns assigned-to-me + @mentions + new comments since last tick; each carries `{ item, comment: { author, mentions[], body } }`.\n- Comment-response rules (hard rules then a conservative soft zone), consuming `classifyOwnership` (task 3.1):\n  1. Never answer its own comments (author == agent OR carries `identity.marker`) — breaks self-reply loops; in shared mode the marker is the ONLY signal.\n  2. Always respond when directly addressed (@mention of the agent's account, or shared-mode explicit `/flow`/`@flow` token) — overrides ownership, even on a teammate's issue.\n  3. Resume when an `agent/needs-input` item gets a non-agent comment — that's the answer the agent parked for.\n  4. Stay out of `other`-owned threads unless mentioned.\n  5. Soft zone leans quiet (`comments.ambiguousBias: \"quiet\"`) — over-responding is the worse failure; silence is the safe default.\n- Durable label claims: `claim(item)` writes the `agent/claimed` label + moves state (survives restart). State machine = `agent/*` labels, not the ephemeral plan field.\n- Comment/assign/handoff: `comment`, `assignToHuman`, `needsInput` (elicitation: label + comment + assign-to-human + stop). On a genuine question: post comment + `agent/needs-input` + assign to human + optional nudge; resume on reply.\n- Soft-escalation: any time genuinely stuck, stop + comment + assign-to-human (Decision #2a) — driven by the calibration ladder (task 2.1).\n- Answers become memory: write resolutions to the decisions table / ADR / config.json so the same question isn't asked twice.\n\nConfig: `comments.respondWhen: \"addressed\"`, `comments.ambiguousBias: \"quiet\"`.\n\nUnit tests: the comment-rule suite (each rule → respond/act/ignore decision). Verification: a live read-only dry run in a shared Linear.\n\nAcceptance (P3): in a shared Linear, the agent claims only permitted classes, recognizes its own comments (marker), responds only when addressed, parks on `needs-input` and resumes on reply — verified against the `classifyOwnership`/comment-rule suites + a live read-only dry run.",
      "activeForm": "Implementing inbox polling, comment/assign/handoff, and durable claims",
      "size": "large",
      "priority": "high",
      "dependencies": ["3.1", "2.4"],
      "parallelWith": []
    },
    {
      "id": "3.3",
      "phase": 3,
      "phaseName": "P3 — Agent-as-team-member v1",
      "subject": "[unified-workflow-system] [P3] Implement crash/stall recovery — the FlowRun record and next-tick recovery ladder",
      "description": "Implement durable crash/stall recovery so the next tick adopts orphaned work and resumes it — never restarts from scratch, never reclaims work parked on a human (§12). Sessions are ephemeral; the work is durable.\n\nThe durable run record (the session↔issue association), keyed by issue, written to `flow-state.json` (v1, disk) → server SQLite (v2):\n```ts\nFlowRun {\n  issueId, identifier;          // tracker id + \"DOR-123\" (worktree/branch key)\n  sessionId;                    // Claude SDK JSONL id — for resume\n  worktreePath, branch;         // ~/.dork/workspaces/<project>/<key>/, dork/<key>\n  status;                       // queued|running|waiting_for_review|complete|failed\n  attemptCount; workerPid;      // v1 liveness\n  heartbeatAt;                  // v2 (concurrent) liveness\n  startedAt, completedAt;\n}\n```\n\nNext-tick recovery ladder (driven by disposition, not by comment presence):\n\n| Orphan signal | Action |\n|---|---|\n| `agent/needs-input` (parked on human) | Skip — resumes only on the human's reply; never reclaimed. |\n| `agent/claimed`, In-Progress, no live worker | Adopt + resume if `worktreeExists AND sessionLogIntact AND attemptCount < recovery.maxRetries`; else restart-clean; `attemptCount++`. |\n| over `recovery.maxRetries` | Escalate — `agent/blocked` + comment + nudge. |\n| In-Progress but no local run record (other machine) | Re-derive from tracker + workspace (tracker-as-truth). |\n\nKey behaviors:\n- The checkpoint is the git commit + the JSONL session ⇒ recovery RESUMES (re-attach worktree at HEAD, `resume` the session), never restarts.\n- v1 (sequential, single machine, WIP 1) needs NO heartbeat or lease: any `agent/claimed` + In-Progress + not-`needs-input` item on a fresh tick is orphaned by definition; a `workerPid` check suffices. With the Pulse seat, `sessionId = run.id` is captured per run, making each issue independently resumable.\n- A startup/tick sweep adopts orphans. 'Parked on a human' is a DISTINCT state the stall sweep never reclaims.\n- v2 (concurrent) adds heartbeat (`heartbeatAt`), fencing token (`attemptId`), atomic multi-claim (`BEGIN IMMEDIATE`/`SKIP LOCKED`), and a stall-detector tick — server residue (DOR-89), NOT built here.\n\nConfig: `recovery.maxRetries: 2`, `recovery.onExhausted: \"block\"`, `recovery.staleAfter: \"5m\"` (v2 only). `flow-state.json` follows the ADR-0043 file-first write-through pattern.\n\nUnit tests (Vitest, §Testing Strategy — Recovery ladder): each orphan-signal row → expected action; assert `needs-input` is never reclaimed; `attemptCount` increments; over-retry escalates.\n\nAcceptance (P3): the recovery-ladder unit suite passes; `needs-input` is never reclaimed; `attemptCount` increments on each reclaim; over-`maxRetries` escalates to `agent/blocked`; recovery resumes (re-attach + session resume), never restarts.",
      "activeForm": "Implementing the FlowRun record and crash/stall recovery ladder",
      "size": "large",
      "priority": "high",
      "dependencies": ["3.1", "2.5"],
      "parallelWith": ["3.2"]
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "P4 — Proof-of-completion",
      "subject": "[unified-workflow-system] [P4] Implement VERIFY browser proof-of-completion and evidence-class attachment",
      "description": "Implement the VERIFY stage browser automation + evidence-class attachment onto the PR + tracker (§13).\n\nRun Playwright (`apps/e2e`) for the touched surface; capture per the `evidence` config:\n- UI → annotated GIF (`gif_creator`, claude-in-chrome, interactive) OR WebM (`recordVideo`, unattended/`retain-on-failure`). `evidence.ui: \"auto\"` selects by trigger (interactive vs unattended).\n- temporal → video (`evidence.temporal: \"video\"`).\n- logic → test-pass summary (`evidence.logic: \"test-summary\"`).\n\nAttach via the adapter (`adapters/linear/`, task 1.1): PR comment (ProofShot-style bundle) and/or tracker `externalUrls` (`evidence.attachTo: [\"pr\", \"tracker\"]`).\n\nScope boundary: the unattended/server variant (headless `recordVideo` → automated Linear `fileUpload`/`attachmentCreate`) is the P5 Extension's job (DOR-95); v1 attaches what an interactive/CLI run can produce + the WebM already wired in `apps/e2e`. Resolved direction (P4, non-blocking): v1 attaches the `apps/e2e` WebM + any `gif_creator` capture; richer headless automation is P5.\n\nDependencies: Playwright library `recordVideo` (WebM, already wired in `apps/e2e`, headless); `gif_creator` (claude-in-chrome, interactive only). This wires into the `verifying-work` skill created in P1 (task 1.4).\n\nE2E test (Playwright, `apps/e2e`, §Testing Strategy): the VERIFY stage produces a WebM for a touched UI surface and the bundle links onto a PR/tracker stub. Confirms the proof pipeline end-to-end.\n\nAcceptance (P4): a UI change run through VERIFY yields a recording linked on the PR and the tracker per the `evidence` config; the E2E proof-pipeline test passes.",
      "activeForm": "Implementing VERIFY browser proof-of-completion and evidence attachment",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.4", "2.3"],
      "parallelWith": []
    },
    {
      "id": "4.2",
      "phase": 4,
      "phaseName": "P4 — Proof-of-completion",
      "subject": "[unified-workflow-system] [P4] Author the system templates set (records/docs/pr.md) and complete README/SPEC + docs",
      "description": "Author the template set the system owns and complete the package documentation (§14, §Documentation).\n\nTemplates the system owns (under `.agents/flow/templates/`):\n- `templates/records/` — PM records BY TYPE (issue: idea/research/hypothesis/task; project) with the canonical `## Validation criteria` / `## On Completion` sections. Generalizes `linear-loop/templates/` (triage-*, plan-*, dispatch-priority, audit) and de-Linear-ifies them. New: explicit per-type record bodies (don't exist today).\n- `templates/docs/` — ideation / specification / `03-tasks.json` / ADR scaffolds, externalized from the inline `/ideate` command (the externalization itself happens in task 1.3; this task formalizes the canonical template files here).\n- `templates/pr.md` — the PR template the REVIEW stage fills: linked issue · test/validation summary · browser-proof links. None exists today.\n\nDocumentation files to create:\n- `.agents/flow/README.md` (the manual: stages, modes, command↔state map, gates, adapter interface, AND the autonomous-mode server dependency — 'autonomous mode depends on a running DorkOS server (Pulse); manual mode does not').\n- `.agents/flow/SPEC.md` (the contract: stage model, `PMClient` interface for P5, config schema).\n- `contributing/flow-engine.md` (internal dev guide; cross-link from `contributing/INDEX.md`).\n\nDocumentation files to update:\n- `AGENTS.md` — replace the separate Linear/Worktrees/loop guidance with a `/flow` section + the 'Compact Instructions' block (an `AGENTS.md` block that preserves the current item, stage, gate state, and artifact pointers if auto-compaction fires; §11).\n- `working-in-worktrees`, `executing-specs`, `ideating-features` skills — point at the unified stage model.\n- `.agents/harness.manifest.json` — confirm the `flow` bundle registration is complete (commands/skills/hooks/templates all accounted for).\n\nAcceptance (P4): the records/docs/pr.md template set exists and is loaded by the relevant skills; README/SPEC/`contributing/flow-engine.md` are complete; `AGENTS.md` carries the `/flow` section + Compact Instructions block; the three named skills point at the unified stage model.",
      "activeForm": "Authoring the templates set and completing README/SPEC and docs",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["1.2", "1.3", "1.4", "2.4", "4.1"],
      "parallelWith": []
    },
    {
      "id": "5.1",
      "phase": 5,
      "phaseName": "P5 — Later: the Flow Engine — Extension (DOR-88…)",
      "subject": "[unified-workflow-system] [P5] Document the v1 promotion surface for the Flow Engine — Extension (NOT built here)",
      "description": "Phase 5 (the server-side Flow Engine — Extension, Linear DOR-88…) is explicitly OUT OF SCOPE for this spec and NOT built here. This task only ensures the v1 contracts that form the promotion surface are documented and discoverable so the P5 server build is additive, not a rewrite.\n\nWhat P5 will promote this proven harness into (the single full-stack DorkOS extension — for context, NOT implementation): server `PMClient` (the typed TS interface the `adapters/linear/` prose contract becomes), webhook/`dorkos.ai` relay + full Linear Agent Accounts, server-side `WorkspaceManager` (graduating the `gtr` flow), unattended evidence pipeline (headless `recordVideo` → automated Linear attachment, DOR-95), heartbeat/fencing concurrency (the DOR-89 server residue). A second PM adapter (Jira/GitHub Issues) proves agnosticism.\n\nThe v1 contracts that ARE the promotion surface (already built in P0–P4; this task verifies they are cleanly documented in `.agents/flow/SPEC.md`):\n- the config schema (`config.schema.json` from the Zod source, task 0.3),\n- the `PMClient` verbs (the documented prose contract in `adapters/linear/`, task 1.1; typed `interface PMClient` in SPEC.md),\n- the `FlowRun` record shape (task 3.3).\n\nNon-goals reaffirmed: do NOT build the server `PMClient`, webhook listener, `WorkspaceManager` service, or unattended evidence pipeline here.\n\nAcceptance (P5): out of scope for this spec — the v1 contracts (config schema, `PMClient` verbs, `FlowRun` record) are documented in `.agents/flow/SPEC.md` as the promotion surface; no server code is written.",
      "activeForm": "Documenting the v1 promotion surface for the P5 Extension",
      "size": "small",
      "priority": "low",
      "dependencies": ["0.3", "1.1", "3.3", "4.2"],
      "parallelWith": []
    }
  ]
}
