{
  "spec": "specs/capture-cloud-link-stub/02-specification.md",
  "slug": "capture-cloud-link-stub",
  "generatedAt": "2026-07-17T19:44:26Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "P1 — The construction seam (accessor pair)",
      "subject": "[capture-cloud-link-stub] [P1] Reseat cloudLinkManager to init/getCloudLinkManager, route routes/cloud.ts (4 sites) + index.ts (import/boot/stop) through the accessor, update the cloud.test.ts mock, seam construction unit tests",
      "description": "Replace the eagerly-constructed `cloudLinkManager` module singleton with an accessor pair mirroring `get/setWorkspaceManager`, and route every consumer through it. This is the keystone task — nothing else in the decomposition can land until the seam exists. SEAM (apps/server/src/services/core/auth/cloud-link.ts) — the file currently ends with `/** Process-wide cloud-link manager used by the /api/cloud/* routes and startup. */\\nexport const cloudLinkManager = new CloudLinkManager();` (verified at the tail of the file, :369). Delete that export and replace it with exactly: `let instance: CloudLinkManager | undefined;\\n\\n/**\\n * Construct the process-wide cloud-link manager. Called once at the composition\\n * root ({@link start} in `index.ts`): with no options in production (real\\n * `fetch`, real defaults), or with an injected fake `fetchImpl` under\\n * `DORKOS_TEST_RUNTIME`. Returns the constructed instance.\\n */\\nexport function initCloudLinkManager(options?: CloudLinkManagerOptions): CloudLinkManager {\\n  instance = new CloudLinkManager(options);\\n  return instance;\\n}\\n\\n/**\\n * The process-wide cloud-link manager used by the `/api/cloud/*` routes and\\n * startup. Throws if read before {@link initCloudLinkManager} runs — a loud,\\n * helpful failure instead of a silent `undefined` dereference.\\n */\\nexport function getCloudLinkManager(): CloudLinkManager {\\n  if (!instance) throw new Error('CloudLinkManager not initialized');\\n  return instance;\\n}`. The `CloudLinkManager` class and `CloudLinkManagerOptions` interface are UNCHANGED — the constructor already accepts `options: CloudLinkManagerOptions = {}` (:157) and defaults `fetchImpl` to `undefined`, which downstream `requestDeviceCode`/`pollForToken`/etc. resolve to `defaultFetch = globalThis.fetch` in cloud-link-client.ts (:86) when absent — so `initCloudLinkManager()` called with no options reproduces today's production behavior byte-for-byte. ROUTES (apps/server/src/routes/cloud.ts) — replace the value import at :14, `import { cloudLinkManager } from '../services/core/auth/cloud-link.js';`, with `import { getCloudLinkManager } from '../services/core/auth/cloud-link.js';`. Replace all four call sites, calling `getCloudLinkManager()` fresh at each site (do not cache it in a module-level const — the accessor's whole point is always resolving the live instance): `POST /link/start` (:22) `const result = await cloudLinkManager.startLink();` → `const result = await getCloudLinkManager().startLink();`; `GET /link/status` (:34) `res.json(cloudLinkManager.getStatus());` → `res.json(getCloudLinkManager().getStatus());`; `POST /unlink` (:40) `await cloudLinkManager.unlink();` → `await getCloudLinkManager().unlink();`; `GET /status` (:50) `res.json(cloudLinkManager.getSummary());` → `res.json(getCloudLinkManager().getSummary());`. The routes stay thin; the handlers only ever run post-boot, so the guard never trips in practice. BOOT/STOP + WIRING SLOT (apps/server/src/index.ts) — drop the value import at :26, `import { cloudLinkManager } from './services/core/auth/cloud-link.js';`, and add `import { initCloudLinkManager, getCloudLinkManager } from './services/core/auth/cloud-link.js';`. In the existing runtime-registration block inside `async function start()` (:169), the `if (env.DORKOS_TEST_RUNTIME) { … } else { … }` block at :440-469, add `initCloudLinkManager();` as the LAST statement in BOTH branches (both call it with no options — real fetch, real defaults — since the fake factory does not exist until task 2.1 replaces the `if` branch's call with the fake-injected version; this task must leave the manager always initialized regardless of which later task has landed). Then replace `cloudLinkManager.initOnStartup()` at :1430 with `getCloudLinkManager().initOnStartup()`, and `cloudLinkManager.stop()` at :1479 with `getCloudLinkManager().stop()`. Both calls already run inside `start()`/`stop()` after the registration block executes, so the accessor's guard never trips. ROUTE TEST MOCK (apps/server/src/routes/__tests__/cloud.test.ts) — the existing suite (:6-20) mocks the module as `vi.mock('../../services/core/auth/cloud-link.js', () => ({ cloudLinkManager: { startLink: vi.fn(), getStatus: vi.fn(), unlink: vi.fn(), getSummary: vi.fn() } }));` then `import { cloudLinkManager } from '../../services/core/auth/cloud-link.js'; const manager = vi.mocked(cloudLinkManager);`. Change the mock to export `getCloudLinkManager: () => mockManager` where `mockManager` is the same four-method object literal (hoist it above the `vi.mock` call or inline it per Vitest's mock-factory hoisting rules), and change the import/assignment to `import { getCloudLinkManager } from '../../services/core/auth/cloud-link.js'; const manager = vi.mocked(getCloudLinkManager());` (or equivalent — the goal is that `manager.startLink`/`getStatus`/`unlink`/`getSummary` remain the exact same mock fns the existing 8 `it(...)` cases already assert against, so no test-body changes are needed beyond the mock/import lines). SEAM CONSTRUCTION UNIT TESTS — add to `apps/server/src/services/core/auth/__tests__/cloud-link.test.ts` (extend the existing suite, which already imports `CloudLinkManager` directly and constructs fresh instances per test in nested `describe`/`beforeEach` blocks, :6,44-68) or a new sibling `cloud-link-seam.test.ts` — three cases per the spec's Testing Strategy: (a) `initCloudLinkManager()` with no options, then `getCloudLinkManager()` returns a `CloudLinkManager` instance; spy on `globalThis.fetch` (e.g. `vi.spyOn(globalThis, 'fetch').mockResolvedValue(...)`), drive `getCloudLinkManager().startLink()`, and assert the spy was called — proving the prod-default construction is behavior-preserving and never touches a fake. (b) `initCloudLinkManager({ fetchImpl: spy })` then `getCloudLinkManager().startLink()` calls `spy`, not `globalThis.fetch` — proving the seam is injectable. (c) Calling `getCloudLinkManager()` with no prior `initCloudLinkManager()` call in that test (reset module state first, e.g. `vi.resetModules()` + a fresh dynamic `await import('../cloud-link.js')`, since `instance` is module-level singleton state that otherwise leaks across test files/cases) throws exactly `'CloudLinkManager not initialized'`. Each test carries a one-line purpose comment. Acceptance: `cloud-link.ts` exports `initCloudLinkManager`/`getCloudLinkManager` and no longer exports `cloudLinkManager`; `routes/cloud.ts` and `index.ts` both resolve the manager only through the accessor; `cloud.test.ts`'s existing 8 cases pass unchanged against the new mock shape; the 3 new seam construction cases pass. Verify: pnpm --filter @dorkos/server typecheck && pnpm --filter @dorkos/server lint && pnpm vitest run apps/server/src/routes/__tests__/cloud.test.ts apps/server/src/services/core/auth/__tests__/cloud-link.test.ts. Cites: spec §Detailed Design 1 (exact accessor-pair code block), §Decisions resolved (\"Seam shape — the accessor pair, not a mutable export. RESOLVED\" — the `get/setWorkspaceManager` precedent chosen over the `configManager` mutable-export pattern, which is explicitly rejected as a foot-gun with no pre-init guard), §Code structure & file organization table, §Testing Strategy (\"Seam construction unit — the keystone honesty proof\"), §References (all cited line numbers). No dependencies — this is the keystone task: 2.1's fake wiring, 3.1's capture drive, and every later task assume `init/getCloudLinkManager` already exist.",
      "activeForm": "Reseating cloudLinkManager to the init/getCloudLinkManager accessor pair",
      "size": "md",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "P2 — The fake FetchLike + composition-root wiring + unreachability",
      "subject": "[capture-cloud-link-stub] [P2] Add createFakeCloudLinkFetch() (four scripted endpoints, pinned constants, per-device-code auto-flip, throwing guard) at fake-cloud-link.ts, wire it via gated dynamic import in index.ts, fake behavior + guard + import-graph tests",
      "description": "Add a `DORKOS_TEST_RUNTIME`-only fake `FetchLike` next to `demo-scenarios.ts`, wire it into the composition root behind the existing gate, and prove — structurally and by test — that it can never reach a production build. THE FAKE MODULE (new file apps/server/src/services/runtimes/test-mode/fake-cloud-link.ts) — mirror `demo-scenarios.ts`'s module-doc honesty pattern (\"These exist ONLY to stage beautiful, truthful UI … reachable only when `DORKOS_TEST_RUNTIME=true` … Nothing here is wired into production\", demo-scenarios.ts:7-15) and `test-control.ts`'s unreachability doc (\"Only mounted when DORKOS_TEST_RUNTIME=true. Returns 404 … in production\", test-control.ts:11-12). Import `type { FetchLike, DeviceCodeResponse } from '../../core/auth/cloud-link-client.js'` (type-only, erased at runtime — the fake→prod direction is allowed even though prod never imports the fake) and `import { env } from '../../../env.js';`. Pinned deterministic constants (module-level, no `Date.now()` in anything a screenshot shows) — copy exactly: `const FAKE_USER_CODE = 'DORK-2F7Q';` (pending code shown in the panel), `const FAKE_ACCOUNT_LABEL = 'Dork Labs';` (account label shown linked), `const FAKE_DEVICE_CODE = 'fake-device-code';` (opaque, never shown), `const FAKE_ACCESS_TOKEN = 'fake-instance-key';` (persisted at `cloud.instanceToken`, never shown/logged), `const FAKE_INSTANCE_ID = 'capture-instance';` (echoed by heartbeat), `const DEVICE_CODE_INTERVAL_SECONDS = 1;` (server poll cadence), `const DEVICE_CODE_EXPIRES_IN_SECONDS = 900;` (long enough to never visibly expire mid-capture), `const PENDING_POLLS_BEFORE_APPROVAL = 2;` (token polls answered `authorization_pending` before approval) — each carrying the one-line TSDoc comment given above, verbatim. THE FACTORY — copy the full function verbatim from spec Detailed Design §2: `export function createFakeCloudLinkFetch(): FetchLike { if (!env.DORKOS_TEST_RUNTIME) { throw new Error('createFakeCloudLinkFetch is test-mode only (DORKOS_TEST_RUNTIME)'); } const pollCounts = new Map<string, number>(); return async (input, init) => { const { pathname } = new URL(input); const json = (status: number, body: unknown) => new Response(JSON.stringify(body), { status, headers: { 'content-type': 'application/json' } }); if (pathname.endsWith('/api/auth/device/code')) { return json(200, { device_code: FAKE_DEVICE_CODE, user_code: FAKE_USER_CODE, verification_uri: 'https://dorkos.ai/activate', verification_uri_complete: `https://dorkos.ai/activate?code=${FAKE_USER_CODE}`, expires_in: DEVICE_CODE_EXPIRES_IN_SECONDS, interval: DEVICE_CODE_INTERVAL_SECONDS } satisfies DeviceCodeResponse); } if (pathname.endsWith('/api/auth/device/token')) { const { device_code } = JSON.parse(String(init?.body ?? '{}')) as { device_code?: string }; const key = device_code ?? FAKE_DEVICE_CODE; const seen = pollCounts.get(key) ?? 0; pollCounts.set(key, seen + 1); if (seen < PENDING_POLLS_BEFORE_APPROVAL) { return json(400, { error: 'authorization_pending' }); } return json(200, { access_token: FAKE_ACCESS_TOKEN }); } if (pathname.endsWith('/api/instances/heartbeat')) { return json(200, { instanceId: FAKE_INSTANCE_ID, lastSeenAt: new Date().toISOString(), accountLabel: FAKE_ACCOUNT_LABEL }); } if (pathname.endsWith('/api/instances/revoke')) { return json(200, {}); } throw new Error(`fake cloud-link: unexpected request ${pathname}`); }; }` — the guard checks `env.DORKOS_TEST_RUNTIME` (the VALIDATED boolean from `apps/server/src/env.ts:90-93`, `z.string().optional().transform(v => v === 'true')`), never a raw string comparison like `!== 'true'` (a correction the SPECIFY stage made to the ideation). Poll counting is per `device_code` (`pollCounts` keyed by the code), so a future unlink→relink cycle resets deterministically. An unknown pathname throws — fail loud, never a silent escape. COMPOSITION-ROOT WIRING (apps/server/src/index.ts) — in the `if (env.DORKOS_TEST_RUNTIME) { … }` branch of the runtime-registration block (:440-469), REPLACE the interim `initCloudLinkManager();` call task 1.1 added with: `const { createFakeCloudLinkFetch } = await import('./services/runtimes/test-mode/fake-cloud-link.js'); initCloudLinkManager({ fetchImpl: createFakeCloudLinkFetch() });` — placed after the existing `TestModeRuntime` registration lines, mirroring that block's own dynamic-import pattern (`const { TestModeRuntime } = await import('./services/runtimes/test-mode/test-mode-runtime.js');`). Dynamic `import()` keeps `fake-cloud-link.ts` OUT of the production module graph — exactly like `TestModeRuntime` and the test-control reset path (test-control.ts:44-50). Leave the `else` branch's `initCloudLinkManager();` (from task 1.1, real fetch, real defaults) UNCHANGED — it already satisfies \"production `else` branch calls `initCloudLinkManager()` (no options)\". This still runs before `getCloudLinkManager().initOnStartup()` (index.ts:1430) and any request, so the accessor's guard never trips; in test mode the isolated capture `config.json` holds no `cloud.instanceToken`, so `initOnStartup()` returns early (cloud-link.ts:240-241) and never heartbeats until the drive links. FAKE BEHAVIOR + GUARD + IMPORT-GRAPH TESTS (new file apps/server/src/services/runtimes/test-mode/__tests__/fake-cloud-link.test.ts) — with `env.DORKOS_TEST_RUNTIME` stubbed true (e.g. `vi.mock('../../../../env.js', ...)` or direct property stub, matching this repo's existing env-stubbing pattern in other test-mode tests): (a) `device/code` request → 200 with the pinned `user_code`, `verification_uri: 'https://dorkos.ai/activate'`, and `interval: 1` — purpose: pinned, deterministic pending content. (b) `device/token` → `400 {error: 'authorization_pending'}` for the first `PENDING_POLLS_BEFORE_APPROVAL` (2) calls to the returned `FetchLike`, then `200 {access_token: FAKE_ACCESS_TOKEN}` on the 3rd — purpose: the offline auto-flip, deterministically. (c) `device/token` counting is per `device_code` — driving a second, distinct `device_code` starts its own count from 0 — purpose: relink determinism, no cross-cycle bleed. (d) `heartbeat` → 200 with the pinned `accountLabel` and a fresh ISO `lastSeenAt` — purpose: the linked view shows the account + \"just now\". (e) `revoke` → 200 — purpose: best-effort no-op covered. (f) an unknown pathname → throws — purpose: fail loud, never a silent escape. GUARD unit (same file): (g) `createFakeCloudLinkFetch()` throws when `env.DORKOS_TEST_RUNTIME` is false; (h) returns a callable `FetchLike` when true. IMPORT-GRAPH unit (same file or a sibling): (i) read the source text of `cloud-link.ts`, `cloud-link-client.ts`, and `routes/cloud.ts` (e.g. `fs.readFileSync`) and assert none contain the string `'fake-cloud-link'` — purpose: the structural half of unreachability, asserted rather than assumed. Each test carries a one-line purpose comment; no always-pass tests. Acceptance: `fake-cloud-link.ts` exists with the exact pinned constants and factory shape; `index.ts`'s `DORKOS_TEST_RUNTIME` branch wires the fake via gated dynamic import; the `else` branch is untouched from task 1.1; all 9 test cases (6 behavior + 2 guard + 1 import-graph) pass. Verify: pnpm --filter @dorkos/server typecheck && pnpm --filter @dorkos/server lint && pnpm vitest run apps/server/src/services/runtimes/test-mode/__tests__/fake-cloud-link.test.ts. Cites: spec §Detailed Design 2 (the fake, exact code block and pinned constants), §Detailed Design 3 (composition-root wiring, exact code block), §Detailed Design 4 (the three layered unreachability defenses), §Decisions resolved (\"The throwing guard checks the validated boolean, not the raw string. RESOLVED\"), §Testing Strategy (fake behavior unit, guard unit, import-graph unit), §Code structure & file organization table. Depends on 1.1 (needs `initCloudLinkManager`/`getCloudLinkManager` to exist before the composition-root wiring can call them, and `CloudLinkManagerOptions`/`FetchLike` are already exported from the seam's neighborhood).",
      "activeForm": "Adding the fake FetchLike and wiring it under the DORKOS_TEST_RUNTIME gate",
      "size": "md",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "P3 — The capture drive (shots + shard pin)",
      "subject": "[capture-cloud-link-stub] [P3] Register accounts-pending + accounts-linked (pinned to SHARD_0_PINNED_SHOTS), update the shots.test.ts pin-equality assertion, add the shootCloudLink drive and its guarded call at the end of captureLightStills",
      "description": "Register the two docs stills as a single pinned-shard flow and add the drive that captures both in one linear pass. REGISTRY (apps/e2e/capture/shots.ts) — add to the \"Desktop stills (docs-only; not on /features)\" block (the block currently ends with the `marketplace-installed` entry, immediately before the `SHOTS_BY_ID` index construction): `{ id: 'accounts-pending', kind: 'still', frame: 'desktop', consumers: ['docs'] },\\n  { id: 'accounts-linked', kind: 'still', frame: 'desktop', consumers: ['docs'] },`. SHARD PIN — both stills come from ONE linear device flow (link → pending → auto-flip → linked) and must ride the same shard, or a round-robin split (`partitionShots`) would put them on different stacks and break the flow. `SHARD_0_PINNED_SHOTS` currently reads `export const SHARD_0_PINNED_SHOTS: readonly string[] = ['multi-session', 'mobile-sessions', 'agent-discovery'];` — change it to `['multi-session', 'mobile-sessions', 'agent-discovery', 'accounts-pending', 'accounts-linked']`, following the same single-stack rationale the session-list shots already use (add a one-line comment explaining the accounts pair rides shard 0 because both stills come from one linear flow). UPDATE THE PIN ASSERTION (apps/e2e/capture/__tests__/shots.test.ts) — the existing `it('pins the session-list surfaces and agent-discovery', ...)` case asserts `expect(SHARD_0_PINNED_SHOTS).toEqual(['multi-session', 'mobile-sessions', 'agent-discovery']);` then `for (const id of SHARD_0_PINNED_SHOTS) expect(getShot(id)).toBeDefined();`. Update the `toEqual` array literal to match the new 5-element `SHARD_0_PINNED_SHOTS`; the `getShot(id)` loop passes unchanged once the registry entries above exist. THE DRIVE (apps/e2e/capture/surfaces-desktop.ts) — `attempt` and `isShotSkipped` are already imported from `./lib.js` at the top of this file (used by the existing `captureAgentDiscovery`), so no new import is needed for those two. Add, as a new function (selectors verified against `apps/client/src/layers/features/cloud-link/ui/CloudLinkPanel.tsx`): `/** Open Settings → DorkOS account, link, and shoot the pending then linked states. */\\nasync function shootCloudLink(page: Page, theme: Theme, rec: RunRecorder): Promise<void> {\\n  await page.goto(url('/agents?settings=account'));\\n  await page.getByRole('heading', { name: 'DorkOS account' }).waitFor({ timeout: WAIT_MS });\\n  await page.getByRole('button', { name: 'Link this instance' }).click({ timeout: WAIT_MS });\\n\\n  // Pending: the code + the \"waiting\" status render immediately (optimistic).\\n  await page\\n    .getByText('Waiting for you to approve', { exact: false })\\n    .waitFor({ timeout: WAIT_MS });\\n  await shoot(page, 'accounts-pending', theme, rec);\\n\\n  // Linked: the fake auto-flips; the client's 2500ms status poll lands \"Linked\"\\n  // within a couple of ticks. Wait the money state — no arbitrary sleep.\\n  await page.getByText('Linked', { exact: true }).waitFor({ timeout: WAIT_MS });\\n  await page.getByText('Dork Labs', { exact: false }).waitFor({ timeout: WAIT_MS });\\n  await shoot(page, 'accounts-linked', theme, rec);\\n}` — `shoot`, `url`, `WAIT_MS` are already imported from `./lib.js`/`./config.js` at the top of the file (used by every existing `shoot*` function). INVOKE IT — place the call at the very END of `captureLightStills` (after the current last statement, the `shootWorkbench` call), so linking persists a token into the isolated capture config only after every earlier surface has already been shot (running last keeps that state from bleeding into any earlier surface): `if (!isShotSkipped('accounts-pending')) {\\n  await attempt('accounts (pending→linked)', () => shootCloudLink(page, theme, rec));\\n}` — this mirrors the `captureAgentDiscovery`/`isShotSkipped` guard pattern already used at surfaces-desktop.ts:513 (skip when a human override supplies it, or it belongs to another shard). CRITICAL SEQUENCING NOTE: the capture stack always runs with `DORKOS_TEST_RUNTIME=true` (apps/e2e/capture/boot.ts:42), so clicking \"Link this instance\" always calls `getCloudLinkManager().startLink()` on the server. Without task 2.1 landed first, that resolves to the real-fetch default from task 1.1 and would attempt a live `fetch` to `https://dorkos.ai` inside the sandbox (hanging or failing the drive) — so this task's drive is only exercisable once 2.1's fake wiring is live. NO CHANGES TO config.ts — per the spec's resolved correction, the pinned `user_code`/`accountLabel` live in the fake (server-side, task 2.1), not in `apps/e2e/capture/config.ts` (a different package the fake cannot import); the drive above waits on selectors, never on the exact code string, so no new pinned values are needed here. Acceptance: `accounts-pending` and `accounts-linked` are registered with `consumers: ['docs']`; both are in `SHARD_0_PINNED_SHOTS`; the `shots.test.ts` pin-equality assertion matches; `shootCloudLink` exists and is invoked exactly once, guarded, at the end of `captureLightStills`. Verify: pnpm --filter @dorkos/e2e typecheck && pnpm vitest run apps/e2e/capture/__tests__/shots.test.ts. Cites: spec §Detailed Design 5 (registry + shard pin + drive, exact code blocks), §Decisions resolved (\"Determinism lives in the fake, not apps/e2e/capture/config.ts\"), §Code structure & file organization table, §References (capture pipeline file:line citations). Depends on 2.1 (the drive is only meaningfully exercisable once the fake is wired — see the sequencing note above; the spec's own Implementation Phases explicitly orders Phase 3 after Phase 2).",
      "activeForm": "Registering the two shots and adding the shootCloudLink drive",
      "size": "sm",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": []
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "P4 — Capture run + docs embeds",
      "subject": "[capture-cloud-link-stub] [P4] Run capture record+process to publish accounts-pending/accounts-linked + the manifest, embed both via <ProductShot> in the two \"Linking from Settings\" Steps of dorkos-accounts.mdx, confirm the site docs-embed guard is green",
      "description": "Produce the real assets from the drive built in 3.1, then land the docs embeds that depend on them — this task is explicitly sequenced last among the code/asset work because the site's docs-embed guard cannot pass until the PNGs and manifest exist. CAPTURE RUN — from the repo root, run `pnpm --filter @dorkos/e2e capture:record` (boots an isolated `DORK_HOME`, a test-mode server + Vite client, drives every registered shot including the new `shootCloudLink` flow from 3.1, and saves raw recordings/screenshots into `apps/e2e/capture/library/<run-id>/raw/`), then `pnpm --filter @dorkos/e2e capture:process` (edits raws + applies any overrides into `apps/site/public/product/`, regenerating `apps/site/public/product/manifest.json` via `writeManifest` with the registry snapshot). Equivalently, `pnpm --filter @dorkos/e2e capture` runs both phases. Confirm the run output reports `accounts-pending` and `accounts-linked` captured (not skipped) — if either is skipped, check `SHARD_0_PINNED_SHOTS`/`isShotSkipped` overrides from 3.1 and the `DORKOS_TEST_RUNTIME`-gated fake wiring from 2.1 both landed. Expect `apps/site/public/product/accounts-pending-light.png` and `apps/site/public/product/accounts-linked-light.png` to exist after the process phase (both `kind: 'still'`, so no `-dark.webm` is expected — only loops publish a video). DOCS EMBEDS (docs/self-hosting/dorkos-accounts.mdx) — the \"Linking from Settings\" section has two `<Step>`s. `ProductShot` is a global MDX component (verified: no import line exists anywhere across `docs/**`, so no import is needed here). After the \"Link this instance\" step's body (\"Click **Link this instance**. The panel shows the same 8-character code and an **Open dorkos.ai/activate** button (the code is pre-filled).\"), add on its own line: `<ProductShot id=\"accounts-pending\" alt=\"Settings → DorkOS account showing the 8-character link code and the 'Open dorkos.ai/activate' button, waiting for approval\" />`. After the \"Approve, and watch it flip\" step's body (\"Approve at `/activate`. The panel updates **in place**, from _pending_ to _linked_, showing the linked account, the instance name, and the last heartbeat. An expired code offers **Generate a new code**.\"), add: `<ProductShot id=\"accounts-linked\" alt=\"The same Settings panel flipped to Linked — a green dot, the linked account, and 'Last synced just now'\" />`. Do not touch any other section of the file (the `dorkos cloud` CLI steps above, or the \"instance registry\"/\"Revoking an instance\" sections below, are all out of scope — the site `/account/instances` table is explicitly DOR-283-parked). VERIFY THE GUARD — the load-bearing check is `apps/site/src/layers/features/marketing/lib/__tests__/shots.test.ts`'s `describe('docs ProductShot embeds', ...)` block: it scans every `<ProductShot id=\"…\">` under `docs/**`, and for each asserts (a) the id is registered in the published manifest (`getProductShot(id)` truthy), (b) `<id>-light.png` exists under `apps/site/public/product/`, and (c) the shot's `consumers` includes `'docs'`. This only goes green once the capture run above has published both assets and the manifest reflects `consumers: ['docs']` for both ids (already set in 3.1's registry entries). Acceptance: `apps/site/public/product/accounts-pending-light.png` and `apps/site/public/product/accounts-linked-light.png` exist; `apps/site/public/product/manifest.json` lists both shots; both `<ProductShot>` tags are embedded at the correct Steps in `dorkos-accounts.mdx`; the site docs-embed guard test passes. Verify: pnpm --filter @dorkos/e2e capture:record && pnpm --filter @dorkos/e2e capture:process && pnpm vitest run apps/site/src/layers/features/marketing/lib/__tests__/shots.test.ts && pnpm --filter @dorkos/site typecheck. Cites: spec §Detailed Design 6 (the two embed locations, exact `<ProductShot>` tags with alt text), §Detailed Design 5 (\"Sequencing (load-bearing)\" — the guard cannot pass until a real capture+process run has published the PNGs and manifest), §Implementation Phases (\"Phase 4 — capture run + docs embeds\"), §Technical Dependencies (`ProductShot` is global, no import line), §References (docs target + guard file:line citations). Depends on 3.1 (the drive and shard pin must exist before a capture run can produce these two assets).",
      "activeForm": "Running the capture record+process pipeline and embedding the two docs stills",
      "size": "sm",
      "priority": "high",
      "dependencies": ["3.1"],
      "parallelWith": []
    },
    {
      "id": "5.1",
      "phase": 5,
      "phaseName": "P5 — Close-out (draft ADR + follow-up pointer)",
      "subject": "[capture-cloud-link-stub] [P5] Extract the draft ADR via /adr:from-spec (accessor seam + fake honesty model + layered unreachability), record the flip-loop follow-up pointer for DONE — no changelog fragment (justified: zero product behavior change)",
      "description": "Close out the spec with its one durable design-decision artifact and a findable pointer for the follow-up the SPECIFY stage deliberately deferred — and explicitly do NOT add a changelog fragment. DRAFT ADR — run `/adr:from-spec specs/capture-cloud-link-stub/02-specification.md` (applies the significance rubric at extraction and writes `decisions/<id>-<slug>.md` + the `decisions/manifest.json` entry using a timestamp id from `.claude/scripts/id.ts`, `YYMMDD-HHMMSS`, e.g. matching the format of the most recent manifest entries); if the command is unavailable, write it by hand per the `writing-adrs` skill with the same content. Status: `proposed`. Title: capture the seam + the fake's honesty model — e.g. \"Test-mode cloud transport: a DORKOS_TEST_RUNTIME-gated fake FetchLike behind a CloudLinkManager construction seam.\" Content, copied from spec §Related ADRs verbatim (all five items are load-bearing, not optional): (1) the honesty model — the fake fakes ONLY the `FetchLike` network dependency to `dorkos.ai`, exactly like `demo-scenarios.ts` fakes the agent backend; the real state machine, token persistence, all four `/api/cloud/*` routes, and the real client panel run unchanged; (2) the construction seam — reseat the eager `cloudLinkManager` singleton to an `init/getCloudLinkManager` accessor pair with a throwing guard, mirroring `get/setWorkspaceManager` (chosen over the `configManager` mutable-export pattern, which was considered and rejected as a foot-gun with no pre-init guard), so the composition root owns construction and production `cloud-link.ts` carries zero test branches; (3) the composition-root injection under the existing `DORKOS_TEST_RUNTIME` branch via dynamic `import()`, mirroring `TestModeRuntime`; (4) the layered unreachability proof — structural (dynamic import, absent from the prod module graph), runtime (the throwing guard on the validated `env.DORKOS_TEST_RUNTIME` boolean), and test (prod-default-construction + import-graph assertions); (5) the two-stills-for-MVP scope decision and the deferred flip-loop follow-up (see below). Cross-link `specs/accounts-and-auth`, `specs/cloud-account-management` (the shipped device-flow/persistence/heartbeat this spec photographs, unchanged), and the `TestModeRuntime`/`test-control.ts` precedent this seam mirrors. Add the `decisions/manifest.json` entry (status `proposed`, `extractedFrom`/`specSlug: capture-cloud-link-stub`, following the shape of the existing entries at the tail of that file). FOLLOW-UP POINTER (recorded here, NOT filed as a tracker issue — DECOMPOSE and EXECUTE do not write to the tracker; filing it is DONE stage's job per the `flow__closing-work` skill) — write a clearly labeled \"Follow-up for DONE\" note into this task's own completion summary (e.g. the PR description, or a short Consequences/Follow-ups section at the bottom of the draft ADR) naming the ONE deferred item so DONE has a concrete pointer instead of re-reading the full specification: **the flip loop.** Spec §Decisions resolved (\"Open Q1 — two stills vs. a flip loop. RESOLVED: two docs stills for the MVP; the flip loop is a follow-up.\") — a single `loop` of the in-place pending→linked flip is a more literal, more compelling reading than two stills, but it needs the fake to hold `pending` long enough to record the code and then flip inside the loop window, and it carries marketing weight plus a larger size budget than this spec's size-3 scope justified. The seam and fake built in 1.1/2.1 make the loop cheap once scoped: `PENDING_POLLS_BEFORE_APPROVAL` and the interval constants in `fake-cloud-link.ts` are exactly the knobs a loop drive would retune. NO CHANGELOG FRAGMENT (the honest call, per spec §Documentation) — this issue ships ZERO product behavior change: the pending→linked flip already worked and already shipped before this spec; the seam is a behavior-preserving refactor invisible to users; the fake is test-only; the only user-visible artifact is two screenshots added to an existing docs page. Do not create a `changelog/unreleased/*.md` fragment for this work — the repo's changelog documents what the product does for users, and the capturing-product-media pipeline routinely refreshes docs media without a fragment. (The steelman — \"new docs images are user-visible, so a small Added entry\" — is explicitly rejected in the spec: it would set a precedent that every screenshot refresh needs a fragment, and the user gains nothing new to do.) Acceptance: `decisions/<id>-<slug>.md` exists with status `proposed`, capturing all five content items above with the cross-links; `decisions/manifest.json` is valid JSON with the new entry; the flip-loop follow-up is recorded in a findable, explicitly labeled spot for DONE to pick up; NO changelog fragment was added for this spec. Verify: node -e \"JSON.parse(require('fs').readFileSync('decisions/manifest.json','utf8'))\" (confirms the manifest still parses) && a manual read-through of the new ADR file against the five content items above. Cites: spec §Related ADRs (the full Proposed ADR content list, verbatim), §Decisions resolved (\"Open Q1 — two stills vs. a flip loop. RESOLVED\" — the follow-up's exact reasoning), §Documentation (\"Changelog: NONE — the honest call\", full rejection-of-the-steelman reasoning), §Implementation Phases (\"Phase 5 — close-out\"). Depends on 2.1 (the ADR documents the server seam + fake's exact honesty gate and construction pattern, which must be landed code — not just spec prose — to cite accurately) and 4.1 (the spec's own two-stills-vs-loop framing and the shipped docs surface are part of what the ADR and follow-up note describe; sequencing the close-out after the visible artifact exists matches the repo's own precedent of writing user-facing/decision prose after the surface it describes). No further parallelization — this is the closing task.",
      "activeForm": "Extracting the draft ADR and recording the flip-loop follow-up pointer",
      "size": "sm",
      "priority": "medium",
      "dependencies": ["2.1", "4.1"],
      "parallelWith": []
    }
  ]
}
