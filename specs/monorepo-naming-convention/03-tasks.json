{
  "spec": "specs/monorepo-naming-convention/02-specification.md",
  "slug": "monorepo-naming-convention",
  "generatedAt": "2026-03-01T16:00:00Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Rename & Regenerate",
      "subject": "[monorepo-naming-convention] [P1] Rename apps/web to apps/site and update package config",
      "description": "Rename the marketing/docs app directory and update its package identity.\n\n## Steps\n\n1. **Rename directory:**\n   ```bash\n   git mv apps/web apps/site\n   ```\n\n2. **Update package name** in `apps/site/package.json`:\n   Change `\"name\": \"@dorkos/web\"` to `\"name\": \"@dorkos/site\"`\n\n3. **Update script comments** in `apps/site/scripts/generate-api-docs.ts` (lines 8-9):\n   - `Must be run from the apps/web/ directory` → `Must be run from the apps/site/ directory`\n   - `Run via: npm run generate:api-docs -w apps/web` → `Run via: npm run generate:api-docs -w apps/site`\n\n4. **Regenerate lockfile:**\n   ```bash\n   pnpm install\n   ```\n   Verify no workspace resolution errors.\n\n## Acceptance Criteria\n- [ ] `apps/site/` exists, `apps/web/` does not\n- [ ] `apps/site/package.json` has `\"name\": \"@dorkos/site\"`\n- [ ] `apps/site/scripts/generate-api-docs.ts` references `apps/site/` not `apps/web/`\n- [ ] `pnpm install` succeeds without errors",
      "activeForm": "Renaming apps/web to apps/site",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Update Live References",
      "subject": "[monorepo-naming-convention] [P2] Update AGENTS.md structure section and references",
      "description": "Update AGENTS.md with the rename and fix the stale app/package counts.\n\n## Changes\n\n### 1. Update count (line 17)\nChange:\n```\nThis is a Turborepo monorepo with four apps and four shared packages:\n```\nTo:\n```\nThis is a Turborepo monorepo with five apps and seven shared packages:\n```\n\n### 2. Update ASCII tree (lines 19-36)\nReplace the current tree with:\n```\ndorkos/\n├── apps/\n│   ├── client/           # @dorkos/client - React 19 SPA (Vite 6, Tailwind 4, shadcn/ui)\n│   ├── server/           # @dorkos/server - Express API (tsc, NodeNext)\n│   ├── site/             # @dorkos/site - Marketing site & docs (Next.js 16, Fumadocs)\n│   ├── obsidian-plugin/  # @dorkos/obsidian-plugin - Obsidian plugin (Vite lib, CJS)\n│   └── e2e/              # @dorkos/e2e - Playwright browser tests\n├── packages/\n│   ├── cli/              # dorkos - Publishable npm CLI (esbuild bundle)\n│   ├── shared/           # @dorkos/shared - Zod schemas, types (JIT .ts exports)\n│   ├── db/               # @dorkos/db - Drizzle ORM schemas (SQLite)\n│   ├── relay/            # @dorkos/relay - Inter-agent message bus\n│   ├── mesh/             # @dorkos/mesh - Agent discovery & registry\n│   ├── typescript-config/ # @dorkos/typescript-config - Shared tsconfig presets\n│   └── test-utils/       # @dorkos/test-utils - Mock factories, test helpers\n```\n\n### 3. Update documentation section reference (line 269)\nChange:\n```\nThe `apps/web` workspace (`@dorkos/web`) is a Next.js 16 marketing site\n```\nTo:\n```\nThe `apps/site` workspace (`@dorkos/site`) is a Next.js 16 marketing site\n```\n\n### 4. Search for any other `apps/web` or `@dorkos/web` references in AGENTS.md\nUse grep to find all occurrences and update them. Known locations:\n- Monorepo structure ASCII tree\n- Documentation section paragraph\n- Any command examples referencing `@dorkos/web`\n\n## Acceptance Criteria\n- [ ] AGENTS.md says \"five apps and seven shared packages\"\n- [ ] ASCII tree includes `apps/e2e`, `packages/db`, `packages/relay`, `packages/mesh`\n- [ ] ASCII tree shows `site/` not `web/` with `@dorkos/site`\n- [ ] No remaining `apps/web` or `@dorkos/web` references in AGENTS.md\n- [ ] Documentation section references `apps/site` workspace",
      "activeForm": "Updating AGENTS.md structure section",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.2"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Update Live References",
      "subject": "[monorepo-naming-convention] [P2] Update remaining live documentation files",
      "description": "Update all non-AGENTS.md live documentation that references `apps/web` or `@dorkos/web`.\n\n## Files to Update\n\n### 1. `CONTRIBUTING.md` (line 36)\nChange table row:\n```\n| `apps/web` | `@dorkos/web` | Marketing site & docs (Next.js 16, Fumadocs) |\n```\nTo:\n```\n| `apps/site` | `@dorkos/site` | Marketing site & docs (Next.js 16, Fumadocs) |\n```\nAlso add missing entries for `apps/e2e`, `packages/db`, `packages/relay`, `packages/mesh` if not already present.\n\n### 2. `contributing/project-structure.md` (line 14)\nChange ASCII tree entry:\n```\n│   ├── web/              # @dorkos/web — Marketing site & docs (Next.js 16, Fumadocs)\n```\nTo:\n```\n│   ├── site/             # @dorkos/site — Marketing site & docs (Next.js 16, Fumadocs)\n```\nAlso add missing app/package entries to match the updated AGENTS.md structure.\n\n### 3. `contributing/environment-variables.md` (line 17)\nChange table row:\n```\n| `apps/web`       | `apps/web/src/env.ts`                    |\n```\nTo:\n```\n| `apps/site`      | `apps/site/src/env.ts`                   |\n```\n\n### 4. `docs/contributing/development-setup.mdx` (lines 107, 137)\n- Line 107: Change `<Folder name=\"web\">` to `<Folder name=\"site\">`\n- Line 137: Change table row from `apps/web` / `@dorkos/web` to `apps/site` / `@dorkos/site`\n\n### 5. `.claude/agents/typescript/typescript-expert.md` (line 247)\nChange:\n```\n{ \"path\": \"./apps/web\" }\n```\nTo:\n```\n{ \"path\": \"./apps/site\" }\n```\n\n### 6. `apps/e2e/BROWSER_TEST_PLAN.md` (line 213)\nChange:\n```\n## 10. Marketing Site (apps/web)\n```\nTo:\n```\n## 10. Marketing Site (apps/site)\n```\n\n## Important: DO NOT update historical artifacts\nThe following files contain `apps/web` references but are point-in-time historical documents — leave them unchanged:\n- `specs/*/` files\n- `research/` files\n- `plans/` files\n- `decisions/` files\n\n## Acceptance Criteria\n- [ ] All 6 live documentation files updated\n- [ ] No remaining `apps/web` or `@dorkos/web` in any live contributing guide\n- [ ] Missing packages/apps added to CONTRIBUTING.md and contributing/project-structure.md tables\n- [ ] Historical artifacts NOT modified",
      "activeForm": "Updating remaining documentation files",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.1"]
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Verification",
      "subject": "[monorepo-naming-convention] [P3] Verify builds, typecheck, and lint pass",
      "description": "Run the full verification suite to confirm the rename didn't break anything.\n\n## Verification Commands\n\n1. **Lockfile regeneration:**\n   ```bash\n   pnpm install\n   ```\n   Should complete without workspace resolution errors.\n\n2. **Site build:**\n   ```bash\n   turbo build --filter=@dorkos/site\n   ```\n   Next.js should build successfully with the new package name.\n\n3. **Full typecheck:**\n   ```bash\n   pnpm typecheck\n   ```\n   All packages should pass — no unresolved `@dorkos/web` imports.\n\n4. **Lint:**\n   ```bash\n   pnpm lint\n   ```\n   No new lint errors.\n\n5. **Tests:**\n   ```bash\n   pnpm test -- --run\n   ```\n   All existing tests should pass (none reference `@dorkos/web` directly).\n\n6. **Grep verification:**\n   ```bash\n   grep -r '@dorkos/web' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.md' --include='*.mdx' apps/ packages/ contributing/ docs/ AGENTS.md CONTRIBUTING.md .claude/\n   ```\n   Should return NO matches in live files (only historical specs/research/plans/decisions may still reference it).\n\n## Acceptance Criteria\n- [ ] `pnpm install` succeeds\n- [ ] `turbo build --filter=@dorkos/site` succeeds\n- [ ] `pnpm typecheck` passes\n- [ ] `pnpm lint` passes\n- [ ] `pnpm test -- --run` passes\n- [ ] No `@dorkos/web` references in live code/config/docs",
      "activeForm": "Verifying builds and checks pass",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1", "2.2"],
      "parallelWith": []
    }
  ]
}
