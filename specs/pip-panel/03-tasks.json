{
  "spec": "specs/pip-panel/02-specification.md",
  "slug": "pip-panel",
  "generatedAt": "2026-07-11T10:10:00-05:00",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Foundation: primitive + state",
      "subject": "[pip-panel] [P1] Build the floating-panel primitive (drag, resize, clamp, chrome, a11y)",
      "description": "Create the presentational floating-panel primitive at `apps/client/src/layers/shared/ui/floating-panel.tsx`. It is a controlled component: geometry lives entirely in props, the primitive owns gesture mechanics only, and it must not import the app store, `useAppStore`, or any router hook — verify with `grep -n \"app-store\\|react-router\" apps/client/src/layers/shared/ui/floating-panel.tsx` returning nothing.\n\nExport exactly these types and the component, matching this shape verbatim:\n\n```tsx\nexport interface FloatingPanelGeometry {\n  x: number;\n  y: number;\n  width: number;\n  height: number;\n}\n\nexport interface FloatingPanelProps {\n  title: string;\n  geometry: FloatingPanelGeometry;\n  onGeometryChange: (g: FloatingPanelGeometry) => void;\n  onClose: () => void;\n  /** Renders a restore control when provided (e.g. \\\"send back inline\\\"). */\n  onRestore?: () => void;\n  minWidth?: number; // default 280\n  minHeight?: number; // default 180\n  children: React.ReactNode;\n  className?: string;\n}\n\nexport function FloatingPanel(props: FloatingPanelProps): React.ReactNode\n```\n\nAlso export a pure helper `export function clampGeometry(geometry: FloatingPanelGeometry, minWidth: number, minHeight: number): FloatingPanelGeometry` used internally by the primitive (and directly unit-testable). It must: cap `width`/`height` to at least `minWidth`/`minHeight` and at most `window.innerWidth - 16` / `window.innerHeight - 16` (the 8px-per-edge viewport margin called out in the goals); clamp `x` to `[0, Math.max(0, window.innerWidth - width)]` and `y` to `[0, Math.max(0, window.innerHeight - height)]` so the panel is always fully reachable inside the viewport.\n\nRendering and mechanics:\n- Portal to `document.body` via `createPortal`. Root container: `fixed z-40` — this repo's `z-40` convention is the embedded sidebar backdrop (`apps/client/src/App.tsx:108`, `className=\"absolute inset-0 z-40 bg-black/40\"`), which sits below the Radix portal layer's uniform `z-50` (dialogs, popovers, sheets, menus) — confirm with `grep -rn \"z-50\" apps/client/src/layers/shared/ui/dialog.tsx` — so modals correctly cover the mini-player while it floats above ordinary content and sticky chrome.\n- Header: drag region containing the truncated `title` (`truncate min-w-0`) plus icon controls — a close button (always) and a restore button (only rendered when `onRestore` is provided). Each control gets an `aria-label` (\"Close\" / \"Restore\") and is sized `size-(--size-icon-sm)`.\n- Drag gesture: follow the document-listener pointer pattern used by `apps/client/src/layers/features/chat/ui/ScrollThumb.tsx` (`handleThumbPointerDown`, lines ~92-129) — on the header's `onPointerDown`, call `e.preventDefault()`, stash the starting pointer position and starting geometry in refs, and attach `pointermove`/`pointerup` listeners to `document` (not `setPointerCapture` — match the existing drag precedents in this codebase, which use the document-listener pattern). During `pointermove`, apply the live delta IMPERATIVELY to the container's inline transform/style via the ref (rAF-throttled, mirroring `ScrollThumb`'s direct `el.scrollTop` write and `DevToolsPanel`'s drag-resize in `apps/client/src/main.tsx:73-100`) — do NOT call `onGeometryChange` on every frame; that would defeat the performance goal below. On `pointerup` or `pointercancel`, compute the final clamped geometry (via `clampGeometry`) and call `onGeometryChange` exactly once with it, then remove the `document` listeners. This is the single source of truth for the \"write on pointerup, not per-frame\" performance goal — the store's `setPipGeometry` action (task 1.2), which persists to localStorage on every call, is only ever invoked by the host in response to this single end-of-gesture callback, so it never fires mid-drag.\n- Resize gesture: identical document-listener pattern, triggered from a single handle anchored bottom-right (`cursor-nwse-resize`), adjusting `width`/`height` (with `x`/`y` fixed) and enforcing `minWidth ?? 280` / `minHeight ?? 180` live during the drag, then clamping and committing once on release exactly like the drag gesture.\n- Viewport re-clamp: register a `window.resize` listener while mounted that re-clamps the current `geometry` prop via `clampGeometry` and calls `onGeometryChange` with the result if it changed. ALSO run this same clamp pass once immediately on mount (not only on the `resize` event) — this is what self-corrects a persisted geometry that has gone stale (e.g. the browser window was resized while the app was closed) the moment the panel is first displayed, without requiring the state slice in task 1.2 to do any clamping of its own on hydrate.\n- Chrome: `bg-card border border-border rounded-lg shadow-floating overflow-hidden` on the panel body; entrance/exit is a ~150ms ease-out fade+scale with no bounce (use `motion`'s `AnimatePresence`/`motion.div` with an `ease: [0, 0, 0.2, 1]`-style easing curve, matching the floating-chrome entrance already used in `apps/client/src/App.tsx:131-134`, or an equivalent CSS transition — pick one and keep it internally consistent).\n- Accessibility: `role=\"complementary\"`, `aria-label={title}` on the root; Escape must NOT close the panel — do not wire any `keydown` listener to `onClose` (the panel is non-modal, ideation decision D8).\n- shadcn conventions: `data-slot=\"floating-panel\"` on the root element, `cn()` (from `@/layers/shared/lib`) to merge `className` last so callers can override, a plain `ref` prop if a ref is needed internally (no `forwardRef` — React 19 convention per `.claude/rules/components.md`), and TSDoc on the exported component, both interfaces, and `clampGeometry`.\n\nBarrel export: add `export { FloatingPanel, clampGeometry } from './floating-panel';` and `export type { FloatingPanelProps, FloatingPanelGeometry } from './floating-panel';` to `apps/client/src/layers/shared/ui/index.ts`, alongside the existing alphabetically-adjacent exports.\n\nUnit tests at `apps/client/src/layers/shared/ui/__tests__/floating-panel.test.tsx` (RTL + jsdom per `.claude/rules/testing.md`) covering:\n- renders `children` and the `title`\n- dragging the header (synthetic `pointerdown` on the header, `pointermove`+`pointerup` dispatched on `document`) calls `onGeometryChange` exactly once with the final, moved geometry\n- resizing from the bottom-right handle calls `onGeometryChange` with `width`/`height` no smaller than `minWidth`/`minHeight` even when the synthetic drag would shrink it further\n- a geometry prop placed outside the mocked viewport bounds is clamped back on a simulated `window.resize` event (fire the event, assert `onGeometryChange` is called with an in-bounds result) AND on initial mount with an already out-of-bounds geometry prop\n- the close control's click calls `onClose`\n- the restore control is absent from the DOM when `onRestore` is omitted, and calls `onRestore` on click when provided\n- `role=\"complementary\"` and `aria-label` equal to `title` are present; both icon controls have `aria-label`\n- a `keydown` event with `key: 'Escape'` does NOT call `onClose`",
      "activeForm": "Building the floating-panel primitive (drag, resize, clamp, chrome, a11y)",
      "size": "large",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.2"],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Foundation: primitive + state",
      "subject": "[pip-panel] [P1] Add the PIP state slice with persisted geometry",
      "description": "Create the state slice at `apps/client/src/layers/shared/model/app-store/app-store-pip.ts`, composed into `useAppStore` (`app-store.ts`), mirroring the composition pattern of `app-store-canvas.ts`/`app-store-right-panel.ts`.\n\nExport exactly this shape verbatim:\n\n```ts\n/** Serializable descriptor — never a React node (ideation D4). */\nexport type PipContent = { kind: 'demo'; title: string };\n// DOR-297 adds { kind: 'mcp_app'; sessionId; serverName; uri; title }\n// DOR-298 adds { kind: 'widget'; sessionId; ... }\n\nexport interface PipSlice {\n  pipContent: PipContent | null;\n  pipGeometry: FloatingPanelGeometry | null; // null → default dock bottom-right\n  openPip: (content: PipContent) => void; // replaces current content (D3)\n  closePip: () => void;\n  setPipGeometry: (g: FloatingPanelGeometry) => void; // persists\n}\n```\n\n`FloatingPanelGeometry` is a type-only import from `@/layers/shared/ui` (task 1.1's barrel export) — this is a legal same-layer cross-import (shared/model importing a type from shared/ui; `.claude/rules/fsd-layers.md` only restricts shared from importing entities/features/widgets, not its own sibling submodules).\n\nWiring:\n- `apps/client/src/layers/shared/lib/constants.ts`: add `PIP_PANEL_STATE: 'dorkos-pip-panel-state'` to the `STORAGE_KEYS` const object, next to `RIGHT_PANEL_STATE`/`RIGHT_PANEL_LAYOUTS`.\n- `apps/client/src/layers/shared/model/app-store/app-store-helpers.ts`: add `readPipGeometry(): FloatingPanelGeometry | null` and `writePipGeometry(g: FloatingPanelGeometry): void`, using the exact try/catch localStorage JSON read/write pattern already used by `readRightPanelState`/`writeRightPanelState` (lines 236-251) — read returns `null` on missing/corrupt data, write silently swallows quota errors.\n- `createPipSlice`: `pipContent` starts `null`. `pipGeometry` hydrates from `readPipGeometry()` at slice creation, UNCLAMPED — clamping-on-display is the primitive's job (task 1.1's mount-time clamp effect), not the slice's, so a stale off-screen geometry self-corrects the instant the panel renders rather than the slice duplicating the same clamp math. `openPip(content)` unconditionally replaces `pipContent` (single-instance replace-on-open, decision D3 — no dedup/queue, unlike canvas's per-source-key dedup in `app-store-canvas.ts`). `closePip()` sets `pipContent` to `null` and leaves `pipGeometry` untouched (position survives close/reopen). `setPipGeometry(g)` sets `state.pipGeometry = g` AND calls `writePipGeometry(g)` — every call persists, so the only caller must be the host wiring the primitive's single end-of-gesture `onGeometryChange` callback (tasks 1.1/2.1's contract) — never call it per-frame.\n- `apps/client/src/layers/shared/model/app-store/app-store-types.ts`: add `import type { PipSlice } from './app-store-pip';` and extend `export type AppState = CoreSlice & PanelsSlice & PreferencesSlice & CanvasSlice & RightPanelSlice & PipSlice;`, plus a line in the file's dependency-order comment block (mirroring the existing `app-store-canvas → app-store-helpers` style entries).\n- `apps/client/src/layers/shared/model/app-store/app-store.ts`: import `createPipSlice` from `./app-store-pip` and add `...createPipSlice(...a),` to the composed-slices block (after `...createRightPanelSlice(...a),`); update the module-level TSDoc slice list (lines 4-9) to add a `PipSlice` bullet describing the floating PIP panel state.\n- `resetPreferences()` in `app-store.ts` (~lines 201-232): add `localStorage.removeItem(STORAGE_KEYS.PIP_PANEL_STATE);` to the removal block, and `pipContent: null, pipGeometry: null,` to the final `set({...})` call, mirroring the existing `rightPanelOpen: false, activeRightPanelTab: null, rightPanelLayoutKey: null,` lines.\n- `apps/client/src/layers/shared/model/index.ts`: add `export type { PipContent } from './app-store/app-store-pip';` next to the existing `export type { CanvasDocument, BrowserHistoryState } from './app-store/app-store-canvas';` line, so consumer features (task 2.1's `PipHost`, and DOR-297/298 later) can import the union from the barrel instead of reaching into the slice file directly.\n\nUnit tests at `apps/client/src/layers/shared/model/app-store/__tests__/app-store-pip.test.ts`, following `app-store-right-panel.test.ts`'s `localStorage.clear()` + `useAppStore.setState({...defaults})` `beforeEach` pattern (lines 11-19):\n- `openPip` sets `pipContent` to the given descriptor\n- `openPip` called a second time with different content REPLACES `pipContent` (no stacking, no dedup)\n- `closePip` sets `pipContent` to `null` and does not change `pipGeometry`\n- `setPipGeometry` updates `state.pipGeometry` and writes the identical value as JSON to `localStorage.getItem('dorkos-pip-panel-state')`\n- a geometry value pre-seeded into `localStorage` under the `PIP_PANEL_STATE` key is reflected in the slice's initial `pipGeometry` at store construction (hydration)\n- `resetPreferences()` (via `useAppStore.getState().resetPreferences()`) clears both `pipContent` and `pipGeometry` to `null` and removes the `dorkos-pip-panel-state` localStorage key\n- after several `openPip` calls, `pipContent` never appears in any localStorage key — assert the persisted `PIP_PANEL_STATE` value only ever contains `{x, y, width, height}` shaped data, proving content is never persisted",
      "activeForm": "Adding the PIP state slice with persisted geometry",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Host + shells",
      "subject": "[pip-panel] [P1] Build the PipHost content-routing feature",
      "description": "Create the content host feature at `apps/client/src/layers/features/pip-panel/` (`ui/PipHost.tsx`, `ui/DemoPipContent.tsx`, `index.ts`), mirroring how `features/canvas` routes `UiCanvasContent` variants.\n\n`PipHost` responsibilities:\n- Read `pipContent`, `pipGeometry`, `closePip`, `setPipGeometry` from `useAppStore` (`@/layers/shared/model`).\n- Render `null` when `pipContent` is `null` (cheap early return — nothing renders when there is nothing to show).\n- Mobile guard first (ideation D2): call `useIsMobile()` (`@/layers/shared/model`, 768px breakpoint); when `true`, render `null`. Pair with a `useEffect` keyed on the mobile flag that calls `closePip()` whenever `isMobile` becomes `true` while `pipContent` is not already `null` — this is what makes crossing the breakpoint while a panel is open close it automatically instead of leaving stale content behind an invisible host.\n- Default geometry: when `pipGeometry` is `null`, compute a bottom-right dock — `{ x: window.innerWidth - 360 - 16, y: window.innerHeight - 240 - 16, width: 360, height: 240 }` (a 360×240 panel, 16px margins from both edges) — recomputed on every render while ungeometried (nothing is written to the store until the user's first drag/resize commits a geometry, so a window resize before that first gesture keeps the dock pinned to the corner instead of going stale).\n- Renderer map: a MODULE-SCOPE constant declared at the top of `PipHost.tsx`, outside the component function — never an inline object literal or a `useMemo` result created inside the component body:\n\n```tsx\nconst PIP_RENDERERS: { demo: React.ComponentType<{ content: Extract<PipContent, { kind: 'demo' }> }> } = {\n  demo: DemoPipContent,\n};\n```\n\nThis mirrors the fix documented in `apps/client/src/layers/features/chat/ui/message/StreamingText.tsx:33-70` (the `FenceContext`/`DorkosUiFence` pair): that file's TSDoc explains that a renderer component recreated as a fresh closure on every render gets a fresh identity, which React treats as a different component type and unmounts/remounts the whole subtree — for PIP this matters the moment DOR-298 puts a live, stateful widget inside; a remount on every parent re-render would destroy its in-flight state. `DemoPipContent` (new file, `ui/DemoPipContent.tsx`) is intentionally trivial: it takes `{ content: { kind: 'demo'; title: string } }` and renders the title centered in the panel body — used only by the playground showcase (task 3.1) and tests.\n- Content routing: switch on `pipContent.kind` to pick the component from `PIP_RENDERERS` and render it with `content={pipContent}`. v1's `PipContent` union only has `'demo'`, so the switch has exactly one case plus an exhaustiveness guard (e.g. a `default` branch that asserts the remaining type is `never`) so adding `'mcp_app'`/`'widget'` in DOR-297/298 becomes a compile error until their case is added.\n- `onRestore`: `undefined` for `'demo'` in v1 (no restore target exists for the playground content) — add a TSDoc comment noting DOR-297 wires this for \"send back to inline/canvas\" per ideation decision D8.\n- Wire `FloatingPanel`'s `onGeometryChange` prop directly to the store's `setPipGeometry` action — because task 1.1's primitive only calls `onGeometryChange` once, at gesture end, with the final clamped geometry, this direct wiring alone satisfies \"persist on pointerup, not per-frame\" without `PipHost` needing any throttling of its own.\n- Wire `onClose` to `closePip`.\n- Title: `pipContent.title` (the only kind in v1 already carries a `title` field; future kinds must too).\n\nBarrel `index.ts`:\n```ts\n/**\n * PIP panel host — routes serializable PipContent descriptors to their\n * renderer inside the shared floating-panel primitive. The content-blind\n * primitive and the typed content union live in shared/ui and shared/model;\n * this feature is the only place that knows how each `kind` renders (mirrors\n * features/canvas routing UiCanvasContent).\n *\n * @module features/pip-panel\n */\nexport { PipHost } from './ui/PipHost';\n```\n\nUnit tests at `apps/client/src/layers/features/pip-panel/__tests__/PipHost.test.tsx` (mock `useAppStore` and `useIsMobile` per `.claude/rules/testing.md`'s Mock Browser APIs pattern):\n- renders nothing when `pipContent` is `null`\n- renders the `demo` content's title when `pipContent` is `{ kind: 'demo', title: '...' }`\n- uses the computed bottom-right default geometry when `pipGeometry` is `null` (assert the rendered position/size matches the 360×240/16px-margin formula)\n- when `useIsMobile()` returns `true` and `pipContent` is non-null, the host renders nothing AND calls `closePip()` (mock it and assert it fired)\n- renderer identity is stable across re-renders: render `PipHost` with open demo content, force a parent re-render (change an unrelated prop or context value), and assert the demo content's mount effect (a spy inside a `useEffect(() => { spy(); return cleanupSpy; }, [])` in `DemoPipContent`, or an equivalent mount-counting technique) fires exactly once — proving no remount happened.",
      "activeForm": "Building the PipHost content-routing feature",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": [],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Host + shells",
      "subject": "[pip-panel] [P1] Mount PipHost in both shells and prove it survives route changes",
      "description": "Mount the host at the singleton tail of both client shells, outside the router-swapped subtree, so a floating panel survives navigation.\n\n- `apps/client/src/AppShell.tsx`: import `{ PipHost }` from `@/layers/features/pip-panel` and add `<PipHost />` to the singleton tail after `<Toaster />` (currently lines 363-367: `<DialogHost />`, `<CommandPaletteDialog />`, `<CreateAgentDialog />`, `<ShortcutsPanel />`, `<Toaster />`) — placed there, OUTSIDE the `<PanelGroup>` (lines 349-354) that wraps `<Outlet />`, so it never remounts on navigation.\n- `apps/client/src/App.tsx` (Obsidian embedded shell, no router at all): same import, `<PipHost />` added to its singleton tail after `<Toaster />` (currently lines 154-156: `<CommandPaletteDialog />`, `<ShortcutsPanel />`, `<Toaster />`).\n- Acceptance: `grep -rn \"react-router\\|tanstack/react-router\" apps/client/src/layers/features/pip-panel apps/client/src/layers/shared/ui/floating-panel.tsx apps/client/src/layers/shared/model/app-store/app-store-pip.ts` returns nothing — confirms no router import reached the primitive, slice, or host (required for the Obsidian embedded shell).\n\nIntegration test at `apps/client/src/__tests__/pip-panel-route-persistence.test.tsx`, following the mocking style of the existing `apps/client/src/__tests__/app-shell-slots.test.tsx` (mocks `@tanstack/react-router`'s `useRouterState`/`Outlet` via a module-level `mockPathname` variable, mocks the sibling shell children that aren't relevant here, wraps in `QueryClientProvider` + `TransportProvider` with `createMockTransport()` from `@dorkos/test-utils`, and mocks `window.matchMedia`):\n- render `<AppShell />` once via RTL's `render()`\n- open PIP content through the real store (`useAppStore.getState().openPip({ kind: 'demo', title: 'Route test' })`) so `PipHost` renders a panel with `role=\"complementary\"`\n- capture the panel's DOM node via `screen.getByRole('complementary')`\n- change `mockPathname` (e.g. `/` → `/session`) and call the SAME render result's `rerender(...)` (not a fresh `render()`/`cleanup()` cycle — that would prove nothing about persistence) with the same `<AppShell />` element\n- assert the panel's DOM node is the SAME node reference as before (`screen.getByRole('complementary')` after the rerender `===` the captured node) and its title text is unchanged — proving `PipHost` never unmounted across the simulated navigation, because it sits outside the `<Outlet/>`/`<PanelGroup>` subtree that a route change actually re-renders.",
      "activeForm": "Mounting PipHost in both shells and proving it survives route changes",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": [],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Verification polish",
      "subject": "[pip-panel] [P2] Add the PIP Panel Dev Playground showcase",
      "description": "Add a showcase so the primitive is fully verifiable before either accepted consumer (DOR-297, DOR-298) exists.\n\n- New file `apps/client/src/dev/showcases/PipPanelShowcases.tsx`, following the `PlaygroundSection` + `ShowcaseDemo` wrapper pattern used by `apps/client/src/dev/showcases/OverlayShowcases.tsx` (the Dialog/Sheet/Popover showcases).\n- Because the Dev Playground (`apps/client/src/dev/DevPlayground.tsx`) never mounts `AppShell`/`App.tsx`, this showcase must mount `<PipHost />` itself, once, inside the showcase component — otherwise `openPip` calls have nothing to render into.\n- Controls exercising the full interaction contract this primitive ships (open at a persisted-or-default position, drag anywhere, resize from the corner, replace-on-reopen, close): a button that calls `useAppStore.getState().openPip({ kind: 'demo', title: 'Demo panel A' })`, a second button with a different title (proves replace-on-open — single instance, no stacking), and a close button calling `closePip()`. Add instructional copy near the controls telling the tester to drag the header and resize from the bottom-right corner to check clamping, and to reload the `/dev` page afterward to see the position survive (geometry persists via localStorage exactly like the production panel).\n- Register the showcase: add `PlaygroundSection` entries (one per demonstrated behavior, at the granularity already used by the existing `FEATURES_SECTIONS` entries) to `apps/client/src/dev/sections/features-sections.ts` with `page: 'features'`, a new `category: 'PIP Panel'`, and `keywords` covering `['pip', 'panel', 'floating', 'drag', 'resize', 'mini-player', 'pop out']`. Import and render `<PipPanelShowcases />` inside `apps/client/src/dev/pages/FeaturesPage.tsx` alongside the other `*Showcases` components (reusing the existing Features page avoids adding a new `Page` union member, route, and sidebar nav entry for a single showcase group — consistent with how Relay's several showcase files already share that one page).\n- No new automated tests required here — there is no end-user entry point in v1, so this showcase IS the verification surface until DOR-297/298 add real affordances. Still confirm `pnpm --filter @dorkos/client typecheck` passes and, running `pnpm dev:dogfood`, that visiting `/dev/features` shows the new \"PIP Panel\" section in the TOC/search and every control behaves as described with no console errors.",
      "activeForm": "Adding the PIP Panel Dev Playground showcase",
      "size": "small",
      "priority": "medium",
      "dependencies": ["2.1"],
      "parallelWith": ["2.2", "3.2"],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Verification polish",
      "subject": "[pip-panel] [P2] Write the changelog fragment for the PIP panel primitive",
      "description": "Add a changelog fragment for this developer-visible surface even though DOR-296 ships no end-user-facing behavior yet (DOR-297/298 carry the docs and changelog entries for the visible behavior their affordances add later).\n\n- New file `changelog/unreleased/<id>-pip-panel-primitive.md` where `<id>` is a FRESH `YYMMDD-HHMMSS` timestamp id minted with `.claude/scripts/id.ts` at write time (coordination-free per ADR-0312) — do not reuse this spec's own id `260711-150300`.\n- No YAML frontmatter — plain Markdown, following the exact convention already in `changelog/unreleased/260711-105140-marketplace-symlink-containment.md`: a single `###` category heading (`Added`, since this ships a new primitive), followed by one bullet ending in a tracker reference in parens (`(DOR-296)`).\n- Write the bullet for a developer audience per the `writing-for-humans` skill: plain language, no jargon for jargon's sake, no em dashes, describing what exists now rather than internal implementation. Example shape, write your own wording rather than copying this: \"Added a reusable floating panel: a draggable, resizable mini-window that can stay open while you move around the app, remembering where you left it. Nothing uses it yet; it's the foundation for popping things like MCP apps and interactive widgets out of the main view in upcoming releases (DOR-296).\"",
      "activeForm": "Writing the changelog fragment for the PIP panel primitive",
      "size": "small",
      "priority": "low",
      "dependencies": ["2.1"],
      "parallelWith": ["2.2", "3.1"],
      "issue": null,
      "parentIssue": null
    },
    {
      "id": "3.3",
      "phase": 3,
      "phaseName": "Verification polish",
      "subject": "[pip-panel] [P2] Final accessibility and design-token review, then full verify",
      "description": "Close out the PR with the polish pass and the full verification loop.\n\n- Design-token audit against `contributing/design-system.md` (elevation/motion/mobile-scale sections) and `.claude/rules/components.md`: confirm `floating-panel.tsx` uses only the Calm Tech tokens already in play elsewhere — `shadow-floating`, `bg-card`, `border-border`, `rounded-lg`, `size-(--size-icon-sm)` on icon controls, 100-300ms animation durations — with no ad hoc hex colors, no `Math.random()`, and no bare `focus:` on interactive controls (must be `focus-visible:` per the Accessibility rules in `.claude/rules/components.md`).\n- Accessibility pass beyond the unit tests already written in tasks 1.1/2.1: drive the Dev Playground showcase (task 3.1) with a keyboard and a screen reader or axe DevTools, confirming the close/restore controls are reachable via Tab with visible focus rings. Drag/resize has no keyboard-only equivalent in v1 — note that as a known limitation if it comes up in review rather than silently adding scope.\n- Mobile-scale check: at a real ~375px viewport (or DevTools device emulation), confirm a panel open above 768px closes the instant the viewport crosses below 768px (the mobile guard from task 2.1), and that nothing renders below 768px (there is no pop-out affordance yet in v1, so this is confirming absence, not a new affordance check).\n- Run the full verification loop and fix anything red: `pnpm --filter @dorkos/client typecheck`, `pnpm --filter @dorkos/client lint`, `pnpm vitest run apps/client/src/layers/shared/ui/__tests__/floating-panel.test.tsx apps/client/src/layers/shared/model/app-store/__tests__/app-store-pip.test.ts apps/client/src/layers/features/pip-panel/__tests__/PipHost.test.tsx apps/client/src/__tests__/pip-panel-route-persistence.test.tsx`, then the full `pnpm verify` (affected-only typecheck+lint+test) before opening the PR. Never substitute a bare `pnpm vitest run` for the full-suite check — it falsely fails two unrelated tests in the dev environment; use `pnpm test -- --run` for that.\n- Confirm no lingering TODOs, no console warnings from the new components, and that every export in `apps/client/src/layers/features/pip-panel/`, `floating-panel.tsx`, and `app-store-pip.ts` carries TSDoc, including the module-level `@module` doc blocks.",
      "activeForm": "Running the final accessibility and design-token review, then full verify",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.2", "3.1", "3.2"],
      "parallelWith": [],
      "issue": null,
      "parentIssue": null
    }
  ]
}
