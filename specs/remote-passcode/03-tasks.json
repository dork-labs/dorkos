{
  "spec": "specs/remote-passcode/02-specification.md",
  "slug": "remote-passcode",
  "generatedAt": "2026-03-24T12:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Add passcode fields to shared schemas and constants",
      "description": "## Overview\n\nExtend the shared package with passcode-related config fields, tunnel status fields, API schemas, transport methods, and constants. This task touches four files in `packages/shared/src/`.\n\n## Step 1: Update `packages/shared/src/config-schema.ts`\n\nAdd three new fields to the `tunnel` object in `UserConfigSchema`. The tunnel schema currently looks like:\n\n```typescript\ntunnel: z\n  .object({\n    enabled: z.boolean().default(false),\n    domain: z.string().nullable().default(null),\n    authtoken: z.string().nullable().default(null),\n    auth: z.string().nullable().default(null),\n  })\n  .default(() => ({ enabled: false, domain: null, authtoken: null, auth: null })),\n```\n\nChange it to:\n\n```typescript\ntunnel: z\n  .object({\n    enabled: z.boolean().default(false),\n    domain: z.string().nullable().default(null),\n    authtoken: z.string().nullable().default(null),\n    auth: z.string().nullable().default(null),\n    passcodeEnabled: z.boolean().default(false),\n    passcodeHash: z.string().nullable().default(null),\n    passcodeSalt: z.string().nullable().default(null),\n  })\n  .default(() => ({\n    enabled: false,\n    domain: null,\n    authtoken: null,\n    auth: null,\n    passcodeEnabled: false,\n    passcodeHash: null,\n    passcodeSalt: null,\n  })),\n```\n\nAdd a top-level `sessionSecret` field to `UserConfigSchema` (after the `agents` field):\n\n```typescript\nsessionSecret: z.string().nullable().default(null),\n```\n\nUpdate the `SENSITIVE_CONFIG_KEYS` array to include the new sensitive fields:\n\n```typescript\nexport const SENSITIVE_CONFIG_KEYS = [\n  'tunnel.authtoken',\n  'tunnel.auth',\n  'tunnel.passcodeHash',\n  'tunnel.passcodeSalt',\n] as const;\n```\n\n## Step 2: Update `packages/shared/src/schemas.ts`\n\nAdd `passcodeEnabled` to `TunnelStatusSchema`. The current schema is:\n\n```typescript\nexport const TunnelStatusSchema = z\n  .object({\n    enabled: z.boolean(),\n    connected: z.boolean(),\n    url: z.string().nullable(),\n    port: z.number().int().nullable(),\n    startedAt: z.string().nullable(),\n    authEnabled: z.boolean(),\n    tokenConfigured: z.boolean(),\n    domain: z.string().nullable(),\n  })\n  .openapi('TunnelStatus');\n```\n\nChange it to:\n\n```typescript\nexport const TunnelStatusSchema = z\n  .object({\n    enabled: z.boolean(),\n    connected: z.boolean(),\n    url: z.string().nullable(),\n    port: z.number().int().nullable(),\n    startedAt: z.string().nullable(),\n    authEnabled: z.boolean(),\n    tokenConfigured: z.boolean(),\n    domain: z.string().nullable(),\n    passcodeEnabled: z.boolean(),\n  })\n  .openapi('TunnelStatus');\n```\n\nAdd three new schemas after the `TunnelStatusSchema` block (before the `HealthResponseSchema`):\n\n```typescript\nexport const PasscodeVerifyRequestSchema = z.object({\n  passcode: z.string().regex(/^\\d{6}$/),\n});\n\nexport type PasscodeVerifyRequest = z.infer<typeof PasscodeVerifyRequestSchema>;\n\nexport const PasscodeVerifyResponseSchema = z.object({\n  ok: z.boolean(),\n  error: z.string().optional(),\n  retryAfter: z.number().optional(),\n});\n\nexport type PasscodeVerifyResponse = z.infer<typeof PasscodeVerifyResponseSchema>;\n\nexport const PasscodeSessionResponseSchema = z.object({\n  authenticated: z.boolean(),\n  passcodeRequired: z.boolean(),\n});\n\nexport type PasscodeSessionResponse = z.infer<typeof PasscodeSessionResponseSchema>;\n```\n\n## Step 3: Update `packages/shared/src/constants.ts`\n\nAdd passcode-related constants after the existing `SDK_TOOL_NAMES` block:\n\n```typescript\n/** Length of the numeric passcode for remote tunnel access. */\nexport const PASSCODE_LENGTH = 6;\n\n/** Session cookie max-age: 24 hours of rolling activity. */\nexport const PASSCODE_SESSION_MAX_AGE_MS = 24 * 60 * 60 * 1000;\n\n/** Rate limit window for passcode verification attempts. */\nexport const PASSCODE_RATE_LIMIT_WINDOW_MS = 15 * 60 * 1000;\n\n/** Maximum passcode verification attempts per rate limit window. */\nexport const PASSCODE_RATE_LIMIT_MAX = 10;\n\n/** Consecutive failure threshold before short block. */\nexport const PASSCODE_CONSECUTIVE_LIMIT = 5;\n\n/** Duration of the short block after consecutive failures (60 seconds). */\nexport const PASSCODE_CONSECUTIVE_BLOCK_MS = 60 * 1000;\n```\n\n## Step 4: Update `packages/shared/src/transport.ts`\n\nAdd two new methods to the `Transport` interface. Place them after the `stopTunnel()` method and before the Pulse Scheduler section:\n\n```typescript\n/** Verify a 6-digit passcode for remote tunnel access. */\nverifyTunnelPasscode(passcode: string): Promise<{ ok: boolean; error?: string; retryAfter?: number }>;\n\n/** Check if the current session is authenticated for tunnel access. */\ncheckTunnelSession(): Promise<{ authenticated: boolean; passcodeRequired: boolean }>;\n```\n\n## Acceptance Criteria\n\n1. `UserConfigSchema` parses successfully with the new tunnel fields (`passcodeEnabled`, `passcodeHash`, `passcodeSalt`) and top-level `sessionSecret`\n2. `SENSITIVE_CONFIG_KEYS` includes `tunnel.passcodeHash` and `tunnel.passcodeSalt`\n3. `TunnelStatusSchema` includes `passcodeEnabled: z.boolean()`\n4. `PasscodeVerifyRequestSchema` validates a 6-digit string and rejects non-numeric or wrong-length input\n5. `PasscodeVerifyResponseSchema` and `PasscodeSessionResponseSchema` parse correctly\n6. `Transport` interface includes `verifyTunnelPasscode` and `checkTunnelSession` method signatures\n7. `pnpm typecheck` passes across all packages (no type errors from the new fields)",
      "activeForm": "Adding passcode fields to shared schemas, constants, and transport interface",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Create passcode-hash utility module with scrypt hashing",
      "description": "## Overview\n\nCreate a new utility module `apps/server/src/lib/passcode-hash.ts` that provides `hashPasscode` and `verifyPasscode` functions using Node.js `crypto.scrypt` with timing-safe comparison.\n\n## Step 1: Create `apps/server/src/lib/passcode-hash.ts`\n\n```typescript\n/**\n * Passcode hashing utilities using Node.js crypto.scrypt.\n *\n * Uses a 32-byte random salt and 64-byte derived key. Verification uses\n * timing-safe comparison to prevent timing attacks.\n *\n * @module lib/passcode-hash\n */\nimport { scrypt, randomBytes, timingSafeEqual } from 'node:crypto';\nimport { promisify } from 'node:util';\n\nconst scryptAsync = promisify(scrypt);\nconst KEY_LENGTH = 64;\n\n/** Hash a plaintext passcode with a random salt. Returns hex-encoded hash and salt. */\nexport async function hashPasscode(passcode: string): Promise<{ hash: string; salt: string }> {\n  const salt = randomBytes(32).toString('hex');\n  const derived = (await scryptAsync(passcode, salt, KEY_LENGTH)) as Buffer;\n  return { hash: derived.toString('hex'), salt };\n}\n\n/** Verify a plaintext passcode against a stored hash and salt using timing-safe comparison. */\nexport async function verifyPasscode(\n  passcode: string,\n  storedHash: string,\n  storedSalt: string\n): Promise<boolean> {\n  const derived = (await scryptAsync(passcode, storedSalt, KEY_LENGTH)) as Buffer;\n  const hashBuffer = Buffer.from(storedHash, 'hex');\n  if (derived.length !== hashBuffer.length) return false;\n  return timingSafeEqual(derived, hashBuffer);\n}\n```\n\n## Step 2: Write tests in `apps/server/src/lib/__tests__/passcode-hash.test.ts`\n\n```typescript\nimport { describe, it, expect } from 'vitest';\nimport { hashPasscode, verifyPasscode } from '../passcode-hash.js';\n\ndescribe('passcode-hash', () => {\n  describe('hashPasscode', () => {\n    it('returns hash and salt as hex strings', async () => {\n      const result = await hashPasscode('123456');\n      expect(result.hash).toMatch(/^[0-9a-f]+$/);\n      expect(result.salt).toMatch(/^[0-9a-f]+$/);\n    });\n\n    it('returns a 128-character hex hash (64 bytes)', async () => {\n      const result = await hashPasscode('123456');\n      expect(result.hash.length).toBe(128);\n    });\n\n    it('returns a 64-character hex salt (32 bytes)', async () => {\n      const result = await hashPasscode('123456');\n      expect(result.salt.length).toBe(64);\n    });\n\n    it('produces different salts for each call', async () => {\n      const a = await hashPasscode('123456');\n      const b = await hashPasscode('123456');\n      expect(a.salt).not.toBe(b.salt);\n    });\n\n    it('produces different hashes for the same input due to unique salts', async () => {\n      const a = await hashPasscode('123456');\n      const b = await hashPasscode('123456');\n      expect(a.hash).not.toBe(b.hash);\n    });\n  });\n\n  describe('verifyPasscode', () => {\n    it('returns true for correct passcode', async () => {\n      const { hash, salt } = await hashPasscode('654321');\n      const valid = await verifyPasscode('654321', hash, salt);\n      expect(valid).toBe(true);\n    });\n\n    it('returns false for incorrect passcode', async () => {\n      const { hash, salt } = await hashPasscode('654321');\n      const valid = await verifyPasscode('000000', hash, salt);\n      expect(valid).toBe(false);\n    });\n\n    it('returns false when hash length does not match (no throw)', async () => {\n      const { salt } = await hashPasscode('654321');\n      const valid = await verifyPasscode('654321', 'short', salt);\n      expect(valid).toBe(false);\n    });\n\n    it('different salts produce different hashes for the same passcode', async () => {\n      const a = await hashPasscode('111111');\n      const b = await hashPasscode('111111');\n      const crossValid = await verifyPasscode('111111', a.hash, b.salt);\n      expect(crossValid).toBe(false);\n    });\n  });\n});\n```\n\n## Acceptance Criteria\n\n1. `hashPasscode('123456')` returns `{ hash: string, salt: string }` where both are hex-encoded\n2. Hash is 128 hex chars (64 bytes), salt is 64 hex chars (32 bytes)\n3. `verifyPasscode` returns `true` for the correct passcode and `false` for incorrect ones\n4. `verifyPasscode` returns `false` (not throws) when stored hash length does not match derived key length\n5. Each call to `hashPasscode` produces a unique salt (and therefore unique hash)\n6. Using the wrong salt for verification returns `false`\n7. All 8 test scenarios pass: `pnpm vitest run apps/server/src/lib/__tests__/passcode-hash.test.ts`",
      "activeForm": "Creating passcode-hash utility module with scrypt hashing and verification",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "1.3",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Install cookie-session and configure session middleware in app.ts",
      "description": "## Overview\n\nInstall the `cookie-session` npm package as a server dependency, then configure it as Express middleware in `apps/server/src/app.ts`. Also set `trust proxy` for correct IP detection behind ngrok. The session secret is auto-generated on first run and persisted to the config file.\n\n## Step 1: Install cookie-session\n\nRun:\n```bash\npnpm --filter=@dorkos/server add cookie-session\npnpm --filter=@dorkos/server add -D @types/cookie-session\n```\n\n## Step 2: Modify `apps/server/src/app.ts`\n\nAdd new imports at the top of the file:\n\n```typescript\nimport crypto from 'node:crypto';\nimport cookieSession from 'cookie-session';\nimport { PASSCODE_SESSION_MAX_AGE_MS } from '@dorkos/shared/constants';\nimport { configManager } from './services/core/config-manager.js';\n```\n\nInside the `createApp()` function, after the existing `app.use(requestLogger);` line and before the API route mounts, add:\n\n```typescript\n// Trust the immediate upstream proxy (ngrok) for correct IP and protocol detection\napp.set('trust proxy', 1);\n\n// Auto-generate session secret on first run, persist to config\nlet sessionSecret = configManager.get('sessionSecret');\nif (!sessionSecret) {\n  sessionSecret = crypto.randomBytes(32).toString('hex');\n  configManager.set('sessionSecret', sessionSecret);\n}\n\napp.use(\n  cookieSession({\n    name: 'dorkos_session',\n    keys: [sessionSecret],\n    maxAge: PASSCODE_SESSION_MAX_AGE_MS,\n    httpOnly: true,\n    secure: true,\n    sameSite: 'strict',\n  })\n);\n```\n\nThe middleware order in `createApp()` must be:\n1. `cors`\n2. `express.json()`\n3. `requestLogger`\n4. `trust proxy` setting\n5. `cookieSession`\n6. Route handlers\n\n## Step 3: Add TypeScript type augmentation for session\n\nCreate a type declaration file at `apps/server/src/types/cookie-session.d.ts` (or add to an existing declarations file):\n\n```typescript\nimport 'cookie-session';\n\ndeclare module 'cookie-session' {\n  interface CookieSessionObject {\n    tunnelAuthenticated?: boolean;\n  }\n}\n```\n\nAlternatively, if the project uses `express-serve-static-core` augmentation, augment `req.session` there. The key point is that `req.session?.tunnelAuthenticated` must typecheck without errors.\n\n## Acceptance Criteria\n\n1. `cookie-session` and `@types/cookie-session` are in server `package.json` dependencies/devDependencies\n2. `app.set('trust proxy', 1)` is called before cookie-session middleware\n3. Session secret is auto-generated via `crypto.randomBytes(32).toString('hex')` and stored via `configManager.set('sessionSecret', ...)`\n4. On subsequent server starts, the persisted secret is reused (not regenerated)\n5. Cookie config: `name: 'dorkos_session'`, `httpOnly: true`, `secure: true`, `sameSite: 'strict'`, `maxAge: PASSCODE_SESSION_MAX_AGE_MS` (24 hours)\n6. `req.session.tunnelAuthenticated` typechecks as `boolean | undefined`\n7. `pnpm typecheck` passes\n8. `pnpm build` succeeds with the new dependency",
      "activeForm": "Installing cookie-session and configuring session middleware in Express app",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.2"]
    },
    {
      "id": "1.4",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Create tunnel-auth middleware for passcode gate enforcement",
      "description": "## Overview\n\nCreate a new Express middleware at `apps/server/src/middleware/tunnel-auth.ts` that intercepts tunnel (non-localhost) requests and returns 401 if the user has not authenticated via passcode. Local requests always pass through. Certain routes are exempt (passcode entry endpoints, health check, static assets).\n\n## Step 1: Create `apps/server/src/middleware/tunnel-auth.ts`\n\n```typescript\n/**\n * Tunnel passcode authentication middleware.\n *\n * Gates non-localhost requests behind passcode verification when enabled.\n * Local access (localhost / 127.0.0.1) is always unrestricted.\n * Exempt routes allow the passcode entry page and health checks to load.\n *\n * @module middleware/tunnel-auth\n */\nimport type { Request, Response, NextFunction } from 'express';\nimport { configManager } from '../services/core/config-manager.js';\n\nconst EXEMPT_PREFIXES = [\n  '/api/tunnel/verify-passcode',\n  '/api/tunnel/session',\n  '/api/health',\n  '/assets/',\n  '/favicon.ico',\n];\n\n/** Check if a request path is exempt from passcode authentication. */\nfunction isExempt(path: string): boolean {\n  return EXEMPT_PREFIXES.some((prefix) => path.startsWith(prefix));\n}\n\n/** Check if a request originates from a tunnel (non-localhost) hostname. */\nfunction isTunnelRequest(req: Request): boolean {\n  const hostname = req.hostname;\n  return hostname !== 'localhost' && hostname !== '127.0.0.1';\n}\n\n/** Express middleware that enforces passcode authentication for tunnel requests. */\nexport function tunnelPasscodeAuth(req: Request, res: Response, next: NextFunction): void {\n  // Local access is always unrestricted\n  if (!isTunnelRequest(req)) {\n    next();\n    return;\n  }\n\n  // Check if passcode is configured and enabled\n  const tunnelConfig = configManager.get('tunnel');\n  if (!tunnelConfig?.passcodeEnabled || !tunnelConfig?.passcodeHash) {\n    next();\n    return;\n  }\n\n  // Exempt routes (passcode entry page, health checks, static assets)\n  if (isExempt(req.path)) {\n    next();\n    return;\n  }\n\n  // Check session cookie (set by cookie-session middleware)\n  if (req.session?.tunnelAuthenticated) {\n    next();\n    return;\n  }\n\n  // Not authenticated — return 401\n  res.status(401).json({ error: 'Passcode required' });\n}\n```\n\n## Step 2: Register middleware in `apps/server/src/app.ts`\n\nAdd the import at the top:\n\n```typescript\nimport { tunnelPasscodeAuth } from './middleware/tunnel-auth.js';\n```\n\nIn `createApp()`, add the middleware call after `cookieSession` and before route handlers:\n\n```typescript\napp.use(tunnelPasscodeAuth);\n```\n\nFinal middleware order:\n1. `cors`\n2. `express.json()`\n3. `requestLogger`\n4. `trust proxy`\n5. `cookieSession`\n6. `tunnelPasscodeAuth`\n7. Route handlers\n\n## Step 3: Write tests in `apps/server/src/middleware/__tests__/tunnel-auth.test.ts`\n\n```typescript\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport type { Request, Response, NextFunction } from 'express';\nimport { tunnelPasscodeAuth } from '../tunnel-auth.js';\nimport { configManager } from '../../services/core/config-manager.js';\n\nvi.mock('../../services/core/config-manager.js', () => ({\n  configManager: { get: vi.fn() },\n}));\n\nfunction createMockReq(overrides: Partial<Request> = {}): Request {\n  return {\n    hostname: 'localhost',\n    path: '/api/sessions',\n    session: {},\n    ...overrides,\n  } as unknown as Request;\n}\n\nfunction createMockRes(): Response {\n  const res = {\n    status: vi.fn().mockReturnThis(),\n    json: vi.fn().mockReturnThis(),\n  };\n  return res as unknown as Response;\n}\n\ndescribe('tunnelPasscodeAuth', () => {\n  let next: NextFunction;\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n    next = vi.fn();\n  });\n\n  it('passes through for localhost requests', () => {\n    const req = createMockReq({ hostname: 'localhost' });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through for 127.0.0.1 requests', () => {\n    const req = createMockReq({ hostname: '127.0.0.1' });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through when passcode is not enabled', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: false,\n      passcodeHash: null,\n    });\n    const req = createMockReq({ hostname: 'abc123.ngrok-free.app' });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through when passcode hash is null', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: null,\n    });\n    const req = createMockReq({ hostname: 'abc123.ngrok-free.app' });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through for exempt route /api/tunnel/verify-passcode', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/tunnel/verify-passcode',\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through for exempt route /api/tunnel/session', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/tunnel/session',\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through for exempt route /api/health', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/health',\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('passes through for exempt route /assets/style.css', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/assets/style.css',\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('returns 401 for tunnel requests without session cookie', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/sessions',\n      session: {},\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).not.toHaveBeenCalled();\n    expect(res.status).toHaveBeenCalledWith(401);\n    expect(res.json).toHaveBeenCalledWith({ error: 'Passcode required' });\n  });\n\n  it('passes through for tunnel requests with valid session cookie', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/sessions',\n      session: { tunnelAuthenticated: true },\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).toHaveBeenCalled();\n  });\n\n  it('returns 401 for tunnel requests with expired/invalid session cookie', () => {\n    vi.mocked(configManager.get).mockReturnValue({\n      passcodeEnabled: true,\n      passcodeHash: 'somehash',\n    });\n    const req = createMockReq({\n      hostname: 'abc123.ngrok-free.app',\n      path: '/api/sessions',\n      session: { tunnelAuthenticated: false },\n    });\n    const res = createMockRes();\n    tunnelPasscodeAuth(req, res, next);\n    expect(next).not.toHaveBeenCalled();\n    expect(res.status).toHaveBeenCalledWith(401);\n  });\n});\n```\n\n## Acceptance Criteria\n\n1. Middleware passes through for `localhost` and `127.0.0.1` hostname requests regardless of passcode state\n2. Middleware passes through when `passcodeEnabled` is `false` or `passcodeHash` is `null`\n3. Middleware passes through for all exempt paths: `/api/tunnel/verify-passcode`, `/api/tunnel/session`, `/api/health`, `/assets/*`, `/favicon.ico`\n4. Middleware passes through when `req.session.tunnelAuthenticated` is `true`\n5. Middleware returns `401 { error: 'Passcode required' }` for unauthenticated tunnel requests\n6. Middleware is registered in `app.ts` after `cookieSession` and before route handlers\n7. All 11 test scenarios pass: `pnpm vitest run apps/server/src/middleware/__tests__/tunnel-auth.test.ts`",
      "activeForm": "Creating tunnel-auth middleware for passcode gate enforcement",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "1.3"],
      "parallelWith": []
    },
    {
      "id": "1.5",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Add passcode verify, session, and set-passcode routes to tunnel router",
      "description": "## Overview\n\nAdd three new endpoints to the existing tunnel router (`apps/server/src/routes/tunnel.ts`): `POST /api/tunnel/verify-passcode`, `GET /api/tunnel/session`, and `POST /api/tunnel/set-passcode`. Also add a rate limiter for the verify endpoint.\n\n## Step 1: Add imports to `apps/server/src/routes/tunnel.ts`\n\nAdd these imports at the top of the file:\n\n```typescript\nimport rateLimit from 'express-rate-limit';\nimport {\n  PASSCODE_RATE_LIMIT_WINDOW_MS,\n  PASSCODE_RATE_LIMIT_MAX,\n} from '@dorkos/shared/constants';\nimport { PasscodeVerifyRequestSchema } from '@dorkos/shared/schemas';\nimport { hashPasscode, verifyPasscode } from '../lib/passcode-hash.js';\n```\n\nNote: `express-rate-limit` is already installed in the server package.\n\n## Step 2: Add rate limiter definition\n\nAfter the existing `resolveTunnelPort()` function, add:\n\n```typescript\nconst passcodeRateLimiter = rateLimit({\n  windowMs: PASSCODE_RATE_LIMIT_WINDOW_MS,\n  max: PASSCODE_RATE_LIMIT_MAX,\n  standardHeaders: true,\n  legacyHeaders: false,\n  message: { ok: false, error: 'Too many attempts. Try again later.', retryAfter: 900 },\n  keyGenerator: (req) => req.ip ?? 'unknown',\n});\n```\n\n## Step 3: Add `POST /verify-passcode` endpoint\n\nAdd before the existing `router.post('/start', ...)` handler:\n\n```typescript\n/** POST /api/tunnel/verify-passcode — Validate passcode and set session cookie. */\nrouter.post('/verify-passcode', passcodeRateLimiter, async (req, res) => {\n  const parsed = PasscodeVerifyRequestSchema.safeParse(req.body);\n  if (!parsed.success) {\n    return res.status(400).json({ ok: false, error: 'Invalid passcode format' });\n  }\n\n  const tunnelConfig = configManager.get('tunnel');\n  if (!tunnelConfig?.passcodeHash || !tunnelConfig?.passcodeSalt) {\n    return res.status(400).json({ ok: false, error: 'No passcode configured' });\n  }\n\n  const valid = await verifyPasscode(\n    parsed.data.passcode,\n    tunnelConfig.passcodeHash,\n    tunnelConfig.passcodeSalt\n  );\n\n  if (!valid) {\n    return res.status(401).json({ ok: false, error: 'Incorrect passcode' });\n  }\n\n  // Set session cookie\n  req.session!.tunnelAuthenticated = true;\n  return res.json({ ok: true });\n});\n```\n\n## Step 4: Add `GET /session` endpoint\n\n```typescript\n/** GET /api/tunnel/session — Check current session authentication status. */\nrouter.get('/session', (_req, res) => {\n  const tunnelConfig = configManager.get('tunnel');\n  const passcodeRequired = !!(tunnelConfig?.passcodeEnabled && tunnelConfig?.passcodeHash);\n  const authenticated = !!_req.session?.tunnelAuthenticated;\n\n  return res.json({ authenticated, passcodeRequired });\n});\n```\n\n## Step 5: Add `POST /set-passcode` endpoint\n\n```typescript\n/** POST /api/tunnel/set-passcode — Set, update, or disable the tunnel passcode (localhost only). */\nrouter.post('/set-passcode', async (req, res) => {\n  // Only allow from localhost (passcode management is local-only)\n  if (req.hostname !== 'localhost' && req.hostname !== '127.0.0.1') {\n    return res.status(403).json({ error: 'Passcode can only be changed locally' });\n  }\n\n  const { passcode, enabled } = req.body;\n\n  // Handle disable (clear passcode)\n  if (enabled === false) {\n    const tunnelConfig = configManager.get('tunnel');\n    configManager.set('tunnel', {\n      ...tunnelConfig,\n      passcodeEnabled: false,\n    });\n    tunnelManager.refreshStatus();\n    return res.json({ ok: true });\n  }\n\n  // Validate passcode format\n  if (!passcode || !/^\\d{6}$/.test(passcode)) {\n    return res.status(400).json({ error: 'Passcode must be exactly 6 digits' });\n  }\n\n  const { hash, salt } = await hashPasscode(passcode);\n  const tunnelConfig = configManager.get('tunnel');\n  configManager.set('tunnel', {\n    ...tunnelConfig,\n    passcodeEnabled: true,\n    passcodeHash: hash,\n    passcodeSalt: salt,\n  });\n\n  tunnelManager.refreshStatus();\n  return res.json({ ok: true });\n});\n```\n\n## Step 6: Write tests in `apps/server/src/routes/__tests__/tunnel-passcode.test.ts`\n\n```typescript\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport express from 'express';\nimport request from 'supertest';\nimport { configManager } from '../../services/core/config-manager.js';\nimport { hashPasscode } from '../../lib/passcode-hash.js';\n\n// Mock configManager\nvi.mock('../../services/core/config-manager.js', () => ({\n  configManager: {\n    get: vi.fn(),\n    set: vi.fn(),\n  },\n}));\n\n// Mock tunnelManager\nvi.mock('../../services/core/tunnel-manager.js', () => ({\n  tunnelManager: {\n    status: { enabled: false, connected: false, url: null, port: null, startedAt: null, authEnabled: false, tokenConfigured: false, domain: null, passcodeEnabled: false },\n    on: vi.fn(),\n    off: vi.fn(),\n    start: vi.fn(),\n    stop: vi.fn(),\n    refreshStatus: vi.fn(),\n  },\n}));\n\nasync function createTestApp() {\n  // Dynamic import to pick up mocks\n  const { default: tunnelRoutes } = await import('../tunnel.js');\n  const app = express();\n  app.use(express.json());\n  // Simulate cookie-session with a simple in-memory session\n  app.use((req, _res, next) => {\n    if (!req.session) (req as any).session = {};\n    next();\n  });\n  app.use('/api/tunnel', tunnelRoutes);\n  return app;\n}\n\ndescribe('tunnel passcode routes', () => {\n  beforeEach(() => {\n    vi.clearAllMocks();\n  });\n\n  describe('POST /api/tunnel/verify-passcode', () => {\n    it('returns 400 for non-numeric passcode', async () => {\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/verify-passcode')\n        .send({ passcode: 'abcdef' });\n      expect(res.status).toBe(400);\n      expect(res.body.ok).toBe(false);\n    });\n\n    it('returns 400 for wrong-length passcode', async () => {\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/verify-passcode')\n        .send({ passcode: '123' });\n      expect(res.status).toBe(400);\n    });\n\n    it('returns 400 when no passcode is configured', async () => {\n      vi.mocked(configManager.get).mockReturnValue({\n        passcodeHash: null,\n        passcodeSalt: null,\n      });\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/verify-passcode')\n        .send({ passcode: '123456' });\n      expect(res.status).toBe(400);\n      expect(res.body.error).toBe('No passcode configured');\n    });\n\n    it('returns 401 for incorrect passcode', async () => {\n      const { hash, salt } = await hashPasscode('654321');\n      vi.mocked(configManager.get).mockReturnValue({\n        passcodeHash: hash,\n        passcodeSalt: salt,\n      });\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/verify-passcode')\n        .send({ passcode: '000000' });\n      expect(res.status).toBe(401);\n      expect(res.body.ok).toBe(false);\n      expect(res.body.error).toBe('Incorrect passcode');\n    });\n\n    it('returns 200 for correct passcode', async () => {\n      const { hash, salt } = await hashPasscode('654321');\n      vi.mocked(configManager.get).mockReturnValue({\n        passcodeHash: hash,\n        passcodeSalt: salt,\n      });\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/verify-passcode')\n        .send({ passcode: '654321' });\n      expect(res.status).toBe(200);\n      expect(res.body.ok).toBe(true);\n    });\n  });\n\n  describe('GET /api/tunnel/session', () => {\n    it('returns passcodeRequired: false when passcode is disabled', async () => {\n      vi.mocked(configManager.get).mockReturnValue({\n        passcodeEnabled: false,\n        passcodeHash: null,\n      });\n      const app = await createTestApp();\n      const res = await request(app).get('/api/tunnel/session');\n      expect(res.body.passcodeRequired).toBe(false);\n    });\n\n    it('returns passcodeRequired: true and authenticated: false for unauthenticated request', async () => {\n      vi.mocked(configManager.get).mockReturnValue({\n        passcodeEnabled: true,\n        passcodeHash: 'somehash',\n      });\n      const app = await createTestApp();\n      const res = await request(app).get('/api/tunnel/session');\n      expect(res.body.passcodeRequired).toBe(true);\n      expect(res.body.authenticated).toBe(false);\n    });\n  });\n\n  describe('POST /api/tunnel/set-passcode', () => {\n    it('returns 403 from non-localhost request', async () => {\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/set-passcode')\n        .set('Host', 'abc123.ngrok-free.app')\n        .send({ passcode: '123456', enabled: true });\n      expect(res.status).toBe(403);\n    });\n\n    it('rejects non-6-digit passcode', async () => {\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/set-passcode')\n        .send({ passcode: '12345', enabled: true });\n      expect(res.status).toBe(400);\n    });\n\n    it('stores hashed passcode via configManager', async () => {\n      vi.mocked(configManager.get).mockReturnValue({});\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/set-passcode')\n        .send({ passcode: '123456', enabled: true });\n      expect(res.status).toBe(200);\n      expect(res.body.ok).toBe(true);\n      // Verify configManager.set was called with hashed values, never plaintext\n      const setCalls = vi.mocked(configManager.set).mock.calls;\n      const lastCall = setCalls[setCalls.length - 1];\n      expect(lastCall[0]).toBe('tunnel');\n      const stored = lastCall[1] as Record<string, unknown>;\n      expect(stored.passcodeEnabled).toBe(true);\n      expect(stored.passcodeHash).toBeDefined();\n      expect(stored.passcodeSalt).toBeDefined();\n      // Hash should NOT be the plaintext passcode\n      expect(stored.passcodeHash).not.toBe('123456');\n    });\n\n    it('disables passcode when enabled: false', async () => {\n      vi.mocked(configManager.get).mockReturnValue({ passcodeEnabled: true });\n      const app = await createTestApp();\n      const res = await request(app)\n        .post('/api/tunnel/set-passcode')\n        .send({ enabled: false });\n      expect(res.status).toBe(200);\n      const setCalls = vi.mocked(configManager.set).mock.calls;\n      const stored = setCalls[setCalls.length - 1][1] as Record<string, unknown>;\n      expect(stored.passcodeEnabled).toBe(false);\n    });\n  });\n});\n```\n\n## Acceptance Criteria\n\n1. `POST /api/tunnel/verify-passcode` validates input with `PasscodeVerifyRequestSchema`, returns 400 for invalid format\n2. `POST /api/tunnel/verify-passcode` returns 401 with `{ ok: false, error: 'Incorrect passcode' }` for wrong passcode\n3. `POST /api/tunnel/verify-passcode` returns 200 with `{ ok: true }` and sets `req.session.tunnelAuthenticated = true` for correct passcode\n4. Rate limiter blocks after 10 attempts per 15-minute window per IP\n5. `GET /api/tunnel/session` returns `{ authenticated, passcodeRequired }` based on config and session state\n6. `POST /api/tunnel/set-passcode` is localhost-only (returns 403 from tunnel hostname)\n7. `POST /api/tunnel/set-passcode` hashes the passcode before storing (never stores plaintext)\n8. `POST /api/tunnel/set-passcode` with `enabled: false` sets `passcodeEnabled: false`\n9. `POST /api/tunnel/set-passcode` calls `tunnelManager.refreshStatus()` to broadcast config changes via SSE\n10. All 10 test scenarios pass: `pnpm vitest run apps/server/src/routes/__tests__/tunnel-passcode.test.ts`",
      "activeForm": "Adding passcode verify, session, and set-passcode routes to tunnel router",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "1.2", "1.3"],
      "parallelWith": []
    },
    {
      "id": "1.6",
      "phase": 1,
      "phaseName": "Server Foundation",
      "subject": "[remote-passcode] [P1] Update tunnel-manager status getter to include passcodeEnabled and add refreshStatus method",
      "description": "## Overview\n\nModify the `TunnelManager` class in `apps/server/src/services/core/tunnel-manager.ts` to include `passcodeEnabled` in the status object and add a `refreshStatus()` method that emits `status_change` events.\n\n## Step 1: Update the DEFAULT_STATUS constant\n\nThe current `DEFAULT_STATUS` in `tunnel-manager.ts` is:\n\n```typescript\nconst DEFAULT_STATUS: TunnelStatus = {\n  enabled: false,\n  connected: false,\n  url: null,\n  port: null,\n  startedAt: null,\n  authEnabled: false,\n  tokenConfigured: false,\n  domain: null,\n};\n```\n\nAdd `passcodeEnabled`:\n\n```typescript\nconst DEFAULT_STATUS: TunnelStatus = {\n  enabled: false,\n  connected: false,\n  url: null,\n  port: null,\n  startedAt: null,\n  authEnabled: false,\n  tokenConfigured: false,\n  domain: null,\n  passcodeEnabled: false,\n};\n```\n\n## Step 2: Update the `status` getter\n\nThe current getter is:\n\n```typescript\nget status(): TunnelStatus {\n  return { ...this._status };\n}\n```\n\nChange it to dynamically read passcode config:\n\n```typescript\nget status(): TunnelStatus {\n  // Import configManager at the top of the file\n  const tunnelConfig = configManager.get('tunnel');\n  return {\n    ...this._status,\n    passcodeEnabled: !!(tunnelConfig?.passcodeEnabled && tunnelConfig?.passcodeHash),\n  };\n}\n```\n\nAdd the import at the top of the file:\n\n```typescript\nimport { configManager } from './config-manager.js';\n```\n\n## Step 3: Add `refreshStatus()` method\n\nAdd a new method to the `TunnelManager` class:\n\n```typescript\n/** Re-emit status_change to broadcast current status (e.g., after passcode config update). */\nrefreshStatus(): void {\n  this.emit('status_change', this.status);\n}\n```\n\n## Acceptance Criteria\n\n1. `tunnelManager.status` includes `passcodeEnabled: boolean` derived from config (`passcodeEnabled && passcodeHash` are both truthy)\n2. `passcodeEnabled` is `false` when config has `passcodeEnabled: false` or `passcodeHash: null`\n3. `DEFAULT_STATUS` includes `passcodeEnabled: false`\n4. `refreshStatus()` emits `status_change` event with the current status object\n5. Existing `start()` and `stop()` behavior is unchanged\n6. `pnpm typecheck` passes (TunnelStatus type now requires `passcodeEnabled`)",
      "activeForm": "Updating tunnel-manager with passcodeEnabled status and refreshStatus method",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["1.2", "1.3"]
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Client Gate",
      "subject": "[remote-passcode] [P2] Install shadcn InputOTP component and add transport methods to HttpTransport",
      "description": "## Overview\n\nInstall the `input-otp` shadcn component in the client app, then implement the two new transport methods (`verifyTunnelPasscode` and `checkTunnelSession`) in `HttpTransport` and `DirectTransport`.\n\n## Step 1: Install shadcn InputOTP\n\nRun from the repo root:\n```bash\ncd apps/client && npx shadcn@latest add input-otp\n```\n\nThis installs the `input-otp` npm package and generates `apps/client/src/layers/shared/ui/input-otp.tsx`. Verify the file is created and exports `InputOTP`, `InputOTPGroup`, and `InputOTPSlot`.\n\nExport the new component from the shared UI barrel. Add to `apps/client/src/layers/shared/ui/index.ts`:\n\n```typescript\nexport { InputOTP, InputOTPGroup, InputOTPSlot } from './input-otp';\n```\n\n## Step 2: Add methods to `HttpTransport`\n\nIn `apps/client/src/layers/shared/lib/transport/http-transport.ts`, add the two new methods to the `HttpTransport` class. Place them after the `stopTunnel()` method:\n\n```typescript\nasync verifyTunnelPasscode(\n  passcode: string\n): Promise<{ ok: boolean; error?: string; retryAfter?: number }> {\n  return fetchJSON(this.baseUrl, '/tunnel/verify-passcode', {\n    method: 'POST',\n    body: JSON.stringify({ passcode }),\n  });\n}\n\nasync checkTunnelSession(): Promise<{ authenticated: boolean; passcodeRequired: boolean }> {\n  return fetchJSON(this.baseUrl, '/tunnel/session', { method: 'GET' });\n}\n```\n\nNote: `fetchJSON` is the existing internal helper used by all HttpTransport methods. It handles Content-Type headers and error parsing.\n\n## Step 3: Add stub methods to `DirectTransport`\n\nIn `apps/client/src/layers/shared/lib/direct-transport.ts`, add the two methods with appropriate stubs (the DirectTransport is used by the Obsidian plugin where tunnel passcode is not relevant):\n\n```typescript\nasync verifyTunnelPasscode(\n  _passcode: string\n): Promise<{ ok: boolean; error?: string; retryAfter?: number }> {\n  return { ok: true };\n}\n\nasync checkTunnelSession(): Promise<{ authenticated: boolean; passcodeRequired: boolean }> {\n  return { authenticated: true, passcodeRequired: false };\n}\n```\n\n## Step 4: Update mock transport in test-utils\n\nIn `packages/test-utils/src/mock-factories.ts` (or wherever `createMockTransport` is defined), add the two new methods:\n\n```typescript\nverifyTunnelPasscode: vi.fn().mockResolvedValue({ ok: true }),\ncheckTunnelSession: vi.fn().mockResolvedValue({ authenticated: true, passcodeRequired: false }),\n```\n\n## Acceptance Criteria\n\n1. `input-otp` package is installed in client dependencies\n2. `apps/client/src/layers/shared/ui/input-otp.tsx` exists and exports `InputOTP`, `InputOTPGroup`, `InputOTPSlot`\n3. Shared UI barrel (`index.ts`) re-exports the InputOTP components\n4. `HttpTransport.verifyTunnelPasscode` calls `POST /tunnel/verify-passcode` with `{ passcode }` body\n5. `HttpTransport.checkTunnelSession` calls `GET /tunnel/session`\n6. `DirectTransport` stubs return `{ ok: true }` and `{ authenticated: true, passcodeRequired: false }` respectively\n7. Mock transport factory includes both new methods\n8. `pnpm typecheck` passes across all packages",
      "activeForm": "Installing shadcn InputOTP and adding transport methods to HttpTransport and DirectTransport",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": []
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Client Gate",
      "subject": "[remote-passcode] [P2] Create PasscodeGate and PasscodeGateWrapper components in features/tunnel-gate",
      "description": "## Overview\n\nCreate a new FSD feature module `apps/client/src/layers/features/tunnel-gate/` with the `PasscodeGate` (full-screen passcode entry) and `PasscodeGateWrapper` (orchestrator that decides whether to show the gate or the app). This module is in the `features` layer because it orchestrates UI + transport calls.\n\n## Step 1: Create `apps/client/src/layers/features/tunnel-gate/ui/PasscodeGate.tsx`\n\n```tsx\nimport { useState, useCallback } from 'react';\nimport { DorkLogo } from '@dorkos/icons';\nimport { InputOTP, InputOTPGroup, InputOTPSlot } from '@/layers/shared/ui';\nimport { useTransport } from '@/layers/shared/model';\nimport { REGEXP_ONLY_DIGITS } from 'input-otp';\n\ninterface PasscodeGateProps {\n  onSuccess: () => void;\n}\n\n/** Full-screen passcode entry gate for remote tunnel access. */\nexport function PasscodeGate({ onSuccess }: PasscodeGateProps) {\n  const transport = useTransport();\n  const [error, setError] = useState<string | null>(null);\n  const [isVerifying, setIsVerifying] = useState(false);\n  const [value, setValue] = useState('');\n\n  const handleComplete = useCallback(\n    async (passcode: string) => {\n      setIsVerifying(true);\n      setError(null);\n\n      try {\n        const result = await transport.verifyTunnelPasscode(passcode);\n        if (result.ok) {\n          onSuccess();\n        } else {\n          setError(result.error ?? 'Incorrect passcode');\n          setValue('');\n        }\n      } catch {\n        setError('Connection error. Try again.');\n        setValue('');\n      } finally {\n        setIsVerifying(false);\n      }\n    },\n    [transport, onSuccess]\n  );\n\n  return (\n    <div className=\"bg-background flex min-h-dvh items-center justify-center p-4\">\n      <div className=\"flex w-full max-w-sm flex-col items-center gap-8\">\n        <DorkLogo variant=\"white\" size={120} />\n\n        <div className=\"flex flex-col items-center gap-2 text-center\">\n          <h1 className=\"text-foreground text-lg font-semibold\">Enter passcode</h1>\n          <p className=\"text-muted-foreground text-sm\">\n            Enter your 6-digit passcode to access this instance.\n          </p>\n        </div>\n\n        <InputOTP\n          maxLength={6}\n          pattern={REGEXP_ONLY_DIGITS}\n          inputMode=\"numeric\"\n          value={value}\n          onChange={setValue}\n          onComplete={handleComplete}\n          disabled={isVerifying}\n          autoFocus\n        >\n          <InputOTPGroup>\n            {[0, 1, 2, 3, 4, 5].map((i) => (\n              <InputOTPSlot key={i} index={i} className={error ? 'border-destructive' : ''} />\n            ))}\n          </InputOTPGroup>\n        </InputOTP>\n\n        {error && <p className=\"text-destructive text-sm\">{error}</p>}\n\n        {isVerifying && <p className=\"text-muted-foreground text-sm\">Verifying...</p>}\n      </div>\n    </div>\n  );\n}\n```\n\nKey design details:\n- Full viewport (`min-h-dvh`) centered layout with `bg-background` for dark theme\n- `DorkLogo` at `variant=\"white\"` + `size={120}` -- prominent but not overwhelming\n- `InputOTP` with `inputMode=\"numeric\"` triggers numeric keyboard on mobile\n- `pattern={REGEXP_ONLY_DIGITS}` rejects non-numeric input\n- `autoFocus` immediately focuses the first digit box\n- `onComplete` auto-submits when all 6 digits are entered (no submit button needed)\n- Error state: `border-destructive` on digit boxes + error message below\n- Disabled state during verification prevents double-submit\n- Value is cleared on failure so user can retry immediately\n\n## Step 2: Create `apps/client/src/layers/features/tunnel-gate/ui/PasscodeGateWrapper.tsx`\n\n```tsx\nimport { useState, useEffect } from 'react';\nimport { useTransport } from '@/layers/shared/model';\nimport { PasscodeGate } from './PasscodeGate';\n\ninterface PasscodeGateWrapperProps {\n  children: React.ReactNode;\n}\n\n/** Orchestrates session check and renders PasscodeGate or app children. */\nexport function PasscodeGateWrapper({ children }: PasscodeGateWrapperProps) {\n  const transport = useTransport();\n  const [state, setState] = useState<'checking' | 'locked' | 'unlocked'>('checking');\n\n  useEffect(() => {\n    // Only gate if we're on a tunnel URL (not localhost)\n    if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') {\n      setState('unlocked');\n      return;\n    }\n\n    transport\n      .checkTunnelSession()\n      .then((result) => {\n        if (!result.passcodeRequired || result.authenticated) {\n          setState('unlocked');\n        } else {\n          setState('locked');\n        }\n      })\n      .catch(() => {\n        // If session check fails, assume unlocked (fail-open for localhost fallback)\n        setState('unlocked');\n      });\n  }, [transport]);\n\n  if (state === 'checking') {\n    return null; // Brief blank screen during session check\n  }\n\n  if (state === 'locked') {\n    return <PasscodeGate onSuccess={() => setState('unlocked')} />;\n  }\n\n  return <>{children}</>;\n}\n```\n\n## Step 3: Create barrel export at `apps/client/src/layers/features/tunnel-gate/index.ts`\n\n```typescript\n/**\n * Tunnel gate feature -- passcode authentication gate for remote tunnel access.\n *\n * @module features/tunnel-gate\n */\nexport { PasscodeGate } from './ui/PasscodeGate.js';\nexport { PasscodeGateWrapper } from './ui/PasscodeGateWrapper.js';\n```\n\n## Acceptance Criteria\n\n1. `features/tunnel-gate/` module exists with `ui/PasscodeGate.tsx`, `ui/PasscodeGateWrapper.tsx`, and `index.ts`\n2. `PasscodeGate` renders DorkOS logo, heading, description, and 6 digit input boxes\n3. `PasscodeGate` calls `verifyTunnelPasscode` when all 6 digits are entered\n4. `PasscodeGate` shows error message and clears input on failed verification\n5. `PasscodeGate` disables input during verification to prevent double-submit\n6. `PasscodeGate` calls `onSuccess` on successful verification\n7. `PasscodeGateWrapper` renders children immediately on localhost\n8. `PasscodeGateWrapper` calls `checkTunnelSession` on non-localhost hostname\n9. `PasscodeGateWrapper` shows `PasscodeGate` when passcode is required and session is not authenticated\n10. `PasscodeGateWrapper` renders children when session is authenticated or passcode is not required\n11. `PasscodeGateWrapper` renders children on session check failure (fail-open)\n12. Barrel export re-exports both components",
      "activeForm": "Creating PasscodeGate and PasscodeGateWrapper components in features/tunnel-gate module",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": []
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Client Gate",
      "subject": "[remote-passcode] [P2] Integrate PasscodeGateWrapper in main.tsx and write client gate tests",
      "description": "## Overview\n\nWrap the app's `RouterProvider` in the `PasscodeGateWrapper` so remote tunnel users see the passcode gate before accessing the app. Then write tests for both `PasscodeGate` and `PasscodeGateWrapper` components.\n\n## Step 1: Modify `apps/client/src/main.tsx`\n\nAdd the import at the top:\n\n```typescript\nimport { PasscodeGateWrapper } from '@/layers/features/tunnel-gate';\n```\n\nIn the `Root` component, wrap `RouterProvider` with `PasscodeGateWrapper` inside `TransportProvider`:\n\nThe current render tree is:\n```tsx\n<QueryClientProvider client={queryClient}>\n  <TransportProvider transport={transport}>\n    <RouterProvider router={router} />\n  </TransportProvider>\n  {import.meta.env.DEV && <DevtoolsToggle />}\n</QueryClientProvider>\n```\n\nChange it to:\n```tsx\n<QueryClientProvider client={queryClient}>\n  <TransportProvider transport={transport}>\n    <PasscodeGateWrapper>\n      <RouterProvider router={router} />\n    </PasscodeGateWrapper>\n  </TransportProvider>\n  {import.meta.env.DEV && <DevtoolsToggle />}\n</QueryClientProvider>\n```\n\nThe gate wrapper checks `window.location.hostname` -- on localhost it renders children immediately. On a tunnel hostname, it calls `GET /api/tunnel/session` first.\n\n## Step 2: Write tests for PasscodeGate\n\nCreate `apps/client/src/layers/features/tunnel-gate/__tests__/PasscodeGate.test.tsx`:\n\n```tsx\n/**\n * @vitest-environment jsdom\n */\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { render, screen, waitFor } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport '@testing-library/jest-dom';\nimport { PasscodeGate } from '../ui/PasscodeGate';\nimport { TransportProvider } from '@/layers/shared/model';\nimport { createMockTransport } from '@dorkos/test-utils';\n\n// Mock @dorkos/icons to avoid import issues in test environment\nvi.mock('@dorkos/icons', () => ({\n  DorkLogo: ({ size, variant }: { size: number; variant: string }) => (\n    <div data-testid=\"dork-logo\" data-size={size} data-variant={variant} />\n  ),\n}));\n\ndescribe('PasscodeGate', () => {\n  let mockTransport: ReturnType<typeof createMockTransport>;\n  const onSuccess = vi.fn();\n\n  function Wrapper({ children }: { children: React.ReactNode }) {\n    return <TransportProvider transport={mockTransport}>{children}</TransportProvider>;\n  }\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n    mockTransport = createMockTransport();\n  });\n\n  it('renders DorkOS logo, heading, and 6 digit input boxes', () => {\n    render(<PasscodeGate onSuccess={onSuccess} />, { wrapper: Wrapper });\n    expect(screen.getByTestId('dork-logo')).toBeInTheDocument();\n    expect(screen.getByText('Enter passcode')).toBeInTheDocument();\n    expect(screen.getByText(/6-digit passcode/)).toBeInTheDocument();\n  });\n\n  it('calls verifyTunnelPasscode when 6 digits entered', async () => {\n    mockTransport.verifyTunnelPasscode.mockResolvedValue({ ok: true });\n    const user = userEvent.setup();\n    render(<PasscodeGate onSuccess={onSuccess} />, { wrapper: Wrapper });\n\n    // Type 6 digits into the OTP input\n    await user.keyboard('123456');\n\n    await waitFor(() => {\n      expect(mockTransport.verifyTunnelPasscode).toHaveBeenCalledWith('123456');\n    });\n  });\n\n  it('shows error message on failed verification', async () => {\n    mockTransport.verifyTunnelPasscode.mockResolvedValue({\n      ok: false,\n      error: 'Incorrect passcode',\n    });\n    const user = userEvent.setup();\n    render(<PasscodeGate onSuccess={onSuccess} />, { wrapper: Wrapper });\n\n    await user.keyboard('000000');\n\n    await waitFor(() => {\n      expect(screen.getByText('Incorrect passcode')).toBeInTheDocument();\n    });\n  });\n\n  it('calls onSuccess on successful verification', async () => {\n    mockTransport.verifyTunnelPasscode.mockResolvedValue({ ok: true });\n    const user = userEvent.setup();\n    render(<PasscodeGate onSuccess={onSuccess} />, { wrapper: Wrapper });\n\n    await user.keyboard('123456');\n\n    await waitFor(() => {\n      expect(onSuccess).toHaveBeenCalled();\n    });\n  });\n\n  it('shows connection error on transport failure', async () => {\n    mockTransport.verifyTunnelPasscode.mockRejectedValue(new Error('Network error'));\n    const user = userEvent.setup();\n    render(<PasscodeGate onSuccess={onSuccess} />, { wrapper: Wrapper });\n\n    await user.keyboard('123456');\n\n    await waitFor(() => {\n      expect(screen.getByText('Connection error. Try again.')).toBeInTheDocument();\n    });\n  });\n});\n```\n\n## Step 3: Write tests for PasscodeGateWrapper\n\nCreate `apps/client/src/layers/features/tunnel-gate/__tests__/PasscodeGateWrapper.test.tsx`:\n\n```tsx\n/**\n * @vitest-environment jsdom\n */\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { render, screen, waitFor } from '@testing-library/react';\nimport '@testing-library/jest-dom';\nimport { PasscodeGateWrapper } from '../ui/PasscodeGateWrapper';\nimport { TransportProvider } from '@/layers/shared/model';\nimport { createMockTransport } from '@dorkos/test-utils';\n\n// Mock @dorkos/icons\nvi.mock('@dorkos/icons', () => ({\n  DorkLogo: () => <div data-testid=\"dork-logo\" />,\n}));\n\ndescribe('PasscodeGateWrapper', () => {\n  let mockTransport: ReturnType<typeof createMockTransport>;\n\n  function Wrapper({ children }: { children: React.ReactNode }) {\n    return <TransportProvider transport={mockTransport}>{children}</TransportProvider>;\n  }\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n    mockTransport = createMockTransport();\n  });\n\n  it('renders children immediately on localhost', async () => {\n    // Default jsdom hostname is 'localhost'\n    render(\n      <Wrapper>\n        <PasscodeGateWrapper>\n          <div data-testid=\"app-content\">App</div>\n        </PasscodeGateWrapper>\n      </Wrapper>\n    );\n\n    await waitFor(() => {\n      expect(screen.getByTestId('app-content')).toBeInTheDocument();\n    });\n    expect(mockTransport.checkTunnelSession).not.toHaveBeenCalled();\n  });\n\n  it('shows PasscodeGate when passcode required and not authenticated', async () => {\n    // Override hostname to simulate tunnel access\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'abc123.ngrok-free.app' },\n      writable: true,\n    });\n\n    mockTransport.checkTunnelSession.mockResolvedValue({\n      authenticated: false,\n      passcodeRequired: true,\n    });\n\n    render(\n      <Wrapper>\n        <PasscodeGateWrapper>\n          <div data-testid=\"app-content\">App</div>\n        </PasscodeGateWrapper>\n      </Wrapper>\n    );\n\n    await waitFor(() => {\n      expect(screen.getByText('Enter passcode')).toBeInTheDocument();\n    });\n    expect(screen.queryByTestId('app-content')).not.toBeInTheDocument();\n\n    // Restore\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'localhost' },\n      writable: true,\n    });\n  });\n\n  it('renders children when session is authenticated', async () => {\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'abc123.ngrok-free.app' },\n      writable: true,\n    });\n\n    mockTransport.checkTunnelSession.mockResolvedValue({\n      authenticated: true,\n      passcodeRequired: true,\n    });\n\n    render(\n      <Wrapper>\n        <PasscodeGateWrapper>\n          <div data-testid=\"app-content\">App</div>\n        </PasscodeGateWrapper>\n      </Wrapper>\n    );\n\n    await waitFor(() => {\n      expect(screen.getByTestId('app-content')).toBeInTheDocument();\n    });\n\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'localhost' },\n      writable: true,\n    });\n  });\n\n  it('renders children when passcode is not required', async () => {\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'abc123.ngrok-free.app' },\n      writable: true,\n    });\n\n    mockTransport.checkTunnelSession.mockResolvedValue({\n      authenticated: false,\n      passcodeRequired: false,\n    });\n\n    render(\n      <Wrapper>\n        <PasscodeGateWrapper>\n          <div data-testid=\"app-content\">App</div>\n        </PasscodeGateWrapper>\n      </Wrapper>\n    );\n\n    await waitFor(() => {\n      expect(screen.getByTestId('app-content')).toBeInTheDocument();\n    });\n\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'localhost' },\n      writable: true,\n    });\n  });\n\n  it('renders children on session check failure (fail-open)', async () => {\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'abc123.ngrok-free.app' },\n      writable: true,\n    });\n\n    mockTransport.checkTunnelSession.mockRejectedValue(new Error('Network error'));\n\n    render(\n      <Wrapper>\n        <PasscodeGateWrapper>\n          <div data-testid=\"app-content\">App</div>\n        </PasscodeGateWrapper>\n      </Wrapper>\n    );\n\n    await waitFor(() => {\n      expect(screen.getByTestId('app-content')).toBeInTheDocument();\n    });\n\n    Object.defineProperty(window, 'location', {\n      value: { hostname: 'localhost' },\n      writable: true,\n    });\n  });\n});\n```\n\n## Acceptance Criteria\n\n1. `main.tsx` wraps `RouterProvider` in `PasscodeGateWrapper` inside `TransportProvider`\n2. PasscodeGate tests: renders logo/heading/inputs, calls `verifyTunnelPasscode`, shows errors, calls `onSuccess` -- all 5 test scenarios pass\n3. PasscodeGateWrapper tests: localhost bypass, locked state, authenticated state, not-required state, fail-open -- all 5 test scenarios pass\n4. `pnpm vitest run apps/client/src/layers/features/tunnel-gate/__tests__/PasscodeGate.test.tsx`\n5. `pnpm vitest run apps/client/src/layers/features/tunnel-gate/__tests__/PasscodeGateWrapper.test.tsx`",
      "activeForm": "Integrating PasscodeGateWrapper in main.tsx and writing client gate tests",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.2"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Settings UI",
      "subject": "[remote-passcode] [P3] Add passcode configuration section to TunnelDialog with toggle, InputOTP, and save",
      "description": "## Overview\n\nModify the existing `TunnelDialog` component (`apps/client/src/layers/features/settings/ui/TunnelDialog.tsx`) to include a passcode configuration section. This section appears between the Custom Domain section and the Separator/Toggle section, only when the auth token is configured and the tunnel is not connected. Users can toggle passcode on/off, enter a 6-digit PIN, and save it.\n\n## Step 1: Add new imports to TunnelDialog.tsx\n\nAdd these imports at the top of the file:\n\n```typescript\nimport { InputOTP, InputOTPGroup, InputOTPSlot } from '@/layers/shared/ui';\nimport { REGEXP_ONLY_DIGITS } from 'input-otp';\nimport { Button } from '@/layers/shared/ui';\n```\n\nNote: Check if `Button` is already imported. If `Switch` is imported, it should already be available.\n\n## Step 2: Add state variables\n\nInside the `TunnelDialog` function, after the existing state declarations (around line 93), add:\n\n```typescript\nconst [passcodeEnabled, setPasscodeEnabled] = useState(false);\nconst [passcodeInput, setPasscodeInput] = useState('');\n```\n\n## Step 3: Add passcodeEnabled sync effect\n\nAfter the existing domain sync effect (around line 115), add:\n\n```typescript\n// Sync passcodeEnabled from server config\nuseEffect(() => {\n  // eslint-disable-next-line react-hooks/set-state-in-effect -- syncing local passcode toggle from server config\n  if (tunnel?.passcodeEnabled !== undefined) {\n    setPasscodeEnabled(tunnel.passcodeEnabled);\n  }\n}, [tunnel?.passcodeEnabled]);\n```\n\nNote: The `tunnel` object comes from `serverConfig?.tunnel` which is the TunnelStatus. However, `passcodeEnabled` is available on TunnelStatus after our schema changes.\n\n## Step 4: Add handler functions\n\nAfter the existing handler functions (after `handleCopySessionLink`), add:\n\n```typescript\nconst handlePasscodeToggle = useCallback(\n  async (checked: boolean) => {\n    if (!checked) {\n      // Disable passcode\n      try {\n        const res = await fetch('/api/tunnel/set-passcode', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ enabled: false }),\n        });\n        if (!res.ok) throw new Error('Failed to disable passcode');\n        setPasscodeEnabled(false);\n        setPasscodeInput('');\n        queryClient.invalidateQueries({ queryKey: ['config'] });\n      } catch {\n        toast.error('Failed to disable passcode');\n      }\n    } else {\n      setPasscodeEnabled(true);\n    }\n  },\n  [queryClient]\n);\n\nconst handleSavePasscode = useCallback(async () => {\n  try {\n    const res = await fetch('/api/tunnel/set-passcode', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ passcode: passcodeInput, enabled: true }),\n    });\n    if (!res.ok) throw new Error('Failed to save passcode');\n    setPasscodeInput('');\n    queryClient.invalidateQueries({ queryKey: ['config'] });\n    toast.success('Passcode saved');\n  } catch {\n    toast.error('Failed to save passcode');\n  }\n}, [passcodeInput, queryClient]);\n```\n\n## Step 5: Add passcode section to JSX\n\nIn the JSX, insert the passcode section after the Custom Domain section (after the closing `}` of the custom domain conditional block, around line 388) and before the Connected/stopping state QR hero section:\n\n```tsx\n{/* Passcode Section */}\n{tunnel?.tokenConfigured && state !== 'connected' && state !== 'stopping' && (\n  <div className=\"space-y-3\">\n    <div className=\"flex items-center justify-between\">\n      <div>\n        <p className=\"text-sm font-medium\">Passcode</p>\n        <p className=\"text-muted-foreground text-xs\">\n          Require a 6-digit PIN for remote access\n        </p>\n      </div>\n      <Switch\n        checked={passcodeEnabled}\n        onCheckedChange={handlePasscodeToggle}\n        disabled={state === 'connected'}\n      />\n    </div>\n\n    {passcodeEnabled && (\n      <div className=\"space-y-2\">\n        <InputOTP\n          maxLength={6}\n          pattern={REGEXP_ONLY_DIGITS}\n          inputMode=\"numeric\"\n          value={passcodeInput}\n          onChange={setPasscodeInput}\n          disabled={state === 'connected'}\n        >\n          <InputOTPGroup>\n            {[0, 1, 2, 3, 4, 5].map((i) => (\n              <InputOTPSlot key={i} index={i} />\n            ))}\n          </InputOTPGroup>\n        </InputOTP>\n        <Button\n          size=\"sm\"\n          onClick={handleSavePasscode}\n          disabled={passcodeInput.length !== 6 || state === 'connected'}\n        >\n          {tunnel.passcodeEnabled ? 'Update passcode' : 'Set passcode'}\n        </Button>\n      </div>\n    )}\n  </div>\n)}\n```\n\n## Step 6: Write tests\n\nCreate `apps/client/src/layers/features/settings/__tests__/TunnelDialog-passcode.test.tsx`:\n\n```tsx\n/**\n * @vitest-environment jsdom\n */\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { render, screen, waitFor } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport '@testing-library/jest-dom';\nimport { QueryClient, QueryClientProvider } from '@tanstack/react-query';\nimport { TransportProvider } from '@/layers/shared/model';\nimport { createMockTransport } from '@dorkos/test-utils';\nimport { TunnelDialog } from '../ui/TunnelDialog';\n\n// Mock dependencies\nvi.mock('@dorkos/icons', () => ({\n  DorkLogo: () => <div data-testid=\"dork-logo\" />,\n}));\n\nvi.mock('react-qr-code', () => ({\n  default: () => <div data-testid=\"qr-code\" />,\n}));\n\nvi.mock('../ui/TunnelOnboarding', () => ({\n  TunnelOnboarding: () => <div data-testid=\"tunnel-onboarding\" />,\n}));\n\n// Mock fetch for set-passcode calls\nconst mockFetch = vi.fn();\nglobal.fetch = mockFetch;\n\ndescribe('TunnelDialog - Passcode Section', () => {\n  let mockTransport: ReturnType<typeof createMockTransport>;\n  let queryClient: QueryClient;\n\n  function renderDialog(tunnelStatus: Record<string, unknown> = {}) {\n    const defaultTunnel = {\n      enabled: false,\n      connected: false,\n      url: null,\n      port: null,\n      startedAt: null,\n      authEnabled: false,\n      tokenConfigured: true,\n      domain: null,\n      passcodeEnabled: false,\n      ...tunnelStatus,\n    };\n\n    mockTransport.getConfig.mockResolvedValue({\n      tunnel: defaultTunnel,\n      version: '1.0.0',\n      latestVersion: null,\n      isDevMode: true,\n      dismissedUpgradeVersions: [],\n      port: 6242,\n      uptime: 1000,\n      workingDirectory: '/tmp',\n      nodeVersion: '20.0.0',\n      claudeCliPath: null,\n      boundary: '/tmp',\n    });\n\n    return render(\n      <QueryClientProvider client={queryClient}>\n        <TransportProvider transport={mockTransport}>\n          <TunnelDialog open={true} onOpenChange={vi.fn()} />\n        </TransportProvider>\n      </QueryClientProvider>\n    );\n  }\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n    mockTransport = createMockTransport();\n    queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });\n    mockFetch.mockResolvedValue({ ok: true, json: () => Promise.resolve({ ok: true }) });\n  });\n\n  it('renders passcode section when token is configured and not connected', async () => {\n    renderDialog({ tokenConfigured: true });\n    await waitFor(() => {\n      expect(screen.getByText('Passcode')).toBeInTheDocument();\n    });\n  });\n\n  it('does not render passcode section when not tokenConfigured', async () => {\n    renderDialog({ tokenConfigured: false });\n    await waitFor(() => {\n      expect(screen.queryByText('Passcode')).not.toBeInTheDocument();\n    });\n  });\n\n  it('shows InputOTP when passcode toggle is enabled', async () => {\n    const user = userEvent.setup();\n    renderDialog({ tokenConfigured: true });\n\n    await waitFor(() => {\n      expect(screen.getByText('Passcode')).toBeInTheDocument();\n    });\n\n    // Toggle the passcode switch\n    const switches = screen.getAllByRole('switch');\n    // Find the passcode switch (the one near the Passcode label)\n    const passcodeSwitch = switches.find((s) =>\n      s.closest('.space-y-3')?.querySelector('p')?.textContent === 'Passcode'\n    ) ?? switches[0];\n    await user.click(passcodeSwitch);\n\n    await waitFor(() => {\n      expect(screen.getByText('Set passcode')).toBeInTheDocument();\n    });\n  });\n\n  it('disables save button when input is incomplete', async () => {\n    renderDialog({ tokenConfigured: true, passcodeEnabled: true });\n\n    await waitFor(() => {\n      const saveBtn = screen.queryByText('Update passcode') ?? screen.queryByText('Set passcode');\n      if (saveBtn) {\n        expect(saveBtn.closest('button')).toBeDisabled();\n      }\n    });\n  });\n\n  it('calls set-passcode API when save is clicked with complete input', async () => {\n    const user = userEvent.setup();\n    renderDialog({ tokenConfigured: true, passcodeEnabled: true });\n\n    await waitFor(() => {\n      expect(screen.getByText('Set passcode')).toBeInTheDocument();\n    });\n\n    // Type 6 digits\n    await user.keyboard('123456');\n\n    await waitFor(() => {\n      const saveBtn = screen.getByText('Set passcode');\n      expect(saveBtn.closest('button')).not.toBeDisabled();\n    });\n\n    await user.click(screen.getByText('Set passcode'));\n\n    await waitFor(() => {\n      expect(mockFetch).toHaveBeenCalledWith(\n        '/api/tunnel/set-passcode',\n        expect.objectContaining({\n          method: 'POST',\n          body: expect.stringContaining('123456'),\n        })\n      );\n    });\n  });\n});\n```\n\n## Acceptance Criteria\n\n1. Passcode section renders when `tunnel.tokenConfigured` is true and state is not `'connected'` or `'stopping'`\n2. Passcode section is hidden when `tokenConfigured` is false\n3. Toggle switch controls `passcodeEnabled` state\n4. Disabling the toggle calls `POST /api/tunnel/set-passcode` with `{ enabled: false }`\n5. InputOTP appears when toggle is ON, with 6 numeric digit slots\n6. Save button text is \"Set passcode\" when no passcode exists, \"Update passcode\" when one exists\n7. Save button is disabled when fewer than 6 digits are entered\n8. Save button calls `POST /api/tunnel/set-passcode` with `{ passcode, enabled: true }`\n9. Toast shows \"Passcode saved\" on success, error toast on failure\n10. Input clears after successful save\n11. `queryClient.invalidateQueries({ queryKey: ['config'] })` is called after save to refresh tunnel status\n12. All test scenarios pass: `pnpm vitest run apps/client/src/layers/features/settings/__tests__/TunnelDialog-passcode.test.tsx`",
      "activeForm": "Adding passcode configuration section to TunnelDialog with toggle, InputOTP, and save handlers",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.1", "1.5"],
      "parallelWith": []
    }
  ]
}
