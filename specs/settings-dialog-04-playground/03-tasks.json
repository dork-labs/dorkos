{
  "spec": "specs/settings-dialog-04-playground/02-specification.md",
  "slug": "settings-dialog-04-playground",
  "generatedAt": "2026-04-07T00:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Foundation — Mock Data",
      "subject": "[settings-dialog-04-playground] [P1] Create settings-mock-data.ts with typed mock data",
      "description": "Create the mock data module that all Settings showcases will share.\n\n## File to create\n\n`apps/client/src/dev/showcases/settings-mock-data.ts` (~70 lines)\n\n## Purpose\n\nProvide typed, hardcoded mock data for `ServerConfig`, `AgentManifest`, and the mesh agents list. These literals power the playground showcases for `ServerTab`, `ToolsTab`, `AgentDialog`, `ChannelsTab`, and `AgentsTab` without making any network calls — the playground uses `createPlaygroundTransport()` which returns `null` for everything.\n\n## Implementation\n\nThe data must be typed against the real schemas so TypeScript catches any drift if the schemas change.\n\n```ts\nimport type { ServerConfig } from '@dorkos/shared/types';\nimport type { AgentManifest } from '@dorkos/shared/mesh-schemas';\n\n/** Realistic mock server config used by ServerTab/ToolsTab showcases. */\nexport const MOCK_SERVER_CONFIG: ServerConfig = {\n  version: '0.30.0',\n  latestVersion: '0.30.0',\n  port: 4242,\n  uptime: 12_345,\n  workingDirectory: '/Users/dev/dorkos',\n  dorkHome: '/Users/dev/.dork',\n  boundary: '/Users/dev',\n  nodeVersion: 'v22.10.0',\n  isDevMode: false,\n  claudeCliPath: '/usr/local/bin/claude',\n  scheduler: { maxConcurrentRuns: 3, timezone: null, retentionCount: 100 },\n  tasks: { enabled: true },\n  relay: { enabled: true },\n  mesh: { enabled: true },\n  agents: { defaultAgent: 'dorkbot' },\n  agentContext: { tasksTools: true, relayTools: true, meshTools: true, adapterTools: true },\n  mcp: {\n    enabled: false,\n    endpoint: 'http://localhost:4242/mcp',\n    authConfigured: false,\n    authSource: 'none',\n    rateLimit: { enabled: false, maxPerWindow: 60, windowSecs: 60 },\n  },\n  logging: { level: 'info', maxLogSizeKb: 500, maxLogFiles: 14 },\n  // Other fields filled in to match the ServerConfig schema\n};\n\n/** Mock agent manifest used by the AgentDialog showcase. */\nexport const MOCK_AGENT_MANIFEST: AgentManifest = {\n  id: 'mock-agent-01',\n  name: 'Mock Agent',\n  slug: 'mock-agent',\n  description: 'A static agent used for playground showcases',\n  color: '#3b82f6',\n  isSystem: false,\n  // Other required fields per the schema\n};\n\n/** Mock mesh agents list used by AgentsTab. */\nexport const MOCK_MESH_AGENTS = {\n  agents: [\n    { id: 'dorkbot', name: 'dorkbot', slug: 'dorkbot', isSystem: true },\n    { id: 'mock-agent-01', name: 'Mock Agent', slug: 'mock-agent', isSystem: false },\n  ],\n};\n```\n\n## Schema-shape risk\n\nThe `ServerConfig` and `AgentManifest` Zod schemas live in `packages/shared/src/types/` and `packages/shared/src/mesh-schemas.ts`. They are large and almost certainly have required fields not listed in the spec sketch above. Run `pnpm typecheck` immediately after writing the file and fill in every missing required field exactly once. Do NOT use `as any` or `as ServerConfig` to bypass — the whole point of this file is that the type is enforced.\n\nIf a required field has no obvious 'realistic' value, pick a sensible default (`false` for booleans, `0` for counters, empty arrays for lists) and add a brief inline comment.\n\n## TSDoc requirements\n\nPer `.claude/rules/documentation.md`, every export in this file must have TSDoc:\n- `MOCK_SERVER_CONFIG` — describe what showcases consume it\n- `MOCK_AGENT_MANIFEST` — describe AgentDialog usage\n- `MOCK_MESH_AGENTS` — describe AgentsTab usage\n\n## Acceptance criteria\n\n- [ ] File exists at `apps/client/src/dev/showcases/settings-mock-data.ts`\n- [ ] All three exports are typed against the real schemas (no `any`, no `as` casts)\n- [ ] `pnpm typecheck` passes — every required field of `ServerConfig` and `AgentManifest` is present\n- [ ] Each export has a TSDoc comment\n- [ ] No imports from `@/layers/features/*` (this is a pure data module)\n- [ ] No runtime cost — file contains only static literal exports\n\n## Out of scope\n\n- Creating any showcase components (Phase 2)\n- Wiring into the registry (Phase 2)\n- Adding helpers like `MockedQueryProvider` (that lives in `SettingsShowcases.tsx`, Phase 3)",
      "activeForm": "Creating settings mock data module",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Foundation — Registry Wiring",
      "subject": "[settings-dialog-04-playground] [P2] Add SETTINGS_SECTIONS registry entries",
      "description": "Create the section registry file that drives the TOC sidebar and Cmd+K search index for the new Settings playground page.\n\n## File to create\n\n`apps/client/src/dev/sections/settings-sections.ts` (~50 lines)\n\n## Pattern reference\n\nFollow `apps/client/src/dev/sections/components-sections.ts` exactly — it's a `PlaygroundSection[]` array using the `PlaygroundSection` interface from `../playground-registry`.\n\nEach entry has:\n- `id` — kebab-case anchor matching the rendered `<section id=\"...\">` (must be unique across the whole `PLAYGROUND_REGISTRY` — verified by `apps/client/src/dev/__tests__/playground-registry.test.ts`)\n- `title` — display name in TOC sidebar and search results\n- `page: 'settings'` — the new Page union member added in task 2.2\n- `category` — TOC section grouping (Dialogs / Tabs / Responsive / States / Primitives)\n- `keywords` — fuzzy-search aliases\n\n## Implementation\n\n```ts\nimport type { PlaygroundSection } from '../playground-registry';\n\n/**\n * Settings sections from SettingsPage.\n *\n * Sources: SettingsShowcases — Full Settings Dialog, Full Agent Dialog,\n * Individual Tabs, Mobile Drill-In, Loading & Empty States, Settings Primitives.\n */\nexport const SETTINGS_SECTIONS: PlaygroundSection[] = [\n  // Dialogs\n  {\n    id: 'full-settings-dialog',\n    title: 'Full Settings Dialog',\n    page: 'settings',\n    category: 'Dialogs',\n    keywords: ['settings', 'dialog', 'modal', 'full', 'tabs'],\n  },\n  {\n    id: 'full-agent-dialog',\n    title: 'Full Agent Dialog',\n    page: 'settings',\n    category: 'Dialogs',\n    keywords: ['agent', 'dialog', 'modal', 'configuration'],\n  },\n  // Individual tabs\n  {\n    id: 'individual-tabs',\n    title: 'Individual Tabs',\n    page: 'settings',\n    category: 'Tabs',\n    keywords: [\n      'appearance',\n      'preferences',\n      'status bar',\n      'server',\n      'tools',\n      'channels',\n      'agents',\n      'advanced',\n      'tab',\n      'isolated',\n    ],\n  },\n  // Responsive\n  {\n    id: 'mobile-drill-in',\n    title: 'Mobile Drill-In',\n    page: 'settings',\n    category: 'Responsive',\n    keywords: ['mobile', 'drill', 'drawer', 'responsive', 'narrow'],\n  },\n  // States\n  {\n    id: 'loading-empty-states',\n    title: 'Loading & Empty States',\n    page: 'settings',\n    category: 'States',\n    keywords: ['loading', 'empty', 'skeleton', 'placeholder', 'no data'],\n  },\n  // Primitives\n  {\n    id: 'settings-primitives',\n    title: 'Settings Primitives',\n    page: 'settings',\n    category: 'Primitives',\n    keywords: ['fieldcard', 'settingrow', 'switch', 'row', 'card', 'wrapper'],\n  },\n];\n```\n\n## Section count rationale\n\nSix entries — one per `<PlaygroundSection>` block. This matches every other playground section file (one entry per `PlaygroundSection`, NOT per `ShowcaseLabel`). Individual tab discoverability comes from the `keywords` array on the `individual-tabs` entry, which lists all eight tab names so a search for 'appearance' or 'channels' lands on that section.\n\n## Acceptance criteria\n\n- [ ] File exists at `apps/client/src/dev/sections/settings-sections.ts`\n- [ ] Exports `SETTINGS_SECTIONS: PlaygroundSection[]` with exactly six entries\n- [ ] Every `id` is unique (will be cross-checked by `playground-registry.test.ts`)\n- [ ] Every `id` contains no spaces (matches anchor generation pattern enforced by tests)\n- [ ] Categories follow the spec: `Dialogs`, `Tabs`, `Responsive`, `States`, `Primitives`\n- [ ] `page: 'settings'` on every entry — TypeScript will fail until task 2.2 adds `'settings'` to the `Page` union, so plan to land 2.1 + 2.2 together or land 2.2 first\n\n## Out of scope\n\n- Creating the showcase components (Phase 3)\n- Modifying `playground-registry.ts` or `playground-config.ts` (those are tasks 2.2 and 2.3)",
      "activeForm": "Creating settings sections registry entries",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Foundation — Registry Wiring",
      "subject": "[settings-dialog-04-playground] [P2] Wire SETTINGS_SECTIONS into playground-registry.ts",
      "description": "Add the new `'settings'` page to the central playground registry so search, navigation, and TOC discovery all see it.\n\n## File to modify\n\n`apps/client/src/dev/playground-registry.ts`\n\n## Current shape (verified)\n\nThe file currently defines a `Page` union of fourteen strings, re-exports each `*_SECTIONS` array, and concatenates them all into `PLAYGROUND_REGISTRY`. Adding a new page is a three-line change in three places.\n\n## Changes\n\n### Step 1 — Add `'settings'` to the `Page` union\n\nFind:\n```ts\nexport type Page =\n  | 'overview'\n  | 'tokens'\n  | 'forms'\n  | 'components'\n  | 'chat'\n  | 'features'\n  | 'promos'\n  | 'command-palette'\n  | 'simulator'\n  | 'topology'\n  | 'filter-bar'\n  | 'error-states'\n  | 'onboarding'\n  | 'tables';\n```\n\nReplace with the same union plus `| 'settings'` at the end.\n\n### Step 2 — Re-export `SETTINGS_SECTIONS`\n\nAdd next to the existing `export { ... } from './sections/...'` block:\n```ts\nexport { SETTINGS_SECTIONS } from './sections/settings-sections';\n```\n\n### Step 3 — Import under alias and append to `PLAYGROUND_REGISTRY`\n\nAdd to the alias-import block (alphabetical order is not enforced — match the existing pattern of grouping by file order):\n```ts\nimport { SETTINGS_SECTIONS as settings } from './sections/settings-sections';\n```\n\nAppend `...settings` to the `PLAYGROUND_REGISTRY` array literal.\n\n## Why aliased imports?\n\nThe file uses local aliases (`as tokens`, `as forms`, etc.) when importing for the registry array, while still re-exporting the canonical names from the same module paths. This avoids circular re-export issues and keeps the registry composition tidy. Follow that pattern.\n\n## Acceptance criteria\n\n- [ ] `Page` union includes `'settings'`\n- [ ] `SETTINGS_SECTIONS` is re-exported from the file\n- [ ] `PLAYGROUND_REGISTRY` includes `...settings`\n- [ ] `pnpm typecheck` passes\n- [ ] `pnpm vitest run apps/client/src/dev/__tests__/playground-registry.test.ts` passes (no duplicate IDs, no spaces in IDs)\n\n## Dependency\n\nDepends on task 2.1 (the imported file must exist).",
      "activeForm": "Wiring SETTINGS_SECTIONS into playground-registry",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": []
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Foundation — Registry Wiring",
      "subject": "[settings-dialog-04-playground] [P2] Add Settings PageConfig to playground-config.ts",
      "description": "Add the `Settings` page entry to `PAGE_CONFIGS` so it appears in the sidebar nav, the overview page, the URL parser (`getPageFromPath`), and the page label lookup.\n\n## File to modify\n\n`apps/client/src/dev/playground-config.ts`\n\n## Pattern reference\n\nThis file is the single source of truth for playground pages — every entry in `PAGE_CONFIGS` automatically appears in:\n- The appropriate sidebar group (`design-system` / `session` / `agents` / `app-shell`)\n- The overview page card grid\n- The URL parser via `getPageFromPath`\n- `PAGE_LABELS` and `PAGE_ORDER` derived lookups\n\n## Changes\n\n### Step 1 — Import the icon and the sections array\n\nThe existing icon import block is at the top of the file. Add:\n```ts\nimport {\n  // ... existing icons\n  Settings as SettingsIcon, // alias to avoid clashing with the lucide `Settings2` import already present\n} from 'lucide-react';\n```\n\nAdd to the existing `from './playground-registry'` import:\n```ts\nimport {\n  // ... existing\n  SETTINGS_SECTIONS,\n} from './playground-registry';\n```\n\n### Step 2 — Add a new entry to `PAGE_CONFIGS`\n\nAppend (or insert into the `app-shell` group section) the following entry. Place it at the END of the array — order matches sidebar render order within each group, and putting it last in `app-shell` keeps the diff small.\n\n```ts\n{\n  id: 'settings',\n  label: 'Settings',\n  description:\n    'Settings dialogs, individual tabs, mobile drill-in, loading and empty states, and the underlying primitives.',\n  icon: SettingsIcon,\n  group: 'app-shell',\n  sections: SETTINGS_SECTIONS,\n  path: 'settings',\n},\n```\n\n## Why `Settings as SettingsIcon`?\n\nThe lucide-react `Settings` symbol could collide with names elsewhere in the file (or in showcases that import the same icon under its bare name). Aliasing avoids any future ambiguity. The spec calls this out explicitly.\n\n## Acceptance criteria\n\n- [ ] `playground-config.ts` imports `SETTINGS_SECTIONS` from `./playground-registry`\n- [ ] `playground-config.ts` imports `Settings as SettingsIcon` from `lucide-react`\n- [ ] `PAGE_CONFIGS` contains a new entry with `id: 'settings'`, `group: 'app-shell'`, `path: 'settings'`\n- [ ] `pnpm typecheck` passes\n- [ ] After Phase 3 lands, navigating to `/dev/settings` resolves via `getPageFromPath`\n\n## Dependency\n\nDepends on task 2.2 (the import path must export `SETTINGS_SECTIONS`).",
      "activeForm": "Adding Settings PageConfig",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.2"],
      "parallelWith": []
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "Foundation — Registry Wiring",
      "subject": "[settings-dialog-04-playground] [P2] Create empty SettingsPage and wire into DevPlayground",
      "description": "Create the page-level component and wire it into the `PAGE_COMPONENTS` lookup so the new `/dev/settings` route renders without a 'Page not found' state. The page starts as an empty `PlaygroundPageLayout` that Phase 3 fills with showcases.\n\n## Files\n\n### Create — `apps/client/src/dev/pages/SettingsPage.tsx` (~20 lines)\n\nFollow `apps/client/src/dev/pages/ComponentsPage.tsx` exactly:\n\n```tsx\nimport { PlaygroundPageLayout } from '../PlaygroundPageLayout';\nimport { SETTINGS_SECTIONS } from '../playground-registry';\n\n/** Settings dialog system gallery for the dev playground. */\nexport function SettingsPage() {\n  return (\n    <PlaygroundPageLayout\n      title=\"Settings\"\n      description=\"Settings dialogs, individual tabs, loading and empty states, and primitives.\"\n      sections={SETTINGS_SECTIONS}\n    >\n      {/* Showcases added in Phase 3 */}\n    </PlaygroundPageLayout>\n  );\n}\n```\n\nNote: do NOT import `SettingsShowcases` yet — it does not exist. Phase 3 task 3.1 imports it.\n\n### Modify — `apps/client/src/dev/DevPlayground.tsx`\n\nThe file currently has a `PAGE_COMPONENTS` lookup that maps page IDs to React components. Add two lines.\n\n#### Step 1 — Import\n\nAdd to the existing block of `import { ... } from './pages/...'` lines:\n```ts\nimport { SettingsPage } from './pages/SettingsPage';\n```\n\n#### Step 2 — Register in `PAGE_COMPONENTS`\n\nFind:\n```ts\nconst PAGE_COMPONENTS: Record<string, React.ComponentType<PlaygroundPageProps>> = {\n  // ... existing entries\n  tables: TablesPage,\n};\n```\n\nAdd:\n```ts\nconst PAGE_COMPONENTS: Record<string, React.ComponentType<PlaygroundPageProps>> = {\n  // ... existing entries\n  tables: TablesPage,\n  settings: SettingsPage,\n};\n```\n\n## Acceptance criteria\n\n- [ ] `apps/client/src/dev/pages/SettingsPage.tsx` exists and exports a default-named `SettingsPage` function\n- [ ] `DevPlayground.tsx` imports `SettingsPage` and registers it under the `'settings'` key in `PAGE_COMPONENTS`\n- [ ] Visiting `/dev/settings` in the browser shows the page header (\"Settings\") with an empty body and the (currently empty) TOC sidebar — no console errors\n- [ ] `pnpm typecheck` passes\n- [ ] `pnpm vitest run apps/client/src/dev/__tests__` passes — none of the existing tests should fail (they're data-driven from the registry)\n- [ ] Sidebar shows a new \"Settings\" item in the \"App Shell\" group with the lucide `Settings` icon\n\n## Dependencies\n\nDepends on tasks 2.1, 2.2, 2.3 (all the registry plumbing must exist for the page to render correctly).",
      "activeForm": "Creating SettingsPage and wiring into DevPlayground",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1", "2.2", "2.3"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Showcases — Implementation",
      "subject": "[settings-dialog-04-playground] [P3] Create SettingsShowcases.tsx with all six sections",
      "description": "Create the main showcase file containing all six `<PlaygroundSection>` blocks. This is the largest task in the spec (~250 lines). After this lands the Settings playground page is complete.\n\n## File to create\n\n`apps/client/src/dev/showcases/SettingsShowcases.tsx`\n\n## CRITICAL: Tab signature drift\n\nThe spec was written assuming `ServerTab(config, isLoading)` and `AdvancedTab(onResetComplete, onRestartComplete)` props. **Verification of the current code shows that all eight tabs are already parameterless:**\n\n- `ServerTab()` — `apps/client/src/layers/features/settings/ui/ServerTab.tsx:7`\n- `ToolsTab()` — `.../ToolsTab.tsx:45`\n- `ChannelsTab()` — `.../ChannelsTab.tsx:23`\n- `AgentsTab()` — `.../AgentsTab.tsx:26`\n- `AdvancedTab()` — `.../AdvancedTab.tsx:25`\n- `AppearanceTab()`, `PreferencesTab()`, `StatusBarTab()` — also parameterless\n\nThis means the post-`settings-dialog-02-tabbed-primitive` migration has already happened. Render every tab without props. The Loading & Empty States section therefore CANNOT use prop-driven loading variants — it must rely on `MockedQueryProvider` configuration (or the absence of it) to control which branch the data-fetching tabs render.\n\n## Imports\n\n```tsx\nimport { useState } from 'react';\nimport { QueryClient, QueryClientProvider } from '@tanstack/react-query';\nimport { Palette, Settings2, Server } from 'lucide-react';\nimport { PlaygroundSection } from '../PlaygroundSection';\nimport { ShowcaseDemo } from '../ShowcaseDemo';\nimport { ShowcaseLabel } from '../ShowcaseLabel';\nimport {\n  Button,\n  FieldCard,\n  FieldCardContent,\n  SettingRow,\n  Switch,\n  NavigationLayout,\n  NavigationLayoutBody,\n  NavigationLayoutSidebar,\n  NavigationLayoutItem,\n  NavigationLayoutContent,\n  NavigationLayoutPanel,\n  NavigationLayoutPanelHeader,\n  NavigationLayoutDialogHeader,\n  ResponsiveDialogTitle,\n} from '@/layers/shared/ui';\nimport { SettingsDialog } from '@/layers/features/settings';\nimport { AgentDialog } from '@/layers/features/agent-settings';\nimport { AppearanceTab } from '@/layers/features/settings/ui/tabs/AppearanceTab';\nimport { PreferencesTab } from '@/layers/features/settings/ui/tabs/PreferencesTab';\nimport { StatusBarTab } from '@/layers/features/settings/ui/tabs/StatusBarTab';\nimport { ServerTab } from '@/layers/features/settings/ui/ServerTab';\nimport { ToolsTab } from '@/layers/features/settings/ui/ToolsTab';\nimport { ChannelsTab } from '@/layers/features/settings/ui/ChannelsTab';\nimport { AgentsTab } from '@/layers/features/settings/ui/AgentsTab';\nimport { AdvancedTab } from '@/layers/features/settings/ui/AdvancedTab';\nimport { MOCK_SERVER_CONFIG, MOCK_MESH_AGENTS } from './settings-mock-data';\n```\n\n> **FSD note:** `dev/` lives outside `layers/` and is allowed to reach into any feature module per ADR-0002 + ADR-0008. The barrel imports from `@/layers/features/settings` and `@/layers/features/agent-settings` are correct, but those barrels do not re-export the individual tabs. Importing tabs from internal paths (`@/layers/features/settings/ui/tabs/AppearanceTab`) is necessary and matches how `NavigationShowcases.tsx` imports `NavigationLayout*` primitives. If the FSD lint rule complains about deep imports for these specific tabs, add a per-line `// eslint-disable-next-line` with a comment explaining the playground exception.\n\n## Top-level export\n\n```tsx\n/** Comprehensive showcase for the Settings dialog system. */\nexport function SettingsShowcases() {\n  return (\n    <>\n      <FullSettingsDialogSection />\n      <FullAgentDialogSection />\n      <IndividualTabsSection />\n      <MobileDrillInSection />\n      <LoadingEmptyStatesSection />\n      <PrimitivesSection />\n    </>\n  );\n}\n```\n\n## Helpers (defined below the export)\n\n### `MockedQueryProvider`\n\n```tsx\n/** Wraps children in a fresh QueryClient prepopulated with mock query data. */\nfunction MockedQueryProvider({ children }: { children: React.ReactNode }) {\n  const [client] = useState(() => {\n    const c = new QueryClient({\n      defaultOptions: { queries: { retry: false, refetchOnWindowFocus: false } },\n    });\n    c.setQueryData(['config'], MOCK_SERVER_CONFIG);\n    c.setQueryData(['mesh', 'agents'], MOCK_MESH_AGENTS);\n    return c;\n  });\n  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;\n}\n```\n\n> **Query key alignment:** the actual query keys used by the settings tabs may differ from `['config']` and `['mesh', 'agents']`. Before finalizing, grep the settings feature for `useQuery` calls and align the keys exactly. If a tab uses `useServerConfig` or `useMeshAgents` hooks, find the underlying `queryKey` and copy it. Mismatched keys mean the prepopulated data is invisible to the hooks and the tabs render their loading/empty branches anyway — that's a silent failure mode.\n\n### `TabShell`\n\n```tsx\n/** Bare NavigationLayout shell with one panel for showcasing a single tab. */\nfunction TabShell({ value, children }: { value: string; children: React.ReactNode }) {\n  return (\n    <div className=\"border-border overflow-hidden rounded-lg border\">\n      <NavigationLayout value={value} onValueChange={() => {}}>\n        <NavigationLayoutBody>\n          <NavigationLayoutContent className=\"p-4\">\n            <NavigationLayoutPanel value={value}>\n              <div className=\"space-y-4\">{children}</div>\n            </NavigationLayoutPanel>\n          </NavigationLayoutContent>\n        </NavigationLayoutBody>\n      </NavigationLayout>\n    </div>\n  );\n}\n```\n\n## Section components\n\n### Section 1 — Full Settings Dialog\n\n```tsx\nfunction FullSettingsDialogSection() {\n  const [open, setOpen] = useState(false);\n  return (\n    <PlaygroundSection\n      title=\"Full Settings Dialog\"\n      description=\"The complete Settings dialog with all tabs. Server/Tools/Channels tabs render in their empty state because the playground transport returns null for all queries — see Loading & Empty States for richer demos.\"\n    >\n      <ShowcaseDemo responsive>\n        <Button onClick={() => setOpen(true)}>Open Settings</Button>\n        <SettingsDialog open={open} onOpenChange={setOpen} />\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n### Section 2 — Full Agent Dialog\n\n```tsx\nfunction FullAgentDialogSection() {\n  const [open, setOpen] = useState(false);\n  return (\n    <PlaygroundSection\n      title=\"Full Agent Dialog\"\n      description=\"The Agent configuration dialog with Identity, Personality, Tools, and Channels tabs. Uses a static mock agent manifest.\"\n    >\n      <ShowcaseDemo responsive>\n        <Button onClick={() => setOpen(true)}>Open Agent Dialog</Button>\n        <MockedQueryProvider>\n          <AgentDialog\n            projectPath=\"/Users/dev/example-project\"\n            open={open}\n            onOpenChange={setOpen}\n          />\n        </MockedQueryProvider>\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n### Section 3 — Individual Tabs\n\nWrap data-driven tabs (`ServerTab`, `ToolsTab`, `ChannelsTab`, `AgentsTab`) in `MockedQueryProvider`. Wrap Zustand-only tabs (`AppearanceTab`, `PreferencesTab`, `StatusBarTab`) and the side-effect-only `AdvancedTab` in `TabShell` only.\n\n```tsx\nfunction IndividualTabsSection() {\n  return (\n    <PlaygroundSection\n      title=\"Individual Tabs\"\n      description=\"Each settings tab rendered in isolation inside a bare NavigationLayout shell. Useful for iterating on a single tab without opening the full dialog.\"\n    >\n      <ShowcaseLabel>Appearance Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"appearance\"><AppearanceTab /></TabShell>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Preferences Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"preferences\"><PreferencesTab /></TabShell>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Status Bar Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"statusBar\"><StatusBarTab /></TabShell>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Server Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <MockedQueryProvider>\n          <TabShell value=\"server\"><ServerTab /></TabShell>\n        </MockedQueryProvider>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Tools Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <MockedQueryProvider>\n          <TabShell value=\"tools\"><ToolsTab /></TabShell>\n        </MockedQueryProvider>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Channels Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <MockedQueryProvider>\n          <TabShell value=\"channels\"><ChannelsTab /></TabShell>\n        </MockedQueryProvider>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Agents Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <MockedQueryProvider>\n          <TabShell value=\"agents\"><AgentsTab /></TabShell>\n        </MockedQueryProvider>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Advanced Tab</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"advanced\"><AdvancedTab /></TabShell>\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n### Section 4 — Mobile Drill-In\n\n```tsx\nfunction MobileDrillInSection() {\n  const [active, setActive] = useState('preferences');\n  return (\n    <PlaygroundSection\n      title=\"Mobile Drill-In\"\n      description=\"At narrow viewports the sidebar collapses to a list view, and tapping an item drills into the panel with a back button. Use the viewport toggle to see the responsive behavior.\"\n    >\n      <ShowcaseDemo responsive>\n        <div className=\"border-border overflow-hidden rounded-lg border\" style={{ height: 480 }}>\n          <NavigationLayout value={active} onValueChange={setActive}>\n            <NavigationLayoutDialogHeader>\n              <ResponsiveDialogTitle className=\"text-sm font-medium\">\n                Settings\n              </ResponsiveDialogTitle>\n            </NavigationLayoutDialogHeader>\n            <NavigationLayoutBody>\n              <NavigationLayoutSidebar>\n                <NavigationLayoutItem value=\"appearance\" icon={Palette}>Appearance</NavigationLayoutItem>\n                <NavigationLayoutItem value=\"preferences\" icon={Settings2}>Preferences</NavigationLayoutItem>\n                <NavigationLayoutItem value=\"server\" icon={Server}>Server</NavigationLayoutItem>\n              </NavigationLayoutSidebar>\n              <NavigationLayoutContent className=\"p-4\">\n                <NavigationLayoutPanel value=\"appearance\">\n                  <div className=\"space-y-4\">\n                    <NavigationLayoutPanelHeader>Appearance</NavigationLayoutPanelHeader>\n                    <AppearanceTab />\n                  </div>\n                </NavigationLayoutPanel>\n                <NavigationLayoutPanel value=\"preferences\">\n                  <div className=\"space-y-4\">\n                    <NavigationLayoutPanelHeader>Preferences</NavigationLayoutPanelHeader>\n                    <PreferencesTab />\n                  </div>\n                </NavigationLayoutPanel>\n                <NavigationLayoutPanel value=\"server\">\n                  <div className=\"space-y-4\">\n                    <NavigationLayoutPanelHeader>Server</NavigationLayoutPanelHeader>\n                    <MockedQueryProvider><ServerTab /></MockedQueryProvider>\n                  </div>\n                </NavigationLayoutPanel>\n              </NavigationLayoutContent>\n            </NavigationLayoutBody>\n          </NavigationLayout>\n        </div>\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n### Section 5 — Loading & Empty States\n\nBecause every tab is now parameterless, `loading vs empty vs loaded` is purely a function of what the surrounding `QueryClient` knows. Render each tab WITHOUT `MockedQueryProvider` so it falls through to the playground's null transport and shows its empty / not-loaded branch.\n\n```tsx\nfunction LoadingEmptyStatesSection() {\n  return (\n    <PlaygroundSection\n      title=\"Loading & Empty States\"\n      description=\"Skeleton and empty-state renderings for data-driven tabs. The playground transport returns null for all queries, so unwrapped tabs render their no-data branches.\"\n    >\n      <ShowcaseLabel>Server Tab — Empty (no config)</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"server\"><ServerTab /></TabShell>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Channels Tab — Empty Catalog</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"channels\"><ChannelsTab /></TabShell>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>Agents Tab — No Default Agent</ShowcaseLabel>\n      <ShowcaseDemo>\n        <TabShell value=\"agents\"><AgentsTab /></TabShell>\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n> **Skeleton variant note:** The spec originally called for an explicit loading-state showcase via `<ServerTab isLoading />`. With parameterless tabs that's no longer possible. If a true loading-state demo is needed it would require either (a) intercepting the query with a never-resolving promise via `MockedQueryProvider` or (b) deferring to a follow-up spec. For now, the empty branch is the visible state and that matches what users see in the actual app when their server has no config.\n\n### Section 6 — Settings Primitives\n\n```tsx\nfunction PrimitivesSection() {\n  const [toggleA, setToggleA] = useState(true);\n  const [toggleB, setToggleB] = useState(false);\n  return (\n    <PlaygroundSection\n      title=\"Settings Primitives\"\n      description=\"Building blocks used by every settings tab — FieldCard wraps groups of rows, SettingRow is the horizontal label/description/control row.\"\n    >\n      <ShowcaseLabel>FieldCard with SettingRows</ShowcaseLabel>\n      <ShowcaseDemo>\n        <FieldCard>\n          <FieldCardContent>\n            <SettingRow label=\"Show timestamps\" description=\"Display message timestamps in chat\">\n              <Switch checked={toggleA} onCheckedChange={setToggleA} />\n            </SettingRow>\n            <SettingRow label=\"Auto-hide tool calls\" description=\"Fade out completed tool calls\">\n              <Switch checked={toggleB} onCheckedChange={setToggleB} />\n            </SettingRow>\n          </FieldCardContent>\n        </FieldCard>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>FieldCard — single row</ShowcaseLabel>\n      <ShowcaseDemo>\n        <FieldCard>\n          <FieldCardContent>\n            <SettingRow label=\"Theme\" description=\"Choose your preferred color scheme\">\n              <Button variant=\"outline\" size=\"sm\">System</Button>\n            </SettingRow>\n          </FieldCardContent>\n        </FieldCard>\n      </ShowcaseDemo>\n\n      <ShowcaseLabel>SettingRow — disabled</ShowcaseLabel>\n      <ShowcaseDemo>\n        <SettingRow label=\"Background sync\" description=\"Requires premium plan\">\n          <Switch checked={false} disabled />\n        </SettingRow>\n      </ShowcaseDemo>\n    </PlaygroundSection>\n  );\n}\n```\n\n## TSDoc requirements\n\nPer `.claude/rules/documentation.md`, the exported `SettingsShowcases` function needs TSDoc. The internal section helpers and `MockedQueryProvider`/`TabShell` are not exports but a TSDoc one-liner on each is friendly to readers.\n\n## File size\n\nEstimated ~280 lines. The file-size threshold (`.claude/rules/file-size.md`) is 300 lines as ideal, 500 as must-split. If the file ends up over 300 lines, consider extracting `MockedQueryProvider` and `TabShell` into a sibling file `settings-showcase-helpers.tsx` — but only if 300 is exceeded; below that it's not worth the indirection.\n\n## Acceptance criteria\n\n- [ ] File exists at `apps/client/src/dev/showcases/SettingsShowcases.tsx`\n- [ ] Exports a single `SettingsShowcases` function rendering six `<PlaygroundSection>` blocks in the order specified\n- [ ] Each `<PlaygroundSection>`'s `id` matches an entry in `SETTINGS_SECTIONS` exactly: `full-settings-dialog`, `full-agent-dialog`, `individual-tabs`, `mobile-drill-in`, `loading-empty-states`, `settings-primitives`\n- [ ] No tabs are passed props (verified parameterless)\n- [ ] `MockedQueryProvider` keys align with the actual `queryKey` arrays used by the settings tabs (grep before finalizing)\n- [ ] `pnpm typecheck` passes\n- [ ] `pnpm lint` passes\n- [ ] `pnpm vitest run apps/client/src/dev/__tests__` still passes\n- [ ] `SettingsPage.tsx` is updated to render `<SettingsShowcases />` as its child\n\n## Dependencies\n\nDepends on tasks 1.1 (mock data), 2.4 (page exists). Should land as one PR with both 1.1 and the registry tasks.",
      "activeForm": "Creating SettingsShowcases with all six sections",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "2.4"],
      "parallelWith": []
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Showcases — Implementation",
      "subject": "[settings-dialog-04-playground] [P3] Wire SettingsShowcases into SettingsPage",
      "description": "Replace the empty `PlaygroundPageLayout` body in `SettingsPage.tsx` with `<SettingsShowcases />` so the Settings playground page actually renders content.\n\n## File to modify\n\n`apps/client/src/dev/pages/SettingsPage.tsx`\n\n## Change\n\nReplace the file body with:\n\n```tsx\nimport { PlaygroundPageLayout } from '../PlaygroundPageLayout';\nimport { SETTINGS_SECTIONS } from '../playground-registry';\nimport { SettingsShowcases } from '../showcases/SettingsShowcases';\n\n/** Settings dialog system gallery for the dev playground. */\nexport function SettingsPage() {\n  return (\n    <PlaygroundPageLayout\n      title=\"Settings\"\n      description=\"Settings dialogs, individual tabs, loading and empty states, and primitives.\"\n      sections={SETTINGS_SECTIONS}\n    >\n      <SettingsShowcases />\n    </PlaygroundPageLayout>\n  );\n}\n```\n\n## Acceptance criteria\n\n- [ ] `SettingsPage.tsx` imports `SettingsShowcases` from `../showcases/SettingsShowcases`\n- [ ] `<SettingsShowcases />` is rendered as the only child of `<PlaygroundPageLayout>`\n- [ ] Visiting `/dev/settings` shows all six sections rendered in order\n- [ ] TOC sidebar lists all six sections under their categories\n- [ ] `pnpm typecheck` passes\n\n## Dependencies\n\nDepends on task 3.1 (the showcase file must exist). This is a trivial follow-up but kept as a separate task because it represents the moment the page goes from empty to live.",
      "activeForm": "Wiring SettingsShowcases into SettingsPage",
      "size": "small",
      "priority": "high",
      "dependencies": ["3.1"],
      "parallelWith": []
    },
    {
      "id": "4.1",
      "phase": 4,
      "phaseName": "Verification",
      "subject": "[settings-dialog-04-playground] [P4] Run automated checks (typecheck, lint, tests)",
      "description": "Run the full automated verification gauntlet from the spec section 8.3 and resolve any failures.\n\n## Commands to run (in order)\n\n```bash\npnpm typecheck                                                # All types resolve\npnpm test -- --run                                            # No existing tests break\npnpm vitest run apps/client/src/dev/__tests__                 # Playground tests specifically\npnpm lint                                                     # No new lint violations\n```\n\n## What to check\n\n### `pnpm typecheck`\n- `MOCK_SERVER_CONFIG` matches the live `ServerConfig` schema (any new required fields added in the codebase since this spec was written must be filled in)\n- `MOCK_AGENT_MANIFEST` matches the live `AgentManifest` schema\n- `Page` union includes `'settings'` everywhere it's referenced\n- No dangling `any` casts\n\n### `pnpm test -- --run`\n- `apps/client/src/dev/__tests__/playground-registry.test.ts` — checks no duplicate IDs across the full registry, no spaces in IDs, every page has at least one section. The new six entries should slot in cleanly.\n- `apps/client/src/dev/__tests__/PlaygroundSearch.test.tsx` — fuzzy search test; should still pass (it's data-driven from `PLAYGROUND_REGISTRY`)\n- `apps/client/src/dev/__tests__/TocSidebar.test.tsx` — TOC sidebar render test; should still pass\n- `apps/client/src/dev/__tests__/OverviewPage.test.tsx` — overview cards test; should still pass\n- All other existing tests untouched\n\n### `pnpm lint`\n- Pay special attention to FSD layer rules — `dev/` is allowed to import from `layers/features/`, but if eslint complains about deep imports like `@/layers/features/settings/ui/tabs/AppearanceTab` (instead of from the `settings` barrel), the answer is to add a per-line `eslint-disable-next-line` with a comment explaining why deep imports are necessary for the playground (the barrel does not re-export individual tabs, and exposing them just for the playground would pollute the public API).\n- TSDoc lint rule (`eslint-plugin-jsdoc`) — every export needs a doc comment. This applies to `SettingsShowcases`, `SettingsPage`, and the three exports in `settings-mock-data.ts`.\n\n## Resolving common issues\n\n| Failure | Likely cause | Fix |\n| --- | --- | --- |\n| `Property 'X' is missing in type { ... } but required in type 'ServerConfig'` | Mock data drift | Add the field with a sensible default; do not cast |\n| `Type '\"settings\"' is not assignable to type 'Page'` | Forgot to add `'settings'` to the union | Update `playground-registry.ts` |\n| `No overload matches this call` on `<ServerTab config=... />` | Spec used outdated tab signatures | Remove the props — all tabs are parameterless now |\n| `Cannot find module '@/layers/features/settings/ui/tabs/AppearanceTab'` | Path typo or missing file | Verify the path with `ls` |\n| Duplicate section ID in `playground-registry.test.ts` | Reused an ID from another section file | Rename to a unique kebab-case slug |\n\n## Acceptance criteria\n\n- [ ] All four commands exit with code 0\n- [ ] No new test failures introduced\n- [ ] No new lint violations introduced\n- [ ] No `any` casts added to bypass type errors\n\n## Dependencies\n\nDepends on task 3.2 (the page must be live for end-to-end verification).",
      "activeForm": "Running automated verification checks",
      "size": "small",
      "priority": "high",
      "dependencies": ["3.2"],
      "parallelWith": []
    },
    {
      "id": "4.2",
      "phase": 4,
      "phaseName": "Verification",
      "subject": "[settings-dialog-04-playground] [P4] Manual smoke checklist for /dev/settings",
      "description": "Execute the manual smoke checklist from spec section 8.4 in a real browser. The playground has no per-page render tests, so manual verification is the only safety net.\n\n## Setup\n\n```bash\npnpm dev\n# Wait for Vite to log the dev URL (something like http://localhost:6241)\n# Open the browser and navigate to http://localhost:6241/dev/settings\n```\n\n## Browser verification\n\nUse the browser-verify flow if running headlessly: open the page, screenshot, check console errors, verify content. Otherwise drive a manual browser session.\n\n## Smoke checklist\n\nWalk through every item and tick it off:\n\n- [ ] Sidebar shows a new \"Settings\" item in the \"App Shell\" group with the lucide `Settings` icon\n- [ ] Clicking it navigates to `/dev/settings` (URL updates, no full page reload)\n- [ ] Page header renders with \"Settings\" title and the description text\n- [ ] TOC sidebar lists all 6 sections grouped by category (`Dialogs`, `Tabs`, `Responsive`, `States`, `Primitives`)\n- [ ] **Section 1 — Full Settings Dialog**\n  - [ ] Clicking \"Open Settings\" opens the full dialog\n  - [ ] All eight tabs render in the dialog sidebar\n  - [ ] Switching between tabs works without errors\n  - [ ] Server / Tools / Channels tabs render in their empty state (expected — playground transport returns null)\n  - [ ] Closing the dialog works (Escape key + clicking outside)\n- [ ] **Section 2 — Full Agent Dialog**\n  - [ ] Clicking \"Open Agent Dialog\" opens the agent dialog\n  - [ ] The mock manifest renders (Identity tab shows mock data)\n  - [ ] All four agent tabs (Identity, Personality, Tools, Channels) render without errors\n- [ ] **Section 3 — Individual Tabs**\n  - [ ] Each of the eight tab shells renders\n  - [ ] AppearanceTab toggles work (Zustand state updates persist within the page)\n  - [ ] PreferencesTab toggles work\n  - [ ] StatusBarTab toggles work\n  - [ ] ServerTab shows mock config values (version 0.30.0, port 4242, etc.)\n  - [ ] ToolsTab renders inside `MockedQueryProvider`\n  - [ ] ChannelsTab renders inside `MockedQueryProvider`\n  - [ ] AgentsTab renders inside `MockedQueryProvider`\n  - [ ] AdvancedTab renders without crashing\n- [ ] **Section 4 — Mobile Drill-In**\n  - [ ] Clicking the responsive viewport toggle (mobile preset) shows the drill-in sidebar list\n  - [ ] Tapping an item drills into the panel with a back button\n  - [ ] Pressing back returns to the list\n- [ ] **Section 5 — Loading & Empty States**\n  - [ ] ServerTab empty variant renders without crash\n  - [ ] ChannelsTab empty state renders the \"no channels\" placeholder\n  - [ ] AgentsTab \"no default\" state renders\n- [ ] **Section 6 — Settings Primitives**\n  - [ ] FieldCard with two SettingRows renders, switches toggle independently\n  - [ ] FieldCard single row renders with a button\n  - [ ] Disabled SettingRow renders with disabled switch\n- [ ] `⌘K` / `Ctrl+K` opens the search palette\n- [ ] Typing \"appearance\" finds the \"Individual Tabs\" entry (matched via keyword)\n- [ ] Typing \"settings dialog\" finds the \"Full Settings Dialog\" entry\n- [ ] Selecting a search result navigates to `/dev/settings#<id>` and scrolls\n- [ ] No console errors during any interaction\n- [ ] No console warnings about missing keys, deprecated APIs, or duplicate IDs\n\n## What to do if something fails\n\n- Console errors → fix in the showcase or mock data, re-verify\n- Tab crashes → check whether the tab depends on a query key not covered by `MockedQueryProvider`; either add the key or document it as a known empty state\n- Visual regressions → screenshot, compare against the in-app version of the same component, file as follow-up if cosmetic\n- Search fails to find a section → verify the `keywords` array in `settings-sections.ts` includes the search term\n\n## Acceptance criteria\n\n- [ ] Every item in the checklist passes\n- [ ] No console errors or warnings during any interaction\n- [ ] At least one screenshot of the live page is captured for the PR description\n\n## Dependencies\n\nDepends on task 4.1 (automated checks must pass first; no point smoke-testing a broken build).",
      "activeForm": "Running manual smoke checklist",
      "size": "medium",
      "priority": "high",
      "dependencies": ["4.1"],
      "parallelWith": []
    },
    {
      "id": "4.3",
      "phase": 4,
      "phaseName": "Verification",
      "subject": "[settings-dialog-04-playground] [P4] Add changelog entry for Settings playground page",
      "description": "Per spec section 11, add a brief note to the next changelog entry under \"Internal\" describing the new playground page.\n\n## File to modify\n\nThe project changelog file. Locate it via:\n```bash\nls CHANGELOG.md docs/CHANGELOG.md apps/site/CHANGELOG.md 2>/dev/null\n```\n\nIf there are multiple, prefer the one most-recently modified or follow the pattern of recent commits like `8209b450 fix(client): address code review findings`.\n\n## Entry text\n\nUnder the next pending release's \"Internal\" or \"Dev experience\" section:\n\n> Dev playground gains a Settings page covering dialogs, tabs, primitives, and responsive states\n\n## Style\n\n- Match the existing changelog entry style exactly (markdown bullets, sentence case, no trailing period if other entries don't have one)\n- Do not bump the version number — that's a release-time concern\n- Do not create a new release section if one is already pending\n\n## If no changelog file exists\n\nDorkOS may not have a top-level CHANGELOG. If `ls` finds nothing, skip this task and note the omission in the PR description so the user can decide whether to add a changelog file as a follow-up. Do NOT create a new CHANGELOG.md without explicit user direction (per `.claude/rules/file-size.md` and the don't-create-files-unless-necessary rule).\n\n## Acceptance criteria\n\n- [ ] Either the changelog has the new entry, or the PR description notes that no changelog file exists\n- [ ] No version bump performed\n\n## Dependencies\n\nDepends on task 4.2 (only document features that work end-to-end).",
      "activeForm": "Adding changelog entry",
      "size": "small",
      "priority": "low",
      "dependencies": ["4.2"],
      "parallelWith": []
    }
  ]
}
