{
  "spec": "specs/agent-tool-context-injection/02-specification.md",
  "slug": "agent-tool-context-injection",
  "generatedAt": "2026-03-04T12:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[agent-tool-context-injection] [P1] Add agentContext section to UserConfigSchema",
      "description": "Add the `agentContext` section to `UserConfigSchema` in `packages/shared/src/config-schema.ts`. This section provides three boolean toggles that control which tool context blocks are injected into agent system prompts.\n\n**File to modify:** `packages/shared/src/config-schema.ts`\n\n**Implementation:**\n\nAfter the existing `onboarding` field (line ~77), add:\n\n```typescript\n  agentContext: z\n    .object({\n      relayTools: z.boolean().default(true),\n      meshTools: z.boolean().default(true),\n      adapterTools: z.boolean().default(true),\n    })\n    .default(() => ({ relayTools: true, meshTools: true, adapterTools: true })),\n```\n\nThis goes inside the `UserConfigSchema = z.object({...})` definition, after the `onboarding` field and before the closing `})`. All toggles default to `true` so context injection is on by default when the underlying feature is available.\n\n**Verification:**\n\n1. Run `pnpm typecheck` to confirm the schema change compiles.\n2. Verify that `USER_CONFIG_DEFAULTS` (parsed from `UserConfigSchema.parse({ version: 1 })`) includes the new `agentContext` section with all three fields defaulting to `true`.\n3. Verify that the existing PATCH `/api/config` handler will accept `{ \"agentContext\": { \"relayTools\": false } }` without code changes (the handler already deep-merges and validates via `UserConfigSchema.safeParse`).\n\n**Acceptance Criteria:**\n- [ ] `agentContext` section exists in `UserConfigSchema` with three boolean fields\n- [ ] All three fields default to `true`\n- [ ] `UserConfig` type (inferred from schema) includes `agentContext` property\n- [ ] `USER_CONFIG_DEFAULTS` includes `agentContext` with all defaults\n- [ ] `pnpm typecheck` passes with no errors",
      "activeForm": "Adding agentContext section to UserConfigSchema",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": []
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[agent-tool-context-injection] [P1] Add static XML context block constants to context-builder",
      "description": "Add three static string constants to `apps/server/src/services/core/context-builder.ts` containing the XML context blocks for relay, mesh, and adapter tools. These are module-level constants computed once at import time.\n\n**File to modify:** `apps/server/src/services/core/context-builder.ts`\n\n**Implementation:**\n\nAdd three constants at module scope (after the existing imports, before `buildSystemPromptAppend`):\n\n```typescript\nconst RELAY_TOOLS_CONTEXT = `<relay_tools>\nDorkOS Relay is a pub/sub message bus for inter-agent communication.\n\nSubject hierarchy:\n  relay.agent.{sessionId}          — address a specific agent session\n  relay.human.console.{clientId}   — reach a human in the DorkOS UI\n  relay.system.console             — system broadcast channel\n  relay.system.pulse.{scheduleId}  — Pulse scheduler events\n\nWorkflows:\n- Register a reply address first: relay_register_endpoint(subject=\"relay.agent.{your-sessionId}\")\n- Message another agent: relay_send(subject=\"relay.agent.{their-sessionId}\", payload={...}, from=\"relay.agent.{your-sessionId}\")\n- Check for replies: relay_inbox(endpoint_subject=\"relay.agent.{your-sessionId}\")\n- See who is listening: relay_list_endpoints()\n\nThe \"from\" field is your own subject. Set \"replyTo\" so the recipient knows where to respond.\n\nError codes: RELAY_DISABLED (feature off), ACCESS_DENIED (subject blocked), INVALID_SUBJECT (malformed), ENDPOINT_NOT_FOUND (inbox miss).\n</relay_tools>`;\n\nconst MESH_TOOLS_CONTEXT = `<mesh_tools>\nDorkOS Mesh is a local agent registry for discovering and communicating with AI agents on this machine.\n\nAgent lifecycle:\n1. mesh_discover(roots=[\"/path\"]) — scan directories for agent candidates (looks for CLAUDE.md, .dork/agent.json)\n2. mesh_register(path, name, runtime, capabilities) — register a candidate as a known agent\n3. mesh_inspect(agentId) — get full manifest, health status, and relay endpoint\n4. mesh_status() — aggregate overview: total, active, stale agent counts\n5. mesh_list(runtime?, capability?) — filter agents by runtime or capability\n6. mesh_deny(path, reason) — exclude a path from future discovery\n7. mesh_unregister(agentId) — remove an agent from the registry\n8. mesh_query_topology(namespace?) — view agent network from a namespace perspective\n\nWorkflows:\n- Find agents: mesh_list() then mesh_inspect(agentId) for details\n- Contact another agent: mesh_inspect(agentId) to get their relay endpoint, then relay_send\n- Register this project: mesh_register(path=cwd, name=\"project-name\", runtime=\"claude-code\")\n\nRuntimes: claude-code | cursor | codex | other\n</mesh_tools>`;\n\nconst ADAPTER_TOOLS_CONTEXT = `<adapter_tools>\nRelay adapters bridge external platforms (Telegram, webhooks) to the agent message bus.\n\nSubject conventions for external messages:\n  relay.human.telegram.{chatId}    — send to / receive from Telegram\n  relay.human.webhook.{webhookId}  — send to / receive from webhooks\n\nAdapter management:\n- relay_list_adapters() — see all adapters and their status (connected, disconnected, error)\n- relay_enable_adapter(id) / relay_disable_adapter(id) — toggle an adapter on/off\n- relay_reload_adapters() — hot-reload config from disk\n\nBindings route adapter messages to agent projects:\n- binding_list() — see current adapter-to-agent bindings\n- binding_create(adapterId, agentId, projectPath) — route an adapter to an agent\n- binding_delete(id) — remove a binding\n\nSession strategies: per-chat (default, one session per conversation), per-user (shared across chats), stateless (new session each message).\n</adapter_tools>`;\n```\n\nAlso add test exports at the bottom of the file (after the existing `_buildAgentBlock` export):\n\n```typescript\n/** @internal Exported for testing only. */\nexport {\n  buildRelayToolsBlock as _buildRelayToolsBlock,\n  buildMeshToolsBlock as _buildMeshToolsBlock,\n  buildAdapterToolsBlock as _buildAdapterToolsBlock,\n  RELAY_TOOLS_CONTEXT as _RELAY_TOOLS_CONTEXT,\n  MESH_TOOLS_CONTEXT as _MESH_TOOLS_CONTEXT,\n  ADAPTER_TOOLS_CONTEXT as _ADAPTER_TOOLS_CONTEXT,\n};\n```\n\nNote: The builder functions (`buildRelayToolsBlock`, etc.) will be implemented in task 1.3. Add placeholder empty functions for now so the export compiles, or implement tasks 1.2 and 1.3 together.\n\n**Acceptance Criteria:**\n- [ ] `RELAY_TOOLS_CONTEXT` constant contains the full XML block with subject hierarchy, workflows, and error codes\n- [ ] `MESH_TOOLS_CONTEXT` constant contains the full XML block with 8-step lifecycle, workflows, and runtimes\n- [ ] `ADAPTER_TOOLS_CONTEXT` constant contains the full XML block with subject conventions, adapter management, and binding routing\n- [ ] All three constants and builder functions are exported with `@internal` annotation\n- [ ] `pnpm typecheck` passes",
      "activeForm": "Adding static XML context block constants to context-builder",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "1.3",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[agent-tool-context-injection] [P1] Add builder functions and wire into buildSystemPromptAppend",
      "description": "Add three synchronous builder functions to `apps/server/src/services/core/context-builder.ts` that gate context block injection on feature flags and config toggles. Wire them into the existing `buildSystemPromptAppend()` function.\n\n**File to modify:** `apps/server/src/services/core/context-builder.ts`\n\n**New imports to add** (at top of file):\n\n```typescript\nimport { isRelayEnabled } from '../relay/relay-state.js';\nimport { configManager } from './config-manager.js';\n```\n\n**Three builder functions** (add after the static constants from task 1.2, before `buildSystemPromptAppend`):\n\n```typescript\n/**\n * Build the `<relay_tools>` context block.\n * Included when Relay is enabled AND the config toggle is on.\n */\nfunction buildRelayToolsBlock(): string {\n  if (!isRelayEnabled()) return '';\n  const config = configManager.get('agentContext');\n  if (config?.relayTools === false) return '';\n  return RELAY_TOOLS_CONTEXT;\n}\n\n/**\n * Build the `<mesh_tools>` context block.\n * Included when Mesh is available AND the config toggle is on.\n * Mesh is always-on per ADR-0062, so no feature flag check.\n */\nfunction buildMeshToolsBlock(): string {\n  const config = configManager.get('agentContext');\n  if (config?.meshTools === false) return '';\n  return MESH_TOOLS_CONTEXT;\n}\n\n/**\n * Build the `<adapter_tools>` context block.\n * Included when Relay is enabled (adapters require Relay) AND the config toggle is on.\n */\nfunction buildAdapterToolsBlock(): string {\n  if (!isRelayEnabled()) return '';\n  const config = configManager.get('agentContext');\n  if (config?.adapterTools === false) return '';\n  return ADAPTER_TOOLS_CONTEXT;\n}\n```\n\nKey design decisions:\n- Config toggles use `=== false` check (not `=== true`) so that `undefined`/missing keys default to included. This ensures backward compatibility when upgrading from a config file that predates the `agentContext` section.\n- Mesh has no feature flag gate because it is always-on per ADR-0062.\n- Adapter tools require relay because the adapter system depends on the relay message bus.\n- All functions are synchronous (static strings + in-memory config reads). No async needed.\n\n**Update `buildSystemPromptAppend()`** to call the three builders and include their output:\n\n```typescript\nexport async function buildSystemPromptAppend(cwd: string): Promise<string> {\n  const [envResult, gitResult, agentResult] = await Promise.allSettled([\n    buildEnvBlock(cwd),\n    buildGitBlock(cwd),\n    buildAgentBlock(cwd),\n  ]);\n\n  // Tool context blocks are synchronous (static strings + config checks)\n  const relayBlock = buildRelayToolsBlock();\n  const meshBlock = buildMeshToolsBlock();\n  const adapterBlock = buildAdapterToolsBlock();\n\n  return [\n    envResult.status === 'fulfilled' ? envResult.value : '',\n    gitResult.status === 'fulfilled' ? gitResult.value : '',\n    agentResult.status === 'fulfilled' ? agentResult.value : '',\n    relayBlock,\n    meshBlock,\n    adapterBlock,\n  ]\n    .filter(Boolean)\n    .join('\\n\\n');\n}\n```\n\nThe tool context builders are called after the async `Promise.allSettled` block because they are synchronous. They are not wrapped in `Promise.allSettled()` to avoid unnecessary promise overhead.\n\n**Acceptance Criteria:**\n- [ ] `buildRelayToolsBlock()` returns the relay XML block when `isRelayEnabled()` is true and `agentContext.relayTools` is not false\n- [ ] `buildRelayToolsBlock()` returns empty string when relay is disabled\n- [ ] `buildRelayToolsBlock()` returns empty string when `agentContext.relayTools` is false\n- [ ] `buildMeshToolsBlock()` returns the mesh XML block by default (no feature flag gate)\n- [ ] `buildMeshToolsBlock()` returns empty string when `agentContext.meshTools` is false\n- [ ] `buildAdapterToolsBlock()` returns the adapter XML block when relay is enabled and toggle is on\n- [ ] `buildAdapterToolsBlock()` returns empty string when relay is disabled\n- [ ] `buildSystemPromptAppend()` includes all three tool context blocks in its output when features are enabled\n- [ ] `buildSystemPromptAppend()` excludes relay and adapter blocks when relay is disabled\n- [ ] Existing `<env>`, `<git_status>`, and `<agent_identity>` blocks are unchanged\n- [ ] `pnpm typecheck` passes",
      "activeForm": "Adding builder functions and wiring into buildSystemPromptAppend",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": []
    },
    {
      "id": "1.4",
      "phase": 1,
      "phaseName": "Foundation",
      "subject": "[agent-tool-context-injection] [P1] Add server unit tests for context builder tool blocks",
      "description": "Add unit tests for the three new builder functions and their integration with `buildSystemPromptAppend()` to the existing test file at `apps/server/src/services/core/__tests__/context-builder.test.ts`.\n\n**File to modify:** `apps/server/src/services/core/__tests__/context-builder.test.ts`\n\n**New mocks to add** (alongside the existing `vi.mock` calls at the top of the file):\n\n```typescript\nvi.mock('../../relay/relay-state.js', () => ({\n  isRelayEnabled: vi.fn(() => true),\n}));\nvi.mock('../config-manager.js', () => ({\n  configManager: {\n    get: vi.fn(() => ({ relayTools: true, meshTools: true, adapterTools: true })),\n  },\n}));\n```\n\n**New imports** (alongside existing imports):\n\n```typescript\nimport { isRelayEnabled } from '../../relay/relay-state.js';\nimport { configManager } from '../config-manager.js';\nimport {\n  _buildRelayToolsBlock,\n  _buildMeshToolsBlock,\n  _buildAdapterToolsBlock,\n} from '../context-builder.js';\n```\n\n**New test suites to add** (after the existing `describe('buildAgentBlock', ...)` block):\n\n```typescript\ndescribe('buildRelayToolsBlock', () => {\n  beforeEach(() => {\n    vi.clearAllMocks();\n    vi.mocked(isRelayEnabled).mockReturnValue(true);\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: true });\n  });\n\n  it('returns relay context when relay enabled and config on', () => {\n    const result = _buildRelayToolsBlock();\n    expect(result).toContain('<relay_tools>');\n    expect(result).toContain('relay.agent.{sessionId}');\n    expect(result).toContain('relay_register_endpoint');\n    expect(result).toContain('relay_send');\n    expect(result).toContain('relay_inbox');\n    expect(result).toContain('</relay_tools>');\n  });\n\n  it('returns empty string when relay disabled', () => {\n    vi.mocked(isRelayEnabled).mockReturnValue(false);\n    expect(_buildRelayToolsBlock()).toBe('');\n  });\n\n  it('returns empty string when config toggle is off', () => {\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: false, meshTools: true, adapterTools: true });\n    expect(_buildRelayToolsBlock()).toBe('');\n  });\n\n  it('returns relay context when config is undefined (default behavior)', () => {\n    vi.mocked(configManager.get).mockReturnValue(undefined);\n    const result = _buildRelayToolsBlock();\n    expect(result).toContain('<relay_tools>');\n  });\n});\n\ndescribe('buildMeshToolsBlock', () => {\n  beforeEach(() => {\n    vi.clearAllMocks();\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: true });\n  });\n\n  it('returns mesh context by default (mesh always-on)', () => {\n    const result = _buildMeshToolsBlock();\n    expect(result).toContain('<mesh_tools>');\n    expect(result).toContain('mesh_discover');\n    expect(result).toContain('mesh_register');\n    expect(result).toContain('mesh_inspect');\n    expect(result).toContain('mesh_status');\n    expect(result).toContain('</mesh_tools>');\n  });\n\n  it('returns empty string when config toggle is off', () => {\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: false, adapterTools: true });\n    expect(_buildMeshToolsBlock()).toBe('');\n  });\n\n  it('returns mesh context when config is undefined (default behavior)', () => {\n    vi.mocked(configManager.get).mockReturnValue(undefined);\n    const result = _buildMeshToolsBlock();\n    expect(result).toContain('<mesh_tools>');\n  });\n\n  it('is not affected by relay feature flag', () => {\n    vi.mocked(isRelayEnabled).mockReturnValue(false);\n    const result = _buildMeshToolsBlock();\n    expect(result).toContain('<mesh_tools>');\n  });\n});\n\ndescribe('buildAdapterToolsBlock', () => {\n  beforeEach(() => {\n    vi.clearAllMocks();\n    vi.mocked(isRelayEnabled).mockReturnValue(true);\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: true });\n  });\n\n  it('returns adapter context when relay enabled and config on', () => {\n    const result = _buildAdapterToolsBlock();\n    expect(result).toContain('<adapter_tools>');\n    expect(result).toContain('binding_create');\n    expect(result).toContain('binding_list');\n    expect(result).toContain('relay.human.telegram');\n    expect(result).toContain('</adapter_tools>');\n  });\n\n  it('returns empty string when relay disabled', () => {\n    vi.mocked(isRelayEnabled).mockReturnValue(false);\n    expect(_buildAdapterToolsBlock()).toBe('');\n  });\n\n  it('returns empty string when config toggle is off', () => {\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: false });\n    expect(_buildAdapterToolsBlock()).toBe('');\n  });\n\n  it('returns adapter context when config is undefined (default behavior)', () => {\n    vi.mocked(configManager.get).mockReturnValue(undefined);\n    const result = _buildAdapterToolsBlock();\n    expect(result).toContain('<adapter_tools>');\n  });\n});\n```\n\n**Integration tests** (add to the existing `describe('buildSystemPromptAppend', ...)` block):\n\n```typescript\n  it('includes tool context blocks in output when features are enabled', async () => {\n    vi.mocked(isRelayEnabled).mockReturnValue(true);\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: true });\n    const result = await buildSystemPromptAppend('/test/dir');\n    expect(result).toContain('<env>');\n    expect(result).toContain('<relay_tools>');\n    expect(result).toContain('<mesh_tools>');\n    expect(result).toContain('<adapter_tools>');\n  });\n\n  it('excludes relay and adapter blocks when relay is disabled', async () => {\n    vi.mocked(isRelayEnabled).mockReturnValue(false);\n    const result = await buildSystemPromptAppend('/test/dir');\n    expect(result).toContain('<env>');\n    expect(result).toContain('<mesh_tools>');\n    expect(result).not.toContain('<relay_tools>');\n    expect(result).not.toContain('<adapter_tools>');\n  });\n\n  it('excludes all tool blocks when all config toggles are off', async () => {\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: false, meshTools: false, adapterTools: false });\n    const result = await buildSystemPromptAppend('/test/dir');\n    expect(result).toContain('<env>');\n    expect(result).not.toContain('<relay_tools>');\n    expect(result).not.toContain('<mesh_tools>');\n    expect(result).not.toContain('<adapter_tools>');\n  });\n```\n\nImportant: The mocks for `relay-state.js` and `config-manager.js` need to be added to the existing `beforeEach` in the `buildSystemPromptAppend` describe block so they reset properly. Set default mock return values in `beforeEach`:\n\n```typescript\n  beforeEach(() => {\n    vi.clearAllMocks();\n    vi.unstubAllEnvs();\n    mockedGetGitStatus.mockResolvedValue(makeGitStatus());\n    mockedReadManifest.mockResolvedValue(null);\n    vi.mocked(isRelayEnabled).mockReturnValue(true);\n    vi.mocked(configManager.get).mockReturnValue({ relayTools: true, meshTools: true, adapterTools: true });\n  });\n```\n\n**Acceptance Criteria:**\n- [ ] `buildRelayToolsBlock` tested: relay enabled + config on returns content, relay disabled returns empty, config off returns empty, config undefined returns content\n- [ ] `buildMeshToolsBlock` tested: default returns content, config off returns empty, config undefined returns content, not affected by relay flag\n- [ ] `buildAdapterToolsBlock` tested: relay enabled + config on returns content, relay disabled returns empty, config off returns empty, config undefined returns content\n- [ ] Integration: `buildSystemPromptAppend` includes tool blocks when enabled, excludes when disabled\n- [ ] All existing tests continue to pass (no regressions from new mocks)\n- [ ] `pnpm test -- --run` passes",
      "activeForm": "Adding server unit tests for context builder tool blocks",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.3"],
      "parallelWith": []
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Client UI",
      "subject": "[agent-tool-context-injection] [P2] Create useAgentContextConfig hook",
      "description": "Create a new hook for reading and updating the `agentContext` config section via the Transport layer.\n\n**New file:** `apps/client/src/layers/features/agent-settings/model/use-agent-context-config.ts`\n\n**Full implementation:**\n\n```typescript\nimport { useCallback } from 'react';\nimport { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';\nimport { useTransport } from '@/layers/shared/model';\n\ninterface AgentContextConfig {\n  relayTools: boolean;\n  meshTools: boolean;\n  adapterTools: boolean;\n}\n\nconst DEFAULTS: AgentContextConfig = {\n  relayTools: true,\n  meshTools: true,\n  adapterTools: true,\n};\n\n/**\n * Read and update the agentContext section of the user config.\n *\n * Uses the shared `['config']` query key so all config consumers\n * stay in sync after mutations.\n */\nexport function useAgentContextConfig() {\n  const transport = useTransport();\n  const queryClient = useQueryClient();\n\n  const { data } = useQuery({\n    queryKey: ['config'],\n    queryFn: () => transport.getConfig(),\n    staleTime: 30_000,\n  });\n\n  const config: AgentContextConfig = {\n    ...DEFAULTS,\n    ...data?.agentContext,\n  };\n\n  const mutation = useMutation({\n    mutationFn: (patch: Partial<AgentContextConfig>) =>\n      transport.updateConfig({ agentContext: { ...config, ...patch } }),\n    onSuccess: () => {\n      queryClient.invalidateQueries({ queryKey: ['config'] });\n    },\n  });\n\n  const updateConfig = useCallback(\n    (patch: Partial<AgentContextConfig>) => {\n      mutation.mutate(patch);\n    },\n    [mutation]\n  );\n\n  return { config, updateConfig };\n}\n```\n\nKey design decisions:\n- Reuses the existing `['config']` query key that other config hooks use, so TanStack Query cache stays shared.\n- `staleTime: 30_000` avoids excessive refetching (config changes are infrequent).\n- `DEFAULTS` object ensures the hook always returns defined boolean values even if the server config predates the `agentContext` section.\n- The mutation sends the full `agentContext` object (merged with the patch) because the PATCH handler deep-merges at the top level but replaces nested objects.\n- `useCallback` wraps the mutation call so the reference is stable for downstream consumers.\n\n**Acceptance Criteria:**\n- [ ] Hook returns `config` object with three boolean fields (relayTools, meshTools, adapterTools)\n- [ ] Hook returns `updateConfig` function that patches the agentContext config section\n- [ ] Defaults to all-true when server config has no agentContext section\n- [ ] Invalidates `['config']` query on successful mutation\n- [ ] Uses `useTransport()` for HTTP calls (works with both HttpTransport and DirectTransport)\n- [ ] File compiles with `pnpm typecheck`",
      "activeForm": "Creating useAgentContextConfig hook",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.2"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Client UI",
      "subject": "[agent-tool-context-injection] [P2] Create ContextTab component",
      "description": "Create the Context tab component for the Agent Settings dialog. This tab displays toggle switches for each tool context block with read-only previews of the block content.\n\n**New file:** `apps/client/src/layers/features/agent-settings/ui/ContextTab.tsx`\n\n**Full implementation:**\n\n```tsx\nimport { useCallback } from 'react';\nimport { useRelayEnabled } from '@/layers/entities/relay';\nimport { Badge, Label, Switch } from '@/layers/shared/ui';\nimport { useAgentContextConfig } from '../model/use-agent-context-config';\n\nconst RELAY_PREVIEW = `DorkOS Relay is a pub/sub message bus for inter-agent communication.\n\nSubject hierarchy:\n  relay.agent.{sessionId}          — address a specific agent session\n  relay.human.console.{clientId}   — reach a human in the DorkOS UI\n  relay.system.console             — system broadcast channel\n  relay.system.pulse.{scheduleId}  — Pulse scheduler events\n\nWorkflows:\n- Register a reply address first: relay_register_endpoint(subject=\"relay.agent.{your-sessionId}\")\n- Message another agent: relay_send(subject=\"relay.agent.{their-sessionId}\", payload={...}, from=\"relay.agent.{your-sessionId}\")\n- Check for replies: relay_inbox(endpoint_subject=\"relay.agent.{your-sessionId}\")\n- See who is listening: relay_list_endpoints()\n\nThe \"from\" field is your own subject. Set \"replyTo\" so the recipient knows where to respond.\n\nError codes: RELAY_DISABLED (feature off), ACCESS_DENIED (subject blocked), INVALID_SUBJECT (malformed), ENDPOINT_NOT_FOUND (inbox miss).`;\n\nconst MESH_PREVIEW = `DorkOS Mesh is a local agent registry for discovering and communicating with AI agents on this machine.\n\nAgent lifecycle:\n1. mesh_discover(roots=[\"/path\"]) — scan directories for agent candidates (looks for CLAUDE.md, .dork/agent.json)\n2. mesh_register(path, name, runtime, capabilities) — register a candidate as a known agent\n3. mesh_inspect(agentId) — get full manifest, health status, and relay endpoint\n4. mesh_status() — aggregate overview: total, active, stale agent counts\n5. mesh_list(runtime?, capability?) — filter agents by runtime or capability\n6. mesh_deny(path, reason) — exclude a path from future discovery\n7. mesh_unregister(agentId) — remove an agent from the registry\n8. mesh_query_topology(namespace?) — view agent network from a namespace perspective\n\nWorkflows:\n- Find agents: mesh_list() then mesh_inspect(agentId) for details\n- Contact another agent: mesh_inspect(agentId) to get their relay endpoint, then relay_send\n- Register this project: mesh_register(path=cwd, name=\"project-name\", runtime=\"claude-code\")\n\nRuntimes: claude-code | cursor | codex | other`;\n\nconst ADAPTER_PREVIEW = `Relay adapters bridge external platforms (Telegram, webhooks) to the agent message bus.\n\nSubject conventions for external messages:\n  relay.human.telegram.{chatId}    — send to / receive from Telegram\n  relay.human.webhook.{webhookId}  — send to / receive from webhooks\n\nAdapter management:\n- relay_list_adapters() — see all adapters and their status (connected, disconnected, error)\n- relay_enable_adapter(id) / relay_disable_adapter(id) — toggle an adapter on/off\n- relay_reload_adapters() — hot-reload config from disk\n\nBindings route adapter messages to agent projects:\n- binding_list() — see current adapter-to-agent bindings\n- binding_create(adapterId, agentId, projectPath) — route an adapter to an agent\n- binding_delete(id) — remove a binding\n\nSession strategies: per-chat (default, one session per conversation), per-user (shared across chats), stateless (new session each message).`;\n\ninterface ContextBlockSectionProps {\n  label: string;\n  description: string;\n  enabled: boolean;\n  available: boolean;\n  unavailableReason?: string;\n  onToggle: (value: boolean) => void;\n  preview: string;\n}\n\nfunction ContextBlockSection({\n  label,\n  description,\n  enabled,\n  available,\n  unavailableReason,\n  onToggle,\n  preview,\n}: ContextBlockSectionProps) {\n  const effective = available && enabled;\n\n  return (\n    <section className=\"space-y-2\">\n      <div className=\"flex items-center justify-between\">\n        <div className=\"space-y-0.5\">\n          <Label className=\"text-sm font-medium\">{label}</Label>\n          <p className=\"text-muted-foreground text-xs\">{description}</p>\n        </div>\n        <div className=\"flex items-center gap-2\">\n          {!available && (\n            <Badge variant=\"secondary\" className=\"text-xs\">\n              {unavailableReason}\n            </Badge>\n          )}\n          <Switch\n            checked={enabled}\n            onCheckedChange={onToggle}\n            disabled={!available}\n            aria-label={`Toggle ${label} context`}\n          />\n        </div>\n      </div>\n      {effective && (\n        <pre className=\"bg-muted max-h-40 overflow-y-auto rounded-md p-3 text-xs leading-relaxed\">\n          {preview}\n        </pre>\n      )}\n    </section>\n  );\n}\n\n/**\n * Context tab for the Agent Settings dialog.\n *\n * Displays toggle switches for each tool context block (relay, mesh, adapter)\n * with read-only previews of the XML content injected into agent system prompts.\n */\nexport function ContextTab() {\n  const relayEnabled = useRelayEnabled();\n  const { config, updateConfig } = useAgentContextConfig();\n\n  const handleToggle = useCallback(\n    (key: 'relayTools' | 'meshTools' | 'adapterTools', value: boolean) => {\n      updateConfig({ [key]: value });\n    },\n    [updateConfig]\n  );\n\n  return (\n    <div className=\"space-y-6\">\n      <p className=\"text-muted-foreground text-sm\">\n        Control which tool usage instructions are injected into the agent system prompt.\n        These blocks teach the agent how to use DorkOS tools together.\n      </p>\n\n      <ContextBlockSection\n        label=\"Relay Tools\"\n        description=\"Subject hierarchy, messaging workflows, and error codes for the Relay message bus.\"\n        enabled={config.relayTools}\n        available={relayEnabled}\n        unavailableReason=\"Relay is disabled\"\n        onToggle={(v) => handleToggle('relayTools', v)}\n        preview={RELAY_PREVIEW}\n      />\n\n      <ContextBlockSection\n        label=\"Mesh Tools\"\n        description=\"Agent lifecycle, discovery workflow, and cross-tool orchestration with Relay.\"\n        enabled={config.meshTools}\n        available={true}\n        onToggle={(v) => handleToggle('meshTools', v)}\n        preview={MESH_PREVIEW}\n      />\n\n      <ContextBlockSection\n        label=\"Adapter Tools\"\n        description=\"External platform subjects, adapter management, and binding routing conventions.\"\n        enabled={config.adapterTools}\n        available={relayEnabled}\n        unavailableReason=\"Relay is disabled\"\n        onToggle={(v) => handleToggle('adapterTools', v)}\n        preview={ADAPTER_PREVIEW}\n      />\n    </div>\n  );\n}\n```\n\nKey design decisions:\n- `ContextBlockSection` is a local helper component (not exported from the barrel) to avoid cluttering the public API.\n- Preview strings match the server-side XML content but without the outer XML tags for cleaner display.\n- The `effective` flag requires both `available` (feature flag) AND `enabled` (user toggle) to show the preview.\n- Mesh is always `available={true}` because mesh is always-on per ADR-0062.\n- Relay and adapter sections show a \"Relay is disabled\" badge when relay is off, and their switches are disabled.\n\n**Acceptance Criteria:**\n- [ ] Component renders three toggle sections: Relay Tools, Mesh Tools, Adapter Tools\n- [ ] Each section has a label, description, toggle switch, and conditional preview\n- [ ] Preview shows when toggle is on AND feature is available\n- [ ] Preview hides when toggle is off OR feature is unavailable\n- [ ] Relay and Adapter switches are disabled when relay is off\n- [ ] \"Relay is disabled\" badge shows when relay is off\n- [ ] Mesh section is always available (no feature gate)\n- [ ] Toggle changes call `updateConfig` with the correct key\n- [ ] All switches have `aria-label` attributes for accessibility\n- [ ] `pnpm typecheck` passes",
      "activeForm": "Creating ContextTab component",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.1"]
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Client UI",
      "subject": "[agent-tool-context-injection] [P2] Integrate ContextTab into AgentDialog as fifth tab",
      "description": "Update the `AgentDialog` component to include the new Context tab as the fifth tab.\n\n**File to modify:** `apps/client/src/layers/features/agent-settings/ui/AgentDialog.tsx`\n\n**Changes required:**\n\n1. Add import for `ContextTab`:\n\n```typescript\nimport { ContextTab } from './ContextTab';\n```\n\n2. Update the JSDoc comment to mention five tabs:\n\n```typescript\n/**\n * Dialog shell for agent configuration with tabbed navigation.\n * Five tabs: Identity, Persona, Capabilities, Connections, and Context.\n */\n```\n\n3. Update `grid-cols-4` to `grid-cols-5` in the `TabsList`:\n\n```tsx\n<TabsList\n  className=\"mx-4 mt-3 grid w-full grid-cols-5\"\n  style={{ width: 'calc(100% - 2rem)' }}\n>\n  <TabsTrigger value=\"identity\">Identity</TabsTrigger>\n  <TabsTrigger value=\"persona\">Persona</TabsTrigger>\n  <TabsTrigger value=\"capabilities\">Capabilities</TabsTrigger>\n  <TabsTrigger value=\"connections\">Connections</TabsTrigger>\n  <TabsTrigger value=\"context\">Context</TabsTrigger>\n</TabsList>\n```\n\n4. Add `TabsContent` for the Context tab after the Connections tab content:\n\n```tsx\n<TabsContent value=\"connections\" className=\"mt-0\">\n  <ConnectionsTab agent={agent} />\n</TabsContent>\n\n<TabsContent value=\"context\" className=\"mt-0\">\n  <ContextTab />\n</TabsContent>\n```\n\nNote: `ContextTab` does NOT receive `agent` or `onUpdate` props because it operates on the global `agentContext` config section, not the per-agent manifest. Tool context settings are server-wide, not per-agent.\n\n**Acceptance Criteria:**\n- [ ] \"Context\" tab trigger appears as the fifth tab in the AgentDialog\n- [ ] Clicking \"Context\" tab renders the ContextTab component\n- [ ] Tab grid layout accommodates 5 tabs without overflow\n- [ ] ContextTab receives no props (operates on global config)\n- [ ] All existing tabs continue to work correctly\n- [ ] `pnpm typecheck` passes",
      "activeForm": "Integrating ContextTab into AgentDialog as fifth tab",
      "size": "small",
      "priority": "high",
      "dependencies": ["2.1", "2.2"],
      "parallelWith": []
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "Client UI",
      "subject": "[agent-tool-context-injection] [P2] Add client component tests for ContextTab",
      "description": "Create component tests for the ContextTab that verify toggle behavior, preview rendering, and feature flag gating.\n\n**New file:** `apps/client/src/layers/features/agent-settings/__tests__/ContextTab.test.tsx`\n\n**Full implementation:**\n\n```typescript\n// @vitest-environment jsdom\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport '@testing-library/jest-dom/vitest';\n\nvi.mock('@/layers/entities/relay', () => ({\n  useRelayEnabled: vi.fn(() => true),\n}));\nvi.mock('../model/use-agent-context-config', () => ({\n  useAgentContextConfig: vi.fn(() => ({\n    config: { relayTools: true, meshTools: true, adapterTools: true },\n    updateConfig: vi.fn(),\n  })),\n}));\n\nimport { ContextTab } from '../ui/ContextTab';\nimport { useRelayEnabled } from '@/layers/entities/relay';\nimport { useAgentContextConfig } from '../model/use-agent-context-config';\n\ndescribe('ContextTab', () => {\n  beforeEach(() => {\n    vi.clearAllMocks();\n    vi.mocked(useRelayEnabled).mockReturnValue(true);\n    vi.mocked(useAgentContextConfig).mockReturnValue({\n      config: { relayTools: true, meshTools: true, adapterTools: true },\n      updateConfig: vi.fn(),\n    });\n  });\n\n  it('renders all three toggle sections', () => {\n    render(<ContextTab />);\n    expect(screen.getByText('Relay Tools')).toBeInTheDocument();\n    expect(screen.getByText('Mesh Tools')).toBeInTheDocument();\n    expect(screen.getByText('Adapter Tools')).toBeInTheDocument();\n  });\n\n  it('renders description text', () => {\n    render(<ContextTab />);\n    expect(\n      screen.getByText(/Control which tool usage instructions are injected/)\n    ).toBeInTheDocument();\n  });\n\n  it('shows preview when toggle is on and feature is available', () => {\n    render(<ContextTab />);\n    // Relay preview should contain subject hierarchy\n    expect(screen.getByText(/relay\\.agent\\.\\{sessionId\\}/)).toBeInTheDocument();\n    // Mesh preview should contain lifecycle steps\n    expect(screen.getByText(/mesh_discover/)).toBeInTheDocument();\n    // Adapter preview should contain binding info\n    expect(screen.getByText(/binding_create/)).toBeInTheDocument();\n  });\n\n  it('hides preview when toggle is off', () => {\n    vi.mocked(useAgentContextConfig).mockReturnValue({\n      config: { relayTools: false, meshTools: true, adapterTools: true },\n      updateConfig: vi.fn(),\n    });\n    render(<ContextTab />);\n    // Relay preview should be hidden\n    expect(screen.queryByText(/relay\\.agent\\.\\{sessionId\\}/)).not.toBeInTheDocument();\n    // Mesh preview should still show\n    expect(screen.getByText(/mesh_discover/)).toBeInTheDocument();\n  });\n\n  it('disables relay and adapter switches when relay is off', () => {\n    vi.mocked(useRelayEnabled).mockReturnValue(false);\n    render(<ContextTab />);\n    const switches = screen.getAllByRole('switch');\n    // Relay switch (index 0) should be disabled\n    expect(switches[0]).toBeDisabled();\n    // Mesh switch (index 1) should be enabled\n    expect(switches[1]).not.toBeDisabled();\n    // Adapter switch (index 2) should be disabled\n    expect(switches[2]).toBeDisabled();\n  });\n\n  it('shows \"Relay is disabled\" badge when relay is off', () => {\n    vi.mocked(useRelayEnabled).mockReturnValue(false);\n    render(<ContextTab />);\n    const badges = screen.getAllByText('Relay is disabled');\n    expect(badges).toHaveLength(2); // One for relay section, one for adapter section\n  });\n\n  it('hides preview when feature is unavailable even if toggle is on', () => {\n    vi.mocked(useRelayEnabled).mockReturnValue(false);\n    vi.mocked(useAgentContextConfig).mockReturnValue({\n      config: { relayTools: true, meshTools: true, adapterTools: true },\n      updateConfig: vi.fn(),\n    });\n    render(<ContextTab />);\n    // Relay and adapter previews should be hidden (feature unavailable)\n    expect(screen.queryByText(/relay\\.agent\\.\\{sessionId\\}/)).not.toBeInTheDocument();\n    expect(screen.queryByText(/binding_create/)).not.toBeInTheDocument();\n    // Mesh preview should show (always available)\n    expect(screen.getByText(/mesh_discover/)).toBeInTheDocument();\n  });\n\n  it('calls updateConfig when a toggle is switched', async () => {\n    const updateConfig = vi.fn();\n    vi.mocked(useAgentContextConfig).mockReturnValue({\n      config: { relayTools: true, meshTools: true, adapterTools: true },\n      updateConfig,\n    });\n    const user = userEvent.setup();\n    render(<ContextTab />);\n\n    const switches = screen.getAllByRole('switch');\n    // Click mesh toggle (index 1)\n    await user.click(switches[1]);\n\n    expect(updateConfig).toHaveBeenCalledWith({ meshTools: false });\n  });\n\n  it('calls updateConfig with correct key for relay toggle', async () => {\n    const updateConfig = vi.fn();\n    vi.mocked(useAgentContextConfig).mockReturnValue({\n      config: { relayTools: true, meshTools: true, adapterTools: true },\n      updateConfig,\n    });\n    const user = userEvent.setup();\n    render(<ContextTab />);\n\n    const switches = screen.getAllByRole('switch');\n    // Click relay toggle (index 0)\n    await user.click(switches[0]);\n\n    expect(updateConfig).toHaveBeenCalledWith({ relayTools: false });\n  });\n\n  it('has accessible labels for all switches', () => {\n    render(<ContextTab />);\n    expect(screen.getByLabelText('Toggle Relay Tools context')).toBeInTheDocument();\n    expect(screen.getByLabelText('Toggle Mesh Tools context')).toBeInTheDocument();\n    expect(screen.getByLabelText('Toggle Adapter Tools context')).toBeInTheDocument();\n  });\n});\n```\n\n**Acceptance Criteria:**\n- [ ] Test file renders ContextTab without errors\n- [ ] All three toggle sections render with correct labels\n- [ ] Preview visibility tests: shown when on+available, hidden when off, hidden when unavailable\n- [ ] Relay/adapter switches disabled when relay feature is off\n- [ ] \"Relay is disabled\" badge appears twice (relay + adapter) when relay is off\n- [ ] Toggle click calls `updateConfig` with correct key and value\n- [ ] Accessibility labels verified for all switches\n- [ ] `pnpm test -- --run` passes for this test file",
      "activeForm": "Adding client component tests for ContextTab",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["2.2"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Verification",
      "subject": "[agent-tool-context-injection] [P3] Run full typecheck and test suite",
      "description": "Run the full typecheck and test suite across the monorepo to verify no regressions.\n\n**Commands to run:**\n\n```bash\npnpm typecheck\npnpm test -- --run\n```\n\n**What to verify:**\n\n1. **TypeScript compilation**: All packages compile without errors. The new `agentContext` type flows correctly through:\n   - `packages/shared/src/config-schema.ts` (schema definition)\n   - `apps/server/src/services/core/context-builder.ts` (config reading)\n   - `apps/client/src/layers/features/agent-settings/model/use-agent-context-config.ts` (hook)\n   - `apps/client/src/layers/features/agent-settings/ui/ContextTab.tsx` (component)\n\n2. **Existing tests pass**: No regressions in:\n   - Existing `context-builder.test.ts` tests (env, git, agent blocks)\n   - `CapabilitiesTab.test.tsx` and other agent-settings tests\n   - All other test suites across the monorepo\n\n3. **New tests pass**: All tests from tasks 1.4 and 2.4 pass.\n\n4. **Lint**: Run `pnpm lint` to check for any lint issues introduced by the new code.\n\n**Acceptance Criteria:**\n- [ ] `pnpm typecheck` exits with code 0\n- [ ] `pnpm test -- --run` exits with code 0\n- [ ] `pnpm lint` shows no new errors\n- [ ] No regressions in existing functionality",
      "activeForm": "Running full typecheck and test suite",
      "size": "small",
      "priority": "high",
      "dependencies": ["1.4", "2.3", "2.4"],
      "parallelWith": []
    }
  ]
}
