# Community server implementation tasks

Generated from `specs/community-server/02-specification.md` on 2026-09-16T11:51:34.109987Z. `03-tasks.json` is canonical; this file is its readable projection.

25 tasks in six coherent PR batches. Dependencies are task IDs; only the first batch advertises parallel implementation because those owners have disjoint files.

## PR1 — Community foundation (6 tasks)

### Task 1.1: [community-server] [P1] Extend the community wire and adapter contracts

**Size:** md · **Priority:** high · **Depends on:** none · **Parallel with:** 1.2

Own `packages/shared/src/community-adapter.ts`, related Zod wire schemas, and `communityConformance` in `@dorkos/test-utils`. Define versioned `/api/v1` request/response DTOs for public metadata, auth-facing admission, channels/members, history/threads/cursors/SSE, attachments, agents, pairing, exports and errors; never expose database rows, credential material or local paths. Keep `(communityRef, remoteRoomId)` as local addressing: communityRef is minted locally and distinct from server-minted immutable community ID and changeable URL. Extend `post` with optional actingMemberId (omission means human), required stable idempotency key for remote writes, and at most bounded attachment IDs; add upload/download operations and explicit capabilities for attachments and acting as an owned agent. `admitAgent` returns identity/receipt while the adapter privately retains its token. Make local and Buzz backends refuse unsupported operations/acting identities explicitly. Acceptance: schema round trips and capability-aware conformance pass for all existing backends; secret fields are absent from serialized DTOs; unsupported operations produce typed refusal, not silent success. Publish exact exported DTO/schema names and `/api/v1` method/path contract to the server-foundation owner before API routes begin; this task alone owns `packages/shared` and `packages/test-utils` files in the first parallel batch. Add `credential: browser-approved` for same-origin human approval followed by a privately held revocable local bearer; update the credential TSDoc, all capability consumers and conformance branches so neither `user-account` (no key) nor `machine-managed` (no sign-in) is misused. Acceptance includes correct credential descriptor and refusal/consumer tests.

### Task 1.2: [community-server] [P1] Scaffold an independent community app and real Postgres schema

**Size:** lg · **Priority:** high · **Depends on:** none · **Parallel with:** 1.1

Own `apps/community` package scaffold, Hono persistent Node entry, Drizzle Postgres schema/migrations, environment parser. Pin workspace-compatible Hono, Better Auth, Drizzle and Postgres packages after checking official migration/runtime docs. Serve built React/Vite assets same-origin and `/api/v1`; imports, build, typecheck and unit tests must not need secrets. Startup validates `COMMUNITY_DATABASE_URL`, `COMMUNITY_AUTH_SECRET`, `COMMUNITY_INVITE_SECRET`, `COMMUNITY_BOOTSTRAP_SECRET`, `COMMUNITY_PUBLIC_URL`, storage and optional OAuth settings, and positive configurable limits with hard ceilings: posts 120/default and 1000/max per rolling ten minutes, agents 20/100, text 16/64 KiB, attachments 4/8 per post and 10/25 MiB each, uploads 200 MiB/1 GiB per owner/day. Create migrations for immutable communities ID; Better Auth tables; members, agents/credentials; bootstrap grants, invites/uses/pending admissions, pairings/grants; channels/members, entries, attachments, read cursors, owner quota windows and audit events, with FKs, indexes, unique singleton owner, `(channel_id,seq)` and `(author_member_id,channel_id,idempotency_key)`. Keep `packages/db` SQLite-only. Add MIT app license and package scripts inside `apps/community`; leave shared/root manifest, lockfile, Vitest/Turbo, server index and census edits to the first-PR integrator in task 1.6 so the parallel contract owner has no overlapping files. Acceptance: clean migrations apply to isolated real Postgres, build runs with no Cloud imports, missing runtime settings fail clearly, unit imports need no settings, and CI real-PG job fails setup if database is absent rather than skipping tests. Supply an initial independent built Docker image and Compose file with Postgres and persistent blob volume for PR1 readiness; a clean image applies migrations and serves the owner/channel/post HTTP path once tasks 1.3–1.6 land.

### Task 1.3: [community-server] [P1] Implement human auth, guarded bootstrap and owner claim

**Size:** lg · **Priority:** high · **Depends on:** 1.1, 1.2

Own community Better Auth mount and common HTTP/SSE authentication guard. Email/password is always enabled; optional Google/GitHub is enabled only with configured client secrets/callback URLs. Set trusted origins, HTTPS proxy and secure HTTP-only cookie behavior, CSRF/origin checks, session rotation and no unverified-email auto-linking. Every protected request joins session to a live human member row; an authenticated but unadmitted account has no roster/history rights. A rate-limited bootstrap preflight checks the deployment secret while no owner exists, issues a ten-minute one-use row-backed grant in an opaque signed HTTP-only cookie; the signup hook permits creation only with a live bootstrap grant or a valid pending invite admission. Owner claim requires the authenticated account, same grant and secret again, transactionally locks/consumes grant and inserts singleton owner. Losing concurrent claimant remains unadmitted; no grant after owner exists; recovery is privileged server procedure, never public escalation. Acceptance: real-Postgres HTTP tests prove exactly one owner under parallel claims, absent/expired/reused grant denied, session-only account cannot access any protected route, and owner survives restart. Route request/response validation through the shared `/api/v1` Zod schemas from task 1.1; include a contract test comparing real HTTP output to those schemas. Rate-limit signup attempts to ten/minute/IP (configurable within startup-validated bounds).

### Task 1.4: [community-server] [P1] Implement channel membership and role authority

**Size:** md · **Priority:** high · **Depends on:** 1.1, 1.2, 1.3

Own channel and channel-member service/routes under `/api/v1/channels`. Owner/admin create, rename and archive channels; humans discover visible channels but can read/post only after joining; members may join/leave visible channels; agent channel access requires explicit membership and agents cannot join themselves. Archived channels remain readable to authorized members and reject writes. Central live-row authorization implements owner/admin/member/agent matrix: owner-only admin promotion/removal and ownership controls; admins cannot remove owner or alter another admin role; admins may moderate ordinary members/agents; ordinary members administer neither. Never accept role, owner ID or membership claims from body; return 404 for hidden private channel rather than revealing it. Serialize membership changes with writes so a removed principal cannot commit afterward. Acceptance: exact role/action table over real HTTP and database rows; removed/archived/nonmember cases fail on list/detail and future post seams; no local `seesEveryRoom` bypass reaches this service. Route request/response validation through the shared `/api/v1` Zod schemas from task 1.1; include a contract test comparing real HTTP output to those schemas.

### Task 1.5: [community-server] [P1] Implement ordered idempotent entries and bounded history

**Size:** lg · **Priority:** high · **Depends on:** 1.1, 1.4

Own community entry service/routes and database transactions. On post, authenticate live human/agent owner and channel membership, check UTF-8 byte cap, parent channel/visibility, atomic owner quota, stable idempotency key and attachment IDs; serialize per-channel sequence allocation with row lock/counter update so `seq` follows commit order, never bare `nextval`. Same key+payload returns original receipt; same key+different payload returns 409. One-level replies accept only a top-level root; replying to an in-thread entry is rejected as HTTP 409 `NESTED_THREAD`, matching `CommunityAdapter.threadDepth: 1` and the local room writer. Cross-channel parent is rejected. History and thread pages default 50/max 100 and use opaque scoped cursors binding channel, thread selector, epoch and position, with explicit `nextCursor:null`; stale cursor is 410. Store author display snapshot for history after leave; never persist remote path, local session ID or raw secret. Acceptance: isolated real-PG tests assert exact rows and subjects after parallel posts, seq/commit ordering, duplicate retries and payload conflict, cross-channel/hidden/nested parent rejection, scoped cursor rejection, and membership removal race. Route request/response validation through the shared `/api/v1` Zod schemas from task 1.1; include a contract test comparing real HTTP output to those schemas.

### Task 1.6: [community-server] [P1] Add authorized SSE replay and read cursors

**Size:** md · **Priority:** high · **Depends on:** 1.1, 1.5

Own channel events/read-cursor routes. SSE opens with authorized snapshot/cursor, replays committed entries after scoped `Last-Event-ID`, then switches to live delivery without a gap; publish only after entry commit and recover from publish failure via database replay. Queue at most 256 events per subscriber, heartbeat every 15 seconds without content, close overflow and resume from last committed cursor. Active streams close promptly on channel/member revocation and reauthorize on resume. Per-member read cursor is monotonic and cannot pass an entry that member could not see; unread is derived per member. Acceptance: real-PG HTTP stream tests force write during snapshot/live transition, publish loss, overflow, stale/foreign cursor and revoked stream, asserting exact delivered seq once and no unauthorized bytes; read cursor regression or over-advance fails. Route request/response validation through the shared `/api/v1` Zod schemas from task 1.1; include a contract test comparing real HTTP output to those schemas. As first-PR integration owner, reconcile package manifest/lock and add `apps/community` to root Vitest projects, Turbo scripts, AGENTS app census and contributing index after tasks 1.1/1.2 converge. Prove the built Docker image plus real Postgres serves owner bootstrap, channel creation and post through HTTP; do not land unused wire exports without callers. Check combined-tree overlap with active #1908 shared manifest/lock and #1906 server index/runtime/FakeAgentRuntime work.

## PR2 — Admission and governance (4 tasks)

### Task 2.1: [community-server] [P2] Implement signed invites and atomic admission

**Size:** lg · **Priority:** high · **Depends on:** 1.3, 1.4

Own invite creation/list/revoke, preview/preflight and redeem service/routes. Owner/admin may issue a row-backed community or channel invite; default seven days/max 30, one seat/max 100. URL token stays in fragment; browser sends only same-origin POST body and clears fragment. Token encodes version, key ID, opaque invite ID, expiry, nonce and HMAC-SHA-256 over canonical length-delimited payload containing immutable community ID and `community-invite-v1`; derive purpose key via HKDF from `COMMUNITY_INVITE_SECRET`, compare constant-time, retain prior key IDs only during documented rotation. Preview is rate-limited 20/min/IP and reveals only name/inviter/scoped channel. Preflight issues ten-minute signed SameSite=Lax HTTP-only cookie carrying opaque pending ID whose row binds invite; signup hook checks current cookie+row for password/OAuth creation but consumes no seat. Redeem locks invite, rechecks signature/expiry/revocation/issuer authority/seats, creates or reactivates human member, joins scoped channel, writes unique `(invite_id,user_id)` use and increments at most once. Exhausted signup leaves account unadmitted with recovery path. Acceptance: real-PG concurrent last-seat test admits exactly one; duplicate same user uses one seat; expired/revoked/tampered/cross-community token fails without roster disclosure; signup without pending grant fails; preview never consumes seat.

### Task 2.2: [community-server] [P2] Enforce member moderation and ownership transfer

**Size:** lg · **Priority:** high · **Depends on:** 2.1, 1.6

Own member administration, role change, removal, leave and transfer operations. Owner alone grants/removes admin and transfers ownership; transfer requires current owner reauthentication, live successor, transaction locking and one-active-owner invariant. Owner cannot leave/remove self before transfer. Admin can remove ordinary member or agent, not owner/another admin role. Removal/leave transaction revokes owned agents and connection grants and membership access while preserving shared entry author display snapshots. Every subsequent cookie, bearer, history, SSE, upload and post request checks live rows; existing streams close. Audit security-relevant changes without token material. Acceptance: real-PG HTTP tests cover complete role matrix, owner transfer race/reauth, owner exit path, removal racing post and stream, immediate agent/grant invalidation, and shared history attribution after leave.

### Task 2.3: [community-server] [P2] Add revocable browser-approved local pairing

**Size:** md · **Priority:** high · **Depends on:** 2.2

Own `/api/v1/pairings/{start,approve,poll,exchange}` and `/api/v1/me/grants` server behavior. A local install presents random verifier/challenge; request lasts ten minutes, single-use, creation limited to five/min/IP. Same-origin approval page requires an authenticated active member, displays requesting install and access scope, and binds approval to that member. Poll yields one-time code, never token in URL/redirect/body visible to browser; code+verifier exchange under row lock creates high-entropy scoped personal token. Store token hash, member, scopes, created/last-used/revoked times; return plaintext only to polling local server. Two polls/exchanges cannot redeem twice; timeout/cancel issues no credential. Member can list/revoke each install grant; removal invalidates all through live member check. Validate public origin and challenge to prevent credential forwarding. Acceptance: real-PG tests exercise simultaneous polls, wrong verifier, cancellation, expiry, revoked grant and removed member; logs/DTOs contain no token.

### Task 2.4: [community-server] [P2] Enroll owned agents with isolated credentials and quotas

**Size:** lg · **Priority:** high · **Depends on:** 2.2

Own community agent enroll/list/eject/rotate and channel membership APIs. Only a valid connected human member can enroll an agent they own, up to 20 active by default/100 hard ceiling; issue high-entropy credential once, store hash only, never return it through shared DTO or logs. Agent has separate opaque member ID, display, `ownerMemberId`, active state and credential record. Every agent request joins live agent and owner rows, verifies token hash and explicit channel membership; owner or admin adds/removes an agent from channel, and admin can eject another member's agent without seeing its token. Agent can read/post/download its joined channels, but cannot invite, join itself, administer, enroll, transfer, export or mark human read cursors. Token rotate/revoke acts immediately. Charge all human and owned-agent posts to human's atomic rolling ten-minute window (120 default/1000 ceiling); attachment bytes to human day window (200 MiB default/1 GiB ceiling), including unbound uploads. Over-limit returns 429/413, not partial writes. Acceptance: real-PG tests show two parallel agent identities cannot multiply owner quota, owner removal invalidates both, token rotation revokes old token, unauthorized agent role operations fail, and exact active-agent cap holds under concurrent enrollment.

## PR3 — Human chat and files (5 tasks)

### Task 3.1: [community-server] [P3] Build filesystem and S3-compatible blob stores

**Size:** md · **Priority:** high · **Depends on:** 1.2

Own `BlobStore` contract and two used implementations under `apps/community`: persistent-volume filesystem and S3-compatible object storage. Put/get/delete use server-generated random opaque keys; metadata and authorization stay in Postgres, never expose local path/object URL/domain-specific ID. Stream bounded bytes without full buffering, compute checksum, sniff bytes against allowed types, reject executable/active inline content, sanitize display names, serve downloads with safe `Content-Disposition` and `X-Content-Type-Options:nosniff`. Runtime config chooses filesystem volume in Compose or S3 for hosted deployment; no `dork-home` layout. Test both implementations with the same contract suite including traversal-like filenames, checksum, byte cap, interrupted put/get/delete and restart persistence; pin official SDK behavior and avoid a dormant abstraction.

### Task 3.2: [community-server] [P3] Implement bounded upload, bind, download and orphan cleanup

**Size:** lg · **Priority:** high · **Depends on:** 1.5, 2.4, 3.1

Own attachment routes/service and entry binding. Authorized joined member or agent streams at most configured 10 MiB/default, 25 MiB hard cap per attachment; max four/default, eight/hard per post; detect type from bytes, rate/account for owner daily bytes atomically even when unbound. Upload requires stable idempotency key so timeout retry returns same ID; creates metadata row with opaque blob key, channel/uploader, sanitized name, detected type, size, checksum, upload time and nullable bound entry. Entry transaction binds only unbound IDs uploaded by same principal in same channel, exactly once, all-or-nothing. Metadata in history/SSE; authorized download rechecks current channel membership. Unbound blobs expire after one hour; bounded sweeper deletes rows/blobs, retries failed blob delete without affecting bound entries, emits observable failure. Agent uploads use own token plus live owner/channel and shared quota. Acceptance: real-PG tests cover parallel bind, other cross-owner/channel IDs, retry, exact cap, type sniff, unauthorized metadata/download, revoke during download, orphan deletion/failure retry and bound-byte persistence.

### Task 3.3: [community-server] [P3] Build standalone community join and channel browser

**Size:** lg · **Priority:** high · **Depends on:** 2.1, 1.6

Own `apps/community` React/Vite/Tailwind same-origin browser shell and core screens. Fresh owner enters bootstrap secret, claims account, names community and creates channel. Invite fragment is read once, cleared, posted same-origin for safe preview and ten-minute preflight; then password signup/sign-in or configured Google/GitHub only. Exhausted/expired/revoked invite and unadmitted account get recovery screens without account enumeration. Signed-in humans can navigate visible vs joined channels, history, one-level threads, live SSE and read/unread state; composer posts stable idempotency key, shows pending/failed/confirmed and does not duplicate SSE echo. Include owner/admin controls for invites, channels and ordinary members, with role-driven affordances but server remaining authoritative. All screens have loading/empty/offline/archived/unauthorized states, semantic labels, keyboard focus, dark theme tokens, and mobile/tablet/desktop navigation. Acceptance: RTL component tests verify states and status; real two-context browser scenario proves owner/invite/join/channel/thread/live/unread flow; inspect accessibility names/keyboard path and screenshots for join, channel, thread, errors, mobile navigation.

### Task 3.4: [community-server] [P3] Complete browser files, account and agent controls

**Size:** md · **Priority:** high · **Depends on:** 3.2, 3.3, 3.5

Extend standalone browser with attachment picker/upload progress, authorized download, agent cards showing human owner, member/channel administration, connection-grant listing/revocation, personal and owner exports, leave and ownership transfer. Enforce four/default upload count and configured size feedback before request while server remains authoritative; never render a raw token or secret. Owner transfer reauth and cannot-leave-yet message are explicit. Errors 401/403/404/409/410/413/415/429/503 have useful distinct recovery. Optional OAuth buttons appear only when configured; password manager fields work. Acceptance: two isolated browser contexts upload/download an allowed file, fail a disallowed file, verify another nonmember cannot download, administer an agent/member, export exact data and leave; mobile/tablet/desktop keyboard and visual states reviewed.

### Task 3.5: [community-server] [P3] Add personal and owner export with recoverable leave

**Size:** md · **Priority:** medium · **Depends on:** 2.2, 3.1

Own `/api/v1/me/export`, `/api/v1/me/leave` and `/api/v1/owner/export` with versioned archive manifests. Personal export includes member identity/membership, own posts, owned agent identities/posts and authorized attachment bytes, excluding other members' private account data. Owner community export requires reauthentication and contains documented whole-community data, attachment metadata and bytes; stream or stage bounded archives through `BlobStore`, never a private file path in DTO. Download authorization must be live, and archive creation/deletion observable. Leave uses transaction/revocation behavior from member service; owner sees transfer requirement before leave. Acceptance: real-PG test opens archive and checks exact included/excluded subjects and bytes, removed member denied export, owner reauth required, and leave invalidates grants/agents while preserving attributable shared posts.

## PR4 — Native connection and cache (4 tasks)

### Task 4.1: [community-server] [P4] Protect local community credentials and pairing transport

**Size:** lg · **Priority:** high · **Depends on:** 2.3, 1.1

Own local server connection store and pairing orchestration. Persist multiple independent `(communityRef, immutableRemoteCommunityId, pinnedOrigin, connectedHumanMemberId)` records with private personal/agent credentials protected at rest using existing local secret-storage conventions; browser/`Transport` DTOs show only non-secret status and descriptors. Accept HTTPS community URL, HTTP only for localhost development; validate DNS/IP/redirect targets against SSRF and pin accepted origin so a redirect cannot forward credentials to another host. Generate verifier/challenge and ten-minute pairing, open same-origin approval page, poll once, exchange code server-to-server, store token privately; cancellation/timeout stores none. Expose list/status/disconnect, with disconnect removing credentials and mapped cache. Two communities with same remote room ID stay isolated by local ref. Acceptance: integration tests exercise redirect to another host, private IP/credential forwarding, two-community isolation, double poll/cancel, storage restart, and DTO/log scans for bearer secrets.

### Task 4.2: [community-server] [P4] Implement native remote community adapter and conformance

**Size:** lg · **Priority:** high · **Depends on:** 4.1, 2.4, 3.2

Own `apps/server/src/services/communities/remote/` HTTP/SSE implementation of `CommunityAdapter`, registered as backend. Use pinned origin and versioned shared schemas, personal bearer for omitted actingMemberId, private agent credential resolved by `(communityRef, actingMemberId)` only after checking local ownership/active state; unknown/revoked/non-owned acting identity is refused before network request. Support channel listing/detail/members, history/threads/scoped cursors, post idempotency and bounded attachment upload/download; turn server 401/403/404/409/410/413/415/429/503 into typed port errors and map remote revocation to `not-admitted`. Enrollment privately receives and stores one-time agent token while returning non-secret identity/receipt. Capabilities declare agent acting and attachments. Acceptance: updated `communityConformance` passes against real HTTP server; tests prove no token crosses adapter DTO/log, wrong agent cannot be selected, remote authorization remains authoritative, and local/Buzz unsupported capabilities are explicit.

### Task 4.3: [community-server] [P4] Mirror authorized remote channels and entries safely

**Size:** lg · **Priority:** high · **Depends on:** 4.2

Own local SQLite cache schema/import and author mapping. Key remote room/entry mappings by `(communityRef,remoteRoomId,remoteEntryId)` with explicit origin; local IDs never impersonate remote IDs. Batch snapshots/history in remote seq order, idempotently upsert by remote entry ID, and resolve remote authors via community-scoped natural keys in AuthorRegistry. A remote human, including this install's owner, has `external` origin; unknown authors stay external. Never trust remote workspace/path/agent-session fields or let imported room inherit `seesEveryRoom`/operator read bypass. Only cache channels the connected human joined or locally owned enrolled agents can access; enforce remote membership on normal reads, with an explicitly scoped stale offline read-only policy for the owner's last authorized rooms. On `not-admitted`, close subscription, stop dispatch and hide/invalidate affected content; temporary outage preserves marked stale cache. Acceptance: two-community ID collision, duplicate import, member removal, offline/restore, unknown author and owner-origin tests assert exact rows; nonmember cache never appears via local operator bypass.

### Task 4.4: [community-server] [P4] Provide remote room routing through the local Transport

**Size:** md · **Priority:** high · **Depends on:** 4.3

Own local server routes and Transport methods for `(communityRef,remoteRoomId)` room list/open, history/thread, roster, read cursor, SSE and composer requests; local room paths continue to use existing behavior. Aggregate remote channels beside local rooms with community labels and per-community degradation; failures in one community do not hide another. The local server, not browser, calls remote origin with protected credential. Translate `not-admitted`, stale, read-only, capability and membership states into non-secret descriptors; never expose bearer/agent token. Enforce authorization on cached reads and remove/purge connection path. Acceptance: mock-Transport client contract and server integration tests cover two independent refs, hidden room, capability failure, stale cache, revoke/purge and no raw browser cross-origin call.

## PR5 — Local agent delivery (4 tasks)

### Task 5.1: [community-server] [P5] Route fresh external mentions through the existing dispatcher

**Size:** lg · **Priority:** high · **Depends on:** 4.3, 2.4

Own remote subscription bridge into local `RoomService`/`RoomTriggerDispatcher`. Cache snapshot/entry before dispatch; map remote mentions only to locally enrolled agent manifests. Reuse the existing dispatcher, runner, response gate, Stop/held-turn/tool-only paths and the same persisted `RoomTurnBudget` instance used by local rooms; no second dispatcher/budget. The bridge explicitly makes the sole dispatch decision; trigger only live eligible remote-human entries in joined channels with owner/agent authorization. Local human/agent posts, remote agent posts and read-only backends do not wake agents by default. Snapshot, process-start replay, pagination and stale cursor recovery never dispatch. Same-process reconnect may dispatch a server-timestamped entry <=30 seconds old only if subscription was active immediately before disconnect and only once per remote ID; recheck Stop, budget and enrollment. Server time is conservative freshness bound, never ordering source. Declines/notices and imported human mirrors remain local. Acceptance: real `RoomService`+dispatcher+same budget integration with deterministic runtime shows exactly one turn for fresh mention, zero after restart/snapshot/remote-agent post, bounded once-only reconnect and no turn on read-only backend; mutate gate to prove tests fail.

### Task 5.2: [community-server] [P5] Preserve external authority through held and resumed turns

**Size:** md · **Priority:** high · **Depends on:** 5.1

Own the dispatcher/runner trust seam and remote author provenance. Every remote human entering through bridge, including the install owner, carries `externalAuthor=true` through trigger, hold, resume, tool post and response gate. Unknown author remains external. Remote requests cannot select local-only aside/control paths or supply agent cwd; resolve per-agent working directory only from local manifest. Recheck local enrollment, remote membership, Stop and the shared turn budget before resumed delivery. Existing prompt fencing treats remote text as untrusted. Acceptance: deterministic integration tests force held/resumed turn, remote owner message, unknown author, malicious path/control payload and Stop midturn; assert exact power classification, cwd, budget reservation and no local-only side effect. Mutate origin guard to prove the test fails.

### Task 5.3: [community-server] [P5] Write agent output to a transactional bounded outbox

**Size:** lg · **Priority:** high · **Depends on:** 5.1, 4.2

Own local SQLite outbox schema and synchronous `RoomTriggerWriter.post`/tool posting hook. At the shared `RoomService.post`/`RoomEntryWriter` seam, add a remote-mirror write policy: suppress automatic post-commit dispatch of local agent narration and `post_to_room`/tool output, while retaining the synchronous atomic entry+outbox transaction and leaving local-room dispatch unchanged. The bridge alone explicitly dispatches eligible imported live human entries. In one local transaction write agent output and pending outbox row with remote address, acting member, original local entry, remote parent mapping, stable post/upload idempotency keys, bounded attachment references, creation/expiry and delivery state. Keep synchronous writer; never call remote HTTP inline. Cap pending at 100/community and 1000/install; a full queue fails local posting visibly before claiming delivery. Only local bounded room attachment store/projection supplies bytes, never remote path. Expire unsent rows after five minutes. Both automatic and tool posts use same path; tool cannot bypass outbox with direct remote endpoint. Acceptance: SQLite tests force transaction rollback, queue cap, tool post, attachment reference and Stop before queue; assert exact local/output/outbox rows and no HTTP call in writer. A real `RoomService`+`RoomTriggerDispatcher` test proves narration and tool posting in a remote mirror never produce a recursive/second agent turn, while equivalent local-room posting still dispatches; mutate the write-policy guard and see the test fail. Derive remote-mirror status from trusted persisted mapping or service configuration, never a caller-provided `skipDispatch` Boolean or client-controlled field. Agent output in mirrored rooms enqueues exactly once; imported remote history writes no outbox; remote clients cannot select local dispatch/control bypasses. Test these at the real service boundary.

### Task 5.4: [community-server] [P5] Deliver and reconcile outbox receipts without duplicate history

**Size:** lg · **Priority:** high · **Depends on:** 5.3

Own outbox worker and echo reconciliation. Before each attachment upload and post, recheck current local agent ownership, remote human/agent membership/connection, Stop/halt and row expiry; abort permanently unauthorized/stopped work. Upload bounded bytes first using stable upload keys, then post with original key and mapped remote parent/attachment IDs. Retry uncertain network failures at 1,2,4,8,16 then 30 seconds plus bounded jitter until five-minute expiry; do not retry permanent auth or content errors. Only remote receipt or matching SSE echo confirms shared delivery; pending/failure state remains explicit until then, with safe retry/recover action. Link local output to remote entry by key/remote ID, do not render second copy. Late remote reply after Stop/revocation is imported as authoritative history but triggers no new local delivery. Acceptance: integration tests cover timeout-after-commit, upload retry, worker restart, echo-before-receipt, receipt-before-echo, permanent 403, Stop during upload and local attachment path refusal; assert exactly one remote entry and one visible local representation.

## PR6 — App UX and acceptance (2 tasks)

### Task 6.1: [community-server] [P6] Integrate community connections and remote rooms in the local app

**Size:** lg · **Priority:** high · **Depends on:** 4.4, 5.4

Own `apps/client` Connections and channel surfaces through `Transport` only. User enters community URL, completes browser approval, returns with identity/admission status, may connect two independent communities, revoke/disconnect, and enroll/eject an existing local agent with visible human owner and channel access. Sidebar shows remote rooms beside local rooms with community label and per-community failure; opening uses `(communityRef,remoteRoomId)` for roster, bounded history/thread, read cursor, live events, authorized attachments and composer. Honor backend capability/role on controls; stale or read-only views explain why posting is unavailable. Show server-confirmed entries and clearly marked pending/failed local outbox outputs; receipt/echo resolves to one entry. Handle empty/loading/removed membership/offline/archived/pairing expiry, semantic controls, keyboard focus, dark-mode tokens and mobile/tablet/desktop layout. Never place bearer token in client state/DTO or call remote API directly. Acceptance: RTL mock-Transport tests cover each state and two-community ID collision; real browser UI tests cover pairing, remote room/thread/unread, attachment and pending→confirmed/failure, mobile navigation/accessibility tree and screenshot review.

### Task 6.2: [community-server] [P6] Verify and document the complete packaged community journey

**Size:** lg · **Priority:** high · **Depends on:** 6.1, 3.4, 5.4

Own final cross-repo integration, root registration, docs and release proof. Merge shared contract and `apps/community` package without duplicate writers; reconcile root manifest/lock, Vitest project census, Turbo jobs and server index/FakeAgentRuntime overlap with active changes #1908/#1906 against combined tree. Add real-Postgres required CI command/job with isolated migrated databases and failure if DB is missing; add community browser job with asserted nonzero test count. Document self-host Docker/Postgres/reverse-proxy TLS, env and hard limits, backup/restore, migrations/rollback, bootstrap secret rotation/removal, owner recovery with no default SMTP, optional OAuth/S3/Render, observability, `/api/v1` auth/cursor/idempotency/SSE, joining, agent enrollment, export/leave and compatibility. Keep MIT source/license and check dependency licenses. Build clean image, migrate fresh Postgres, perform two-context owner→invite→thread→attachment→unread→export/leave flow; enroll actual local test-runtime agent and confirm one reply in both browsers. Restart app and local client, prove history/blobs/credentials persist and old mentions do not run; repeat core flow with Cloud domains blocked and no paid credentials. Include real-PG concurrency/security races, local shared-budget/Stop/outbox cases, mobile/tablet/desktop accessibility and visual screenshots. Test built bundle/runtime, not dev-only imports; record commands/artifacts and subject counts. Acceptance is the full frozen checklist: exact-one owner, last-seat winner, ordered idempotent entries, immediate member/agent revocation, two independent connections, one confirmed local-agent reply, zero replay/Stop leak, and all gates green on the combined tree. Confirm first-batch contract and server edits integrate as one mergeable PR after the two disjoint authors finish.

## Private credential delivery boundary

Tasks 1.1, 2.3, 2.4, and 4.2 distinguish public CommunityAdapter/Transport DTOs from server-only `community-private-wire` response schemas. Pairing exchange and agent enrollment/rotation must deliver a one-time personal or agent token to the authorized local server credential store. Code/verifier validation protects pairing exchange; a live scoped personal bearer grant protects agent credential issuance. Browser approval, polling, management, logs, errors, and all public DTOs carry only redacted receipts. Responses carrying a credential use `no-store` and no browser CORS access. HTTP-through-Transport tests prove both successful private storage and the absence of browser/log/error exposure.
