{
  "spec": "specs/chat-input-always-editable/02-specification.md",
  "slug": "chat-input-always-editable",
  "generatedAt": "2026-03-10T12:00:00.000Z",
  "mode": "full",
  "lastDecomposeDate": null,
  "tasks": [
    {
      "id": "1.1",
      "phase": 1,
      "phaseName": "Always-Editable Input",
      "subject": "[chat-input-always-editable] [P1] Decouple disabled states and make textarea always editable during streaming",
      "description": "## Objective\n\nDecouple the single `isDisabled` boolean in `ChatInput.tsx` into two separate concerns: input acceptance and submit readiness. This allows the textarea to remain editable during streaming while still blocking submission.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/ChatInput.tsx`\n\n## Implementation Steps\n\n### 1. Replace the single `isDisabled` boolean (current line 187)\n\n**Current code:**\n```typescript\nconst isDisabled = isLoading || sessionBusy;\n```\n\n**Replace with:**\n```typescript\nconst isInputDisabled = sessionBusy; // Only server lock disables textarea\nconst isSubmitDisabled = isLoading || sessionBusy; // Blocks submission\n```\n\n### 2. Update textarea disabled prop (current line 250)\n\nChange from `disabled={isDisabled}` to `disabled={isInputDisabled}`. This means the textarea stays editable during streaming (`isLoading=true`) but locks when another client holds the session (`sessionBusy=true`).\n\n### 3. Update paperclip button disabled prop (current line 220)\n\nChange from `disabled={isDisabled}` to remove the `disabled` prop entirely. Users should always be able to stage files for the next message, even during streaming.\n\n### 4. Update clear button visibility logic (current line 184)\n\n**Current:**\n```typescript\nconst showClear = hasText && !isLoading && !sessionBusy;\n```\n\n**New:**\n```typescript\nconst showClear = hasText && !sessionBusy;\n```\n\nThe clear button should work whenever text exists, regardless of streaming state. Users need to be able to clear their draft while watching the agent work.\n\n### 5. Guard Enter key submission (current line 111-116)\n\n**Current `handleKeyDown`:**\n```typescript\nif (e.key === 'Enter' && !e.shiftKey && !isMobile) {\n  e.preventDefault();\n  if (!isLoading && value.trim()) {\n    onSubmit();\n  }\n}\n```\n\n**New — add `sessionBusy` guard:**\n```typescript\nif (e.key === 'Enter' && !e.shiftKey && !isMobile) {\n  e.preventDefault();\n  if (!isLoading && !sessionBusy && value.trim()) {\n    onSubmit();\n  }\n}\n```\n\nThis ensures Enter does nothing during streaming (Phase 1 behavior). Phase 2 will change Enter to queue during streaming.\n\n### 6. Update send button disabled logic (current line 273)\n\nThe send button's `disabled` prop currently uses `!showButton || sessionBusy`. Since `showButton` already includes `isLoading`, the button correctly stays visible but the onClick logic routes to `onStop` when loading. No change needed for the send button disabled prop, but verify the pointer-events-none class uses `isSubmitDisabled` for the non-loading case.\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/ChatInput.test.tsx`:\n\n### Tests to modify\n\n1. **\"disables textarea when loading\"** — This test currently asserts `disabled=true` when `isLoading=true`. This test must be CHANGED to expect `disabled=false` when only `isLoading=true` (textarea is now always editable during streaming). Rename to \"does NOT disable textarea when loading\".\n\n2. **\"hides clear button when loading\"** (line 270-274) — This test asserts clear is hidden during loading. CHANGE to assert clear is VISIBLE during loading (only hidden when sessionBusy).\n\n### Tests to add\n\n3. Add test: \"textarea is NOT disabled during streaming\" — render with `isLoading={true}`, assert `disabled` is `false`.\n\n4. Add test: \"textarea IS disabled when sessionBusy is true\" — render with `sessionBusy={true}`, assert `disabled` is `true`.\n\n5. Add test: \"paperclip button is NOT disabled during streaming\" — render with `isLoading={true}` and `onAttach` callback, assert paperclip button does not have `disabled` attribute.\n\n6. Add test: \"clear button works during streaming\" — render with `value='hello'` and `isLoading={true}`, assert clear button is interactive (no `pointer-events-none` class).\n\n## Acceptance Criteria\n\n- [ ] Textarea remains editable when `isLoading=true` (streaming)\n- [ ] Textarea is disabled when `sessionBusy=true`\n- [ ] Enter key does not submit during streaming\n- [ ] Clear button works during streaming\n- [ ] Paperclip button works during streaming\n- [ ] Send button correctly shows stop icon during streaming\n- [ ] All existing tests pass (with modifications noted above)\n- [ ] New tests written and passing",
      "activeForm": "Decoupling textarea disabled states from submit readiness",
      "size": "medium",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.2"]
    },
    {
      "id": "1.2",
      "phase": 1,
      "phaseName": "Always-Editable Input",
      "subject": "[chat-input-always-editable] [P1] Add dynamic placeholder and isStreaming prop threading",
      "description": "## Objective\n\nAdd a dynamic placeholder that changes based on streaming state, and thread the `isStreaming` boolean properly through the component hierarchy.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/ChatInput.tsx`\n- `apps/client/src/layers/features/chat/ui/ChatInputContainer.tsx`\n\n## Implementation Steps\n\n### 1. Add `placeholder` prop to ChatInput\n\nIn `ChatInput.tsx`, add an optional `placeholder` prop to `ChatInputProps`:\n\n```typescript\ninterface ChatInputProps {\n  // ... existing props\n  /** Custom placeholder text for the textarea. Defaults to \"Message Claude...\". */\n  placeholder?: string;\n}\n```\n\nUpdate the destructured props to include `placeholder` with a default:\n```typescript\n{\n  // ... existing props\n  placeholder = 'Message Claude...',\n}\n```\n\nUpdate the textarea element (current line 247) from hardcoded `placeholder=\"Message Claude...\"` to `placeholder={placeholder}`.\n\n### 2. Compute dynamic placeholder in ChatInputContainer\n\nIn `ChatInputContainer.tsx`, compute the `isStreaming` boolean locally from the existing `status` prop:\n\n```typescript\nconst isStreaming = status === 'streaming';\n```\n\nCompute the placeholder value based on state:\n```typescript\nconst placeholder = isStreaming\n  ? 'Compose next \\u2014 will send when ready'\n  : 'Message Claude...';\n```\n\nPass it to the `ChatInput` component:\n```typescript\n<ChatInput\n  ref={chatInputRef}\n  value={input}\n  onChange={autocomplete.handleInputChange}\n  onSubmit={handleSubmit}\n  isLoading={status === 'streaming' || isUploading}\n  sessionBusy={sessionBusy}\n  onStop={stop}\n  onEscape={autocomplete.dismissPalettes}\n  onClear={() => {\n    setInput('');\n    autocomplete.dismissPalettes();\n  }}\n  isPaletteOpen={autocomplete.isPaletteOpen}\n  onArrowUp={autocomplete.handleArrowUp}\n  onArrowDown={autocomplete.handleArrowDown}\n  onCommandSelect={autocomplete.handleKeyboardSelect}\n  activeDescendantId={autocomplete.activeDescendantId}\n  onCursorChange={autocomplete.handleCursorChange}\n  onAttach={onFilesSelected}\n  placeholder={placeholder}\n/>\n```\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/ChatInput.test.tsx`:\n\n1. **\"renders textarea with placeholder\"** — Existing test checks for `Message Claude` and should still pass (default prop).\n\n2. Add test: \"renders custom placeholder when provided\" — render with `placeholder='Compose next'`, assert `getByPlaceholderText('Compose next')` exists.\n\n3. Add test: \"uses default placeholder when not provided\" — render without `placeholder` prop, assert `getByPlaceholderText('Message Claude...')` exists.\n\n## Acceptance Criteria\n\n- [ ] Placeholder shows \"Compose next — will send when ready\" during streaming\n- [ ] Placeholder reverts to \"Message Claude...\" when idle\n- [ ] ChatInput accepts optional `placeholder` prop with correct default\n- [ ] Existing placeholder test continues to pass\n- [ ] New placeholder tests written and passing",
      "activeForm": "Adding dynamic placeholder text based on streaming state",
      "size": "small",
      "priority": "high",
      "dependencies": [],
      "parallelWith": ["1.1"]
    },
    {
      "id": "2.1",
      "phase": 2,
      "phaseName": "Message Queue Core",
      "subject": "[chat-input-always-editable] [P2] Create useMessageQueue hook with queue state and auto-flush",
      "description": "## Objective\n\nCreate the `useMessageQueue` hook that manages a FIFO message queue, provides CRUD operations for queue items, tracks editing state, auto-flushes queued messages when the agent transitions from streaming to idle, and cleans up on session/cwd changes.\n\n## Files to Create\n\n- `apps/client/src/layers/features/chat/model/use-message-queue.ts`\n\n## Full Implementation\n\n```typescript\nimport { useState, useEffect, useRef, useCallback } from 'react';\nimport type { ChatStatus } from './chat-types';\n\n/** A single item in the message queue. */\nexport interface QueueItem {\n  id: string;\n  content: string;\n  createdAt: number;\n}\n\ninterface UseMessageQueueOptions {\n  status: ChatStatus;\n  sessionBusy: boolean;\n  sessionId: string | null;\n  selectedCwd: string | null;\n  onFlush: (content: string) => void;\n}\n\ninterface UseMessageQueueReturn {\n  queue: QueueItem[];\n  editingIndex: number | null;\n  addToQueue: (content: string) => void;\n  updateQueueItem: (index: number, content: string) => void;\n  removeFromQueue: (index: number) => void;\n  startEditing: (index: number) => string;\n  cancelEditing: () => void;\n  saveEditing: (content: string) => void;\n  clearQueue: () => void;\n}\n```\n\n### State Management\n\n- `queue` via `useState<QueueItem[]>([])` — triggers renders for QueuePanel\n- `editingIndex` via `useState<number | null>(null)` — which queue item is loaded in textarea\n- `onFlushRef` via `useRef` — avoids stale closure in auto-flush effect\n- `prevStatusRef` via `useRef<ChatStatus>('idle')` — detects idle transition\n\n### Auto-Flush Effect\n\nThe auto-flush fires when `status` transitions from `'streaming'` to `'idle'`. It takes the first non-editing item from the queue, prepends a timing annotation, removes it from the queue, adjusts `editingIndex` if needed, and calls `onFlushRef.current(annotatedContent)`.\n\n```typescript\nuseEffect(() => {\n  if (prevStatusRef.current === 'streaming' && status === 'idle') {\n    if (queue.length > 0 && !sessionBusy) {\n      const firstNonEditing = editingIndex === 0\n        ? queue.length > 1 ? 1 : null\n        : 0;\n      if (firstNonEditing !== null) {\n        const item = queue[firstNonEditing];\n        const annotated = `[Note: This message was composed while the agent was responding to the previous message]\\n\\n${item.content}`;\n        setQueue(prev => prev.filter((_, i) => i !== firstNonEditing));\n        if (editingIndex !== null && editingIndex > firstNonEditing) {\n          setEditingIndex(prev => prev !== null ? prev - 1 : null);\n        }\n        onFlushRef.current(annotated);\n      }\n    }\n  }\n  prevStatusRef.current = status;\n}, [status, sessionBusy, queue, editingIndex]);\n```\n\n### Cleanup Effect\n\nClear queue on session or cwd change:\n```typescript\nuseEffect(() => {\n  setQueue([]);\n  setEditingIndex(null);\n}, [sessionId, selectedCwd]);\n```\n\n### Method Implementations\n\n- `addToQueue(content)`: If `!content.trim()` return early. Otherwise append `{ id: crypto.randomUUID(), content, createdAt: Date.now() }` to queue.\n- `updateQueueItem(index, content)`: Replace content at index using functional setState, preserve `id` and `createdAt`.\n- `removeFromQueue(index)`: Filter out item at index. If `editingIndex === index`, set `editingIndex = null`. If `editingIndex > index`, decrement `editingIndex`.\n- `startEditing(index)`: Set `editingIndex = index`, return `queue[index].content` so the caller can load it into the textarea.\n- `cancelEditing()`: Set `editingIndex = null`.\n- `saveEditing(content)`: Call `updateQueueItem(editingIndex!, content)`, then set `editingIndex = null`.\n- `clearQueue()`: Set `queue = []`, `editingIndex = null`.\n\nAll methods should be wrapped in `useCallback` with appropriate dependencies.\n\n## Testing Requirements\n\nCreate `apps/client/src/layers/features/chat/__tests__/use-message-queue.test.ts`:\n\nUse `renderHook` from `@testing-library/react` with `act()` for state updates.\n\n```typescript\n// @vitest-environment jsdom\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { renderHook, act } from '@testing-library/react';\nimport { useMessageQueue } from '../model/use-message-queue';\n```\n\nDefault options for tests:\n```typescript\nconst defaultOptions = {\n  status: 'idle' as const,\n  sessionBusy: false,\n  sessionId: 'test-session',\n  selectedCwd: '/test/dir',\n  onFlush: vi.fn(),\n};\n```\n\n### Required test cases\n\n1. `addToQueue appends item with unique id and content` — add two items, verify queue length is 2, each has a unique `id`, `content` matches, and `createdAt` is a number.\n2. `addToQueue with empty string is rejected` — call `addToQueue('')`, verify queue length remains 0.\n3. `updateQueueItem modifies content at index, preserves id` — add item, capture its id, update content, verify id unchanged and content updated.\n4. `removeFromQueue removes item and adjusts editingIndex` — add 3 items, start editing index 2, remove index 0, verify queue has 2 items and editingIndex is now 1.\n5. `removeFromQueue when editing the removed item resets editingIndex to null` — add 2 items, start editing index 0, remove index 0, verify editingIndex is null.\n6. `removeFromQueue when editing item after removed one decrements editingIndex` — add 3 items, start editing index 2, remove index 1, verify editingIndex is 1.\n7. `startEditing sets editingIndex and returns content` — add item with content 'test', call startEditing(0), verify return value is 'test' and editingIndex is 0.\n8. `cancelEditing resets editingIndex to null` — startEditing, then cancelEditing, verify editingIndex is null.\n9. `saveEditing updates item content and resets editingIndex` — add item, startEditing(0), saveEditing('updated'), verify queue[0].content is 'updated' and editingIndex is null.\n10. `auto-flush fires on streaming to idle transition` — render with status 'streaming', add an item to queue, then rerender with status 'idle', verify onFlush was called with annotated content.\n11. `auto-flush prepends timing annotation to flushed content` — verify the flushed string starts with '[Note: This message was composed while the agent was responding to the previous message]'.\n12. `auto-flush skips when sessionBusy is true` — render with status 'streaming' and sessionBusy true, add item, transition to idle, verify onFlush not called.\n13. `auto-flush skips the item being edited` — add 2 items, start editing index 0, transition streaming to idle, verify onFlush is called with the second item's content (index 1).\n14. `auto-flush does nothing when queue is empty` — transition streaming to idle with empty queue, verify onFlush not called.\n15. `queue clears on sessionId change` — add items, rerender with different sessionId, verify queue is empty.\n16. `queue clears on selectedCwd change` — add items, rerender with different cwd, verify queue is empty.\n\n## Acceptance Criteria\n\n- [ ] Hook is created at `apps/client/src/layers/features/chat/model/use-message-queue.ts`\n- [ ] All CRUD methods work correctly\n- [ ] Auto-flush fires only on streaming-to-idle transition\n- [ ] Timing annotation is prepended to flushed content\n- [ ] Auto-flush skips items being edited\n- [ ] Queue clears on session/cwd change\n- [ ] All 16 tests written and passing\n- [ ] `QueueItem` type is exported",
      "activeForm": "Creating the useMessageQueue hook with auto-flush logic",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.2"]
    },
    {
      "id": "2.2",
      "phase": 2,
      "phaseName": "Message Queue Core",
      "subject": "[chat-input-always-editable] [P2] Add submitContent method to useChatSession for direct content submission",
      "description": "## Objective\n\nAdd a `submitContent` method to `useChatSession` that accepts content directly as a string parameter, bypassing the `input` state. This is needed for the auto-flush mechanism in `useMessageQueue`, which must submit queued messages without manipulating the textarea's `input` state.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/model/use-chat-session.ts`\n- `apps/client/src/layers/features/chat/model/chat-types.ts` (if needed for type export)\n\n## Implementation Steps\n\n### 1. Add `submitContent` method inside `useChatSession`\n\nThe existing `handleSubmit` (line 340) reads from `input` state and guards with `if (!input.trim() || status === 'streaming') return`. The new `submitContent` method accepts content directly:\n\n```typescript\nconst submitContent = useCallback(async (content: string) => {\n  if (!content.trim() || status === 'streaming') return;\n\n  // Create session on first message if no active session\n  let targetSessionId = sessionId;\n  if (!targetSessionId) {\n    targetSessionId = crypto.randomUUID();\n    const now = new Date().toISOString();\n    insertOptimisticSession(queryClient, selectedCwdRef.current, {\n      id: targetSessionId,\n      title: `Session ${targetSessionId.slice(0, 8)}`,\n      createdAt: now,\n      updatedAt: now,\n      permissionMode: 'default',\n    });\n    onSessionIdChangeRef.current?.(targetSessionId);\n  }\n\n  const userContent = content.trim();\n  const userMessage: ChatMessage = {\n    id: crypto.randomUUID(),\n    role: 'user',\n    content: userContent,\n    parts: [{ type: 'text', text: userContent }],\n    timestamp: new Date().toISOString(),\n  };\n\n  setMessages((prev) => [...prev, userMessage]);\n  // Do NOT clear input — submitContent is for programmatic submission (queue flush),\n  // the textarea may have a user's current draft.\n  setStatus('streaming');\n  statusRef.current = 'streaming';\n  setError(null);\n  currentPartsRef.current = [];\n  const streamStart = Date.now();\n  streamStartTimeRef.current = streamStart;\n  estimatedTokensRef.current = 0;\n  setStreamStartTime(streamStart);\n  setEstimatedTokens(0);\n\n  assistantIdRef.current = crypto.randomUUID();\n  assistantCreatedRef.current = false;\n\n  const abortController = new AbortController();\n  abortRef.current = abortController;\n\n  try {\n    // Apply transform (e.g., file prefix) to content\n    const finalContent = transformContentRef.current\n      ? await transformContentRef.current(userContent)\n      : userContent;\n\n    if (finalContent !== userContent) {\n      setMessages((prev) =>\n        prev.map((m) => (m.id === userMessage.id ? { ...m, content: finalContent } : m))\n      );\n    }\n\n    if (relayEnabled) {\n      const correlationId = crypto.randomUUID();\n      correlationIdRef.current = correlationId;\n      streamReadyRef.current = false;\n      await waitForStreamReady(streamReadyRef, 5000);\n      await transport.sendMessageRelay(targetSessionId, finalContent, {\n        clientId: clientIdRef.current,\n        correlationId,\n        cwd: selectedCwdRef.current ?? undefined,\n      });\n      resetStalenessTimer();\n    } else {\n      await transport.sendMessage(\n        targetSessionId,\n        finalContent,\n        (event) => streamEventHandler(event.type, event.data, assistantIdRef.current),\n        abortController.signal,\n        selectedCwd ?? undefined\n      );\n      setStatus('idle');\n    }\n  } catch (err) {\n    if ((err as Error).name !== 'AbortError') {\n      if ((err as { code?: string }).code === 'SESSION_LOCKED') {\n        setSessionBusy(true);\n        // Don't restore input for programmatic submission\n        if (sessionBusyTimerRef.current) clearTimeout(sessionBusyTimerRef.current);\n        sessionBusyTimerRef.current = setTimeout(() => {\n          setSessionBusy(false);\n          sessionBusyTimerRef.current = null;\n        }, TIMING.SESSION_BUSY_CLEAR_MS);\n      } else {\n        setError((err as Error).message);\n      }\n      setStatus('error');\n    }\n    if (textStreamingTimerRef.current) clearTimeout(textStreamingTimerRef.current);\n    isTextStreamingRef.current = false;\n    setIsTextStreaming(false);\n  }\n}, [status, sessionId, relayEnabled, streamEventHandler, queryClient, resetStalenessTimer]);\n```\n\n### 2. Return `submitContent` from the hook\n\nAdd `submitContent` to the return object:\n```typescript\nreturn {\n  messages,\n  input,\n  setInput,\n  handleSubmit,\n  submitContent, // NEW\n  status,\n  error,\n  sessionBusy,\n  stop,\n  isLoadingHistory,\n  sessionStatus,\n  streamStartTime,\n  estimatedTokens,\n  isTextStreaming,\n  isWaitingForUser,\n  waitingType,\n  activeInteraction,\n  markToolCallResponded,\n};\n```\n\n### 3. Consider extracting shared submission logic\n\nThe `handleSubmit` and `submitContent` methods share significant logic. To keep the code DRY, extract the common submission logic into a private helper function within the hook:\n\n```typescript\nconst executeSubmission = useCallback(async (content: string, clearInput: boolean) => {\n  // ... shared logic ...\n  if (clearInput) setInput('');\n  // ... rest of submission\n}, [/* deps */]);\n```\n\nThen `handleSubmit` calls `executeSubmission(input, true)` and `submitContent` calls `executeSubmission(content, false)`. This avoids duplicating 60+ lines.\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/use-chat-session.test.tsx`:\n\nNote: This file already exists and has mock patterns established. Add tests:\n\n1. `submitContent sends message with provided content` — call `submitContent('test message')`, verify the transport's `sendMessage` or `sendMessageRelay` was called with 'test message'.\n2. `submitContent does not clear input state` — set input to 'my draft', call `submitContent('queued msg')`, verify `input` state is still 'my draft'.\n3. `submitContent guards against streaming status` — set status to streaming, call `submitContent('msg')`, verify no transport call is made.\n4. `submitContent guards against empty content` — call `submitContent('')`, verify no transport call.\n\n## Acceptance Criteria\n\n- [ ] `submitContent` method added to `useChatSession` return value\n- [ ] Method accepts content string directly and submits without touching `input` state\n- [ ] Same streaming guard as `handleSubmit` (rejects when streaming)\n- [ ] Same session creation logic for first message\n- [ ] Same error handling (session locked, abort, etc.)\n- [ ] Shared submission logic is DRY (extracted helper or equivalent)\n- [ ] Tests written and passing",
      "activeForm": "Adding submitContent method for programmatic message submission",
      "size": "medium",
      "priority": "high",
      "dependencies": ["1.1"],
      "parallelWith": ["2.1"]
    },
    {
      "id": "2.3",
      "phase": 2,
      "phaseName": "Message Queue Core",
      "subject": "[chat-input-always-editable] [P2] Create QueuePanel component with stagger animations",
      "description": "## Objective\n\nCreate the `QueuePanel` component that renders inline cards above the textarea showing queued messages with stagger animations, selection state, and remove buttons.\n\n## Files to Create\n\n- `apps/client/src/layers/features/chat/ui/QueuePanel.tsx`\n\n## Implementation\n\n```typescript\nimport { AnimatePresence, motion } from 'motion/react';\nimport { X } from 'lucide-react';\nimport { cn } from '@/layers/shared/lib';\nimport type { QueueItem } from '../model/use-message-queue';\n\ninterface QueuePanelProps {\n  queue: QueueItem[];\n  editingIndex: number | null;\n  onEdit: (index: number) => void;\n  onRemove: (index: number) => void;\n}\n\nconst staggerContainer = {\n  animate: {\n    transition: { staggerChildren: 0.05 },\n  },\n};\n\nconst staggerChild = {\n  initial: { opacity: 0, y: -4 },\n  animate: {\n    opacity: 1,\n    y: 0,\n    transition: { type: 'spring', stiffness: 320, damping: 28 },\n  },\n  exit: { opacity: 0, scale: 0.95, transition: { duration: 0.15 } },\n};\n\n/** Inline queue card list rendered above the chat textarea. */\nexport function QueuePanel({ queue, editingIndex, onEdit, onRemove }: QueuePanelProps) {\n  if (queue.length === 0) return null;\n\n  return (\n    <AnimatePresence mode=\"popLayout\">\n      <motion.div\n        initial={{ opacity: 0, height: 0 }}\n        animate={{ opacity: 1, height: 'auto' }}\n        exit={{ opacity: 0, height: 0 }}\n        transition={{ duration: 0.2 }}\n        className=\"mb-1.5 overflow-hidden\"\n        layout\n      >\n        <div className=\"text-muted-foreground mb-1 text-xs font-medium\">\n          Queued ({queue.length})\n        </div>\n        <motion.div variants={staggerContainer} initial=\"initial\" animate=\"animate\" className=\"space-y-0.5\">\n          <AnimatePresence mode=\"popLayout\">\n            {queue.map((item, i) => (\n              <motion.div\n                key={item.id}\n                variants={staggerChild}\n                exit={staggerChild.exit}\n                layout\n              >\n                <button\n                  type=\"button\"\n                  onClick={() => onEdit(i)}\n                  className={cn(\n                    'group flex w-full items-center gap-2 rounded-md px-3 py-1.5 text-left transition-colors',\n                    editingIndex === i\n                      ? 'border-primary bg-muted border-l-2'\n                      : 'hover:bg-muted/50'\n                  )}\n                >\n                  <span className=\"text-muted-foreground shrink-0 text-xs font-medium\">\n                    {i + 1}.\n                  </span>\n                  <span className=\"text-muted-foreground line-clamp-1 flex-1 text-sm\">\n                    {item.content}\n                  </span>\n                  <span\n                    role=\"button\"\n                    tabIndex={0}\n                    onClick={(e) => {\n                      e.stopPropagation();\n                      onRemove(i);\n                    }}\n                    onKeyDown={(e) => {\n                      if (e.key === 'Enter' || e.key === ' ') {\n                        e.stopPropagation();\n                        e.preventDefault();\n                        onRemove(i);\n                      }\n                    }}\n                    className=\"text-muted-foreground hover:text-foreground shrink-0 rounded-sm p-0.5 opacity-0 transition-opacity group-hover:opacity-100\"\n                    aria-label={`Remove queued message ${i + 1}`}\n                  >\n                    <X className=\"size-3\" />\n                  </span>\n                </button>\n              </motion.div>\n            ))}\n          </AnimatePresence>\n        </motion.div>\n      </motion.div>\n    </AnimatePresence>\n  );\n}\n```\n\n### Key Design Decisions\n\n- **QueueCard is inline** — not a separate component since it's only 15 lines of JSX. Can be extracted later if it grows.\n- **Button wraps entire card** — clicking anywhere on the card triggers `onEdit`.\n- **Remove button uses `stopPropagation`** — prevents the card click handler from firing when x is clicked.\n- **Remove button is keyboard accessible** — `role=\"button\"`, `tabIndex={0}`, `onKeyDown` for Enter/Space.\n- **Remove button hover-gated** — `opacity-0 group-hover:opacity-100` on desktop. For mobile, this will need to be always visible (handled in Phase 3 polish).\n- **Stable keys** — Uses `item.id` (crypto.randomUUID) for React keys, not array index.\n- **AnimatePresence `popLayout`** — smooth reflow when items are removed.\n- **Compact padding** — `px-3 py-1.5` fits the 8pt grid (12px vertical, 24px horizontal).\n\n## Testing Requirements\n\nCreate `apps/client/src/layers/features/chat/__tests__/QueuePanel.test.tsx`:\n\n```typescript\n// @vitest-environment jsdom\nimport { describe, it, expect, vi, afterEach, beforeAll } from 'vitest';\nimport { render, screen, fireEvent, cleanup } from '@testing-library/react';\nimport { QueuePanel } from '../ui/QueuePanel';\nimport type { QueueItem } from '../model/use-message-queue';\n```\n\n### Required test cases\n\n1. `renders nothing when queue is empty` — render with `queue=[]`, verify no output (container returns null).\n2. `renders card for each queue item with truncated text` — render with 3 items, verify 3 cards exist with correct content text.\n3. `renders \"Queued (N)\" header with correct count` — render with 2 items, verify text \"Queued (2)\" is present.\n4. `clicking card calls onEdit with correct index` — render with 2 items, click the second card, verify `onEdit` called with `1`.\n5. `clicking x button calls onRemove with correct index` — render with 2 items, click the x button on first card, verify `onRemove` called with `0` and `onEdit` NOT called.\n6. `editing item shows selected state` — render with `editingIndex=1`, verify the second card has `border-l-2` and `bg-muted` classes.\n7. `non-editing items do not have selected state` — render with `editingIndex=0`, verify the second card does NOT have `border-l-2` class.\n\nHelper factory:\n```typescript\nconst makeItem = (content: string, index: number): QueueItem => ({\n  id: `id-${index}`,\n  content,\n  createdAt: Date.now() + index,\n});\n```\n\n## Acceptance Criteria\n\n- [ ] QueuePanel renders nothing when queue is empty\n- [ ] Cards display numbered, truncated preview text\n- [ ] Cards are clickable to trigger editing\n- [ ] Remove button stops propagation and calls onRemove\n- [ ] Editing state shows visual differentiation (left border + background)\n- [ ] Stagger animations use spring physics (stiffness: 320, damping: 28)\n- [ ] Stable keys via `item.id`\n- [ ] All 7 tests written and passing",
      "activeForm": "Creating the QueuePanel component with inline card animations",
      "size": "medium",
      "priority": "high",
      "dependencies": ["2.1"],
      "parallelWith": ["2.4"]
    },
    {
      "id": "2.4",
      "phase": 2,
      "phaseName": "Message Queue Core",
      "subject": "[chat-input-always-editable] [P2] Implement three-state button and queue-aware Enter key in ChatInput",
      "description": "## Objective\n\nTransform the ChatInput send button from a two-state (send/stop) model to a four-state (send/stop/queue/update) model, and update the Enter key behavior to support queueing during streaming and saving edits.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/ChatInput.tsx`\n\n## Implementation Steps\n\n### 1. Update ChatInputProps interface\n\nReplace `isLoading: boolean` with more granular props:\n\n```typescript\ninterface ChatInputProps {\n  value: string;\n  onChange: (value: string) => void;\n  onSubmit: () => void;\n  isStreaming: boolean;        // Agent is streaming (replaces isLoading)\n  isUploading?: boolean;       // File upload in progress\n  sessionBusy?: boolean;\n  editingQueueItem?: boolean;  // Currently editing a queue item\n  queueDepth?: number;         // Number of items in queue (for badge)\n  onStop?: () => void;\n  onQueue?: () => void;        // Queue the current input\n  onSaveEdit?: () => void;     // Save the queue item being edited\n  onCancelEdit?: () => void;   // Cancel editing queue item\n  onEscape?: () => void;\n  onClear?: () => void;\n  isPaletteOpen?: boolean;\n  onArrowUp?: () => void;\n  onArrowDown?: () => void;\n  onCommandSelect?: () => void;\n  activeDescendantId?: string;\n  onCursorChange?: (pos: number) => void;\n  onAttach?: (files: File[]) => void;\n  placeholder?: string;\n  // Queue navigation props\n  onQueueNavigateUp?: () => void;\n  onQueueNavigateDown?: () => void;\n  queueHasItems?: boolean;\n}\n```\n\nNote: `isLoading` is replaced by `isStreaming`. The uploading state is a separate concern handled by `isUploading`.\n\n### 2. Derive button state\n\nReplace the current `Icon` and button logic with a state machine:\n\n```typescript\ntype ButtonState = 'send' | 'stop' | 'queue' | 'update' | 'hidden';\n\nconst isLoading = isStreaming || (isUploading ?? false);\nconst buttonState: ButtonState = (() => {\n  if (editingQueueItem && hasText) return 'update';\n  if (isStreaming && hasText) return 'queue';\n  if (isLoading) return 'stop';\n  if (hasText) return 'send';\n  return 'hidden';\n})();\n\nconst showButton = buttonState !== 'hidden';\n```\n\n### 3. Update button rendering\n\nImport additional icons:\n```typescript\nimport { ArrowUp, CornerDownLeft, Square, X, Paperclip, Clock, Check } from 'lucide-react';\n```\n\nButton icon and style mapping:\n```typescript\nconst SendIcon = isMobile ? ArrowUp : CornerDownLeft;\n\nconst buttonConfig = {\n  send: { icon: SendIcon, className: 'bg-primary text-primary-foreground hover:bg-primary/90', label: 'Send message', onClick: onSubmit },\n  stop: { icon: Square, className: 'bg-destructive text-destructive-foreground hover:bg-destructive/90', label: 'Stop generating', onClick: onStop },\n  queue: { icon: Clock, className: 'bg-muted text-muted-foreground hover:bg-muted/80', label: 'Queue message', onClick: onQueue },\n  update: { icon: Check, className: 'bg-primary text-primary-foreground hover:bg-primary/90', label: 'Save edit', onClick: onSaveEdit },\n  hidden: { icon: SendIcon, className: '', label: '', onClick: undefined },\n};\n\nconst config = buttonConfig[buttonState];\nconst ButtonIcon = config.icon;\n```\n\nRender the button with a `relative` container for the badge:\n```typescript\n<div className=\"relative\">\n  <motion.button\n    animate={{ opacity: showButton ? 1 : 0, scale: showButton ? 1 : 0.8 }}\n    transition={{ duration: 0.15 }}\n    whileHover={showButton && !sessionBusy ? { scale: 1.1 } : undefined}\n    whileTap={showButton && !sessionBusy ? { scale: 0.9 } : undefined}\n    onClick={config.onClick}\n    disabled={!showButton || sessionBusy}\n    className={cn(\n      'shrink-0 rounded-lg p-1.5 transition-colors max-md:p-2',\n      config.className,\n      (!showButton || sessionBusy) && 'pointer-events-none opacity-50'\n    )}\n    aria-label={config.label}\n  >\n    <ButtonIcon className=\"size-(--size-icon-sm)\" />\n  </motion.button>\n  {(queueDepth ?? 0) > 0 && buttonState === 'queue' && (\n    <span className=\"absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-foreground text-background text-[10px] font-medium\">\n      {queueDepth}\n    </span>\n  )}\n</div>\n```\n\n### 4. Update Enter key behavior\n\nReplace the current Enter key handler (from Phase 1) with the priority chain:\n\n```typescript\nif (e.key === 'Enter' && !e.shiftKey && !isMobile) {\n  e.preventDefault();\n  if (editingQueueItem && value.trim()) {\n    onSaveEdit?.();\n  } else if (isStreaming && value.trim()) {\n    onQueue?.();\n  } else if (!isStreaming && !sessionBusy && value.trim()) {\n    onSubmit();\n  }\n}\n```\n\nPriority: editing save > queue > submit.\n\n### 5. Add editing visual state\n\nWhen `editingQueueItem` is true, add an accent border to the textarea wrapper:\n\n```tsx\n<div\n  className={cn(\n    'border-input flex items-end gap-1.5 rounded-md border bg-transparent p-1.5 shadow-xs transition-[color,box-shadow]',\n    isFocused && 'border-ring ring-ring/50 ring-[3px]',\n    editingQueueItem && 'border-primary/40',\n    !onAttach && 'pl-3'\n  )}\n>\n```\n\nAdd an editing label above the textarea row (inside the flex column):\n```tsx\n{editingQueueItem && (\n  <div className=\"text-muted-foreground px-0.5 text-xs\">\n    Editing message {(queueDepth ?? 0) > 0 ? `—` : ''}\n  </div>\n)}\n```\n\n### 6. Update isInputDisabled and isSubmitDisabled\n\n```typescript\nconst isInputDisabled = sessionBusy;\nconst isSubmitDisabled = isLoading || sessionBusy;\n```\n\nThese remain unchanged from Phase 1.\n\n### 7. Update handleKeyDown dependencies\n\nAdd the new callback props to the dependency array of `handleKeyDown`.\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/ChatInput.test.tsx`:\n\n### Update existing tests\n- All tests that pass `isLoading` must be updated to pass `isStreaming` instead. Also add `isUploading={false}` where needed.\n\n### New test cases\n\n1. `Enter key queues message during streaming when onQueue provided` — render with `isStreaming={true}`, `value='test'`, `onQueue=vi.fn()`, press Enter, verify `onQueue` called.\n2. `Enter key saves edit when editingQueueItem is true` — render with `editingQueueItem={true}`, `value='edited'`, `onSaveEdit=vi.fn()`, press Enter, verify `onSaveEdit` called.\n3. `Enter key prioritizes edit save over queue` — render with `editingQueueItem={true}`, `isStreaming={true}`, `value='text'`, both `onSaveEdit` and `onQueue`, press Enter, verify only `onSaveEdit` called.\n4. `send button shows Queue icon during streaming with text` — render with `isStreaming={true}`, `value='text'`, verify `aria-label` is 'Queue message'.\n5. `send button shows Update icon when editing queue item` — render with `editingQueueItem={true}`, `value='text'`, verify `aria-label` is 'Save edit'.\n6. `send button shows Stop icon during streaming without text` — render with `isStreaming={true}`, `value=''`, verify `aria-label` is 'Stop generating'.\n7. `queue badge renders with correct count` — render with `isStreaming={true}`, `value='text'`, `queueDepth={3}`, verify a span with text '3' exists.\n8. `queue badge not rendered when queueDepth is 0` — render with `isStreaming={true}`, `value='text'`, `queueDepth={0}`, verify no badge span.\n9. `editing label shows when editingQueueItem is true` — render with `editingQueueItem={true}`, `value='text'`, verify text containing 'Editing message' is present.\n10. `editing border applied when editingQueueItem is true` — render with `editingQueueItem={true}`, verify textarea wrapper has `border-primary/40` class.\n\n## Acceptance Criteria\n\n- [ ] Button state machine correctly derives send/stop/queue/update/hidden\n- [ ] Enter key priority chain: edit save > queue > submit\n- [ ] Queue badge renders on queue button with correct count\n- [ ] Editing label appears above textarea when editing\n- [ ] Editing border accent applied to textarea wrapper\n- [ ] All existing tests updated for `isStreaming` prop rename\n- [ ] 10 new tests written and passing",
      "activeForm": "Implementing the three-state button and queue-aware Enter key",
      "size": "large",
      "priority": "high",
      "dependencies": ["1.1", "1.2"],
      "parallelWith": ["2.3"]
    },
    {
      "id": "2.5",
      "phase": 2,
      "phaseName": "Message Queue Core",
      "subject": "[chat-input-always-editable] [P2] Wire useMessageQueue into ChatPanel and thread props through ChatInputContainer",
      "description": "## Objective\n\nIntegrate `useMessageQueue` into `ChatPanel`, thread all queue state and handlers down through `ChatInputContainer`, render the `QueuePanel`, and wire the `onFlush` callback to `submitContent`.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/ChatPanel.tsx`\n- `apps/client/src/layers/features/chat/ui/ChatInputContainer.tsx`\n\n## Implementation Steps\n\n### 1. Import and instantiate `useMessageQueue` in `ChatPanel.tsx`\n\nAdd imports:\n```typescript\nimport { useMessageQueue } from '../model/use-message-queue';\nimport type { QueueItem } from '../model/use-message-queue';\n```\n\nAfter the `useChatSession` call, instantiate the queue hook:\n```typescript\nconst messageQueue = useMessageQueue({\n  status,\n  sessionBusy,\n  sessionId,\n  selectedCwd: cwd,\n  onFlush: submitContent, // From useChatSession\n});\n```\n\n### 2. Add draft preservation ref and navigation handlers\n\n```typescript\nconst draftRef = useRef('');\n\nconst handleQueueNavigateUp = useCallback(() => {\n  if (messageQueue.editingIndex === null) {\n    // Entering queue from new composition — save draft\n    draftRef.current = input;\n    const content = messageQueue.startEditing(messageQueue.queue.length - 1);\n    setInput(content);\n  } else if (messageQueue.editingIndex > 0) {\n    // Navigate to older item\n    const content = messageQueue.startEditing(messageQueue.editingIndex - 1);\n    setInput(content);\n  } else {\n    // At oldest item — wrap to composing new\n    messageQueue.cancelEditing();\n    setInput(draftRef.current);\n  }\n}, [input, messageQueue, setInput]);\n\nconst handleQueueNavigateDown = useCallback(() => {\n  if (messageQueue.editingIndex !== null) {\n    if (messageQueue.editingIndex < messageQueue.queue.length - 1) {\n      // Navigate to newer item\n      const content = messageQueue.startEditing(messageQueue.editingIndex + 1);\n      setInput(content);\n    } else {\n      // At newest item — return to composing new\n      messageQueue.cancelEditing();\n      setInput(draftRef.current);\n    }\n  }\n}, [messageQueue, setInput]);\n```\n\n### 3. Create queue action handlers\n\n```typescript\nconst handleQueue = useCallback(() => {\n  if (input.trim()) {\n    messageQueue.addToQueue(input.trim());\n    setInput('');\n  }\n}, [input, messageQueue, setInput]);\n\nconst handleQueueEdit = useCallback((index: number) => {\n  // Save current draft before loading queue item\n  if (messageQueue.editingIndex === null) {\n    draftRef.current = input;\n  }\n  const content = messageQueue.startEditing(index);\n  setInput(content);\n  chatInputRef.current?.focus();\n}, [input, messageQueue, setInput]);\n\nconst handleQueueSaveEdit = useCallback(() => {\n  if (messageQueue.editingIndex !== null && input.trim()) {\n    messageQueue.saveEditing(input.trim());\n    setInput(draftRef.current);\n  }\n}, [input, messageQueue, setInput]);\n\nconst handleQueueCancelEdit = useCallback(() => {\n  messageQueue.cancelEditing();\n  setInput(draftRef.current);\n}, [messageQueue, setInput]);\n\nconst handleQueueRemove = useCallback((index: number) => {\n  messageQueue.removeFromQueue(index);\n  // If we were editing the removed item, restore draft\n  if (messageQueue.editingIndex === index) {\n    setInput(draftRef.current);\n  }\n}, [messageQueue, setInput]);\n```\n\n### 4. Update ChatInputContainer props interface\n\nAdd queue-related props to `ChatInputContainerProps`:\n\n```typescript\ninterface ChatInputContainerProps {\n  // ... existing props\n  queue: QueueItem[];\n  editingIndex: number | null;\n  onQueue: () => void;\n  onQueueRemove: (index: number) => void;\n  onQueueEdit: (index: number) => void;\n  onQueueSaveEdit: () => void;\n  onQueueCancelEdit: () => void;\n  onQueueNavigateUp: () => void;\n  onQueueNavigateDown: () => void;\n}\n```\n\n### 5. Render QueuePanel in ChatInputContainer\n\nImport QueuePanel:\n```typescript\nimport { QueuePanel } from './QueuePanel';\n```\n\nRender between FileChipBar and ChatInput:\n```tsx\n{pendingFiles.length > 0 && (\n  <FileChipBar files={pendingFiles} onRemove={onFileRemove} />\n)}\n\n<QueuePanel\n  queue={queue}\n  editingIndex={editingIndex}\n  onEdit={onQueueEdit}\n  onRemove={onQueueRemove}\n/>\n\n<ChatInput\n  ref={chatInputRef}\n  // ... existing props\n/>\n```\n\n### 6. Thread queue props to ChatInput\n\nUpdate the ChatInput invocation in ChatInputContainer:\n\n```tsx\n<ChatInput\n  ref={chatInputRef}\n  value={input}\n  onChange={autocomplete.handleInputChange}\n  onSubmit={handleSubmit}\n  isStreaming={status === 'streaming'}\n  isUploading={isUploading}\n  sessionBusy={sessionBusy}\n  onStop={stop}\n  onEscape={autocomplete.dismissPalettes}\n  onClear={() => {\n    setInput('');\n    autocomplete.dismissPalettes();\n  }}\n  isPaletteOpen={autocomplete.isPaletteOpen}\n  onArrowUp={autocomplete.handleArrowUp}\n  onArrowDown={autocomplete.handleArrowDown}\n  onCommandSelect={autocomplete.handleKeyboardSelect}\n  activeDescendantId={autocomplete.activeDescendantId}\n  onCursorChange={autocomplete.handleCursorChange}\n  onAttach={onFilesSelected}\n  placeholder={placeholder}\n  editingQueueItem={editingIndex !== null}\n  queueDepth={queue.length}\n  onQueue={onQueue}\n  onSaveEdit={onQueueSaveEdit}\n  onCancelEdit={onQueueCancelEdit}\n  onQueueNavigateUp={onQueueNavigateUp}\n  onQueueNavigateDown={onQueueNavigateDown}\n  queueHasItems={queue.length > 0}\n/>\n```\n\n### 7. Update dynamic placeholder with queue count\n\n```typescript\nconst isStreaming = status === 'streaming';\nconst placeholder = (() => {\n  if (editingIndex !== null) return '';\n  if (isStreaming && queue.length > 0) {\n    return `Compose another \\u2014 ${queue.length} queued`;\n  }\n  if (isStreaming) return 'Compose next \\u2014 will send when ready';\n  return 'Message Claude...';\n})();\n```\n\n### 8. Thread all props from ChatPanel to ChatInputContainer\n\n```tsx\n<ChatInputContainer\n  chatInputRef={chatInputRef}\n  input={input}\n  autocomplete={autocomplete}\n  handleSubmit={handleSubmit}\n  status={status}\n  sessionBusy={sessionBusy}\n  stop={stop}\n  setInput={setInput}\n  sessionId={sessionId ?? ''}\n  sessionStatus={sessionStatus}\n  pendingFiles={fileUpload.pendingFiles}\n  onFilesSelected={fileUpload.addFiles}\n  onFileRemove={fileUpload.removeFile}\n  isUploading={fileUpload.isUploading}\n  queue={messageQueue.queue}\n  editingIndex={messageQueue.editingIndex}\n  onQueue={handleQueue}\n  onQueueRemove={handleQueueRemove}\n  onQueueEdit={handleQueueEdit}\n  onQueueSaveEdit={handleQueueSaveEdit}\n  onQueueCancelEdit={handleQueueCancelEdit}\n  onQueueNavigateUp={handleQueueNavigateUp}\n  onQueueNavigateDown={handleQueueNavigateDown}\n/>\n```\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/ChatPanel.test.tsx`:\n\nThe existing tests mock child components, so integration testing here is limited. However, add:\n\n1. Verify that `ChatInputContainer` (or its mock) receives the queue-related props.\n2. If testing with real components (not mocks), test that typing during streaming and pressing Enter adds a card to QueuePanel.\n\nAlternatively, most integration testing happens through the hook tests (2.1) and component tests (2.3, 2.4). The wiring task is primarily a structural integration.\n\n## Acceptance Criteria\n\n- [ ] `useMessageQueue` is instantiated in ChatPanel with correct options\n- [ ] `submitContent` is used as the `onFlush` callback\n- [ ] QueuePanel is rendered in ChatInputContainer between FileChipBar and ChatInput\n- [ ] All queue action handlers are created and threaded through\n- [ ] Draft preservation works via `draftRef`\n- [ ] Dynamic placeholder includes queue count\n- [ ] Navigation handlers manage `startEditing`/`cancelEditing` correctly\n- [ ] All existing ChatPanel tests still pass\n- [ ] TypeScript compiles without errors",
      "activeForm": "Wiring useMessageQueue into ChatPanel and threading props through component hierarchy",
      "size": "large",
      "priority": "high",
      "dependencies": ["2.1", "2.2", "2.3", "2.4"],
      "parallelWith": []
    },
    {
      "id": "3.1",
      "phase": 3,
      "phaseName": "Shell-History Navigation & Polish",
      "subject": "[chat-input-always-editable] [P3] Implement arrow key queue navigation with cursor position gating",
      "description": "## Objective\n\nImplement shell-history-style arrow key navigation in `ChatInput.handleKeyDown` that allows users to navigate through queued messages using Up/Down arrows, gated by cursor position and palette state.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/ChatInput.tsx`\n\n## Implementation Steps\n\n### 1. Add queue navigation to handleKeyDown\n\nInsert the following block **BEFORE** the palette-open interception block and **AFTER** the Escape handling:\n\n```typescript\n// Queue navigation (takes priority over palette when queue has items and palette is closed)\nif (!isPaletteOpen && queueHasItems) {\n  if (e.key === 'ArrowUp') {\n    const textarea = textareaRef.current;\n    const isAtStart = !textarea || textarea.selectionStart === 0;\n    const isEmpty = !value.trim();\n    if (isEmpty || isAtStart) {\n      e.preventDefault();\n      onQueueNavigateUp?.();\n      return;\n    }\n  }\n  if (e.key === 'ArrowDown') {\n    const textarea = textareaRef.current;\n    const isAtEnd = !textarea || textarea.selectionStart === textarea.value.length;\n    if (editingQueueItem && isAtEnd) {\n      e.preventDefault();\n      onQueueNavigateDown?.();\n      return;\n    }\n  }\n}\n```\n\n### 2. Add Escape-to-cancel-edit\n\nInsert before the existing Escape logic at the top of `handleKeyDown`:\n\n```typescript\nif (e.key === 'Escape' && editingQueueItem) {\n  onCancelEdit?.();\n  return;\n}\n```\n\nThis ensures that Escape while editing a queue item cancels the edit and restores the user's draft, rather than triggering the normal Escape behavior (dismiss palette / clear text).\n\n### 3. Cursor position gating logic\n\n- **ArrowUp** triggers navigation when:\n  - The textarea is empty (no text), OR\n  - The cursor is at position 0 (start of text)\n  - This mirrors shell behavior: Up arrow at the start of input goes to history\n\n- **ArrowDown** triggers navigation when:\n  - The user is currently editing a queue item (`editingQueueItem` is true), AND\n  - The cursor is at the end of the text\n  - Down arrow only navigates forward when already in the queue\n\n- **ArrowUp when palette is open** does NOT trigger queue navigation (palette takes priority).\n\n### 4. Update handleKeyDown dependency array\n\nAdd the new props to the dependency array:\n```typescript\n[\n  isStreaming,\n  isMobile,\n  value,\n  onSubmit,\n  onEscape,\n  onClear,\n  isPaletteOpen,\n  onArrowUp,\n  onArrowDown,\n  onCommandSelect,\n  editingQueueItem,\n  onQueue,\n  onSaveEdit,\n  onCancelEdit,\n  queueHasItems,\n  onQueueNavigateUp,\n  onQueueNavigateDown,\n]\n```\n\n## Testing Requirements\n\nUpdate `apps/client/src/layers/features/chat/__tests__/ChatInput.test.tsx`:\n\n### New test cases\n\n1. `Up arrow navigates to queue when queue has items and textarea is empty` — render with `queueHasItems={true}`, `value=''`, `isPaletteOpen={false}`, `onQueueNavigateUp=vi.fn()`, press ArrowUp, verify `onQueueNavigateUp` called.\n\n2. `Up arrow navigates to queue when cursor is at position 0` — render with `queueHasItems={true}`, `value='hello'`, `isPaletteOpen={false}`, `onQueueNavigateUp=vi.fn()`. Set cursor to position 0 via `selectionStart=0`. Press ArrowUp. Verify `onQueueNavigateUp` called.\n\n3. `Up arrow does NOT navigate when cursor is in middle of text` — render with `queueHasItems={true}`, `value='hello world'`, press ArrowUp. The textarea's default selectionStart is at the end, so this should NOT trigger navigation unless at position 0.\n\n4. `Up arrow does NOT navigate when palette is open` — render with `queueHasItems={true}`, `isPaletteOpen={true}`, `onQueueNavigateUp=vi.fn()`, `onArrowUp=vi.fn()`, press ArrowUp. Verify `onArrowUp` (palette) called, `onQueueNavigateUp` NOT called.\n\n5. `Up arrow does NOT navigate when queue is empty` — render with `queueHasItems={false}`, `value=''`, `onQueueNavigateUp=vi.fn()`, press ArrowUp. Verify `onQueueNavigateUp` NOT called.\n\n6. `Down arrow navigates forward when editing queue item and at end of text` — render with `queueHasItems={true}`, `editingQueueItem={true}`, `value='text'`, `onQueueNavigateDown=vi.fn()`, press ArrowDown. Verify `onQueueNavigateDown` called.\n\n7. `Down arrow does NOT navigate when not editing queue item` — render with `queueHasItems={true}`, `editingQueueItem={false}`, `onQueueNavigateDown=vi.fn()`, press ArrowDown. Verify `onQueueNavigateDown` NOT called.\n\n8. `Escape cancels edit when editingQueueItem is true` — render with `editingQueueItem={true}`, `onCancelEdit=vi.fn()`, `onEscape=vi.fn()`, press Escape. Verify `onCancelEdit` called and `onEscape` NOT called.\n\n9. `Escape does NOT cancel edit when not editing` — render with `editingQueueItem={false}`, `onCancelEdit=vi.fn()`, `onEscape=vi.fn()`, press Escape. Verify `onEscape` called and `onCancelEdit` NOT called.\n\n## Acceptance Criteria\n\n- [ ] ArrowUp navigates to queue when empty or cursor at start\n- [ ] ArrowUp does NOT navigate when palette is open\n- [ ] ArrowUp does NOT navigate when queue is empty\n- [ ] ArrowDown navigates forward only when editing a queue item and cursor at end\n- [ ] Escape cancels edit when editing a queue item\n- [ ] Normal Escape behavior preserved when not editing\n- [ ] All 9 new tests written and passing\n- [ ] Existing arrow key tests still pass",
      "activeForm": "Implementing arrow key queue navigation with cursor gating",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["2.4"],
      "parallelWith": ["3.2"]
    },
    {
      "id": "3.2",
      "phase": 3,
      "phaseName": "Shell-History Navigation & Polish",
      "subject": "[chat-input-always-editable] [P3] Mobile polish and barrel export updates",
      "description": "## Objective\n\nEnsure the queue UX works correctly on mobile (tap-to-edit, always-visible remove buttons), and update barrel exports.\n\n## Files to Modify\n\n- `apps/client/src/layers/features/chat/ui/QueuePanel.tsx`\n- `apps/client/src/layers/features/chat/index.ts`\n\n## Implementation Steps\n\n### 1. Mobile remove button visibility in QueuePanel\n\nThe current implementation uses `opacity-0 group-hover:opacity-100` for the remove button, which is invisible on touch devices. Update to use responsive classes:\n\n```tsx\n<span\n  role=\"button\"\n  tabIndex={0}\n  onClick={(e) => {\n    e.stopPropagation();\n    onRemove(i);\n  }}\n  onKeyDown={(e) => {\n    if (e.key === 'Enter' || e.key === ' ') {\n      e.stopPropagation();\n      e.preventDefault();\n      onRemove(i);\n    }\n  }}\n  className=\"text-muted-foreground hover:text-foreground shrink-0 rounded-sm p-0.5 opacity-100 transition-opacity md:opacity-0 md:group-hover:opacity-100\"\n  aria-label={`Remove queued message ${i + 1}`}\n>\n  <X className=\"size-3\" />\n</span>\n```\n\nOn mobile (`< md` breakpoint), the remove button is always visible (`opacity-100`). On desktop (`md:` and above), it's hidden until hover.\n\n### 2. Update barrel exports in `features/chat/index.ts`\n\nAdd `useMessageQueue` and `QueueItem` type to the barrel export:\n\n```typescript\n/**\n * Chat feature — message streaming UI, tool call cards, and session interaction hooks.\n *\n * @module features/chat\n */\nexport { ChatPanel } from './ui/ChatPanel';\nexport { useChatSession } from './model/use-chat-session';\nexport { useCelebrations } from './model/use-celebrations';\nexport { useTaskState } from './model/use-task-state';\nexport { useMessageQueue } from './model/use-message-queue';\nexport type { QueueItem } from './model/use-message-queue';\n```\n\nNote: `QueuePanel` is NOT exported from the barrel — it's an internal UI component used only by `ChatInputContainer`.\n\n### 3. Verify mobile Enter key behavior\n\nMobile Enter key inserts a newline (existing behavior via `!isMobile` guard). Queue submission on mobile happens via the Queue button tap. Verify this continues to work by checking the Enter key handler:\n\n```typescript\nif (e.key === 'Enter' && !e.shiftKey && !isMobile) {\n```\n\nThe `!isMobile` guard ensures mobile users must tap the button to queue.\n\n## Testing Requirements\n\nNo new test file needed. Update existing tests:\n\n1. In `QueuePanel.test.tsx`, add test: `remove button is always visible (no hover-gated opacity)` — render a QueuePanel item, verify the remove button does not have `opacity-0` class (since in jsdom, `matchMedia` is mocked and there's no breakpoint concept, the base `opacity-100` class should apply).\n\n## Acceptance Criteria\n\n- [ ] Remove button visible on mobile without hover\n- [ ] Remove button hover-gated on desktop\n- [ ] Barrel exports include `useMessageQueue` and `QueueItem`\n- [ ] `QueuePanel` is NOT exported from barrel\n- [ ] Mobile Enter key continues to insert newline (not queue)\n- [ ] Queue button tap works on mobile for queueing",
      "activeForm": "Polishing mobile experience and updating barrel exports",
      "size": "small",
      "priority": "medium",
      "dependencies": ["2.5"],
      "parallelWith": ["3.1"]
    },
    {
      "id": "3.3",
      "phase": 3,
      "phaseName": "Shell-History Navigation & Polish",
      "subject": "[chat-input-always-editable] [P3] Write integration tests for full queue workflow",
      "description": "## Objective\n\nWrite comprehensive integration tests that verify the end-to-end queue workflow: typing during streaming, queueing messages, auto-flush with timing annotations, arrow key navigation with draft preservation, and session change cleanup.\n\n## Files to Create/Modify\n\n- `apps/client/src/layers/features/chat/__tests__/ChatPanel.test.tsx` (update with new tests)\n\n## Test Strategy\n\nThe existing `ChatPanel.test.tsx` heavily mocks child components (`ChatInput`, `MessageList`, etc.). For integration tests of the queue workflow, we need to either:\n\n**Option A (Recommended):** Test at the `ChatInputContainer` level with real `ChatInput` and `QueuePanel` components but mocked hooks.\n\n**Option B:** Add tests to the existing ChatPanel test file that verify prop threading via mock component call args.\n\nGo with Option B to stay consistent with the existing test patterns in this file.\n\n### Test Implementation\n\nThe existing ChatPanel test file mocks `ChatInput` as `vi.fn(() => <div>ChatInput</div>)`. We can inspect the mock calls to verify props are passed correctly.\n\nAdd a new describe block:\n\n```typescript\ndescribe('ChatPanel queue integration', () => {\n  // We need to also mock useMessageQueue or test with the real hook.\n  // Since useChatSession is already mocked, test that ChatInputContainer\n  // receives the queue-related props.\n\n  it('passes queue props to ChatInputContainer', () => {\n    // Verify that when useMessageQueue has items,\n    // ChatInput receives queueDepth, editingQueueItem, etc.\n  });\n\n  it('queue clears when session changes', () => {\n    // Render with sessionId='a', add items, rerender with sessionId='b',\n    // verify queue is empty.\n  });\n});\n```\n\nAlternatively, create a focused integration test file:\n\n### New file: `apps/client/src/layers/features/chat/__tests__/queue-integration.test.tsx`\n\nThis file tests the `useMessageQueue` hook integrated with mock `useChatSession` status transitions:\n\n```typescript\n// @vitest-environment jsdom\nimport { describe, it, expect, vi, beforeEach } from 'vitest';\nimport { renderHook, act } from '@testing-library/react';\nimport { useMessageQueue } from '../model/use-message-queue';\n\ndescribe('Queue workflow integration', () => {\n  const onFlush = vi.fn();\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n  });\n\n  it('full workflow: queue during streaming, auto-flush on idle', () => {\n    const { result, rerender } = renderHook(\n      ({ status }) => useMessageQueue({\n        status,\n        sessionBusy: false,\n        sessionId: 'test',\n        selectedCwd: '/dir',\n        onFlush,\n      }),\n      { initialProps: { status: 'streaming' as const } }\n    );\n\n    // Queue two messages while streaming\n    act(() => { result.current.addToQueue('First followup'); });\n    act(() => { result.current.addToQueue('Second followup'); });\n    expect(result.current.queue).toHaveLength(2);\n\n    // Transition to idle — first message should auto-flush\n    rerender({ status: 'idle' as const });\n    expect(onFlush).toHaveBeenCalledTimes(1);\n    expect(onFlush).toHaveBeenCalledWith(\n      expect.stringContaining('[Note: This message was composed while the agent was responding to the previous message]')\n    );\n    expect(onFlush).toHaveBeenCalledWith(\n      expect.stringContaining('First followup')\n    );\n    expect(result.current.queue).toHaveLength(1);\n    expect(result.current.queue[0].content).toBe('Second followup');\n  });\n\n  it('auto-flush includes timing annotation prefix', () => {\n    const { result, rerender } = renderHook(\n      ({ status }) => useMessageQueue({\n        status,\n        sessionBusy: false,\n        sessionId: 'test',\n        selectedCwd: '/dir',\n        onFlush,\n      }),\n      { initialProps: { status: 'streaming' as const } }\n    );\n\n    act(() => { result.current.addToQueue('My message'); });\n    rerender({ status: 'idle' as const });\n\n    const flushedContent = onFlush.mock.calls[0][0];\n    expect(flushedContent).toBe(\n      '[Note: This message was composed while the agent was responding to the previous message]\\n\\nMy message'\n    );\n  });\n\n  it('arrow key navigation cycles through queue items with draft preservation', () => {\n    const { result } = renderHook(() => useMessageQueue({\n      status: 'streaming',\n      sessionBusy: false,\n      sessionId: 'test',\n      selectedCwd: '/dir',\n      onFlush,\n    }));\n\n    act(() => { result.current.addToQueue('First'); });\n    act(() => { result.current.addToQueue('Second'); });\n\n    // Navigate up to last item\n    let content: string;\n    act(() => { content = result.current.startEditing(1); });\n    expect(content!).toBe('Second');\n    expect(result.current.editingIndex).toBe(1);\n\n    // Navigate up to first item\n    act(() => { content = result.current.startEditing(0); });\n    expect(content!).toBe('First');\n    expect(result.current.editingIndex).toBe(0);\n\n    // Cancel editing (return to draft)\n    act(() => { result.current.cancelEditing(); });\n    expect(result.current.editingIndex).toBeNull();\n  });\n\n  it('editing queue item and saving preserves changes', () => {\n    const { result } = renderHook(() => useMessageQueue({\n      status: 'streaming',\n      sessionBusy: false,\n      sessionId: 'test',\n      selectedCwd: '/dir',\n      onFlush,\n    }));\n\n    act(() => { result.current.addToQueue('Original content'); });\n    act(() => { result.current.startEditing(0); });\n    act(() => { result.current.saveEditing('Modified content'); });\n\n    expect(result.current.queue[0].content).toBe('Modified content');\n    expect(result.current.editingIndex).toBeNull();\n  });\n\n  it('queue clears when session changes', () => {\n    const { result, rerender } = renderHook(\n      ({ sessionId }) => useMessageQueue({\n        status: 'streaming',\n        sessionBusy: false,\n        sessionId,\n        selectedCwd: '/dir',\n        onFlush,\n      }),\n      { initialProps: { sessionId: 'session-a' } }\n    );\n\n    act(() => { result.current.addToQueue('Queued msg'); });\n    expect(result.current.queue).toHaveLength(1);\n\n    rerender({ sessionId: 'session-b' });\n    expect(result.current.queue).toHaveLength(0);\n    expect(result.current.editingIndex).toBeNull();\n  });\n\n  it('auto-flush skips item being edited and flushes next', () => {\n    const { result, rerender } = renderHook(\n      ({ status }) => useMessageQueue({\n        status,\n        sessionBusy: false,\n        sessionId: 'test',\n        selectedCwd: '/dir',\n        onFlush,\n      }),\n      { initialProps: { status: 'streaming' as const } }\n    );\n\n    act(() => { result.current.addToQueue('Being edited'); });\n    act(() => { result.current.addToQueue('Should flush'); });\n    act(() => { result.current.startEditing(0); });\n\n    rerender({ status: 'idle' as const });\n    expect(onFlush).toHaveBeenCalledWith(\n      expect.stringContaining('Should flush')\n    );\n    expect(result.current.queue).toHaveLength(1);\n    expect(result.current.queue[0].content).toBe('Being edited');\n  });\n\n  it('multiple rapid idle transitions do not double-flush', () => {\n    const { result, rerender } = renderHook(\n      ({ status }) => useMessageQueue({\n        status,\n        sessionBusy: false,\n        sessionId: 'test',\n        selectedCwd: '/dir',\n        onFlush,\n      }),\n      { initialProps: { status: 'streaming' as const } }\n    );\n\n    act(() => { result.current.addToQueue('Only once'); });\n    rerender({ status: 'idle' as const });\n    rerender({ status: 'idle' as const });\n\n    // Should only flush once because prevStatusRef tracks the last status\n    expect(onFlush).toHaveBeenCalledTimes(1);\n  });\n});\n```\n\n## Acceptance Criteria\n\n- [ ] Full queue workflow test passes (queue during streaming, auto-flush on idle)\n- [ ] Timing annotation format verified in tests\n- [ ] Arrow key navigation with draft preservation tested\n- [ ] Edit and save workflow tested\n- [ ] Session change cleanup tested\n- [ ] Auto-flush skip-editing behavior tested\n- [ ] Double-flush prevention tested\n- [ ] All 7 integration tests written and passing",
      "activeForm": "Writing integration tests for the full queue workflow",
      "size": "medium",
      "priority": "medium",
      "dependencies": ["2.5"],
      "parallelWith": ["3.1", "3.2"]
    }
  ]
}
