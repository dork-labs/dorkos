### Changed

- Rewrite the main README in plain language so a newcomer can tell what DorkOS does before installing it. The contributor-only architecture details now live behind a short "Want to hack on DorkOS?" pointer.
- Rewrite the Mesh concepts page in plain language: it now leads with what you get, and the developer-only tables (detection markers, manifest fields, access-rule priority) sit in a clearly labeled reference section.
