### Added

- Browse, read, and edit workspace files from the cockpit: new file-service endpoints power a gitignore-aware file tree, text-file reads with change detection, and create/rename/delete for files and folders — all confined to the session's working directory (DOR-217)
- Stream 3D model files (glTF, GLB, STL, OBJ) into the workbench viewers
