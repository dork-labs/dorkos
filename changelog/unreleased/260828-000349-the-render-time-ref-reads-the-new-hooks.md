---
covers:
  - 'refactor(client): the render-time ref reads the new hooks lint found — chat, sidebar, mesh (DOR-1558)'
---

### Changed

- The render-time ref reads the new hooks lint found — chat, sidebar, mesh (DOR-1558)
