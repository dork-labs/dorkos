### Added

- Run a shell right inside the cockpit — the workbench right panel now has a Terminal tab that opens a live terminal in your session's working directory, so you can check output or run a command without leaving DorkOS (web only)
