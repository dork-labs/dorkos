### Added

- Started building a capability registry: one place where DorkOS declares what agents can do, so its tools, commands, and a live catalog can all come from the same source. This first piece is internal groundwork, so there is no visible change yet.
