### Added

- Changed images now get a real comparison, GitHub-style: see before and after side by side (2-up), drag a divider across them (Swipe), or blend between them with a slider (Onion skin). Restore the previous image with one confirmed click, or mark the new one reviewed. A brand-new image says so honestly instead of pretending there's something to compare. Web-only, like the image viewer (DOR-212)
