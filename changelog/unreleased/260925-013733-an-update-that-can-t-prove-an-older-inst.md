---
covers:
  - "feat(server): an update that can't prove an older install's files keeps each one, names it and remembers it (DOR-2322)"
---

### Added

<!-- dorkos-changelog:seeded — rewrite this bullet for a human, then delete this comment. If the change needs no changelog entry, delete the whole fragment instead. See changelog/README.md#seeded-fragments. -->

- An update that can't prove an older install's files keeps each one, names it and remembers it (DOR-2322)
