### Fixed

- Agents can now switch which agent you're viewing. When an agent runs the "switch agent" command, the cockpit jumps to that agent's folder and chat — before, the request quietly did nothing (DOR-354)
