### Added

- Relay metrics now include real delivery-latency percentiles (p50/p95/p99) instead of a placeholder, and the task scheduler can now compute p95 run duration and success rate per schedule (DOR-166)
