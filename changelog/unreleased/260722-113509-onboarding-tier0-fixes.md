### Added

- Start a chat from the dashboard: the dashboard header now has a "New conversation" button that opens DorkBot, and the sidebar "Getting started" card leads with a "Talk to DorkBot" row (DOR-416).

### Fixed

- The setup finish screen now waits for you. It used to close on its own a second or two after appearing, dropping you on the dashboard before you could click "Start your first session" (DOR-416).
- A brand-new agent no longer looks broken. A DorkBot you just set up shows as "New" instead of "Stale" or "Never active", and it stays visible in the sidebar instead of hiding under "inactive agents" (DOR-416).
- The setup confetti now stops after a few seconds and clears when you move on, instead of drifting across the screen long after setup (DOR-416).
