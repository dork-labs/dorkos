---
covers:
  - 'fix(client): pressing Enter twice while a message is being queued lines it up once (DOR-1133 review)'
---

### Fixed

- Pressing Enter twice while a message was being added to the line no longer lines it up
  twice, so the agent does not say the same thing over again (DOR-1133)
