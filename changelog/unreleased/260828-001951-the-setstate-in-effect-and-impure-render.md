---
covers:
  - 'refactor(client): the setState-in-effect and impure-render reads the new hooks lint found (DOR-1558)'
---

### Changed

- The setState-in-effect and impure-render reads the new hooks lint found (DOR-1558)
