### Fixed

- The built-in Linear extension now works in the desktop app. Its dashboard and sidebar were calling the wrong address for their data, the same issue already fixed for extensions in general (DOR-243), so Loop status silently failed to load (DOR-255)
