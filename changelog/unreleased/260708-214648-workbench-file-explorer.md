### Added

- File explorer in the workbench — browse the session's working directory as a lazy-expanding tree with a worktree/branch badge, create/rename/delete/drag-to-move files and folders, and keyboard navigation. Click a file to open it in the canvas viewer; deleting a non-empty folder asks first (DOR-214).
