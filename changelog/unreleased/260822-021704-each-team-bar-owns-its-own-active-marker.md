---
covers:
  - "fix(client): each team bar owns its own active marker, and the phone's + is a real target"
---

### Fixed

- Each team bar owns its own active marker, and the phone's + is a real target
