---
covers:
  - "fix(client): every page's header names the page, not the dashboard (DOR-919)"
---

### Fixed

- Workspaces, Connections, and Feedback & requests now show their own name at the top of the page instead of "Dashboard" (DOR-919)
