---
covers:
  - 'fix(server): renameSession test stops re-transforming the runtime graph inside its own timeout (DOR-1139)'
---

### Fixed

- RenameSession test stops re-transforming the runtime graph inside its own timeout (DOR-1139)
