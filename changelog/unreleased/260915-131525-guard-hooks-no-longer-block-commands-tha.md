---
covers:
  - 'fix(hooks): guard hooks no longer block commands that only mention a blocked command inside single quotes or a quoted heredoc'
---

### Fixed

<!-- dorkos-changelog:seeded — rewrite this bullet for a human, then delete this comment. If the change needs no changelog entry, delete the whole fragment instead. See changelog/README.md#seeded-fragments. -->

- Guard hooks no longer block commands that only mention a blocked command inside single quotes or a quoted heredoc
