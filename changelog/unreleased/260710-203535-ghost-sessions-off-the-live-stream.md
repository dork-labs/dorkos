### Fixed

- Sessions that don't belong to any project no longer get announced to every open cockpit over the live update stream. Before, a Codex or OpenCode session with no working directory could show up as a nameless ghost row under agents it had nothing to do with (DOR-202)
