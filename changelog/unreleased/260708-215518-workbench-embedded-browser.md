### Added

- Embedded browser in the workbench canvas — open a URL, a local HTML file, or a localhost dev server with back/forward/reload and an address bar. Local files and dev servers preview inline; sites that refuse embedding offer an "open in system browser" escape hatch (DOR-216, closes DOR-98).
