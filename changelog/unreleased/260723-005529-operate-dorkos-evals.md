### Added

- DorkOS now tests that agents can really run it: our eval harness has new checks that an agent can create and change agents, read the activity feed, change your settings, and install a package just by being asked — each one proving the real change happened, not just that the agent said the right words (DOR-435)
