---
covers:
  - 'fix(e2e): the manifest points at the rows that exist, and keeps its own history (DOR-1328)'
---

### Fixed

- The manifest points at the rows that exist, and keeps its own history (DOR-1328)
