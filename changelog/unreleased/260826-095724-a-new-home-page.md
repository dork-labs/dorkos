---
covers:
  - "fix(site): the home page's shared card says which site it is from"
  - 'refactor(site): the clips-rail barrel stops offering a type nobody asks it for'
  - 'feat(site): every beat of the stage reaches the page, not only the one you scrolled to'
  - 'test(site): four bounds that were knowable become the numbers they knew'
  - 'chore(site): the home page stops carrying a dev-only console shim and four dead exports'
  - 'fix(site): the product loops keep their shape under reduced motion, and wait to be seen'
  - 'fix(site): the faces do not change shape when you ask for less motion'
  - 'feat(site): the film-first page becomes the home page (DOR-1562)'
  - 'feat(site): the stage tells you where you are while it plays'
  - 'feat(site): the stage ends in the MacBook, and only in the MacBook'
  - "fix(site): the deck's hinge corners get the small ease the real machine has"
  - 'feat(site): the stage can end in a drawn MacBook, and you can compare it'
  - 'feat(site): the laptop that forms around the chat is a modern one'
  - 'feat(site): the clips rail ends on a test card, not a photograph'
  - 'fix(site): three things only a browser could tell me about the signup panel'
  - 'feat(site): a tile with no clip behind it stops swallowing the press'
  - 'feat(site): the clips rail ends on a shelf of blank tapes'
  - 'feat(site): /new ends where every other page ends, mailing list and all'
  - "fix(site): /new's pill survives its own steering, and the shelf runs off the screen"
  - 'feat(site): /new gets a shelf of clips, the catalogue, and a pill that steers itself'
  - 'feat(site): /new leads with the film, then proves it'
  - "refactor(site): the film's scale factor earns its keep"
  - "feat(site): /new gets the film's cast, its chat, and the site's own palette"
  - 'refactor(site): the published home page stays; the experiment moves to /new'
  - 'fix(site): the home page only shows what actually ships'
  - 'refactor(site): the home page barrel is a door, not a manifest'
  - 'feat(site): the home page plays the promo film'
  - 'fix(site): the home page copy follows the site voice canon'
  - 'refactor(site): the scroll experience becomes the home page'
---

### Changed

- dorkos.ai has a new home page. It opens with the short film about Dave, who
  gets three agents and stops doing everything himself, and then shows you the
  same thing working for real: scroll, and one conversation plays out in front
  of you, apps land in it as they are used, and the whole thing settles into a
  laptop at the end. The old page told you what DorkOS was. This one shows you
  (DOR-1562)
- Underneath the film there is a shelf for short walkthroughs. The only thing on
  it today is the film itself, cut to fit a phone. The rest are empty frames,
  and the page says so on each one rather than pretending otherwise. You can
  leave your email on any tile and hear when the walkthroughs are made
  (DOR-1562)
- The story page's footer now links to npm as well as GitHub, so it ends the way
  every other page on the site does (DOR-1562)
- The list of features and the answers to common questions are still there, in
  the same words, further down the page where someone who has decided to check
  goes looking for them (DOR-1562)
- The menu at the bottom of the home page now moves you around the page instead
  of pointing at other pages, and folds the rest of the site behind one button.
  Every other page keeps the menu it had (DOR-1562)

### Removed

- The bar across the top of the home page is gone. The floating menu at the
  bottom does that job now, and it moves you around the page. Every other page
  keeps its top bar (DOR-1562)
- The old home page is gone, and so is what only it carried: the scrolling
  activity feed at the top, the cards about what goes wrong when you run agents
  by hand, the history of how DorkOS was built, and the closing note with the
  contact address in it. The address is still in the footer of every page
  (DOR-1562)
