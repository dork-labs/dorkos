### Added

- Eval cadence workflow for CI: the structural eval suite now runs on a pull request when someone adds the `run-evals` label, and nightly on a schedule. A separate manual run can exercise the credentialed cases against a real model and upload the results for a person to review. Contributors only (DOR-450)
