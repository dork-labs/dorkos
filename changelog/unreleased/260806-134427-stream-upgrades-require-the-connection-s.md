---
covers:
  - "fix(server): stream upgrades require the connection's own scheme when no proxy declares one (DOR-932)"
---

### Fixed

- Stream upgrades require the connection's own scheme when no proxy declares one (DOR-932)
