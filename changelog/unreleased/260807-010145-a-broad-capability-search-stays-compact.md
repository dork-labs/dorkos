---
covers:
  - 'fix(server): a broad capability search stays compact instead of dumping every schema (DOR-988)'
---

### Fixed

- A broad capability search stays compact instead of dumping every schema (DOR-988)
