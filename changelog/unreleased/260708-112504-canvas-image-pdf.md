### Added

- Show images and PDFs in the agent canvas — agents can open a canvas of an image (with click-to-zoom) or a PDF (native browser viewer), sourced from an https URL, a data URI, or a local file in the session's working directory
