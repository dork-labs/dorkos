### Added

- **Feedback reports now have a permanent home.** The site can receive an
  in-app feedback report, keep a durable record of it, and — once a Linear
  key is configured — automatically open a tracked issue for it. This is the
  receiving side of a larger feedback pipeline; sending a report from the app
  itself isn't wired up to this yet.
