---
covers:
  - "fix(client): the sidebar's motion is pinned by tests that can fail, and costs one boot subscription (DOR-1374)"
---

### Fixed

- The sidebar's motion is pinned by tests that can fail, and costs one boot subscription (DOR-1374)
