---
covers:
  - 'fix(server): a refused dispatch opens no window, so the message stays queued (P3.3 review, DOR-1168)'
---

### Fixed

- A refused dispatch opens no window, so the message stays queued (P3.3 review, DOR-1168)
