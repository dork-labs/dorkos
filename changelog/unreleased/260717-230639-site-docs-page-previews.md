### Added

- Every docs page now has its own link-preview image. Share a docs link in Slack, Discord, or on X and it shows a card with the page's section and title instead of one generic sitewide image (DOR-346)
- Docs pages now describe themselves to search engines and AI answer tools: each page says what it is (a documentation article) and where it sits in the docs, which helps it show up as a source (DOR-346)

### Fixed

- Docs pages now point search engines at a single canonical address and set their own preview title and description, instead of falling back to the sitewide default (DOR-346)
