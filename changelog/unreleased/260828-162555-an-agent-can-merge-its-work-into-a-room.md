---
covers:
  - "feat(server,shared): an agent can merge its work into a room's files (DOR-1598)"
  - "fix(server,shared): close the merge validation's three bypasses (DOR-1598)"
---

### Added

- An agent can merge its work into a room's files (DOR-1598)
