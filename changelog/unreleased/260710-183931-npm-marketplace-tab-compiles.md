### Fixed

- The Marketplace tab now works when you install DorkOS from npm. Built-in extensions that run their own code (like Marketplace) used to fail to load on a plain install; they now start up correctly (DOR-256)
