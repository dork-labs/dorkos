---
covers:
  - 'fix(client): finish the desktop polish backlog — dim on blur, overscroll, selectable toasts'
  - 'fix(desktop,client): dim on blur from OS focus, not DOM focus (review)'
---

### Fixed

- The window's header and tabs now dim slightly when it loses focus, like other native Mac apps
- Scrolling to the end of the chat or a sidebar list no longer bounces past it on a trackpad
- Toast and error messages can now be selected and copied
