### Fixed

- Generative UI widgets now render even when an agent writes a value slightly off-spec — a stack whose spacing is given as a pixel number (`gap: 16`) or a progress bar reported over 100% renders correctly instead of collapsing to an error card.
