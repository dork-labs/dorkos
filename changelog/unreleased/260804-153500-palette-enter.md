---
covers:
  - 'fix(client): Enter in the palette opens the row you see, even when a message just moved it (DOR-699)'
---

### Fixed

- When a new message reorders the command palette while it is open, the highlight now
  stays on the top row until you move it yourself. It used to ride the old row down the
  list, so Enter opened something other than the row now sitting on top (DOR-699)
