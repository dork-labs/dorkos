### Fixed

- Codex and OpenCode conversations now keep their history when the DorkOS server restarts. Before, the session showed up in your list after a restart, but opening it gave you a blank transcript — every completed reply is now saved to disk the moment its turn finishes, so the full conversation is right where you left it.
