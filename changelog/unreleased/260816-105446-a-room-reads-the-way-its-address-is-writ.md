---
covers:
  - 'fix(client): a room reads the way its address is written, and a page needs a row (DOR-1252)'
---

### Fixed

- A room reads the way its address is written, and a page needs a row (DOR-1252)
