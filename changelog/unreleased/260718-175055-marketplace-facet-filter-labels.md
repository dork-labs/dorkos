### Fixed

- Marketplace browse now labels its two filter rows — "Type" and "Category" — so they no longer read as two identical rows that both start with "All" (#334)
- The category filter drops its redundant "All" chip; the selected category now shows a small ✕ you click to clear it (#334)
