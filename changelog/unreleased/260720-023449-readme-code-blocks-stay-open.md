### Fixed

- Opening a Marketplace package whose README has code blocks no longer risks blanking the whole page. If the code viewer ever fails to load, the package stays open and the README shows a short note in its place. Chat messages get the same safety net, so a message with code can never take down the transcript.
