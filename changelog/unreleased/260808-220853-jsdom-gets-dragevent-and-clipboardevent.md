---
covers:
  - 'fix(client): jsdom gets DragEvent and ClipboardEvent, so a paste cannot crash the run (DOR-948)'
---

### Fixed

- Jsdom gets DragEvent and ClipboardEvent, so a paste cannot crash the run (DOR-948)
