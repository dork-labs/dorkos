### Fixed

- The file explorer keeps your place more reliably: if a delete fails the file stays selected and its folder stays open, your scroll position is fully restored after a page refresh, and switching on hidden files no longer flashes the tree empty for a moment.
