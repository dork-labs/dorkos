---
covers:
  - 'fix(server): something waiting is still shown after you reload the page'
  - 'fix(server): bound the connect snapshot, and scope what the SSE doc promises'
---

### Fixed

- Something waiting is still shown after you reload the page. A chat that stopped with an error, or one asking for your go-ahead, used to vanish from the sidebar's Now the moment you refreshed.
