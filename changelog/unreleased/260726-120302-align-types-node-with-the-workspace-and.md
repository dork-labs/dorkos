---
covers:
  - "fix(scripts): align @types/node with the workspace and cover scripts in verify (DOR-500)"
---

### Fixed

- Align @types/node with the workspace and cover scripts in verify (DOR-500)
