### Fixed

- Widget line and area charts no longer show gaps in the line on certain slopes — the draw-in animation now reveals the chart cleanly left to right.
- The widget loading shimmer is a soft light sweep instead of a dark moving band in light mode.

### Changed

- Generative UI widgets have their own section in the dev playground (`/dev/gen-ui`), including a new loading-state showcase, instead of crowding the Components page.
