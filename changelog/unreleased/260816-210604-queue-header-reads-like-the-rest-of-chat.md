---
covers:
  - 'fix(client): queue header separator follows the house style (DOR-1246)'
---

### Changed

- The queued-messages header now uses a dot instead of a long dash, matching the rest of chat (DOR-1246)
