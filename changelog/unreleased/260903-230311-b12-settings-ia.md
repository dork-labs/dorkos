---
covers:
  - 'fix(client,server,cli): address batch 12 review findings for the settings reshuffle (DOR-1758)'
---

### Fixed

- Turning on remote access and closing Settings no longer goes silent — you get the "reconnecting" and "turned off" notices again even while Settings is closed (DOR-1758)
- Messages that used to point you to "Settings → Security" now say "Settings → Access", matching the tab that's actually there (DOR-1758)
- "Show dev tools" now lives on the Experiments tab, where it works even if the server is slow to answer or restarting (DOR-1758)
- The two stacked cards on the Server tab line up their arrows now, instead of one sitting further in than the other (DOR-1758)
