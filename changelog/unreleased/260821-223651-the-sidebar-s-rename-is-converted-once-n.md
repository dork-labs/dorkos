---
covers:
  - "refactor(server,shared,client): the sidebar's rename is converted once, not on every read (DOR-588)"
---

### Changed

- The sidebar's rename is converted once, not on every read (DOR-588)
