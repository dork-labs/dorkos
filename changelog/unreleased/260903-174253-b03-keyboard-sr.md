---
covers:
  - 'fix(client): adversarial-review fixes for UI audit batch 03 (DOR-1749)'
---

### Fixed

- The right panel's selected tab (Pulse, Canvas, and the rest) is visibly highlighted again — a recent fix for its keyboard focus ring had accidentally made every tab look unselected.
- Closing the "open this link?" dialog now returns your keyboard focus to the link you opened it from, instead of dropping it to the top of the page.
- The app no longer logs a console warning about the right panel's tabs every time it loads.
- The sort control in list filter bars (Tasks, Team, Activity) is bigger and easier to tap.
