### Added

- You can now drag the desktop app window from the top of the sidebar and the header — the sidebar no longer hides behind the Mac window buttons, whether it's expanded or collapsed (DOR-253)
- Links you click in the desktop app open in your regular browser instead of popping up a broken, chrome-less extra window (DOR-253)

### Changed

- In the desktop app, most of the interface now reads like an app instead of a document — text in the sidebar and navigation is no longer selectable. Chat messages, code blocks, and other content you'd actually want to copy still are (DOR-253)
