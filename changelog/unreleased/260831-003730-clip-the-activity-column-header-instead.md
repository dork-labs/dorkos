---
covers:
  - 'fix(client): clip the Activity column header instead of letting it bleed (DOR-1287)'
---

### Fixed

- Fixed the team table's column headers overlapping into garbled text (like
  "Manaigedy by") when the table was narrowed, such as by the Profile panel
  being open (DOR-1287)
