---
covers:
  - "fix(client): thread allowsDenyReason to the transcript's own approval cards (DOR-825 review)"
---

### Fixed

- A parked or batched approval shown directly in the conversation no longer offers a deny-reason field on runtimes that cannot deliver it — the same fix DOR-825 shipped, now covering that card too
