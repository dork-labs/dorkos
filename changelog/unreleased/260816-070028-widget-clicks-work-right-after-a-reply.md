---
covers:
  - 'fix(server): a widget click right after the agent finishes is not a busy session (DOR-1239)'
---

### Fixed

- Clicking a button in an agent's reply right after it finishes now works, instead of saying the session is locked (DOR-1239)
