---
covers:
  - 'fix(server,client): an agent-proposed schedule announces itself — SSE + activity on create, live task list (DOR-1380)'
---

### Fixed

- When an agent proposes a scheduled task, the Scheduled list now updates immediately. You no
  longer have to reload to see it waiting for your approval. It also shows up in Activity, so
  there's a record that it happened (DOR-1380).

### Changed

- The "Feedback & requests" entry in the help menu, and its page, are now called "Product
  feedback" (DOR-1380).
