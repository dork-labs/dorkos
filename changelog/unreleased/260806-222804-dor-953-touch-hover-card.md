---
covers:
  - 'fix(client): open identity hover cards on touch (DOR-953)'
---

### Fixed

- Tapping and holding a person's or agent's name (like an `@mention` in a room) now opens their
  identity card on phones and tablets, the same card you already got by hovering with a mouse.
  A quick tap still does nothing, so it won't be mistaken for a button (DOR-953)
